---
title: "실습으로 배우는 의료 데이터"
subtitle: "현실 데이터의 복잡함을 길들이기"
author: "앤드류 응우엔(Andrew Nguyen)"
---

# 제1장 의료 데이터 소개

의료 데이터는 데이터 과학 분야에서 흥미로운 분야이며 임상적이든 기술적이든 실질적인 영향을 미칠 수 있는 기회가 많다. 환자와 의료진에게는 환자에게 적절한 치료를 적절한 시기에 유전체, 환경, 신념, 라이프스타일에 맞춰 제공하는 진정한 개인화 의료라는 매력적인 약속이 있다. 이 각각에는 고도로 복잡한 데이터의 효과적인 통합, 조율, 분석이 필요하다. 데이터 과학자와 컴퓨터 과학자에게는 자연어 처리, 그래프, 시맨틱 웹, 데이터베이스 등에서 수많은 미해결 문제가 있다. 또한 특정 기술과 의료의 미묘함 및 복잡성의 조합에서 발생하는 '최첨단' 문제도 있다. 예를 들어, 의료 데이터 자체나 데이터 과학에 '규제 등급'의 재현성이 요구되는 것은 없다. 데이터 과학자들은 Git과 같은 버전 관리 도구 사용법을 알고 IT 전문가는 데이터베이스 스냅샷을 만들고 Docker 컨테이너를 사용하는 법을 알고 있다. 그러나 미국 식품의약국(FDA)이나 유럽의약품청(EMA)과 같은 규제 기관이 있다면, 분석 결과를 '증명'하기 위해 메타데이터와 기타 산출물을 추적하고 저장하는 특정 요구 사항이 있다. 비슷하게, 학계에서 연구의 재현성이나 부정적 결과의 공유를 보장해야 한다는 요구와 압력이 커지고 있다. 이러한 도전을 대규모로 어떻게 해결할 수 있는지는 아직 미해결 문제이다.

다양한 유형의 의료 데이터를 다루는 것에 대한 흥분에도 불구하고 여전히 많은 오해가 있다. 기업 환경에서 풍부한 경험을 가진 사람들은 종종 복잡성을 과소평가하며 현실 의료 데이터 프로젝트를 기업 통합과 비교한다. 일반적인 기업 데이터 프로젝트가 단순하거나 쉽다는 말은 아니다. 주요 차이점 중 하나는 기업에서의 호스팅과 의료에서의 호스팅 관계이다. 거의 모든 산업에서 오늘날 데이터 사용은 설계된 시스템의 함수이다. 즉, 대부분의 데이터는 소프트웨어 시스템에 의해 생성되며 사람이 수집하고 입력하는 것이 아니다. 예를 들어 광고/마케팅 분석에서는 클릭과 노출을 추적하는 웹사이트에 의해 데이터가 생성된다. 이 장에서는 의료 데이터의 미묘함과 복잡성을 살펴볼 것이다. 이 복잡성의 상당 부분은 의료 서비스 자체의 전달을 반영한다-실제로 매우 복잡하다! 전통적인 IT 배경을 가진 사람들이나 복잡한 데이터 문제를 다루는 대기업에서 일한 경험이 있는 사람들을 위해, 기업 사고방식과 의료 데이터를 어떻게 설정할 수 있는지에 대해 간단히 논의하겠다. 그 후 의료 데이터의 복잡성에 대한 더 넓은 관점을 살펴볼 것이다. 이 기초가 확립되면, 의료 데이터의 일반적인 출처에 대한 개요를 살펴볼 것이다.

## 기업 사고방식
데이터 과학 산업은 많은 성공을 거두었다-데이터 과학을 활용하는 회사뿐 아니라 새로운 데이터 과학 방법을 만드는 회사도 있다. 데이터 과학을 활용하고 사용할 때 대부분의 조직은 전통적인 기업 사고방식을 따르는 이점을 갖는다. 조직 내 정보 및 데이터 아키텍트가 모여 다양한 데이터 소스와 의도된 사용 사례를 논의하고 그런 다음 최상위 정보 모델과 아키텍처를 설계할 수 있다. 이 과정의 일부는 이해관계자들을 한 방에 모아 데이터 또는 정보의 개별 단편을 어떻게 정의할지 합의하는 것이다. 최근까지 이것이 대부분의 회사가 데이터 웨이하우스를 구축할 때 취한 접근 방식이었다. 의료 분야의 도전은 데이터 소스가 격리된 사일로에서 작동한다는 것이다. 환자가 의료 시스템에 들어올 때, 일반적으로 가정의, 응급의학과, 또는 응급실을 통해 들어온다. 자연스럽게, 우리는 여기서부터 정보를 시작해야 한다고 말할 수 있다...

보험 회사가 사람을 비인간화하고 통계만 생각한다고 말하기 쉬지만 그것은 Mostly 혜택 추적의 운영적 측면에 달려 있다. 치료를 구하는 사람은 환자이지만 그 사람이 보험 계약자인 다른 사람의 수혜자가 될 수 있다. 예를 들어, 아이가 놀이터에서 넘어져서 응급실에서 봉합을 받으러 간다면, 진료소 관점에서 그 아이는 환자이다. 하지만 보험 회사 관점에서 추적해야 할 사람은 두 명이다-아이와 보험 계약자인 부모 또는 보호자. 보험 회사는 보험 청구를 위해 이 두 개인을 각각 다른 사람으로 추적해야 한다. 물론 둘은 명백히 관련이 있다.

### 그래프의 잠재력
이전 예시는 복잡한 데이터를 다룰 때 그래프가 특히 유용할 수 있음을 보여준다. 단일 청구(claim)는 두 개의 다른 노드에 연결된 하나의 노드로 만들 수 있다. 하나는 아이용이고 다른 하나는 부모/보호자용이다. 이 상대적으로 '단순한' 예시도 빠르게 복잡해질 수 있으며 이 책 전체에서 비슷한 예시를 다루며 데이터에 숨겨진 복잡성을 부각할 것이다. 데이터 분야의 베테랑들은 이전 논의와 데이터 웨이하우스 구축의 도전 사이의 유사점을 볼 수 있을 것이다. 조직이 전체 제품으로 통일되어 있을지라도, 조직 내 각 사업부나 부서는 고객이나 사용자에 대해 매우 다른 관점을 가질 수 있다. 경험적으로, 조직의 기업 정보 아키텍처를 구성하는 개념에 대한 공통 이해에 도달하기 위해 모든 이해관계자를 합의시키는 데 18개월 이상 걸릴 수 있다. 이 시간을 줄이는 방법을 찾는 것이 중요하지만 그것은 모두를 합의시키는 것보다는 올바른 사람, 프로세스, 도구를 갖추고 더 효과적으로 반복할 수 있도록 돕는 것에 더 가깝다. 말할 필요도 없이, 의료 서비스를 제공할 때 본래적으로 내재된 복잡성이 있으며 그 복잡성은...

## 현실 데이터 (Real-World Data, RWD)
현실 데이터(real-world data)라는 용어는 다소 어색하다. 현실 세계에서 오지 않는 어떤 종류의 데이터가 존재한다는 것을 암시하기 때문이다. 이 책은 물론 제약 및 기타 의료 분야 전반에서, 우리는 의료 서비스를 제공하는 동안 환자에 대해 수집된 데이터를 가리킨다. 병원에서 일한다면, 그것은 그냥 '데이터'이다(물론 학술 의료 센터와 건강 시스템이 임상 연구의 일환으로 수집된 데이터를 구별할 필요도 있다).

## 의료 데이터의 복잡성
데이터 메쉬(data mesh)는 데이터와 관련된 변화하는 환경의 한 예이다. 데이터베이스에서 데이터 웨이하우스로, 데이터 레이크로 진화해왔으며 이 진화는 계속될 것이다. 그러나 이것은 의료 데이터의 핵심 측면을 부각하는 데이터에 대한 사고의 변화를 보여주는 예시이다. 조직과 그 리더십은 매출이나 제조를 지원하는 데이터베이스와 애플리케이션 같은 전통적인 시스템 외부에 잠재적으로 유용한 데이터가 많이 존재한다는 것을 깨닫기 시작했다. 점점 더 이질적인 데이터 환경을 따라잡기 위해 고군분투하는 조직에서 기존 접근 방식의 단점을 해결하려는 아이디어가 데이터 메쉬 같은 것으로 발명되고 있다. 그러나 의료 분야에서는 이러한 복잡성이 항상 속도를 제한하는 요인이었다. 관리자들이 데이터에서 가치를 보기 시작하고 결과적으로 병원 내 다양한 서비스 라인에서 데이터를 연결할 수 있는 확장 가능하고 반복 가능한 방법을 찾으려고 하는 것은 최근의 일이 아니다. 제약 회사들도 의료 사무 관점에서 청구 데이터에 엄청난 가치가 있다는 것을 이제 막 깨닫고 있는 것도 아니다. 결과적으로 의료는 여전히 서로 다른 데이터 소스를 통합하는 방법을 찾으면서도, 보안 및 개인정보 보호, 거버넌스, 그리고 (데이터 과학자의 관점에서 가장 중요한) 데이터의 정규화(normalization) 및 조율(harmonization) 같은 중요한 문제를 균형 있게 유지하는 데 어려움을 겪어왔다. 제3장에서 더 자세히 다루겠지만 데이터 과학자들은 종종...

역학자, 생물통계학자, 임상 연구자, 의료 정보학자들은 이러한 문제들을 잘 알고 있다. 다만 다양한 접근 방식을 보아왔다. 가장 흔히 보는 것은 사람들이 필수적인 조율을 직접 코드에 포함시키는 것이다. 특히 쿼리 시간에 SQL을 사용한다. 엔지니어링 관점에서 이것이 상당히 어렵게 만든다. 왜냐하면 무언가를 변경해야 할 경우 쿼리를 수정해야 하기 때문이다. 물론 이는 데이터프레임이 달라지고 하위 코드에 영향을 미칠 수 있음을 의미한다. 대안으로, 많은 과학자들은 SQL에서 최소한의 처리로 데이터를 추출하고 R, Python, SAS 또는 기타 환경에서 조율을 수행한다. 이것이 코드의 유지보수성을 개선하지만 재사용이 쉽지는 않다. 이 책 전체에서 논의하겠지만 전반적인 엔지니어링을 개선하는 다른 방법들이 있다. 그러나 모든 기술적 결정과 마찬가지로, 트레이드오프가 있다!

의료 데이터의 복잡성에 대해 어느 정도 노출되었으니, 이제 의료 데이터의 특정 유형과 출처에 대해 논의해 보자.

## 의료 데이터의 주요 출처
의료 데이터의 출처와 유형은 매우 다양하다. 이 책은 현실 데이터에 초점을 맞추며 이는 의료 서비스를 제공하는 과정에서 수집된 데이터를 의미한다. 다양한 형태로 존재할 수 있지만 가장 일반적인 것은 전자건강기록(EHR)에서 나온 데이터이다. 정의에 따르면, 그리고 끊임없는 혁신을 감안하면, 의료 서비스의 품질과 효율성을 개선하거나 환자 결과를 개선하는 데 사용된다면 거의 모든 것이 현실 데이터로 간주될 수 있다. 우리는 코로나19에서 이것을 약간 보았다. 다양한 데이터가 코로나 확산 방지에 사용되고 있다. 여기에는 GPS/위치 데이터와 같은 일반적인 것부터 연락추적 앱 데이터와 같은 더 구체적인 것까지 포함될 수 있다. 의료 데이터의 몇 가지 일반적인 출처를 검토하여 전체적인 파노라마를 파악할 것이다. 이것은 결점 목록이 아니며...

### 전자건강기록(EHR) 대 전자의무기록(EMR)
EHR과 전자의무기록(EMR)의 차이점이 궁금할 수 있다. 외래 EHR인 NextGen은1 EMR을 단일 기관의 환자 기록으로, EHR을 여러 소스에 걸친 포괄적인 기록으로 구별한다. 국가 보건 IT 조정자 사무국(ONCHIT)은 약간 더 구체적인 정의를 제공한다: EHR은 환자 치료에 관련된 모든 임상의의 포괄적인 기록이다.

### 용어 정의의 혼선과 표준화 필요성
EHR과 EMR에 대한 다양한 정의가 있을 수 있다. 하나의 일반적인 구별은 EMR이 주로 단일 기관 내 종이 차트를 대체하기 위해 임상의가 사용하는 반면, EHR은 환자의 더 포괄적인 기록으로 여러 소스의 데이터를 포함할 수 있다는 것이다. 실무에서는 산업계가 이러한 용어를 상호 교환적으로 사용하는 경우가 많으므로, 누군가 EHR 대 EMR이라는 용어를 사용할 때 무슨 의미인지 반드시 확인해야 한다. 이 구별에도 불구하고 내 경험상 이 용어들은 같은 의미로 상호 교환적으로 사용된다. 이 책에서 계속 반복해서 언급하겠지만(특히 제3장에서), 모두가 같은 정의를 사용하고 있는지 확인하는 것이 중요하다. 이 경우 EHR이나 EMR에 대해 논의할 때, 단일 기관의 기록인지 여러 기관의 기록인지 반드시 확인해야 한다. 이 책 전체에서 EHR이라는 용어는 하나 이상의 소스에서 나온 환자의 임상 기록을 의미하지만 더 구체적인 설명이 필요한 상황도 부각할 것이다.

### EHR과 데이터 조화 (Harmonization)
이 책을 한 가지 주제로 요약한다면, 그것은 데이터 조율(data harmonization)이라는 개념일 것이다. 데이터 조율과 상호운용성(interoperability)은 밀접하게 관련되어 있고 종종 상호 교환적으로 사용된다. 이 책에서 나는 상호운용성을 거래 관점에서 의료 데이터를 공유하는 것으로 설명한다. 즉, 환자 여정이나 치료 과정의 일부로서 환자에 대한 데이터를 어떻게 공유할 수 있는가...

반면에 데이터 조율은 여러 소스의 데이터를 통합하는 과정을 가리키며 이때 가능한 한 많은 기본 컨텍스트가 보존되고 데이터의 의미가 서로 다른 데이터셋 전반에서 정규화된다. 예를 들어, 제약 회사로서 보험자(payer)의 청구 데이터셋과 병원 EHR의 데이터셋을 결합하고 싶을 수 있다. 어떻게 하면 두 데이터셋의 약물을 정규화하여 "백금 기반 화학요법을 받은 모든 환자"와 같은 간단한 쿼리가 EHR 데이터셋에서 올바른 환자와 보험자 데이터셋에서 올바른 청구를 반환할 수 있을까? 상호운용성과 데이터 조율은 의료에서 큰 도전이다. 실제 환경에서는 기본 문제와 관련 솔루션에 대해서도 상당한 겹침이 있다. 예를 들어, 한 병원에서 다른 병원으로 환자의 약물 이력을 전송하든 두 데이터셋을 결합하든, RxNorm이나 국가 의약품 코드(NDC)와 같은 표준 코딩 시스템에 약물을 연결하는 것이 해결책이 될 수 있다.

이 책의 목적에 핵심적인 문제는 데이터 해석이 왜 데이터가 수집되었는지, 누가 데이터를 수집했는지, 수집 과정 중 워크플로우나 사용자 경험의 제약이 있었는지에 따라 크게 달라진다는 것이다. 이것을 나는 종합적으로 컨텍스트(context)라고 부른다. 데이터 엔지니어와 데이터 과학자로서 우리는 항상 모든 필수 컨텍스트에 접근할 수 있는 것은 아니며 때로는 최선의 추측을 해야 한다. 그러나 데이터가 어떻게 영향을 받았을지 적어도 스스로에게 물어보는 것이 중요하다. EHR 데이터의 데이터 조율과 관련된 가장 일반적으로 부각되는 예시 중 하나는 환자의 임상 기록에서 국제질병분류(ICD) 코드의 사용이다. ICD 코드는 환자의 임상 기록에서 진단을 추적하는 일반적인 방법이다-적어도 이것이 높은 수준의 의도와 가정이다...

그러나 조금 더 깊이 파고들기 위해 몇 가지 질문을 해야 한다:
- ICD 코드는 실제로 어떻게 사용되는가?
- 왜 ICD 코드인가 (다른 유형의 코드나 코딩 시스템 대신)?
- 누가 환자의 임상 기록에 코드를 할당하는가?

이것들은 당연해 보일 수 있지만 모든 조직이 데이터에 접근하는 방식이 같지는 않다는 것을 기억해야 한다. 미국에서 EHR의 ICD 코드는 일반적으로 청구 과정의 일부로 사용되며 의사나 간호사가 아닌 의료 청구 전문가에 의해 시스템에 입력된다. 이것은 우리의 분석에 영향을 미친다. 왜냐하면 코드가 보험 청구의 다른 부분을 정당화하거나 내부 보고 요구 사항을 충족하기 위해 사용될 수 있으며 결코 환자의 의학적 이력을 정확하게 문서화하기 위한 것이 아닐 수 있기 때문이다. 진단 코드와 같이 단순해 보이는 것을 다룰 때 발생할 수 있는 잠재적 복잡성을 파고드는 예시를 살펴보자. 환자가 일상 검진을 위해 가정의를 방문할 수 있다. 진료 과정에서 의사는 환자가 당뇨병이 있을 수 있다고 의심하고 혈색소 A1c 검사를 주문할 수 있다. 청구 전문가가 이 진료를 검토하고 보험 회사에 제출할 청구를 준비할 때, 당뇨병 선별 검사 조사(Encounter for screening for diabetes mellitus)라는 ICD-10 코드 Z13.1을 추가한다. 동시에, 교육 및 기타 환자 참여 자료를 통한 후속 조치를 위한 대시보드에 사용되는 '당뇨 전 단계(Prediabetes)' 코드 R73.03을 이러한 환자에게 추가하는 내부 방침도 있다. 데이터 과학자로서 우리는 몇 달이나 몇 년 후에 당뇨 전 단계(아직 당뇨병은 아닌)이지만 이후 당뇨병으로 진단된 환자 집단을 생성하려 한다. 우리는 완벽한 ICD-10 코드(R73.03)가 있다는 것을 알고 해당 코드를 가진 모든 환자를 찾기 위해 SQL 쿼리를 작성한다. 그런데 만약 우리 환자의 A1c 결과가 5.5%(정상 범위 내)였고 의사가 단지 지나치게 조심했을 경우는? 이 시나리오에서 우리의 SQL 쿼리는 이 환자를 당뇨 전 단계 집단의 일부로 잘못 반환할 것이다. 잠재적 복잡성을 더 잘 부각하기 위해, 우리가 현재 해당 지역의 프로그램에 참여하고 있다고 가정해 보자...

이 예시를 주어, 해결책은 무엇인가? 일반적으로 '당뇨 전 단계'의 객관적인 정의가 있는 이런 상황에서는 SQL 쿼리에 임계값을 추가하여 A1c 결과에 따라 트리거할 수 있다. 예를 들어 당뇨 전 단계의 임계값은 5.7-6.4%일 수 있으므로 SQL 쿼리의 WHERE 절을 그에 맞게 업데이트할 수 있다. 그러나 당뇨병으로 진단되는 임계값은 모든 병원, 진료소, 건강 시스템에 걸쳐 균일하지 않다. 예를 들어, 우리 진료소는 6.4%를 사용하지만 같은 카운티나 주의 다른 진료소는 6.7%의 임계값을 사용할 수 있다. 데이터 과학자와 데이터 엔지니어로서 우리는 그런 다음 원시 검사 결과를 쿼리하고 후속 프로세스에서 추가 필터링을 적용해야 한다. 이전 예시가 불필요하게 복잡해 보일 수 있지만 실제로는 진단과 관련된 다른 많은 상황보다 단순하다. 특히 종양학이나 신경학 같은 전문 분야에서는 질병 자체가 충분히 이해되지 않아 더욱 그렇다. 특정 질병 영역의 복잡성을 고려하기 시작하면 상황이 상당히 복잡해지기도 한다. 예를 들어, '치료 라인(line of therapy)'은 연구자와 데이터 과학자에게 상당히 미묘한 주제이며 도전이다.2 나는 비슷한 예시를 계속 공유하고 그래프 데이터베이스의 사용이 재현성(reproducibility)이라는 관점을 통해 데이터 엔지니어와 데이터 과학자로서 우리의 일을 어떻게 더 쉽고 효율적으로 만들 수 있는지 보여줄 것이다. 전자건강기록 데이터에 대한 개요는 여기서 마무리하지만 제4장에서 EHR 데이터를 조금 더 다룰 것이다. EHR 데이터는 누군가 현실 데이터를 언급할 때 가장 먼저 떠오르는 데이터 소스 중 하나이지만 또 다른 매우 일반적인 유형의 데이터는 상환 청구(reimbursement claims)이다. 다음 섭션에서 다룰 것이다.

### 의료 청구 데이터 (Claims Data)
청구 데이터(claims data)는 의료 서비스 제공의 재무적 측면을 포착한다. 이는 미국 의료 시스템의 사보험자 의존도 때문에 흔히 관련되지만 국가 의료 시스템을 가진 나라들도 유사한 데이터를 추적한다(본질적으로 정부가 유일한 보험 회사 역할을 한다). 청구 데이터는 비용을 지불하고 환자를 추적하는 메커니즘이다. 미국 의료 시스템은 보험자와 의료 공급자가 많아 훨씬 복잡하지만 모든 의료 시스템은 의료 지출을 추적하는 방법이 필요하다. 이 장에서는 청구 데이터를 경험할 수 있는 공개 접근 가능한 데이터셋을 검토할 것이다...

단순한 상황에서는 그럴 수 있다. 그러나 청구 데이터와 EHR 데이터 사이에는 일반적으로 심각한 불일치가 있다. 업코딩(upcoding)이라는 용어를 들어보았을 것이다. 이는 종종 진료소와 병원( collectively 의료 공급자)이 지불금을 늘리기 위해 사기적으로 코드를 제출하는 과정을 설명하는 데 사용된다. 예를 들어, 의료 공급자가 환자를 짧은 진료로 보았을 수 있는데, 이때 일상적인 선별 서비스만 수행되었다. 이 경우 99212 코드(문제 중심 병력, 문제 중심 검사, 단순한 의사결정 중 최소 2개를 수행한 확립된 환자에 대한 평가)를 사용해야 했다. 그러나 진료소는 코드 99215(포괄적 병력, 상세 검사, 고도의 복잡성이 있는 의사결정 중 최소 2개를 요구하는 평가)를 제출하면 더 많이 상환된다는 것을 알고 있다. 이것은 사기적 업코딩의 매우 명확한 예시이지만 코드가 부풀려진 것처럼 보이는 모든 상황이 사기인 것은 아니다. 또 다른 예시는 의료 공급자가 환자의 복잡성을 높이는 것으로 보이는 특정 진단 코드를 추가하는 것이다(이전 섹션의 당뇨병 예시에서 논의한 바와 같이). 이 상황에서 코드는 관련 검사 테스트를 정당화하기 위해 청구에 첨부되었다. 후속 분석 관점에서, 이것은 당연히 업코딩처럼 보일 수 있다. 왜냐하면 환자는 실제로 당뇨병이 아니었기 때문이다. 어느 쪽이든, 이것은 청구 조정 및 상환 과정 때문에 임상적으로는 부정확할 수 있으며 심지어 사기적인 업코딩일 수 있음을 보여준다. 핵심은 우리 데이터 엔지니어와 데이터 과학자가 청구 데이터의 기저에 있는 미묘함을 실제로 이해해야 하며 우리 특정 사용 사례에 대해 정확하다고 가정해서는 안 된다는 것이다.

### 자가보험 고용주 데이터
미국의 대부분의 대기업은 자가보험(self-insured) 또는 자금조달(selffunded)이다. 이는 보험 회사 대신 재정적 위험을 직접 감수한다는 의미이다...

### 임상 및 질병 등록부 (Registries)
임상 및 질병 등록부는 일반적으로 특정 기준(종종 연구 프로토콜) 하에서 전향적으로 데이터를 수집하기 위해 사용된다. 등록부에도 EHR 데이터와 동일한 데이터 조율 문제가 존재한다. 그러나 핵심적인 차이점 중 하나는 질병 등록부의 주요 의도가 후속 분석을 위한 데이터 수집(예: 임상 연구, 인구 건강, 공중보건 감시)이라는 것이다. 이 의도의 개념에 대해서는 제3장과 제4장에서 어느 정도 상세히 논의하지만 이것은 EHR/청구 데이터와 의료에서 수집되는 대부분의 다른 데이터 사이의 가장 큰 차이를 부각한다-데이터가 후속 분석을 위해 수집되었는지의 여부이다. EHR과 청구 데이터는 주로 의료 서비스의 거래를 위해 수집된다. 의사가 EHR에 데이터를 기록할 때, 그들은 (또는 다른 임상의가) 환자에게 적절한 치료를 제공할 수 있도록 문서화하려 한다. 의료 코딩 전문가가 ICD-10 코드를 할당할 때, 그들은 진료소가 상환 청구를 제출하도록 돕고 있다. 의도는 데이터 과학을 위한 데이터 수집이 아니다. 반면에, 등록부는 분석가와 과학자가 데이터를 사용하여 환자 집단에 대한 통찰력을 도출할 수 있도록 수집된다. 특정 병원의 워크플로우를 깊이 파고들어 데이터의 컨텍스트와 미묘함을 이해하는 대신, 연구 프로토콜을 참조하면 된다. 이것은 여러 기관이나 데이터 수집 지점의 데이터를 다룰 때 우리에게 특히 유익하다. 등록부에서 모든 데이터 수집 지점은 동일한 연구 프로토콜을 사용하고 데이터를 가능한 한 균일하게 수집하려고 한다. 반면에, EHR과 청구 데이터에 대한 지역적 영향은 진료소, 보험 회사, 심지어 고용주마다 다양하다...

### 임상 시험 데이터 (Clinical Trials)
임상 시험 데이터는 상당히 더 많은 재정적 및 규제적 인센티브가 있기 때문에 의료 데이터 중 가장 '깨끗한' 데이터일 수 있다. 임상 시험의 성공과 규제 당국의 승인은 깨끗한 데이터와 견고한 분석에 달려 있다. 결과적으로, 제약 회사 및 임상/수탁 연구 기관(CRO)은 데이터 수집, 정리, 처리, 분석에 상당한 자원을 투입한다. 또한 규제 기관이 명확한 제출 요구 사항을 정의했기 때문에 임상 시험 데이터와 관련된 명확한 기준(예: CDISC)이 있다. 이러한 기준은 시험 데이터를 조율할 때 일부 어려움을 줄이지만 모든 문제를 해결하지는 못한다. 이제 임상 시험 데이터를 목록에 추가했으니, 지금까지 환자에 대한 가장 큰 데이터 소스를 다루었다. 웨어러블, 모바일 기기, 앱이 의료의 한 부분이 되면서 상황은 이미 변하고 있다. 종종 이러한 솔루션은 디지털 건강(digital health)이라는 용어로 묶인다. 이것은 주시해야 할 대상이다! 이제 전자건강기록이든 임상 등록부이든 관계없이, 데이터 수집 과정 자체에 초점을 맞추자. 데이터가 어떻게 그리고 왜 수집되었는지를 이해하는 것은 현실 데이터에서 실행 가능한 통찰력을 효과적으로 추출하는 데 필수적이다.

### 데이터 수집 맥락이 데이터 과학자에게 미치는 영향
데이터 과학자로서, 우리는 데이터를 손에 넣고 놀아보기를 좋아한다. 대부분의 회사와 대부분의 데이터에서 데이터가 왜 그리고 어떻게 수집되었는지를 정확히 알고 있다. 비즈니스 운영을 촉진하기 위해 수집되었으며 분석도 비즈니스에 봉사한다. 즉, 데이터가 왜 수집되었는지와 어떻게 사용될 것인지의 정렬이 상당히 일치한다. 예를 들어, 스트리밍 서비스에서는 누가 어떤 노래를 듣고 있는지에 대한 데이터가 수집되는 것을 볼 수 있다...

그러나 의료에서 현실 데이터를 다룰 때는 데이터가 왜 수집되었는지와 어떻게 분석되는지 사이에 종종 큰 괴리가 있다. EHR의 데이터는 환자 치료를 촉진하기 위해 수집되었고, 임상 연구 데이터는 특정 연구 프로토콜을 따르기 위해 수집되었고, 청구 데이터는 상환을 촉진하기 위해 수집되었으며, 목록은 계속된다. 그런 다음 우리는 데이터를 사용하여 디지털 바이오마커를 식별하고, 위험 예측 머신러닝 모델을 만들고, 인구 수준 분석을 수행하려 한다. 이것은 우리 현재의 사용 사례와 데이터 요구 사항을 데이터에 맞추도록 강제한다. 이 섹션에서는 의료에서 데이터가 수집되는 다양한 방법을 안내할 것이다. 이것들은 현실 데이터를 다르게 생각하기 시작하는 데 도움이 되지만 유일한 고려 사항은 아니다. 의료 센터 유형, 질병 영역 또는 적응증, 데이터 수집에 사용된 플랫폼 등 다른 컨텍스트도 여전히 고려해야 한다. 우리의 첫 번째 정류장은 의료 연구의 두 가지 일반적인 구별을 살펴보는 것이다. 전향적(prospective)과 후향적(retrospective) 연구의 미묘함을 이해하면 데이터가 수집되고 분석되는 컨텍스트를 더 잘 이해하는 데 도움이 될 것이다. 전향적 연구와 후향적 연구의 구별, 그리고 분석 유형의 관점에서 초점을 맞추겠다. 이 구별과 함께 1차 데이터와 2차 데이터의 개념에 대해서도 조금 논의할 것이다. 내가 접한 대부분의 현실 데이터 사용 사례는 2차 데이터의 후향적 분석을 포함한다.

### 전향적 연구 (Prospective Studies)
전향적(prospective)이라는 용어는 임상 연구 기술에서 차용한 것으로, 연구가 시작되는 시점과 최종 결과가 측정되는 시점 사이의 관계를 부각한다. 전향적 연구에서는 연구 프로토콜이 확립되고 데이터가 수집된다. 데이터는 연구 프로토콜에 따라 연구가 끝날 때까지 계속 수집된다. 데이터 분석은 즉시 시작될 수 있다(연구가 진행 중일 때조차도) 또는...

전향적 연구와 1차 데이터의 개념은 종종 혼동된다. 전향적 연구의 맥락에서 수집된 데이터는 일반적으로 연구 목적으로 수집되었기 때문에 1차 데이터로 간주된다. 그러나 데이터가 전향적 연구에서 수집되었더라도 후속 연구에서 2차 데이터로 사용될 수 있다. 앞서 언급한 암 연구를 계속 예로 들면, 연구자들이 그 데이터를 가져와 다양한 임상 징후와 다양한 약물 사이의 상관관계를 찾고 싶다면, 이것은 데이터의 2차 사용으로 간주된다. 즉, 데이터가 원래 수집된 이유 외의 다른 이유로 사용되고 분석되고 있다. 전향적 연구의 데이터는 일반적으로 1차 데이터로 간주되지만 다른 분석에서는 2차 데이터로 간주될 수도 있다-1차 사용과 2차 사용의 이 구별은 데이터에 대해 질문하는 내용에 따라 완전히 달라진다. 따라서 전향적 연구는 앞으로를 바라보는 연구이다. 뒤를 바라보는 연구를 살펴보자.

### 후향적 연구 (Retrospective Studies)
역사적으로, 전향적 연구는 의료 데이터를 수집하여 분석하는 주요 메커니즘이었다. 그러나 대부분의 산업과 마찬가지로, 데이터가 점점 더 자주 다양한 형태로 수집되고 있다-전자건강기록, 디지털 건강 도구, 심지어 임상 및 질병 등록부를 통해서도. 결과적으로 사람들은 이러한 데이터에서 통찰력을 찾고 있으며 본질적으로 후향적 연구를 수행하고 있다. 후향적 연구는 결과가 이미 알려져 있는(예: 데이터셋의 모든 환자의 전체 생존율을 이미 알고 있는) 연구로, 기존 소스나 기억에서 데이터를 수집한다. 결과적으로 후향적 연구는 일반적으로 데이터의 2차 사용을 포함한다. 이것이 전향적 연구와 후향적 연구, 그리고 1차...

 예를 들어 대장암 치료에서 표준 화학요법과 베바시주맙(bevacizumab)을 병용한 화학요법의 치료 효과를 비교하는 연구 등이 있다. 데이터 관점에서 볼 때, 연구가 전향적인지 후향적인지 여부보다 더 중요한 것은 데이터의 수집 및 활용이 1차적인지 2차적인지이다. 데이터 엔지니어 또는 데이터 과학자로서 우리는 데이터가 어떻게, 왜 수집되었는지를 반드시 깊이 고려해야 한다. 데이터의 수집 배경과 목적은 데이터의 정제, 가공, 정규화 및 조화(harmonization) 방식에 직접적인 영향을 미치기 때문이다. 특정 연구 목적으로 설계되어 수집된 데이터는 가공하기가 비교적 수월하다. 데이터 유형과 형식이 사전에 명확히 정의되어 있고 각 데이터 항목의 의미와 수집 절차에 대해 연구 참여자 간의 공통된 합의가 존재하기 때문이다. 비록 이것이 완벽한 데이터 품질을 보장하지는 않더라도, 데이터 처리 과정의 기술적 난이도를 크게 낮춰준다. 반면 데이터의 2차 활용에서는 데이터가 다양하고 때로는 서로 상충되는 진료 및 행정적 이유로 수집되었기 때문에, 데이터를 어떤 기준으로 정제하고 조화시켜야 하는지 판단하기 어려울 때가 많다. 예를 들어 흔한 오해 중 하나는 환자의 전자건강기록(EHR)에 기재된 ICD-10 진단 코드 목록이 특정 질환을 앓고 있는 환자를 식별하는 완벽한 근거가 된다는 믿음이다. ICD-10 코드가 다양한 의료 데이터셋에서 진단명을 추적하는 표준으로 널리 사용되지만 수많은 EHR 시스템에서 이 코드가 입력되는 실제 맥락을 정확히 이해해야 한다. 정기 건강검진을 위해 주치의를 방문한 환자가 있고 의사가 당뇨병 가능성을 배제(rule-out)하기 위해 당화혈색소(HbA1c) 검사를 처방했다고 가정해보자. 즉, 의사는 환자가 당뇨병일 수 있다는 가설을 세우고 이를 확인하고자 검사를 지시한 것이다. 그런데 의료 보험 청구 및 비용 정산 과정에서 청구 담당자가 해당 검사의 보험 적용을 위해 환자 기록에 '기타 명시된 당뇨병'을 뜻하는 ICD-10 코드 E13을 임시로 부여할 수 있다. 이 경우 실제로는 당뇨병이 확진되지 않았음에도 청구 코드만 보면 당뇨병 환자로 오분류될 위험이 발생한다.

## 결론 및 요약

이 장의 대부분은 의료 데이터에 대해 높은 수준으로 논의하는 데 할애하였다. 임상 시험과 임상 연구 같은 연구를 언급했지만 이 책의 대부분의 초점은 많은 사람들이 현실 데이터라고 부르는 것에 맞춰져 있다. 물론 전자건강기록 데이터를 이해하려는 의료 센터라면, 그것은 단순히 데이터일 뿐이다. 바이오테크와 제약 산업에서는 중재적 및 비중재적 연구 데이터와 구별하기 위해 RWD를 사용한다-기본적으로 엄격한 프로토콜에 따라 특정 연구 목적으로 수집된 데이터 대비 의료 서비스를 제공하는 동안 명확한 기준 없이 수집된 데이터. 현실 데이터의 여정을 시작하면서 데이터가 왜 수집되었는지와 우리가 수행하려는 분석 유형 사이의 본래적인 복잡성에 대해 논의하였다. 이 책의 상당 부분은 이 복잡성을 완화하기 위해 사용하는 접근 방식에 전념하고 있다. 나는 직업 생활의 상당 부분을 의료 RWD를 다루는 데 바쳤지만 배울 것은 여전히 많다. 이 책은 하루아침에 전문가로 만들어주지 않을 것이지만 올바른 어휘를 제공하는 동시에 많은 교훈을 부각하여 바로 뛰어들 수 있도록 하는 것이 목표이다. 어떤 유형의 데이터를 받을지 선택할 수 있는 경우는 많지 않지만 데이터 유형과 수집 방식/이유에 대한 기본적인 이해는 더 나은 데이터 엔지니어와 과학자가 되는 데 도움이 된다. 이것들을 전반적인 데이터 파이프라인과 분석에 고려하기 시작할 수 있다. 이 책(또는 다른 책, 블로그 포스트, 라이브러리 설명서/문서, 연구 논문 등)을 읽으면서 명심할 것이 하나 있다면, 그것은 의료가 본래적으로 복잡하고, 데이터가 복잡하며, 이 모든 복잡성을 해결하기 위한 만능 해결책은 없다는 것이다. 의료 서비스의 제공는 인간 상태의 다양성을 포착하고 대표한다-만능 해결책을 찾는 것은 단순히 불가능하다. 의료 정보학자로서, 나는 항상 한쪽 발은...

# 제2장 기술 소개: 컨테이너와 데이터베이스

제1장에서 의료 데이터에 대한 논의로 여정을 시작했다. 이제 방향을 바꿔 Docker와 데이터베이스와 같은 순수 기술 주제에 대해 논의하겠다. 이 장의 목표는 모든 사람을 컨테이너 실행과 데이터베이스 상호작용의 기초로 빠르게 안내하는 것이다. 첫 번째 섹션은 Docker의 기본 검토를 포함한다. 이미 시스템에 Docker가 설치되어 실행 중이라면, 이 내용은 익숙할 것이다. 두 번째 섹션은 데이터베이스의 기본 개념에 대해 어느 정도 논의한다. 문서(data) 데이터베이스와 그래프 데이터베이스를 포함한 구조화 질의 언어(SQL) 및 NoSQL(SQL을 사용하지 않는 모든 것) 데이터베이스를 검토할 것이다. 의도는 배포 및 관리의 모든 세부 사항에 깊이 파고드는 것이 아니라 기본 소개를 제공하는 것이다. 소프트웨어 및 IT 배경을 가진 사람들에게 이것은 아마도 익숙할 것이다. 임상 배경을 가진 사람들이나 CSV 파일의 데이터로 일상적으로 작업하는 데이터 과학자들에게는 향후 장에 도움이 되는 몇 가지 기본 소개를 제공할 것이다. 말할 필요도 없이, 이 책은 일반적으로 SQL에 대한 어느 정도의 숙련도를 가정하지만 의료 분야에서 오고 데이터베이스 경험이 많지 않을 수 있는 사람들도 있을 것이다. 이 개요는 SQL 데이터베이스에 대한 심층 분석을 제공하려는 것이 아니다. 대신, 그래프 데이터베이스와 비교하고 대조할 수 있는 어휘 개념에 대한 기본 소개를 제공한다. 첫 번째 섹션에서는 Docker를 검토할 것이다. Docker는 애플리케이션과 기본 운영 체제 사이의 까다로운 종속성에 신경 쓰지 않고 서버 및 기타 소프트웨어를 빠르게 배포할 수 있게 해주는 도구 모음이다.

## Docker 및 컨테이너 기본 소개

웹 서버, 애플리케이션 서버 또는 데이터베이스 서버와 같은 서버 측 소프트웨어를 설치해본 적이 있다면, 환경이 문서와 일치하도록 하는 것이 얼마나 고통스러운지 알고 있을 것이다. 디렉토리 구조가 약간 다를 수 있고, 운영 체제가 다른 버전일 수 있으며, Java의 다른 버전이 설치되어 있을 수 있다. 이것에 대한 하나의 해결책은 VMware나 Parallels와 같은 가상 머신을 사용하는 것이다. 그러나 둘 다 게스트 운영 체제를 여전히 설치해야 하며 RAM과 디스크 공간을 포함한 상당한 오버헤드가 발생한다. 이 문제에 대한 현대적인 해결책은 컨테이너를 사용하는 것이다. 컨테이너는 애플리케이션 계층에 초점을 맞춘 가상화의 한 형태이다. 즉, 소프트웨어의 버전과 모든 종속성을 제어하는 특정 환경(컨테이너)에서 소프트웨어를 배포할 수 있게 해준다. 그러나 기존 가상 머신과 달리 운영 체제를 가상화하지 않으므로 RAM과 디스크 공간을 절약한다. 물론 디스크 접근, 네트워킹, 보안 등을 가능하게 하기 위한 어떤 마법이 필요하다. 여기서 Docker와 같은 프로젝트가 등장한다. rkt 및 LXC/LXD와 같은 다른 컨테이너 엔진도 있지만 우리는 macOS, MS Windows, Linux 운영 체제에 대한 광범위한 채택과 지원 때문에 Docker에 초점을 맞춘다. 추가적으로 컨테이너는 애플리케이션 개발자, 데이터 과학자 및 스택 상단의 구성원이 기본 운영 체제에 관계없이 반복 가능하고 재현 가능한 배포를 만들 수 있게 해준다. Mac, Windows, Linux 워크스테이션과 서버가 혼합된 환경에서 특히 중요하다. Docker는 이 책의 범위를 벗어나는 많은 옵션이 있다. 추가 정보와 Docker에 대한 자세한 내용은 Docker의 교육 자료를 확인하기 바란다. meanwhile, Docker를 시작하고 실행하는 기본 사항을 안내할 것이다...

이제 Docker의 실행 중인 버전이 시스템에 있고 올바르게 작동하는지 테스트했으니, 데이터베이스로 여정을 계속할 수 있다. 이 장의 나머지는 데이터베이스에 대한 고수준 소개로 시작하여 다양한 데이터베이스 기능과 특성에 대한 검토로 이어진다.

## 데이터베이스 핵심 개념
특정 프로젝트에 사용할 데이터베이스 유형을 선택하는 데는 여러 측면이 있다. 일반적으로 쓰기 요구 사항이나 읽기 요구 사항에 따라 선택한다. 쓰기 요구 사항은 데이터 수집과 데이터가 손실되지 않도록 시스템이 처리할 수 있어야 하는 사항을 중심으로 한다. 반면에 읽기 요구 사항은 데이터베이스에서 데이터를 쿼리할 때 시스템의 요구 사항을 고려한다. 이 섹션에서는 이러한 고려 사항 중 일부를 논의하며, 원자성, 일관성, 격리성, 내구성(ACID) 준수로 시작하고, 거래(비즈니스 거래를 촉진하기 위한 데이터 읽기 및 쓰기)와 분석(모든 수준의 분석을 지원하기 위한 데이터 읽기) 데이터베이스의 차이점에 초점을 맞춘다. 그 후 관계형/SQL, 문서, 그래프 데이터베이스와 일반적인 질의 언어 예제를 포함하여 바로 세부 사항으로 들어간다.

### ACID 트랜잭션 보장
데이터베이스의 일반적인 특성 집합(데이터베이스에 쓰는 관점에서)은 ACID로 불린다. ACID 준수는 데이터베이스가 특정 보증 하에 쓰기 작업을 수행하도록 보장한다:

원자성(Atomicity) - 원자성은 데이터베이스에 대한 쓰기가 단일 원자적 연산으로 처리되어 모든 구성 요소가 성공하거나 데이터베이스에 아무것도 적용되지 않는다는 개념이다. 고전적인 예는 자금을 한 계좌에서 다른 계좌로 이체하는 은행 데이터베이스이다. 조가 수지에게 $10를 이체하고 싶다고 가정하면, 데이터베이스는 조의 계좌에서 $10를 차감하고 수지의 계좌에 $10를 입금해야 한다. 조의 계좌에서 돈이 차감된 후 수지의 계좌에 입금되기 전에 정전이나 기타 시스템 결함이 발생하면 어떻게 될까? 그런 경우 돈은 본질적으로 영원히 사라질 것이다. 데이터베이스가 원자성을 지원한다면 차감과 입금이 모두 성공해야 한다. 그렇지 않으면 차감은 아무 일도 없었던 것처럼 롤백된다. 함께 실행되어야 하는 쓰기 그룹은 거래(transaction)로 그룹화된다.

일관성(Consistency) - 일관성은 데이터베이스가 항상 유효한 상태를 유지하도록 보장한다. 즉, 소프트웨어 시스템이 데이터베이스와 상호작용할 때 쿼리는 성공적으로 실행되거나 전혀 실행되지 않는다. 이것은 원자성과 유사해 보이지만 더 기본적이다. 일관성의 개념은 거래 내 쿼리 수에 관계없이 적용되며 쓰기가 성공하는지에 초점을 맞춘다. 앞서의 예를 계속하자면, 조가 자신의 계좌에서 돈을 인출하고 싶다면? 거래는 실제로 단계가 하나뿐이다(조의 계좌에서 $10 차감), 따라서 원자성은 적용되지 않는다. 그러나 정전이 조의 차감 기록을 남기는 도중에 발생하면, 데이터베이스는 조의 계좌와 데이터베이스 전체가 손상되지 않도록 보장해야 한다. 어떤 의미에서, 일관성은 원자성의 구성 요소이다-데이터베이스는 일관성을 사용하여 거래의 각 단계가 성공적인지...

내구성(Durability) - 내구성은 거래가 완료되어 데이터베이스에 기록되면 시스템 장애로 인해 거래 완료가 손실되지 않도록 보장한다. 이것들이 모두 매우 유사하게 들리므로, 각각을 개별적으로 논의해야 했는지 궁금할 것이다. 데이터 엔지니어나 데이터 과학자의 관점에서 세부 사항에 너무 신경 쓰지 않을 수 있지만 결국 데이터 파이프라인과 사용 가능한 옵션에 영향을 미친다. 때로는 데이터 파이프라인 관점에서 '완벽한' 솔루션이 다른 고려 사항 때문에 배포할 수 없는 경우가 있다. 말할 필요도 없이, 이것은 데이터베이스 엔지니어, 관리자, 시스템/네트워크 관리자에게는 중요하다. 내구성은 성공적인 거래가 결코 손실되지 않도록 보장하고, 일관성은 성공적인 거래가 데이터베이스를 손상시키지 않도록 보장하며, 격리성은 하나의 거래의 성공이나 실패가 다른 거래에 영향을 미치지 않도록 보장하고, 원자성은 거래의 모든 부분이 성공해야 거래가 성공하도록 보장한다. 이러한 보증을 제공하는 기반 엔지니어링은 여러 서버, 데이터 센터 또는 클라우드 공급자에 걸쳐 있는 분산 데이터베이스를 고려할 때 특히 복잡해진다. ACID 고려 사항은 모든 데이터베이스에 적용되지만 우리는 데이터베이스를 두 그룹으로 정리하는 경우가 많다-거래 작업 부하에 적용되는 것과 분석 작업 부하에 적용되는 것. 데이터 과학자로서 우리는 거의 전적으로 분석 데이터베이스에 초점을 맞춘다. 데이터 엔지니어로서 우리는 거래 시스템에서 데이터를 가져와 분석 시스템에 넣을 수 있다. 따라서 온라인 거래 처리(OLTP)와 온라인 분석 처리(OLAP) 시스템에 대해 빠르게 논의하자.

### OLTP (온라인 트랜잭션 처리) 시스템
ACID 준수는 일반적으로 OLTP 시스템, 즉 온라인 거래 처리의 맥락에서 논의된다. 데이터베이스 사용의 대부분은 비즈니스 거래를 촉진하도록 설계된 OLTP 시스템에 집중되어 있었다. 은행이...

작업에 초점을 맞추므로, OLTP 시스템은 전체 데이터베이스를 스캔하여 특정 데이터를 찾는 데 상당히 저조한 성능을 발휘한다. 반면에 데이터 과학자들은 종종 특정 필드 세트를 위해 전체 데이터베이스를 스캔하고 싶어한다. 예를 들어, 아나필락시스(生命的을 위협하는 전신 알레르기 반응)를 유발하는 약물에 대한 이상 반응을 분석하고 싶다고 가정하자. 모든 환자의 알려진 알레르기, 처방된 약물, 병원 입원 기록을 전자건강기록에서 추출하고 싶다. Kaiser Permanente와 같은 대형 조직에 있다면 1250만 명에 대한 이 쿼리를 실행할 것이다. OLTP 시스템에 대해 이러한 쿼리를 실행하면 결과를 반환하는 데 매우 오래 걸릴 수 있으며 서비스 거부(DoS) 공격과 유사하게 시스템을 다운시킬 수도 있다. 어느 쪽이든 데이터베이스 관리자는 아마 기분이 좋지 않을 것이다! 물론 이것은 다소 인위적인 예시이다. 데이터베이스와 IT 관리자가 이미 미션 크리티컬 시스템이 이러한 상황으로부터 격리되도록 하고 있다는 것을 알고 있다(바라건대!). 따라서 OLTP 시스템에 대한 분석 작업 부하를 실행해서는 안 되며 그러한 작업 부하를 위해 설계되지 않았다. 대신 다음에서 논의할 OLAP 시스템을 사용해야 한다.

### OLAP (온라인 분석 처리) 시스템
온라인 분석 처리(OLAP) 시스템은 앞서의 예와 유사한 분석 작업 부하를 처리하기 위해 도입되었다. 이러한 데이터베이스는 거래 사용 사례에서는 성능이 좋지 않을 수 있지만 데이터의 하위 집합에 대한 대규모 스캔에는 뛰어나다. OLAP 시스템에 대한 논의는 데이터 웨이하우스, 데이터 마트 및 분석을 목적으로 데이터를 집중화하는 기타 중앙 집중식 데이터 저장 시스템의 맥락에서 들을 수 있을 것이다. OLAP 시스템은 읽기 요구 사항-데이터를 쿼리하고 추출하는 방법에 대한 고려 사항-을 중심으로 구축된다. OLTP 시스템과 달리, 데이터 아키텍트는 예상되는 분석 작업 부하를 고려한 다음 데이터를 설계한다...

의료 데이터의 특성을 해결할 수 있을 뿐만 아니라, 의료 데이터에 대한 최선의 또는 유일한 솔루션은 반드시 아닐 수 있다. 궁극적으로 특정 유형의 데이터베이스는 множество 요인에 따라 달라진다. 다음으로 높은 수준에서 SQL 대 NoSQL 데이터베이스를 살펴볼 것이다. 그 후 여러 유형의 그래프 데이터베이스에 대해 논의할 것이다.

### SQL 대 NoSQL 데이터베이스
OLAP 시스템을 논의할 때 사람들은 종종 NoSQL 솔루션을 찾는다(OLTP 시스템에서는 SQL 시스템과 비교). 그러나 SQL과 NoSQL 데이터베이스는 OLAP이나 OLTP 시스템 모두에 사용될 수 있다. 어떤 유형의 데이터베이스를 사용할지에 대한 근본적인 결정은 복잡한 주제이며 이 책의 범위를 벗어난다. 데이터 엔지니어와 데이터 과학자로서, 우리는 종종 데이터베이스가 이미 배포된 후 프로젝트에 참여한다. 기억해야 할 핵심은 SQL 데이터베이스가 상당히 동질적인 데이터베이스 그룹이라는 것이다. 공급자마다 차이가 있지만 개념적으로는 모두 동일하다. 행과 열의 테이블이 있고 기본키-외부키 관계로 연결된다. 반면에 NoSQL 데이터베이스는 극도로 이질적인 데이터베이스 그룹이다. 정의에 따라 모두 관계형 패러다임을 따르지 않는 데이터베이스이다. 결과적으로 개념적으로 모두 매우 다르다. 가장 일반적인 것은 열 중심 데이터베이스(예: Cassandra), 키-값 저장소(예: Redis), 문서 중심 데이터베이스(예: MongoDB), 그래프 데이터베이스(예: Neo4j)이다. 각 클래스에는 각각의 특정 초점을 가진 많은 옵션과 공급자가 있다. 또한 많은 데이터베이스 공급자가 여러 패러다임을 지원하기 시작했다. 예를 들어 ArangoDB는 그래프 기능도 제공하는 문서 중심 데이터베이스이고, RedisGraph는 Redis 키-값 저장소용 그래프 애드온이며, Datastax는 Cassandra 열 중심 데이터베이스 위에 그래프 데이터베이스도 제공한다. 이 목록은 결함 목록이 아니라 NoSQL 환경이 넓고 다양하다는 것을 단순히 설명하기 위한 것이다...

### SQL 데이터베이스 (관계형 DBMS)
이 섹션은 SQL 데이터베이스에 대한 간략한 개요/검토를 제공한다. 몇 년 전 데이터베이스에 대해 배웠지만 빠른 검토가 필요한 사람들, 또는 SQL 데이터베이스에 대해 들어보았지만 좀 더 자세한 내용이 부족한 사람들을 위한 것이다. 이 섹션의 주요 의도는 어휘와 기본 개념의 기준선을 설정하는 것이다. 다양한 유형의 그래프 데이터베이스를 검토하면서 이 개념들을 참조할 것이다. 앞에서 언급했듯이 SQL 데이터베이스는 데이터베이스 기술의 표준이다. SQL 데이터베이스라고 불리기도 하지만 관계형 데이터베이스 관리 시스템(RDBMS) 또는 관계형 데이터베이스라고도 불린다. 약간 혼란스럽지만 관계형 데이터베이스에서 관계(relations)는 테이블 자체를 가리키지 테이블 사이의 관계를 가리키지 않는다. 말할 필요도 없이 관계형 데이터베이스의 강점은 테이블을 서로 연결하고 이러한 연결을 쿼리할 수 있는 능력이다. 관계형 데이터베이스에서는 환자가 약물과 진단 데이터에 테이블 조인을 통해 연결된다.

보듯이, Patients 테이블은 각 환자의 이름(Patient_name)과 Demographic_Info(표시되지 않음)의 링크를 가진 개별 환자를 추적하도록 설계되었다. 추가적으로 Medication과 Diagnoses라는 두 개의 다른 테이블이 있으며 각각 Patients에 다시 연결되는 링크를 가지고 있다. PK와 FK는 각각 기본키(primary key)와 외부키(foreign key)에 해당한다. 이러한 키는 관계형 데이터베이스의 기본 구성 요소이며 테이블을 함께 연결할 수 있게 해주는 것이다. 기본키(PK)는 특정 테이블 내의 행을 고유하게 식별하는 데 사용된다. 때로는 이러한 키가 현실 속 속성을 나타내기도 한다(예: 의료 기록 번호, 사회 보장 번호, 보험 가입자 ID). 다른 때는 인위적으로 생성되어 기본 데이터에 의미나 의미적 관련이 없는 경우도 있다. 후자는 종종 순차적으로 생성된 정수이다(의 예시처럼). 외부키는 테이블에서 다른 테이블의 행을 참조하는 데 사용된다. 이전 예시에서 Patients 테이블의 PK는 Patient_id인데, 아마도 진료소나 병원이 할당한 환자 식별자일 것이다. 각 환자는 하나 이상의 약물을 복용하고 있을 가능성이 높다. 따라서 데이터베이스 설계자는 약물을 저장하기 위해 두 번째 테이블을 만들기로 결정했다. 이러한 약물을 특정 환자와 연결하기 위해 Patients와 Medication 테이블 사이에 기본키-외부키 관계를 설정했다. Medication 테이블에는 항상 Patient_id를 저장하는 열이 있다. 이 특정 설정에서 단일 환자는 Medication 테이블의 여러 항목에 연결될 수 있다. 왜냐하면 Medication의 모든 행이 동일한 환자를 참조할 수 있기 때문이다. 반면에 각 약물 항목은 단일 환자만 참조할 수 있다. 이것이 우리가 Patients와 Medication 사이의 일대다 관계라고 부르는 것이다. Patients와 Medication과 같은 두 테이블을 연결할 때, 일반적으로 일대일, 일대다, 다대일 관계를 볼 수 있다. 일대일의 경우, 한 테이블의 단일 행이 다른 테이블의 단일...

이것을 조인 테이블(join table)이라고 부른다. 현재 조인 테이블은 특정 약물 이름에 대한 단일 항목을 갖는 기본 요구 사항을 해결한다. 그래서 "아세트아미노펜(타이레놀)"에 대한 여러 항목을 갖지 않는다. 그러나 조인 테이블을 Medication_Order로 표시한 것을 눈치챘을 수 있다. 은 주문 날짜(order_date) 및 주문자(ordered_by)와 같은 Medication_Order 테이블의 여러 열을 추가로 포함하여 기본 조인 예시를 확장한다.

ordered_by 열은 외부키 관계이며 약물 주문을 배치할 수 있는 의사와 간호사(표시되지 않은 다른 테이블)를 가리킨다. order_date 열은 약물 주문이 실제로 시스템에 입력된 시간이다. 이것은 조인 테이블의 강점과 약물 주문이나 투여 같은 복잡한 시스템을 모델링하는 데 사용될 수 있음을 보여준다. 후자의 경우 Medication_Administration이라는 또 다른 테이블을 만들어 Medication_Order와 일대일 관계를 가질 수 있다. 일대일 관계를 가진 것을 왜 만들어야 하는지 궁금할 수 있다. 왜냐하면 추가 열을 더하면 되기 때문이다. 이 경우 두 테이블은 워크플로우의 매우 다른 부분을 포착하므로 데이터베이스에서 분리하는 것이 합리적이다. 이것은 데이터를 데이터 웨이하우스에 로드할 때 더 쉽게 만들 수 있지만 약물 주문 대비 투여 프로세스의 업데이트를 독립적으로 테스트할 수 있으므로 엔지니어링과 데이터베이스 관리를 간소화한다.

환자, 약물, 약물 주문에 대한 예시를 계속하자면, 예시 2-1과 2-2에서 SQL 쿼리 관점에서 어떻게 보이는지 살펴보자.

예시 2-1. SQL의 약물 주문

```sql
-- 아세트아미노펜의 약물 ID 가져오기
SELECT Medication_id FROM Medication WHERE Medication_name = "Acetaminophen (Tylenol)";
-- 조인 테이블 없이 아세트아미노펜을 복용하는 모든 환자 가져오기
SELECT P.Patient_id FROM Patients P INNER JOIN Medication M ON P.Patient_id = M.Patient_id WHERE M.Medication_name = "Acetaminophen (Tylenol)";
-- 조인 테이블을 사용하여 아세트아미노펜을 복용하는 모든 환자 가져오기
SELECT Patient_id FROM Medication_Order MO INNER JOIN Medication M ON MO.Medication_id = M.Medication_id WHERE M.Medication_name = "Acetaminophen (Tylenol)";
```

예시 2-2. SQL의 환자 생년월일이 포함된 약물 주문

```sql
-- 조인 테이블 없이 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
SELECT P.Patient_id, P.DOB FROM Patients P INNER JOIN Medication M ON P.Patient_id = M.Patient_id WHERE M.Medication_name = "Acetaminophen (Tylenol)";
-- 조인 테이블을 사용하여 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
SELECT P.Patient_id, P.DOB FROM Patient P INNER JOIN Medication_Order MO ON P.Patient_id = MO.Patient_id INNER JOIN Medication M ON MO.Medication_id = M.Medication_id WHERE M.Medication_name = "Acetaminophen (Tylenol)";
```

예시 2-1은 환자와 그들의 약물 사이에 조인 테이블이 없는 의 스키마에 해당하는 기본 SQL 쿼리를 보여준다. 이것은 가장 단순한 스키마이며 INNER JOIN이 이를 가능하게 하는 SQL의 핵심 요소이다. 조인은 관계형 데이터베이스를 쿼리하고 여러 테이블에 걸쳐 분산된 데이터를 추출할 수 있게 해주는 메커니즘이다. 과거에 SQL로 작업한 적이 있다면 이것은 검토를 위한 것이다. SQL이 처음이라면, 여기서의 주요 의도는 높은 수준의 개요를 제공하고 관계형 데이터베이스를 쿼리하는 방법에 대한 기본적인 이해를 개발하도록 돕는 것이다. 예시 2-2는 SQL 쿼리를 확장하고 에서 제공되는 스키마를 가정한다. 여기서는 세 개의 다른 테이블에서 조인해야 하기 때문에 쿼리가 약간 더 복잡해지는 것을 볼 수 있다. 예시 2-2의 마지막 쿼리는 환자의 생년월일과 같은 추가 속성을 쿼리하려 할 때 쿼리가 어떻게 더 복잡해질 수 있는지를 보여준다. 이러한 예시와 특히 조인 테이블의 개념은 데이터의 그래프 기반 표현과 그래프 데이터베이스를 생각하는 데 중요하다. 첫 번째 유형의 그래프 데이터베이스인 속성 그래프 데이터베이스를 살펴보자.

### 속성 그래프 데이터베이스 (Property Graph DB)
레이블이 있는 속성 그래프(LPG) 데이터베이스는 흔히 속성 그래프 데이터베이스라고 불리며 오늘날 사용되는 가장 일반적인 그래프 데이터베이스 중 하나일 것이다. 유일한 유형의 그래프 데이터베이스도 아니고 첫 번째도 아니다. 그러나 Neo4j와 같은 회사가 최근 인기를 얻었다. 핵심에서 속성 그래프는 본질적으로 노드(가끔 정점이라고도 함)와 관계(가끔 엣지라고도 함)의 모음이며 에서 볼 수 있다.

관계는 방향적이며 항상 하나의 노드에서 다른 노드를 가리킨다. 그러나 그래프 질의 언어는 일반적으로 방향성을 무시하고 관계를 쿼리할 수 있게 해준다. 레이블이 있는 속성 그래프에서는 노드에 레이블도 있어 데이터베이스에 있을 수 있는 모든 노드의 더 높은 수준의 조직을 제공한다. 이러한 레이블은 관계형 데이터베이스의 테이블과 어느 정도 유사하다. 앞선 예시를 계속하자면, Patient와 Medication이라는 두 가지 레이블 또는 유형의 노드가 있을 것이다. 각 노드에는 테이블의 열과 유사한 속성을 가질 수 있다. 의 스키마에 대한 한 가지 가능한 그래프 기반 표현은 에 나와 있다.

보듯이, 관련 열을 추적하면서 환자, 약물, 약물 주문을 표현할 수 있는 능력을 여전히 가지고 있다. 이렇게 사용될 때, 속성 그래프와 관계형 데이터베이스는 많은 유사점을 가지며 일반적으로 동일한 기능을 제공한다. 차이점이 무엇이며 왜 그래프 데이터베이스를 쓰는지(특히 이미 관계형 데이터베이스가 배포되어 있다면) 궁금할 수 있다. 관계형 데이터베이스에서 관계는 데이터베이스의 기본 스키마의 일부로 테이블을 연결하는 데 초점을 맞춘다. 두 데이터 포인트가 연결되는 의미는 기본 데이터베이스의 설계와 관리에 녹아든다. 이것은 잘 문서화되어야 한다. 그렇지 않으면 담당 엔지니어가 조직을 떠나면 이 지식은 사라질 것이다. 기본 이유가 데이터의 의미와 관련이 없고 기술적 문제의 결과인 경우 특히 문제가 될 수 있다. 또한 데이터 엔지니어나 데이터 과학자가 테이블 사이에 새로운 유형의 관계를 추가하려면 데이터베이스 관리자 및 엔지니어와 협력하여 데이터베이스 스키마를 해당 방식으로 업데이트해야 한다. 반면에 속성 그래프 데이터베이스 내에서는 모든 데이터 측면(레이블, 속성, 관계 유형)이 데이터로 간주된다. 즉, 온더플라이로 업데이트할 수 있으며 데이터베이스 스키마를 재설계할 필요가 없다. 따라서 속성 그래프 데이터베이스는 스키마리스 데이터베이스 범주에 속한다. 이것은 약간 오명이다. 왜냐하면 데이터베이스에 분명히 스키마가 있기 때문이다. 그러나 스키마는 온더플라이로 변경될 수 있으며 데이터베이스 관리자에 의해 설정/관리되지 않는다. 더 나은 설명은 동적 스키마를 가지고 있다는 것이다.

#### 스키마리스(Schemaless) 대 NoSQL
스키마리스와 NoSQL 같은 용어는 많은 데이터베이스를 설명할 때 자주 사용된다. 약간의 겹침이 있지만 다른 아이디어를 포착한다. NoSQL 데이터베이스는 관계형이 아닌 모든 SQL 데이터베이스이다. 스키마리스 데이터베이스는 고정된 스키마를 요구하지 않는 데이터베이스이다...

특정 결정은 대체로 여러 주변 요인에 따라 달라진다. 레이블이 있는 속성 그래프 대 관계형 데이터베이스의 맥락에서 몇 가지 핵심 고려 사항은 다음과 같다:
- 다양한 개념 사이의 관계 유형이 확정적인가?
- 새로운 관계가 추가될 가능성은? 누가 추가하는가?
- 다음 요구 사항의 상대적 중요성:
  -ACID 준수 -읽기/쓰기 지연 -OLTP 대 OLAP -쿼리 작성/유지보기 용이성 -생태계 내 기존 도구(예: Tableau와 같은 보고 도구) 및 조직에서 사용 가능한 전문 지식 -SQL과 유사한가? 개발자 친화적인가?

그래프 및 그래프 데이터베이스 작업을 위한 일반적인 질의 언어와 프레임워크를 논의할 것이다. SQL 데이터베이스와 달리 그래프 데이터베이스 작업을 위한 단일 표준은 없다. SPARQL과 RDF를 표준으로 논의하겠지만 그래프 데이터베이스의 일부 집합만 포착한다. 다른 NoSQL 데이터베이스와 마찬가지로 각 공급자는 자체 기술의 미묘함에 맞춘 질의 언어를 만들어냈다. 속성 그래프의 가장 일반적인 두 질의 언어는 Cypher(및 오픈소스 대응물인 openCypher)와 Gremlin이다. 몇몇 공급자(예: RedisGraph)는 openCypher를 채택했고 일부는 Gremlin을 채택했다(예: JanusGraph, AWS Neptune, OrientDB). 많은 공급자들은 서드파티 라이브러리를 통해 지원한다(예: Neo4j). 서드파티 라이브러리의 도전은, Neo4j 지원에서 명백하듯이, 특정 데이터베이스의 최신 버전을 지원하지 않을 수 있으며 기본 유지보수자의 필요에 따라 지원이 올 수 있고 사라질 수 있다. Cypher, Arango Query Language, TigerGraph의 GSQL과 같은 질의 언어는 구조적으로 SQL과 유사하다. 이것은 SQL로 작업하는 데 익숙한 사람들에게 학습 곡선을 줄여준다. 반면에 Gremlin과 같은 접근 방식은 훨씬 더 프로그래머 지향적이며 기존 질의 언어와는 거리가 있다. 이것들은 절대적으로 장점도 단점도 아니다. 그러나 팀의 인구 통계학과 특정 도구 세트가 얼마나 효과적일 수 있는지를 고려할 때 고려 사항이 된다. 팀에서 아무도 도구를 사용할 수 없다면, 이론적으로 더 우수한 솔루션이라도 중요하지 않다. 측정 가능한 영향을 미치지 않을 것이기 때문이다.

SQL 쿼리가 그래프 질의 언어에서는 어떻게 보이는지 예시를 살펴보자. 앞서 정의한 그래프 스키마를 바탕으로 쿼리를 작성한다. 먼저 Neo4j의 주요 질의 언어인 Cypher부터 시작한다.

Cypher - 예시 2-3은 이름으로 약물을 찾고, 그 약물이 주문된 모든 환자를 찾고, 환자에 대한 일부 정보를 찾는 기본 Cypher 쿼리를 포함한다. SQL과 많은 면에서 유사하여 관계형 세계에서 작업해온 사람들에게 배우기 쉽다.

예시 2-3. Cypher의 약물 주문

```cypher
// 아세트아미노펜의 약물 ID 가져오기
MATCH (m:Medication {Medication_name: "Acetaminophen (Tylenol)"}) RETURN m.Medication_id
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
MATCH (p:Patient)-[:has_order_for]-> (m:Medication {Medication_name: "Acetaminophen (Tylenol)"}) RETURN p
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
MATCH (p:Patient)-[:has_order_for]-> (m:Medication {Medication_name: "Acetaminophen (Tylenol)"}) RETURN p.Patient_id, p.DOB
```

쿼리들이 서로 유사하며 SQL 대응과 크게 다르지 않다. 그러나 구문이 훨씬 더 간결하다. 이 특정 사용 사례를 모델링하는 것은 SQL로도 가능하지만 Cypher 기반 접근 방식은 훨씬 더 단순하고 쉽다. 이 단순함은 환자의 생년월일에 대한 추가 쿼리에서 특히 두드러진다. SQL 예시에서는 추가 INNER JOIN 절이 필요했지만 Cypher 버전에서는 추가 p.DOB만 필요했다. Cypher는 SQL로 작업하는 데 익숙한(그리고 아마 선호하는) 사람들을 위해 설계된 것으로 보인다. 대안으로, Tinkerpop 그래프 프레임워크가 만든 Gremlin은 코드를 쓰는 것과 훨씬 더 유사하다. 많은 개발자들은 질의 언어를 코드로 감싸는 것보다 Gremlin을 선호한다. Gremlin에서 우리의 예시가 어떻게 보이는지 살펴보자.

Gremlin - 예시 2-4에서 보듯이 구문은 Cypher와 상당히 다르다. 이것은 질의 언어보다는 Python이나 Groovy 코드처럼 보인다. 이러한 근본적인 설계 차이로 인해 SQL에 익숙하거나 코드 작성 경험이 부족한 데이터 분석가나 데이터 과학자에게는 Gremlin이 도전적일 수 있다.

예시 2-4. Gremlin의 약물 주문

```groovy
// 아세트아미노펜의 약물 ID 가져오기
g.V().has("Medication_name", "Acetaminophen (Tylenol)").property("Medication_id")
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
g.V().has("Medication_name", "Acetaminophen (Tylenol)").in("has_order_for")
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
g.V().has("Medication_name", "Hydrocodone").in("has_order_for").values("Patient_id", "DOB")
```

다시 한번, 그래프 내 관계의 순회가 전체 쿼리에 최소한의 구문을 추가하는 것을 볼 수 있다. 초기에는 Gremlin을 사용한다는 것은 Java 또는 Groovy를 주요 프로그래밍 언어로 사용하여 Java 생태계 위에 스택을 구축하는 것을 의미했다. 그러나 이제 Python 드라이버가 존재하여 점점 더 보편화되는 Python 데이터 생태계에 통합하기가 더 쉽다. 이제 단일 데이터베이스에 특정한 덜 인기 있는 질의 언어로 전환하자. Neo4j가 훨씬 더 인기 있는 데이터베이스이지만 ArangoDB는 문서(예: JSON)와 그래프를 기본적으로 지원하는 독특한 접근 방식을 가지고 있다. Fast Health Interoperability Resources(FHIR)와 같은 JSON 기반 형식의 데이터를 다룰 때 특히 유용할 수 있다.

ArangoDB 쿼리 언어 - ArangoDB는 JSON 기반 생태계에 매끄럽게 통합되는 문서 중심 데이터베이스로서 내 관심에 들어왔다. 그 위에 쿼리 언어도 그래프를 지원한다. 멀티모달 데이터베이스로서 ArangoDB는 문서 중심 및 그래프 데이터베이스의 장점을 결합할 잠재력을 가지고 있다. 예시 2-5에서 동일한 쿼리를 AQL로 적용한다.

예시 2-5. ArangoDB 쿼리 언어의 약물 주문

```aql
# 아세트아미노펜의 약물 ID 가져오기
FOR m IN Medication FILTER m.Medication_name == "Acetaminophen (Tylenol)" RETURN m.Medication_id
# has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
FOR m IN Medication FILTER m.Medication_name == "Acetaminophen (Tylenol)" FOR p in 1..1 INBOUND m has_order_for RETURN p
# has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
FOR m IN Medication FILTER m.Medication_name == "Acetaminophen (Tylenol)" FOR p in 1..1 INBOUND m has_order_for RETURN {id: p.Patient_id, dob: p.DOB}
```

AQL은 질의 언어와 순회 언어의 조합처럼 느껴진다. 말할 필요도 없이 Cypher와 Gremlin에 비해 구문적 복잡성이 증가하는 것을 볼 수 있다. 속성 그래프 데이터베이스에 대한 소개는 여기서 마무리한다. 다음은 하이퍼그래프이다. 그래프 데이터베이스에 대한 약간 더 일반화된 접근 방식이다. 노드와 관계에 대한 속성을 생성하는 동일한 능력을 지원하지만 단일 관계에서 여러 노드를 표현하거나 서로 연결된 관계를 갖는 추가 능력을 가지고 있다.

### 하이퍼그래프 데이터베이스 (Hypergraph DB)
하이퍼그래프는 그래프 데이터베이스 환경에서 거의 그렇게 흔하지 않다. 누군가 "그래프 데이터베이스"를 언급하면, 거의 모든 사람이 속성 그래프나 RDF 데이터베이스를 이야기한다고 가정할 것이다. 속성 그래프에서 엣지는 정확히 두 개의 정점을 연결한다(또는 관계가 두 노드를 연결한다). 반면에 하이퍼그래프는 임의의 수의 정점을 연결할 수 있는 하이퍼엣지를 포함한다. 에서 동일한 정보의 속성 그래프 표현과 하이퍼그래프 표현을 비교한다.

보듯이 노드 A는 some_edge 관계를 통해 노드 B와 C에 연결된다. 속성 그래프 데이터베이스에서는 A→B와 A→C라는 두 개의 별도 관계가 저장된다. 하이퍼그래프 표현에서는 단일 엣지가 저장되지만 그 엣지는 A를 B와 C 둘 다에 연결한다. 추가적으로 내가 이 책에서 다룰 하이퍼그래프 데이터베이스는 하이퍼엣지를 정점으로도 취급한다.这意味着 엣지 자체가 다른 엣지의 일부가 될 수 있다. 엣지를 정점으로 취급하는 이유와 이것이 혼란을 야기하지 않을지 궁금할 수 있다. 이것은 머릿속으로 이해하기 어려운 개념일 수 있으며 매우 "메타"처럼 느껴질 수 있다. 환자와 약물 주문에 대한 우리의 예시에서 하이퍼그래프 아이디어는 아무런 이점을 제공하지 않는다. 결국 우리의 관계는 두 노드(환자와 약물, has_order_for 관계를 통해)만 연결하고 있다. 하이퍼그래프 표현도 동일하다. 그러나 복합 요법의 경우는? 하나의 환자에 대해 여러 약물이 주문되는 상황은?

그러나 추가 컨텍스트는 약물 A와 B를 복합 요법으로 함께 복용해야 한다는 것이다-환자는 하나를 다른 것 없이 복용해서는 안 된다. 추가적으로 환자는 약물 C도 복용하고 있다. 속성 그래프 표현에서는 환자가 단지 동시에 세 개의 별도 약물을 복용하고 있는지, A와 B가 함께 주문되는지 명확하지 않다. 하이퍼그래프 표현에서는 A와 B가 동일한 has_order_for 관계로 연결되어 있어 이것이 매우 명확하다. 물론 속성 그래프 표현에 추가 속성이나 노드를 추가하여 이 문제를 해결할 수 있지만 우리는 기본 그래프 표현의 단점을 설명하기 위해 데이터의 표현을 변경하고 있는 것이다. 다음 섹션에서는 TypeDB(이전 Grakn)와 그 쿼리 언어인 TypeQL을 하이퍼그래프의 특정 구현으로서 잠시 살펴볼 것이다.

#### TypeDB와 TypeQL
TypeDB는 강력한 타입의 데이터베이스로, 기본 그래프 데이터베이스 위의 추상화이다. 이를 통해 강력한 타입의 스키마를 활용하여 그래프 데이터베이스 내에서 즉시 사용할 수 없는 강력한 추론 기능을 제공할 수 있는 추론 엔진을 제공할 수 있다.1 이 책을 시작할 때 이 회사는 Grakn Labs로 알려져 있었고 데이터베이스는 Grakn이라고 불렸다. 그 후 회사가 리브랜딩되었지만 기술적 접근 방식은 동일하며 기능은 독특하며 의료에서 많은 사용 사례에 적합할 수 있다. 예시 2-6에서 아세트아미노펜을 시스템에 쿼리하고 처방된 환자의 환자 수준 데이터를 추출하기 위해 쿼리를 작성하는 동일한 예시를 계속한다.

예시 2-6. TypeQL의 약물 주문

```typeql
// 아세트아미노펜의 약물 ID 가져오기
match $medication isa Medication, has Medication_name "Acetaminophen (Tylenol)", has Medication_id $id; get $id;
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
match $medication isa Medication, has Medication_name "Acetaminophen (Tylenol)"; $patient isa Patient; (patient: $patient, medication: $medication) isa order;
get $patient
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
match $medication isa Medication, has Medication_name "Acetaminophen (Tylenol)"; $patient isa Patient, has dob $dob, has Patient_id $id; (patient: $patient, medication: $medication) isa order;
get $dob, $id;
```

TypeQL은 다른 질의 언어와 유사한 구조를 가지고 있으며 관계나 속성을 쿼리하는 것이 꽤 명확하고 쉽다. 마지막으로 RDF 데이터베이스를 다루겠다. 때때로 트리플 스토어라고도 한다. 상당히 오래전부터 존재해왔으며 유연하고 표현력이 풍부하다. 그러나 학습 곡선이 상당히 가파르게 만드는 추가 복잡성을 수반한다. 또한 가장 일반화된 그래프 데이터베이스이다-속성 그래프나 하이퍼그래프로 할 수 있는 모든 것은 RDF 데이터베이스로도 가능하다.

### RDF (자원 기술 프레임워크) 데이터베이스
자원 기술 프레임워크(RDF) 그래프는 오랫동안 존재해왔으며 월드 와이드 컨소시엄(W3C)과 시맨틱 웹의 일부이다. RDF 그래프는 매우 유연하고 상당히 강력하지만, 학습 곡선이 상당히 가파르다는 트레이드오프가 있으며, 결과적으로 "지나치게 학술적"이라는 낙인이 찍히는 경우가 많다. RDF 그래프 데이터베이스를 트리플 스토어라고 부르는 경우가 많다. 이것은 RDF 그래프가 피연산자-서술어-객체(subject-predicate-object) 형태의 트리플 모음이라는 개념에서 나온 것이다(참조).

환자의 RDF 표현에서, 원은 세 개의 서로 다른 트리플에서 피연산자이다-각각 생년월일, 환자 ID, 노드 유형용이다. 서술어는 객체가 어떻게 해석되어야 하는지의 컨텍스트를 제공한다. 이 경우 서술어는 2020-01-01이 실제로 생년월일이고 1234가 환자 식별자임을 알려준다. 이 접근 방식의 강점은 거의 모든 것이 이 기본 스키마를 사용하여 모델링될 수 있다는 것이다. 예시 전체에서 두 개의 문자열이 콜론으로 구분되는 특정 표기법이 사용되는 것을 눈치챘을 수 있다. 이것은 XML 네임스페이스의 변형으로, URI의 기본에 대응하는 네임스페이스를 정의하고 여기에 식별자를 추가할 수 있다. 핵심은 RDF가 균일 자원 식별자(URI)에 크게 의존한다는 것이다. 특정 의미적 컨텍스트에 대한 URI는 종종 동일한 기본을 가지므로 네임스페이스 사용은 표현을 간소화하여 더 간결하고 읽기 쉽게 만든다. RDF가 웹에서 데이터와 정보를 표현하기 위한 표준으로 만들어졌기 때문에 URI는 기본 표준에 녹아 있다. 의도는 특정 아이디어나 개념이 지정된 URI에서 접근할 수 있는 명확하게 정의된 개념에 의해 뒷받침된다는 것이다. 이것은 RDF로 표현된 모든 것이 자기 설명적이 되도록 한다. 시맨틱 웹의 맥락에서 이것은 특별히 중요하다. 누구나 웹에서 다른 모든 것에 정보를 게시하고 연결할 수 있기 때문이다. 대부분의 데이터베이스 시스템과 달리(정보 모델이 매우 특정한 사용 사례를 지원하도록 설계된), RDF는 개방형 정보 모델을 지원하도록 설계되었다.

#### SPARQL 프로토콜 및 RDF 질의 언어
SPARQL은 SPARQL 프로토콜 및 RDF 쿼리 언어를 의미하며("스파클"로 발음), RDF 그래프의 기본 질의 언어이다. 시맨틱 웹 표준의 일부로서 완전히 개방되어 있으며 여러 공급자에 의해 구현된다. 물론 RDF 트리플 스토어는 SPARQL을 구현하지만 많은 속성 그래프도 SPARQL 구현을 가지고 있다. 예를 들어, SPARQL-gremlin 커넥터가 있어 SPARQL을 gremlin을 지원하는 모든 데이터베이스에서 실행할 수 있다. SPARQL은 많은 면에서 SQL과 유사해 보이지만 네임스페이스와 URI와 같은 개념을 통합한다. 예시 2-7에서 주의할 점은 PREFIX의 추가이다. 이를 통해 질의 작성자는 네임스페이스를 활용하여 질의를 축약할 수 있다. 그렇지 않으면 SQL과 유사해야 한다. 그러나 WHERE 절이 좀 더 장황하고 RDF의 피연산자-서술어-객체 특성을 반영하는 것을 눈치챘을 것이다. WHERE 블록의 각 절은 자체적으로 트리플이다.

예시 2-7. SPARQL의 약물 주문

```sparql
# 아세트아미노펜의 약물 ID 가져오기
PREFIX schema: <http://schema.org/>
PREFIX ex: <http://mydomain.com/some_example/>
SELECT ?medication_id
WHERE {?medication ex:medication_name "Acetaminophen (Tylenol)". ?medication ex:medication_id ?medication_id. }
# has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
SELECT ?patient_id
WHERE {?medication ex:medication_name "Acetaminophen (Tylenol)". ?patient ex:has_order_for ?medication. ?patient ex:patient_id ?patient_id. }
# has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
SELECT ?patient_id ?dob
WHERE {?medication ex:medication_name "Acetaminophen (Tylenol)". ?patient ex:has_order_for ?medication. ?patient ex:patient_id ?patient_id. ?patient schema:birthDate ?dob. }
```

이 구문에 익숙해지려면 시간이 걸리지만 매우 유연하고 강력하다. RDF 접근 방식은 다양한 데이터 및 정보 모델을 표현하는 데 많은 유연성을 허용한다. 그러나 이 유연성은 일반적인 트레이드오효를 수반한다-성능이다. 데이터가 일련의 트리플로 저장되기 때문에 대규모 그래프의 쿼리는 상당한 지연을 유발할 수 있다. 사용 사례에 따라 이것이 허용될 수 있으며 또는 유연성이 상당한 CPU와 RAM 자원을 가진 서버의 비용을 감수할 가치가 있을 수 있다.

데이터베이스에 대해 상당히 많은 것을 다루었으며 특히 그래프 데이터베이스에 대해 논의했다. 제4장에서 데이터베이스를 사용하여 의료 어휘 및 전문 용어를 관리하는 방법으로 넘어가기 전에 빠르게 요약하자.

## 결론 및 요약
이 장의 의도는 그래프 데이터베이스의 다양한 유형에 대한 전문가를 만드는 것이 아니었다. 목표는 각각에 대한 높은 수준의 개요와 쿼리 언어가 서로 어떻게 비교되는지를 제공하는 것이었다. 이 책의 나머지 부분, 특히 전자건강기록 및 청구 데이터 분석의 맥락에서 이러한 각 유형의 데이터베이스에 훨씬 더 깊이 파고들 것이다. 어떤 데이터베이스를 사용할지의 선택은 여러 요인에 따라 다르며 일반적으로 이 책의 범위를 벗어난다. 다른 데이터 프로젝트와 마찬가지로 결정은 기능적 요구 사항과 비기능적 요구 사항 사이에서 이루어진다. 물론 질의 언어와 데이터의 기본 표현은 중요한 고려 사항이다. 그러나 거의 모든 소프트웨어 프로젝트와 마찬가지로, 어떤 데이터베이스를 사용하여 현재 작업을 수행할 수 있다-추가 엔지니어링이 필요할 수 있을 뿐이다. 그래프 데이터베이스를 구체적으로 살펴볼 때, 그래프 네이티브(graph-native)라는 용어를 자주 듣게 될 것이다. 데이터의 그래프 기반 표현이 점점 인기를 얻으면서 많은 데이터베이스 공급자가 그래프 기능을 점점 더 많이 제공하고 있다. 그래프를 표현하는 것과 관련하여 그래프 네이티브는 기본 데이터가 저장되는 방식을 가리킨다. 특히 그래프 네이티브 데이터베이스는 인덱스 프리 어드잭시(index-free adjacency)를 제공한다. 관계로 연결된 노드는 관련 노드/엣지의 검색에 추가 인덱싱이 필요하지 않도록 저장된다. 이것이 문제인지 아닌지는 상황에 따라 다르다. 결국 기본 데이터베이스가 그래프 기능을 제공하기 위해 추가 인덱싱이 필요한 것이 중요한가? 작은 그래프에서는 중요하지 않을 수 있다. 큰 그래프에서는 인덱스 자체가 매우 크고 비효율적일 수 있다. 이것이 그래프 데이터베이스를 고려할 때 추가 고려 사항의 유형에 대한 한 가지 예시이다. 이제 그래프 데이터베이스에 대한 기술적 소개가 있었으니, 다음 장에서는 어휘, 전문 용어, 온톨로지에 초점을 맞추겠다. 과거에 시맨틱 웹로 작업한 적이 있다면, 온톨로지로 작업한 적이 있을 것이다. 다음 장은 온톨로지나 시맨틱 웹 원칙에 대한 일반적인 소개가 아니다-대신 의료 내에서 이러한 접근 방식의 특정 응용 및 예시에 초점을 맞추고 있다. 이 맥락에서 우리는 의료 관련 예시를 지원하기 위한 의미론에 대한 몇 가지 기본 아이디어를 다룰 것이다.

> **참고:** has_order_for 관계를 통해
MATCH (p:Patient)-[:has_order_for]-> (m:Medication {Medication_name: "Acetaminophen (Tylenol)"}) RETURN p.Patient_id, p.DOB 쿼리들이 서로 유사하며 SQL 대응과 크게 다르지 않다. 그러나 구문이 훨씬 더 간결하다. 이 특정 사용 사례를 모델링하는 것은 SQL로도 가능하지만 Cypher 기반 접근 방식은 훨씬 더 단순하고 쉽다. 이 단순함은 환자의 생년월일에 대한 추가 쿼리에서 특히 두드러진다. SQL 예시에서는 추가 INNER JOIN 절이 필요했지만 Cypher 버전에서는 추가 p.DOB만 필요했다. Cypher는 SQL로 작업하는 데 익숙한(그리고 아마 선호하는) 사람들을 위해 설계된 것으로 보인다.

대안으로, Tinkerpop 그래프 프레임워크가 만든 Gremlin은 코드를 쓰는 것과 훨씬 더 유사하다. 많은 개발자들은 질의 언어를 코드로 감싸는 것보다 Gremlin을 선호한다. Gremlin에서 우리의 예시가 어떻게 보이는지 살펴보자.

Gremlin - 예시 2-4에서 보듯이 구문은 Cypher와 상당히 다르다. 이것은 질의 언어보다는 Python이나 Groovy 코드처럼 보인다. 이러한 근본적인 설계 차이로 인해 SQL에 익숙하거나 코드 작성 경험이 부족한 데이터 분석가나 데이터 과학자에게는 Gremlin이 도전적일 수 있다.

예시 2-4. Gremlin의 약물 주문

```groovy
// 아세트아미노펜의 약물 ID 가져오기
g.V().has("Medication_name", "Acetaminophen (Tylenol)").property("Medication_id")
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
g.V().has("Medication_name", "Acetaminophen (Tylenol)").in("has_order_for")
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
g.V().has("Medication_name", "Acetaminophen (Tylenol)").in("has_order_for").values("Patient_id", "DOB")
```

다시 한번, 그래프 내 관계의 순회가 전체 쿼리에 최소한의 구문을 추가하는 것을 볼 수 있다. 초기에는 Gremlin을 사용한다는 것은 Java 또는 Groovy를 주요 프로그래밍 언어로 사용하여 Java 생태계 위에 스택을 구축하는 것을 의미했다. 그러나 이제 Python 드라이버가 존재하여 점점 더 보편화되는 Python 데이터 생태계에 통합하기가 더 쉽다. 이제 단일 데이터베이스에 특정한 덜 인기 있는 질의 언어로 전환하자. Neo4j가 훨씬 더 인기 있는 데이터베이스이지만 ArangoDB는 문서(예: JSON)와 그래프를 기본적으로 지원하는 독특한 접근 방식을 가지고 있다. Fast Health Interoperability Resources(FHIR)와 같은 JSON 기반 형식의 데이터를 다룰 때 특히 유용할 수 있다.

ArangoDB 쿼리 언어 - ArangoDB는 JSON 기반 생태계에 매끄럽게 통합되는 문서 중심 데이터베이스로서 내 관심에 들어왔다. 그 위에 쿼리 언어도 그래프를 지원한다. 멀티모달 데이터베이스로서 ArangoDB는 문서 중심 및 그래프 데이터베이스의 장점을 결합할 잠재력을 가지고 있다. 예시 2-5에서 동일한 쿼리를 AQL로 적용한다.

예시 2-5. ArangoDB 쿼리 언어의 약물 주문

```aql
# 아세트아미노펜의 약물 ID 가져오기
FOR m IN Medication FILTER m.Medication_name == "Acetaminophen (Tylenol)" RETURN m.Medication_id
# has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
FOR m IN Medication FILTER m.Medication_name == "Acetaminophen (Tylenol)" FOR p in 1..1 INBOUND m has_order_for RETURN p
# has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
FOR m IN Medication FILTER m.Medication_name == "Acetaminophen (Tylenol)" FOR p in 1..1 INBOUND m has_order_for RETURN {id: p.Patient_id, dob: p.DOB}
```

AQL은 질의 언어와 순회 언어의 조합처럼 느껴진다. 말할 필요도 없이 Cypher와 Gremlin에 비해 구문적 복잡성이 증가하는 것을 볼 수 있다. 속성 그래프 데이터베이스에 대한 소개는 여기서 마무리한다. 다음은 하이퍼그래프이다. 그래프 데이터베이스에 대한 약간 더 일반화된 접근 방식이다. 노드와 관계에 대한 속성을 생성하는 동일한 능력을 지원하지만 단일 관계에서 여러 노드를 표현하거나 서로 연결된 관계를 갖는 추가 능력을 가지고 있다.

### 하이퍼그래프 데이터베이스 (Hypergraph DB)
하이퍼그래프는 그래프 데이터베이스 환경에서 거의 그렇게 흔하지 않다. 누군가 "그래프 데이터베이스"를 언급하면, 거의 모든 사람이 속성 그래프나 RDF 데이터베이스를 이야기한다고 가정할 것이다. 속성 그래프에서 엣지는 정확히 두 개의 정점을 연결한다(또는 관계가 두 노드를 연결한다). 반면에 하이퍼그래프는 임의의 수의 정점을 연결할 수 있는 하이퍼엣지를 포함한다. 에서 동일한 정보의 속성 그래프 표현과 하이퍼그래프 표현을 비교한다.

보듯이 노드 A는 some_edge 관계를 통해 노드 B와 C에 연결된다. 속성 그래프 데이터베이스에서는 A→B와 A→C라는 두 개의 별도 관계가 저장된다. 하이퍼그래프 표현에서는 단일 엣지가 저장되지만 그 엣지는 A를 B와 C 둘 다에 연결한다. 추가적으로 내가 이 책에서 다룰 하이퍼그래프 데이터베이스는 하이퍼엣지를 정점으로도 취급한다.这意味着 엣지 자체가 다른 엣지의 일부가 될 수 있다. 엣지를 정점으로 취급하는 이유와 이것이 혼란을 야기하지 않을지 궁금할 수 있다. 이것은 머릿속으로 이해하기 어려운 개념일 수 있으며 매우 "메타"처럼 느껴질 수 있다. 환자와 약물 주문에 대한 우리의 예시에서 하이퍼그래프 아이디어는 아무런 이점을 제공하지 않는다. 결국 우리의 관계는 두 노드(환자와 약물, has_order_for 관계를 통해)만 연결하고 있다. 하이퍼그래프 표현도 동일하다. 그러나 복합 요법의 경우는? 하나의 환자에 대해 여러 약물이 주문되는 상황은?

그러나 추가 컨텍스트는 약물 A와 B를 복합 요법으로 함께 복용해야 한다는 것이다-환자는 하나를 다른 것 없이 복용해서는 안 된다. 추가적으로 환자는 약물 C도 복용하고 있다. 속성 그래프 표현에서는 환자가 단지 동시에 세 개의 별도 약물을 복용하고 있는지, A와 B가 함께 주문되는지 명확하지 않다. 하이퍼그래프 표현에서는 A와 B가 동일한 has_order_for 관계로 연결되어 있어 이것이 매우 명확하다. 물론 속성 그래프 표현에 추가 속성이나 노드를 추가하여 이 문제를 해결할 수 있지만 우리는 기본 그래프 표현의 단점을 설명하기 위해 데이터의 표현을 변경하고 있는 것이다. 다음 섹션에서는 TypeDB(이전 Grakn)와 그 쿼리 언어인 TypeQL을 하이퍼그래프의 특정 구현으로서 잠시 살펴볼 것이다.

#### TypeDB와 TypeQL
TypeDB는 강력한 타입의 데이터베이스로, 기본 그래프 데이터베이스 위의 추상화이다. 이를 통해 강력한 타입의 스키마를 활용하여 그래프 데이터베이스 내에서 즉시 사용할 수 없는 강력한 추론 기능을 제공할 수 있는 추론 엔진을 제공할 수 있다.1 이 책을 시작할 때 이 회사는 Grakn Labs로 알려져 있었고 데이터베이스는 Grakn이라고 불렸다. 그 후 회사가 리브랜딩되었지만 기술적 접근 방식은 동일하며 기능은 독특하며 의료에서 많은 사용 사례에 적합할 수 있다. 예시 2-6에서 아세트아미노펜을 시스템에 쿼리하고 처방된 환자의 환자 수준 데이터를 추출하기 위해 쿼리를 작성하는 동일한 예시를 계속한다.

예시 2-6. TypeQL의 약물 주문

```typeql
// 아세트아미노펜의 약물 ID 가져오기
match $medication isa Medication, has Medication_name "Acetaminophen (Tylenol)", has Medication_id $id; get $id;
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
match $medication isa Medication, has Medication_name "Acetaminophen (Tylenol)"; $patient isa Patient; (patient: $patient, medication: $medication) isa order;
get $patient
// has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
match $medication isa Medication, has Medication_name "Acetaminophen (Tylenol)"; $patient isa Patient, has dob $dob, has Patient_id $id; (patient: $patient, medication: $medication) isa order;
get $dob, $id;
```

TypeQL은 다른 질의 언어와 유사한 구조를 가지고 있으며 관계나 속성을 쿼리하는 것이 꽤 명확하고 쉽다. 마지막으로 RDF 데이터베이스를 다루겠다. 때때로 트리플 스토어라고도 한다. 상당히 오래전부터 존재해왔으며 유연하고 표현력이 풍부하다. 그러나 학습 곡선이 상당히 가파르게 만드는 추가 복잡성을 수반한다. 또한 가장 일반화된 그래프 데이터베이스이다-속성 그래프나 하이퍼그래프로 할 수 있는 모든 것은 RDF 데이터베이스로도 가능하다.

### RDF (자원 기술 프레임워크) 데이터베이스
자원 기술 프레임워크(RDF) 그래프는 오랫동안 존재해왔으며 월드 와이드 컨소시엄(W3C)과 시맨틱 웹의 일부이다. RDF 그래프는 매우 유연하고 상당히 강력하지만, 학습 곡선이 상당히 가파르다는 트레이드오프가 있으며, 결과적으로 "지나치게 학술적"이라는 낙인이 찍히는 경우가 많다. RDF 그래프 데이터베이스를 트리플 스토어라고 부르는 경우가 많다. 이것은 RDF 그래프가 피연산자-서술어-객체(subject-predicate-object) 형태의 트리플 모음이라는 개념에서 나온 것이다(참조).

환자의 RDF 표현에서, 원은 세 개의 서로 다른 트리플에서 피연산자이다-각각 생년월일, 환자 ID, 노드 유형용이다. 서술어는 객체가 어떻게 해석되어야 하는지의 컨텍스트를 제공한다. 이 경우 서술어는 2020-01-01이 실제로 생년월일이고 1234가 환자 식별자임을 알려준다. 이 접근 방식의 강점은 거의 모든 것이 이 기본 스키마를 사용하여 모델링될 수 있다는 것이다. 예시 전체에서 두 개의 문자열이 콜론으로 구분되는 특정 표기법이 사용되는 것을 눈치챘을 수 있다. 이것은 XML 네임스페이스의 변형으로, URI의 기본에 대응하는 네임스페이스를 정의하고 여기에 식별자를 추가할 수 있다. 핵심은 RDF가 균일 자원 식별자(URI)에 크게 의존한다는 것이다. 특정 의미적 컨텍스트에 대한 URI는 종종 동일한 기본을 가지므로 네임스페이스 사용은 표현을 간소화하여 더 간결하고 읽기 쉽게 만든다. RDF가 웹에서 데이터와 정보를 표현하기 위한 표준으로 만들어졌기 때문에 URI는 기본 표준에 녹아 있다. 의도는 특정 아이디어나 개념이 지정된 URI에서 접근할 수 있는 명확하게 정의된 개념에 의해 뒷받침된다는 것이다. 이것은 RDF로 표현된 모든 것이 자기 설명적이 되도록 한다. 시맨틱 웹의 맥락에서 이것은 특별히 중요하다. 누구나 웹에서 다른 모든 것에 정보를 게시하고 연결할 수 있기 때문이다. 대부분의 데이터베이스 시스템과 달리(정보 모델이 매우 특정한 사용 사례를 지원하도록 설계된), RDF는 개방형 정보 모델을 지원하도록 설계되었다.

#### SPARQL 프로토콜 및 RDF 질의 언어
SPARQL은 SPARQL 프로토콜 및 RDF 쿼리 언어를 의미하며("스파클"로 발음), RDF 그래프의 기본 질의 언어이다. 시맨틱 웹 표준의 일부로서 완전히 개방되어 있으며 여러 공급자에 의해 구현된다. 물론 RDF 트리플 스토어는 SPARQL을 구현하지만 많은 속성 그래프도 SPARQL 구현을 가지고 있다. 예를 들어, SPARQL-gremlin 커넥터가 있어 SPARQL을 gremlin을 지원하는 모든 데이터베이스에서 실행할 수 있다. SPARQL은 많은 면에서 SQL과 유사해 보이지만 네임스페이스와 URI와 같은 개념을 통합한다. 예시 2-7에서 주의할 점은 PREFIX의 추가이다. 이를 통해 질의 작성자는 네임스페이스를 활용하여 질의를 축약할 수 있다. 그렇지 않으면 SQL과 유사해야 한다. 그러나 WHERE 절이 좀 더 장황하고 RDF의 피연산자-서술어-객체 특성을 반영하는 것을 눈치챘을 것이다. WHERE 블록의 각 절은 자체적으로 트리플이다.

예시 2-7. SPARQL의 약물 주문

```sparql
# 아세트아미노펜의 약물 ID 가져오기
PREFIX schema: <http://schema.org/>
PREFIX ex: <http://mydomain.com/some_example/>
SELECT ?medication_id
WHERE {?medication ex:medication_name "Acetaminophen (Tylenol)". ?medication ex:medication_id ?medication_id. }
# has_order_for 관계를 통해 아세트아미노펜을 복용하는 모든 환자 가져오기
SELECT ?patient_id
WHERE {?medication ex:medication_name "Acetaminophen (Tylenol)". ?patient ex:has_order_for ?medication. ?patient ex:patient_id ?patient_id. }
# has_order_for 관계를 통해 아세트아미노펜을 복용하는 환자 ID와 생년월일 가져오기
SELECT ?patient_id ?dob
WHERE {?medication ex:medication_name "Acetaminophen (Tylenol)". ?patient ex:has_order_for ?medication. ?patient ex:patient_id ?patient_id. ?patient schema:birthDate ?dob. }
```

이 구문에 익숙해지려면 시간이 걸리지만 매우 유연하고 강력하다. RDF 접근 방식은 다양한 데이터 및 정보 모델을 표현하는 데 많은 유연성을 허용한다. 그러나 이 유연성은 일반적인 트레이드오효를 수반한다-성능이다. 데이터가 일련의 트리플로 저장되기 때문에 대규모 그래프의 쿼리는 상당한 지연을 유발할 수 있다. 사용 사례에 따라 이것이 허용될 수 있으며 또는 유연성이 상당한 CPU와 RAM 자원을 가진 서버의 비용을 감수할 가치가 있을 수 있다.

> **참고:** 관계
SELECT ?patient_id ?dob WHERE {?medication ex:medication_name "Acetaminophen (Tylenol)". ?patient ex:has_order_for ?medication. ?patient ex:patient_id ?patient_id. ?patient schema:birthDate ?dob. } 이 구문에 익숙해지려면 시간이 걸리지만 매우 유연하고 강력하다. RDF 접근 방식은 다양한 데이터 및 정보 모델을 표현하는 데 많은 유연성을 허용한다. 그러나 이 유연성은 일반적인 트레이드오효를 수반한다-성능이다. 데이터가 일련의 트리플로 저장되기 때문에 대규모 그래프의 쿼리는 상당한 지연을 유발할 수 있다. 사용 사례에 따라 이것이 허용될 수 있으며 또는 유연성이 상당한 CPU와 RAM 자원을 가진 서버의 비용을 감수할 가치가 있을 수 있다.

데이터베이스에 대해 상당히 많은 것을 다루었으며 특히 그래프 데이터베이스에 대해 논의했다. 제4장에서 데이터베이스를 사용하여 의료 어휘 및 전문 용어를 관리하는 방법으로 넘어가기 전에 빠르게 요약하자.

## 결론 및 요약
이 장의 의도는 그래프 데이터베이스의 다양한 유형에 대한 전문가를 만드는 것이 아니었다. 목표는 각각에 대한 높은 수준의 개요와 쿼리 언어가 서로 어떻게 비교되는지를 제공하는 것이었다. 이 책의 나머지 부분, 특히 전자건강기록 및 청구 데이터 분석의 맥락에서 이러한 각 유형의 데이터베이스에 훨씬 더 깊이 파고들 것이다. 어떤 데이터베이스를 사용할지의 선택은 여러 요인에 따라 다르며 일반적으로 이 책의 범위를 벗어난다. 다른 데이터 프로젝트와 마찬가지로 결정은 기능적 요구 사항과 비기능적 요구 사항 사이에서 이루어진다. 물론 질의 언어와 데이터의 기본 표현은 중요한 고려 사항이다. 그러나 거의 모든 소프트웨어 프로젝트와 마찬가지로, 어떤 데이터베이스를 사용하여 현재 작업을 수행할 수 있다-추가 엔지니어링이 필요할 수 있을 뿐이다. 그래프 데이터베이스를 구체적으로 살펴볼 때, 그래프 네이티브(graph-native)라는 용어를 자주 듣게 될 것이다. 데이터의 그래프 기반 표현이 점점 인기를 얻으면서 많은 데이터베이스 공급자가 그래프 기능을 점점 더 많이 제공하고 있다. 그래프를 표현하는 것과 관련하여 그래프 네이티브는 기본 데이터가 저장되는 방식을 가리킨다. 특히 그래프 네이티브 데이터베이스는 인덱스 프리 어드잭시(index-free adjacency)를 제공한다. 관계로 연결된 노드는 관련 노드/엣지의 검색에 추가 인덱싱이 필요하지 않도록 저장된다. 이것이 문제인지 아닌지는 상황에 따라 다르다. 결국 기본 데이터베이스가 그래프 기능을 제공하기 위해 추가 인덱싱이 필요한 것이 중요한가? 작은 그래프에서는 중요하지 않을 수 있다. 큰 그래프에서는 인덱스 자체가 매우 크고 비효율적일 수 있다. 이것이 그래프 데이터베이스를 고려할 때 추가 고려 사항의 유형에 대한 한 가지 예시이다. 이제 그래프 데이터베이스에 대한 기술적 소개가 있었으니, 다음 장에서는 어휘, 전문 용어, 온톨로지에 초점을 맞추겠다. 과거에 시맨틱 웹로 작업한 적이 있다면, 온톨로지로 작업한 적이 있을 것이다. 다음 장은 온톨로지나 시맨틱 웹 원칙에 대한 일반적인 소개가 아니다-대신 의료 내에서 이러한 접근 방식의 특정 응용 및 예시에 초점을 맞추고 있다. 이 맥락에서 우리는 의료 관련 예시를 지원하기 위한 의미론에 대한 몇 가지 기본 아이디어를 다룰 것이다.

# 제3장 의료에서의 표준화된 어휘와 온톨로지

이 장에서는 표준화된 어휘(standardized vocabularies), 제어된 어휘(controlled vocabularies), 전문 용어(terminologies), 온톨로지(ontologies)를 다룬다.有些人은 이 용어들을 상당히 상호 교환적으로 사용하는 반면, 다른有些人은 처음에는 혼란스러울 수 있는 상당히 정확한 정의를 고수한다. 우리는 이러한 아이디어들에 대한 간략한 소개, 그들이 어떻게 유사한지, 어디서 겹치는지, 그리고 어떻게 다른지로 시작할 것이다. 나는 전문 용어(terminology)라는 용어를 어휘에서 온톨로지에 이르는 스펙트럼에 걸쳐 있는 특정 콘텐츠 소스를 모호하게 지칭하는 데 사용하는 것을 볼 수 있을 것이다. 예를 들어, 국제질병분류 제10개정(ICD-10), ICD-9, 그리고 의학 체계화 명명법-임상 용어(SNOMED-CT)는 모두 전문 용어로 지칭될 것이다. 어떤 것은 단순히 용어와 정의의 모음(예: 어휘)인 반면, 다른 것은 위계 구조나 심지어 더 풍부한 관계를 포착한다(예: 온톨로지). 콘텐츠 외에도 시맨틱 웹 위에 구축된 관련 도구와 프로세스도 있다. 예를 들어, 많은 온톨로지가 웹 온톨로지 언어(OWL)로 제공되며 자원 기술 프레임워크(RDF) 위에 구축된다. 이러한 기술은 의료나 의학에 특화된 것은 아니지만 우리가 이러한 기술의 더 광범위한 채택을 보았던 분야 중 하나이다. 이 책에서 다른 산업의 데이터에 관한 책(특히 의료)과 차별화되는 단일 기술 장이 있다면, 그것은 바로 이것일 것이다. 의료 데이터를 다룰 때의 많은 문제점들이 시맨틱 기술-도구뿐 아니라 콘텐츠-의 사용에 의해 완화된다. 예를 들어, ICD 코드는 종종 EHR 내에서 진단을 추적하는 데 사용된다. 이것은 우리의 데이터 접근 방식을 간소화하고 질병을 코딩하는 방법을 표준화하기 위한 것이다. 이것을 통해 전국의 진료소와 병원이 정부와 보험 회사로부터 상환 청구를 제출할 수 있다. 반면에 이러한 제어된 어휘를 유지보수하고 관리하는 것은 종종 산만하고 다루기 힘들게 느껴질 수 있는 핵심 정보학 기능이 된다.

이러한 어휘, 전문 용어, 온톨로지에 대한 높은 수준의 소개 외에도, 우리는 통합 의학 언어 시스템(UMLS)-利用가능한 다양한 소스(UMLS 메타사전)를 쉽게 사용할 수 있게 해주는 일련의 데이터 파일과 관련 소프트웨어-에도 어느 정도 시간을 할애할 것이다. 또한 많은 소스 간의 연결을 제공하여 서로 다른 어휘 간의 의미론적 상호운용성을 촉진한다(UMLS 의미론적 네트워크). 어휘, 전문 용어, 온톨로지의 다양한 소스를 사용하는 작업의 세부 사항에 너무 깊이 들어가기 전에, 다음 섹션은 이러한 개념에 대한 기본적인 기반을 제공하고 의료 데이터를 다룰 때 우리가 이것들을 어떻게 사용하는지에 대한 이해를 돕는 역할을 할 것이다.

## 제어된 어휘, 전문 용어, 온톨로지
의료 분야에서는 시맨틱 웹과 정보 과학 분야의 아이디어를不同程度로 채택하고 있다. 결과적으로 정보학 전문가들이 온톨로지나 어휘에 대해 이야기하는 것을 들을 수 있다. 대부분의 경우 이러한 아이디어의 채택은 W3C 표준을 따르려고 노력한다. 그러나 시맨틱 웹의 목표와 우리가 의료에서 이러한 아이디어를 사용하는 방식 사이에는 하나의 큰 차이가 있다-시맨틱 웹은 개방형 웹 전반에 걸쳐 데이터, 정보, 지식을 연결하는 기반을 설정한다; 의료에서 우리는 종종 훨씬 더 좁고 고도로 큐레이션된 세계관을 가지며 매우 미묘한 해석에 초점을 맞춘다. 결과적으로 우리는 시맨틱 웹 아이디어와 표준의 다양한 채택을 보게 되며 어떤 것은 다른 것보다 더 엄격하다. 누군가 온톨로지나 어휘와 같은 시맨틱 웹 개념에 대해 이야기하는 것을 들을 때마다, 그 용어가 어떻게 사용되는지 알아내는 것이 현명할 것이다. 예를 들어, 누군가 "파킨슨병에 대한 온톨로지를 만들었다"고 말한다면, 그들이 파킨슨병(PD)과 관련된 소수의 개념을 수집했는지, 또는 PD와 그 증상 및 약물에 대한 공식적인 정의를 OWL로 개발했는지, 또는 그 사이의 어떤 것인지 궁금해해야 한다. W3C에 따르면, 어휘와 온톨로지 사이의 구별은 명확하지 않다-"추세는 보다 복잡하고 아마도 상당히 공식적인 용어 모음에는 '온톨로지'라는 단어를 사용하는 반면, 엄격한 형식주의가 반드시 필요하지 않거나 매우 느슨한 의미로만 사용되는 경우에는 '어휘'를 사용하는 것이다."

어떤 경우든, 어휘는 특정 컨텍스트에서 사용되는 개념 세트를 나타내는 용어와 그 정의의 모음으로 일반적으로 받아들여진다. 예를 들어, ICD-10 코드 세트는 어휘로 간주될 수 있다. 다른 예로는 자체 개발한 전자건강기록에서 수동으로 코딩된 약물의 드롭다운 목록이나 고용주의 건강 검진 앱의 코로나19 증상 목록이 있다. 어떤 경우에는(예: ICD-10이나 SNOMED CT), 어휘가 주제 전문가 및 도메인 전문가에 의해 명시적으로 개발 및 유지보수되어 산업 표준으로 기능한다. 다른 경우에는(많은 데스크톱 및 모바일 앱처럼), 소프트웨어 개발자 및 IT 전문가에 의해 암묵적으로 개발된다. 주제 전문가의 입력이 있었을 수 있지만 의도는 특정 앱 기능을 제공하는 것이었지 반드시 엄격한 용어 세트와 그 정의를 만드는 것은 아니었다.

### 코딩 시스템 (Coding Systems)
코딩 시스템(coding system)이라는 용어도 볼 수 있으며 주로 의료 코딩의 맥락에서 사용된다. 우리는 종종 의료 코드와 코딩 시스템을 어휘와 전문 용어로 사용한다(이 장의 뒷부분에서 논의하겠지만). 도전은 의료 코딩이 상환을 위한 문서화에 초점을 맞추고 분석을 위한 데이터를 포착하고 보존하는 것이 아니라는 것이다. 결과적으로, 코딩 시스템은 정의된 용어/개념에 있어 반드시 포괄적이거나 엄격하지 않다. 스펙트럼의 다른 쪽 끝에서, 대부분의 사람들은 온톨로지를 용어와 그 정의의 모음으로 생각하지만 기본 개념(반드시 이름이나 텍스트 표현은 아님)과 개념 간의 공식적인 관계에 특별히 초점을 맞춘다. 하나의 개념이 다른 이름이나 레이블을 가질 수 있는 경우, 그들은 동의어로 개념에 첨부되며 하나가 선호 이름/레이블로 지정된다. 온톨로지는 모든 것이 공식적으로 정의되기 때문에 빠르게 상당히 복잡해질 수 있다. 예를 들어, "선호 이름"은 실제로 무엇을 의미하는가? 이 속성의 정의를 어떻게 표준화하는가? 많은 온톨로지가 SKOS(Simple Knowledge Organization System)를 활용하여 레이블과 같은 것들을 위한 동일한 기반 접근 방식으로 온톨로지를 개발할 수 있게 한다.

어떤 소프트웨어 라이브러리와 마찬가지로, 온톨로지가 개발되는 방식에는 광범위한 스펙트럼이 있다. 어떤 것은 구현 세부 사항에 초점을 맞추어 시맨틱 웹 표준과 접근 방식을 엄격하게 준수하는 반면, 다른 것은 더 철학적이거나 단순한 다이어그램일 수도 있다. 그럼에도 불구하고 둘 다의 저자는 자신의 제품을 온톨로지라고 부를 수 있다. 데이터 엔지니어와 과학자로서, 특정 온톨로지를 고려할 때 일반적으로 두 가지 질문을 해야 한다:
1. 콘텐츠가 내 데이터 및 사용 사례와 정렬되는가?
2. 이 온톨로지를 내 데이터 파이프라인에 통합하려면 무엇이 필요한가?

온톨로지와 어휘 사이 어딘가에는 더 모호한 전문 용어(terminologies)가 있다. 나는 사람들이 어휘부터 온톨로지에 이르기까지 모든 것을 단순히 "전문 용어"라고 부르는 것을 보아왔다. 나는 개념 지향적이고, 비의미론적 고유 식별자를 가지며, 개념 간에 일부 관계를 가질 수 있는 것(용어와 그 정의의 모음)을 지칭할 때 이 용어를 사용하는 경향이 있다.1 일반적으로 이러한 관계는 어떤 종류의 위계 구조를 포착하거나 하나의 개념이 다른 개념과 어떻게 직접적으로 관련되는지를 포착한다. 전문 용어를 관리하는 많은 다른 방법들이 있으며 종종 스펙트럼 어디에 위치하는지에 따라 달라진다. 예를 들어, ICD 코드만 사용하고 조회를 수행하는 경우, 완전한 RDF 트리플 스토어를 배포하는 것은 과잉 대응일 수 있다. 그러나 복잡한 온톨로지를 데이터 모델의 단일 테이블로 간추리려고 하면 많은 두통을 유발할 수 있다. 그럼에도 불구하고 우리는 종종 전문 용어의 스펙트럼을 관리하고 유지보수(그리고 때로는 연결)하는 데 사용되는 기본 시스템을 설명하기 위해 전문 용어 서비스(terminology service)라는 용어를 듣는다. 전문 용어 서비스가 제공해야 하는 기능에 대한 표준 정의는 없지만 HL7 FHIR 표준에는 전문 용어 서비스에 대한 사양이 있다. FHIR에 대해서는 제4장에서 좀 더 자세히 다룰 것이다. 오픈소스에서 상용 제품에 이르기까지 다양한 도구가 있으며 나는 여전히 새로운 솔루션을 지속적으로 접하고 있다. 전문 용어 자체나 관리를 돕는 도구를 살펴볼 때 명심해야 할 몇 가지 사항이 있다. 다음으로, 팀 전체(도메인 전문가부터 IT까지)에서 고려하고 논의해야 할 몇 가지 핵심 사항을 살펴볼 것이다.

### 데이터베이스 선택 시 핵심 고려 사항
앞서 간략히 언급했듯이, 전문 용어를 데이터 프로젝트와 파이프라인에 통합하는 방법에 대해 생각해야 할 두 가지 수준이 있다. 도구 자체와 모든 것을 연결하는 데 필요한 배관을 고려해야 한다. 반면에, 전문 용어 자체와 콘텐츠의 품질 및 특정 사용 사례에 대한 정렬을 고려해야 한다. 많은 다른 프로젝트를 가진 대규모 조직의 경우, 여러 다른(그리고 아마 충돌할 수 있는) 요구 사항을 지원할 수 있도록 인프라도 고려해야 할 수 있다. 도구 관점에서 가장 큰 질문은 다음과 같다:
- 전문 용어 서비스가 프로젝트에서 어떻게 보이는가?
- 단순한 조회만 필요한 경우, 필요한 전문 용어를 SQL 데이터베이스에 로드하고 내장된 무료 텍스트 매칭을 사용하여 처리할 수 있을지도 모른다.
- 또는, ElasticSearch나 Solr의 간단한 설정만으로 충분할 수 있다.

그러나 FHIR을 사용하여 다른 시스템과 상호작용할 계획이라면, FHIR 사양을 지원하는 전문 용어 서비스가 필요할 수 있다. 이것들은 사소한 질문이 아니며, 사용 사례를 이해하고, 데이터 소스를 이해하고, 다양한 옵션과 의사결정 지점을 안내하는 데 필요한 의료 정보학 전문가를 확보해야 하는 것이다. 전문 용어를 데이터와 어떻게 통합하는가? 이것은 도구에 도전할 뿐만 아니라 데이터에 도전임을 부각한다. SQL이나 속성 그래프 데이터베이스에서 데이터를 다루면서 사용 사례에 완벽한 온톨로지를 찾았지만 OWL로만 제공되는 경우, 데이터를 온톨로지에 연결하는 것이 가장 좋은 방법을 결정해야 한다. RDF와 속성 그래프 사이의 간극을 메우려는 neosemantics와 같은 라이브러리가 있다. SQL로 작업하는 경우, 온톨로지의 고유 식별자를 포함하는 테이블에 새 열을 추가하는 것으로 충분할 수 있다. 또는 RDF를 올인으로 사용하여 OntoRefine과 같은 도구를 사용하여 SQL에서 RDF 트리플 스토어로 데이터를 변환하기로 결정했을 수 있다. 다시 말하지만 이것은 필요한 전문가뿐 아니라 기술적 요구 사항과 데이터 측면을 모두 이해하는 사람이 필요하다.

그렇다면, 전문가와 협의한 후, FHIR을 지원하는 상용 전문 용어 서비스(예: Ontoserver)를 통합하기로 결정했다. 또한 의미론적 개념을 표현하는 주요 접근 방식으로 SNOMED 코드를 사용하기로 결정했다. 그러나 소스 데이터는 주로 ICD-10 코드로 구성되어 있다. 여전히 고려해야 할 핵심 사항이 있으며 특히 도구에서 실제 의미론적 조율로 이동함에 따라 더욱 그렇다.

서로 다른 조직이 용어/개념을 다르게 사용한다
ICD가 표준으로 설계되었지만 하나의 코드를 다른 유사한 코드보다 선택하는 미묘함은 지역 컨텍스트에 따라 달라진다. 예를 들어, 코드가 상환 청구의 일부로 선택되는가, 사망 원인 보고에 사용되는가, 아니면 임상의나 청구 전문가에 의해 입력되는가? 이러한 종류의 컨텍스트(제1장에서 어느 정도 논의한 바와 같이)는 코드가 해석되고 정규화되는 방식에서 주요 역할을 한다. ICD-9이나 ICD-10과 같은 표준화된 코딩 시스템을 사용하더라도 의료 데이터를 다룰 때 이러한 종류의 질문에 여전히 답해야 한다.

서로 다른 ICD 릴리스 및 버전(예: ICD-9 대 ICD-10)
ICD가 때때로 단일 표준으로 생각되지만 여러 다른 ICD 버전이 있다. 오늘날 생성되는 ICD로 코딩된 대부분의 데이터는 ICD-10을 사용할 것이다. 전 세계 대부분이 수십 년 동안 ICD-10을 사용해왔다. 미국은 2009년에 ICD-10을 채택하기로 결정했지만 최종 구현은 2015년까지 이루어지지 않았다. 결과적으로 수집 시점과 장소에 따라 ICD-9와 ICD-10으로 코딩된 데이터를 모두 접할 가능성이 있다. 더 어렵게 만들기 위해...

1 잠시 전에 출판되었지만 Jim Cimino의 "Desiderata for Controlled Medical Vocabularies in the Twenty-First Century"라는 논문이 있다. 이 논문은 어휘, 전문 용어, 온톨로지를 다룰 때의 많은 공통 주제를 부각한다. 이것들을 만들거나 유지보수하는 모든 사람에게 필독서이며 이것들을 소비하는 모든 사람에게는 꼭 읽어야 할 자료이다.

더 어렵게 만들기 위해, ICD-11이 출시되었지만 대부분의 국가와 조직에 의해 아직 공식적으로 채택되지 않았다. ICD-9이나 ICD-10의 특정 버전 내에서도 서로 다른 "릴리스"가 있다. 이러한 릴리스는 여러 수준에서 발생하여 데이터 생산자/수집자와 데이터 과학자 모두에게 매우 어렵게 만든다. WHO는 공식 릴리스를 게시한다. 추가적으로 지역 보건 기관(예: 메디케어 및 메디케이드 서비스 센터)도 코딩 시스템을 수정하고 업데이트를 게시한다. 이러한 서로 다른 수준의 릴리스를 관리하는 것이 충분히 복잡하지 않은 것처럼, 코드가 어떻게 사용되어야 하는지에 대한 지침도 있다. 따라서 어떤 ICD 릴리스(예: ICD-10)를 사용하는지 알 뿐만 아니라, 데이터에 어떤 업데이트가 적용되었는지, 어떤 지역적 수정이 있는지, 그리고 어떤 버전의 지침이 적절한 코드를 선택하는 데 사용되었는지도 알아야 한다.

때로는 모든 수준에서 명확성을 얻는 것이 불가능하지만 이러한 각 층위가 데이터와 그 해석에 어느 정도 영향을 미친다는 것을 인식하는 것이 중요하다. 예를 들어, 한 병원이 2005년 ICD-10 업데이트를 사용하고 다른 병원이 2010년을 사용한다면, 이것은 두 병원 간 코드의 비교 가능성에 직접적인 영향을 미칠 수 있다. 이러한 미묘함과 코딩 시스템 및 전문 용어의 기저 컨텍스트, 그리고 그것들이 데이터에 어떻게 적용되는지를 이해하는 것은 사소한 디테일로 들어가는 것처럼 보일 수 있다. 그러나 이것은 우리에게 컨텍스트를 제공하기 때문에 중요하다. 이 컨텍스트는 데이터 조율 과정에서 무엇을(아무것도 하지 않는다면) 해결해야 하는지 결정하는 데 도움이 된다. 만약 운 좋게도 이미 정리되고 큐레이션된 데이터셋으로만 작업한다면, 이것은 덜 중요한 관심사가 될 수 있다. 데이터 소스가 원시에 가까울수록, 누군가는 이러한 질문을 해야 한다.

전문 용어가 처음인 사람들 사이에서 흔히 보는 한 가지 오해는 코딩 시스템, 데이터 또는 매핑 과정에 대해 너무 많은 가정을 하는 것이다. 예를 들어, RWD 경험이 부족한 데이터 과학자들이 코드가 선택되면 가장 적절한 코드라고 가정하는 경우를 자주 듣는다. 이 하나의 가정에는 여러 수준의 문제가 있다. 첫째, "적절한" 코드를 선택한다는 개념이 절대적이라고 가정한다. 하나의 사용 사례(예: 청구)에 적절한 것이 다른 사용 사례(예: 결과 추적)에서는 다를 수 있으며 데이터가 왜 수집되었는지와最終的に 캡처된 코드를 선택하거나 매핑한 사람이 누구인지에 대한 깊은 이해 없이 이 진술을 평가하기는 어렵다. 둘째, 서로 다른 질병 영역은 서로 다른 세분화 수준을 가질 것이며 특히 하나의 전문 용어에서 다른 것으로 이동할 때 더욱 그렇다. 이것은 일반적으로 두 가지 입력의 함수이다-전문 용目的 purpose와 질병 및 그 치료에 대한 우리의 이해. 예를 들어, SNOMED는 ICD-10보다 훨씬 더 세분화되고 상세하지만 과학적 관점과 임상 실천 관점 모두를 다룬다. ICD-10은 훨씬 더 높은 수준에서 진단을 추적하기 위해 만들어졌다. 추가적으로, 암 코드를 살펴보면, 하나의 암에서 다른 암으로 이동함에 따라 세분화 수준의 차이를 보게 될 것이다. 때로는 이것은 암의 아형에 대한 이해 수준의 함수이다. 다른 때는 용어의 사전 조율 대 사후 조율의 함수이다. 다음 섹션에서는 사전 조율과 사후 조율의 차이를 탐구하고 이것이 코딩된 데이터에 어떻게 영향을 미치는지 살펴볼 것이다.

### 사전 조화(Pre-coordination) 대 사후 조화(Post-coordination)
제어된 어휘는 RWD와 같은 복잡한 데이터를 다룰 때 유용할 수 있다. 우리는 모호함을 가능한 한 최소화하고 의미와 컨텍스트를 가능한 한 정확하게 포착하고 싶다. 그러나 생물학과 의학에서 흔히 접하는 개념은 상당히 복잡하며 종종 더 근본적인 개념의 조합이다. 예를 들어, 누군가 오른쪽 척골(새끼손가락 쪽의 전완골)이 골절되면, 제어된 어휘의 맥락에서 이것을 어떻게 포착하는가? 측면성(오른쪽), 손상 유형(골절), 뼈(척골)에 대해 별도의 개념을 만들고 필요에 따라 결합할 수 있다. 이것이 사후 조율(post-coordination)이라고 불린다. 이 접근 방식의 문제는 특정 부상을 포착하기 위해 이들을 결합해야 한다는 것이다. 추가적으로, 결합이 임상적으로 의미가 없는 상황에 빠질 수 있다. 표 3-1에 캡처된 신체 내 측면성, 손상 유형, 뼈의 과도하게 단순화된 예시를 살펴보자. 사후 조율 접근 방식을 사용하면 각 열에서 하나의 값을 선택할 것이다.

표 3-1. 사후 조율 개념
| 측면성 | 손상 | 신체 부위 |
|--------|------|-----------|
| 왼쪽   | 골절 | 척골      |
| 오른쪽 | 염좌 | 요골      |
| ...    | ...  | 미골      |
| ...    | ...  | ...       |

이것은 이전 예시인 "오른쪽 척골의 골절"을 다룰 수 있지만 누군가가 "오른쪽 미골의 골절"을 코딩할 수도 있게 한다. 미골은 꼬리뼈로도 알려져 있으며 척추 기저부에 위치하므로 측면성이 없다. 따라서 "오른쪽 미골의 골절"은 혼란스러울 수 있다.

이 문제에 대한 해결책은 측면성을 선택 사항으로 만드는 것이다. 그러면 특정 값을 비워둘 수 있어 "미골의 골절"이 유효한 조합이 될 수 있다. 그러나 이것은 잠재적인 데이터 품질 문제로 이어질 수 있다-"척골의 골절"도 유효한 개념으로 받아들일 것인가? 이 경우 측면성은 적용 가능하지만 부재한다. 또 다른 접근 방식은 사전 조율(pre-coordination)을 사용하는 것이다. 세 개의 별도 목록에서 선택하는 대신, 가능한 모든 조합을 사전에 지정할 수 있다: 왼쪽 척골의 골절, 오른쪽 척골의 골절, 미골의 골절 등. 이 상황에서는 모든 유효한 조합을 지정하고 유효하지 않은 조합을 방지할 수 있다. 그러나 트레이드오프는 가능한 조합의 목록이 매우 길어질 수 있지만 실제 환자에서는 드물게 관찰된다는 것이다. 또한 사전 조율된 용어를 옵션 목록의 일부로 사용하려면, 사용자 인터페이스가 초기 필터링 후에도 수백에서 수천 개의 옵션을 표시해야 한다. 어떤 것이 더 나은 옵션인지 궁금할 수 있다-사전 조율 대 사후 조율. 의료 RWD의 대부분의 경우와 마찬가지로, 답은 상황에 따라 다르다. 유효하지 않은 조합이 허용되지 않도록 데이터를 매핑하는 경우 사전 조율 접근 방식이 가장 적합할 수 있다. 반면에 사전 조율 접근 방식이 다루기 어려운 수의 옵션을 생성하는 경우 사후 조율 접근 방식이 더 나을 수 있다. 추가적으로 스택의 다른 측면에 따라 제한될 수 있다. 예를 들어, OMOP(관찰 보건 데이터 과학 및 정보학 프로그램의 데이터 모델)에 저장해야 하는 데이터로 작업하는 경우 사전 조율을 사용해야 한다. 제4장과 제5장에서 더 논의하겠지만 OMOP 공통 데이터 모델은 사전 조율된 개념을 가정한다. 전문 용어를 사용할 때 고려해야 할 모든 다양한 고려 사항에 대한 포괄적인 체크리스트를 제공하는 것은 불가능하다. 그러나 나는 지난 수년간 본 가장 일반적인 문제를 부각하려고 노력했으며 이것은 단지 시작점으로서의 의도이다.

이제 전자건강기록 데이터의 세부 사항으로 들어가면서 사례 연구를 살펴보자. 이 사례 연구 외에도 제4장에서 EHR에 대해 훨씬 더 깊이 논의할 것이다.

### 사례 연구: EHR 데이터
제1장에서 논의했듯이, 전자건강기록에서 나온 데이터는 매우 파편화되어 있고 복잡하다. 이 섹션에서는 Harvard와 MIT가 공개한 공개 연구 데이터셋인 중환자실 의료 정보 마트(MIMIC) 데이터셋을 소개할 것이다. 이 데이터셋은 원래 매사추세츠주 보스턴의 베스 이스라엘 디코니스 의료 센터의 중환자실 환자로 시작했다. 일반적으로 병원 전자건강기록 시스템과 관련 모듈(영상, 청구)에서 추출한 데이터이다. 현재 MIMIC 데이터의 릴리스는 MIMIC-IV이다. 그러나 MIMIC-IV 데이터셋에는 데이터 사용 계약(DUA) 없이 접근할 수 있는 네이티브 스키마 버전의 데이터가 없다. MIMIC-IV의 유일한 공개 데이터는 현재 OMOP 형식으로 존재한다. 이 책에서 OMOP 데이터 구조에 대해 어느 정도 논의하겠지만 핵심 초점 중 하나는 네이티브 데이터 모델과 OMOP 데이터 모델의 비교이다.

#### MIMIC 데이터의 복잡성
MIMIC은 Extremely 귀중한 데이터셋이며 전자건강기록에 포함된 데이터에 대한 좋은 통찰력을 제공한다. 그러나 이것이 전체 생물의학 정보학 팀의 산물이라는 것도 중요하게_NOTE해야 한다. 데이터는 자동화 및 수동 기술을 사용하여 정리, 큐레이션, 비식별화되었다. 결과적으로 EHR 데이터를 다룰 때의 많은 복잡성을 직접 경험할 수 있지만 데이터를 어느 정도 정리하고 큐레이션하는 데 이 전문가 팀의 도움도 얻고 있다. MIMIC 데이터가 어느 정도 정리되고 큐레이션되었지만 여전히 전자건강기록 데이터를 다룰 때 흔히 접하는 많은 데이터 품질 문제를 겪고 있다. ICD 코드의 맥락에서, MIMIC 문서에서 볼 수 있듯이, d_icd_diagnoses 테이블(진단 코드 데이터 포함)의 스키마에 icd9_code라는 열이 있다. 이것은 이 특정 데이터베이스의 모든 코드가 업계 전반에서 현재 ICD-10 코드를 사용하고 있음에도 불구하고 ICD-9 코드를 사용하고 있음을 알려준다.

- ICD 코드는 병원 입원 종료 시 청구 목적으로 생성된다.
- MIMIC-III의 모든 ICD 코드는 ICD-9 기반이다. 베스 이스라엘 디코니스 의료 센터는 2015년부터 ICD-10 코드를 사용하기 시작할 것이다.
- ICD-9-CM 주요 및 기타 진단 코드의 코드 필드는 V 코드를 제외한 모든 진단 코드에서 세 번째와 네 번째 숫자 사이에 소수점이 암시되는 6자리이다. V 코드의 경우 소수점은 두 번째와 세 번째 숫자 사이에 암시된다.

이것에 기반하여 코드가 청구 목적으로 생성되었으므로 환자의 실제 임상 상태를 정확하게 반영하지 못할 수 있음을(제1장에서 논의한 바와 같이) 알고 있다. 추가적으로, ICD-10 코드가 2015년까지 의료 센터 시스템에 통합되지 않았기 때문에 이 데이터셋에 ICD-9 코드만 포함되어 있음을 확인했다. 문서가 어떤 지침이 사용되었는지 또는 자동 코더가 사용되었는지에 대한 세부 사항을 다루고 있지 않지만 이 특정 코드 세트에 대해 훨씬 더 잘 이해할 수 있다. 따라서 특정 환자 코호트를 식별하려 한다면, 이러한 ICD 코드로 시작할 수 있지만 많은 거짓 음성(예: 환자가 암이 있을 수 있지만 베스 이스라엘에서의 입원이 완전히 무관한 상태 때문이었을 수 있으므로 암 서비스가 청구되지 않았기 때문에 데이터셋에 나타나지 않을 수 있음)과 거짓 양성(예: 특정 ICD 코드가 청구 목적으로 할당되었지만 환자의 현재 임상 상태를 반영하지 못할 수 있음)이 있을 것임을 알고 있다. 전자건강기록 데이터에 대해서는 제4장에서 훨씬 더 자세히 논의하겠지만 이상적으로 이 사례 연구는 EHR의 맥락에서 전문 용어와 특정 용어/코드에 대해 어떻게 생각해야 하는지를 부각했다. 이제 한 걸음 물러서서 의료에서 사용되는 몇 가지 일반적인 전문 용어를 살펴보자.

## 주요 의료 전문 용어 및 어휘 체계
UMLS는 산업 표준 제어된 어휘와 전문 용어의 모음이므로, 몇 가지 일반적으로 사용되는 것을 높은 수준에서 다룰 것이다. 이것은 다음 몇 장에서 전자건강기록 및 청구 데이터로 들어갈 때 필요한 배경을 제공할 것이다. 이 섹션에서 논의되는 시스템은 내가 가장 자주 접한 것들이지만 다음 목록은 포괄적이지 않다-분명히 다른 것들도 있다! 내가 제공하는 설명은 주로 데이터 엔지니어와 데이터 과학자로서의 데이터 해석을 안내하기 위한 것이다. 의료 서비스 제공의 전체적인 미묘함과 복잡성을 반드시 포착하지는 않는다. 예를 들어, 의료 공통 절차 코딩 시스템(HCPCS) 및 진단 관련 그룹(DRG) 코드와 같은 미국 의료 상환에 사용되는 추가 코드 시스템이 있다. 이 섹션에서는 현재 절차 전문 용어(CPT), 국제질병분류(ICD-9/10), 논리적 관찰 식별자, 이름, 코드(LOINC), RxNorm, SNOMED CT를 다룰 것이다.

### CPT (Current Procedural Terminology)
절차에 초점을 맞춘 또 다른 제어된 어휘는 미국 의료 협회가 관리하는 현재 절차 전문 용어(CPT)이다. 일반적으로 CPT 코드는 의사 및 외래 환자 서비스를 청구하는 데 사용되는 반면, ICD 코드는 궁극적으로 입원 환자 절차를 청구하는 데 사용된다. 절차 코드(약물과 유사하게)는 환자가 약물 외에 어떤 중재를 받았는지에 대한 통찰력을 제공한다. 예를 들어, 절차 코드를 살펴보면 암 환자가 종양 감소술(가능한 한 많은 종양 조직을 제거하는 수술)을 받았는지 확인할 수 있다. 때로는 어떤 절차가 수행되었는지 알면 기저 상태나 진단에 대한 감을 잡을 수 있다. 종양 감소술 예시를 계속하자-특정 코드가 58575("의료 공급자가 자궁, 자궁경부, 난소, 나팔관을 제거한 다음 복강경을 통해 또는 트로카르 포트를 통해 또는 질을 통해 전체 대망을 제거한다. 의료 공급자는 암 때문에 이 시술을 수행한다.")라면, 환자가 암이 있다고 추론할 수 있다. 그러나 암의 아형, 난소에서만 발견되었는지, 전이되었는지 등의 구체적인 세부 사항은 알 수 없다. 이러한 추론에 대해 논의할 때, 규칙 엔진은 강력한(그리고 종종 활용되지 않는) 도구이다. TypeDB와 같은 시스템에는 미리 정의된 규칙에 기반하여 새로운 노드와 관계를 추론하는 내장 추론 엔진이 있지만 규칙 엔진(예: Drools, clara-rules)을 전체 솔루션에 통합하는 것도 가능하다.

### ICD-9 및 ICD-10 (국제질병분류)
RWD를 다룰 때 접하게 될 가장 일반적인 코드 중 하나는 ICD 코드이다. 2015년부터 미국은 완전히 ICD-10으로 전환했다. 2015년 이전에는 ICD-9 코드를 사용하고 있었다. 전 세계 대부분이 ICD-10 코드를 훨씬 더 오래 사용해왔다! 의료의 대부분의 경우와 마찬가지로, 상황은 매우 빠르게 복잡해질 수 있다. "ICD-10"의 가장 순수한 의미는 ICD 코딩 시스템의 공식 유지 관리자인 세계 보건 기구(WHO)의 ICD-10 릴리스를 가리켜야 한다. WHO 버전에는 진단 코드만 포함되어 있다. 그러나 미국 의료 조직과 작업하는 경우 대부분의 경우 사람들이 "ICD-10"이라고 부르더라도 ICD-10-CM을 의미한다. -CM은 "임상 수정"을 의미하며 미국 의료 시스템에 특화된 수정을 포함한다. ICD-10-CM 외에도 절차 코드에 초점을 맞춘 ICD-10의 확장인 ICD-10-PCS도 있다. 다시 말하지만 이것은 미국 특유의 수정이며 공식 WHO 릴리스의 일부가 아니다. 역사적으로 ICD 코드는 원래 사망률 통계를 추적하기 위해 만들어졌다. 그러나 그 이후로 상환 과정뿐 아니라 일반적인 진단을 추적하기 위한 EHR에도 내재되었다. 그러나 초기 사망률 통계에 대한 초점을 감안하면, ICD 코드에는 임상의와 데이터 과학자에게 직관적이지 않을 수 있는 특정 미묘함이 있다. 예를 들어, ICD의 각 코드는 단일 위계 구조에만 속할 수 있지만(이중 계산 방지), 이것이 자궁암과 같은 질병에는 문제가 된다. 자궁암은 산부인과적 고려 사항일 뿐만 아니라 암이기도 하기 때문이다.

### LOINC (검사 및 임상 관측 표준)
또 다른 일반적인 코드 세트는 LOINC-논리적 관찰 식별자, 이름, 코드("로잉크"로 발음)-코드로 알려져 있다. 우리는 종종 LOINC 코드를 검사(예: 혈액 검사)의 관점에서 생각하지만 많은 다른 유형의 관찰을 포착하는 데 도움을 준다. 예를 들어, 설문조사 및 설문지, 심지어 영상 코드도 LOINC 내에서 이용 가능하다. LOINC 코드는 기본 검사나 관찰에 대한 상당한 양의 지식을 제공한다. 예를 들어, LOINC 코드 57698-3(직접 LDL이 포함된 지질 패널, 혈청 또는 혈장)을 살펴보자-화학 패널의 일부이고, 시점 테스트(시간 간격 대비)이며, 명시적으로 주문된 것임을 알 수 있다.

### RxNorm
RxNorm은 미국에서 이용 가능한 약물을 포착하는 미국 특유의 전문 용어이다. 국립 의학 도서관이 유지보수하며 UMLS의 일부로 제공되지만 전용 API를 통해서도 접근할 수 있다. RxNorm은 일반명과 상품명뿐 아니라 약물-약물 상호작용, 약물 분류, 국가 의약품 코드(NDC)와의 연결과 같은 기타 정보를 포함한다. RxNorm은 완전히 약물 및 관련 데이터에 초점을 맞추고 있으므로 약물의 효능, 측정 단위(5mg/mL과 같은 농도 포함)와 같은 특정 속성/속성을 가지고 있다. RxNorm이 UMLS에도 연결되어 있으므로 관련 개념 고유 식별자(CUI)와 RxNorm 용어 및 개념을 UMLS 내의 다른 소스와 연결할 수 있는 기능이 있다. 진단이나 절차 코드와 달리, 약물 코드는 사용되는 국가나 지역에 밀접하게 관련되어 있다. 이것은 약물 승인 과정의 결과이다-한 국가에서 승인된 약물이 다른 국가에서는 승인되지 않을 수 있다. RxNorm은 미국에 특화되어 있으므로, 다른 국가의 데이터로 작업하는 경우 다른 표준 및 전문 용어의 코드를 볼 수 있다. 과거에 접한 다른 두 표준은 해부학적 치료 화학(ATC) 분류 시스템과 의약품 식별(IDMP)이다.

### SNOMED CT
SNOMED CT(보통 SNOMED으로만 불리며 "스노메드"로 발음)는 의학 체계화 명명법이다. 다른 일반적으로 사용되는 전문 용어보다 훨씬 더 포괄적이며 의료 서비스 제공 및 의학(예: 진단, 절차)뿐 아니라보다 일반화된 생물의학 지식(예: 해부학, 화학, 미생물)을 다룬다. 가장 강력한 기능 중 하나는 이러한 개념이 모두 의미론적 관계로 연결되어 있어 SNOMED를 단순한 제어된 어휘보다는 온톨로지에 가깝게 만든다는 것이다. 이러한 관계는 많은 다른 유형의 개념을 연결하여 "심장과 관련된 모든 질병"과 같은 쿼리를 가능하게 한다. Extremely 강력하고 데이터 과학자로서 우리에게 certainly 도움이 되지만 SNOMED에 포함된 "지식"이 전문가 위원회에 의해 큐레이션되며 항상 따라잡고 있는 상태라는 것을 기억하는 것이 중요하다. 의학 분야, 생물학적 과정에 대한 이해부터 의학 실천 자체에 이르기까지, 끊임없이 진화하고 있다. 결과적으로, SNOMED의 콘텐츠(개념과 관계 모두)는 당신이 작업하고 있는 데이터와 완전히 정렬되지 않을 수 있다. "오, SNOMED가 있으니 이제 추가 도메인 전문가 큐레이션이 필요하지 않다"는 생각의 함정을 피하라. 추가적으로, SNOMED에는 수십만 개의 용어가 있다는 것을 명심하라! 많은 프로젝트와 데이터셋에서 내가 눈치챈 한 가지는 코드 선택이 일관성이 없거나 혼란스러워 보이는 경우가 있다는 것이다. 이것에는 많은 이유가 있다-다음 화면으로 넘어갈 수 있도록 성가신 임상의가 아무 코드나 선택하는 것부터, 비슷하게 들리는 많은 코드와 최적의 코드가 어떤 것인지 명백한 징후가 없는 것까지. 추가적으로, 이렇게 많은 용어와 다양한 전문적 배경과 경험이 있을 때, 데이터에 불일치가 있을 것은 거의 확실하다.

### SNOMED CT 활용 예시
"잘못된" 코드가 어떻게 선택될 수 있는지에 대한 한 가지 예시는 심근 경색(심장 마비)을 살펴볼 때 찾을 수 있다. 두 명의 환자가 "급성 후방 심근 경색"이라는 진단으로 입원했다고 가정하자. 첫 번째 환자에게는 누군가가 SNOMED 코드 233838001("급성 후방 심근 경색")을 선택하고 두 번째 환자에게는 다른 누군가가 57054005("급성 심근 경색")를 선택한다. 후자의 코드가 잘못된 것은 아니다; 단지 그렇게 구체적이지 않을 뿐이다. 아마도 두 번째 사람은 코딩 당시에 이 수준의 구체성을 갖지 않았거나 이 수준의 구체성을 포착할 필요가 없다고 생각했을 것이다. 어느 쪽이든, 이것은 우리에게 문제를 일으킬 수 있다. 왜냐하면 훈련된 모든 모델은 이것을 별도의 범주형 변수로 처리할 것이다. 다른 많은 코딩 시스템, 어휘, 전문 용어가 있다. 많은 건강 시스템과 의료 센터는 자체 내부 코딩 시스템도 가지고 있다. 모두를 다룰 수는 없지만 가장 일반적인 것들에 대한 기본적인 이해를 가져야 한다. 그러나 어떤 특정 전문 용어를 접하든, 다음에서 논의하겠지만 몇 가지 핵심 사항이 있다.

#### 전문 용어 활용 핵심 요약
우리는 몇 가지 일반적인 전문 용어와 코딩 시스템을 상당히 빠르게 검토하였다. 진단, 절차 또는 약물과 의약품을 표준화하려 하든, "최고의" 전문 용어는 거의 존재하지 않는다. 대부분의 경우, 특정 전문 용어 세트가 이미 채택되거나 사용된 데이터로 작업하고 있다. 그러나 실제로 전문 용어를 선택할 기회가 있다면, 상당히 도전적인 결정이 될 수 있다. 전문 용어의 표현력과 개념적 정의가 사용 사례 및 데이터와 얼마나 잘 정렬되는지의 균형을 맞춰야 한다. 라이선스도 고려 사항이다. 모든 국가와 조직이 접근할 수 있는 것은 아니기 때문이다. 또한 전문 용어를 호스팅하고 유지보수할 수 있는 조직 내 도구도 고려해야 한다. 그러나 가장 두드러진 요인 중 하나는 조직에서 이용 가능한 전문 지식 수준과 선택된 전문 용어에 대한 수용량일 수 있다.

적게 활용되는 자원 중 하나는 임상 전문 용어학자(clinical terminologists)이다. 이것은 일반적으로 생물학자부터 임상의까지 자신의 실천 도메인에서 실무 경험을 가진 주제 전문가들이다. 이러한 기반 위에서, 앞서 나열된 것과 같은 전문 용어에 대한 실무 지식도 개발한다. 직무 설명은 다양하지만 내가 본 가장 일반적인 두 가지는 다음과 같다. 일부 전문 용어학자는 개념과 그 분류체계/위계 구조를 큐레이션하는 데 뛰어나다. 다른 사람들은 전문 용어 간 데이터 매핑에 더 초점을 맞출 수 있다. 의료의 다른 실천 분야와 마찬가지로, 많은 사람들이 직함으로 임상 전문 용어학자를 가질 수 있지만 특정 전문 지식과 초점 영역은 다를 수 있다. 마지막으로, 앞서 나열된 전문 용어는 포괄적인 목록이 아니다. 상용을 포함하여 많은 다른 소스와 매핑/크로스워크가 있다. 의료 조직 자체(예: 건강 시스템 및 병원)도 내부 코딩 시스템과 전문 용어를 가지고 있는 경우가 많으며 그렇게 부르지 않을 수도 있다. 산업, 상용 또는 내부 전문 용어 간 매핑 시에도 동일한 질문과 문제가 적용된다.

이제 서로 다른 전문 용어에 대한 감을 잡았고 그것들과 작업할 때 고려해야 할 몇 가지 핵심 사항도 알았으니, UMLS로 넘어가자. 미국 국립 의학 도서관이 유지보수하며 많은 서로 다른 소스를 하나로 모아 고유 개념 식별자를 통해 연결하고 많은 개념 간의 의미론적 관계를 제공한다. 모든 문제를 해결하는 만능 해결책은 아니지만 상당히 강력하며 다음 섹션에서 어느 정도 시간을 들여 자세히 살펴볼 것이다.

## 통합 의학 언어 시스템 (UMLS) 활용
UMLS는 미국 국립 의학 도서관(NLM)이 자금을 지원하고 유지보수하는 프로젝트이며 NIH 내의 하나의 기관이다. 사람들은 UMLS를 온톨로지나 전문 용어라고 부르는 경우가 있지만 이것은 사실 정확하지 않다-UMLS는 복잡성이 다양한 전문 용어와 어휘의 모음에 가깝다. SNOMED CT와 같은 일부 소스는 온톨로지에 매우 가깝며 많은 사람들이 그것을 온톨로지로 간주할 수 있다. 그러나 반드시 공식 온톨로지에서 기대할 수 있는 엄격함을 사용하여 정의되고 유지보수되는 것은 아니다. 종종 이러한 정의는 특정 컨텍스트 내에 있으며 다른 컨텍스트나 사용 사례로 일반화될 수 있는 것은 아니다. 의료와 의료 데이터의 복잡성으로 점점 더 깊이 들어가면서 이것은 특히 중요한 지점이다. 예를 들어, "치료(therapy)"라는 개념을 살펴보자-의료에 대한 지식을 기반으로 어떻게 정의할 것인가? 대부분은 치료사와의 세션을 생각한다. 그러나 암에서 "치료"는 가장 흔히 화학요법과 같은 암 자체에 대한 특정 치료 과정을 가리킨다. 따라서 우리는 암에서 "치료" 용어를 포함하는 제어된 어휘를 개발하거나 사용할 수 있으며 그 정의와 의미는 정신과나 심리학 분야의 어휘를 사용하는 것과 매우 다를 수 있다.

의료 데이터를 다룰 때의 첫 번째 단계 중 하나는 데이터의 기저에 있는 제어된 어휘를 식별하는 것이다. 데이터가 관계형 데이터베이스(우리가 종종 구조화된 데이터와 연관시키는)에서 오든, 본질적으로 더 자유 텍스트이든, 기저에 있는 어휘가 있을 것이다. 대부분의 임상 자연어 처리(NLP) 기술은 특정 문자열과 동의어인 개념을 자동으로 식별하려는 시도이다.不幸하게도, 사용 사례에 따라 제어된 어휘가 아직 명확하게 정의되지 않았을 수 있으며,你在Implicit하게 하나를 정의하는 임무를 맡게 된다. 데이터 과학자들은 새로운 데이터셋을 받을 때 이것을 항상 한다. 우리는 특정 데이터 열을 취하고 그것이 어떤 종류의 범주형 데이터를 포함하고 있음을 식별한다. 탐색적 데이터 분석(EDA) 단계의 일부로서, 특정 데이터 열에 포함된 고유/유니크 값을 살펴볼 것이다. 다른 열을 살펴봄으로써 데이터의 의미를 해독하는 데 추가 컨텍스트를 추가할 수 있다. 이 과정은 본질적으로 이 특정 데이터셋에 대한 제어된 어휘를 만드는 것이다. 때로는 데이터 열에 "ICD-10 코드를 포함한다"거나 "약물을 포함한다"고 알려져 있어 시작점을 제공받는다. 이상적으로, 소스 데이터는 산업 표준 제어된 어휘를 사용했다. 그러나 당신의 일이 끝났다고 안심하지 마라. 산업 표준 어휘에도 불구하고 주변 컨텍스트는 코드가 어떻게 해석되어야 하는지에 상당한 영향을 미칠 수 있다. 예를 들어, 약물 정보를 포착하기 위해 RxNorm(UMLS 내의 일반적으로 사용되는 소스) 코드를 사용하는 데이터셋을 접할 수 있다. 예를 들어, "이부프로펜 200mg(예: Advil이나 Motrin)"을 살펴보자. 이 약물의 RXCUI 코드는 316074이다. 약물의 기반 코딩 시스템으로 RxNorm을 사용한다고 명확하게 말하는 데이터셋과 medications이라는 열이 있을 수 있다. 이것이 환자의 의료 이력의 일부인 약물, 현재 처방된 약물 목록, 또는 환자가 구매하는 약물인가? 이러한 종류의 컨텍스트는 환자 수준 데이터를 해석하고 분석하는 데 매우 중요하며 UMLS로 해결되지 않는다.

UMLS에 너무 깊이 들어가기 전에, UMLS를 더 쉽게 논의할 수 있는 몇 가지 핵심 용어와 정의를 다루자.

### UMLS 핵심 개념 및 식별자
UMLS를 더 깊이 다루고 사용하는 방법을 논의하기 전에, 몇 가지 기본 정의를 다루자. 추가 세부 사항은 UMLS 용어집에서 찾을 수 있다.

아톰(Atom)
아톰은 UMLS 내에서 가장 근본적인 의미 블록이다. 각 아톰은 UMLS 내의 단일 소스 맥락에서 하나의 개념을 나타낸다. 일반적으로 아톰은 문자열 레이블, 코드, 소스의 튜플로 생각할 수 있다. 따라서 서로 다른 소스(예: ICD-9 대 ICD-10)에서 동일한 문자열 레이블(및 개념)은 각각 고유한 아톰 고유 식별자(AUI)를 가진 별도의 아ATOM으로 처리된다(개념과 아톰의 비교는 참조).

아톰 고유 식별자(AUI)
AUI는 숫자 앞에 문자 A가 붙은 식별자이다. CUI와 달리, UMLS 내의 여러 서로 다른 소스에 걸쳐 있는 동일한 개념은 서로 다른 AUI를 갖는다. 결과적으로, AUI를 볼 때 본래적인 의미는 없다. 그들의 주요 기능은 UMLS 전반에 걸쳐 기본키-외부키 관계를 제공하는 것이다.

개념(Concept)
개념은 UMLS의 핵심에 있으며 텍스트 표현의 소스에 관계없이 단일 의미를 나타낸다. 동일한 개념은 UMLS 내의 여러 소스에 걸쳐 존재할 수 있다-아톰과 AUI는 소스 간에 다르지만 아톰이 동일한 개념을 나타내는 경우 모든 것이 동일한 CUI를 공유할 것이다. 의 예시에서 두 아톰은 ICD-9와 ICD-10에 걸쳐 동일한 개념을 나타내므로 공통 CUI를 공유한다.

#### CUI (Concept Unique Identifier)
CUI는 숫자 앞에 문자 C가 붙은 식별자이며 각 CUI는 고유하고 동의어 개념은 동일한 CUI를 공유한다. 기본 소스나 텍스트 표현에 관계없이, CUI를 공유하는 두 개념은 의미상 동등한 것으로 간주된다.

소스(Source)
UMLS는 어휘, 전문 용어, 온톨로지의 모음이며 각각은 소스로 지칭된다. 따라서 UMLS를 소스와 그들 사이의 매핑 및 관계의 모음으로 생각할 수 있다. 개념 수준에서의 매핑 외에도, UMLS는 텍스트/단어 수준에서 작동하는 어휘 변형 생성기와 같은 추가 도구를 가지고 있다. 이러한 아이디어와 정의는 UMLS에 포함된 기능의 일부만 포착한다. 강력하지만, 그 결과로 상당히 복잡하며, 이것이 UMLS에 대한 첫 노출인 경우 압도적일 수 있다. 이것들은 시작하는 데 도움이 되고 UMLS를 좀 더 자세히 탐구하면서 기본적인 어휘를 제공할 것이다. 이 두 아이디어, 개념과 개념 고유 식별자(CUI)는 UMLS와 작업하는 데 핵심적이다. 다음은 "개념 지향"이라는 이 아이디어에 대한 더 깊은 탐구-단순히 코드나 용어가 아니라 기본 개념에 초점을 맞추는 것이다.

개념 지향(Concept Orientation)
우리가 제어된 어휘, 전문 용어 또는 온톨로지를 사용하든, 공통 주제 중 하나는 레이블이나 용어가 아니라 기본 개념에 초점을 맞추려는 것이다. 예를 들어, "심장 마비"라고 부르든 "심근 경색"이라고 부르든 ICD-10 코드 "I21"이라고 부르든, 우리는 심장의 관상 동맥이 막혀 심장 근육과 조직이 괴사하는 의학적 상태를 가리킨다. UMLS 내에서 이것은 개념 고유 식별자를 사용하여 포착되며 이는 무작위로 생성된 숫자 코드로 문자 C가 앞에 붙는다. 각 CUI는 UMLS 내의 특정 소스에서의 개념 항목에 해당하는 여러 아톰에 의해 표현될 수 있는 특정 개념을 나타낸다. UMLS와 작업할 때 기억해야 할 핵심은 CUI의 사용을 중심으로 한다는 것이다. 그들을 나타내는 아톰이나 문자열에 관계없이, 동등한 개념은 서로 다른 소스 전반에 걸쳐 동일한 CUI를 공유할 것이다. 다시 말해, UMLS 내의 서로 다른 원어 전문 용어에서 온 두 용어나 개념이 동일한 CUI를 공유하면, 그것들은 동의어로 간주된다. 그럼에도 불구하고 "동등"이나 "동의어"라는 개념이 사용 사례에 크게 의존할 수 있음을 기억하는 것이 중요하다. 두 개념은 하나의 관점에서는 동등할 수 있지만 다른 관점에서는 아닐 수 있다.

UMLS에 대한 기본적인 이해를 얻었으니, UMLS를 선택한 데이터베이스에 실제로 통합하기 전에 몇 가지 기본 사항을 먼저 다뤄야 한다.

## UMLS 데이터 처리 및 로딩 실습
UMLS에 접근하려면 라이선스를 신청해야 한다. 등록 및 신청 링크를 방문하기 바란다. 규모와 복잡성을 감안하면, 사람들이 UMLS를 사용하고 작업하는 몇 가지 서로 다른 방법이 있다.

### MetamorphoSys를 활용한 UMLS 서브셋 추출
가장 유연하지만 어떤 소스를 포함할지 결정한 다음 MetamorphoSys 도구를 사용하여 관심 있는 하위 집합을 생성해야 하므로 학습 곡선이 더 높다. 이것이 끝나면 여전히 데이터베이스某种(에 추출, 변환, 로드(ETL)하거나 원시 RRF 파일에 직접 접근해야 한다. 하위 집합에 포함할 소스에 따라 결과 RRF 파일은 상당히 클 수 있다. 어떤 개념을 로드해야 할지 모르기 때문에 이 접근 방식을 사용하여 전체 소스를 다운로드하는 경우가 많다. 예를 들어, 모든 연구 정보학을 지원하는 학술 의료 센터의 정보학과는 다른 연구자들이 사용할 수 있는 개념을 예측할 수 없다. 이 상황에서는 거의 모든 연구 질문을 지원할 수 있도록 하기 위해 초기 번거로움이 가치 있을 수 있다.

### UMLS REST API 활용
전체 소스를 다운로드할 필요가 없거나 원하지 않는 경우, REST API는 매우 매력적인 대안이 된다. 간단한 REST API이므로 MetamorphoSys와 상호작용하는 방법을 배우고 RRF 파일을 파싱하고 ETL할 필요가 없다. 적절한 API 엔드포인트를 쿼리하고 다른 API와 마찬가지로 JSON 결과를 파싱할 것이다. 이 API 접근 방식은 제한된 개념 세트로 작업하고 있을 때 훌륭하다. 예를 들어, 다음 장에서는 MIMIC-III 데이터셋의 공개 하위 집합으로 작업할 것이다. UMLS에서 수십만 개의 개념을 다운로드하는 대신, MIMIC-III 데이터를 약간 빠르게 처리하여 고유한 코드 목록을 추출할 수 있다. 그런 다음 이 코드를 API에서 조회하고 개념을 선택한 데이터베이스에 로드하거나 나중에 프로젝트에서 사용할 수 있는 CSV 파일로 내보낼 수 있다. UMLS REST API와 작업할 때, 서비스와 먼저 인증해야 한다. 미국 보건복지부에서 인증 방법에 대한 샘플 코드를 제공하고 있다. 이 코드를 사용하려면 예시 3-1과 유사하게 API 키를 전달하여 Authentication 클래스의 인스턴스를 만들면 된다.

예시 3-1. UMLS REST API 인증

```python
import json
import os
import requests

# 환경 변수 'UMLS_APIKEY'에 키가 포함되어 있다고 가정
umls = Authentication(os.environ.get("UMLS_APIKEY"))
tgt = umls.gettgt()
ticket = umls.getst(tgt)
```

#### API 연동 파이썬 예제
제2장의 데이터베이스 쿼리에서 처음 본 것처럼, 이 책 전체에서 논의되는 주제와 관련된 가장 두드러진 세부 사항을 포착하는 작은 코드 조각이 있다. 이 조각들은 대표적이며 당신의 시스템에서 실행하거나 필요에 맞게 조정할 수 있는 예시로 사용하기 위한 것이다. 이 책의 예시와 함께 당신의 시스템에서 실행할 수 있는 코드는 이 책과 관련된 저장소를 확인하기 바란다.

UMLS와 작업하는 것에 대한 더 구체적인 예시를 제공하기 위해, 실제 데이터를 RxNorm(UMLS 내의 소스/전문 용어 중 하나)과 연결하는 핵심 단계를 부각하는 작은 연습을 진행할 것이다. 자, 시작하자. 높은 수준에서 우리가 할 일은 다음과 같다:

1. MIMIC-III 공개 데이터셋에서 PRESCRIPTIONS.csv 파일을 읽는다.
2. 고유한 NDC 코드 목록을 추출한다.
3. 각 NDC 코드에 대해, NDC 코드 자체와 함께 관련된 여러 문자열 레이블을 유지한다.
4. 각 NDC 코드에 대해 UMLS REST API를 쿼리하고 UMLS에 따라 RxCUI와 개념 이름을 다운로드한다. 이 단계가 가장 오래 걸린다는 점에 유의하라. 각 코드에 대해 서버에 별도의 호출을 하기 때문이다. 이를 위해 multiprocess 라이브러리와 같은 것을 사용하여 작업을 병렬화한다.

pandas 라이브러리에 익숙하지 않다면, CSV 파일을 읽는 것은 Extremely 간단하다. 예시 3-2는 CSV 파일을 읽은 다음 데이터를 약간 필터링하는 몇 줄을 포함한다. 구체적으로, 고유한 NDC 코드를 가진 행만 유지하고 몇 개의 열만 유지한다. 본질적으로 고유한 NDC 코드 목록과 관련 열을 추출하고 있다.

예시 3-2. PRESCRIPTIONS.csv 읽기

```python
# 가정:
# import pandas as pd
data = pd.read_csv("mimic_1.4_demo/PRESCRIPTIONS.csv", dtype=str)
unique_rx = data.drop_duplicates(subset=['ndc']).filter(items=[
    'drug', 'drug_name_poe', 'drug_name_generic', 'ndc'
])
```

이제 고유한 NDC 코드 목록과 몇 개의 추가 열을 포함하는 데이터프레임이 있으므로, 각 NDC 코드에 대해 UMLS API를 쿼리하자. 예시 3-3은 API를 쿼리하는 데 사용할 수 있는 일련의 함수를 포함한다. 이것을 unique_rx 데이터프레임의 모든 행에 적용해야 한다. RxNorm API 자체는 UMLS 인증이 필요하지 않다는 점에 유의하라. 국립 의학 도서관이 자유롭게 제공하는 데이터를 반환하기 때문이다.

예시 3-3. 단일 코드에 대한 RxNorm API 쿼리 함수

```python
import requests

def rxcui_from_ndc_status(ndc_resp):
    if ndc_resp[1]:
        return ndc_resp[1]['ndcStatus']['rxcui']
    else:
        return 'MISSING RXCUI'

def conceptName_from_ndc_status(ndc_resp):
    if ndc_resp[1]:
        return ndc_resp[1]['ndcStatus']['conceptName']
    else:
        return 'MISSING NAME'

def get_NDC_status(ndc):
    rxnorm_base = "https://rxnav.nlm.nih.gov/REST"
    uri = rxnorm_base + "/ndcstatus.json"
    query = {'ndc': ndc}
    r = requests.get(uri, params=query)
    r.encoding = 'utf-8'
    if r.status_code == 200:
        body = json.loads(r.text)
    else:
        # 오류를 다른 방식으로 처리하고 싶을 수 있음
        body = {}
    return r.status_code, body

ndc_code = "00006494300"  # rxcui 1658472를 반환해야 함
status = get_NDC_status(ndc_code)
rxcui = rxcui_from_ndc_status(status)
name = conceptName_from_ndc_status(status)
```

코드를 약간 더 읽기 쉽게 만들기 위해 단계를 별도의 함수로 나눴지만 필요에 따라 최적화할 수 있다. NDC 상태 세부 사항을 가져오는 RxNorm API 문서는 좋은 자료이다.

API를 통해 UMLS와 상호작용하는 방법을 보았으니, 다른 옵션은 하위 집합을 추출하여 데이터베이스에 로드하는 것이다. UMLS 하위 집합을 만드는 것에 대해서는 자세히 다루지 않겠지만 NLM은 매우 좋은 문서를 제공하고 있다.

#### MetamorphoSys 출력 파일 구조
UMLS 다운로드를 풀면 MetamorphoSys 도구가 포함된 mmsys.zip이라는 zip 파일이 있을 것이다. 일부 도구는 파일이 풀리는 mmsys라는 하위 디렉토리를 만든다. 파일이 하위 디렉토리에 풀리지 않고 대신 최상위 UMLS 디렉토리에 있는지 확인하라. 다시 말해, *.nlm과 run*_ 파일은 모두 동일한 디렉토리에 있어야 한다. 하위 집합을 만들면, 파이프(|) 문자로 구분된 일련의 리치 릴리스 형식(RRF) 파일이 생성될 것이다. UMLS는 SQL 데이터베이스로의 로딩에 대한 기본적인 지원을 제공하지만 다른 것은 제공하지 않는다. 다음 몇 섹션에서는 UMLS RRF 파일을 다양한 데이터베이스로 가져오는 과정을 안내하고 잠재적인 기회와 트레이드오효를 살펴볼 것이다. 각 데이터베이스에 대해 간략한 소개를 제공한 다음 로딩하고 쿼리하는 방법을 안내할 것이다. 자, 관계형/SQL 데이터베이스부터 시작하자.

### UMLS와 관계형 데이터베이스 (RDBMS)
RRF 파일 구조는 본래적으로 관계형 데이터베이스에 적합하며 UMLS는 데이터를 MySQL 및 Oracle 데이터베이스에 로드하기 위한 스크립트를 제공한다. 이것은 PostgreSQL이나 선택한 데이터베이스와 같은 다른 SQL 데이터베이스로 쉽게 적용될 수 있다. 하위 집합의 크기와 포함된 전문 용어 소스의 수에 따라 데이터베이스 관리 팀과 협력하여 데이터베이스가 적절히 튜닝되도록 하려 할 것이다. 기본 옵션은 성능 문제로 이어질 가능성이 높지만 이것은 궁극적으로 특정 워크로드 세부 사항에 따라 달라진다. 자, UMLS 하위 집합으로 MySQL 인스턴스를 로드하는 것으로 시작하자.

#### MySQL에 UMLS 데이터 로드하기
간단하게 하기 위해 여기서 MySQL을 사용할 것이다. UMLS가 테이블과 인덱스를 만들기 위한 DDL 파일을 제공하기 때문이다. Oracle 데이터베이스도 지원한다. 먼저 예시 3-4와 유사한 것을 사용하여 Docker 인스턴스를 만들어야 한다.

예시 3-4. MySQL Docker 인스턴스 만들기

```bash
docker run --name umls-mysql \
  -e MYSQL_ROOT_PASSWORD=umls \
  -p3306:3306 \
  -v $PWD/umls-mysql:/var/lib/mysql \
  -d \
  mysql:5.7
```

#### Docker 기반 MySQL 연결
예시 3-4와 유사한 명령을 사용하여 명령줄에서 MySQL Docker 인스턴스에 연결하는 경우, -h localhost 대신 -h 127.0.0.1을 사용하여 mysql 클라이언트가 소켓 대신 TCP를 사용하도록 강제해야 한다. MySQL 인스턴스를 만들었으면, MetamorphoSys가 만든 UMLS 하위 집합의 META 디렉토리에 있는 제공된 populate_mysql_db.sh 스크립트를 호출할 수 있어야 한다. 이 과정은 시간이 걸릴 수 있다-내 시스템에서는 약 2.5시간이 걸렸다.

데이터가 로드되면 UMLS를 어떻게 쿼리할 수 있는지 살펴보자. 핵심은 서로 다른 테이블과 열의 미묘함을 이해하는 것이다. UMLS 참조 매뉴얼은 좋은 자료이다.

#### MySQL에서의 UMLS SQL 쿼리 실습
"헤파린" 문자열을 포함하는 모든 개념을 쿼리하고 싶다고 가정하자. 모든 개념과 아톰 및 관련 문자열을 포함하는 MRCONSO 테이블을 쿼리하는 것으로 시작할 것이다. 주의할 점은 MRCONSO 테이블에 다른 언어의 것들을 포함하여 개념과 관련된 모든 아톰이 포함되어 있다는 것이다. 따라서 쿼리에 LAT = "ENG"를 포함하도록 하려 할 것이다. 우리는 종종 특정 용어에 신경 쓰지 않는다. 기본 개념을 찾고 있기 때문이다. CUI로 GROUP BY하여 개념 고유 식별자별로 모든 결과를 집계할 수 있다. 예시 3-5가 이것을 모두 종합한다.

예시 3-5. 간단한 UMLS SQL 쿼리

```sql
SELECT c.CUI, MIN(c.STR)
FROM MRCONSO c
WHERE LAT = "ENG" AND c.STR LIKE "%heparin%"
GROUP BY CUI;
```

이 쿼리는 꽤 많은 항목을 반환할 것이다. 결과를 필터링하여 특정 의미 유형에 해당하는 개념만 얻고 싶다면? 제4장에서 이 예시를 훨씬 더 깊이 다루겠지만 헤파린의 약리학적 물질(임상 약물, 치료 또는 기타 의미 유형 대비)인 것만 원한다고 가정하자. 예시 3-6에서 보듯이 다른 테이블과 조인하고 해당에 따라 필터링해야 한다.

예시 3-6. 개념과 의미 유형을 조인하는 UMLS SQL 쿼리

```sql
SELECT c.CUI, MIN(c.STR), MIN(s.TUI), MIN(s.STY)
FROM MRCONSO c
LEFT JOIN MRSTY s on c.CUI = s.CUI
WHERE c.LAT = "ENG" AND c.STR LIKE "%heparin%" AND s.TUI = "T121"
GROUP BY c.CUI;
```

우리는 쿼리를 의미 유형 고유 식별자(TUI)가 "T121"인 개념만 포함하도록 좁힌다. 이것은 결과를 "약리학적 물질"인 개념만 포함하도록 좁힐 것이며 이것은 UMLS에서 다음과 같이 정의된다: 병리학적 장애의 치료 또는 예방에 사용되는 물질. 여기에는 신체에서 자연적으로 발생하며 치료적으로 투여되는 물질이 포함된다. 의미 유형을 지정하지 않았다면, "임상 약물"과 같은 다른 의미 개념도 일치할 것이다. 이것은 다음과 같이 정의된다: 제조사에 의해 생산된 의약품 제제. 이름은 일반적으로 물질, 효능, 형태를 포함하지만 물질과 다른 두 항목 중 하나만 포함할 수도 있다.

이것은 이전 것과 상당히 비슷하게 들리지만 기본 물질 대 제조된 약물이라는 미묘한 차이가 있다. 이것이 임상 전문 용어학자가 팀에게 매우 귀중한 자산이 되는 곳이다. 그들은 다양한 임상 전문 용어의 미묘함과 용어 및 개념이 어떻게 정의되는지를 탐색하는 훈련을 받았거나 경험을 가지고 있다.

#### 임상 전문용어학자의 역할
전문 용어학자는 구조화된 코드나 용어가 포함된 생물의학 데이터를 다룰 때 중요한 자원이다. 그들은 도메인 지식과 전문 용어 내의 특정 미묘함, 그리고 용어와 개념이 어떻게 정의되는지 사이의 간극을 메우는 데 도움을 준다. 일부 전문 용어학자는 생물학이나 생리학에 대한 과학적 배경을 가지고 있는 반면, 다른 사람들은 임상적 배경을 가지고 있다. 후자는 종종 임상 전문 용어학자라고 불리며 기본 질병 과정과 그 치료 및 중재에 대한 지식을 결합한다.

그렇다면, 이제 "헤파린" 문자열을 포함하지만 "약리학적 물질"인 개념만 포함하도록 결과를 필터링했다. 이전 섹션은 SQL 데이터베이스를 사용하여 UMLS 하위 집합을 로드하고 쿼리하는 것이 상당히 straightforward했지만 UMLS 데이터를 처리하고 조작하여 Neo4j나 TypeDB와 같은 다른 유형의 데이터베이스에 로드하는 데 도움이 되도록 하고 싶을 수 있다. 다음 섹션에서는 다른 데이터베이스로 가져오기 위한 UMLS 전처리를 살펴볼 것이다.

### 그래프 DB 로드를 위한 UMLS 전처리
UMLS 하위 집합의 RRF 파일의 크기와 복잡성을 감안하면, 텍스트 파일로 직접 작업하는 것은 번거롭고 느리다. 이 섹션에서는 UMLS를 SQLite 데이터베이스로 가져온 다음 해당 데이터베이스에 대한 쿼리를 실행하여 선택한 데이터베이스로 가져올 수 있는 CSV 파일을 생성하는 오픈소스 프로젝트를 살펴볼 것이다. 나는 원래 clinical_informatics_umls 저장소의 포크를 만들어 코드를 약간 더 이식 가능하게 만들고 Neo4j뿐 아니라 다른 것들도 지원하도록 약간의 변경을 했다. 저장소를 클론한 후 Python 가상 환경을 설정해야 한다. 나는 개인적으로 virtualenvwrapper를 사용한다. 가상 환경은 종종 충돌하는 서로 다른 Python 모듈 세트를 관리할 수 있게 해주므로 강력히 권장한다. 그렇지 않으면 필요한 의존성이 시스템에 이미 있는 것과 충돌할 위험이 있다.

가상 환경을 설정했으면 clinical_informatics_umls 저장소로 변경하고 poetry를 설치한 다음 예시 3-7에 표시된 대로 프로젝트를 초기화해야 한다. Python 버전 3.8.x를 실행하고 있고 mariadb가 설치되어 있어야 한다(또는 최소한 Python 라이브러리에서 필요한 의존성이 있어야 한다). Mac에서 Homebrew를 사용하는 경우 brew install mariadb이면 된다.

예시 3-7. clinical_informatics_umls 프로젝트 설정

```bash
# 디렉토리로 변경
cd clinical_informatics_umls

# 가상 환경에 poetry 설치
# 이미 가상 환경을 활성화했다고 가정
pip install poetry

# 프로젝트 초기화
poetry install
```

프로젝트가 초기화되면 하위 집합 디렉토리의 위치를 알고 있는지 확인한 다음, 많은 스크립트 중 첫 번째인 create_sqlite_db.py에 전달해야 한다. 이것은 새로운 SQLite 데이터베이스를 만들고 UMLS RRF 파일로 로드할 것이다.

예시 3-8. SQLite 로딩 및 CSV 파일 생성

(UMLS를 SQLite에 로드하고 CSV로 변환하는 과정이 이어진다)

```bash
# 프로젝트 디렉토리의 최상위에서 실행
# <db dir>: sqlite db가 저장될 디렉토리
poetry run python clinical_informatics_umls/create_sqlite_db.py <UMLS 하위 집합 디렉토리 경로> <db dir>

# <csv dir>: csv 파일이 생성될 디렉토리
poetry run python clinical_informatics_umls/neo4j.py <db dir> <csv dir>
```

이전 예시는 Neo4j로 쉽게 가져올 수 있는 CSV 파일을 생성하는 neo4j.py 스크립트를 사용한다. 다른 데이터베이스에 대한 지원은 GitLab 저장소에 포함되어 있으며 그렇지 않다면 프로젝트를 자유롭게 확장하기 바란다! CSV 파일을 생성하는 데 사용되는 SQL 쿼리를 실행할 때 RRF 파일에 적용할 수 있는 특정 필터에 대해서는 논의하지 않을 것이다. UMLS는 특정 사용 사례에 맞게 조정될 수 있으므로 완전한 문서는 참조 매뉴얼을 확인하기 바란다.

#### UMLS 전처리 SQL 쿼리
CSV 파일을 생성하는 데 사용되는 SQL 쿼리는 반드시 특정 사용 사례에 최적화된 것은 아니다. UMLS의 모든 것을 포함하지 않는 한(대부분 사용하지 않을 가능성이 높다), 프로젝트에 필요한 정확한 데이터를 추출하는 SQL 쿼리 세트를 만드는 것은 불가능하다. 이 장에서 생성된 CSV 파일이 사용 사례에 적합할 것이라고 가정하지 마라. UMLS와 작업하는 방법을 부각하기 위한 것일 뿐이다.

이러한 전처리된 파일은 이제 선택한 데이터베이스로 가져올 수 있다. 다음 섹션에서는 UMLS를 Neo4j 속성 그래프 데이터베이스로 가져오는 과정을 안내할 것이다.

### UMLS와 속성 그래프 데이터베이스
UMLS 하위 집합은 관계형 데이터 파일 세트로 제공되지만 UMLS에 포함된 정보는 단순한 테이블 시리즈보다는 그래프에 가깝다. 시맨틱 네트워크를 사용할 때 더욱 그렇다. 빠른 검토로(제2장의 초기 소개에 이어), 속성 그래프 데이터베이스는 지난 10년 정도 동안 점점 인기를 얻고 있는 그래프 데이터베이스의 한 유형이다. SQL 데이터베이스가 테이블 수준에서 데이터를 연결하는 반면, 속성 그래프는 노드 수준에서 데이터를 연결할 수 있게 해준다. 개념적으로, 레이블(Neo4j 용어 사용)은 SQL 테이블과 유사하며 레이블의 각 노드는 테이블의 행이 될 것이다. 관계는 조인 테이블과 유사하게 작동하지만 개별 노드를 연결한다. 속성은 각 테이블 내의 열과 동일하며 노드와 관계 모두 속성을 가질 수 있다. 그래프를 사용하여 데이터를 모델링하는 것은 일반적으로 사용 사례의 관점에서 수행되며 데이터를 어떻게 표현하는 것이 가장 좋은지 결정하기 위해 작업한다. 다시 말해, 데이터로 어떤 질문에 답하려 하는지, 그리고 이 질문에 가장 잘 답하기 위해 데이터를 어떻게 표현하는가이다. 이 섹션(그리고 하이퍼그래프를 포함하는 다음 섹션)의 목적을 위해, 그래프 스키마를 UMLS에 최대한 가깝게 유지하고 있다. 그래프를 사용 사례에 통합하기로 결정하고 UMLS를 사용할 계획이라면, 사용 사례의 필요에 따라 그래프 스키마를 간소화하고 단순화할 수 있는지 확인하기 위해 시간을 투자하는 것이 좋을 것이다.

자, 시작하자. UMLS를 Neo4j에 로드하기 시작할 것이다. 이전 섹션의 전처리 단계를 따라 생성된 CSV 파일을 사용할 것이다.

#### Neo4j에 UMLS 데이터 로드하기
이전 코드를 실행했다면, <csv dir>에 이제 Neo4j에 로드할 수 있는 일련의 CSV 파일이 포함되어 있어야 한다. 먼저 예시 3-9에 표시된 대로 Docker에서 Neo4j 인스턴스를 시작해야 한다.

예시 3-9. Docker에서 Neo4j 시작

```bash
docker run \
  --name umls-neo4j \
  -p7474:7474 -p7687:7687 \
  -d \
  -v $PWD/neo4j/data:/data \
  -v $PWD/neo4j/logs:/logs \
  -v $PWD/neo4j/import:/var/lib/neo4j/import \
  -v $PWD/neo4j/plugins:/plugins \
  --env=NEO4J_dbms_memory_heap_initial__size=8g \
  --env=NEO4J_dbms_memory_heap_max__size=8g \
  --env=NEO4J_AUTH=neo4j/test \
  --env NEO4J_apoc_import_file_enabled=true \
  --env=NEO4JLABS_PLUGINS='["apoc", "graph-data-science", "n10s"]' \
  neo4j:4.4-community
```

Neo4j 인스턴스가 실행되면, 예시 3-10에 표시된 대로 neo4j-admin 도구를 사용하여 데이터를 가져오는 것이 한 옵션이다.

예시 3-10. CSV 파일을 Neo4j로 가져오기

```bash
./bin/neo4j-admin import \
  --database=neo4j \
  --nodes='import/semanticTypeNode.csv' \
  --nodes='import/conceptNode.csv' \
  --nodes='import/atomNode.csv' \
  --nodes='import/codeNode.csv' \
  --relationships='import/has_sty_rel.csv' \
  --relationships='import/has_aui_rel.csv' \
  --relationships='import/has_cui_rel.csv' \
  --relationships='import/tui_tui_rel.csv' \
  --relationships='import/concept_concept_rel.csv' \
  --relationships='import/child_of_rel.csv' \
  --relationships='import/cui_code_rel.csv' \
  --skip-bad-relationships=true \
  --skip-duplicate-nodes=true \
  --trim-strings=true
```

이 접근 방식의 두 가지 주요 단점 또는 문제가 있다:
- 가져오기 중에 데이터베이스를 중지해야 한다. 이를 완화하려면 동일한 Neo4j 디렉토리/공유 볼륨을 가리키는 동안 neo4j-admin을 실행하는 자체 Docker 컨테이너를 만들어야 한다.
- 데이터베이스가 새로 사용되지 않아야 한다. 이것은 우리가 UMLS를 완전히 새로운 Neo4j 인스턴스에만 로드할 수 있음을 의미하기 때문에 더 큰 문제이다. 그러나 UMLS와 결합하고 싶은 기존 그래프가 있다면 대안을 찾아야 한다.

한 가지 대안은 Neo4j의 APOC 애드온을 사용하는 것이다. 예시 3-11에 표시된 대로 추가 기능을 포함하고 있다. apoc.import.csv() 함수는 admin import 함수와 유사한 의미를 가지지만 라이브 데이터베이스에서 실행할 수 있다.

예시 3-11. APOC를 사용하여 CSV 파일을 Neo4j로 가져오기

```cypher
CALL apoc.import.csv(
  [
    {fileName: 'file:/semanticTypeNode.csv', labels: ['test', 'UMLS']},
    {fileName: 'file:/conceptNode.csv', labels: ['test', 'UMLS']},
    {fileName: 'file:/atomNode.csv', labels: ['test', 'UMLS']},
    {fileName: 'file:/codeNode.csv', labels: ['test', 'UMLS']}
  ],
  [
    {fileName: 'file:/has_sty_rel.csv', type: ''},
    {fileName: 'file:/has_aui_rel.csv', type: ''},
    {fileName: 'file:/has_cui_rel.csv', type: ''},
    {fileName: 'file:/tui_tui_rel.csv', type: ''},
    {fileName: 'file:/concept_concept_rel.csv', type: ''},
    {fileName: 'file:/child_of_rel.csv', type: ''},
    {fileName: 'file:/cui_code_rel.csv', type: ''}
  ],
  {}
)
```

주의할 점은 CSV 파일에 각 노드에 첨부된 Neo4j 레이블(제2장에서 소개한)이 포함되어 있다는 것이다. 그러나 apoc.import.csv() 함수는 레이블도 지정해야 한다. 이 레이블은 CSV 파일에 지정된 것 외에 추가로 할당된다. 이 추가 레이블을 사용하여 가져온 노드를 "태그"하는 것이 도움이 된다는 것을 알았다. 시간 스탬프나 필요에 맞는 다른 식별자를 사용할 수 있다.

UMLS를 Neo4j에 로드했으니, 속성 그래프에서 UMLS와 상호작용하는 방법을 더 잘 이해하기 위해 몇 가지 쿼리를 시도해 보자.

#### Neo4j Cypher를 활용한 UMLS 쿼리 실습
이 섹션에서는 몇 가지 쿼리를 안내할 것이다. 명심해야 할 한 가지는 UMLS 네트워크가 상당히 복잡하며 사용 사례에 효과적으로 통합하기 위해 어느 정도 실험이 필요할 것이라는 것이다. 서로 다른 의미 유형, 개념이 어떻게 정의되는지, 또는 다양한 요소가 어떻게 연결되는지를 살펴볼 때 많은 미묘함이 있다.

예시 3-12에서, SQL 예시에서 이전에 한 것처럼 "헤파린" 문자열을 포함하는 아톰과 관련 개념을 검색하는 것으로 시작하는 Cypher 쿼리가 있다. 결과를 더 좁히기 위해, TUI가 "T121"인 의미 유형에 연결된 개념을 원한다고 다시 지정한다.

예시 3-12. 헤파린에 대한 UMLS Neo4j 쿼리

```cypher
MATCH (a:AUI)-[:HAS_CUI]->(c:Concept)-[:HAS_STY]->(t:TUI {TUI: "T121"})
WHERE a.STR contains "heparin"
RETURN c, t
```

보듯이, 데이터 모델이 상당히 유사함에도 불구하고 이 쿼리는 SQL 버전보다 훨씬 더 간결하다. 쿼리를 DIRECT_SUBSTANCE_OF 관계를 따라가도록 변경하면, "치료 또는 예방 시술"이라는 정의를 가진 몇 가지 개념에 더 연결되어 있음을 알게 될 것이다: 질병이나 장애를 예방하거나 신체 기능을 개선하도록 설계된 시술, 방법 또는 기술, 또는 질병이나 부상을 치료하는 과정에서 사용되는 것. 이전 SQL 예시에서 그렇게 하지 않았지만 예시 3-13은 헤파린 약물과 관련된 치료를 제공하는 쿼리를 포함한다. 이것은 쿼리에 그래프 순회를 하나 더 추가하는 것만큼 간단했지만 SQL 버전은 아톰 간의 관계를 포함하는 MRREL 테이블과의 추가 조인이 필요할 것이다.

예시 3-13. 관계에 대한 UMLS Neo4j 쿼리

```cypher
MATCH (a:AUI)-[:HAS_CUI]->(c:Concept)-[:HAS_STY]->(t:TUI {TUI: "T121"}),
      (c)-[:DIRECT_SUBSTANCE_OF]->(c2)
WHERE a.STR contains "heparin"
RETURN c, c2, t
```

그래프 순회를 통해 특정 물질을 치료제로서의 용도와 연결하는 UMLS의 강점을 보기 시작할 수 있다. 이를 통해 그래프 순회 기반의 탐색이 가능해집니다.

Neo4j에서 UMLS와 작업하는 것에 대한 간략한 소개를 받았으니, 하이퍼그래프 데이터베이스인 TypeDB에서 어떻게 하는지 살펴보자.

### UMLS와 하이퍼그래프 데이터베이스 (TypeDB)
우리의 마지막 고려 사항은 제2장에서 소개된 하이퍼그래프의 기능도 제공하는 강력한 타입의 데이터베이스인 TypeDB이다. 빠른 검토로, 하이퍼그래프에 대해 생각하는 가장 간단한 방법은 다른 그래프와 유사하지만 엣지가 임의의 수의 정점을 연결할 수 있는(또는 임의의 수의 노드를 연결하는 관계) 추가 기능을 가지고 있다는 것이다. 이것을 통해 데이터를 약간 더 표현력 있게 모델링할 수 있다. 그럼에도 불구하고 이전 Neo4j 예시와 개념적으로 유사하게 유지하기 위해 동일한 입력 CSV 파일을 사용하고 노드와 관계의 동일한 구조를 유지할 것이다. TypeDB에서는 이것들을 엔티티와 관계라고 부른다. 자, UMLS를 TypeDB에 로드하는 과정을 안내하자.

#### TypeDB에 UMLS 데이터 로드하기
예상대로, 예시 3-14와 유사한 명령을 사용하여 Docker 컨테이너를 시작해야 한다. TypeDB 로더 도구가 있지만 활발히 개발 중이며 Java 소스에서 도구를 빌드해야 한다. 따라서 이 섹션에서는 Chapter 4에서 MIMIC 데이터로 수행할 로딩과 동일한 일반적인 구조를 따르는 Python 스크립트를 통해 UMLS를 로드할 것이다.

예시 3-14. Docker에서 TypeDB 시작

```bash
docker run \
  --name umls-typedb \
  -p 1729:1729 \
  -d \
  -v $PWD/typedb:/typedb-all-linux/server/data/ \
  vaticle/typedb:latest
```

실제 데이터를 로드하기 시작하기 전에 스키마와 TypeDB 내에서 어떻게 사용되는지 살펴볼 시간을 갖자. TypeDB는 강력한 타입의 데이터베이스이므로 스키마를 정의하고 데이터베이스에 로드하기 위한 약간의 사전 작업이 있다. 자, 예시 3-15에서 스키마의 하위 집합을 살펴보자(전체 파일은 GitLab 저장소에서 이용 가능하다). 이 스키마는 원래 UMLS RRF 스키마가 아니라 전처리 단계의 CSV 출력 파일 구조에 맞게 조정되었다. 모든 것을 문자열로 처리하고 있으므로 속성 정의는 모두 생략했으며 다음 장의 예시 4-13에서 속성 정의 예시를 볼 수 있을 것이다.

예시 3-15. UMLS의 TypeDB 스키마

```typeql
define
childof sub relation, relates child, relates parent;
atom sub entity, plays childof:parent, plays childof:child,
  plays atomcui:atom, plays auicode:atom,
  owns id, owns str, owns sab, owns code, owns tty, owns ispref, owns ts;
conceptcode sub relation, relates concept, relates code;
atomcode sub relation, relates atom, relates code;
```

이 스키마를 로드하려면 네이티브 Python 드라이버 typedb-client를 사용할 것이다. 연결과 트랜잭션이 닫혀야 하므로 Python의 with... 구조를 사용할 것이다. 예시 3-16은 데이터베이스가 아직 존재하지 않으면 만들고 쓰기 트랜잭션 내에서 스키마를 로드하는 with 구조를 사용하는 두 개의 매우 단순한 코드 블록을 포함한다.

예시 3-16. TypeDB 스키마 로드

```python
from typedb.client import TypeDB, SessionType, TransactionType

db = "test2"
schema_file = "umls-schema.tql"

with TypeDB.core_client("localhost:1729") as client:
    if not client.databases().contains(db):
        client.databases().create(db)

with TypeDB.core_client("localhost:1729") as client:
    with client.session(db, SessionType.SCHEMA) as session:
        with session.transaction(TransactionType.WRITE) as tx:
            with open(schema_file, "r") as f:
                q = f.read()
            tx.query().define(q)
            tx.commit()
```

스키마가 로드되면 이제 각 CSV 파일을 로드해야 한다. 로딩 과정의 전체 골격은 예시 3-17에 나와 있다. 이 코드의 대부분은 TypeDB와 작업하기 위한 보일러플레이트와 필요한 최소한이다. 각 입력 파일에 대해 생성할 엔티티의 수를 감안하면, 각 트랜잭션에 대해 10,000개의 배치로 쿼리를 나눈다(이것은 엄격하게 테스트된 숫자가 아니므로 어떤 배치 크기가 가장 적합한지 실험해 볼 수 있다).

예시 3-17. TypeDB에 UMLS 로드

```python
files = [
    "semanticTypeNode.csv", "atomNode.csv", "codeNode.csv",
    "conceptNode.csv", "child_of_rel.csv", "cui_code_rel.csv",
    "has_aui_rel.csv", "has_cui_rel.csv", "has_sty_rel.csv",
    "tui_tui_rel.csv", "concept_concept_rel.csv"
]

def chunks(lst, n):
    for i in range(0, len(lst), n):
        yield lst[i:i + n]

with TypeDB.core_client("localhost:1729") as client:
    with client.session(db, SessionType.DATA) as session:
        for f in files:
            print(f"Processing: {f}")
            data = pd.read_csv(os.path.join(csv_path, f), dtype=str)
            data = data.replace(np.nan, "")
            queries = [templaters[f](x) for x in data.itertuples()]
            print(queries[0])
            batches = chunks(queries, 10000)
            counter = 1
            for batch in batches:
                print(f"Batch: {counter}, {len(batch)} items")
                counter = counter + 1
                with session.transaction(TransactionType.WRITE) as tx:
                    for q in batch:
                        tx.query().insert(q)
                    tx.commit()
```

작업의 대부분은 데이터프레임의 각 행을 입력으로 받아 TypeQL 쿼리 문자열을 반환하는 템플릿 함수에 의해 처리된다. 예시 3-18은 엔티티와 관리를 위한 몇 가지 템플릿과 문자열 정리 헬퍼 함수를 보여준다.

예시 3-18. TypeDB 템플릿 예시

```python
def clean_getattr(x, attr):
    return getattr(x, attr).replace("\"", "\\\"")

def code_template(x):
    return f"""
    insert $x isa sourcecode,
      has id "{clean_getattr(x, '_1')}",
      has sab "{clean_getattr(x, 'SAB')}",
      has code "{clean_getattr(x, 'CODE')}";
    """

def conceptsty_template(x):
    return f"""
    match $c isa concept, has id "{clean_getattr(x, '_1')}";
          $sty isa semantictype, has id "{clean_getattr(x, '_2')}";
    insert (concept: $c, semantictype: $sty) isa conceptsty;
    """

templaters = {
    "atomNode.csv": atom_template,
    "codeNode.csv": code_template,
    ...
    "tui_tui_rel.csv": tuitui_template,
    "concept_concept_rel.csv": concept_concept_template
}
```

각 파일 이름에 사용할 적절한 템플릿을 연결하는 사전이 있다. 이것은 로더가 모든 파일을 순환하여 로드하는 데 사용된다. 로딩 과정은 시간이 걸릴 것이므로, TypeDB 로더가 프로덕션 준비가 되기를 기대한다.

그렇다면, UMLS가 로드되었다-몇 가지 쿼리를 실행해 보자.

#### TypeDB TypeQL을 활용한 UMLS 쿼리 실습
이 예시를 여러 번 안내했으니, 쿼리로 바로 넘어가자. 예시 3-19는 "헤파린"을 포함하는 모든 아톰과 관련 개념을 찾은 다음 약리학적 물질만 포함하도록 필터링하는 TypeQL 쿼리를 포함한다. 이것은 본질적으로 Neo4j에서 한 것과 동일하지만 구문이 상당히 다르다.

예시 3-19. 헤파린에 대한 UMLS TypeDB 쿼리

```typeql
match
> **참고:** "헤파린"을 포함하는 모든 아톰 찾기
$atom isa atom, has str contains "heparin";
> **참고:** "약리학적 물질"의 의미 유형 엔티티 가져오기
$pharmsub isa semantictype, has id "T121";
> **참고:** 일치하는 아톰에 연결된 모든 개념 가져오기
(atom: $atom, concept: $drug) isa atomcui;
$drug isa concept, has str $str;
> **참고:** 의미 유형으로 필터링
(concept: $drug, semantictype: $pharmsub) isa conceptsty;
get $drug, $str;
```

이것을 더 확장하여(예시 3-13에서 한 것처럼) 더 많은 관리를 가져와 연결된 치료 또는 예방 시술을 찾을 수 있다. 예시 3-20은 확장된 쿼리를 보여주며 Neo4j 버전보다 상당히 더 복잡한 것을 볼 수 있다. 쿼리의 간결함이나 단순함에 완전히 초점을 맞추면 Cypher가 이러한 쿼리에 훨씬 더 깔끔하다. 그러나 데이터베이스에 프리로드할 수 있는 규칙을 구현하여 이러한 쿼리를 단순화할 수 있다. 함수를 통해 코드를 추상화하는 것과 유사하게, 다음 쿼리를 규칙으로 바꾸면, 약리학적 물질과 치료/예방 시술을 연결하는 데 의존하는 향후 쿼리가 크게 단순해질 것이다. 제4장에서 전자건강기록 데이터의 맥락에서 TypeDB를 논의할 때 규칙에 대해 좀 더 자세히 다룰 것이다.

예시 3-20. 치료에 대한 UMLS TypeDB 쿼리

```typeql
match
> **참고:** 약리학적 및 치료 의미 유형 가져오기
$pharmsub_type isa semantictype, has id "T121";
$therapy_type isa semantictype, has id "T061";
> **참고:** "헤파린"을 포함하는 아톰과 약리학적 물질인 관련 개념 찾기
$atom isa atom, has str contains "heparin";
$drug isa concept, has str $drug_name;
(atom: $atom, concept: $drug) isa atomcui;
$pharmsub_links (concept: $drug, semantictype: $pharmsub_type) isa conceptsty;
> **참고:** 약물이 DIRECT_SUBSTANCE_OF인 연결된 모든 치료 개념 찾기
$therapy isa concept, has str $therapy_name;
$dso (from: $drug, to: $therapy) isa conceptrel, has reltype "DIRECT_SUBSTANCE_OF";
$therapy_links (concept: $therapy, semantictype: $therapy_type) isa conceptsty;
get $therapy, $therapy_name;
```

이것으로 여러 유형의 데이터베이스를 통한 여정이 마무리된다. 이것이 UMLS와 작업하는 것의 표면만 긁었을 뿐이라는 것을 알고 있으며 UMLS를 다양한 사용 사례에 효과적으로 통합하는 방법을 배우는 데 전체 책을 쏟아붓기 쉬울 것이다. 그러나 이것이 UMLS에 대한 맛보기와 시작할 수 있는 기본적인 기반 이해를 제공하기를 바란다.

# 제4장 구조화된 의료 데이터 다루기

## MIMIC-III 데이터셋 개요 및 구조

### MIMIC 데이터의 약물 처방 데이터

MIMIC-III의 약물 데이터는 여러 테이블에 걸쳐 분포되어 있다. PRESCRIPTIONS 테이블에는 처방 정보가, INPUTEVENTS_MAR에는 투여 기록이, OUTPUTEVENTS에는 배출량이 기록된다. 이 데이터를 효과적으로 분석하려면 이 테이블들을 올바르게 조인하고 해석해야 한다.

약물 데이터를 다룰 때 가장 큰 과제 중 하나는 동일한 약물이 여러 이름으로 기록될 수 있다는 것이다. 예를 들어, 아세트아미노펜은 Tylenol, Acetaminophen, APAP 등으로 기록될 수 있다. RxNorm 코드를 사용하여 이 문제를 완화할 수 있지만 모든 처방에 RxNorm 코드가 기록되어 있지는 않다.

## EHR 데이터의 구조화된 필드

EHR 데이터의 대부분은 구조화된 필드로 구성되어 있다. 환자 인구통계학적 정보, 진단 코드, 시술 코드, 약물 처방, 실험실 결과 등이 이에 해당한다. 이 구조화된 데이터는 비교적 쉽게 쿼리하고 분석할 수 있지만 항상 완전하거나 정확하지는 않다.

### 실험실 검사 결과 (Lab Results)

MIMIC의 실험실 결과 데이터는 LABEVENTS 테이블에 기록된다. LOINC 코드로 식별되며 수치형 결과와 범주형 결과를 모두 포함한다. 실험실 결과는 환자의 임상 상태를 객관적으로 측정하는 데 매우 유용하지만 검사 시점과 결과 기록 시점의 차이, 그리고 검사 방법의 차이를 고려해야 한다.

### 생체 신호 (Vital Signs) 데이터

Vitals(생명 징후) 데이터는 CHARTEVENTS 테이블에 기록된다. 혈압, 심박수, 체온, 호흡수, 산소 포화도 등을 포함한다. 이것은 시계열 데이터로, 환자의 상태 변화를 추적하는 데 유용하다. 그러나 측정 시점, 측정 방법, 측정자의 변동성 등을 고려해야 한다.

## EHR 데이터와 청구 데이터의 비교

EHR 데이터와 청구 데이터는 헬스케어 분석에서 흔히 함께 사용되지만 중요한 차이가 있다:

| 특성 | EHR 데이터 | 청구 데이터 |
|------|-----------|-----------|
| 주요 목적 | 환자 진료 문서화 | 상환 청구 |
| 세부 사항 수준 | 높음 (임상 세부 사항 포함) | 낮음 (코딩된 코드 중심) |
| 시간 범위 | 전체 환자 여정 | 개별 청구 건 |
| 데이터 품질 | 임상적으로 더 정확할 수 있음 | 코딩 오류 가능 |
| 접근성 | 일반적으로 제한적 | 상대적으로 접근 용이 |

## 약물 데이터 정규화 및 조화

약물 정규화는 여러 소스에서 나온 약물 데이터를 표준 형식으로 변환하는 과정이다. 이것이 왜 중요한지 예시를 들어보겠다.

MIMIC 데이터에서 한 환자가 "acetaminophen 500mg tablet"으로 처방되고 같은 환자가 나중에 "Tylenol 500mg"으로 기록될 수 있다. 이것이 동일한 약물인지 확인하려면 RxNorm 코드와 같은 표준 코드를 사용해야 한다. 또한 복용량 정보를 정규화하여 "500mg"과 "0.5g"이 동일하게 해석되도록 해야 한다.

### MIMIC 데이터 실제 분석 예시

MIMIC 데이터를 사용한 간단한 분석 예시를 살펴보겠다. 예를 들어, 특정 약물이 ICU 체류 기간에 미치는 영향을 분석하고 싶을 수 있다.

```sql
-- 특정 약물을 투여받은 환자의 ICU 체류 기간 분석
SELECT
    p.drug,
    COUNT(DISTINCT i.subject_id) as patient_count,
    AVG(i.los) as avg_los_days
FROM PRESCRIPTIONS p
JOIN ICUSTAYS i ON p.subject_id = i.subject_id
    AND p.hadm_id = i.hadm_id
WHERE p.drug LIKE '%acetaminophen%'
GROUP BY p.drug
ORDER BY patient_count DESC;
```

이 쿼리는 acetaminophen을 투여받은 환자 수와 평균 ICU 체류 기간을 보여준다. 그러나 해석할 때 주의해야 할 점들이 있다:

1. 인과관계와 상관관계의 차이
2. 교란 요인(confounding factors)의 존재
3. 누락 데이터의 영향
4. 약물 처방 타이밍의 불확실성

## 결론 및 요약

이 장에서 MIMIC 데이터셋의 구조와 특징을 살펴보았다. MIMIC은 EHR 데이터로 작업할 때의 많은 도전을 잘 보여주는 귀중한 자료이다. 핵심 교훈:

1. EHR 데이터는 복잡하고 단편적이며 항상 완전하지 않다.
2. 데이터의 목적(진료 대 상환)이 데이터의 품질과 의미에 영향을 미친다.
3. 여러 테이블과 데이터 소스를 효과적으로 결합하려면 깊은 도메인 지식이 필요하다.
4. 데이터 조화는 단순한 코드 매핑이 아니라 의미를 포착하는 것이다.

다음 장에서는 청구 데이터를 더 깊이 살펴보고 EHR 데이터와 결합하는 방법을 논의할 것이다.

### MIMIC-III 스키마 세부 사항

MIMIC-III 스키마의 핵심 테이블들을 살펴보겠다.

**CHARTEVENTS**: 모든 환자, 모든 입원, 모든 ICU 체류에 대한 모든 시술/관찰을 포함한다. D_ITEMS 테이블과의 기본키-외부키 관계에 크게 의존한다. 시작/종료 시간 같은 필드는 자명하지만 대부분은 임상 실천이나 해부학/생리학에 대한 지식이 필요하다.

**D_ITEMS**: CareVue 또는 MetaVision 데이터베이스의 항목 정의를 저장한다. 이 테이블이 제어된 어휘를 포함하는 것처럼 보일 수 있지만 그렇지 않다! 동일한 개념을 포착하지만 철자 오류나 동의어가 있는 여러 항목이 있을 수 있다. 이것이 그래프 데이터베이스가 빛나는 영역이다-동의어 또는 동등 개념을 데이터셋 전반에 걸쳐 연결할 때. 예를 들어, ITEMID가 211과 220045인 항목은 모두 "심박수"를 나타내지만 CareVue와 MetaVision에서 나온 것이다. SQL 기반 시스템에서는 이것을 SQL 쿼리 수준에서 연결해야 한다.

**D_ICD_DIAGNOSES**: 환자 진단과 관련된 ICD-9 코드를 포함한다. MIMIC-III이 2001-2012년 데이터만 포함하므로 ICD-10 코드는 없다.

**D_ICD_PROCEDURES**: 2015년 이전 병원 시술과 관련된 ICD-9 코드를 포함한다.

CPT 코드와 ICD 시술 코드의 차이: CPT 코드는 주로 의사 서비스에 대한 청구에 사용되고 ICD 시술 코드는 병원 서비스에 대한 청구에 사용된다. 의사 서비스와 병원 서비스는 별도로 청구되며 다른 코드 세트를 사용한다.

**D_LABITEMS**: 실험실 및 기타 관찰 검사의 정의를 포함하며 LOINC 코드와 연결된다. 그러나 모든 개념이 LOINC 코드로 매핑된 것은 아니다.

### MIMIC 데이터셋의 한계점

MIMIC-III 데이터셋의 가장 큰 한계는 ICU 체류를 가진 환자에 초점을 맞추고 있다는 것이다. 결과적으로 더 복잡한 의학적 상황을 가진 고중증 환자에 대한 편향이 존재한다. 이 데이터에서 도출된 모든 통찰은 이 인구 집단에만 적용될 수 있다. 또한 학술 메디컬 센터에서 수집되었으므로 커뮤니티 병원과의 임상 실천 차이도 고려해야 한다.

## Synthea 합성 데이터셋

Synthea는 완전히 합성된 데이터셋으로, 실제 환자 데이터를 포함하지 않는다. 다양한 형식이나 데이터 모델을 포함해야 할 때 유용하다. 합성 데이터이므로 진정한 통찰을 추출하는 데는 도움이 되지 않지만 데이터 엔지니어링 파이프라인을 테스트하는 데는 탁월하다.

CSV 버전은 정규화된 관계형 구조로, 각 CSV 파일은 관계형 데이터베이스의 별도 테이블로 로드될 수 있다. UUID를 사용하여 파일 간을 연결한다. 알레르기, 약물, 케어플랜, 관찰, 청구, 환자, 진단, 보험자, 기기, 진료, 영상 검사, 제공자, 면역, 진료 등을 포함하는 파일이 각각 있다.

## 의료 데이터 모델 표준

데이터로 작업할 때 첫 번째 고려사항 중 하나는 데이터 모델이다. 헬스케어 데이터의 경우 데이터 모델은 보통 EHR 제조사나 임상 데이터 웨어하우스를 개발한 팀이 결정한다. "공통" 데이터 모델에 초점을 맞추는 느리지만 증가하는 추세가 있다.

### OMOP 공통 데이터 모델 (CDM)

OHDSI("오디세이"로 발음)는 컬럼비아 대학교 연구 그룹을 통해 중앙 조정되는 협업이다. 공통 데이터 모델(OMOP), 시맨틱 네트워크, 아테나(표준 전문용어에 대한 쿼리 인터페이스), 그리고 유사도 분석 도구(HADES)를 포함한다.

OMOP CDM v5.4는 환자 중심이며 모든 것이 Person 테이블에서 나온다. 환자는 즉시 방문과 연결된다. OMOP는 입원 데이터에만 국한되지 않으므로 방문은 외래 또는 입원이 될 수 있다. 조건(진단), 약물, 시술 등 다양한 관찰 데이터로 연결된다. 임상 데이터, 파생 요소, 건강 시스템 데이터, 메타데이터, 건강 경제학 데이터, 전문용어로 그룹화된다. 거의 모든 임상 테이블이 Concept 테이블과 연결되어 있다. SNOMED-CT 및 LOINC와 같은 산업 표준 전문용어와 밀접하게 정렬되어 있다.

### HL7 FHIR 표준

FHIR("파이어"로 발음)는 상호운용성 표준으로, 다른 임상 정보 시스템 간의 정보 교환을 촉진하도록 설계되었다. "데이터 모델"이라고 부르는 것은 약간 잘못된 명칭이다. 데이터 저장을 위해 설계된 것이 아니지만 JSON 형식으로 제공되므로 많은 사람들이 이를 문서 지향 데이터베이스에 직접 저장하기 시작했다.

FHIR의 핵심은 리소스이다. 리소스는 두 헬스케어 정보 시스템 간에 공유될 데이터 페이로드의 구조와 내용에 대한 정의이다. 프로필은 특정 구현이 기본 리소스 세트에 대한 제약 조건이나 확장을 포함하는 특정 리소스 정의 세트를 만들고 게시할 수 있게 해준다. 구현 가이드는 특정 리소스/프로필을 사용하는 방법에 대한 규칙과 제약 조건 세트이다.

### 기타 의료 데이터 모델

- **CDISC/SDTM/ADaM**: 임상 시험 데이터 제출을 위한 표준. SDTM은 원시 데이터를, ADaM은 분석용 데이터를 정의한다.
- **i2b2**: 스타 스키마를 사용하는 연구 중심 데이터 모델로, 사실과 그 사실의 수정자를 나타낸다.
- **PCORnet**: 환자 중심 결과 연구를 지원하기 위한 데이터 모델.
- 독점 데이터 모델도 많지만 라이선스와 비공개 계약이 필요하다.

어떤 데이터 모델을 선택하든 한계가 있다. 가장 명백한 것은 데이터 모델이 지원하지 않거나 너무 번거로운 유스케이스가 있을 수 있다는 것이다. 하나의 데이터 모델에서 다른 것으로 데이터를 번역하면 자연스럽게 정보 손실이나 변화가 발생한다.

## 사례 연구: 약물 데이터 조화 (Drug Harmonization)

약물 데이터로 작업하는 공통적인 문제는 코딩 시스템의 다양성이다. NDC 코드와 같은 코딩 시스템을 사용할 때의 연결 문제에 초점을 맞추겠다.

### 약물 조화의 주요 과제

헤파린은 입원 환경에서 흔히 사용되는 약물로, 다양한 상황에서 혈전 형성을 줄이는 데 도움이 된다. 그러나 환자가 아스피린이나 기타 비스테로이드성 항염증제(NSAID)와 같은 다른 혈전 감소 약물을 이미 복용하고 있다면 위험한 상황이 될 수 있다.

데이터에서 헤파린을 찾으려면 PRESCRIPTIONS 테이블의 drug, drug_name_generic, drug_name_poe 열에서 대소문자를 구분하지 않고 "heparin"을 검색할 수 있다. 또는 헤파린을 포함하는 제형의 모든 NDC 코드를 조회할 수 있다.

그러나 문자열 "heparin"은 "heparin flush"(IV 카테터 세척에 사용됨)와도 일치할 수 있다. 헤파린 플러시는 환자를 치료하는 데 사용되지 않으며 대부분의 분석에서 제외되어야 한다. 이 문제를 해결하려면 점점 더 복잡하고 취약한 데이터 파이프라인에 들어서게 된다.

해결책으로는 NDC 코드를 사용하여 치료용 헤파린만 식별하는 것이다. 7개의 NDC 코드를 유지하기로 결정하면, SQL 쿼리에서 이 목록을 관리해야 한다. 이것이 그래프 기반 접근법이 유용한 지점이다.

### OMOP 데이터 모델 적용

OHDSI는 OMOP와 함께 높은 수준의 접근법을 취해 왔으며 데이터 엔지니어와 데이터 과학자로서의 관점에서 고려사항을 살펴보겠다. 특히 우리가 비교하는 서로 다른 데이터베이스 유형과 관련된 부분에 초점을 맞추겠다.

OMOP CDM의 최신 버전은 v5.4이며 2021년 9월에 출시되었다. v6.0도 언급되지만 OHDSI는 새로운 프로젝트가 계속 5.x 분기를 사용할 것을 특별히 권장한다. v6.0은 일부 호환되지 않는 변경 사항을 만들었기 때문이다.

CDM은 환자 중심이며 모든 것이 Person 테이블에서 나온다. 환자는 방문과 연결되고 조건, 약물, 시술 등 다양한 관찰 데이터로 이어진다. 임상 데이터, 파생 요소, 건강 시스템 데이터, 메타데이터, 건강 경제학 데이터, 전문용어로 그룹화된다. 거의 모든 임상 테이블이 Concept 테이블과 연결되어 있어 SQL 형식으로 관계를 포착할 수 있다. 이것이 매우 편리하지만 SQL 쿼리 자체가 매우 복잡하고 읽기 어려워질 수 있다.

## FHIR

FHIR는 상호운용성 표준으로 설계되어 별도의 임상 정보 시스템 간의 정보 교환을 촉진한다. 결과적으로 데이터가 여러 곳에 중복되어 효율적인 정규화 대신 장황하게 표현된다. 그러나 상호운용성 표준으로서 구현 가이드와 프로필 같은 강력한 기능을 제공한다.

리소스는 FHIR의 핵심이며 두 헬스케어 정보 시스템 간에 공유될 데이터 페이로드의 구조와 내용에 대한 정의이다. 프로필은 특정 구현이 기본 리소스 세트의 제약 조건이나 확장을 포함하는 특정 리소스 정의 세트를 만들고 게시할 수 있게 해준다. 구현 가이드는 특정 리소스/프로필을 사용하는 방법에 대한 규칙과 제약 조건 세트이다.

## 사례 연구: 약물 데이터 조화 (Drug Harmonization) 조화

약물 조화 문제를 다시 살펴보겠다. 헤파린의 예에서 데이터를 세 가지 데이터베이스-SQL(SQLite), 프로퍼티 그래프(Neo4j), 하이퍼그래프(TypeDB)-에 로드하고 쿼리하는 방법을 살펴보겠다.

### SQLite로 작업하기

MIMIC-III 데모 데이터를 SQLite에 로드하는 것은 간단하다. 각 CSV 파일을 별도의 테이블로 가져오면 된다.

```python
import os
import pandas as pd
import sqlite3

mimic_path = "mimic-iii-clinical-database-demo-1.4"
mimic_path = os.path.expanduser(mimic_path)
db_path = "mimic-iii.db"
conn = sqlite3.connect(db_path)

csv_files = [f for f in os.listdir(mimic_path) if f.endswith(".csv")]
csv_files.sort()

for f in csv_files:
    path = os.path.join(mimic_path, f)
    table_name = f.split(".")[0]
    data = pd.read_csv(path, dtype=str)
    data.to_sql(table_name, conn, if_exists="replace")
```

### SQLite에서 헤파린 쿼리하기

대소문자를 구분하지 않고 "heparin"을 포함하는 약물을 검색한다:

```sql
SELECT count(*)
FROM PRESCRIPTIONS
WHERE drug LIKE "%heparin%"
   OR drug_name_generic LIKE "%heparin%"
   OR drug_name_poe LIKE "%heparin%"
```

이 쿼리는 치료용 헤파린뿐만 아니라 IV와 카테터를 유지하는 데 사용되는 헤파린도 모두 반환한다. 고유한 항목을 보면 플러시용과 치료용을 구분해야 한다는 것을 알 수 있다. 7개의 NDC 코드를 유지하기로 결정하면 SQL 쿼리에서 이 목록을 관리해야 한다. 이것이 나중에 그래프 기반 접근법과 비교될 때의 핵심 트레이드오프이다.

### Neo4j로 작업하기

그래프를 사용하여 데이터를 조화하는 것은 마법과 재미가 일어나는 곳이다! 그래프 데이터베이스의 핵심 이점 중 하나는 개별 데이터 포인트를 연결/조인하는 능력이다. 헤파린을 포함하지만 플러시가 아닌 약물을 나타내는 새로운 개념을 정의할 수 있다. 이 개념을 만든 후未来的 쿼리에서 사용할 수 있다.

```cypher
// 헤파린 관련 모든 처방 찾기
MATCH (p:PRESCRIPTIONS)
WHERE toLower(p.drug) CONTAINS "heparin"
   OR toLower(p.drug_name_generic) CONTAINS "heparin"
   OR toLower(p.drug_name_poe) CONTAINS "heparin"
RETURN DISTINCT p.drug

// 관련 환자 찾기
MATCH (pt:PATIENTS)-[r:has_prescription]->(p:PRESCRIPTIONS)
WHERE toLower(p.drug) CONTAINS "heparin"
RETURN DISTINCT pt
```

새로운 헤파린 개념을 만들고 관련 NDC 코드를 연결하면未来的 쿼리에서 이 개념을 사용할 수 있다. 이것이 관계형 데이터베이스에서는 쉽게 할 수 없는 것이다-실제 처방 데이터에 연결할 새 노드를 만들 수 있다.

### TypeDB로 작업하기

TypeDB는 하이퍼그래프 기능과 함께 내장된 추론 엔진을 제공한다. 데이터 조화 매핑을 두 가지 접근법으로 관리할 수 있다:

1. 프로퍼티 그래프나 RDF 트리플 스토어와 유사하게, 그래프에 지식을 직접 포함하여 새로운 관계를 만들 수 있다.
2. 추론 엔진을 활용하여 매핑을 처리하는 규칙을 만들고 쿼리 요구에 따라 규칙을 로드/언로드할 수 있다.

TypeDB는 강 타입 데이터베이스이므로 스키마를 정의하고 로드하는 것이 중요하다. 스키마는 엔티티, 속성, 역할, 관계를 정의한다.

```typeql
define
patient sub entity,
  plays prescription:patient,
  owns row_id, owns subject_id, owns gender, owns dob;

prescription sub relation,
  relates patient, relates prescribed_drug,
  owns startdate, owns enddate;

druginstance sub entity,
  plays prescription:prescribed_drug,
  owns drug, owns ndc, owns prod_strength;
```

데이터를 로드하려면 CSV 파일을 순회하면서 각 행에 대해 TypeQL 쿼리를 생성한다. 추론 엔진을 사용하면 규칙을 데이터베이스에 로드하여未来的 쿼리에서 자동으로 매핑을 적용할 수 있다.

### 세 가지 접근법 비교

| 특성 | SQLite | Neo4j | TypeDB |
|------|--------|-------|--------|
| 조화 지식 저장 | SQL 쿼리 내장 | 그래프에 새 노드/관계 | 규칙으로 관리 |
| 유지보수성 | 낮음 (코드 수정 필요) | 중간 (노드 관리 필요) | 높음 (규칙 관리) |
| 유연성 | 낮음 | 중간 | 높음 |
| 학습 곡선 | 낮음 | 중간 | 높음 |

이 사례 연구는 동일한 조화 문제를 다양한 데이터베이스로 해결하는 방법을 보여주었다. 핵심 교훈는 조화 지식을 데이터베이스 자체에 저장하면未来的 쿼리가 더 간단하고 관리하기 쉬워진다는 것이다. 그러나 지식 관리의 과제-누가, 언제, 왜 매핑을 변경했는지 추적하는 것-는 여전히 남아 있다.

## 기술 심층 분석 및 비교

이 섹션에서는 다음 과정을 탐구한다:

1. MIMIC 데이터를 데이터베이스에 로드하기
2. 기본 조화 작업 처리하기
3. 데이터베이스에서 데이터 추출/쿼리하기

각 과정을 SQL, 프로퍼티 그래프, 하이퍼그래프 등 다양한 데이터베이스로 살펴보겠다. 스키마는 특별한 유스케이스에 최적화되지 않았으므로 자유롭게 프로젝트에 맞게 조정할 수 있다. MIMIC-III 클리니컬 데이터베이스 데모(100명의 환자를 포함하는 하위 집합)를 사용할 것이다.

### SQLite로 작업하기

SQLite는 가벼운 SQL 데이터베이스로, 단일 파일로 데이터베이스를 저장하고 관리한다. 데이터베이스 서버를 설정하거나 네트워크 설정을 할 필요가 없다. MIMIC 데이터를 각 CSV 파일마다 별도의 테이블로 가져오면 된다.

헤파린을 포함하는 약물을 검색하는 SQL 쿼리는 간단하다. 그러나 플러시용 헤파린도 포함되므로 치료용만 필터링해야 한다. 7개의 NDC 코드를 유지하기로 결정하면 이 목록을 SQL 쿼리 전체에서 관리해야 한다. 코드를 다른 데이터 과학자와 공유해야 한다면 문제가 더 커진다.

### Neo4j로 작업하기

그래프를 사용하면 데이터베이스 자체에 조화 지식을 저장할 수 있다. 새로운 "헤파린(비플러시)" 개념을 만들고 관련 NDC 코드를 연결하면未来的 쿼리에서 이 개념을 직접 사용할 수 있다. 이것이 관계형 데이터베이스에서는 어려운 것이다.

Cypher 쿼리는 간단하고 직관적이다. 새로운 개념에 연결된 환자를 찾으려면 노드와 관계를 따라가면 된다. 내부 거버넌스 프로세스에 따라 새로운 개념과 관계에 원하는 만큼 많은 속성을 추가할 수 있다.

### TypeDB로 작업하기

TypeDB는 스키마 정의와 로딩이 필요하지만 추론 엔진이라는 강력한 기능을 제공한다. 규칙을 데이터베이스에 로드하면未来的 쿼리에서 자동으로 매핑을 적용할 수 있다. 규칙을 관리하는 것은 다른 코드를 관리하는 것과 유사하다.

스키마는 엔티티, 속성, 역할, 관계를 정의한다. 환자, 처방, 약물 인스턴스 등의 엔티티를 정의하고 처방 관계를 통해 연결한다.

```typeql
define
patient sub entity, owns subject_id, owns gender, owns dob;
druginstance sub entity, owns drug, owns ndc, owns prod_strength;
prescription sub relation, relates patient, relates prescribed_drug;
```

데이터를 로드하려면 CSV 파일을 순회하면서 각 행에 대해 TypeQL 쿼리를 생성한다. 약물 처방의 경우 고유한 약물 인스턴스를 추출하여 처방 관계로 연결하는 추가 처리가 필요하다.

## 데이터 조화의 핵심 교훈

이 사례 연구에서 얻을 수 있는 핵심 교훈:

1. **조화 지식의 저장**: 조화 지식을 SQL 쿼리에 내장하는 대신 데이터베이스 자체에 저장하면未来的 쿼리가 더 간단해지고 유지보수가 쉬워진다.

2. **데이터베이스 선택의 영향**: 프로퍼티 그래프는 연결에 유리하지만 모든 것에 노드를 만들어야 하고 하이퍼그래프는 더 유연하지만 더 복잡하다. 선택은 유스케이스와 팀의 기술에 달려 있다.

3. **지식 관리의 과제**: 매핑을 만들고 변경하는 것은 기술적 해결책만으로는 충분하지 않다. 누가, 언제, 왜 변경했는지를 추적하는 프로세스가 필요하다.

4. **임상적 뉘앙스의 중요성**: "헤파린"과 "헤파린 플러시"의 차이는 임상의에게는 명백하지만 데이터 파이프라인에서는 놓치기 쉽다. 도메인 전문 지식은 필수적이다.

5. **실용적 접근**: 완벽한 조화는 불가능할 수 있다. 현재 필요한 것과 미래에 필요할 것의 균형을 맞추는 것이 중요하다.

### TypeDB에서 데이터 쿼리하기

TypeDB에서 헤파린을 포함하는 약물을 찾으려면 기본 문자열 매칭을 사용한다. SQLite와 Neo4j와 달리 새로 만든 약물 인스턴스를 쿼리한다. 처방 관련 정보를 반환하려면 처방 관계를 추가로 매칭하면 된다.

### TypeDB에서 데이터 조화하기

데이터를 로드한 후 새로운 약물 개념을 만들어야 한다. 두 가지 접근법이 있다:

**직접 업데이트**: 프로퍼티 그래프와 같은 패턴으로 데이터베이스를 직접 업데이트한다.

**규칙 생성**: TypeDB 추론 엔진을 사용하여 규칙을 만든다. 규칙은 실행 시 실제 데이터를 저장하지 않고 새로운 데이터를 동적으로 "추가"한다. 이를 통해 쿼리가 데이터가 데이터베이스에 있는 것처럼 상호작용할 수 있다.

```typeql
define rule heparin-non-flush-subset-ndc-63323026201:
when {
  $d isa druginstance, has ndc "63323026201";
  $c isa concept, has purl "http://some-ontology.org/12345";
} then {
  (parent: $c, child: $d) isa hierarchy;
};
```

TypeDB 규칙 조건은 결합적(conjunctive)이다. 분리 조건은 별도의 규칙으로 만들어야 한다. 7개의 NDC 코드에 대해 각각 별도의 규칙을 자동 생성할 수 있다.

쿼리를 실행하면 추론 엔진이 규칙을 적용하여 새로운 헤파린 개념과 기존 약물 인스턴스 간의 연결을 추론한다. 데이터베이스에 지식을 직접 저장하는 대신 규칙으로 관리하면 데이터베이스를 깔끔하게 유지할 수 있다. 그러나 실시간 추론은 성능 병목이 될 수 있다.

### RDF 그래프 모델링

RDF 트리플 스토어도 이 문제를 해결할 수 있다. 모든 것을 주어-술어-객체 트리플로 취급하므로 프로퍼티 그래프보다 복잡해 보일 수 있다. 그러나 RDF 트리플 스토어는 확립된 표준을 따르므로 다른 시스템과 지식을 공유하기가 더 쉬울 수 있다. 예를 들어, 의약품 개념에 표준화된 PURL을 사용하여 트리플을 만들고 다른 시스템으로 보낼 수 있다.

### UMLS 온톨로지 연계

이전 예제에서 전자건강기록 데이터를 구조화된 개념에 연결할 수 있었다. SQL의 경우 구조화된 코드나 개념이 없으면 일반적으로 새 열로 추가한다. 그래프에서는 데이터 포인트와 구조화된 개념 사이의 새로운 연결을 빠르게 만들 수 있다. UMLS와 같은 전문용어를 동일한 그래프에 로드하면 기존 개념과의 동일한 연결을 수행할 수 있다.

### 구조화된 의료 데이터 정규화의 난제

이 장에서 논의한 약물 정규화 시나리오는 비교적 간단한 상황이다. 헤파린 같은 것을 매핑할 때 복잡성과 잠재적 모호성이 있지만 약물은 일반적으로 객관적인 데이터이다. 주관적이거나 명확한 합의가 없는 데이터로 작업하면 상황이 훨씬 더 어려워진다. 예를 들어, 암 분류는 복잡한 주제이다. 그룹 분류 대 TNM 분류, AJCC 분류 지침의 업데이트 등 많은 뉘앙스가 존재한다. MIMIC 데이터에서 ICD 코드를 OMOP CONDITION_OCCURRENCE 테이블로 매핑할 때 기본적인 의미론적 불일치가 발생할 수 있다.

## 결론 및 요약

이 장에서 EHR 데이터로 작업하는 과정을 깊이 파고들었다. MIMIC과 Synthea 데이터셋을 살펴보고 관계형과 그래프 기반 데이터베이스에서 약물 조화 예제를 통해 데이터를 저장하고 쿼리하는 방법을 비교했다. 각 데이터베이스 유형의 장단점은 표 4-2에 정리되어 있다.

다음 장에서는 청구 데이터로 깊이 파고들겠다. 청구 데이터는 상환 과정에서 생성되며 환자 치료와 관련된 재정적 측면을 이해하는 데 필수적이다.

# 제5장 청구 데이터와 EHR 데이터 결합하기

## 효과성(Effectiveness)과 유효성(Efficacy)의 차이

약물의 효과(efficacy)는 매우 통제된 환경(예: 임상시험)에서의 성능으로, 교란 요인을 제거하거나 완화하기 위한 것이다. 반면 유효성(effectiveness)은 일반적인 조건 하의 실제 세계에서의 성능이다.

## CMS SynPUF 합성 청구 데이터셋

허가odata와 재무 데이터는 접촉하기 어렵다. 몇 가지 요인이 작용한다: 개인 식별 정보가 포함되어 있어 비식별화가 필요하고 재무 데이터를 공유할 인센티브가 거의 없다. 병원, 의사 단체, 보험회사 간의 섬세한 협상 때문에 청구 데이터를 공유하는 것은 오히려 마이너스 요인이다.

그럼에도 불구하고 CMS(Centers for Medicare and Medicaid Services)에서 공개하는 데이터 엔트리프레너즈 합성 공용 파일(SynPUF)이 있다. 이 데이터셋은 실제 CMS 청구 데이터를 기반으로 합성 생성되었으며 청구 데이터로 작업하는 방법을 익히기에 좋은 방법이다.

### 인구통계학적 편향과 데이터 대표성

질병이나 의료 데이터 분석에서 인구통계학적 특성이 분석에 큰 영향을 미칠 수 있다. 가장 흔히 인용되는 편향 중 하나는 지리적 위치이다. "우편번호가 유전 코드보다 중요하다"는 말이 있을 정도다. 의료 데이터셋을 고려할 때 데이터셋에 포함된 환자 인구통계를 고려하는 것이 중요하다.

## 데이터 모델 매핑 전략

EHR와 마찬가지로, 청구 데이터를 다룰 때도 데이터 모델의 선택이 중요하다. OMOP, FHIR, hl7v2 등 여러 공통 데이터 모델(CDM)이 존재한다. 어떤 모델을 선택할지는 현재 사용 중인 모델, 분석 유형, 팀의 전문성 등에 달려 있다.

## 데이터 모델 매핑 전략 선택하기

올바른 데이터 모델을 선택하는 것은 프로젝트 초기에 너무 많은 시간을 투자하기 쉬운 영역이다. "새 프로젝트를 시작하는데 OMOP를 사용해야 할까요?"라는 질문을 자주 받는다. 내 초기 대응은 보통 일련의 질문이다: 정확히 무엇을 하려는가? 어떤 유형의 데이터를 추출하는가? 어떤 과학적 질문에 답하려는가?

데이터 모델은 우리가 데이터로 무엇을 하려는지에 따라 달라진다. OMOP는 관찰 데이터의 종단 분석에 맞춰져 있어 환자 중심적이다. 단일 시점의 환자 코호트만 분석하는 분석에는 불필요하게 복잡할 수 있다.

다음 관점을 고려해야 한다:
- 소스 데이터가 현재 사용 중인 데이터 모델은 무엇인가?
- 데이터 과학자나 분석 도구가 사용 중인 모델은 무엇인가?
- 향후 어떤 유형의 분석을 계획하는가?
- ML 모델을 개발하는 경우 프로덕션에 배포할 때 어떤 모델이 가장 적합한가?
- 새 데이터를 얼마나 자주 처리할 것으로 기대하는가?
- 외부 전문용어 관리 솔루션이 있는가?
- 가장 많은 정보학 전문성과 자원이 어디에 있는가?

실용적으로 보면 최대한의 이익을 얻으면서도 상대적으로 단순하고 쉽게 유지하는 균형이 필요하다.

## 청구 데이터와 EHR 데이터 결합 실습

다양한 데이터셋을 단일 분석으로 결합하든, 동일한 분석을 다른 데이터셋에서 실행하든, 다루게 될 주제의 상당수가 적용된다. 여러 데이터셋을 결합하고 분석할 때는 특히 임상 유효성 관점에서 고려해야 할 사항이 있다.

### OMOP CDM 적용

OMOP를 단순한 데이터 모델이라고 부르면 결례가 된다. OMOP 커뮤니티, 문서화, Athena 전문용어 서비스 관리 모두 OMOP가 데이터 모델로 성공하는 데 기여하는 요소들이다. 기존 데이터 모델을 OMOP로 변환할 때 따라야 할 확립된 모범 사례가 있다.

일반적으로 두 가지 주요 작업 범위가 있다: 소스 데이터의 구조화된 개념을 Athena 내의 개념으로 매핑하고 소스 스키마의 테이블과 열을 OMOP 스키마로 매핑한다.

### Athena를 통한 어휘 통합 및 매핑

Athena는 OMOP에서 전문용어를 직접 SQL로 쿼리할 수 있는 메커니즘이다. UMLS와 다양한 소스 전문용어의 개념을 저장하고 매핑하는 일련의 테이블을 제공한다. 주요 목표는 최상의 개념을 선택하는 것이다.

MIMIC 데이터에서는 진단이 이미 ICD-9 코드로 저장되어 있으므로 Athena에서 조회하기만 하면 된다. 그러나 NDC 코드의 경우 코드 재사용 문제가 있다. 비활성화된 코드는 새 제품에 재사용될 수 있다. OMOP의 Athena 전문용어 서비스는 버전 관리가 되지 않는다. 따라서 과거의 NDC 코드를 현재의 Athena NDC 코드에 연결하는 것은 도전적이다.

### 데이터 모델 간 변환과 크로스워크

의료 데이터 모델 간 변환은 반복적인 과정으로, 기술적 역량과 주제 전문 지식의 결합이 필요하다. 일반적으로 다음 단계를 반복한다:

1. 소스 데이터 탐색
2. 대상 스키마로 데이터 매핑
3. 소스와 대상 데이터의 분석 비교
4. 잘못 매핑된 데이터와 잠재적 원인 파악
5. 2단계부터 반복

그래프 데이터베이스와 지식 그래프는 이 반복 주기를 단축하는 데 잠재적인 도움이 될 수 있다.

## 진단과 약물 결합 사례 연구

MIMIC-III의 환자 수준 임상 데이터와 Synthea의 환자 수준 임상 및 청구 데이터를 결합하는 예를 살펴보겠다. 공통된 욕구는 서로 다른 데이터셋을 결합하여 더 큰 데이터셋을 만드는 것이다. 그러나 MIMIC은 입원(특히 중환자실) 데이터에 편향되어 있고 Synthea 데이터셋은 외래/외래 환경에 초점을 맞추고 있다.

### OMOP CDM 대 그래프 데이터베이스 비교

OMOP 공통 데이터 모델(CDM)로 변환하여 서로 다른 데이터셋을 결합할 수 있다. 그래프 데이터베이스를 사용하면 동일한 인스턴스 내에서 여러 다른 매핑을 적용하여 서로 다른 매핑을 비교/대조하기가 더 쉽다. 노드/정점과 관계/엣지에 특정 매핑 시도를 식별하는 속성을 추가하면 된다.

소스 데이터 모델과 OMOP 간의 매핑이 명확한 경우 표준 접근법이 최선이다. 그러나 탐색 단계에서 아직 데이터를 어떻게 매핑할지 실험하고 있다면 그래프가 흥미로운 기회를 제공한다.

### 상황별 최적의 아키텍처 선택

George EP Box의 명언: "모든 모델은 틀렸지만 어떤 것은 유용하다." 데이터 모델에도 동일하게 적용된다. 모든 데이터 모델은 트레이드오프가 있고 특정 가정 세트로 우리를 몰아세운다. 완벽한 단일 데이터 모델은 존재하지 않는다.

### 구조적 조화 대 의미론적 조화

의료 데이터를 결합할 때 고려해야 할 사항이 많다. HIMSS 상호운용성 수준에서 차용한 세 가지 조화 수준이 있다.

**구문적 조화**: 데이터의 형식과 구조를 일치시키는 것이다. 예를 들어, 모든 필드를 날짜/시간 유형으로 변환하는 것이 포함된다. OMOP를 사용하면 구조적 조화 과제를 해결할 수 있다. 그러나 구조적 조화는 필요하지만 충분하지 않다.

**의미론적 조화**: 데이터의 전체 문맥과 의미를 보존하면서 보낼 수 있는 것이다. 원본 데이터의 의미와 문맥을 이해하고 대상 데이터 모델의 의미를 파악해야 한다. 복잡한 통계적 조정이나 정교한 기술이 필요할 수 있다.

### 코딩 시스템 및 크로스워크 활용

코드화된 데이터(예: ICD-9/10 코드, CPT 코드 등)가 포함된 데이터셋을 매핑할 때 흔히 발생하는 문제는 크로스워크 사용에 대한 과신이다. 크로스워크는 본질적으로 두 코드 시스템 간의 매핑 테이블이다. 그러나 모든 크로스워크는 특정 목적을 위해 만들어졌으며 귀하의 고유한 문제에 적용 가능한지는 별도의 검증이 필요하다.

## 결론 및 요약

서로 다른 데이터셋을 결합하거나 병합하는 것은 점점 더 인기를 얻고 있다. EHR과 청구 데이터를 결합하든, 여러 EHR 소스를 결합하든, 동일한 과제에 직면한다. 기술적 고려사항 외에도 두 데이터셋을 결합할 때의 의미론적 차이도 고려해야 한다. 구조적으로 조화할 수 있더라도 본질적으로 다른 의미의 데이터 요소를 결합하지 않도록 주의해야 한다.

다음 장에서는 이 개념들을 분석과 연결하는 방법을 살펴보겠다.

사전 조율된 용어/개념은 하나의 용어/개념이 기본 개념의 모든 측면을 포착하는 것이다. 간단한 예로 "왼쪽 척골 골절"은 하나의 용어로 이 개념을 포착한다. 반면 사후 조율을 사용하면 "왼쪽"과 "오른쪽", "골절"과 "염좌", "척골"과 "桡骨" 등 각 개념에 대해 하나의 용어를 갖게 된다.

MIMIC 데이터셋에서는 환자 진단의 거친 정의를 제공하는 ICD-9 코드가 있을 수 있고 더 세부적인 정보를 찾기 위해 차트 또는 노트 이벤트 테이블을 살펴봐야 한다. 원하는 세부 수준을 가장 잘 나타내는 단일 개념을 식별해야 한다. 여기서의 과제는 사전 조율을 사용할 수 있도록 모든 가능한 용어 조합을 만드는 것이 다루기 어려운 수의 조합을 만들 수 있다는 것이다. 어떤 조합은 임상적으로 의미가 없을 수도 있다.

하나의 해결책은 포괄적인 개념의 각 측면을 해당 테이블의 별도 행(예: 진단의 경우 CONDITION_OCCURRENCE, 기타 관찰의 경우 OBSERVATION)으로 캡처하고 동일한 방문, 날짜/시간 등에 연결하는 것이다. 그러나 이는 모든 개념을 다시 결합하는 복잡성을 쿼리로 전가한다. 환자에게 사후 조율이 필요한 여러 중복 조건이 있으면 어떻게 데이터베이스에 캡처하고 미래에 올바르게 다시 결합할 것인가?

### 테이블 및 컬럼 매핑 전략

테이블과 열을 매핑해야 하는 필요성은 새로운 문제가 아니며 의료 데이터에 국한된 것도 아니다. 이 과정은 ETL(점점 더 ELT) 파이프라인의 핵심에 자리 잡고 있다. OMOP 생태계 내에서 이 과정에는 특별한 고려사항이 있다.

### OMOP CDM에서의 데이터 모델링

OMOP를 사용하면 여러 출처의 데이터를 하나의 공통 모델로 변환할 수 있다. MIMIC 데이터를 OMOP로 변환하는 개방형 소스 프로젝트가 있다. Synthea 데이터도 마찬가지이다. 이 프로젝트들은 기여자의 지속적인 노력으로 관리되고 있다.

### OMOP CDM의 구조적 한계

OMOP는 완벽하지 않다. 의미론적 불일치는 MIMIC 데이터를 OMOP로 변환할 때 주요 과제이다. MIMIC의 ICD 코드 날짜는 공식 진단 기간만 포착하지만 OMOP의CONDITION_OCCURRENCE 테이블은 진단이 시작되고 해결된 시점을 포함한다. 10년 전 당뇨병으로 진단받은 환자가 최근에야 MIMIC 데이터에 나타나는 경우, MIMIC 데이터만 보면 condition이 지난주에 시작된 것으로 오해할 수 있다.

### 데이터 모델 간 변환과 크로스워크

의료 데이터 모델 간 변환은 반복적인 과정으로, 기술적 역량과 주제 전문 지식의 결합이 필요하다. 그래프 데이터베이스와 지식 그래프는 이 반복 주기를 단축하는 데 잠재적인 도움이 될 수 있다.

## 진단과 약물 결합 사례 연구

MIMIC-III의 환자 수준 임상 데이터와 Synthea의 환자 수준 임상 및 청구 데이터를 결합하는 예를 살펴보겠다. 공통된 욕구는 서로 다른 데이터셋을 결합하여 더 큰 데이터셋을 만드는 것이다. 자연스럽게 가장 많은 데이터셋을 합치고 싶은 유혹이 있다. 그러나 MIMIC은 입원(특히 중환자실) 데이터에 편향되어 있고 Synthea 데이터셋은 외래/외래 환경에 초점을 맞추고 있다.

### OMOP CDM 대 그래프 데이터베이스 비교

OMOP 공통 데이터 모델(CDM)로 변환하여 서로 다른 데이터셋을 결합할 수 있다. 그래프 데이터베이스를 사용하면 동일한 인스턴스 내에서 여러 다른 매핑을 적용하여 비교하기가 더 쉽다. 소스 데이터 모델과 OMOP 간의 매핑이 명확한 경우 표준 접근법이 최선이다. 그러나 탐색 단계에서 아직 데이터를 어떻게 매핑할지 실험하고 있다면 그래프가 흥미로운 기회를 제공한다.

### 상황별 최적의 아키텍처 선택

George EP Box의 명언: "모든 모델은 틀렸지만 어떤 것은 유용하다." 데이터 모델에도 동일하게 적용된다. 모든 데이터 모델은 트레이드오프가 있고 특정 가정 세트로 우리를 몰아세운다. 실용적인 접근법을 유지하고 단일 완벽한 데이터 모델이라는 함정에 빠지지 않는 것이 중요하다.

### 데이터 결합 시 핵심 고려사항

**구조적 조화 대 의미론적 조화**: HIMSS 상호운용성 수준에서 차용한 세 가지 조화 수준이 있다.

**구문적 조화**: 데이터의 형식과 구조를 일치시키는 것이다. OMOP를 사용하면 구조적 조화 과제를 해결할 수 있다. 그러나 구조적 조화는 필요하지만 충분하지 않다.

**의미론적 조화**: 데이터의 전체 문맥과 의미를 보존하면서 보낼 수 있는 것이다. 원본 데이터의 의미와 문맥을 이해하고 대상 데이터 모델의 의미를 파악해야 한다.

**코딩 시스템과 크로스워크 사용하기**: 크로스워크 사용에 대한 과신은 흔한 문제이다. 귀하의 고유한 문제에 적용 가능한지는 별도의 검증이 필요하다.

## 결론 및 요약

서로 다른 데이터셋을 결합하거나 병합하는 것은 점점 더 인기를 얻고 있다. CDM을 사용하면 데이터 과학자의 효율성을 높일 수 있다. 그래프는 반복 과정에서 더 효율적이 될 수 있다.

다음 장에서는 이 개념들을 분석과 연결하는 방법을 살펴보겠다.

### 상황별 최적의 아키텍처 선택

George EP Box의 명언: "모든 모델은 틀렸지만, 어떤 것은 유용하다." 머신러닝이나 통계 모델에 대해 이야기할 때 자주 인용되지만, 데이터 모델에도 동일하게 적용된다. 모든 데이터 모델은 트레이드오프가 있고 특정 가정 세트로 우리를 몰아세운다. 어떤 데이터 모델도 데이터에 내재된 모든 문맥과 미묘한 차이를 정확하게 포착할 수 없다. 그러나 특정 유스케이스에서는 다른 모델보다 더 유용할 수 있다. 실용적인 접근법을 유지하고 단일 완벽한 데이터 모델이라는 함정에 빠지지 않는 것이 중요하다.

OMOP와 관계형 데이터베이스, 또는 그래프 데이터베이스를 사용하는 최선의 방법에 대해 끝없이 토론하고 실험할 수 있지만 이는 문제를 순수하게 기술적 관점에서 조망하는 것이다. 이제 기술 솔루션과 독립적인 관점에서 고려사항을 살펴보겠다.

## 다중 의료 데이터 소스 통합 시 주요 고려사항

이전 섹션에서 논의했듯이 데이터베이스 수준에서도 고려해야 할 사항이 많다. 일부는 임상 지향적이고 다른 것은 기술적이다. 몇 가지 핵심 영역을 더 깊이 파고들겠다.

**구조적 조화 대 의미론적 조화**

다음 수준은 구조적 조화이다. 여기서 데이터 모델, 비구조화된 데이터의 과제, 의료 분야의 모든 스캔 문서 처리에 대한 논의가 일반적으로 이루어진다. 예를 들어, 클리닉의 모든 환자 차트를 디지털화하여 클라우드 제공자에 업로드할 수 있다. 이제 PDF 문서 더미를 전자건강기록에서 추출한 구조화된 데이터와 어떻게 결합하는가?

구조적 조화는 기술자가 RWD를 처음 접할 때 흔히 보이는 함정 중 하나를 부각한다. 데이터 모델(예: OMOP)을 선택하면 구조적 조화 과제를 해결할 수 있다. OMOP를 선택하면 특정 데이터 필드를 정의하고 데이터의 구문과 형식을 명확히 한다. 이는 두 데이터셋을 조화하는 목표에 더 가까이 다가가지만 아직 거기에는 이르지 못한다! 구조적 조화는 필요하지만 충분하지 않다.

앞서 논의한 병원 대 응급실 입원 시간을 조건 시작의 프록시로 사용하는 예를 생각해보자. 필드가 날짜/시간 필드라는 것을 알고 특정 조건이 시작된 날짜와 시간을 포착해야 한다는 것을 안다. 그러나 어떤 소스 필드를 사용해야 하는지 여전히 조정해야 한다. 이를 위해서는 소스 데이터의 기본 의미와 문맥(병원 입원 타임스탬프와 응급실 입원 타임스탬프의 차이점은?)과 대상 데이터 모델의 의미(OMOP가 조건 시작 시간의 일부로 무엇을 포착하려고 하는가?)를 이해해야 한다.

이것을 피라미드 꼭대기인 의미론적 조화로 이끈다. 궁극적 목표는 데이터의 전체 문맥과 의미가 보존되는 방식으로 의료 데이터를 주고받을 수 있는 것이다. 조건 시작 대 병원 또는 응급실 입원 시간에 대한 이전 예제가 이 문제를 잘 보여준다. 의미론적 조화는 시맨틱 웹과 지식 그래프의 핵심 영역이다.

의미론적 조화가 어려운 이유는 두 모델의 의미가 동일하지 않기 때문이다. 한 모델의 데이터 요소가 다른 모델의 데이터 요소와 "동일하거나" "정확히 동일하지 않을 때" 매핑 결정이 필요하다. 이는 주변 문맥에 완전히 의존한다-과학적 질문이 무엇인지, 데이터가 왜 수집되었는지, 얼마나 정확해야 하는지 등.

MIMIC 데이터의 진단 코드 "순서"(SEQ_NUM)를 살펴보자. 이 열은 특정 병원 입원에 연결된 진단 코드의 순서 또는 순위를 저장한다. MIMIC-III 문서에 따르면 "코드의 우선순위. 시퀀스 1은 주요 코드이다." 주요 진단 코드는 일반적으로 방문의 원인 또는 이유이다. 그러나 이 값이 실제로 무엇을 의미하는지 파고들기 시작하면 모호함에 부딪힌다. SEQ_NUM 열은 어떤 종류의 순서를 전달하지만 관련성이나 우선순위를 정확하게 포착하는지는 의문이다. SEQ_NUM이 증가할 때 상대적 가중치가 불분명하다-1, 2, 3의 차이가 얼마나 되는가?

데이터 과학자로서 우리는 명확한 답을 원하여 데이터를 신뢰할 수 있게 변환하고 해석할 수 있기를 바란다. 그러나 의료 진단의 현실은 훨씬 더 미묘하다. 예를 들어, 911 시스템의 구급차에서 일할 때 교통사고로 부상을 입어 외상 센터로 급히 이송해야 하는 환자들을 상당히 많이 마주쳤다. 머리 부상을 입은 환자가 당뇨병으로 인한 저혈당 사건이 있었고 이로 인해 사고가 발생했다면? 결과적으로 이 환자의 SEQ_NUM 1은 당뇨병일 가능성이 높다. 사고는 당뇨병의 결과이지 그 반대가 아니다. 그러나 사고 자체가 환자를 심하게 다치게 했다면?

가장 흥미로운 점은 교통사고 환자와 당뇨병 환자가 별도의 테이블에 있으면서도 동일한 환자일 수 있다는 것이다. 이 문제를 해결하기 위해 OMOP와 OMOP 자체를 비교하는 것이 아니라 OMOP와 그래프를 비교한다.

### 코드 매핑 시스템 활용 방안

코드화된 데이터(예: ICD-9/10 코드, CPT 코드 등)가 포함된 데이터셋을 매핑할 때 흔히 발생하는 문제는 크로스워크 사용에 대한 과신이다. 크로스워크는 본질적으로 두 코드 시스템 간의 매핑 테이블이다. 예를 들어, ICD-9 코드에서 ICD-10 코드로의 변환을 돕기 위해 GEMs(General Equivalence Mappings)가 만들어졌다. ICD-10은 ICD-9보다 훨씬 더 구체적이며 세분화된 코드 체계이다.

그러나 GEMs에 대한 많은 학술 논문과 백서가 이 과제를 강조했다. Jones와 Nachimson이 백서에서 쓴 것처럼: "사실, GEMs를 '크로스워크'로 설명하는 것 자체가 심각하게 결함이 있다. 코드 변환 방법으로서의 수많은 단점과 결점을 포함하고 있으며 전혀 그런 목적으로 설계되지 않았다."

크로스워크는 일반적으로 특정 목적을 위해 특정 데이터 세트를 사용하여 만들어진다. 귀하의 고유한 문제에 해당 크로스워크가 적용되는지는 먼저 해결해야 할 질문이다. 문제와 크로스워크를 만드는 방법론이 크게 일치하면 목적에 맞을 수 있다. 그러나 대부분의 경우 최소한 출발점이거나 완전히 오해의 소지가 있다. 학술 기관, 정부 기관, 상업 기관에서 나온 크로스워크 모두에 적용된다.

## 결론 및 요약

서로 다른 데이터셋을 결합하거나 병합하는 것은 점점 더 인기를 얻고 있다. EHR과 청구 데이터를 결합하든, 여러 EHR 소스를 결합하든, 환자가 생성하거나 제공한 정보를 포함하든, 일반적으로 동일한 과제에 직면한다. 기술적 고려사항 외에도 두 데이터셋을 결합할 때의 의미론적 차이도 고려해야 한다. 구조적으로 조화할 수 있더라도 본질적으로 다른 의미의 데이터 요소를 결합하지 않도록 주의해야 한다. 그래프 데이터베이스와 데이터 처리 파이프라인은 이러한 과제를 해결할 수 없다. 임상 도메인 전문가가 필요하다.

다음 장에서는 이 개념들을 분석과 연결하는 방법을 살펴보겠다.

# 제6장 머신러닝과 분석: 그래프 기반 접근법

설명 통계/생물통계를 사용하여 데이터를 분석하려면 데이터를 테이블로 추출하는 것이 일반적으로 가장 의미 있다. 기존 도구와 라이브러리를 활용하여 바퀴를 재발명할 필요가 없다. 그러나 고급 분석과 머신러닝 유스케이스로 넘어가면 임베딩 사용이 점점 더 인기를 얻고 있다. 임베딩은 자연어 처리(NLP) 분야에서 인기를 얻었으며 대규모 데이터셋으로 작업할 때 강력하고 비용 효율적인 것으로 입증되었다. 데이터 과학자가 모든 데이터를 수동으로 주석 처리할 필요 없이 데이터셋에 내재된 암묵적 정보/지식을 활용할 수 있게 해준다.

그래프 임베딩은 단어 임베딩과 유사하게 그래프 구조의 벡터 표현을 생성한다. 목표는 그래프의 구조적 정보를 벡터 공간에 인코딩하여 기계가 이해할 수 있는 형태로 변환하는 것이다. 이렇게 하면 그래프 구조 분석, 클러스터링, 유사도 계산, 그래프 기반 머신러닝 알고리즘 적용이 가능해진다.

“비인슐린 의존 당뇨병(non-insulin dependent diabetes mellitus)”과 같습니다. 궁극적으로 데이터 모델을 어떻게 설계하거나 구축할지는 시맨틱 개념을 파이프라인에 어떻게 통합할 계획인지에 따라 달라집니다. 데이터가 UMLS CUI로 교체하고자 하는 ICD-10 코드 컬럼으로 구성되어 있다면, 앞서 살펴본 형식은 조회해야 하는 ICD 코드가 JSON 객체의 깊숙한 곳에 묻혀 있기 때문에 매우 비효율적입니다. 반대로 CUI 컬럼이 있고 이를 연결된 ICD-10 코드로 교체하고자 한다면 이전 구조로도 쉽게 처리할 수 있습니다. 또 다른 대안은 데이터를 따라 용어집을 함께 통합하여 SQL 데이터베이스에 표준 어휘/용어 체계를 직접 통합하는 것입니다. 이렇게 하면 SQL 쿼리 자체에 모든 것을 결합할 수 있으므로 데이터 파이프라인이 단순해집니다. 물론 그에 따른 트레이드오프(trade-off)는 SQL 쿼리가 전반적으로 훨씬 더 복잡해진다는 점입니다. 이것이 바로 OHDSI 커뮤니티가 OMOP에 사용하는 접근 방식이며 다음 절의 주제이기도 합니다.

## OMOP CDM 데이터 쿼리 및 특징 추출

용어 체계를 통합하기 위한 여러 가지 선택지 중에서, OMOP 커뮤니티는 이를 데이터베이스에 직접 완전히 통합하기로 결정했습니다. 이를 통해 동일한 SQL 쿼리를 모든 OMOP 인스턴스에서 활용할 수 있어, 임상 연구자들은 연합 데이터 공유/연구 네트워크(다만 “연합 학습(federated learning)”의 의미는 아닙니다) 전반에서 쿼리를 공유할 수 있게 됩니다.

### OHDSI 표준 쿼리 활용

*The Book of OHDSI*는 훌륭한 자료이며 OMOP 작업을 수행할 때 항상 가장 최신의 자료로 고려해야 합니다. 다른 소프트웨어 라이브러리와 마찬가지로 OMOP도 지속적으로 개선되고 업데이트되므로 항상 공식 소스를 직접 확인하는 것이 가장 좋습니다.

OMOP에서 데이터를 쿼리하고 제어 어휘집(controlled vocabularies)에 연결된 구조화된 개념(concepts)을 다룰 때는 어휘(vocabulary) 테이블에 매우 익숙해져야 합니다. 이것이 많은 OMOP SQL 쿼리가 중첩(nested)되는 주된 이유 중 하나입니다. OHDSI 커뮤니티 저장소에서 가져온 예제 6-2에서는 특정 약물 세트에 “노출(exposure)”된 환자 수를 세려고 합니다. 여기서 “노출(Exposure)”이라는 용어를 사용하는 이유는 (아마도 간병인이나 의료진에 의한) 투약부터 환자가 일반의약품(OTC)으로 복용하는 것까지 모든 것을 포괄하기 때문입니다.

```sql
-- 예제 6-2. OMOP 쿼리 예시
SELECT concept.concept_name, drug_concept_id, count(person_id) AS num_persons
FROM drug_exposure
JOIN concept ON drug_concept_id = concept.concept_id
WHERE lower(domain_id)='drug'
  AND vocabulary_id='RxNorm'
  AND standard_concept='S'
  AND drug_concept_id IN (40165254, 40165258)
GROUP BY concept.concept_name, drug_concept_id;
```

보시다시피, 이 쿼리는 `drug_exposure`와 `concept` 테이블을 조인하여 RxNorm(약물/의약품 용어 체계)의 일부이면서 특정 세트(concept_id가 40165254 또는 40165258인 항목)와 일치하는 개념들을 검색합니다. 이 예제에서는 고콜레스테롤 치료에 사용되는 “스타틴(statin)” 계열 약물인 크레스토(Crestor, 성분명 로수바스타틴[rosuvastatin])에 해당하는 두 개의 특정 개념(`concept_id`로 표현됨)을 찾고 있습니다. 아테나(Athena)에서 “crestor”라는 키워드를 검색하고 결과를 RxNorm 내의 유효한 표준 약물 개념으로 제한하더라도, 잠재적으로 사용될 수 있는 개념이 여전히 11개나 존재함을 알 수 있습니다(특정 용량을 무시한다고 가정할 때). 만약 데이터셋이 미국 국립의약품코드(NDC, National Drug Code)를 기준으로 약물을 추적하고 있다면, 일치하는 개념이 143개나 있음을 확인할 수 있습니다. 여기서 선택할 수 있는 방법은 143개의 모든 NDC 개념을 포함하도록 쿼리를 확장하거나, 중첩 SQL 쿼리를 사용하여 비표준 NDC 코드를 표준 RxNorm 코드로 매핑하는 것입니다.

중첩 쿼리를 다루기 전에, 예제 6-3과 같이 관심 있는 두 RxNorm 개념과 관련된 모든 NDC 코드를 어떻게 쿼리할 수 있는지 살펴보겠습니다.

```sql
-- 예제 6-3. RxNorm 및 NDC 코드 매핑
SELECT concept_id_2 AS standard_concept_id
FROM concept_relationship
INNER JOIN concept source_concept ON concept_id = concept_id_1
WHERE concept_id IN (40165254, 40165258)
  AND vocabulary_id = 'RxNorm'
  AND relationship_id = 'Maps to';
```

이 쿼리는 크레스토 20mg 및 40mg에 매핑된 모든 개념을 반환합니다. 이 쿼리를 활용하면 모든 단일 `concept_id`를 수동으로 일일이 복사할 필요 없이 (예제 6-4에 나타난 것처럼) 중첩 SQL 쿼리를 사용할 수 있습니다. 이는 코드를 깔끔하게 정리해주고, 오류/버그의 가능성을 줄이며, 쿼리의 취약성을 낮춰줍니다.

```sql
-- 예제 6-4. 어휘집을 위한 중첩 SQL 쿼리
SELECT concept.concept_name, drug_concept_id, count(person_id) AS num_persons
FROM drug_exposure
JOIN concept ON drug_concept_id = concept.concept_id
WHERE lower(domain_id)='drug'
  AND vocabulary_id='RxNorm'
  AND standard_concept='S'
  AND drug_concept_id IN (
    SELECT concept_id_2 AS standard_concept_id
    FROM concept_relationship
    INNER JOIN concept source_concept ON concept_id = concept_id_1
    WHERE concept_id IN (40165254, 40165258)
      AND vocabulary_id = 'RxNorm'
      AND relationship_id = 'Maps to'
  )
GROUP BY concept.concept_name, drug_concept_id;
```

눈 밝은 독자라면 이전 쿼리가 우리가 의도했던 바와 완전히 일치하게 동작하지는 않을 수 있음을 눈치챘을 것입니다. 우리는 크레스토 20mg 또는 40mg을 복용 중인 모든 환자의 수를 세고자 했지만 이전 쿼리는 실제로는 우리가 제공한 두 RxNorm 코드에 연결된 각 NDC 코드별로 환자를 그룹화하게 됩니다. 이 시점에서 이 쿼리를 추가로 다듬는 것은 표준적인 SQL 연습 문제에 불과하지만 OMOP 커뮤니티가 환자 수준 데이터와 용어 체계 및 제어 어휘집을 어떻게 결합했는지 엿볼 수 있습니다. 덕분에 SQL 기반으로 구축된 수많은 기존 도구 및 플랫폼과 시스템을 손쉽게 통합할 수 있습니다.

## 그래프에서 데이터프레임으로 변환

이전 절에서는 특히 제어 어휘집이나 용어 체계를 다루는 맥락에서 SQL 데이터베이스로부터 데이터를 추출하는 방법을 살펴보는 데 시간을 보냈습니다. 관계형 데이터베이스는 데이터를 테이블에 저장하므로, SQL 쿼리는 자연스럽게 테이블 형식의 구조로 데이터를 반환합니다.

이번 절에서는 그래프에서 데이터프레임(dataframe)으로 데이터를 추출하는 방법을 살펴보겠습니다. 개념적으로 그래프를 테이블로 변환하는 것은 데이터를 저장하고 표현하는 방식이 본질적으로 다르기 때문에 결코 간단하지 않은 작업입니다. 하지만 우리는 그래프 자체를 테이블 형식으로 광범위하게 표현하려는 것이 아닙니다. 대신 매우 특정한 관점에서 데이터의 하위 집합(subset)을 추출하려는 것입니다. 예를 들어 환자, 환자의 투약 약물, RxNorm 및 NDC 기반의 모든 표준화된 약물 표현, 환자의 진단, ICD-10 코드가 완전히 연결된 그래프가 있다고 가정해 봅시다. 우리는 이 모든 것을 단일 데이터프레임(혹은 소수의 데이터프레임)으로 전부 추출하고 싶지는 않을 것입니다. 그렇게 하려면 기본적으로 의료 데이터용 테이블과 어휘집용 테이블을 갖추고 둘을 조인하는 OMOP과 유사한 데이터 모델을 다시 만들어야 하기 때문입니다.

### Neo4j 및 py2neo 활용

Neo4j로 작업할 때는 `RETURN` 절에서 무엇을 반환할지 정확하게 제어할 수 있습니다. 일반적으로는 2장에서 소개했던 것과 동일한 모델을 사용하여 예제 6-5와 같이 노드와 엣지(관계) 자체를 반환합니다.

```cypher
// 예제 6-5. Cypher: 노드 및 엣지 반환
MATCH (p:Patient)-[o:has_order_for]-> (m:Medication {Medication_name: "Acetaminophen (Tylenol)"})
RETURN p
```

이를 테이블로 변환하려고 한다면, 노드 `p`와 엣지 `o`의 프로퍼티(속성)를 컬럼으로 "풀어내기(unpack)" 위한 추가 코드를 작성해야 합니다. 하지만 예제 6-6에서 강조하듯이 Cypher 쿼리의 일부로서 직접 이를 풀어낼 수 있습니다. 엣지의 프로퍼티를 다루는 방법을 보여주기 위해, 환자와 약물을 연결하는 엣지/관계에 `date_prescribed`라는 프로퍼티도 포함되어 있다고 가정해 보겠습니다.

```cypher
// 예제 6-6. Cypher: 프로퍼티 반환
MATCH (p:Patient)-[o:has_order_for]-> (m:Medication {Medication_name: "Acetaminophen (Tylenol)"})
RETURN p.Patient_ID, p.DOB, o.date_prescribed, m.Medication_name
```

보시다시피 환자 ID, 환자 생년월일, 처방 일자, 약물 이름이라는 4개의 컬럼 목록을 얻게 됩니다. `py2neo`와 같은 드라이버를 사용하여 이전 쿼리를 실행하면, 예제 6-7과 같이 반환값을 `pandas`로 직접 전달하여 데이터프레임을 생성할 수 있습니다.

```python
> **참고:** 예제 6-7. py2neo: Cypher를 데이터프레임으로 변환
from py2neo import Graph
import pandas as pd

> **참고:** 데이터베이스와 상호작용하기 위한 세션 설정
session = Graph("bolt://somehost:7687",...)

query = '''
MATCH (p:Patient)-[o:has_order_for]-> (m:Medication {Medication_name: "Acetaminophen (Tylenol)"})
RETURN p.Patient_ID, p.DOB, o.date_prescribed, m.Medication_name
'''

> **참고:** Neo4j를 대상으로 쿼리 실행
result = session.run(query).data()
```

```python
> **참고:** `result`를 기반으로 데이터프레임 생성
df = pd.DataFrame(result)
```

이러한 예제들에서 볼 수 있듯이, 쿼리 결과를 데이터프레임으로 가져오는 것은 무엇보다도 프로그래밍 및 소프트웨어 엔지니어링 작업에 가깝습니다. 파이썬(Python) 이터레이터와 딕셔너리 사이에서, 이러한 작업은 대개 데이터베이스 드라이버 자체에 의해 처리됩니다. pandas 데이터프레임의 인기가 계속 높아짐에 따라, 데이터를 데이터프레임과 상호 쉽게 변환할 수 있는 라이브러리가 점점 더 많이 등장할 것으로 보입니다. 데이터를 데이터프레임으로 변환하는 엔지니어링 측면 외에도, 이전 예제들은 그래프를 쿼리하는 개념적 접근 방식이 3장과 4장에서 논의했던 예제들과 다르지 않다는 점을 보여줍니다. 그래프의 구조와 쿼리를 이해하고 나면, 단순히 그 결과를 데이터프레임으로 변환하기만 하면 됩니다. 그렇다면 그래프를 쿼리하고 데이터프레임으로 변환하는 일이라면, 애초에 왜 굳이 그래프를 사용할까요? 결국 처음부터 관계형 데이터베이스를 사용하기만 해도 데이터는 이미 테이블 형태로 나올 텐데 말입니다. 다음 절에서 이 질문에 대한 답을 시작해 보겠습니다.

## 왜 그래프의 복잡성을 도입하는가?

지금까지 우리는 그래프에서 데이터를 테이블로 추출하는 방법을 살펴보는 데 얼마간의 시간을 보냈습니다. 결국 모든 것을 다시 테이블로 변환할 것이라면 왜 이런 번거로운 과정을 거치는지 궁금할 수 있습니다. 주요 이점은 데이터의 문맥과 의미를 가능한 한 많이 보존하면서 더욱 자연스러운 방식으로 데이터를 캡처하고 표현하기 위해 그래프 데이터베이스를 활용할 수 있다는 점입니다. 는 전통적인 비그래프(nongraph) 접근 방식(a)이 그래프 접근 방식(b)보다 본질적으로 더 많은 리소스를 소모하고 복잡하다는 가설과 함께 두 가지 옵션을 강조합니다.

현재 우리는 데이터의 구조화(structuring), 정제(cleaning), 정규화(normalizing), 조화(harmonizing) 프로세스를 하나로 융합하여 다루고 있습니다. 이러한 모든 단계는 거의 모든 프로젝트에서 필요하며 항상 원천 데이터와 다운스트림 분석 모두에 의존적이지만 이들을 분리(decoupling)하고 프로세스 초기에 그래프 사용을 도입하면 분석을 위한 데이터 준비의 전반적인 과정을 단순화할 수 있습니다. 각 단계를 관리하기가 훨씬 쉬워지며 전체 시스템의 복잡성이 줄어듭니다.

이 마지막 부분은 특히 흥미로우며 데이터 과학 분야에서 다소 과소평가되고 있는 부분이기도 합니다. 소프트웨어 엔지니어의 관점에서 볼 때 모놀리식(monolithic) 코드베이스를 작성하려는 것은 재앙을 부르는 지름길입니다. 예를 들어 머신러닝 모델, 사용자 인터페이스(UI), 서버와의 네트워크 통신을 결합한 웹 애플리케이션을 상상해 보십시오. 사용자 인터페이스는 네트워크 코드와 상호작용하고, 네트워크 코드는 예측을 위해 ML 모델로 데이터를 전송하며, 결과는 다시 사용자에게 전송됩니다. 만약 이 전체 앱이 단일하고 연속적인 프로세스로 작성되었다면 극도로 복잡하고 취약할 것이며 무언가 변경될 때마다 상당한 재작업이 필요할 것입니다. ML 모델의 특성(feature) 중 일부를 변경해야 한다면 네트워크 코드 역시 수정해야 할 것입니다. 하지만 소프트웨어 엔지니어들은 표준적인 관행이 이러한 각 컴포넌트/모듈을 분리하여 한쪽의 변경이 다른 쪽에 미치는 영향을 최소화하도록 가능한 한 분리(디커플링)하는 것이라고 곧바로 말해줄 것입니다. 올바르게 엔지니어링되었다면, ML 모델의 변경으로 인해 UI에 새 필드를 추가해야 할 수는 있지만 네트워크 코드는 영향을 받지 않을 것입니다.

우리의 데이터 파이프라인에 대해서도 동일한 방식으로 생각해야 합니다. 저는 데이터 팀이 데이터 조화, 데이터 모델 설계(또는 선택), 데이터 정제를 모두 한 번에 해결하려는 단일한 모놀리식 문제로 취급하는 프로젝트를 많이 봅니다. 대신 이를 명확한 단계로 나누어 봅시다. 여기서 우리는 전통적인 접근 방식의 어려움에 직면하기 시작합니다. SQL 시스템에서 데이터 모델을 설계하거나 선택할 때, 우리는 null 값을 어떻게 처리할지, 데이터를 어떻게 연결할지, 무엇을 연결할 수 있고 연결해야 하는지 등 수많은 사전 결정을 내려야 합니다. 트랜잭션 사용 사례에서 정형 데이터를 안정적으로 캡처하는 데 SQL 데이터베이스를 훌륭한 선택지로 만들어 주는 바로 그 특성들이, 데이터를 해석, 처리, 정규화, 조화하는 방법이 다양한 분석 사용 사례에서는 오히려 취약한 플랫폼이 되게 만듭니다.

그래프를 통합하면 데이터 파이프라인의 특정 부분을 표준화하는 동시에, 다른 부분은 분석에 최대한 가까운 시점으로 미룰(defer) 수 있습니다. 예를 들어, 초기에 데이터 모델의 미묘한 뉘앙스와 세부 사항에 대해 걱정하는 대신(특히 데이터를 정확히 어떻게 해석해야 할지 여전히 고민 중인 경우), 특정 데이터 포인트를 여러 다른 코딩 시스템이나 의미적 개념에 쉽게 연결할 수 있는 그래프에 데이터를 캡처합니다. 그런 다음 사용 사례가 명확해지고 데이터를 해석하는 방법을 더 잘 이해하게 됨에 따라, 그래프 순회(graph traversal)를 수행하고 기존 도구에 부합하는 테이블 형태로 데이터를 추출합니다. 테이블 중심의 워크플로에 그래프를 도입해야 하는 이유를 더 잘 이해했으므로, 이제 머신러닝, 특히 특성 엔지니어링 과정에서 그래프가 어떻게 도움이 될 수 있는지 몇 가지 예제를 살펴보겠습니다.

## 그래프 기반 머신러닝과 특성 엔지니어링 파이프라인

이 절의 주된 초점은 그래프를 머신러닝 및 특성 엔지니어링에 어떻게 통합할 수 있는지이지만 동일한 개념이 다른 분석(예: 생존 곡선, 혼합 효과 모델 등)을 위한 데이터 가공에도 적용됩니다. 특성 엔지니어링(feature engineering)은 대개 데이터를 변환하거나 가공한 후 머신러닝에 포함할 항목을 필터링하거나 선택하는 일련의 조합으로 정의됩니다. 그러나 이는 모든 분석을 수행할 때도 똑같이 해야 하는 작업입니다. 데이터를 변환하거나 가공하는 것은 일반적으로 엄밀히 정의되어 있지 않으며 실제로 거의 모든 것을 포함할 수 있습니다. 데이터 과학자들은 종종 정량적 정규화(예: 데이터를 평균 0, 표준편차 1로 표준화하거나 [0, 1]과 같은 특정 구간에 오도록 정규화) 또는 주성분 분석(PCA)과 같은 더욱 정교한 기법을 생각합니다(때로는 이것이 데이터를 "필터링"하는 과정의 일부로 간주되기도 합니다). 그러나 이 책 전반에서 살펴보았듯이 헬스케어 RWD로 작업할 때는 데이터의 의미론(데이터의 문맥과 의미)을 고려하여 또 다른 수준의 변환을 추가해야 합니다. 따라서 여러 NDC 코드를 하나의 RxNorm 코드로 결합하기로 결정할 때, 이는 어떤 수준에서는 특성 엔지니어링에 해당합니다. 때로는 이러한 결정이 데이터 과학자에 의해 분석의 일부이자 사용 사례에 매우 특화된 형태로 이루어집니다. 때로는 이러한 결정이 단순히 병원의 내부 프로세스나 원천 데이터에 대한 이해의 결과이기도 합니다. 이 절의 논의에서는 보다 구체적으로 프로세스를 다음과 같은 정의로 더욱 세분화하겠습니다.

### 데이터 정제 (Cleaning)
정제(cleaning)는 더 복잡한 변환을 진행하기 전에 데이터가 기본적인 구문 및 구조적 요구사항을 충족하는지 확인하는 프로세스입니다. 구체적인 작업의 예로는 데이터 타입 수정(예: 숫자 대 문자열), 문자열이 정규 표현식을 준수하는지 확인(예: 날짜 또는 ICD 코드), 문자열의 대소문자 변경(예: "male"을 "MALE"로) 등이 있습니다. 간단히 말해, 이는 후속 단계가 오류 없이 성공할 가능성을 높이기 위해 구문(syntactic) 수준에서 데이터를 정리하는 단계입니다. 정제는 특정 사용 사례에 가장 덜 의존적이며 주로 주어진 데이터 소스에 따라 달라집니다. 특정 분석 알고리즘이나 데이터의 문맥 및 의미에 의존하는 변환은 조화의 일부로 처리됩니다.

### 데이터 조화 (Harmonization)
조화(harmonization)는 이 책 전반에서 다룬 대부분의 주제를 포괄하며 종종 의미론적(semantic) 수준에서 데이터를 처리하는 것을 포함합니다. 여기에는 한 코드 시스템의 코드를 다른 코드 시스템으로 변환하거나, 서로 다른 데이터셋을 병합 또는 결합하거나, 정량적 데이터를 생리학적 한계 범위로 제한하거나, 약물 이름을 RxNorm 개념과 매칭하는 작업 등이 포함됩니다(이에 국한되지 않음). 이 단계에서는 열을 제거하는 경우가 드물며 대신 추가 열을 추가합니다. 예를 들어 약물 이름이나 NDC 코드가 있는 열이 있고 NDC 코드를 RxNorm 개념으로 변환하려는 경우, 매핑된 RxNorm 코드가 포함된 새 열을 추가하기만 하면 됩니다. 메모리 제약이 있거나 속도가 너무 느린 경우가 아니라면, 이는 정보 손실을 줄여주므로 권장되는 관행입니다. 분석을 수행할 때 매핑된 데이터와 매핑 전 데이터 모두에 접근할 수 있기 때문입니다.

### 특성 엔지니어링 (Feature Engineering)
특성 엔지니어링(feature engineering)은 분석 알고리즘의 구체적인 특성을 고려한 데이터 처리와 변수/데이터 요소의 순위 지정 및 필터링입니다. 한 예로, 데이터 간의 "거리" 계산에 의존하는 알고리즘을 다룰 때 특정 변수가 다른 변수보다 더 큰 영향을 미치지 않도록 숫자 범위를 스케일링하는 경우를 자주 볼 수 있습니다(예: min-max 스케일링). 또 다른 예로는 정보 손실을 최소화하면서 데이터셋의 차원을 축소하기 위해 주성분 분석을 적용하는 경우가 있습니다. 또한 항암화학요법(chemotherapy) 치료법을 비교할 때 환자의 혈압 측정값을 무시하는 것처럼, 특정 변수나 데이터 요소가 관련이 없음을 알고 배제하는 경우도 여기에 해당합니다. 또 다른 흔한 예는 범주형 데이터로 작업할 때 원-핫 인코딩(one-hot encoding)을 사용하는 것입니다. 목록은 끝없이 이어지지만 여기서 핵심은 매우 구체적인 사용 사례의 맥락에서, 그리고 종종 특정 분석 기법의 수학적 또는 생물통계학적 요구사항에 따라 데이터를 처리한다는 점입니다.

정제, 조화, 특성 엔지니어링을 이렇게 상세히 구분한 이유는 그래프가 (일반적으로 "정제된" 데이터가 필요한) 조화에 특히 유용하며 특성 엔지니어링에는 그렇지 않다는 점을 강조하기 위해서입니다. 그러나 제가 본 대부분의 데이터 파이프라인은 에 나와 있는 것처럼 이 모든 단계를 하나로 묶어 처리합니다. 초기 탐색적 데이터 분석(EDA)을 거친 후, 원하는 결과/인사이트를 얻을 때까지 정제/조화/특성 엔지니어링 및 데이터 분석 프로세스를 반복합니다.

이러한 접근 방식의 문제는 모든 프로젝트나 사용 사례마다 이 과정을 계속해서 반복하게 된다는 점입니다(). 이는 데이터 과학자, 역학자(epidemiologist), 생물통계학자가 작업의 많은 부분을 반복하게 되므로 극도로 비효율적입니다. 데이터 팀에서 사용하는 세 개의 주요 데이터 소스가 있고 팀에 10명의 데이터 과학자가 있다고 상상해 보십시오. 한 열의 형식이 변경되면, 모든 데이터 과학자가 본질적으로 코드에 동일한 수정을 가해야 함에도 불구하고 각 데이터 과학자가 개별적으로 이를 처리해야 합니다.

'특성 저장소(feature store)는 어떤가?'라고 생각할 수도 있습니다. 이는 데이터 엔지니어링 프로세스를 더 잘 관리할 수 있는 프레임워크를 제공하므로 인기를 얻고 있는 비교적 새로운 개념입니다. 특성 저장소의 세부 사항 중 일부는 학습 데이터에 적용된 모든 데이터 처리가 예측을 위한 새로운 데이터에도 동일하게 적용되도록 보장하는 등 머신러닝 파이프라인에 특화되어 있습니다. 은 특성 저장소를 포함하도록 데이터 파이프라인이 어떻게 발전했는지를 단순화하여 보여줍니다. 기본 파이프라인에서 보았듯이 분석 단계와 데이터 처리 단계 사이에 피드백 루프가 있음을 알 수 있습니다. 다이어그램 중간에서 (운이 좋다면) 특성 저장소의 특성을 재사용할 수 있는 추가적인 사용 사례가 있다는 점도 확인할 수 있습니다. 이는 기본 데이터 파이프라인보다 분명 한 단계 발전한 것이지만 데이터를 처리할 때 여전히 일부 중복 작업이 발생합니다. 기술 통계나 생물통계와 관련된 사용 사례의 경우에도, 학습 데이터나 예측 생성과 같은 머신러닝 특화 개념을 걱정할 필요는 없지만 특성 저장소의 개념은 여전히 적용 가능합니다. 계보(lineage), 재사용성(reusability), 재현성(reproducibility), 거버넌스(governance) 개념은 여전히 적용되며 유용합니다.

지금까지 우리는 데이터 파이프라인, 머신러닝, 특성 저장소에 대해 논의해 왔으며 이들 모두 그래프를 사용하지 않는 솔루션이 많이 존재합니다. 특성 저장소를 추가하여 전반적인 데이터 파이프라인을 개선했음에도 불구하고 우리는 여전히 정제, 조화, 특성 엔지니어링을 단일 파이프라인으로 묶고 있습니다. 이는 데이터 과학자의 관점에서는 종종 가장 빠르고 쉬운 해결책입니다. 특정 데이터셋에 접근하여 EDA를 수행한 다음 사용 사례에 맞게 데이터를 처리하기 시작하면 되기 때문입니다. 앞서 논의했듯이 특히 데이터 과학 팀이 성장함에 따라 이러한 접근 방식에는 여러 단점이 있습니다. 소프트웨어 엔지니어들은 무언가가 계속해서 반복된다면 이를 추상화하여 라이브러리로 유지 관리해야 한다고 종종 말합니다. 이 경우에도 마찬가지이며 파이프라인에 또 다른 계층을 추가함으로써 이를 시작할 수 있습니다. 정제와 조화 후 파이프라인의 시작 부분에 지식 그래프(knowledge graph)를 통합할 수 있습니다. 지식 그래프가 주목받고 있지만 무엇이 지식 그래프를 구성하는지에 대한 단일하고 엄밀한 정의는 없습니다. 이 맥락에서 저는 지식 그래프를 환자 수준 데이터와 용어체계 및 온톨로지의 표준화된 개념이 결합된 것으로 정의합니다. 데이터 파이프라인에 이 추가 단계를 도입함으로써 데이터 소스당 한 번만 데이터 정제와 조화가 처리되는 프로세스를 구축하게 되었습니다. 데이터 과학자는 지식 그래프의 다운스트림에서 데이터를 다루며 평소에 사용하던 것과 동일한 워크플로를 따릅니다.

이 지식 그래프를 어떻게 한 번 만들어서 수많은 사용 사례에 활용할 수 있는지 궁금할 것입니다. 이는 매우 좋은 질문이며 이를 한 번에 완벽하게 끝내려는 의도는 아닙니다. 우리가 아직 논의하지 않은 한 가지는 파이프라인의 피드백 순환 구조입니다. 이는 데이터 과학자가 특정 사용 사례의 맥락에서 데이터에 대한 더 깊은 이해를 발전시키고 데이터 처리 방식을 개선하는 프로세스를 나타냅니다. 분석 단계에서 데이터를 다르게 정제하거나, 조화하거나, 특성을 엔지니어링하고 싶다는 점을 깨달을 수 있습니다. 지식 그래프를 통합하면 두 가지 피드백 기회를 얻게 됩니다. 특성을 다르게 엔지니어링하도록 선택할 수도 있고 더 근본적인 문제가 있어 지식 그래프를 업데이트해야 한다는 점을 깨달을 수도 있습니다. 이러한 특정 접근 방식은 몇 가지 기본적인 가정을 전제로 합니다.

1. 환자 수준 데이터(예: 진단, 투약 등)가 동일한 그래프 내에서 용어체계/온톨로지의 개념과 함께 저장됩니다.
2. 노드/관계(정점/엣지)에는 기본 데이터 및 분석 사용 사례에 대한 추가 문맥을 제공하는 메타데이터가 태그로 지정됩니다. 예를 들면 다음과 같습니다.
   a. 원본/원시 데이터 대 파생된 노드 또는 관계.
   b. 파생 데이터의 경우, 기본 코드 또는 프로세스로 다시 연결되는 링크(예: git 커밋 해시, 날짜/타임스탬프 또는 임상 진료 지침).
3. 지식 그래프는 특정 사용 사례를 위해 하위 그래프(subgraph)가 추출되는 상위 그래프(supergraph)입니다.

이제 여러분의 분석 스택이 데이터프레임과 테이블 형태의 데이터 표현을 중심으로 구성되어 있더라도, 왜 그래프가 데이터 과학 워크플로 및 파이프라인에서 가치 있는 부분이 될 수 있는지 감을 잡으셨을 것입니다. 비교적 동질적인 데이터로 작업하는 매우 작은 팀의 경우 이러한 접근 방식을 정당화하기 어려울 수 있지만 팀의 규모나 사용 사례의 수가 증가할수록 그 가치는 더욱 커집니다. 다음 절에서는 딥러닝에 기반을 둔 기법을 비롯하여 그래프 구조를 직접 분석하는 방법들을 살펴보겠습니다.

## 헬스케어 머신러닝과 그래프 임베딩

기술 통계/생물통계를 사용하여 데이터를 분석하려는 경우, 데이터를 테이블로 추출하는 것이 일반적으로 가장 합리적입니다. 이미 존재하는 바퀴를 다시 발명할 필요 없이 기존 도구와 라이브러리를 활용할 수 있기 때문입니다. 그래프를 어떻게 저장하고 있는지에 따라, 저장 프로시저(stored procedure)나 쿼리에 로직을 포함하는 기타 커스텀 방식을 사용하여 쿼리의 일부로 계산을 직접 내장할 수도 있습니다. 그러나 고급 분석 및 머신러닝 사용 사례로 넘어가면서 점점 더 인기를 얻고 있는 접근 방식 중 하나는 임베딩(embedding)을 활용하는 것입니다. 임베딩은 자연어 처리(NLP)와 같은 분야에서 매우 대중화되었으며 대규모 데이터셋을 다룰 때 강력하고 훨씬 더 비용 효율적임이 입증되었습니다. 데이터 과학자는 모든 데이터에 수동으로 레이블을 달(annotate) 필요 없이 데이터셋 내에 포함된 암묵적인 정보/지식을 활용할 수 있습니다.

NLP나 임베딩을 조금이라도 접해본 적이 있다면 king, queen, man, woman 단어를 사용한 고전적인 예제를 보았을 것입니다. 이는 임베딩과 벡터 연산의 근본적인 개념적 측면을 잘 포착하지만 과제를 지나치게 단순화한 면이 있습니다. 데이터 과학의 다른 모든 것과 마찬가지로 이러한 예제들도 많은 튜닝이 필요합니다. 하지만 임베딩은 우리 도구함에 또 하나의 훌륭한 도구를 제공하며 우리는 이를 가져와 그래프에 맞게 적용할 것입니다. 단어 임베딩과 유사하게, 그래프 임베딩은 그래프 구조의 벡터 표현을 생성하여 신경망과 같은 머신러닝 접근 방식에서 이러한 벡터를 사용할 수 있게 해줍니다. 본질적으로 그래프 임베딩을 사용하면 그래프의 공간 구조를 수많은 알고리즘에 입력할 수 있는 수치형 값의 벡터로 변환할 수 있습니다. 이 절에서는 node2vec을 시작으로 그래프 임베딩의 몇 가지 응용 예제를 살펴보겠습니다.

### node2vec: 그래프 구조 임베딩

이후에 나오는 예제 중 일부는 생의학적(biomedical) 응용에 특화되어 있지만 node2vec은 그래프 데이터의 임베딩을 생성하는 일반적인 접근 방식입니다. 기본적으로 node2vec은 임베딩을 생성하기 위해 word2vec에서 사용하는 접근 방식을 활용합니다. word2vec의 작동 방식에 익숙하지 않더라도, 이는 매우 널리 알려진 주제이며 수많은...

블로그 글과 동영상 등에서 자세한 튜토리얼을 확인할 수 있습니다. 본질적으로 신경망은 단어 시퀀스에서 다음 단어를 예측하도록 훈련됩니다. 그러나 우리는 이 예측 모델을 실제로 사용하지는 않습니다. 우리가 진정으로 원하는 것은 은닉층(hidden layer)의 가중치(weights)입니다. 이 가중치들이 바로 단어의 벡터 표현(vector representation)을 구성합니다.

그래프 임베딩을 위해 그로버(Grover)와 레스코벡(Leskovec)[^4]은 그래프의 노드에 무작위 보행(random walk)을 적용하여 노드 시퀀스를 생성하는 접근 방식을 고안했습니다. 그들 접근 방식의 핵심 요소는 그래프를 무작위로 탐색(walk)하여 일련의 시퀀스를 생성하는 것입니다. 이러한 노드 시퀀스는 토큰 시퀀스와 유사하며 단어 임베딩에 사용된 것과 동일한 접근 방식에 입력됩니다. 다시 말해, 텍스트를 각 노드가 문장이나 구문 내의 토큰인 노드 시퀀스로 생각할 수 있으므로, 그래프를 일련의 노드 시퀀스로 축소할 수만 있다면 단어 임베딩을 생성하는 접근 방식을 임의의 그래프에 맞게 조정할 수 있습니다.

node2vec을 이해하려면 알아야 할 두 가지 핵심 사항이 있습니다. 첫째는 앞서 논의했듯이 word2vec을 이해하는 것이고 둘째는 노드 시퀀스를 생성하기 위해 그래프에 무작위 보행을 적용하는 것을 이해하는 것입니다. 가장 단순하게 말하면, 무작위 보행은 특정 노드에서 시작하여 다음에 따라갈 엣지(edge)를 무작위로 선택함으로써 그래프를 순회하고 방문한 노드 시퀀스를 생성합니다. node2vec은 무작위 보행을 기반으로 구축되었으며 p와 q라는 두 매개변수를 기반으로 순회에 편향(bias)을 줍니다. 이 두 매개변수를 통해 다음과 같이 탐색 방식에 영향을 줄 수 있습니다.

- p - "복귀(return)" 매개변수로, 이전에 방문했던 노드로 되돌아갈 가능성에 영향을 줍니다.
- q - "입출(in-out)" 매개변수로, 현재 노드의 이웃에 머무르게 하거나 현재 노드에서 더 먼 곳으로 이동시키는 엣지를 따를 가능성에 영향을 줍니다.

두 경우 모두 실제로 p와 q의 역수를 사용합니다. p 값이 높으면 노드를 다시 방문할 가능성이 낮아지고 p 값이 낮으면 되돌아갈(backtrack) 가능성이 높아져 더 국소적인(local) 영역에 머무르게 됩니다. 반면에 q 값이 높으면 안쪽(가까운 곳)에 집중할 가능성이 높아지고 q 값이 낮으면 바깥쪽(네트워크의 더 먼 곳)을 탐색하는 데 집중할 가능성이 높아집니다.[^5]

node2vec을 다룰 때, 입출 매개변수를 논의하면서 등장하는 두 가지 개념인 동종성(homophily)과 구조적 동등성(structural equivalence)을 보게 될 것입니다. 동종성은 노드들이 일반적으로 동일한 커뮤니티에 속하는지 여부를 나타내는 일종의 유사성입니다. 그래프를 공간적으로 바라보고 노드의 위치를 기반으로 함께 그룹화한다면, 동종성을 사용하고 있을 가능성이 가장 높습니다.

[^4]: Aditya Grover and Jure Leskovec, “node2vec: Scalable Feature Learning for Networks”, in Proceedings of the 22nd ACM SIGKDD International Conference on Knowledge Discovery and Data Mining, July 3, 2016, Cornell University arXiv.
[^5]: MemGraph에서 p와 q에 대한 훌륭한 설명을 제공합니다.

---

q의 관점에서 보면, 여러분도 짐작할 수 있듯이 동종성은 높은 q 값의 함수이며 특정 시작 노드에 가깝게 머물도록 순회를 유도합니다. 반면에 q를 낮추어 순회가 더 먼 노드를 탐색하도록 유도하면 구조적 동등성을 중심으로 한 유사성을 보게 됩니다. 이는 주변 노드와의 연결 관계를 고려할 때 그래프에서 유사한 역할을 수행하는 노드를 나타냅니다.

그렇다면 환자 데이터의 맥락에서 동종성과 구조적 동등성에 대해 생각해 보겠습니다. 환자, 진단, 약물이라는 세 가지 유형의 노드로 구성된 환자 데이터셋을 로드한다고 가정해 보겠습니다. 동종성은 노드의 클러스터를 반환할 것으로 기대되며 이는 데이터의 환자 중심적 관점(patient-centric view)에 해당할 가능성이 높습니다. 즉, 가장 유사한 노드는 특정 환자와 연관된 노드일 가능성이 높습니다. 반면에 구조적 동등성을 살펴보면 진단과 약물이 각각 함께 그룹화되는 것을 볼 수 있을 것입니다.

NLP에서 보았듯이 이는 여전히 활발한 연구와 혁신이 이루어지는 분야이며 node2vec만이 그래프 데이터로부터 임베딩을 생성하는 유일한 방법은 아닙니다.[^6],[^7],[^8] 그러나 상대적으로 단순한 접근 방식 덕분에 훌륭한 출발점이 됩니다. 다음 절에서는 실제 임상 데이터(RWD)를 다룰 때 매우 중요한 부분이므로 임상 개념(clinical concept)의 임베딩에 대해 간략히 논의하겠습니다. 단, 일부 임베딩은 실제 그래프 구조 대신 텍스트 표현 및 동시 출현(co-occurrence)을 사용하는 등 비그래프(non-graph) 접근 방식을 사용하여 생성된다는 점에 유의해야 합니다.

### cui2vec: 의학 개념 임베딩

3장에서 UMLS와 CUI에 대해 간략히 다루었으므로, 개념 임베딩을 생성하는 접근 방식인 cui2vec부터 살펴보겠습니다. UMLS를 다루며 임베딩을 고려할 때, 가장 먼저 떠오르는 생각은 UMLS를 그래프로 로드한 다음 그래프 임베딩을 생성하는 것일 수 있습니다. 이 접근 방식의 단점은 임베딩이 해당 분야에서 이전에 UMLS에 통합해 둔 지식의 표현만을 포함하게 된다는 점입니다. 다시 말해, 약물과 진단 사이에 아직 알려지지 않은 관계가 있다면 이는 UMLS나 다른 어떤 온톨로지 혹은 지식 베이스에도 포착되지 않을 것입니다.

[^6]: Haochen Chen et al., “HARP: Hierarchical Representation Learning for Networks”, Cornell University arXiv, November 16, 2017.
[^7]: Bryan Perrozi et al., “DeepWalk: Online Learning of Social Representations”, Cornell University arXiv, June 27, 2014.
[^8]: Jian Tang et al., “LINE: Large-Scale Information Network Embedding”, Cornell University arXiv, March 12, 2015.

---

> **오프라벨 사용과 이상반응**
>
> 여러 약물 간의 또는 약물과 진단 간의 이전에 알려지지 않았던 상호작용은 헬스케어 분야에서 특히 큰 관심사입니다. 의료진은 동시에 투여되었을 때 약물 간의 미지의 상호작용으로 인해 환자에게 이상반응(adverse event)을 일으킬 수 있는 치료법을 처방하지 않기를 원합니다.
>
> 마찬가지로, 시판 중인 기존 약물이 치료적 이점을 가질 수 있지만 해당 용도로 판매되지 않는 새로운 진단을 발견하는 것에도 관심이 있습니다. 이를 오프라벨 사용(off-label use, 허가 외 사용)이라고 하며 의료진과 환자가 임상시험과 실제 사용 모두에서 안전성 프로파일이 잘 확립되어 있을 수 있는 기존 약물로 질환을 치료할 수 있게 해줍니다.

cui2vec 접근 방식은 UMLS 내의 구조화된 개념을 활용하면서도 RWD를 함께 활용하여 임베딩을 생성합니다. CUI 쌍의 동시 출현(co-occurrence)을 찾지만 다음에 논의할 것처럼 데이터 소스에 따라 약간씩 다른 알고리즘을 사용합니다. 모든 유형의 데이터에서 먼저 개념이 CUI로 변환되도록 데이터가 전처리됩니다.

- **학술지 논문(Journal articles)**: 텍스트 전처리 후 CUI 쌍을 식별하며 10단어 윈도우 내에서 식별되면 동시 출현으로 계산합니다.
- **청구 데이터(Claims data)**: 청구 데이터의 환자 중심적 특성을 고려하여 두 CUI가 30일 윈도우 내에 나타나는 환자 수로 동시 출현을 계산합니다.
- **임상 기록(Clinical notes)**: 임상 NLP의 차이점으로 인해 임상 기록에 대한 구체적인 접근 방식은 약간 더 복잡합니다.[^9] 일반적인 개념은 30일 윈도우당 동시 출현을 계산하는 것입니다(단일 환자당 여러 번 발생할 수 있음).

이 접근 방식을 통해 여러 다양한 맥락의 데이터를 기반으로 임베딩을 생성할 수 있습니다. 연구 논문, 청구/수가 청구 내용, 또는 임상의가 환자에 대해 소통하는 방식 등 각각은 동시 출현에 대해 서로 다른 관점을 제공합니다.

여러 다양한 유형의 RWD를 바탕으로 어떤 종류든 모델을 훈련하는 데에는 일정한 직관이 작용하지만 여러분의 사용 사례에 비추어 모델과 그 출력을 먼저 테스트하는 것이 중요합니다. 가능한 한 많은 데이터를 포함하고 싶겠지만 동시에 서로 다른(그리고 상충되는) 여러 가지를 학습하려는 조현병적(schizophrenic) 모델을 만들 위험도 안고 있습니다.

[^9]: Youngduck Choi et al., “Learning Low-Dimensional Representations of Medical Concepts”, Proceedings-AMIA Joint Summits on Translational Science (2016): 41-50.

---

따라서 여러 다양한 유형의 데이터를 고려하는 포괄적인 임베딩 모델을 구축하는 것이 매력적으로 보일 수 있지만 항상 여러분의 구체적인 사용 사례의 미묘한 특성에 맞추어 접근 방식을 테스트하는 것이 중요합니다.

많은 분석 기법과 마찬가지로 cui2vec은 데이터 간의 시간적 관계를 거의 고려하지 않고 데이터를 분석하는 데 초점을 맞춥니다. 시간 윈도우를 사용하여 동시 출현을 결정하지만 임베딩 자체는 임상 기록의 변화를 실제로 반영하지 않습니다. 다음으로는 시간적 맥락을 유지하는 임베딩을 구축하려는 med2vec을 살펴보겠습니다.

### med2vec: 전자의무기록 시퀀스 임베딩

cui2vec과 유사하게 med2vec 역시 RWD를 통합하려고 시도합니다. 그러나 여러 다양한 유형의 RWD를 결합하는 대신 med2vec은 EHR 데이터, 특히 종단적 환자 기록(longitudinal patient record)의 시간적 측면에 초점을 맞춥니다.

간단히 말해, med2vec은 특정 내원(visit) 또는 진료 접촉(encounter)이 시간상의 한 시점이며 과거에 발생했던 일이나 미래에 발생할 일을 예측하는 데 사용될 수 있다고 가정합니다. 이를 통해 단일 환자를 단순한 개념의 "가방(bag)"으로 취급하는 대신 개념 간의 시간적 관계를 포착할 수 있는 임베딩 생성이 가능해집니다.

med2vec은 한 방문에서 다음 방문으로의 시간적 차이를 추적할 뿐만 아니라 단일 방문 내에서의 개념 동시 출현도 추적합니다. 결과적으로 med2vec은 임상 개념에 대해 방문 중심적(visit-centric) 맥락을 제공합니다.

우리는 종종 환자를 단일 시점으로 생각하지만 많은 환자는 시간이 지남에 따라 호전되거나 악화되는 일련의 질환을 중심으로 헬스케어 시스템과 여러 번 상호작용합니다. 방문 중심적 접근 방식을 취함으로써 med2vec 임베딩은 이러한 임상적 변화를 드러내기 시작합니다. 순수하게 환자 중심적인 접근 방식을 취한다면 50년 전 환자의 병력 요소와 현재 상태를 자연스럽게 뒤섞게 됩니다. 다시 말하지만 이러한 뉘앙스가 우리의 사용 사례에 미치는 영향을 고려하는 것이 중요합니다.

이러한 접근 방식들은 주로 동시 출현에 의존해 왔으므로, 이번에는 온톨로지, 특히 SNOMED CT의 그래프적 특성을 활용하는 방식을 살펴보겠습니다.

### snomed2vec: 온톨로지 기반 임베딩

우리는 단순히 인코딩된 지식뿐만 아니라 RWD를 통합한 몇 가지 임베딩을 살펴보았습니다. 이 절에서는 SNOMED 온톨로지를 다시 살펴보겠습니다. 3장에서 논의했듯이 SNOMED는 생물의학 전반에 걸친 개념과 그들 간의 관계를 담고 있는 포괄적인 원천입니다. 이러한 개념은 여러 신체 계통과 질환 영역에 걸쳐 기초 과학부터 임상 진료에 이르기까지 다양합니다.

RWD를 통합하는 접근 방식은 이전에 알려지지 않았던 맥락과 개념 간의 관계를 포착하는 데 뛰어나지만 우리가 수년에 걸쳐 축적해 온 모든 지식을 잊어버리기도 합니다. 다소 과장이 섞여 있긴 하지만 필자는 새로운 학생들에게 맥락에 대한 이해 없이 데이터만 본다면 그들이 훈련한 모델은 입원 환자의 사망률이 높기 때문에 사람들은 절대 병원에 가지 말아야 한다는 결론을 내릴 것이라고 말하곤 했습니다.

---

다시 말해, 데이터에 대한 해석을 이끌기 위해 생물의학과 의료 제공(delivery of healthcare)에 대한 우리의 지식을 여전히 활용하는 것이 중요합니다. 그러한 지식의 한 원천이 바로 SNOMED입니다(3장에서 간략히 논의했습니다).

snomed2vec 프로젝트는 SNOMED CT 개념에 적용된 여러 다양한 임베딩 접근 방식을 비교했습니다. 이들은 다른 두 가지 임베딩 기법인 metapath2vec과 푸앵카레 임베딩(Poincaré embeddings)을 도입했습니다. 비록 이들의 구현 세부사항까지 들어가지는 않겠지만 이들은 node2vec에 대한 대안을 제공하며 snomed2vec 논문은 SNOMED와 관련하여 이들을 훌륭하게 비교하고 있습니다.

거시적 관점에서 보면, metapath2vec은 노드 자체나 노드가 연결된 방식에서 훨씬 더 높은 수준의 이질성(heterogeneity)을 갖는 그래프를 대상으로 합니다. 예를 들어 앞서 논의했던 환자 대 내원의 관계를 들어보겠습니다. node2vec과 같은 접근 방식은 진단이나 약물에 대해 유사한 연결을 가질 수 있기 때문에 이 둘을 혼동할 수 있습니다. 다시 말해, 임베딩 알고리즘 입장에서는 환자와 내원 사이에 큰 차이가 없을 수 있습니다.

그러나 metapath2vec과 같은 접근 방식은 이종 그래프(heterogeneous graphs, 저자들은 이를 네트워크라고 부름)에서 발견되는 구조와 의미론(semantics)을 모두 보존하는 데 중점을 둡니다. 이는 SNOMED 그래프의 규모와 범위를 고려할 때 특히 유용합니다. SNOMED 그래프는 다양한 수준의 의미적 차이를 갖는 수많은 개념 유형과 함께 여러 계층 구조를 포착합니다. 예를 들어 약물의 유효 성분(active ingredient)과 약물의 특정 제형(formulation)은 약물과 해부학적 신체 부위보다 훨씬 더 유사한 의미적 뜻을 갖습니다.

또한 그들은 그래프 데이터의 임베딩을 생성하는 또 다른 대안을 제공하는 푸앵카레 임베딩의 사용에 대해서도 논의합니다. 이 경우 임베딩은 계층 구조(hierarchy)와 유사성(similarity) 모두에 초점을 맞춥니다. 비록 우리가 생물의학 내의 계층 구조에 대해 어느 정도 이해하고 있고 SNOMED 내에 많은 구조를 명시적으로 포착해 두었지만 잠재적 계층 구조(latent hierarchies), 즉 데이터 내에 암시적으로 존재하는 계층 구조도 존재할 수 있음을 알고 있습니다. 물론 cui2vec 및 med2vec에서 보았듯이 이 기법은 전자의무기록이나 기타 실제 임상 데이터 소스에도 직접 적용될 수 있습니다.[^10]

이제 몇 가지 임베딩의 세부사항을 살펴보았으니, 한 걸음 물러나 임베딩 전반을 살펴보겠습니다.

[^10]: Quihao Lu et al., “Learning Electronic Health Records through Hyperbolic Embedding of Medical Ontologies”, BCB ’19: Proceedings of the 10th ACM International Conference on Bioinformatics, Computational Biology and Health Informatics, September 2019, pp. 338-46.

---

### 임베딩 기법에 대한 종합 고찰

보시다시피, 헬스케어 RWD를 표 형식 구조로 되돌릴 필요 없이 그래프를 사용하여 작업하는 데에는 엄청난 잠재력이 있습니다. 임베딩은 그래프 데이터를 직접 다루는 방법의 한 가지 예시에 불과하지만 그래프에서 임베딩을 거쳐 딥러닝으로 이어지는 경로를 고려할 때 확실히 흥미로운 분야입니다. 임베딩의 사용(및 임베딩 생성 방법에 대한 혁신)은 비정형 텍스트(free text)를 더 잘 이해하고(이제는 생성까지 하는) 딥러닝을 활용하는 NLP의 새로운 시대를 촉발했습니다. 개인적으로 저는 매우 고무되어 있으며 그래프 및 그래프 임베딩에서도 이와 유사하고 엄청난 잠재력을 보고 있습니다. 환자 치료 중에 생성되는 데이터는 본질적으로 그래프이며 대규모로 더 나은 임베딩을 생성하는 방법뿐만 아니라 헬스케어의 수많은 복잡한 문제를 해결하기 위해 임베딩을 활용하는 데 있어 앞으로 펼쳐질 혁신을 기대하고 있습니다.

하지만 흥미진진한 만큼, 그래프와 그래프 임베딩을 다룰 때 고려해야 할 몇 가지 핵심 사항이 있습니다. 이는 반드시 그래프에만 국한된 것은 아니지만(NLP와 같은 다른 애플리케이션에서도 볼 수 있음), 그래프의 맥락에서 꼭 짚고 넘어가고자 합니다.

첫째, 그래프 임베딩을 검색하기 시작하면 SNOMED CT용 의료 개념 임베딩(Medical Concept Embeddings for SNOMED CT, 2019년 1월 버전)과 같이 임베딩을 생성하기 위한 사전 훈련된 모델(pretrained models)을 빠르게 찾을 수 있습니다. 가장 먼저 던져야 할 질문은 모델을 훈련하는 데 어떤 원천 데이터가 사용되었는가입니다. 이 경우에는 SNOMED CT가 사용되었음을 알 수 있습니다. 프로젝트 내에서 이러한 임베딩을 사용하려면 해당 임베딩에서 사용된 SNOMED 버전과 우리 프로젝트의 SNOMED 버전이 호환되는지 확인해야 합니다. SNOMED는 주기적으로 배포되며 개념이 지속적으로 추가, 삭제, 수정되므로 불일치로 인한 영향을 확실히 이해해야 합니다.

그러나 RWD로 훈련된 임베딩을 살펴보는 경우, 원천 데이터가 무엇이며 우리의 데이터와 어떻게 비교될 수 있는지 이해하는 것이 중요합니다. 예를 들어 cui2vec 프로젝트는 기본 모델을 훈련하는 데 청구 데이터를 활용합니다. 만약 그 청구 데이터가 미국 메디케어 및 메디케이드 서비스 센터(CMS)에서 제공한 것이라면 어떨까요? CMS의 주 대상을 고려할 때 데이터는 고령층 인구에 크게 편향될 것입니다. 신생아부터 노인까지 다양한 일반 병원 입원 환자 집단의 데이터를 분석하기 위해 이러한 임베딩을 사용한다면 많은 문제를 야기할 수 있습니다.

둘째, 이제 관리하고 유지해야 할 추가적인 모델과 파이프라인이 생기므로 프로젝트의 전반적인 복잡성이 증가합니다. 패혈증의 조기 발견을 위한 모델을 구축하려 하고(6장에서 간략히 논의하듯이) 임베딩을 통합하기로 결정했다고 가정해 보겠습니다. 이제 자체 임베딩 모델을 만들거나 사전 훈련된 임베딩을 사용해야 합니다. 또한 (다른 데이터와 함께) 임베딩으로 생성된 벡터를 입력으로 받는 실제 딥러닝 모델도 튜닝해야 합니다. 결과적으로 튜닝해야 할 모델의 계층이 두 개가 되며,

최적화하고 검증할 수 있다. 모든 데이터 분석 사용 사례와 마찬가지로, 최신 최고의 기법을 사용하고 싶은 유혹이 들더라도 모델과 전반적인 접근 방식을 가능한 한 단순하게 유지하라! 더 단순한 방법으로 해결되지 않을 때에만 접근 방식의 복잡도를 높여야 한다.

셋째, 임베딩을 생성하거나 사전 훈련된(pretrained) 임베딩을 고려하는 다양한 방법을 탐색하다 보면 벤치마크와의 수많은 비교를 접하게 될 것이다. 이러한 벤치마크는 매우 개략적인 수준에서 다양한 옵션을 비교하는 데 훌륭한 도구 역할을 한다. 또한 여러분의 특정 사용 사례가 특정 벤치마크와 얼마나 잘 부합하는지도 고려해야 한다. 여러분의 소스 데이터와 벤치마크에 사용된 데이터를 비교하는 것뿐만 아니라, 여러분이 던지는 구체적인 질문과 그에 따른 학습 과제가 벤치마크와 어떻게 비교되는지도 살펴봐야 한다.

마지막으로, 여느 머신러닝이나 딥러닝 프로젝트와 마찬가지로, 모델이 얼마나 과적합(overfit)되었는지-여기서는 특정 데이터셋에 지나치게 특화되어 다른 데이터셋에는 일반화되지 않는 상태로 정의됨-를 고려하는 것이 절대적으로 중요하다. 앞서 사전 훈련된 임베딩과 벤치마크에서 논의했듯이, 임베딩 생성에 사용된 모델이 과적합되어 다른 데이터에 일반화되지 못할 가능성이 있다. 예를 들어, "진단, 투약, 검사 수치로 훈련된" 사전 훈련 임베딩 모델을 가져와 유사한 데이터 기반의 사용 사례에 적용한다고 가정해 보자. 게다가 임베딩 훈련에 사용된 데이터는 약물의 특정 제형(formulation)을 구분하는 데 초점을 맞추고 있었다. 반면 여러분의 사용 사례는 환자의 이상 반응(adverse event)을 예측하는 것이며 약물이 어떻게 제형화되었는지가 아니라 약물의 유효 성분(active ingredient)에만 관심이 있다. 이는 약물 데이터의 특정한 관점에만 맞춰 훈련되어 여러분의 사용 사례에는 더 이상 일반화될 수 없으므로, 과적합된 임베딩 모델의 수많은 예시 중 하나라고 볼 수 있다.

이 분야는 현재 매우 활발히 연구되고 있으므로, 이것이 RWD를 다룰 때 그래프 기반 분석의 흥미로운 영역 중 하나에 대한 기초적인 소개가 되었기를 바란다. 다음 절에서는 이러한 개념 중 일부를 연합 학습(federated learning)의 맥락에 맞게 적용해 볼 것이다. 또한 임베딩을 넘어 그래프와 연합 학습이 가지는 의미에 대해서도 잠시 논의할 것이다.

## 그래프 기반 분석을 지지하는 이유

이상적으로는 그래프에서 데이터를 추출하고 그래프를 직접 분석하는 이 짧은 여정을 통해 여러분이 그래프와 헬스케어 RWD를 더 많이 실험해 보도록 영감을 얻었기를 바란다. 오늘날 우리가 사용하는 분석 도구와 기법의 상당수는 테이블형(tabular) 데이터를 전제로 하므로, 데이터 조율(data harmonization)을 돕기 위해 그래프 기법을 사용하려면 현재의 도구와 통합할 수 있는 형식으로 이 데이터를 추출해야 한다.

헬스케어 RWD의 복잡성과 코딩 시스템, 용어집, 온톨로지의 긴밀한 통합을 고려할 때, 그래프는 데이터 및 분석 엔지니어링을 위한 강력한 도구가 될 수 있다. 우리는 서로 다른 데이터셋을 조율하거나 재사용 가능한 데이터 파이프라인을 구축하기 위해 그래프와 그래프 데이터베이스를 활용할 수 있다. 그 결과 (그리고 추가적인 복잡성에도 불구하고), 현재 분석 스택 전체가 테이블형 데이터를 요구하더라도 그래프는 여러분의 도구 상자에 필수적인 도구로 남아 있어야 한다.

그래프 데이터베이스와 같은 신기술에는 항상 닭이 먼저냐 달걀이 먼저냐 하는 문제가 존재한다. 사람들은 데이터를 소비할 도구가 갖춰질 때까지 데이터베이스와 같은 새로운 기술 도입을 주저한다. 우리는 이번 장을 현재의 도구에 그래프를 통합하는 방법(닭과 달걀의 순환 고리를 끊는 한 가지 방법!)을 살펴보는 것으로 시작하여 그래프 임베딩 탐색으로 이어갔다.

대규모 헬스케어 RWD를 활용하는 데 있어 그래프 기반 분석이 미래라는 점에는 의심의 여지가 거의 없다. 많은 이들이 예측 모델의 맥락에서 임베딩과 딥러닝을 주로 바라보지만 임베딩을 다른 방식으로 활용할 기회도 많다. 우리는 이미 그래프와 함께 트랜스포머를 사용하는 분야(예: Graph-BERT) 등에서 추가적인 연구들이 진행되는 것을 보고 있다. 데이터 조율 과정의 일부로 그래프를 통합하는 방법을 배우고 환자 매칭, 코호트 정의, 적절한 환자가 적절한 치료와 중재를 적절한 시기에 받을 수 있도록 지원하는 등 헬스케어의 다른 과제들을 해결해 나감에 따라 상당한 혁신이 일어날 것이다.

다음 장에서는 연합 학습, NLP 등 헬스케어의 다른 트렌드를 살펴보고 그래프의 몇 가지 측면을 다시 다룰 것이다.

## 결론 및 요약

이번 장은 데이터 조율이나 랭글링보다는 데이터 과학과 딥러닝에 더 초점을 맞추었다는 점에서 다른 장들과 다소 달랐다. 하지만 어떤 유형의 분석을 사용하든 그래프가 데이터 랭글링의 일부로 어떻게 활용될 수 있는지 확인할 수 있는 기회였다. 데이터를 다시 테이블이나 데이터프레임으로 추출해야 한다면, 그것은 주로 코딩/소프트웨어 엔지니어링 작업에 해당한다. 핵심은 그래프 순회(graph traversal)를 통해 데이터를 조작하고 처리할 수 있다는 점이다. 이를 통해 데이터를 표현하고 질의하는 근본적으로 다른 방식의 이점을 누린 다음 결과를 다시 그래프로 변환할 수 있다.

이것이 "기존" 방식(데이터프레임)과 "새로운" 방식(그래프) 간의 간극을 메우는 데 훌륭한 방법이기는 하지만 우리는 데이터를 그래프 형태로 직접 다루기 시작하는 방법도 살펴보았다. 이를 통해 데이터의 컨텍스트를 더 잘 유지할 수 있는 능력을 포함하여 그래프 기반 표현의 풍부함을 활용할 수 있다.

마지막으로, 데이터를 그래프로 직접 다룰 수 있을 뿐만 아니라, 그래프에서 임베딩을 추출하는 새로운 접근 방식을 통해 전반적으로 훨씬 더 의미 있는 임베딩을 생성할 수 있다. 이러한 임베딩은 환자에 대해 알고 있는 정보와 학술 논문에서 얻은 지식을 결합하여 기존 방식보다 훨씬 더 많은 컨텍스트를 포착하는 특징(feature)을 갖춘 벡터 표현을 생성할 수 있다.

딥러닝은 실제 임상 데이터(RWD) 전반을 보든 그래프를 집중적으로 보든 우리 앞에 놓인 흥미로운 수많은 주제 중 하나일 뿐이다. 다음 장에서는 연합 학습과 같이 헬스케어 분석에서 새롭게 떠오르는 트렌드 몇 가지를 살펴본다.

---

# 제7장 헬스케어 분석의 최신 트렌드

데이터 엔지니어링은 실제 임상 데이터(RWD)를 활용하는 데 있어 매우 중요한 요소이지만 가장 화려한 주제는 아니다. 당연하게도, 데이터를 정제하고 처리하는 이 모든 작업은 목적을 위한 수단이다. IT 부서에 있든, 데이터 팀에 있든, 비즈니스/임상 부서에 있든 궁극적인 목표는 의사결정을 개선할 수 있는 인사이트를 데이터에서 도출하는 것이다.

비즈니스 사용 사례의 경우, 이는 더 효율적인 치료 지원, 비용 절감 개선, 또는 수익 증대(비영리 기관이라 하더라도 여전히 중요함)일 수 있다. 임상 사용 사례의 경우, 질병의 조기 및 정확한 진단 지원, 치료팀이 적시에 환자에 대한 올바른 정보에 접근할 수 있도록 연결, 또는 환자 중증도(acuity)에 따른 적절한 인력 배치 수준 결정 등일 수 있다. 마지막 항목은 임상적 이점과 비즈니스적 이점이 모두 존재하는 사용 사례의 하위 집합을 나타내므로 특히 흥미롭다.

스펙트럼의 한쪽 끝에서 데이터 분석가는 Tableau나 Cognos와 같은 비즈니스 인텔리전스(BI) 도구에 의존한다. 다른 한편에서 데이터 과학자는 예측 모델을 구축하기 위해 NLP와 머신러닝(딥러닝 포함)에 의존한다. 흔히 "데이터 과학"이나 "분석" 업무를 "비즈니스 인텔리전스"와 "머신러닝" 같은 단순한 범주로 나누곤 하지만 실제로는 데이터를 임상 및 비즈니스 워크플로우에 더 깊이 통합함에 따라 이러한 경계가 점점 더 모호해지고 있다.

이 장에서는 분석 분야의 전반적인 트렌드와 발전을 논의하고 문헌에 소개된 몇 가지 구체적인 사례를 살펴본다. 먼저 연합 학습(federated learning)과 연합 분석(federated analytics)에 대한 논의부터 시작한다. 이는 프라이버시 및 보안 문제를 균형 있게 유지하면서 계속해서 증가하는 방대한 양의 데이터를 활용할 수 있는 솔루션을 찾는 우리 모두에게 데이터 과학 분야의 매우 흥미로운 영역이다.

## 연합의 과거와 현재

'연합(federated)'이라는 용어는 헬스케어 분야, 특히 연구자들 사이에서 꽤 오랫동안 사용되어 왔다. 그 개념은 연구자들이 데이터를 공통 용어를 사용하는 공통 데이터 모델(OMOP이 대표적인 예)로 변환한 다음 SQL 쿼리를 공유하여 연구 네트워크에 참여하는 각 기관에서 동일한 쿼리를 실행하도록 하는 것이었다. 그런 다음 결과를 수동으로 집계하여 연구의 통계적 검정력(statistical power)을 높일 수 있었다. 이 접근 방식을 통해 연구자들은 상당한 시간과 자원이 소모되는 비식별화(de-identification)나 익명화(anonymization)를 거칠 필요 없이 기관 간 협업을 진행할 수 있었다.

그러나 오늘날 데이터 접근의 연합이라는 개념은 완전히 다른 의미를 띠게 되었다. 대부분의 사람들에게 가장 먼저 떠오르는 것은 연합 (머신)러닝(federated learning), 연합 평균/확률적 경사 하강법(federated average/stochastic gradient descent), 차분 프라이버시(differential privacy), 프라이버시 보존 분석(privacy-preserving analytics)이다. 모든 경우에서 분석이나 머신러닝의 연합화는 소프트웨어 라이브러리에 의해 전적으로 처리되는 자동화를 전제로 한다. 따라서 다른 데이터 과학자 및 연구자들과 교류할 때, 특히 의료정보학 및 임상 연구자들과 일하는 경우라면 용어 정의를 먼저 명확히 맞추는 것이 좋다.

따라서 다음 절에서는 특히 헬스케어 RWD의 맥락에서 연합 학습과 연합 분석의 현재 정의를 자세히 살펴볼 것이다.

## 연합 학습과 연합 분석

우리의 첫 번째 목적지는 헬스케어뿐만 아니라 여러 산업 전반에서 목격되는 가장 뜨거운 트렌드 중 하나다. 구글의 만화(comic)와 같은 맥락에서 연합 학습이라는 용어를 들어보았을 것이다. 엣지 컴퓨팅(edge computing)과 결합된 이러한 연합 학습 사례는 불과 몇 년 전만 해도 상상할 수 없었던 수준의 데이터 "공유"를 가능하게 했다.

예를 들어, 오늘날 스마트폰에서 사용되는 수많은 키보드를 생각해 보자. 구글, 애플, 삼성과 같은 기업들은 키보드의 예측 성능을 향상시키기 위해 노력하고 있다. 하지만 이러한 모델을 훈련하려면 사용자가 휴대폰에 입력하는 모든 내용에 접근할 수 있어야 한다. 우리 모두가 휴대폰을 사용하여 뱅킹 서비스, 건강 기록에 접근하고 캘린더와 이메일을 통해 일상생활의 세부 사항까지 다루고 있다는 점을 감안할 때 이는 엄청난 프라이버시 악몽이 될 것이다.

헬스케어 분야에서는 (아직은!) 모든 환자의 개별 스마트폰에 접근하는 것을 논하지는 않는다. 현재의 이니셔티브는 기관이나 조직 간의 연합에 초점을 맞추고 있다. 다음 몇 개 절에서는 연합 학습이 어떻게 작동하는지, 그리고 일부 연구자들이 연합 머신러닝(예: 여러 조직에 걸친 데이터로 모델 훈련)과 연합 분석(예: 기술 통계, 생물통계학)을 어떻게 구분하고 있는지 살펴볼 것이다. 그런 다음 연합 접근 방식, 데이터 조율, 그래프의 교차점을 살펴볼 것이다. 이제 연합 학습이 어떻게 작동하는지 알아보자.

### 연합 학습의 작동 원리

그렇다면 기업들은 프라이버시 문제를 완화하면서 어떻게 우리의 데이터로부터 학습할 수 있을까?
연합 학습이 그 해답이지만 정확히 무엇일까?

많은 이들에게 연합 학습은 완전히 새로운 개념이 아니다. 연산을 데이터가 있는 곳으로 더 가깝게 이동시키는 자연스러운 확장에 불과하다. 우리는 하둡(Hadoop)에 대한 열풍 때와 마찬가지로 한동안 "연산을 데이터로 가져오기(bring the compute to the data)"라는 트렌드를 보아왔다. 머신러닝의 맥락에서 연산을 데이터에 더 가깝게 가져온다는 것은 데이터를 제자리에 둔 채 그 데이터 위에서 머신러닝 모델을 훈련하는 것을 의미한다. 스마트폰 키보드의 예에서 이는 누군가가 입력하는 모든 텍스트를 회사의 서버로 전송할 필요 없이 키보드의 예측 모델을 훈련하는 것을 뜻한다.

데이터를 제자리에 두고 연산을 데이터로 이동시키는 회사는 매우 많다. 이는 맵리듀스(map-reduce) 알고리즘부터 분산 SQL 쿼리에 이르기까지 다양하다. 이러한 기술들이 이미 존재해 왔다면 왜 연합 학습에 그토록 열광하는 것일까? 간단히 말해, 분산된 데이터 소스에서 단일 머신러닝 모델을 훈련하는 것은 결코 단순하지 않기(nontrivial) 때문이다.

스마트폰 키보드 예시로 돌아가서, 타이핑할 때 다음 단어를 예측하는 신경망을 훈련한다고 가정해 보자. 각 휴대폰의 로컬 데이터에 대해 `train()` 함수를 호출하고 끝내는 것은 매우 간단하다. 하지만 그렇게 하면 설치된 키보드 버전마다 서로 다른 모델을 갖게 된다. 이렇게 해도 각 사용자에 맞게 텍스트를 예측하는 모델은 생성되겠지만 모든 사용자로부터 제공된 데이터를 학습하는 단일 모델을 훈련할 기회를 놓치게 된다.

여기서부터 상황이 복잡해지기 시작한다. 신경망을 훈련할 때는 모든 훈련 인스턴스에 대해 가중치를 확인하고 업데이트할 방법이 필요하다.[^1] 그러나 이러한 인스턴스가 설치된 모든 키보드 버전에 널리 분산되어 있다면 이 프로세스는 훨씬 더 어려워진다. 키보드를 사용하는 각 사용자에 대해 (다른 키보드들로부터 훈련된) 가중치를 사용자의 휴대폰으로 다운로드하고 해당 사용자의 데이터를 기반으로 가중치를 업데이트한 다음, 중앙 조정 서버로 가중치를 다시 전송해야 한다.

[^1]: 신경망과 훈련 세부 사항에 대한 자세한 내용은 https://oreil.ly/icRrC 및 https://oreil.ly/IoQYB 를 참조하라.

주의 깊게 살펴본 독자라면 "이거 전이 학습(transfer learning) 문제와 매우 유사하지 않은가?"라는 의문이 들 수 있다. 그것도 이 문제에 접근할 수 있는 한 가지 방법이다. 훈련된 모델을 가져와 특정 휴대폰에 다운로드하고 해당 모델을 업데이트한 다음, 중앙 서버로 다시 보낸다. 거기서부터 다음 휴대폰으로 보내 훈련을 진행하는 식이다. 전이 학습과 유사한 프로세스를 따르게 되지만 모델을 관련이 있거나 유사한 도메인에 반드시 적응시키는 것은 아니다. 머신러닝 프로세스를 연합화하는 다른 접근 방식들도 있지만 이들 모두 데이터가 한 데이터셋에서 다른 데이터셋으로 얼마나 잘 정규화/조율되었는지에 상당히 민감하다.

이 장 전체에서 나는 일반적으로 연합 학습을 주된 예시로 사용할 것이다. 하지만 머신러닝 모델 훈련을 필요로 하지 않는 분석에 초점을 맞춘 연합 분석이라는 전체 분야도 존재한다. 연합 분석은 기본적으로 머신러닝이 아닌 모든 것을 의미하므로 명확하게 정의되지는 않지만 때로는 '프라이버시 보존 분석(privacy-preserving analytics)' 또는 '차분 프라이버시(differential privacy)'라는 문구가 함께 사용되기도 한다. 이 장에서는 연합 학습을 머신러닝 모델의 훈련을 연합화해야 하는 것으로 정의하고 연합 분석은 모델 훈련을 수반하지 않는 모든 유형의 분석(시각화 등 포함)으로 정의한다. 연합 분석과 연합 학습 사이에는 구체적인 구현 세부 사항과 기저의 수학적 원리가 다르지만 이 두 가지를 RWD에 적용할 때 마주하는 가장 큰 함정은 동일하다.

이제 연합 학습과 연합 분석의 차이점에 대해 어느 정도 이해했을 것이다. 또한 이것이 프라이버시 문제를 완화할 수 있는 잠재력을 지니고 있음도 확인했다. 다음 절에서는 연합 분석/학습을 사용하려는 이유에 대해 구체적으로 살펴본다.

### 왜 연합 분석 및 학습을 도입하는가?

프라이버시 위험과 우려를 완벽하게 완화하면서 통계, 분석, 머신러닝을 위해 데이터를 활용할 수 있다는 개념에는 분명 매력적인 면이 있다. 물론 모든 일이 그렇게 쉽고 명확하지는 않지만 연합 접근 방식은 몇 가지 잠재적인 이점을 제공한다.

여러분이 대형 제약회사에 근무하며 근거 생성(evidence generation) 프로젝트를 진행하고 있다고 상상해 보자. 즉, 질병의 특정 하위 집단에 대해 자사 약물 중 하나의 사용을 뒷받침하는 근거를 생성하기 위해 RWD를 분석하려는 것이다. 여러분의 약물은 FDA의 승인을 받았지만(최소 안전성 및 유효성 요건을 충족했음을 의미), 아직 표준 치료(standard of care)로 간주되지는 않는다. 따라서 여러분의 목표는 신약이 1차 치료제가 되어야 함을 "입증"하는 근거를 생성하는 것이다. 제4장에서 우리는 효능(efficacy) 대 효과(effectiveness)에 대해 논의했다. 여기서는 RWD를 사용하여 효과를 정량화하고 우리 약물이 기존 표준 치료보다 더 우수하다는 과학적 주장을 펼치고자 한다.

회사가 이미 라이선스를 확보한 데이터셋 중 일부를 사용할 수도 있지만 여러분과 팀은 지역 의료 시스템의 외부 데이터셋에 접근해야 한다고 결정한다. 내부 데이터셋만으로는 연구 설계에 필요한 환자 수가 충분하지 않기 때문이다.

이제 여러분의 파트너십 팀이 지역 의료원들과 나눌 법한 대화를 상상해 보십시오. “저희 데이터 및 분석 플랫폼에 귀원의 모든 환자 데이터를 복사해 근거 생성 연구를 수행하고자 합니다. 저희는 비식별화/익명화된 데이터만을 원하므로, 귀원에서 모든 데이터를 비식별화해 주셔야 합니다.”

이때 CIO와 CMIO의 머릿속에 드는 생각은 다음과 같습니다. “우리 환자 데이터 사본을 전부 달라고 하면서 모든 데이터가 제대로 비식별화/익명화되었는지 보장할 책임까지 우리더러 지라는 건가? 그렇다면 PHI/PII가 조금이라도 유출되면 우리 탓이라는 뜻이잖아.”

### 개인건강정보(PHI)와 개인식별정보(PII)의 보호
PHI와 PII라는 용어는 다소 혼용되어 사용되는 것을 볼 수 있습니다. PHI(Protected Health Information, 보호 대상 건강 정보)는 환자를 식별할 수 있는 일련의 항목(예: 진료 예약 일시, 담당 의사 이름 등)을 의미합니다. PHI는 HIPAA 법령의 일부로 법적으로 정의되어 있습니다. 반면 PII(Personally Identifiable Information, 개인 식별 정보)는 의료 분야뿐만 아니라 모든 산업 전반에 걸쳐 훨씬 더 광범위하게 적용되는 개념입니다. 따라서 담당 의사의 이름과 같은 항목은 환자 본인의 PHI로는 간주되지 않지만 해당 의사의 PII로는 간주될 수 있습니다. PII는 일반적으로 소비자 보호법이나 일반 데이터 법률(예: GDPR)의 규제를 받습니다.

필자는 이와 같은 대화에 수없이 참여해 보았는데, 데이터를 보유한 측은 데이터를 외부에 제공하는 일에 대해 위험을 극도로 회피하며 상당한 두려움을 느끼는 경우가 많습니다. 게다가 비영리 단체나 대학 의료원에 데이터를 요청하는 주체가 영리 기업일 때는 상황이 더욱 어려워집니다.

반대로 CIO나 CMIO에게 다음과 같은 대안적인 제안을 건네는 상황을 상상해 보십시오. “저희 약물 치료로 호전될 가능성이 높은 환자를 식별하는 예측 알고리즘을 귀원의 데이터로 학습시키고자 합니다. 하지만 걱정하지 마십시오! 데이터는 절대 귀원의 시스템 밖으로 나가지 않으며 귀원이 데이터에 대한 완전한 통제권을 항상 유지하게 됩니다. 저희는 귀원의 시스템으로 알고리즘을 전송할 뿐이며 외부로 반출되는 것은 오직 집계 통계량이나 모델 가중치뿐입니다! 심지어 애초에 저희 알고리즘이 어떤 데이터에 접근할 수 있는지조차 귀원에서 직접 통제하실 수 있습니다.”

연합 분석(federated analytics)과 연합 학습(federated learning)의 매력은 환자 데이터에 책임을 지는 이들이 데이터에 대한 통제권을 결코 포기할 필요가 없다는 점입니다. 데이터에 접근하기를 원하는 데이터 과학자와 조직의 관점에서도 훌륭한 방식입니다. 데이터 소비자로서 우리는 보안 책임에 대한 부담을 질 필요 없이 데이터의 모든 이점을 누릴 수 있습니다. 대부분의 관점에서 볼 때 이는 모두가 원하는 것을 얻는 진정한 윈윈(win-win) 상황입니다.

하지만 이토록 훌륭하다면 왜 의료 산업 전반에서 더 널리 도입되지 못했을까요? 결국 이는 모든 의료 산업 이해관계자들이 흔히 언급하는 가장 큰 걸림돌 중 하나인 '충분한 데이터에 대한 접근성' 문제를 해결해 줍니다. 우리는 분명 더 많은 데이터를 원하지만 핵심은 단순히 데이터의 양을 늘리는 것이 아니라 '고품질의 데이터'를 더 많이 확보하는 데 있습니다. 이는 결국 책 전체를 관통하며 다루어 온 데이터 조화(data harmonization)라는 주제로 우리를 다시 이끕니다.

## 연합 환경에서의 데이터 조화 과제

키보드의 예측 알고리즘 학습을 연합 방식으로 수행했던 첫 번째 예시로 돌아가 보면, 거기에는 실질적인 데이터 조화 문제가 없음을 알 수 있습니다. 키보드 개발자가 각 스마트폰에서 데이터가 수집되는 방식을 직접 제어하기 때문입니다. 따라서 연합 학습 시스템을 구축할 때 모든 스마트폰이 동일한 형식과 구조로 데이터를 제공하도록 보장되며 데이터의 맥락과 의미 역시 균일합니다.

반면 실세계 데이터(Real-World Data, RWD)는 구조, 의미, 맥락 면에서 균일하지도 일관되지도 않습니다. 앞선 장들에서 논의했듯이 RWD를 어떻게 해석하고 이를 정제하고 처리하기 위해 무엇을 해야 하는지에는 수많은 뉘앙스가 존재합니다. 이제 서로 다른 두 조직이 각자의 데이터가 상호 간에 적절히 조화되도록 해야 하지만 상대방의 데이터를 서로 전혀 볼 수 없는 상황을 상상해 보십시오.

가상의 대학 의료원(University Medical Center)이 지역 의료원(Community Medical Center)과 연합 학습 네트워크를 구축한다고 가정해 봅시다. 두 기관의 분석 엔지니어들은 Athena 어휘집과 함께 OMOP 공통 데이터 모델(CDM)을 사용하기로 결정합니다. 두 기관의 정보학 및 데이터 팀은 자체 로컬 데이터 모델을 OMOP로 변환하기 위해 전력을 다하고 빠른 내부 데이터 검증 테스트를 거친 후 머신러닝 모델 학습을 위한 데이터 준비가 완료되었다고 상대 팀에 자랑스럽게 알립니다.

인기 있는 연합 학습 프레임워크(예: Substra, FATE, TensorFlow Federated, Nvidia 등)를 사용하여 연합 네트워크를 구축하고 모델 학습을 시작합니다! 하지만 완성된 모델을 검증해 보니 성능이 그다지 좋지 않습니다.

일반적인 데이터 과학 워크플로라면 이는 그리 심각한 문제가 아닙니다. 그저 데이터로 돌아가 살펴보고 그에 맞게 특성(feature)이나 하이퍼파라미터를 조정하면 됩니다. 하지만 연합 환경에서는 대학 의료원의 데이터 과학자는 자신의 데이터만 볼 수 있고 지역 의료원의 데이터는 전혀 볼 수 없으며 그 반대도 마찬가지입니다.

모델 성능에 영향을 미치는 몇 가지 잠재적 시나리오는 다음과 같습니다.

### 데이터 내 신호(Signal) 부재 문제
다시 말해 데이터에 실제로 아무런 패턴이 없어서 어떤 특성 엔지니어링이나 하이퍼파라미터 튜닝으로도 좋은 모델을 만들 수 없는 경우입니다. 겉보기에 우수한 성능을 내는 것처럼 보이는 모델이 있더라도 이는 데이터에 극도로 과적합된(overfitted) 모델에 불과할 것입니다.

### 부적절한 모델 아키텍처 선택
데이터 내에 찾아낼 수 있는 패턴이 존재하지만 단순히 성능이 잘 나오지 않는 모델을 사용하고 있는 경우입니다. 전통적인 머신러닝의 경우 랜덤 포레스트가 더 나은 성능을 낼 수 있는 상황에서 서포트 벡터 머신(SVM)을 사용하고 있을 수 있습니다. 딥러닝의 경우에는 다른 신경망 구조(topology)가 필요할 수 있습니다.

### 도메인 특화 특성 엔지니어링의 필요성
이 상황에서는 찾을 수 있는 패턴이 존재하지만 올바른 특성을 추출하지 못했거나 값을 적절히 정규화하지 못한 경우입니다. 딥러닝의 경우 해당 데이터에 맞지 않는 특정 구조를 사용하고 있는 또 다른 사례일 수 있습니다. 핵심은 패턴이 존재한다는 점이며 단지 우리가 이를 아직 "풀어내지(unlock)" 못했을 뿐이라는 것입니다.

### 데이터 변환 파이프라인의 불일치
앞서 논의한 문제들과 달리, 이는 여러 서로 다른 데이터셋을 결합하는 상황에 특유한 문제입니다. 문제는 데이터셋 A와 B를 결합할 때, 각 데이터셋을 서로 불일치하는 방식으로 변환한다는 점입니다.

두 번째와 세 번째 시나리오는 모든 머신러닝 프로젝트에 공통적으로 해당하며 여기서 굳이 논의할 필요가 없는 내용입니다. 하지만 연합 학습 프로젝트를 다룰 때는 첫 번째 시나리오와 네 번째 시나리오를 어떻게 구별할 수 있을지 자문해 보아야 합니다.

왜 이것이 일반적인 머신러닝 프로젝트와 다를까요? 바로 네트워크 전체에서 자신들의 데이터 외에는 다른 데이터를 볼 수 없기 때문입니다. 이로 인해 데이터 과학자가 자리에 앉아 모델의 출력을 확인하고 데이터를 체계적으로 분석하며 문제를 해결해 나가기가 어렵습니다.

다음 예시들에서 살펴보겠지만 데이터 조화 문제는 연합 분석이나 연합 학습을 다룰 때라고 해서 근본적으로 달라지지 않습니다. 단지 데이터 엔지니어와 데이터 과학자가 자신의 기관 데이터만 볼 수 있고 다른 기관의 데이터는 전혀 볼 수 없다는 사실 때문에 문제가 한층 더 어려워질 뿐입니다. 이들은 대부분의 데이터를 집계 통계량이나 기타 프라이버시 보호 방식으로만 접할 수 있어, 사실상 한쪽 손이 뒤로 묶인 채 평소처럼 분석을 수행해야 하는 처지에 놓입니다.

앞서 들었던 대학 의료원과 지역 의료원의 단순한 예시를 다시 살펴보겠습니다. 수많은 질병에서 연령은 위험 요인을 결정하고 치료 옵션을 안내하는 데 종종 중요한 요인이 됩니다. 고령층의 프라이버시를 보호하기 위한 일반적인 기법 중 하나로, 많은 조직에서 고령 환자의 나이를 미리 정해진 상한값으로 제한(cut off)합니다. 예를 들어 85세를 초과하는 모든 사람의 나이를 85세로 기록하는 식입니다. 따라서 85세 이상인 모든 사람은 마치 정확히 85세인 것처럼 표시됩니다.

그렇다면 대학 의료원은 85세를 상한값으로 사용하고 지역 의료원은 90세를 상한값으로 사용한다면 어떻게 될까요? 대다수 환자가 젊은 연령층인 질병의 경우에는 이것이 큰 영향을 미치지 않을 수 있습니다. 하지만 특히 85~90세 연령대를 중심으로 나이가 중요한 요인으로 작용한다고 판단되는 노인의학적 질환을 연구하는 중이라면 어떨까요? 연령 데이터의 불일치로 인해 모델은 유의미한 내용을 전혀 포착해 내지 못할 것이 분명합니다.

물론 이러한 유형의 불일치는 비교적 쉽게 식별할 수 있습니다. 프라이버시 보호 프레임워크 내에서도 히스토그램 및 기타 집계 통계량을 주고받을 수 있기 때문입니다. 숙련된 데이터 과학자라면 여러 기관 간의 히스토그램을 살펴보고 각 의료 정보학 팀에 탐색적인 질문을 던지기 시작할 것입니다.

하지만 상황이 조금 더 미묘하다면 어떨까요? MIMIC-III 데이터셋과 4장에서 다루었던 "입원(admission)" 날짜 및 시간에 대한 논의로 돌아가 봅시다. 재원 기간(Length of Stay, LOS)을 30일 이내 재입원의 예측 변수로 사용하는 머신러닝 모델을 구축한다고 가정해 보겠습니다.

지역 의료원에서는 환자가 병원 내 어떤 부서에서든 처음 진료를 받은 날짜와 시간을 매핑합니다. 이 의료원에서는 대부분의 환자에 대해 이것이 응급실(ED) 입실 시간이 될 가능성이 높습니다.

대학 의료원에서는 환자가 실제로 응급실 밖의 병원 내 특정 병동으로 이동한 날짜/시간을 사용하기로 결정합니다. 응급실을 거치지 않고 병원에 직접 입원하는 환자 수가 많고 이렇게 하는 편이 데이터를 더 깔끔하게 만들기 때문에 이러한 결정을 내렸습니다.

상황을 더욱 복잡하게 만드는 것은, 예측 모델을 개발하는 데이터 과학자가 재원 기간을 계산할 때 시간을 무시하고 날짜 부분만을 사용하기로 결정한다는 점입니다. 이 데이터셋 전체의 평균 재원 기간이 10일이라면, 두 데이터셋 간의 재원 기간에 약 10%의 차이가 발생하는 셈입니다. 이것이 모델의 학습과 평가에 어떤 영향을 미칠까요? 더 중요하게는, 데이터 과학자가 이러한 구체적인 불일치를 사전에 어떻게 식별할 수 있을까요?

안타깝게도 이는 두 의료원의 데이터 팀에 무엇을 물어봐야 할지 알고 있을 때만 명백히 드러나는 문제 중 하나입니다. 그러나 이러한 논의가 없다면 이 불일치가 존재한다는 사실조차 영영 알지 못할 가능성이 높습니다.

또 다른 예로는 필자가 전자의무기록(EHR) 데이터의 엔터프라이즈급 관리에 주력하던 초기 스타트업 시절에 겪었던 실제 시나리오가 있습니다. 당시 우리는 다른 병원과 합병을 진행 중이던 한 병원 시스템의 CMIO와 논의 중이었는데, 그는 마취 지속 시간(time under anesthesia)과 관련된 매우 구체적인 사례를 언급했습니다.

그들은 수술실 활용을 최적화하기 위해 수술 환자에 대한 분석을 수행하고자 했습니다. 대형 병원 시스템에서 수술실은 귀중한 자원입니다. 외상 환자가 이송되어 왔을 때 사용 가능한 수술실이 없다면, 생명을 구하는 필수적인 처치가 지연되거나 차선의 환경인 응급실에서 시행되어야 할 수도 있습니다. 반대로 여러 수술실이 예약되지 않은 채 비어 있다면, 이는 다른 환자들의 예정 수술(elective surgery)이 불필요하게 미뤄지고 있음을 뜻합니다. 여기에 마취 지속 시간을 다룰 때 추가적인 복잡성이 발생하는데, 수술실에서 나온 환자는 마취 회복실(PACU)에서 관리되어야 하기 때문입니다. 병원이 수술실 일정을 완벽하게 최적화하더라도, 이로 인해 PACU가 과부하될 수 있습니다.

그렇다면 무엇이 문제였으며 데이터에 관한 책에서 이를 논의하는 이유는 무엇일까요? 언뜻 보기에 마취 지속 시간은 매우 단순해 보입니다. 마취과 의사가 환자에게 마취를 시작할 때 시계를 작동시키고 환자가 깨어나 더 이상 마취 상태가 아닐 때 시계를 멈추면 됩니다. 하지만 데이터가 수집된 방식과 임상 워크플로가 미치는 다양한 영향을 파헤쳐 보면, 이것이 데이터 관점에서 어떻게 우리에게 문제가 되는지 드러나기 시작합니다.

첫째, 한 병원에서는 공식 "시작 시간"이 수술 전 처치실(pre-op)에서 환자에게 정맥 주사(IV)를 시작한 시점이었습니다. 반면 다른 병원에서는 환자가 수술실로 입실("wheels in")한 후에야 비로소 시계가 작동되었습니다. 환자가 수술실로 이동된 후 몇 분 내에 마취과 의사에 의해 실제로 마취 상태에 들어가기 전까지는 공식적으로 마취 중인 것이 아니라는 것이 그들의 근거였습니다.

둘째, 첫 번째 병원(IV 투여 시점에 시계를 작동시킨 병원)은 환자가 PACU에서 퇴실할 때까지 환자를 "마취 중"인 상태로 추적했습니다. 시작과 종료 시간에 대한 그들의 근거는, 환자의 수술이 확정되고 수술 준비가 본격적으로 시작되면 환자가 PACU를 퇴실할 때까지 자원이 투입된 상태라는 점이었습니다. 수술실과 나아가 PACU의 활용도를 최적화하기 위해 이 데이터를 추적한다는 관점에서는 전적으로 타당한 논리였습니다. 반면 두 번째 병원은 환자가 수술실로 들어오고 나갈 때 시계를 시작하고 멈추었습니다. 그들이 당초 이 시간을 추적하기 시작한 주된 목적은 마취과 의사의 가용성을 파악하는 것이었기 때문입니다. 규모가 작은 병원이었기에 주된 관심사는 수술실이나 PACU가 아닌 마취과 의사 인력 자체였습니다.

따라서 데이터 관점에서 보면, 각 데이터 웨어하우스에 "마취 지속 시간"을 추적하도록 계산된 필드가 존재한다는 설명을 들었습니다. 그러나 일부 환자의 경우 두 병원 간의 마취 시간이 최대 몇 시간까지 차이가 날 수 있었습니다. 연합 시스템에서 하듯이 두 병원의 데이터로 동일한 모델을 학습시키려 한다면, 다양한 입력값(예: 진단명, 투약 내역, 예정 수술 여부 및 응급 수술 여부 등)을 기반으로 예상 마취 지속 시간을 추정하는 예측 알고리즘을 개발하기가 극도로 어려울 것입니다.

대형 병원 및 외상 센터에서 근무하는 데이터 과학자로서 모델의 성능이 저조한 것을 보고 그 원인을 조사하기 시작할 수 있습니다. 자체 병원의 데이터셋을 깊이 파고들어 보면 모델이 꽤 우수한 성능을 내는 것을 확인할 수 있습니다. 하지만 이 쿼리를 연합 방식으로 확장하는 순간 모델 성능은 곤두박질칩니다. 그러나 다른 병원의 구체적인 데이터를 들여다볼 수 없기 때문에, 두 병원 간에 결과 변수(outcome variable)가 상당히 왜곡되어 있다는 사실을 인지하지 못합니다.

앞서 우리는 잠재적인 데이터 불일치를 파악하는 데 도움을 줄 수단으로 히스토그램이나 기타 집계 통계량을 살펴보는 방안을 논의했습니다. 하지만 자체 외상 센터의 데이터를 살펴본 다음 소규모 지역 병원의 집계 데이터를 확인했을 때, 동료들과 상의한 끝에 지역 병원의 환자 시술이 전반적으로 덜 복잡하기 때문에 평균 시간이 더 짧다는 결론을 내리고 맙니다. 물론 집계 통계량에만 의존하는 것은 이러한 차이의 진짜 근본적인 원인을 가려버립니다.

마취 지속 시간은 겉보기에는 아주 단순해 보였지만 (단 두 개 병원 간의 데이터를 조화시키려 할 때조차) 훨씬 더 복잡한 것으로 드러났습니다. 종양학(oncology)이나 신경학(neurology)과 같은 다른 질환 분야는 훨씬 더 큰 도전 과제입니다. 이러한 질환들과 그 관리(그리고 그에 따른 데이터)는 이미 고도로 세분화되고 복잡한 뉘앙스를 지니고 있습니다. 게다가 질환과 치료법에 대한 우리의 이해도 끊임없이 진화하고 있습니다. 이는 데이터 조화 문제를 더욱 까다롭게 만듭니다.

6장에서는 의료 데이터를 분석할 때 그래프를 활용하여 데이터를 조화시키는 방법과 그래프 특화 분석법을 살펴보는 데 시간을 할애했습니다. 이제 이러한 주제 중 일부가 연합 접근법의 맥락에서 어떻게 구현될 수 있는지 살펴보겠습니다.

## 그래프와 연합 접근법 및 멀티태스크 학습

연합에 대한 뜨거운 관심에 발맞추어, 그래프와 연합 접근법이 어떻게 결합될 수 있는지 잠시 살펴보겠습니다. 앞서 논의했듯이 연합 환경에서는 데이터 조화의 필요성이 훨씬 더 큽니다. 여러 기관에 걸쳐 데이터를 분석하기 전에, 각 기관에서 데이터가 조화되었는지, 그리고 네트워크 전체에 걸쳐 일관되게 조화되었는지 확인해야 합니다. 그래프는 연합 환경 밖에서 유용한 것과 마찬가지로 여기서도 확실히 유용하게 쓰일 수 있습니다. 하지만 이 절에서는 연합 분석 및 연합 학습의 일환으로 그래프를 명시적으로 통합할 수 있는 몇 가지 방법을 논의할 것입니다.

한 가지 유의할 점은, 여기서 논의할 내용의 상당수가 수많은 새로운 아이디어와 접근법이 등장하고 있는 활발한 연구 분야라는 사실입니다. 이 중 일부는 매우 유용한 것으로 입증될 수 있고 일부는 작고 특정한 사용 사례에만 유용할 수도 있습니다. 하지만 6장에서 언급했듯이 필자는 그래프 전반에 대해 여전히 큰 기대를 품고 있으므로, 그래프와 연합 학습의 결합은 자연스럽게 탐구해 볼 만한 분야입니다.

6장에서 논의했듯이 우리는 (그래프 순회와 같은 다른 방법 외에도) 임베딩(embedding)을 활용하여 그래프 데이터를 직접 분석할 수 있습니다. node2vec과 같은 기법의 암묵적인 가정은 모든 데이터가 한곳에 모여 있다는 점입니다. 이는 온톨로지나 기타 인코딩된 지식(예: SNOMED 또는 UMLS)을 기반으로 한 임베딩에는 문제가 없지만 (cui2vec이나 med2vec에서 보았듯이) 실제 환자 데이터를 기반으로 임베딩을 구축하고자 할 때는 까다로운 과제가 될 것입니다.

그렇다면 자연스럽게 생기는 의문은, 임베딩 생성을 위한 모델 학습을 어떻게 연합(federate)할 것인가 하는 점입니다. 특히 여기에는 잠재적으로 매우 큰 이점이 있기 때문입니다. 자체 데이터로 임베딩 모델을 학습시키는 것도 훌륭한 출발점이지만 우리의 데이터에 편향이 존재할 수 있다는 점을 알고 있습니다. 예를 들어 샌프란시스코 베이 지역(Bay Area) 환자들의 데이터는 캘리포니아 농촌 지역 환자들과는 매우 다른 인구통계학적 특성을 나타냅니다. 이 모든 데이터에 걸쳐 모델을 학습시키기 위해 우리에게는 몇 가지 선택지가 있습니다. 데이터를 단일 위치로 모으거나(집계하거나) 연합 학습(federated learning)을 사용할 수 있습니다. 전자(단일 위치 집계)는 기술적 관점에서는 가장 단순하고 쉽지만 여러 조직 간의 행정적·법률적 측면에서 합의를 이끌어내야 합니다. 보안 및 개인정보 유출 사고에 대한 조사가 강화되는 상황을 고려할 때, 리더십(경영진)은 다른 측면의 합의가 이루어졌음에도 불구하고 전반적인 위험을 완화하기 위해 연합 방식을 선택할 수 있습니다.

구체적인 솔루션과 접근 방식은 궁극적으로 생성하려는 임베딩 유형에 따라 달라집니다. node2vec의 경우, 첫 번째 단계는 전체 그래프에 걸쳐 무작위 보행(random walk)을 생성하는 것입니다. 다음 단계는 전이 학습(transfer learning)과 같은 기법을 사용하거나 학습 자체를 연합(예: FedAvg 사용)하여 연합 학습 참여 기관(sites)의 전체 네트워크에 걸쳐 모델 가중치를 학습시키는 것입니다.2,3 전체 네트워크에 걸쳐 임베딩 모델을 학습시키려고 시도하는 대신, 더 단순한 접근 방식은 피처 엔지니어링(feature engineering) 과정의 일환으로 전체 연합 네트워크에 동일한 사전 학습(pre-trained) 모델을 사용하는 것입니다. 예를 들어 동일한 snomed2vec 모델을 연합 네트워크의 모든 사이트에 배포한 다음, 해당 모델을 사용하여 각 사이트의 데이터(진단 코드, 약물 코드 등)를 인코딩할 수 있습니다. 설계에 따라 데이터가 인코딩되고 나면, 다른 여타 프로젝트와 마찬가지로 머신러닝/딥러닝 프로젝트의 나머지 부분을 실행합니다.

이는 사이트 간에 환자 기록의 임베딩을 공유할 수 있는 가능성도 보여줍니다. 특히 국경을 넘어 환자 데이터를 공유하는 최선의 방법에 대한 법적·윤리적 문제를 여전히 해결해 나가는 중이지만 환자 수준의 임베딩을 공유하는 것은 개인정보 보호 우려를 완화할 수 있는 한 가지 방법이 될 수 있습니다. 물론 이는 우리가 다음 두 가지 핵심 사항을 실행하고 검증할 수 있다는 전제하에 가능합니다.

• 각 사이트 내의 데이터를 전체 네트워크에 걸친 공통 의미론(semantics)으로 조화(harmonize)시킨다.
• 임베딩이 전체 네트워크에 걸쳐 일관된 의미론을 갖도록 보장한다.

첫 번째 사항은 이 책 전반에서 다양한 수준으로 논의해 온 내용이며 연합 환경에서도 동일하게 적용됩니다. 다만 앞 절에서 논의했듯이 연합 환경에서는 이것이 훨씬 더 중요합니다. 또한 불일치를 식별하기가 전반적으로 어렵고 특히 왜 불일치가 발생하는지 파악하기 어렵다는 점에서 더욱 까다롭습니다. 개별 사이트는 자신의 데이터만 볼 수 있기 때문에, 의미론적 조화(semantic harmonization)를 위한 지침은 가능한 한 명확해야 합니다(OHDSI 커뮤니티가 OMOP 매핑 가이드라인으로 수행한 작업과 유사함).

두 번째 사항도 의미론과 관련이 있지만 매우 다른 관점입니다. 여기서는 ICD-10 코드가 일관되게 사용되고 있는지, 혹은 모두가 SNOMED 코드를 사용하고 있는지에 대해 이야기하는 것이 아닙니다. 또한 "투약 처방(medication order)"이 "약물 처방(medication prescription)"과 동일한 의미를 갖는지 여부를 이야기하는 것도 아닙니다. 대신, 임베딩 모델이 학습됨에 따라 학습되는 다양한 가중치가 네트워크 전반에 걸쳐 일관되게 유지되는지를 의미합니다. 이는 현재 활발히 연구되고 있는 분야이며, 모범 사례나 즉시 사용 가능한 완성형(batteries-included) 상용 라이브러리에 대한 합의는 아직 보지 못했습니다.4

자연어 처리 (Natural Language Processing)

지금까지 우리는 연합 분석과 연합 학습에 대해 조금 배웠고, 데이터 조화의 과제가 그러한 환경에서 어떻게 증폭되는지 살펴보았습니다. 또한 그래프를 사용하여 이러한 과제 중 일부를 완화하는 방법도 살펴보았습니다. 이 책에서 논의된 여러 주제와 자주 교차하는 헬스케어 데이터 분석의 또 다른 분야는 자연어 처리(natural language processing, NLP)입니다.

헬스케어 분야의 대부분의 개념이 그렇듯, "자연어 처리"라는 문구는 매우 다양한 의미를 가질 수 있습니다. ElasticSearch, Solr, Lucene을 사용하여 텍스트를 인덱싱하는 것부터 자유 텍스트(free text)에서 고도로 미묘한 질병 진행 과정을 추출하기 위한 본격적인 트랜스포머(transformer) 기반 NLP에 이르기까지 다양한 프로젝트들을 보아왔습니다.

지난 몇 년 동안 부상한 관심 분야로는 자연어 이해(natural language understanding, NLU)와 자연어 생성(natural language generation, NLG) 두 가지가 더 있습니다. 자연어를 깊이 있게 다루는 사람들은 종종 이러한 용어를 구분하여 사용하지만 실제로는 단순히 자연어 처리라는 하나의 범주로 묶여서 다루어지는 경우를 자주 봅니다. 헬스케어 분야에서 자연어 생성과 관련된 작업도 일부 이루어지고 있지만 대부분의 작업은 NLU의 개념과 주로 일치합니다.

이 분야는 거의 모든 산업 전반에서 연구와 구현이 모두 활발하게 진행되는 영역입니다. 결국 생성되는 데이터의 상당 부분은 어떤 형태로든 자유 텍스트를 포함하고 있기 때문입니다. 소셜 미디어부터 IT 티켓 관리, 임상 기록(clinical notes)에 이르기까지 구조화된 데이터와 나란히 존재하는 방대한 양의 텍스트를 마주하게 됩니다. 때로는 이 텍스트가 구조화된 데이터와 겹치기도 하고 때로는 상호보완적이기도 합니다(이러한 구분의 중요성에 대해서는 잠시 후에 살펴보겠습니다).

헬스케어 NLP에서는 데이터를 임상 텍스트(clinical text)와 비임상 텍스트(nonclinical text)의 두 가지 범주로 나눕니다. 이 장에서는 주로 임상 텍스트에 초점을 맞춥니다. 비임상 텍스트를 다루는 경우라면 여러 산업 전반에 걸쳐 참고할 수 있는 리소스가 이미 많이 존재합니다.

이제 NLP와 NLU에 대한 기본적인 소개를 마쳤으니, 임상 텍스트를 활용하는 거의 모든 프로젝트에서 가장 흔하게 접하는 NLP 내의 한 가지 특정 작업에 집중해 보겠습니다.

개념 추출 (Concept Extraction)

RWD 기반의 "NLP" 프로젝트에 대해 들을 때, 십중팔구는 일종의 개념 추출(concept extraction) 작업입니다. 때로는 이를 개체명 인식(named entity recognition, NER)이라는 일반적인 NLP 프로세스로 부르기도 합니다. 본질적으로 비정형 텍스트를 스캔하여 알려진 용어 체계/통제 어휘집(controlled vocabularies)에 해당하는 용어/개념을 추출하는 것입니다.

예를 들어 의료 청구(billing) 프로세스 중의 자동 코딩(autocoding)은 개념 추출의 한 가지 응용 사례입니다. 병원과 클리닉에서는 구조화된 필드와 비정형 필드 모두에서 감지된 텍스트를 매칭하여 청구서에 첨부할 적절한 청구 코드(예: ICD-10, E/M, CPT 등)를 식별할 수 있는 자동 코딩 서비스를 자주 사용합니다. 자동 코딩은 자동화된 프로세스이지만 청구서를 검증하고 오류를 수정하기 위해 워크플로에 사람이 개입하는 것이 일반적입니다.

자동 코딩, 또는 더 일반적으로 개념 추출에는 다양한 수준의 정교함이 존재합니다. 일부 접근 방식은 ElasticSearch, Solr, Lucene 등에서 볼 수 있는 퍼지 문자열 매칭(fuzzy string matching) 기반의 단순 키워드 매칭 도구를 사용할 수 있습니다. 이러한 도구 중 일부는 어간 추출(stemming)이나 표제어 추출(lemmatization)과 같은 보다 정교한 NLP 기법을 활용하기 시작합니다. 어간 추출과 표제어 추출은 레벤슈타인(Levenshtein) 거리나 다메라우-레벤슈타인(Damerau-Levenshtein) 거리 같은 것을 맹목적으로 살펴보는 대신, 단어를 보다 의미 있는 "어근(root)"으로 분해하는 과정을 포함합니다.

거리 측정 지표 (Distance Metrics)
레벤슈타인 거리는 두 문자열 사이의 차이를 단순하게 측정하고 정량화한 것입니다. 단일 문자 삽입, 삭제, 대체 횟수를 계산하여 문자열 간의 거리 형태로 나타냅니다. 다메라우-레벤슈타인 거리는 삽입, 삭제, 대체 외에도 텍스트의 글자 위치 전위(transposition)까지 계산하여 이를 확장한 것입니다. 이러한 유형의 지표는 실제 텍스트에 대한 이해를 요구하지 않으면서도 오타가 있는 텍스트를 처리하는 데 효과적일 수 있습니다. 헬스케어 데이터의 다른 측면과 마찬가지로 텍스트 처리 기법을 살펴볼 때 염두에 두어야 할 한 가지는 해당 기법이 텍스트의 구문(syntax)에 초점을 맞추고 있는지 아니면 의미론(semantics)에 초점을 맞추고 있는지 여부입니다. 종종 단순한 구문 중심 솔루션이 매우 우수한 결과를 제공하기도 합니다.

어간 추출(Stemming)은 토큰을 어간(word stem)으로 분해하는 과정입니다. 이는 일반적으로 복수형 단어의 s, 동명사의 ing, 동사 활용형과 같은 접미사를 제거하기 위한 영어의 기본 규칙들을 따릅니다. 예를 들어 humble, humbled, humbles라는 단어는 humbl이라는 어간으로 축약될 수 있습니다. 중요한 점은 어간이 반드시 유효한 단어일 필요는 없다는 것입니다. 어간 추출을 도입함으로써 검색 및 인덱싱 엔진은 레벤슈타인 거리는 멀지만 영어를 모국어로 사용하는 사람에게는 명백하게 연관성이 있는 용어들을 매칭할 수 있게 됩니다.

유사하게 표제어 추출(Lemmatization) 역시 토큰이나 단어를 분해하여 매우 유사한 용어 간의 매칭률을 높입니다. 그러나 표제어 추출은 단어의 문맥과 의미를 반영합니다. 상호운용성 및 조화에 대한 앞선 논의와 마찬가지로 어간 추출이 구문론적(syntactic)이라면 표제어 추출은 의미론적(semantic)입니다. 예를 들어 corpus와 corpora(NLP 알고리즘을 학습시키는 텍스트 세트를 설명할 때 자주 사용하는 용어)라는 단어는 어간 추출 시 corp가 되지만 표제어 추출 결과는 corpus가 됩니다. 또 다른 예로 bore, bearing, bears라는 단어는 동사 to bear의 활용형이므로 표제어 결과가 bear가 됩니다.

눈치채셨겠지만 마지막 예시의 단어들은 여러 개의 표제어로 귀결될 수 있습니다. 예를 들어 bore는 동사 to bear의 과거형일 수도 있지만 단순한 명사일 수도 있습니다. 또한 bears는 동물 곰(bear)의 복수형일 수도 있습니다. 이러한 단어들은 모두 문맥과 품사에 따라 여러 가지 의미를 가집니다. 따라서 용어를 표제어 추출할 때는 품사도 함께 고려됩니다. 품사를 알 수 없는 경우에는 가능한 모든 표제어를 반환하도록 선택할 수도 있습니다.

다시 개념 추출 이야기로 돌아가 보겠습니다. 이상적으로는 헬스케어 용어의 복잡성과 뉘앙스를 고려할 때 단순 어간 추출보다는 일종의 표제어 추출기(lemmatizer)를 사용하는 것이 바람직합니다. 물론 위키피디아와 같은 텍스트를 기반으로 구축된 기성 모델(out-of-the-box models)은 임상 NLP에 적합하지 않습니다. 그렇다고 하더라도 표제어 추출 프로세스 자체는 RWD를 다루는 대부분의 헬스케어 활용 사례에 적용하기에는 지나치게 광범위하고 일반적입니다.

근거 생성(evidence generation) 맥락에서는 대개 진단, 약물, 검사 결과 수치 등 몇 가지 유형의 개념만 추출하면 되는 경우가 많습니다. 이들은 모두 명사이며 매우 구체적인 맥락을 가지고 있습니다. 그러나 유사하거나 동의어인 용어를 식별하려면 공통 어간이나 적절한 표제어를 찾는 것 이상의 작업이 필요합니다. 예를 들어 환자가 복용 중일 수 있는 약물을 추출할 때 "acetaminophen", "paracetamol", "Tylenol"은 대부분의 맥락에서 모두 동일한 대상을 의미합니다. 이러한 개념의 중복을 식별하려면 전통적인 NLP 기법만으로 표제어 추출기를 훈련할 수 있는 수준보다 훨씬 더 많은 지식이 필요합니다.

아세트아미노펜 외에 다른 약물 성분도 포함하고 있는 DayQuil과 같은 약물을 살펴보면 상황은 더욱 복잡해집니다. 따라서 현재 Tylenol을 복용 중인 모든 환자를 매칭하려는 경우 paracetamol과 acetaminophen을 식별해야 할 뿐만 아니라, DayQuil 및 이를 함유한 기타 복합 제제 약물까지 찾아내야 합니다.

바로 이 지점에서 UMLS와 수많은 용어 체계 및 통제 어휘집이 매우 유용해집니다. 한 가지 접근 방식은 NLP 기반 도구 및 라이브러리(예: MetaMap, cTAKES, MedSpaCy)를 사용한 다음 추가적인 맥락과 지식을 활용하여 추출된 개념들을 연결하는 것입니다.

현재 이것이 가장 일반적인 접근 방식입니다. MetaMap과 같은 도구는 (3장에서 논의했듯이) UMLS 내에 포함된 개념에 해당하는 개념 고유 식별자(Concept Unique Identifiers, CUI)를 반환합니다. 개념의 CUI를 확보하고 나면 UMLS 내의 기존 관계들을 활용하여 이 개념에 추가적인 맥락과 지식을 보강할 수 있습니다.

짧은 텍스트 조각과 이를 통해 개념/CUI를 추출하는 구체적인 예시를 살펴보겠습니다.

### 사례 연구: DayQuil 복용 텍스트 분석
어떤 사람이 "patient reports taking DayQuil PRN"이라고 기록한 텍스트 블록이 있다고 가정해 보겠습니다.

#### PRN (필요시 복용) 용어의 의미
PRN은 "상황이 필요로 할 때(as the thing is needed)"라는 의미의 라틴어 어구인 "pro re nata"의 약어입니다. 이는 임상 기록에서 (항생제 등에서 흔히 볼 수 있는) 정기적인 일정에 따른 투약과 달리 약물(또는 기타 중재)이 필요할 때만 사용됨을 나타내기 위해 일반적으로 사용됩니다.

이 텍스트에 대해 MetaMap 도구를 실행하면 단일 CUI인 C0719665가 반환됩니다. 그런 다음 그래프에서 더 광범위한 개념을 쿼리하면 다음 CUI 목록이 반환됩니다:

1. C0000970 (acetaminophen)
2. C1511537 (cough and cold preparations)
3. C0350422 (dextromethorphan hydrobromide)
4. C0304430 (pseudoephedrine hydrochloride)

이 CUI 중 세 가지는 구성 성분 약물에 해당합니다: acetaminophen(진통 및 해열제), dextromethorphan(기침 억제제), pseudoephedrine(비충혈 완화제). UMLS를 쿼리함으로써 우리는 임상의들 사이에서 매우 일반적인 지식을 시스템에 추가로 통합할 수 있었습니다.

이와 동일한 접근 방식을 반정형 데이터(semistructured data) 요소에도 적용할 수 있습니다. 제가 보아온 많은 상용 데이터셋에는 진단이나 약물 컬럼이 종종 포함되어 있습니다. 단일 데이터셋 내에서는 용어가 일관될 수 있지만(예: 해당 컬럼에 "acetaminophen"이라는 표기가 한 가지 방식으로만 나타남), 여전히 해당 컬럼은 통제되지 않은 용어 세트를 포함하고 있습니다. 우리가 가장 먼저 해야 할 일 중 하나는 이러한 용어들을 알려진 용어 체계와 매칭하는 것입니다. 이러한 상황에서는 본격적인 NLP가 아니더라도 동일한 접근 방식을 사용할 수 있습니다.

그래프(및 데이터 과학의 다른 많은 분야)에서 보았듯이 딥러닝에 대한 관심은 NLP로도 확장되고 있습니다. 다음 섹션에서는 하나의 작업이 다른 작업에 영향을 미칠 수 있음을 알고 있으므로 여러 NLP 작업을 통합하는 방법에 대해 논의합니다.

통합적 접근 방식 (Integrated approaches)
점차 여러 작업을 단일 알고리즘으로 결합하려는 시도가 늘어나고 있습니다. 한 가지 명확한 접근 방식은 서로 다른 작업들을 동시에 학습시키는 멀티태스크 학습(multitask learning)입니다. 멀티태스크 학습은 여러 개의 공유 레이어 뒤에 작업별 전용 레이어가 이어지는 신경망 아키텍처를 활용합니다. 핵심 아이디어는 다양한 작업의 레이어들이 서로 영향을 미칠 수 있다는 점입니다.

예를 들어 어간 추출과 표제어 추출에 대한 이전 논의로 돌아가 보겠습니다. 표제어 추출과 품사 감지를 위해 별도의 모델을 학습시킬 수 있습니다. 하지만 표제어 추출은 단어의 품사를 아는 것으로부터 이점을 얻기 때문에, 이 두 작업을 단일 신경망으로 결합하면 전반적인 성능을 향상시킬 수 있습니다.

멀티태스크 학습에 관한 추가 자료 (Additional Resources on Multitask Learning)
멀티태스크 학습에 대한 추가 정보는 다음 자료들을 참고하시기 바랍니다:
• An overview of multi-task learning
• Deciding Which Tasks Should Train Together in Multi-Task Neural Networks
• Natural Language Processing and Graph Representation Learning for Clinical Data

임상 NLP 분야에서 멀티태스크 학습을 적용한 최근 사례 중 하나는 Mulyar 등이 2021년에 발표한 MT-clinical BERT: Scaling Clinical Information Extraction with Multitask Learning이라는 제목의 논문입니다. 연구진은 여러 영역에 걸친 작업을 수행하도록 모델을 구성했습니다:

• 문장 쌍 유사도 (Sentence pair similarity)
• 문장 쌍 함의 (Sentence pair entailment)
• 약물 사건 및 약물 이상반응 추출 (Drug event and adverse drug event extraction)
• PHI 비식별화 (PHI de-identification)
• 문제, 치료, 검사 추출 (Problem, treatment, and test extraction)
• UMLS 의미 그룹 식별 (UMLS semantic group identification)

이 작업들은 모두 서로 영향을 미칠 수 있으므로 단일 딥러닝 모델로 결합하는 것이 합리적입니다. 다양한 유형의 임상 개념을 감지하려 할 때, 특정 용어가 약물일 가능성이 높아지면 당연히 문제(증상, 징후, 진단)일 가능성은 낮아지며 그 반대도 마찬가지입니다. 마찬가지로 문자열의 의미 그룹(semantic group)을 알면 개념의 감지 및 추출을 개선하는 데 도움이 됩니다.

특정 질환 영역에 국한되지 않고 RWD의 광범위한 과제를 해결하려 할 때, 앞서 설명한 문제, 치료법, 약물 등의 추출과 같은 접근 방식을 자주 볼 수 있습니다. 때로는 암이나 다른 질환 영역과 관련된 치료법에만 집중함으로써 탐색 공간을 조금 좁힐 수도 있습니다. 그러나 데이터 관점에서 보면 이러한 기법들은 여전히 상당히 일반적(generic)입니다.

하지만 NLP와 지식(그래프)을 통합하는 멀티태스크 모델을 사용하여 특정 작업에 집중하는 사례가 점점 더 늘어나고 있습니다. 한 예로 Alawad 등의 Integration of Domain Knowledge using Medical Knowledge Graph Deep Learning for Cancer Phenotyping이라는 프로젝트가 있습니다. 이 프로젝트는 병리 보고서에서 특정 암 표현형(부위, 세부 부위, 편측성, 행동 양식, 조직학, 등급)을 식별하기 위해 NLP와 UMLS 기반 지식 그래프를 결합했습니다.

지금까지 NLP에 대한 우리의 논의는 다양한 기법과 접근 방식을 사용하여 텍스트에서 구조화된 개념을 추출하는 데 주로 초점을 맞추었습니다. 다음 섹션에서는 구조화된 개념을 명시적으로 추출하는 단계 없이 텍스트를 기반으로 직접 모델을 구축하는 방법을 살펴보겠습니다.

개념 추출을 넘어서 (Beyond Concept Extraction)
앞 섹션에서는 임상적 관점에서 개념 추출과 개체명 인식을 살펴보았습니다. 오늘날의 많은 활용 사례는 임상 개념을 다른 구조화된 데이터와 나란히 비교할 수 있도록 피처 엔지니어링의 일환으로 적절한 임상 개념을 추출하는 데 중점을 두고 있습니다.

또한 감성 분석(sentiment analysis)과 유사하게 명시적인 개념 추출 단계 없이 텍스트를 직접 분석하는 활용 사례도 있습니다. 패혈증(sepsis)을 직접 예측하기 위해 다양한 임상 데이터와 임상 텍스트를 함께 결합하여 패혈증을 감지하는 활용 사례를 살펴보겠습니다. 본질적으로 이들은 텍스트 데이터를 또 하나의 데이터 스트림으로 취급합니다. (6장에서 논의했듯이) 임베딩 접근 방식은 텍스트의 기저에 있는 의미론과 맥락의 일부를 암묵적으로 포착합니다. 그러나 피처 엔지니어링 과정에서 데이터 과학자가 직접 검사할 수 있는 개념 세트는 존재하지 않습니다.

### 사례 연구: 패혈증(Sepsis) 조기 예측

패혈증(Sepsis)은 감염에 의해 유발되는 신체 장기의 전신적 기능 부전입니다. 흔히 사람들은 패혈증을 "혈액 감염(blood infection)"이라고 부르지만 실제로는 감염에 대한 신체의 반응입니다. 치료하지 않고 방치할 경우 패혈증은 대부분 치명적입니다. 치료가 빠를수록 생존 확률이 높아집니다. 그 결과, 병원 응급실에서 환자가 중증도 분류 간호사(triage nurse)를 만나는 즉시 적용되는 알고리즘을 포함하여 패혈증 조기 감지 알고리즘에 대한 관심이 점점 높아지고 있습니다.

생체 징후(vital signs) 활용부터 심전도 모니터(EKG/ECG), 임상 텍스트 통합에 이르기까지 패혈증 감지에 머신러닝의 다양한 측면을 적용한 수많은 논문이 있습니다. 한 예로 Amrollahi 등이 발표한 *'Contextual Embeddings from Clinical Notes Improves Prediction of Sepsis'*라는 제목의 프로젝트가 있습니다. 이것이 유일한 연구 사례는 아니지만 명시적인 개념 추출(concept extraction)이나 개체명 인식(NER)에 의존하지 않고 임상 텍스트를 통합하는 대안적 접근법을 잘 보여줍니다. 저자들은 궁극적으로 개념을 먼저 추출할 필요 없이 정형 데이터(예: 생체 징후)와 Clinical-BERT 임베딩을 모두 통합하는 모델을 구축했습니다.

#### 패혈증 예측을 위한 임상 NLP와 머신러닝
Yan 등이 2022년 초에 발표한 기존 연구에 대한 최신 체계적 문헌고찰(systematic review)은 패혈증, NLP, 머신러닝에 대해 더 자세히 알아보고자 할 때 유용할 것입니다.

이러한 Clinical-BERT 임베딩 활용 사례는 텍스트 데이터를 딥러닝 및 기타 고급 분석 기법에 직접 통합하는 수많은 방법 중 하나일 뿐입니다. 딥러닝에 단 하나의 모범 사례(best practice)만 존재하는 것이 아니듯, 텍스트에 임베딩을 적용하고 여러 서로 다른 벡터를 결합하는 이러한 접근 방식은 임상 텍스트와 관심 있는 임상 영역에 대한 지식뿐만 아니라 NLP와 딥러닝에 대한 경험과 지식을 필요로 합니다.

이제 임상 NLP에 대한 기본적인 이해와 몇 가지 구체적인 예제를 살펴보았으니, 사용할 수 있는 몇 가지 임상 NLP 도구를 살펴보겠습니다. 먼저 오픈소스 또는 공개적으로 사용 가능한 도구들을 살펴보겠습니다. 그 후에는 수년에 걸쳐 접했던 몇 가지 상용 도구를 나열하겠지만 세부적인 내용까지 깊이 다루지는 않겠습니다.

---

## 임상 자연어 처리 (Clinical NLP) 도구

"임상 NLP"의 범주 내에는 꽤 많은 도구와 제품이 존재합니다. 앞서 논의했듯이 어떤 도구는 검색 엔진에 훨씬 더 가깝고 어떤 도구는 자연어 이해(NLU) 솔루션에 더 가깝습니다. 이 절에서는 몇 가지 솔루션과 그 장단점을 살펴보겠습니다. 솔루션은 특정 순서 없이 설명합니다.

---

### NCBO BioPortal Annotator

NCBO는 국립 생물의학 온톨로지 센터(National Center for Biomedical Ontology)의 약칭으로, 스탠퍼드 대학교에서 관리하는 NIH 지원 이니셔티브입니다. NCBO는 주로 대표 제품인 BioPortal로 잘 알려져 있습니다.

BioPortal은 연구자가 웹 인터페이스와 REST API를 사용하여 용어 체계(terminologies), 통제 어휘집(controlled vocabularies), 온톨로지와 상호작용할 수 있도록 지원하는 호스팅 플랫폼입니다. UMLS에 포함된 것과 같은 표준 소스를 호스팅할 뿐만 아니라, 사용자가 자신의 온톨로지를 업로드하고 공유할 수도 있습니다.

BioPortal을 특히 강력하게 만드는 점은 여러 온톨로지에 걸쳐 용어와 개념을 연결하는 기능입니다. 이를 통해 누구나 자신의 사용 사례에 맞는 온톨로지를 생성하면서 이 온톨로지를 ICD-10, SNOMED CT, LOINC 등 기존 개념에 연결할 수 있습니다.

BioPortal 내에는 Annotator와 Annotator+ Beta라는 두 가지 NLP 도구가 있습니다. 이 도구들은 비정형 텍스트를 입력받아 BioPortal 내의 모든 "온톨로지"에서 일치하는 개념 목록을 반환합니다.

#### BioPortal 내의 온톨로지 체계
BioPortal 내에 업로드된 모든 소스는 온톨로지라고 불린다는 점에 유의해야 합니다. 그러나 온톨로지의 엄밀함(formality)은 저자의 배경과 온톨로지의 의도/목적에 따라 다릅니다. 어떤 것은 단순한 통제 어휘집인 반면, 어떤 것은 시맨틱 웹(semantic web) 원칙을 준수하는 완전한 온톨로지입니다.

소량의 텍스트를 테스트하고 검색 매개변수(예: 온톨로지 하위 집합 또는 시맨틱 유형 지정)를 세부 조정할 수 있는 웹 인터페이스가 제공됩니다.

DayQuil과 관련된 사례 연구로 돌아가 보겠습니다. - "patient reports taking DayQuil PRN(환자가 필요 시 데이퀼을 복용한다고 보고함)." 이를 Annotator에 입력하면 약 150개의 표준 매핑 결과가 추출됩니다.

Annotator에서 이를 직접 시도해 보면, 여러 다양한 온톨로지에 걸쳐 `patient`라는 용어와 일치하는 수많은 결과를 보게 될 것입니다. 우리의 특정 사용 사례에서는 관련 없는 사용 사례에 특화된 온톨로지(예: Malaria Ontology)가 대부분이므로 이러한 온톨로지는 신경 쓸 필요가 없을 것입니다. Annotator를 사용하면 관심 있는 특정 온톨로지로 제한할 수 있을 뿐만 아니라 "가장 긴 일치만 선택(match longest only)"과 같은 특정 제한 조건을 적용할 수도 있습니다.

---

---

#### 최장 일치 우선 매핑 (Match Longest Only)
이는 BioPortal뿐만 아니라 대부분의 임상 NLP 도구에 포함된 기능입니다. 가능한 가장 긴 문자열을 매칭함으로써 문자열의 각 부분과 일치하는 수많은 개념 대신 가능한 한 가장 구체적인 개념을 얻을 수 있습니다. 예를 들어, "prostate cancer(전립선암)"라는 문자열은 189개의 결과를 반환하며 전립선암 자체뿐만 아니라 `prostate`와 `cancer` 각각에 대한 개념도 매칭합니다. "Match longest only"를 체크하면 전립선암에 관한 결과만 46개 나타납니다. 추가 검색 매개변수를 지정함으로써 본질적으로 프로세스에 추가 컨텍스트를 통합하여 추출되는 개념의 품질을 향상시킬 수 있습니다.

BioPortal에는 이제 Annotator+라는 새로운 베타 버전의 Annotator가 있습니다. Annotator와 유사하게 작동하지만 부정 감지(negation detection)와 같은 추가 기능이 더해졌습니다.

#### 임상 부정 감지 (Negation Detection)
부정 감지는 임상 기록에서 "부정적(negative)" 진술을 감지하는 기능입니다. 예제를 시작했을 때 우리는 아세트아미노펜이 함유된 약물을 복용 중일 수 있는 환자를 찾고 있었습니다. 하지만 임상 기록에 "patient denies taking…(환자가 ~ 복용을 부인함)" 또는 "patient stopped taking…(환자가 ~ 복용을 중단함)"이라고 적혀 있다면 결과에서 이 항목을 제거해야 합니다. 다른 예로는 "rule out(배제)", "no evidence(증거 없음)", "absent(없음/음성)"와 같은 어구가 있습니다. 임상 개념과 함께 사용될 때 환자의 임상 기록에서 해당 개념의 존재를 부정하게 됩니다.

부정 감지는 임상 NLP에서 어렵지만 중요한 측면이며 일반적으로 여전히 미해결 과제로 남아 있습니다. 텍스트 말뭉치에 대해 개념 추출/NER 작업을 맹목적으로 수행하면 임상 개념에 대한 모든 언급을 찾아냅니다. 즉, 최근 아세트아미노펜을 복용한 모든 환자를 검색하거나 필터링할 때 복용을 "부인한(denies)" 환자까지 매칭되어 수많은 위양성(false positive)이 발생하게 됩니다.

문구를 "patient denies taking dayquil"로 변경하고 부정 감지를 선택한 후 검색 대상을 SNOMED CT 및 NCIT로 제한해 보겠습니다. 이 경우 DayQuil에 대한 개념이 감지되면서도 부정(negated) 상태로 정확히 플래그 처리됩니다.

---

---

Annotator의 웹 인터페이스는 소량의 텍스트를 처리하거나 가장 적절한 매개변수를 결정하는 데 유용한 도구이지만 BioPortal과 자동화된 상호작용을 수행할 수 있는 REST API도 제공됩니다. Annotator API는 더 광범위한 BioPortal API의 일부이므로 일반적인 BioPortal과의 상호작용과 동일한 규칙을 따릅니다. 여기에는 API 키 사용 및 JSON(및 JSON-LD)과 XML 지원이 포함됩니다.

### Apache cTAKES

Apache cTAKES는 임상 텍스트 분석 및 지식 추출 시스템(Clinical Text Analysis and Knowledge Extraction System)의 약어로, 메이요 클리닉(Mayo Clinic)의 학술 프로젝트로 시작되었습니다. 그 이후 Apache 탑레벨 프로젝트가 되었으며 Apache UIMA와 같은 복잡한 프레임워크를 통합하고 있습니다.

수년 동안 cTAKES는 복잡한 임상 NLP 파이프라인을 구현하기 위한 대표적인 표준(go-to) 프레임워크였습니다. UIMA를 사용하므로 cTAKES 파이프라인을 프로젝트 고유의 단계와 통합할 수 있었습니다. 또한 UIMA 덕분에 cTAKES 파이프라인의 단계를 커스텀 프로세서로 교체하는 것도 가능합니다.

본질적으로 cTAKES(및 UIMA 프레임워크)를 사용하면 공통 분석 시스템(Common Analysis System, CAS)과 통합되는 주석기(annotator) 파이프라인을 구축하고 관리할 수 있습니다.

---

입력 텍스트가 처리되어 CAS에 로드되는 것을 볼 수 있습니다. 그런 다음 각 주석기에 의해 처리되고 분석됩니다(점선 화살표로 표시됨). 하지만 주석기끼리 직접 통신하지는 않으며 CAS와 상호작용합니다. 각 주석기는 CAS에서 필요한 데이터/정보를 추출하고 텍스트를 처리 및 분석한 다음 결과를 다시 CAS로 보냅니다.

이를 통해 다른 주석기에 의존하지 않고 프로젝트 필요에 따라 주석기를 추가하거나 제거할 수 있습니다. 예를 들어, 한 주석기는 들어오는 텍스트를 일련의 문장으로 분할하는 문장 감지 알고리즘일 수 있습니다. 그러면 다른 주석기가 이 문장들을 입력으로 받아 품사 태깅(part-of-speech detection, POS)을 수행할 수 있습니다. 만약 POS 감지기에 문장이 필요한데 사용할 수 있는 문장이 없다면, 전체 분석 엔진은 단순히 다음 단계로 넘어갑니다. 텍스트를 UMLS CUI와 매칭하기 위한 분석 엔진뿐만 아니라 흡연력 감지와 같은 기타 특정 작업을 위한 분석 엔진도 제공됩니다.

cTAKES의 주된 난점은 학습 곡선이 상당히 가파르고 신규 사용자에게 다소 버거울 수 있다는 점입니다. 이러한 복잡성은 특히 Python이나 R과 같은 비-JVM 언어에서 데이터 과학 프로젝트에 cTAKES를 설정하고 통합할 때 많은 오버헤드가 발생함을 의미합니다.

(cTAKES에만 국한되지 않고) 모든 사전 구축 모델에서 나타나는 또 다른 문제는 모델이 원래 학습된 텍스트 유형으로 인해 기본 상태(out of the box)에서는 기대에 미치지 못할 수 있다는 점입니다.

하지만 NLP 프로세스의 각 단계를 들여다볼 수 있는 시각적 디버거가 함께 제공되어 NLP 파이프라인의 정교한 튜닝과 최적화를 용이하게 해줍니다. 또한 사용자가 일련의 문서나 기타 텍스트를 일괄 처리할 수 있는 독립형 "처리 엔진(processing engine)" 그래픽 사용자 인터페이스(GUI)도 함께 제공됩니다. 분석 엔진 파이프라인의 단계 중 하나로 NLP 출력을 데이터베이스나 기타 영구 스토리지에 유지(persist)하도록 할 수 있습니다. Java로 구현되어 있으므로 지원 라이브러리가 있다는 전제하에 온프레미스이든 클라우드 기반이든 거의 모든 유형의 데이터베이스와 인터페이스하도록 분석 엔진을 설정할 수 있습니다.

---

### MedSpaCy

마지막으로 살펴볼 프로젝트는 SpaCy 프레임워크를 기반으로 구축된 MedSpaCy 도구입니다. 이 도구가 빠르게 인기를 얻고 있는 주요 이유 중 하나는 Python으로 작성되어 기존 Python 기반 프로젝트에 편리하게 통합할 수 있기 때문입니다.

cTAKES와 유사하게 모듈식 구조이며 객체 지향 접근 방식을 기본 아키텍처로 채택하고 있습니다. 이를 통해 데이터 과학자는 기본 제공되는 기능을 그대로 활용할 수 있을 뿐만 아니라 필요에 따라 파이프라인을 쉽게 맞춤 설정하고 확장할 수 있습니다(실제로는 거의 항상 확장이 필요합니다!). 또한 UMLS를 대상으로 개념 추출/개체명 인식을 제공하는 모듈도 포함되어 있습니다.

지금까지 몇 가지 FOSS(무료 오픈소스) 임상 NLP 프로젝트를 살펴보았습니다. 다른 모든 FOSS 접근 방식과 마찬가지로, 특히 특정 문제를 단순히 해결하고자 하고 NLP 파이프라인 배포의 세부적인 부분까지 신경 쓰고 싶지 않다면 상당한 숨은 비용이 발생할 수 있습니다. 다음으로는 논의를 마무리하기 위해 몇 가지 상용 임상 NLP 솔루션을 살펴보겠습니다.

---

### 상용 임상 자연어 처리 솔루션

온프레미스와 클라우드 기반 솔루션의 조합을 제공하는 여러 상용 제품이 있습니다:

- **Amazon Comprehend Medical**: AWS에서 제공하는 웹 서비스로, BioPortal Annotator와 유사하게 작동합니다. ICD-10 및 SNOMED CT와 같이 널리 사용되는 용어 체계와 연결됩니다. UMLS와의 연계도 일부 제공하지만 소스 용어 체계 및 UMLS 릴리스의 구체적인 버전 정보를 확인하는 것은 문서 깊숙이 숨겨져 있습니다.
- **Google Healthcare Natural Language API**: AWS와 유사하게 Google도 UMLS와 연결된 임상 개념 추출용 API를 제공합니다. 또한 사전 구축된 모델이 특정 요구사항에 맞지 않을 경우 커스텀 모델을 통합할 수 있는 기능도 제공합니다.
- **Melax Tech**: CLAMP(Clinical Language Annotation, Modeling, and Processing) 툴킷을 상용화하고 있는 스타트업입니다. CLAMP는 텍사스 대학교 보건과학센터 생물의학정보학 대학원의 학술 프로젝트로 시작되었으며 개별 연구 프로젝트를 진행하는 학술 사용자에게는 무료로 제공됩니다.
- **Spark NLP for Healthcare**: John Snow Labs가 자사의 Spark NLP 솔루션을 기반으로 제공하는 솔루션입니다. 일반적인 NLP 벤치마크 및 임상 개체 추출(여기서는 임상 개체 확인[clinical entity resolution]이라고 부름)에 대해 AWS, Google Cloud, Azure와의 솔루션 비교 결과를 발표했습니다.

---

- **IQVIA NLP**: IQVIA는 광범위한 헬스케어 산업에 분석 및 기술 솔루션, 임상 연구 서비스, 데이터 자산을 제공합니다. 솔루션의 일부로 NLP 플랫폼도 제공합니다.
- **Linguamatics**: 또 다른 헬스케어 전문 기업인 Linguamatics는 병원 및 의료 시스템부터 제약 회사에 이르기까지 업계 전반의 모든 수준을 위한 NLP 솔루션을 제공해 왔습니다. 2019년에 IQVIA에 인수되었으므로 더 광범위한 IQVIA 솔루션 제품군에 통합되고 있을 것으로 보입니다.
- **Wolters Kluwer Health Language Clinical NLP**: IQVIA 및 Linguamatics와 마찬가지로 Wolters Kluwer는 헬스케어 산업을 위한 광범위한 솔루션 제품군의 일부로 NLP 솔루션을 제공합니다.

오픈소스 도구를 평가하든 상용 도구를 평가하든, 어떤 도구가 임상 텍스트를 중심으로 특별히 구축되었는지 고려하는 것이 중요합니다. 다음으로 논의하겠지만 임상 NLP와 "일반" NLP 사이에는 몇 가지 중요한 차이점이 있습니다.

---

## 임상 NLP와 일반 NLP 분야의 주요 차이점

보시다시피 임상 NLP 문제를 다룰 때 많은 선택지가 있습니다. 이러한 옵션은 모두 임상 텍스트 처리에 매우 특화되어 있으며 우리는 종종 임상 NLP가 다른 사용 사례보다 다르고 더 어렵다는 말을 듣습니다. 이것이 얼마나 사실일까요?

어쨌든 영어(또는 모든 언어)는 매우 미묘한 뉘앙스를 지니고 있으며 종종 여러 가지로 해석될 여지가 있습니다. 결국 이것이 우리가 시와 문학을 즐길 수 있는 이유이기도 합니다!

모든 데이터 과학 프로젝트에는 다음 프로젝트와는 다른 뉘앙스와 구체적인 고려 사항이 있습니다. 금융과 같은 산업에서도 한 조직의 요구사항은 다른 조직과 다르며 이는 분석 과정에서 이러한 뉘앙스를 고려해야 하는 데이터 과학자에게 영향을 미칩니다.

그렇다면 임상 NLP의 어떤 점이 "더 어렵게" 만드는 것일까요? 이 질문은 약간 엉뚱한 방향으로 이끄는 질문(red herring)이며 불필요하게 NLP 측면에만 초점을 맞추게 하는 착오입니다. 실제로 임상 NLP에서 겪는 어려움은 모든 헬스케어 데이터에서 나타나는 근본적인 어려움입니다. 자유 형식의 비정형 텍스트를 적용하면 상황이 조금 더 어려워지기는 하지만 텍스트 자체가 반드시 더 어려운 것은 아닙니다.

1장과 2장에서 논의했듯이 헬스케어 데이터는 거대한 "다양성(variety)" 문제로 어려움을 겪고 있습니다. 동일한 내용을 표현하는 방법이 매우 다양하며 겉보기에 동일해 보이는 것을 해석하는 방법도 매우 다양합니다. 여러 면에서 이러한 이질성(heterogeneity)은...

는 헬스케어가 실행되는 방식의 편차에서 기인한 증상입니다. 헬스케어의 전달과 의학적 진료는 항상 인간의 생리학과 병태생리학에 대한 우리의 이해를 뒤따라가는 형태입니다. 결과적으로 헬스케어가 "표준"을 따르는 방식으로 균일하게 제공되도록 보장하는 것은 거의 불가능합니다. 따라서 NLP가 필요한 문제를 다룰 때는 항상 이 점을 우리의 사고와 문제 해결 과정에 반영해야 합니다.

예를 들어, "단어 가방(bag of words)" 기법은 텍스트 데이터를 분석에 통합하는 초기적이고 단순한 방법으로 자주 사용됩니다. 이와 유사하게 단어 빈도-역문서 빈도(TF-IDF, term frequency-inverse document frequency)도 자주 사용됩니다. 이러한 접근법은 문자열 간의 차이에 의존하며 때로는 어간 추출(stemming)이나 표제어 추출(lemmatization)과 결합되기도 합니다. 그러나 앞서 논의했듯이 임상 텍스트를 다룰 때는 어간 추출과 표제어 추출만으로는 충분하지 않습니다. 결과적으로 우리는 이 점을 고려해야 합니다. 따라서 "단어 가방" 접근법 대신 "개념 가방(bag of concepts)"과 유사한 방식을 고려해야 합니다. 즉, 텍스트를 (최소한으로) 가공된 문자열의 묶음으로만 보는 대신, 개념 수준에서 데이터를 처리하고 분석해야 합니다.

어떤 NLP 접근법이든 그렇게 해야 하는 것이 아닌가 하는 의문이 들 수도 있습니다. 일반적으로는 그렇습니다! 하지만 다른 사용 사례에서는 대량의 텍스트가 존재하기 때문에 굳이 그렇게 할 필요가 없는 경우가 많습니다. 비슷한 의미를 가진 용어들이 기저 모델에 자동으로 반영되는 경우가 많기 때문입니다. 이에 비해 접근 가능한 임상 텍스트의 양은 위키피디아, 트위터(현 X) 또는 기타 공개 텍스트 코퍼스에서 얻을 수 있는 텍스트 양과 비교하면 극히 적습니다. 분명히 우리가 분석할 수 있는 모든 텍스트 중에서 임상 텍스트가 차지하는 비율은 극히 일부에 불과합니다. 그러나 상황을 더욱 어렵게 만드는 것은 프라이버시 문제와 PHI/PII의 유출 우려로 인해 임상 텍스트를 쉽게 공유할 수 없다는 점입니다(177페이지의 "PHI 및 PII" 참조). 비식별화/익명화는 활발한 연구 개발 분야이기는 하지만 텍스트를 비식별화하는 것은 매우 어렵습니다. 이러한 어려움 때문에 기관들은 모델 개선을 원하는 연구자나 데이터 과학자에게 임상 텍스트를 공개하는 경우가 드뭅니다. 결과적으로 사용 가능한 NLP 모델은 해당 모델이 훈련된 특정 기관에 고도로 특화됩니다.

이는 임상 NLP를 다룰 때 또 다른 주요 과제인 방언(dialect)으로 이어집니다. 이 문제 역시 사용 가능한 데이터의 부족으로 인해 더욱 두드러집니다. 따라서 단일 기관(이 경우 메이요 클리닉)의 텍스트를 기반으로 사전 훈련된 모델을 제공하는 cTAKES와 같은 프로젝트의 경우, 결국 단 하나의 방언에만 훈련된 모델을 얻게 됩니다. 단일 기관 내에서도 진료과목(service line)마다 서로 다른 "하위 방언(subdialect)"이 나타나기도 합니다. 즉, 심장내과 전문의가 진료기록(노트)을 작성하는 방식은 정신건강의학과 전문의나 응급의학과 전문의와 다를 수 있습니다. 따라서 데이터의 부족은 NLP 및 텍스트 분석에서 이미 나타나는 수많은 과제를 더욱 심화시킵니다. 설상가상으로 모델 성능에 대한 기대치(일반적으로 민감도와 특이도로 측정됨)는 다른 산업군보다 훨씬 높습니다.

최근의 코로나19(COVID) 경험을 예로 들어보겠습니다. 검사가 아직 널리 보급되지 않았던 팬데믹 초기 단계에 코로나19 환자를 조기에 탐지하는 능력을 향상시키기 위한 알고리즘을 개발하고 있었다고 가정해 봅시다. 위양성(false positive)의 "비용"은 해당 환자가 2주 동안 출근하지 못한다는 것을 의미합니다. 반면 위음성(false negative)은 동일한 환자가 여러 직장 동료, 가족/친구 및 다른 사람들을 감염시키게 되므로 훨씬 더 치명적입니다. 물론 우리는 민감도를 높이고(즉, 위음성 수를 줄이고) 싶겠지만 그렇게 하면 위양성이 증가하게 됩니다. 따라서 코로나19의 확산을 막기 위해 사람들에게 출근을 중단하고 가족 및 친구와 격리되도록 요구하게 됩니다.

이제 이를 스포티파이(Spotify), 넷플릭스(Netflix) 또는 기타 미디어 스트리밍 제공업체의 추천 엔진과 비교해 보겠습니다. 노래나 영화가 단순히 추천되지 않았을 뿐이므로 위음성이 발생했는지조차 알 수 없습니다. 반면에 위양성은 심지어 하나의 기능으로 마케팅될 수도 있습니다! "엉뚱한" 추천이라도 스스로는 결코 찾아보지 않았을 콘텐츠를 접하게 해 주어 시야를 넓혀주는 방법으로 포장될 수 있습니다. 간단히 말해, 걸려 있는 이해관계(stakes)가 더 크기 때문에 기대치도 높아지며(당연하게도), 그에 따라 데이터 과학 과제도 훨씬 더 어렵습니다.

## 결론 및 요약
이 섹션에서 다룬 각 주제는 그 자체로 책 한 권 분량이 될 수 있을 정도입니다. 이 주제들은 매우 복잡하고 미묘하며 디지털 헬스 스타트업부터 대형 제약회사에 이르기까지 학계와 산업계 모두에서 여전히 매우 활발한 연구 분야입니다. 그러나 핵심 주제는 머신러닝, 딥러닝, 혹은 연합 학습(federated learning)에 주로 집중하더라도 데이터를 조화시키는(harmonize) 방법이 데이터 과학의 매우 중요한 측면이라는 점입니다. 실제로 연합 분석과 연합 학습에 대한 뜨거운 관심과 기대는 효과적인 데이터 조화(harmonization)가 그 어느 때보다 중요하다는 점을 강조해 줍니다.

따라서 그래프는 데이터 조화에서 계속해서 중요한 역할을 담당하고 있습니다. 그러나 서로 다른 코딩 시스템이나 용어 체계 간에 개념을 매핑하는 방식을 개선하는 것 외에도, 그래프는 모델 개발 프로세스에 점점 더 많이 통합되고 있습니다. NLP 사용 사례에 통합하든 의사 결정 지원(decision support) 사용 사례에 통합하든, 그래프에 담긴 정보와 지식은 딥러닝 알고리즘의 핵심 특징(feature)이 되고 있습니다. 그래프 영역으로의 임베딩 확장은 단어 임베딩이 데이터 과학 툴박스의 핵심 요소로 자리 잡은 NLP 세계의 수많은 주요 학습 내용과 기법들을 활용할 수 있게 해 줍니다.

---

# 제8장 그래프, 데이터 조화 및 마지막 고찰

우리는 지난 몇 장에 걸쳐 실사용 헬스케어 데이터를 다루는 다양한 측면을 깊이 있게 살펴보았습니다. 우리는 일반적인 헬스케어 데이터 소스에 대한 개요를 시작으로, 헬스케어 데이터에 대한 꽤 거시적인 관점에서 출발했습니다. RWD를 다룰 때 가장 까다로운 측면 중 하나는 데이터 소스의 수와 유형이 사실상 무한하다는 점입니다. 임상의, 연구자, 기업가, 테크 기업들이 지속적으로 혁신함에 따라 활용 가능한 RWD의 다양한 유형은 계속해서 증가할 것입니다.

우리가 '실사용 데이터(real-world data)'라는 용어를 사용할 때, 대부분은 가장 흔한 데이터 소스로 전자의무기록(EHR), 청구 데이터(claims), 임상 레지스트리(clinical registries)를 즉시 떠올립니다. 많은 이들은 관찰 연구(observational studies)라고도 불리는 비중재 연구(noninterventional studies)도 포함합니다. 이러한 연구들은 대개 환자 인구통계학적 정보, 질환 및 진단명, 약물, 기타 중재(intervention) 수집에 초점을 맞춘다는 점에서 모두 유사합니다.

그러나 환자 치료를 목적으로 생성(및 수집)되는 데이터의 세계를 더 깊이 파고들수록 우리는 빠르게 완전히 새로운 영역에 발을 들이게 됩니다. 타이핑 속도를 이용해 정신 건강의 변화를 감지하는 앱이든, 보행 패턴을 이용해 운동 장애를 추적하는 스마트폰이든, 발작을 감지하는 야구 모자든 가능성은 무궁무진합니다. 하지만 이는 어떤 종류의 건강 상태를 감지, 치료 또는 관리하는 데 사용될 수만 있다면 거의 모든 것이 실사용 데이터로 간주될 수 있음을 의미합니다. 이러한 데이터를 다루는 것은 매우 복잡하며 대부분의 데이터 유형과 비교할 때 RWD 여정을 시작할 때는 프로세스, 워크플로 및 방법론을 새롭게 조정해야 합니다.

"실사용 데이터"의 구체적인 정의는 다양하며 명확한 합의는 없습니다. 그러나 대부분은 다음의 개념들을 (일부는 더 암묵적인 형태로) 포함하고 있는 것으로 보입니다.

* **일상적인 임상 진료 반영 (Reflects routine clinical practice)**
  다시 말해, 데이터가 임상시험과 같이 엄격하게 통제된 상황이 아닌 일상적인 진료를 반영합니다.
* **2차 분석 (Secondary analysis)**
  데이터가 의료 서비스 제공 중에 수집되었으며 현재는 과학적 연구나 근거 생성 등에 사용되고 있습니다.
* **후향적 (Retrospective)**
  데이터가 이미 수집되었으며 연구 프로토콜을 개발한 후 특정 환자 코호트에 대해 장기간 데이터를 수집하는 방식과 달리 과거 데이터를 거슬러 올라가 분석합니다.

어떤 이들은 RWD를 무작위 배정 임상시험(randomized clinical trials) 환경 밖에서 수집된 모든 헬스케어 데이터로 광범위하게 정의합니다. 또 다른 이들은 비중재 연구(즉, 치료적 중재를 받은 집단과 받지 않은 집단 간의 결과 차이를 측정하기 위해 수집된 것이 아닌 데이터)로 정의하기도 합니다. 아직 합의된 정의에 도달하지는 못했지만 무엇이 RWD이고 무엇이 아닌지에 대한 단 하나의 정의가 과연 유용한지는 확신할 수 없습니다. 한 가지 정의를 따르면 임상 레지스트리가 RWD로 간주될 수 있지만 다른 정의에서는 제외될 수도 있습니다. 이것이 실제로 중요할까요? 데이터 과학자, 데이터 엔지니어, 애널리틱스 엔지니어, 또는 주요 헬스케어 이해관계자로서 우리는 환자 치료의 질과 효율성을 향상시킬 수 있도록 접근 가능한 모든 데이터를 활용하고자 합니다.

따라서 무엇이 실사용 데이터인지 아닌지를 정의하는 데 집중하기보다는, 이러한 데이터의 핵심 특성과 데이터를 효과적으로 활용하기 위해 무엇을 해야 하는지에 집중해 보겠습니다.

먼저, 데이터의 출처(source)가 무엇인지 고려해야 합니다. 이는 기저 데이터의 맥락과 의미를 이해하는 데 도움이 됩니다. 데이터를 우리의 사용 사례에 맞게 적용할 수 있는지, 그리고 데이터와 분석 결과를 해석할 때 어떤 과학적 고려사항이 필요한지(필요한 경우) 파악하려면 이러한 맥락을 이해해야 합니다.

소스 데이터의 유형에 따라 물어봐야 할 구체적인 질문들이 있습니다. 이러한 질문들을 사전에 모두 나열하는 것은 불가능하므로 적절한 도메인 전문가를 참여시키는 것이 중요합니다. 예를 들어 EHR 데이터를 다루는 경우 최고 의료정보 책임자(CMIO, chief medical informatics officer), 최고 간호정보 책임자(CNIO, chief nursing information officer), 그리고 해당 임상 도메인의 뉘앙스를 이해하는 전문의를 참여시키는 것이 중요합니다. 유방암에 대한 조언을 신경과 의사에게 구하지 않는 것처럼, 종양학(oncology) 프로젝트에서 종양내과 전문의가 아닌 의사에게 의존하는 것은 프로젝트에 치명적일 수 있습니다. 마찬가지로 임상 레지스트리 데이터를 다루는 경우에는 임상 레지스트리 관리자(clinical registrar)를 참여시켜야 합니다. CMIO가 헬스케어 데이터에 익숙할 수는 있지만 임상 레지스트리 관리 전문가는 아니므로 레지스트리 내부 데이터의 미묘한 차이에 대해 정확히 설명할 수 없습니다.

그 점을 염두에 두고 우리가 물어보고자 하는 몇 가지 질문 예시는 다음과 같습니다.

* 전자건강기록(EHR)에서 나온 데이터인가요? 그렇다면 어떤 유형의 EHR인가요? 병원 전반에 걸친 시스템인가요, 아니면 특정 진료과목이나 질환 영역에 특화된 시스템인가요(예: Flatiron의 OncoEMR과 같은 종양학 특화 EHR)?
* 임상 레지스트리에서 나온 데이터인가요? 이 레지스트리는 어떻게 구축되었으며 본래 목적은 무엇이었나요? 생체 시료 추적, 특정 환자 코호트 추적, 또는 특정 중재(intervention) 세트를 추적하기 위한 것이었나요?
* 의료보험 지불자(healthcare payer)로부터 나온 데이터인가요? 정부 지불기관(공공보험)인가요, 민간 보험사인가요? 청구 프로세스는 어떻게 되나요? 청구를 처리하는 데 얼마나 많은 세부 정보가 필요한가요?
* 디지털 헬스 기기나 앱에서 나온 데이터인가요? 이 기기나 앱은 FDA 승인/허가(clearance)를 받았나요? 그렇다면 510(k) 허가인가요, 그리고 어떤 기기와 동등성(substantial equivalence)을 인정받았나요?

앞서 나열한 데이터 소스의 유형과 관계없이, 데이터의 맥락을 더 잘 이해하기 위해 항상 물어봐야 하는 질문들도 있습니다.

* 누가 데이터를 입력했나요? 그 사람의 임상적 배경(의료 전문성)은 무엇인가요?
* 데이터 입력/수집을 안내하는 프로토콜이나 프로세스는 무엇인가요?
* 사용된 코딩 시스템이 있다면 무엇인가요? 음성/부정(negative) 결과는 어떻게 처리되었나요(예: 양성 코드의 부재인지, 음성 코드의 존재인지)? 개념이 기존 코드와 잘 일치하지 않을 때는 어떻게 처리했나요? 코드를 생략하고 자유 텍스트 메모를 추가했나요, "가장 적절한" 코드를 선택했나요, 아니면 다른 방식을 취했나요?
* 데이터가 정제(cleaned)되거나 가공되었나요? 그렇다면 어떤 방식으로 처리되었나요?
* 데이터에 대해 데이터 품질 평가가 수행된 적이 있나요? 누가 수행했으며 그 결과는 어떠했나요?

이 목록이 결코 전부는 아니지만 반드시 확인해야 하는 질문 유형들을 잘 보여줍니다. 데이터에 대해 지레짐작하는 것은 실패를 자초하는 지름길입니다.

이 모든 점은 헬스케어 RWD와 이를 다루는 우리의 방식이 끊임없이 진화하고 있음을 시사합니다. 우리는 지속적으로 학습하고 지식을 공유하며 헬스케어 RWD를 수집(ingest), 정제, 처리, 분석하는 방식을 지속적으로 발전시켜 나가야 합니다. 이러한 진화의 일환은 헬스케어 제공 과정에서 어떤 데이터가 생성되고 통합되는지의 형태에서도 나타납니다. 향후 몇 년 동안 RWD가 어떻게 변화할지 간략히 살펴보겠습니다.

## 다양한 유형의 헬스케어 현실 데이터 (RWD)

잠시 관점을 바꾸어 보겠습니다. 사용 사례와 다양한 데이터 소스 유형이라는 관점에서 RWD를 생각하는 대신, 데이터 자체의 관점에서 데이터를 살펴보겠습니다. 지금까지 우리는 대부분의 데이터가 정형(structured) 또는 반정형(semistructured) 데이터라는 점을 명시적으로 짚지 않은 채 많은 시간을 할애해 데이터를 논의했으며 비정형/자유 텍스트 데이터에 대해서는 간헐적으로만 언급했습니다.

정형 데이터는 일반적으로 관계형 데이터베이스 형태로 제공되며 미리 지정된 목록에서 선택된 값을 반영합니다. 이는 우리가 다루기에 가장 쉬운 데이터이지만 데이터를 입력, 생성 또는 수집하는 사람에게는 가장 제한적인 형식이기도 합니다.

반정형 데이터는 일반적으로 특정 유형의 데이터를 요구하는 텍스트 필드이며 대개 몇 단어 길이에 불과합니다. 이에 대한 예로는 약물 이름을 묻는 필드가 있을 수 있습니다. 특정 문자열 자체는 통제되지 않지만 해당 값이 약물을 의미한다는 점은 알 수 있습니다.

마지막으로 자유 텍스트(free text)가 있는데, 이는 일반적으로 제약이 없어 사용자가 원하는 것은 무엇이든 입력할 수 있습니다.

관계형 데이터베이스, 문서 데이터베이스, 그래프 데이터베이스는 이러한 유형의 데이터를 저장하는 데 매우 적합하며 NLP는 자유 텍스트에 일정한 질서와 구조를 부여하는 데 유용합니다. 하지만 아직 논의하지 않은 다른 모든 유형의 데이터는 어떨까요?

우리는 RWD로 간주될 수 있는 이러한 다른 모든 데이터 유형에 대해 많은 시간을 들여 이야기하지 않았습니다. 적어도 제약 산업에서는 현재 대부분의 초점이 EHR 및 오믹스(예: 유전체학[genomics], 단백체학[proteomics], 대사체학[metabolomics] 등) 데이터에 대한 접근에 맞춰져 있습니다. 점점 더 흔해지는 RWD 소스 중 하나는 차세대 염기서열 분석(NGS, next-generation sequencing)과 같은 분자 검사 결과입니다. 또한 시계열(time-series) 데이터와 연속적 생체 신호에 초점을 맞춘 연구도 활발히 증가하고 있습니다.

헬스케어의 많은 것들이 그렇듯, "시계열"은 매우 광범위한 용어이며 일/주/월 단위의 주기적인 데이터 수집부터 저주파 생체 센서(예: 연속 혈당 측정기), 고주파 센서(예: 뇌파 검사[EEG])에 이르기까지 모든 것을 설명하는 데 사용됩니다. 또한 스마트폰이나 스마트워치의 가속도계 데이터처럼 헬스케어에 특화되지 않은 센서의 활용도 포함됩니다.

### 디지털 표현형 (Digital Phenotypes)
표현형(phenotype)이라는 용어는 일반적으로 유전자형(genotype)의 맥락에서 사용되지만 디지털 바이오마커를 포함한 다른 바이오마커를 살펴볼 때도 점점 더 흔하게 사용되고 있습니다. 표현형이라는 용어는 생명체의 관찰 가능한 특성들의 집합을 설명하는 데 사용됩니다. 예를 들어 나뭇잎의 모양, 눈의 색깔, 또는 제1형 당뇨병을 앓고 있는 것 모두가 표현형입니다.

시계열 데이터 자체만으로는 환자에 대해 알 수 있는 정보가 제한적입니다. 우리는 이러한 데이터를 환자의 임상적 표현형과 연결해야 합니다. 그래야 시계열 데이터를 실제 임상적 변화와 연관 지을 수 있습니다. 거시적인 수준에서 이는 우리가 오믹스 데이터에 적용하는 접근 방식과 동일합니다. 유전체학에서 우리는 유전자형을 표현형과 연관 짓습니다. 우리는 하나의 표현형에서 다음 표현형으로 넘어갈 때 공통적인 부분(또는 다른 부분)을 살펴보고 변이/돌연변이와 표현형 간의 상관관계를 탐색합니다. 강한 상관관계를 발견했을 때, 우리는 종종 식별된 변이를 바이오마커(biomarker)라고 부릅니다. 바이오마커는 기저에 있는 생물학적 또는 질병 과정에 대해 무언가를 알려주는 객관적인 척도나 징후를 말합니다. 시계열 데이터를 다룰 때 우리는 이를 종종 디지털 바이오마커(digital biomarkers)라고 부릅니다(디지털 바이오마커에는 시계열 데이터보다 훨씬 더 많은 것이 포함되기는 하지만 말입니다).

다양한 유형의 RWD를 둘러싼 혁신이 점점 더 늘어남에 따라 정규화(normalization)와 조화(harmonization)에 대한 필요성은 더욱 커질 것입니다. 데이터 조화는 책 전체를 관통하는 핵심 주제 중 하나였지만 다음 섹션은 이를 조금 다른 관점에서 검토해 볼 수 있는 기회입니다.

## 데이터 정규화 및 조화의 심층 고찰

순수한 정형 임상 데이터를 다루든 최신 시계열 및 멀티오믹스(multi-omics) 데이터를 통합하든, 헬스케어 RWD 과제의 핵심은 정규화와 조화 문제입니다.

RWD를 조화시켜야 하는 필요성은 헬스케어에만 국한된 것이 아닙니다. 잠시 헬스케어는 잊어보겠습니다. 실사용 데이터를 '광범위한 산업의 서로 다른 측면을 담당하고 각자의 비즈니스 영역을 설명하는 방식이 서로 다른 여러 소스로부터 데이터를 통합해야 하는 모든 비즈니스의 일상적인 운영 과정에서 생성되는 데이터'로 정의해 보겠습니다. 이는 다소 인위적인 정의이기는 하지만 헬스케어 RWD에서 우리가 겪는 핵심 문제들을 잘 부각해 줍니다.

결국 RWD를 다루는 우리들은 서로 다른 데이터 소스를 의미 있는 방식으로 통합해야 합니다. 이 책의 대부분은 데이터 조화에 필요한 데이터 랭글링(data wrangling)에 초점을 맞추어 왔습니다. 데이터 조화와 관련하여 우리가 고려해야 할 두 가지 서로 다른 관점이 있습니다.

### 다중 데이터셋 병합의 현실과 과제

첫 번째 관점은 흔히 우리가 자연스럽게 떠올리는 질문, 즉 여러 출처의 데이터를 어떻게 하나의 단일 데이터 소스로 결합할 것인가에 관한 것입니다. 대개의 경우 이는 데이터셋의 유효 크기(effective size)를 키우기 위한 노력의 일환입니다. 대형 병원 시스템은 일반적으로 서로 겹치는 환자 집단을 진료하는 여러 다양한 병원과 클리닉으로 구성되어 있으므로, 이러한 시도의 흔한 대상이 됩니다. 관리자와 연구자가 전체 시스템에 걸쳐 환자 데이터를 조회할 수 있도록 이 모든 데이터를 단일 데이터 웨어하우스나 데이터 커먼즈(data commons)로 통합하는 것은 상당한 가치가 있을 수 있습니다.

하지만 제약 업계에서는 우리가 접근하는 RWD의 거의 전부가 제3자(외부 기관)로부터 오기 때문에 이러한 방식이 활용 사례가 되는 경우가 드뭅니다. 데이터 사용 계약(Data use agreements)으로 인해 데이터셋을 병합하거나 연계(linking)하는 것이 금지되는 경우가 많기 때문입니다.

데이터셋을 연계할 때는 관련된 연계 수준(level of linking)을 명확히 정의하는 것이 중요합니다. 개체 식별(entity resolution, 개체 해소) 같은 작업은 비교적 훨씬 단순합니다. 만약 병원 데이터셋을 중복되는 처방 청구(claims) 데이터셋과 연계한다면, 각 데이터셋에서 동일한 환자를 식별하고 있는지 확인하고 싶을 것입니다. 이것이 첫 번째 수준이며 은행/금융이나 광고 분야의 개체 식별과 상당히 유사합니다.

두 번째 수준의 연계는 임상 데이터의 조화(harmonization)를 포함합니다. 즉, 서로 다른 데이터셋 전반에 걸쳐 진단명, 약물 등이 동일한 의미를 갖도록 보장하는 것입니다. 많은 프로젝트가 지나치게 창의력을 발휘하다가 잘못된 인사이트를 낳을 수 있는 데이터셋 병합 기법을 고안하기 시작하는 지점이 바로 여기입니다.

저는 한 환자 집단의 진단 데이터를 가져와 (두 환자 집단 간의 겹치는 정도를 알 수 없는 상태에서) 다른 환자 집단의 치료 데이터와 결합하려고 시도하는 프로젝트들을 보아왔습니다. 이는 의료 서비스 제공 자체의 변동성(variation) 때문에 문제가 됩니다. 이러한 연계는 첫 번째 데이터셋의 진단 데이터가 두 번째 데이터셋의 진단 데이터와 비교 가능할 것이라는 암묵적인 가정을 전제로 하지만 실제로는 그렇지 않은 경우가 대부분입니다.

자연스러운 다음 단계는 진단 데이터(예: 조직학, 형태학 등)를 사용하여 이 두 데이터셋을 결합하는 것일 것입니다. 이 진단 정보를 바탕으로 두 데이터셋을 연계함으로써 우리는 본질적으로 (다른 것들 중에서도) 다음과 같은 가정을 하게 됩니다:

* 두 번째 데이터셋의 치료 데이터는 분자 검사 결과를 기반으로 한다.
* 두 데이터셋은 동일한 병기 가이드라인(및 해당 가이드라인의 동일한 버전)을 따른다.

이러한 시도는 수많은 위험을 내포하고 있습니다. 이러한 데이터 여정을 시작하기 전에는, 데이터에 대한 이해도 갖추고 있는 적절한 임상의(즉, 관심 있는 특정 질환 분야나 적응증을 전문으로 하는 임상의)를 참여시키는 것이 절대적으로 중요합니다. 이는 헬스케어 데이터 프로젝트에서 제가 계속해서 목격하는 가장 큰 격차 중 하나, 즉 다양한 모든 팀과 이해관계자를 연결해야 할 필요성을 잘 보여줍니다. 다음 섹션에서는 특히 IT 팀과 "비즈니스(현업)" 부서에 대해 구체적으로 살펴보겠습니다.

### IT 부서와 비즈니스/임상 부서 간의 간극 극복

임상의 및 기타 도메인 전문가(SME)를 기술 팀과 더 긴밀하게 연결한다는 개념은 마땅히 그래야 하는 만큼 널리 퍼져 있지 않습니다. 학술 의료 센터들은 일반 산업계보다 이를 더 잘 수행해 왔으며, 훈련된 정보학자(informaticist/informatician)들에게 의존해 왔습니다.<sup>1</sup> 컴퓨터 과학이나 IT에 개인적인 관심이 있거나 사전 교육을 받은 의사와 간호사를 통합하는 것에서 출발했지만, 이제는 헬스 정보학(health informatics), 의료 정보학(medical informatics), 임상 정보학(clinical informatics) 등의 분야에서 석사 및 박사 학위를 취득한 인재들이 이러한 역할을 맡는 경우가 늘고 있습니다.

심도 있는 기술 전문성과 심도 있는 헬스케어 전문성을 두루 갖춘 진정한 다기능(cross-functional) 팀을 구축하는 것이 그 어느 때보다 중요합니다. 저는 IT 팀이 기술적 세부사항에만 집중하고 "비즈니스" 부서에 명확한 요구사항을 제시해 달라고 요청하는 전통적인 접근 방식 때문에 수많은 프로젝트(수백만 달러에서 수천만 달러 규모의 프로젝트 다수)가 실패하는 것을 보아왔습니다.

이러한 전통적인 접근 방식은 (설령 애자일 방법론으로 포장하더라도) 임상의가 환자와 상호작용하는 방식부터 데이터가 수집되고 처리되는 이유와 방식, 그리고 수많은 분석 활용 사례에 이르기까지 헬스케어 "스택" 전반에 존재하는 복합적인 복잡성의 층위를 해결하지 못합니다.

RWD 프로젝트를 다룰 때는 특히 역할이 더 명확히 나뉘어 있는 대기업에서 팀, 프로젝트, 프로덕트를 구성하는 새로운 접근 방식이 필요합니다. 현재 우리는 비즈니스 팀과 IT 팀(여기서 "IT"는 인프라 IT뿐만 아니라 소프트웨어 엔지니어링/개발을 모두 포함하는 포괄적인 의미로 사용합니다)이 분리되어 있는 모습을 자주 봅니다. 그 결과 일종의 콜앤리스폰스(주고받기식) 비효율적인 상호작용이 발생합니다.

두 팀 간의 대화는 흔히 다음과 같이 흘러갑니다(물론 단순화한 예시입니다):

**IT 팀:** 필요한 게 무엇인지 말씀해 주세요…
**비즈니스 팀:** 난소암 3차 치료(third-line therapy)를 넘어선 시점에서 잠재적인 치료 옵션을 찾기 위해 단일 환자를 환자 저장소(repository)와 비교하려고 합니다.
**IT 팀:** 글쎄요, 어떤 종류의 데이터를 받게 되나요? CSV인가요? JSON인가요? 바이러스 검사를 해야 하나요?
**비즈니스 팀:** 무슨 말씀이신가요? 우리는 한 번에 한 환자씩 데이터를 받습니다. FHIR에 대해 들었는데, 그걸 써야 할까요?
**IT 팀:** 네, FHIR은 JSON 문서이고 RedShift에 JSON을 로드할 수 있습니다. 악성코드 검사 및 데이터 유효성 검사를 위한 파이프라인을 설정할 수 있습니다.
**비즈니스 팀:** "데이터 유효성 검사"란 무엇을 의미하나요?
**IT 팀:** null/결측값을 확인하고 데이터 타입을 검증합니다. 예를 들어 숫자 필드에 문자열이 없거나 날짜 형식이 올바른지 확인하는 식입니다.
**비즈니스 팀:** 치료 차수(line of therapy) 같은 항목과 관련하여 데이터를 올바르게 캡처했는지는 어떻게 검증하나요?
**IT 팀:** 네, RedShift에 이미 ICD-10 코드가 로드되어 있습니다.

---

<sup>1</sup> 용어상의 난제에 대해 덧붙이자면, 정보학 전문가를 가리킬 때 informaticist를 쓸지 informatician을 쓸지에 대해 여전히 합의된 바가 없습니다.
Data Normalization and Harmonization | 207

---

여러분 중 일부는 이 대화의 요점이 무엇인지 의아해할 수도 있습니다. 어떤 분들은 헛웃음을 짓고 계실 테고 어떤 분들은 눈물을 흘리고 계실지도 모릅니다. 안타깝게도 이런 유형의 대화는 너무나 빈번하게 일어나며 데이터/비즈니스 팀과 IT 팀 간의 주요한 단절을 잘 보여줍니다.

앞의 예에서 비즈니스 팀은 데이터/과학적 관점에서 자신들이 원하는 바를 명확히 말하고 있습니다. 반면 IT 팀은 기반이 되는 기술 인프라의 관점에서 이를 생각하고 있습니다. 기술 스택뿐만 아니라 역학(epidemiology), 근거 생성(evidence generation), 임상 진료(clinical practice), 보험 급여(reimbursement) 및 기타 헬스케어 도메인 주제에 모두 정통한 인재를 찾을 수 있다면 좋겠지만 그런 인재는 극히 드뭅니다.

IT와 "비즈니스" 사이의 깔끔한 분리는 활용 사례와 기초 데이터가 점점 더 복잡해짐에 따라 필연적으로 작업 속도를 늦춥니다. 우리는 기술 인력과 나란히 임상 및 도메인 전문가를 배치해야 합니다. "비즈니스"와 IT 사이의 이러한 간극을 좁히는 것은 RWD를 활용하여 인사이트를 도출하고 궁극적으로 환자가 받는 치료의 질을 향상시키는 역량을 확장하는 데 결정적입니다.

이러한 간극을 좁히는 사람들은 대개 비즈니스에 대한 훌륭한 실무 지식을 쌓은 기술자이거나, 기술적 기술과 지식을 습득한 도메인 전문가들입니다. 여기에 완벽하게 부합하는 학문 분야가 바로 의료 정보학(medical informatics, 점차 헬스 정보학(health informatics)으로 발전하고 있는)입니다.

### 의료 정보학(Medical Informatics)의 핵심 역할

앞서 간략히 언급했듯이, 의료 센터와 보건의료 시스템(특히 대학 부속 기관)은 의료 정보학이라는 비교적 "새로운" 학문을 통해 비즈니스와 IT 사이의 이러한 간극을 메워왔습니다. 헬스 정보학(health informatics)이나 임상 정보학(clinical informatics)이라는 용어도 자주 사용되는 것을 들을 수 있을 것입니다.

미국의 대표적인 의료 정보학 전문 학회인 미국 의료 정보학회(American Medical Informatics Association, AMIA)는 기존의 여러 단체가 합병하여 1989년에 설립되었습니다. 따라서 이 분야와 학문은 수십 년 동안 존재해 왔지만 헬스케어 RWD를 다룰 때 이 학문의 역할을 알고 있거나 제대로 이해하는 사람은 여전히 드문 것 같습니다.

하나의 학문 분야로서 의료 정보학은 널리 이해되지 못하고 있어, 명확한 가치 제안(value proposition)을 확립하기가 상당히 까다롭습니다. 의료 정보학 분야는 매우 광범위하며 수많은 기술과 역량을 아우르고 있는데, 그중 다수는 데이터 작업과 직접적인 관련이 없습니다. 하지만 이 책과 RWD의 맥락에서 볼 때, 의료 정보학은 데이터의 생성/수집과 데이터의 확장 가능하고 효과적인 분석 사이의 간극을 좁히는 데 중추적인 역할을 합니다.

Data Normalization and Harmonization | 209

매일 데이터를 다루는 의료 정보학 전문가들은 일반적으로 다음에 대해 깊이 이해하고 있습니다:

* 의료 현장 및 헬스케어 전달 과정에서 헬스케어 데이터가 사용되는 방식
* 헬스케어 데이터 모델과 그에 따른 트레이드오프(절충점)
* 임상 어휘(vocabularies), 용어집(terminologies) 및 온톨로지(ontologies)
* 상호운용성(Interoperability) 표준
* 데이터 시각화 및 분석

의료 정보학은 단일한 기본 전공 분야가 아니기 때문에, 대부분의 종사자는 기술적 배경(예: 소프트웨어 엔지니어링, IT) 또는 임상적 배경(예: 의사, 간호사, 약사)을 가지고 진입합니다. 이것이 의료 정보학 커뮤니티와 직업군이 직면한 가장 큰 도전 과제 중 하나의 원인입니다. 분야가 너무 광범위하고 사람들이 너무나 다양한 배경에서 오기 때문입니다. 프로젝트 및 팀 리더들은 지속적으로 가장 적합한 팀원을 찾으려 노력하지만 모두가 동일한 직함을 가지고 있으면 적임자를 가려내기가 상당히 어려워집니다.

앞서 다룬 대화 예시로 돌아가 봅시다. 우리는 환자를 기존 환자 데이터베이스와 매칭하는 데 도움이 되는 시스템을 구축해야 한다는 점을 알고 있습니다. 따라서 우리는 모종의 환자 데이터베이스를 구축하고, 한 환자 분량의 데이터를 송수신하며, 난소암과 관련하여 데이터의 적절한 맥락과 의미가 보존되도록 해야 합니다. 그렇다면 기술 중심의 인재를 더 원해야 할까요, 아니면 임상 중심의 인재를 원해야 할까요? 물론 가장 쉬운 대답은 "네! 둘 다 전부요"라고 말하는 것입니다.

최근 들어 저는 **의료 정보학 아키텍트(medical informatics architect)**라는 새로운 역할을 제안하고 있습니다. 이 사람의 주된 초점은 임상적 또는 과학적 질문의 맥락에서 데이터 흐름의 관점을 통해 솔루션의 고수준(high-level) 측면을 면밀히 고민하는 것입니다. 솔루션 아키텍트와 유사하지만 이 역할은 계정/신원 관리(identity management), 네트워킹, 데이터 스토리지, 보안 같은 요소에는 덜 집중합니다. 대신 어떤 상호운용성 표준을 사용할지, 용어 서비스(terminology service)가 필요한지, 또는 "암 치료 차수(cancer line of therapy)"를 계산 가능한(computable) 방식으로 정의하는 방법 등에 집중합니다.

이 사람은 솔루션 아키텍트, 데이터 아키텍트, 데이터 엔지니어, 데이터 과학자, 지식 엔지니어이자 헬스케어 전문가의 면모를 두루 갖추고 있습니다. 올바른 질문을 던지고 고수준의 솔루션을 스케치한 다음, 그에 맞춰 적절한 도메인 전문가(SME)를 식별하고 참여시킬 수 있어야 합니다.

앞의 예시의 경우, 우리는 전통적인 솔루션 아키텍트, FHIR 경험이 있는 데이터 엔지니어, 난소암 및 기존 환자 특정 데이터베이스에 대한 경험이 있는 데이터 과학자, 우리가 데이터베이스와 매칭하려는 특정 환자 집단에 익숙한 종양내과 의사, 암 분야 경험이 있는 임상 용어 전문가(clinical terminologist)를 참여시켜야 합니다. 프로젝트를 더 깊이 파고들수록,

종양학을 전문으로 하는 임상 약사와 같이 훨씬 더 전문화된 기술과 지식이 필요할 수 있습니다. 아키텍트는 적절한 전문가들을 식별하고 참여시킬 수 있는 기반이 되는 초기 아키텍처를 개발할 수 있을 만큼 충분한 지식과 경험을 갖추고 있어야 합니다.

의료 정보학 분야는 아키텍처보다 훨씬 더 많은 것을 포괄하며 임상 용어 전문가, 임상 정보학자, 애널리틱스 엔지니어와 같은 다른 핵심 역할들을 포함합니다. 헬스케어 산업이 밸류 체인 전반에 걸쳐 RWD를 가장 잘 통합하는 방법을 계속해서 배워나감에 따라, 기술, 데이터 과학, 헬스케어/의학을 자연스럽게 넘나드는 배경을 가진 인재에 대한 관심과 활용이 더욱 증가할 것입니다.

우리는 많은 과제를 해결하기 위해 종종 기술을 바라보지만 현실에서 기술은 (기껏해야) 솔루션의 일부에 불과합니다. 최악의 경우 기술은 주의를 분산시키는 요소가 됩니다. 저는 헬스케어 데이터와 분석의 확장 가능한 영향력에는 세 가지 기둥, 즉 **사람(People), 프로세스(Process), 기술(Technology)**이 필요하다고 굳게 믿습니다. 다음 섹션에서는 RWD를 다룰 때 이것이 실제로 우리에게 무엇을 의미하는지 논의하는 데 시간을 할애해 보겠습니다.

## 기술의 문제가 아닌 사람과 프로세스의 문제

처음 의료 정보학 학회에 참석하기 시작했을 때, 한 발표자가 "의료 정보학은 인간의 문제를 해결하기 위해 기술을 적용하는 것입니다"라고 말했던 기억이 납니다. 안타깝게도 그분이 누구였는지는 기억나지 않지만 알았더라면 술이라도 한잔 샀을 것입니다! 이 말은 그 이후로 줄곧 제 뇌리에 남아 있습니다.

소프트웨어 엔지니어로서 저는 기반 기술에 대해 생각하는 것, 즉 최신 데이터베이스 기술, 머신러닝 알고리즘 또는 오케스트레이션 도구에 열광하는 것을 아주 좋아합니다. 하지만 헬스케어 분야에서 우리의 최우선 목표는 비용을 고려하면서 환자가 받는 치료의 질을 향상시키는 것이라는 사실을 잊어버리기 쉽습니다. 임상의와 환자는 우리가 최신 딥러닝 접근법을 사용했는지, 아니면 단순한 막대 그래프를 사용했는지에 신경 쓰지 않습니다.

헬스케어 데이터를 다룰 때 발생하는 문제들은 기술의 부족 때문이 아닙니다. 맞습니다, 전자의무기록(EHR)의 사용자 경험(UX)은 석기 시대 수준입니다. 맞습니다, 데이터 수집 시점에서의 데이터 품질을 개선해야 합니다. 하지만 그 해답은 부트스트랩(Bootstrap)과 앵귤러/뷰/리액트(Angular/Vue/React)로 구축된 차세대 전자의무기록을 배포하는 것만큼 단순하지 않습니다. 기반 데이터베이스 기술이나 데이터 모델을 바꾸는 것만으로 해결되는 문제도 아닙니다.

RWD에서 "실제 세계(Real World)"란 데이터가 의료 서비스 전달 과정의 복잡성과 미묘한 특성을 반영한다는 것을 의미합니다. 현실은 전자의무기록이 여러 가지 상이한 목적을 위해 동시에 데이터를 수집한다는 것입니다. 임상의들은 서로 간에, 그리고 자기 자신과 소통하려고 노력하며 행정 담당자들은 (단일 지불자/국가 보건의료 시스템과 민간 보험 시스템 모두에서) 진료비 청구/상환(reimbursement) 및 보고 요건을 충족하려고 애씁니다.

Data Normalization and Harmonization | 211

이러한 영향 요인 외에도 의학 진료(practice of medicine) 자체도 끊임없이 발전하고 있습니다. 오늘의 표준 치료(standard of care)가 내일의 표준 치료가 아닐 수 있으며 한 보건의료 시스템의 표준 치료가 다른 시스템의 표준 치료와 다를 수 있습니다. 종양내과(medical oncology) 진료에서 질환을 진단하는 방식은 부인종양과(gynecologic oncology) 진료에서의 방식과 다를 수 있습니다. 인체와 그 병리 및 중재법(interventions)을 완벽하게 이해할 때까지는, 의료진마다 달라지는 데이터의 모호성과 뉘앙스가 언제나 존재할 것입니다.

그렇다면 문제는 헬스케어 RWD를 다루는 데 있어 현재의 최첨단 기술을 어떻게 활용할 수 있는가 하는 점입니다. 이러한 복잡성이 존재할 것임을 알고 있는 상황에서, 우리의 데이터 과학자와 데이터 분석가들이 자신의 역량을 최대한 발휘할 수 있도록 어떻게 지원해야 할까요? 우리는 대부분의 데이터 과학자가 단순히 데이터에 접근하고, 정제하고, 처리하는 데 대부분의 시간을 소비한다는 것을 알고 있으며, 헬스케어 분야에서 일하는 이들에게는 이 수치가 훨씬 더 심각할 것으로 짐작됩니다.

비록 제가 처음으로 한 말은 아니지만 저는 이 메시지를 계속해서 발전시키고자 합니다. 그 해결책은 바로 **사람, 프로세스, 기술(그리고 물론 데이터)**의 결합입니다. 이 해결책은 핵심 기반인 사람을 필두로 프로세스, 그리고 이를 뒷받침하는 기술이 유기적으로 결합되는 구조를 갖습니다.

### 사람 (People)

우리는 RWD를 다룰 때 핵심적인 리소스로서 의료 정보학자에 대해 논의하는 데 약간의 시간을 보냈습니다. 여기서 핵심은 조직들이 "데이터 과학자"라는 포괄적인 역할에 너무 많이 의존하고 있다는 점입니다. 대규모로 RWD를 효과적으로 활용하기 위해서는 다양한 수준의 인재들이 필요하다는 점을 기억하는 것이 중요합니다. 임상 정보학자, 임상 용어 전문가, 의료 정보학 아키텍트 외에도, 우리는 "데이터 과학"이라는 개념을 두루뭉술하게 적용하는 방식을 재고해야 합니다.

우리에게는 RWD와 관련된 엔지니어링 요구사항에 깊이 정통한 데이터 엔지니어가 필요합니다. 예를 들어 수 테라바이트에 달하는 유전체나 영상 데이터를 이전하려 할 때, 기능적 및 비기능적 요구사항을 고려하면 무엇이 실현 가능할까요? 용어 체계(terminologies)를 활용한다는 점을 고려할 때, 이러한 데이터 엔지니어는 시맨틱 웹 기술(특히 성능부터 배포/관리의 용이성에 이르는 다양한 접근 방식의 장단점)에도 익숙해야 합니다.

또한 데이터 엔지니어링과 데이터 과학 양쪽에 모두 발을 걸치고 있는 애널리틱스 엔지니어도 필요합니다. 이들은 데이터의 적절한 맥락(context)을 온전히 보존하면서 데이터 모델 간(특히 '공통' 데이터 모델 간)의 효과적인 매핑을 보장할 책임이 있습니다. 저는 데이터가 특정 유스케이스를 위해 한 번 정제/가공되고 나면 이제 모든 유스케이스에 두루 사용될 수 있다고 지레 짐작하는 사람들을 너무나 자주 봅니다. 하지만 한 유스케이스를 위해 데이터를 정제하는 과정에서, 본질적으로 그 데이터를 다른 용도로 재사용하는 것을 불가능하게 만들어 버렸을 가능성이 큽니다.

우리는 모두 마지막 단계를 완수하기 위해 데이터 분석가와 데이터 과학자가 필요하다는 점을 잘 알고 있습니다. 하지만 그들은 지난 수십 년 동안 의료정보학(medical informatics) 커뮤니티가 발견해 온 수많은 모범 사례(best practices)를 과연 알고 있을까요? 우리는 데이터 과학자가 어떤 문제(예: ICD-10 및 SNOMED 코드 정규화)에 부딪혔을 때 용어 서비스를 처음부터 다시 바퀴를 재발명하듯 만드는 상황을 피하고 싶습니다.

### 프로세스 (Process)

적합한 인재가 있더라도 팀들이 효과적이고 효율적으로 협력하도록 보장해야 합니다. 소프트웨어 엔지니어링과 마찬가지로, 모든 데이터 과학자가 각자 알아서 데이터 전처리와 피처 엔지니어링을 수행하도록 내버려 두는 것은 너무나 흔히 저지르기 쉬운 실수입니다. 이것이 최선의 접근법이라고 생각하기 쉽지만(결국 우리는 가장 우수하고 똑똑한 데이터 과학자들을 채용했으니, 그들이 가장 잘하는 일을 하도록 두어야 한다고 생각할 수 있습니다!), 이는 코드의 난잡한 혼합(hodgepodge)과 잦은 바퀴의 재발명으로 이어집니다.

저는 물을 얻기 위해 우물을 파는 비유를 아주 좋아합니다. 어떤 데이터 과학자는 국을 끓이기 위해 물을 원하고 다른 과학자는 빵을 만들기 위해 물을 구하지만 모든 사람이 똑같은 수원(water source)을 향해 각자 자기만의 우물을 파고 있는 셈입니다. 이를 헬스케어 데이터로 다시 가져와 보면, 여러 데이터 과학자가 동일한 전자의무기록(EHR) 데이터를 바탕으로 작업하면서도 암 치료 라인(cancer line of therapy)을 추출하는 로직을 각자 다시 구현하는 모습을 본 적이 있습니다. 두 데이터 과학자에게 서로 다른 치료 라인의 정의가 필요한 상황이 있을 수도 있지만 우리는 가능한 한 언제 어디서든 재사용을 장려해야 합니다.

DataOps, MLOps, FAIR, 혹은 <다른 유행어를 여기에 입력하세요> 원칙 중 무엇을 따르기로 하든, 핵심은 우리 팀과 유스케이스에 효과적인 체계를 찾는 것입니다. 이는 생물통계학(biostatistics)에서 머신러닝/딥러닝으로 전환할 때 특히 중요합니다. 원천 데이터가 갱신될 때마다 재학습하는 모델이 있다면, 우리는 모델 드리프트(model drift), 원천 데이터의 변화, 또는 기타 다른 요인 간의 차이를 감지하고 구분할 수 있을까요?

### 기술 (Technology)

사람과 프로세스에 집중하고 싶은 마음만큼이나, 가장 까다로운 문제들을 해결하는 데 도움이 될 적절한 기술을 활용할 수 있는 중요한 기회들도 분명 존재합니다. 이러한 기술 중 다수는 등장한 지 꽤 되었지만 상당수가 학술 프로젝트로 시작되었으며 종종 '못을 찾아 헤매는 망치'인 경우가 많았습니다. 하지만 저는 여전히 이러한 기술들을 적재적소에 적용할 충분한 기회가 있다고 생각합니다.

예를 들어 용어 체계와 온톨로지를 관리해야 할 필요성에 따라, OWL부터 RDF 트리플 저장소(triple stores)에 이르기까지 시맨틱 웹 기술을 둘러싼 활동이 증가하고 있습니다. 저는 룰 엔진(rule engines)과 기타 추론(inferencing) 방식들이 여전히 충분히 활용되지 못하고 있다고 굳게 믿습니다. 데이터를 가공하고 다루는 역량을 확장하기 위해 우리가 해야 할 일의 상당 부분은 지식 공학(knowledge engineering)으로 분류될 수 있습니다.

이러한 기술을 적재적소에 적용하는 것 외에도, 이를 어떻게 확장(scale)할 것인가를 고민해야 할 필요가 있습니다. 예를 들어, 룰 엔진은 코드에 몇 가지 변환을 직접 하드코딩하는 것에 비해 상당히 느립니다. 데이터 과학자의 관점에서는 모든 것을 하드코딩하면 훨씬 빠른데, 왜 몇 자릿수나 더 느린 복잡한 시스템(룰 엔진을 포함함으로써)을 구축해야 하는지 의문을 가질 수 있습니다. 재사용성이나 어쩌면 재현성/반복성 측면에서의 이점은 잘 알고 있지만 데이터를 처리하는 데 며칠 혹은 몇 시간이 걸린다면 그러한 이점은 무의미해집니다.

물론 저는 그래프와 그래프 데이터베이스의 열렬한 지지자입니다. SPARQL/RDF 기반 데이터베이스는 아름다운 시맨틱을 제공하며 매우 우아한 솔루션을 가능하게 하지만 우리의 유스케이스에는 지나치게 과도하거나(overkill) 단순히 팀의 기술/지식 수준을 넘어설 수도 있습니다. 속성 그래프(Property graphs)는 조금 더 제약이 있지만 개발자와 데이터 과학자가 훨씬 쉽게 배울 수 있습니다. 어느 쪽이든, 저는 헬스케어 RWD를 다룰 때 그래프(및 지식 그래프)를 실험하는 단계가 이제 막 시작되었을 뿐이라고 깊이 믿고 있습니다. 이는 그래프를 데이터 엔지니어링 프로세스의 일부로 사용하는 가능성을 고려할 때 특히 그렇습니다. 그래프의 흥미로운 활용 사례 대다수가 그래프에 적용되는 일종의 분석을 포함하고 있지만 그래프를 데이터의 중간 표현(intermediate representation)으로 사용한다면 어떨까요?

### ETLT 파이프라인과 그래프 기술의 가치

이 책 전반에서 살펴보았듯이, 그래프는 헬스케어 데이터의 본질적인 네트워크 특성을 수용하여 RWD와 상호작용하는 새로운 접근 방식을 제공합니다. 관계형 데이터베이스는 SQL을 통해 데이터에 대한 단순한 인터페이스를 제공하여 수많은 분석 및 시각화 도구와 연결할 수 있게 해주지만 우리가 표현할 수 있는 범위에 제약을 가하기도 합니다.

관계형 데이터베이스로 데이터를 추출, 변환, 적재(ETL)하는 작업의 본질은 정보 손실(lossy)이 발생한다는 점입니다. 이는 우리가 데이터에 대해 특정한 가정을 세웠고 이 변환 과정에서 일부 정보를 잃어버렸음을 의미합니다. 또한 추출, 적재, 변환(ELT)에 대한 많은 논의가 이루어지고 있는데, 저는 이 접근 방식이 헬스케어 RWD의 성공에 절대적으로 중요하다고 생각합니다. 우리는 유스케이스가 변화하고 발전함에 따라 데이터를 (재)변환할 준비가 되어 있어야 합니다.

이러한 맥락에서 제안하는 최적의 아키텍처는 두 번의 변환을 거치는 추출, 변환, 적재, 변환(ETLT: Extract-Transform-Load-Transform) 프로세스입니다. 여기서는 가능한 한 많은 맥락을 포착하고 표현하면서 용어/온톨로지와 통합할 수 있도록 데이터를 그래프 기반 구조로 변환하는 명시적인 단계가 존재합니다. 그런 다음 구체적인 분석 요구사항에 따라 이를 다시 데이터프레임, 관계형 데이터베이스, 또는 기타 적절한 표현 형태로 변환할 수 있습니다.

그래프와 이를 데이터 파이프라인에 통합하는 방법에 대한 저의 열정에도 불구하고 다음 절은 '모든 문제를 해결해 주는 단 하나의 최고 솔루션은 없다'는 점을 일깨워주는 현실 점검(reality check)입니다.

### 그래프 기술의 현실적인 한계와 고려사항

저는 헬스케어 전반에 걸친 그래프의 잠재력에 대해 깊은 열정과 기대를 품고 있습니다. 여러분이 디지털 헬스케어 기업, 병원 시스템, 보험사, 제약회사 중 어디에 있든 누군가는 전체 데이터 스택에 그래프를 도입하는 방법을 고민하고 있을 것입니다.

하지만 모든 유스케이스에 그래프가 필요한 것은 아니라는 점을 알아두는 것도 중요합니다. 다른 모든 것과 마찬가지로 그래프 역시 툴박스에 들어 있는 또 하나의 도구일 뿐이며 만병통치약(silver bullet)이 아닙니다.

데이터가 비교적 평면적(flat)이고 유스케이스가 단순하다면, 관계형 데이터베이스나 문서 저장소(document store)가 적절한 솔루션일 수 있습니다. ArangoDB와 같은 데이터베이스는 문서 저장소와 그래프 데이터베이스의 기능 모두를 기본적으로 아우르는 네이티브 기능을 제공하여 이러한 경계를 다소 모호하게 만듭니다.

그래프 도입의 주요 걸림돌 중 하나는 우리가 테이블 형태의 표(tabular) 구조에 너무 많은 것을 투자해 왔다는 점입니다. 간단한 대시보드를 구축하든 최신 딥러닝 접근법을 적용하든, 테이블과 데이터프레임은 거의 모든 도구나 라이브러리의 중심에 자리 잡고 있습니다. 그래프가 여러분의 데이터에 완벽한 표현이라 할지라도, 기존 도구, 기술, 지식 간의 불일치를 감수할 만큼 투자할 가치가 있는지 판단해야 합니다.

많은 이들에게 그래프의 이점은 그래프의 하위 집합을 다시 테이블 형태로 변환하는 추가 프로세스를 데이터 파이프라인에 더할 만한 가치가 있습니다. 이것이 실행 가능한 옵션인지는 궁극적으로 팀, 유스케이스, 조직 문화 및 기술 스택의 기타 요소에 달려 있습니다. 그래프 데이터베이스의 성능이 매 릴리스마다 향상되고는 있지만 특히 그래프 규모가 커질수록 여전히 성능 문제에 시달리곤 합니다.

그렇기는 하지만 저는 다음과 같은 이유로 새로운 기술을 시도해 볼 가치가 항상 있다고 믿습니다:

• 무언가를 배울 수 있습니다.
• 새로운 유스케이스를 발견할 수도 있습니다.
• 그냥 재미있습니다.

하지만 해결하고자 하는 문제의 본질을 놓쳐서는 안 되며 만병통치약을 찾으려 하는 것은 아닌지, 혹은 단순히 멋진 기술이라는 이유만으로 네모난 못을 둥근 구멍에 억지로 끼워 맞추려(forcing a square peg into a round hole) 하는 것은 아닌지 항상 자문해 보아야 합니다.

## 맺음말 및 향후 전망

지금까지 함께해 주셔서 감사합니다! 우리는 헬스케어 실세계 데이터(RWD)의 개요와 이러한 데이터가 수집될 수 있는 다양한 방법들을 시작으로, 헬스케어 RWD에 대해 꽤 심도 있는 논의를 나누었습니다. 전자의무기록(EHR)부터 디지털 헬스케어 앱, 임상 레지스트리에 이르기까지, RWD의 정의 자체만으로도 그 가능성은 무궁무진합니다. 환자를 진료하고 돌보는 방식이 진화함에 따라 우리가 생성하고 수집하는 데이터 또한 변화할 것입니다.

그러나 데이터 원천이 무엇이든 간에, 우리는 분석하려는 데이터의 뉘앙스를 고려하고 그것이 당면한 과제에 적합한지 따져보아야 합니다. 데이터가 수집된 이유, 데이터를 수집한 주체, 기저의 연구 프로토콜(있는 경우), 그리고 데이터에 존재할 수 있는 기타 편향(bias)들을 고려해야 합니다. 명확한 연구 프로토콜에 따라 전향적(prospectively)으로 수집된 데이터를 분석하는 경우라면, 여러 기관이나 조직에 걸쳐 수집된 데이터일지라도 데이터 조화(harmonization)의 어려움이 그리 크지 않을 수 있습니다. 반면에 일상적인 환자 진료 과정에서 임상의들에 의해 수집된 EHR 데이터를 후향적(retrospective)으로 분석하는 경우라면, 데이터를 조화시키기 위해 헤쳐 나가야 할 수많은 작업이 기다리고 있을 것입니다.

제가 이 이야기를 너무 많이 반복해서 고장 난 레코드판 같다고 생각하실지도 모르겠습니다. RWD 관련 프로젝트에서 제가 목격한 가장 흔한 실패는 헬스케어 데이터 조화의 비용과 복잡성을 제대로 인식하지 못하는 것이었습니다. 저는 다른 업계의 노련한 데이터 과학자들이 "헬스케어가 데이터 수집만 좀 더 제대로 해준다면, 우리는 …을 할 수 있을 텐데"와 같은 말을 하는 것을 자주 듣습니다. 바로 이 점이 핵심입니다. 헬스케어 시스템과 의학 진료의 복잡성은 이 분야 고유의 본질적인 특성입니다. 인체에 대한 우리의 이해는 끊임없이 진화하고 있으며 환자 개개인은 유전체, 병태생리, 생활 습관, 식이, 그리고 최종적으로 얻게 되는 데이터의 '불완전함(dirtiness)'에 기여하는 기타 요인들의 고유한 조합입니다. RWD로 성공을 거두려면 이러한 차이점을 포용하고 데이터의 이질성(heterogeneity)을 완화하기 위한 기법들을 찾아내야 합니다.

저의 첫 스타트업에서 공동 창업자와 저는 '대량 맞춤화(mass customization)'라는 문구를 만들었습니다. 이는 분명 모순 형용(oxymoron)이지만 혁신적인 접근 방식의 기회를 강조하기도 합니다. 데이터 조화 파이프라인을 맞춤화해야 하는 요구와 이 프로세스를 확장 가능하게 만드는 것 사이에서 어떻게 균형을 맞출 수 있을까요?

바로 이 지점에서 그래프 데이터베이스가 엄청난 기회를 제공하며 그렇기 때문에 우리는 다양한 유형의 그래프 데이터베이스를 탐색하는 데 적지 않은 시간을 할애했습니다. 대부분의 컴퓨터 과학 도구/라이브러리와 마찬가지로, 각자 고유한 장단점을 지니고 있습니다. 어느 하나가 다른 것보다 절대적으로 낫다고 말하기는 어렵습니다. 결국 여러분의 기능적 및 비기능적 요구사항을 데이터베이스와 얼마나 잘 맞추느냐의 문제입니다.

물론 통제 어휘집(controlled vocabularies), 용어 체계, 온톨로지와 같은 요소를 통합하기 시작할 때 그래프 데이터베이스는 진가를 발휘합니다. 헬스케어는 형식화된 시맨틱 개념을 도입하는 데 있어 다른 많은 분야보다 앞서 있습니다. 인간의 모든 지식이 그렇듯, 이러한 개념들의 기저에 깔린 의미(semantics)는 유스케이스마다 다릅니다. 따라서 서로 중복되는 여러 용어 체계와 온톨로지를 자주 접하게 됩니다. 그래프 데이터베이스는 이러한 매핑을 자연스러운 방식으로 쉽게 저장하고 관리할 수 있게 해줍니다. 더 중요한 것은, 그래프 쿼리 언어를 사용하면 SQL로는 불가능한 방식으로 그래프를 탐색(traverse)할 수 있다는 점입니다.

여러분은 전자의무기록과 청구(claims) 데이터 모두의 데이터를 연결하기 위해 그래프 쿼리 언어를 어떻게 활용할 수 있는지 확인하셨습니다. RWD와 통제 어휘집을 통합한 뒤 이 결합된 데이터셋을 쿼리하는 수많은 가능성을 모두 보여주는 것은 어렵습니다. 모쪼록 이 책에서 제공한 예제들이 여러분이 직접 실험하고 창의력을 발휘할 수 있는 출발점이 되었기를 바랍니다!

EHR 데이터, 청구 데이터, 또는 기타 어떤 RWD 원천을 다루든 간에, 여러분은 이 책에서 다루지 않은 수많은 다른 상황에 마주치게 될 것입니다. 자연어 처리(NLP)는 여전히 활발한 연구 분야이며 시맨틱 컴퓨팅 및 지식 공학의 다른 측면들과 빠르게 교차하고 있습니다. 이 두 분야 모두 그래프 기술을 적극적으로 활용합니다. 따라서 NLP와 그래프 간의 융합, 특히 그래프 임베딩(graph embeddings)의 통합이 더욱 증가할 것으로 기대합니다.

NLP 외에도 연합 분석(federated analytics)이나 연합 학습(federated learning)과 같은 트렌드 역시 훌륭한 데이터 조화의 필요성을 부각합니다. 이는 그래프가 구현 세부사항(implementation detail)으로 활용될 수 있는 영역 중 하나입니다. 즉, 특정 기관(site)이 데이터 조화 프로세스를 돕기 위해 그래프를 사용하기로 선택할 수 있습니다. 연합 학습 라이브러리 및 플랫폼 전반, 그리고 각 참여 기관 자체에 요구되는 성숙도를 고려할 때, 시스템에 그래프 데이터베이스를 직접 연결하는 것은 어려울 수 있습니다. 구체적인 접근 방식과 무관하게, 연합 환경은 전체 네트워크에 걸쳐 확고한 데이터 조화를 절대적으로 요구합니다. 그렇지 않으면 모델의 성능 저하가 발견할 신호나 패턴이 없기 때문인지, 아니면 네트워크 전반에 걸쳐 데이터가 제대로 조화되지 않았기 때문인지 구별하는 것이 거의 불가능할 것입니다.

궁극적으로 RWD를 통해 성공을 거두려면, 전통적으로 '단순 IT'로 치부되던 영역과 '비즈니스' 영역 사이의 경계를 허무는 작업을 더 훌륭히 해내야 합니다. 데이터는 복잡하며 데이터를 조화시키는 접근법 또한 복잡합니다. 시맨틱 관점에서는 아름다운 솔루션이 계산적(computational) 관점에서는 다루기 힘들(intractable) 수도 있고 그 반대일 수도 있습니다. 하나의 산업으로서 우리는 의학 진료 자체뿐만 아니라 소프트웨어 엔지니어링, 데이터 엔지니어링, 데이터 과학, 시맨틱 컴퓨팅, 용어 체계 및 온톨로지뿐만 아니라 헬스케어 데이터(특히 RWD)의 뉘앙스에 정통한 인재를 더 많이 필요로 합니다.

학문으로서의 의료정보학(Medical informatics)이 이러한 간극을 좁히기 시작하고 있습니다. 그러나 뛰어난 의료정보학 전문가 팀이 있더라도, 기술 전문 인력은 헬스케어 데이터에 대해 더 많이 배우고 임상의들은 IT와 데이터에 대해 더 많이 배워야 합니다. 그래야만 지속적으로 증가하는 RWD를 활용하여 의료 시스템이 환자에게 제공할 수 있는 진료의 질과 효율성을 향상시킬 수 있을 것입니다. 적절한 환자가 적절한 시점에 적절한 치료를 받도록 보장하려 하든, 고위험 환자를 예측하려 하든, 혹은 단순히 병원 운영을 개선하려 하든 간에 RWD는 헬스케어의 미래입니다.

사람들은 흔히 "데이터는 새로운 석유다"라고 말합니다. 저는 이 비유에 전적으로 동의하지만 많은 이들이 이 원유를 먼저 적절히 정제하지도 않고 즉각 더 나은 의사결정을 이끄는 데 사용할 수 있다고 생각한다는 점을 덧붙이고 싶습니다. 정제 과정은 비용이 많이 들고 번거로우며 지루하지만 절대적으로 필수적입니다. 또한 이는 목표로 하는 최종 상태에 맞게 맞춤화된(tailored) 프로세스입니다. 제트 연료를 생산하든 링거(IV) 수액 백에 쓰이는 플라스틱을 만들든, 우리는 그에 맞추어 정제 공정을 구축해야 합니다. 제트 연료용 정제 시설을 갖추어 놓고 이를 다른 용도로 쓸 수 있다고 기대하는 것이 터무니없듯이, 모든 유스케이스를 위해 데이터를 단 한 번만 정제, 처리, 조화시키면 된다고 생각하는 것 또한 똑같이 어리석은 생각입니다.

여러분의 배경, 소속 기업, 또는 역할에 관계없이, 이 책이 헬스케어 실세계 데이터를 다룰 때 마주하는 도전 과제와 기회에 대해 더 깊은 통찰을 제공했기를 바랍니다. 무엇보다도 환자를 돕는다는 궁극적인 목표를 가지고 RWD를 진정으로 활용할 수 있도록, 적절한 인재, 프로세스, 기술을 갖춘 팀을 구축하는 데 있어 조직과 경영진을 도울 수 있는 논점과 구체적인 예시를 제공해 주었기를 바랍니다.