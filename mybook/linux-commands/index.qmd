---
title: "기초 리눅스 명령어"
author: "Bobby Iliev"
---

# `ls` 명령어

`ls` 명령어는 특정 디렉터리(기본값: 현재 작업 디렉터리) 내의 파일과 디렉터리 목록을 확인할 때 사용합니다.
기본적으로 파일과 디렉터리를 알파벳 오름차순으로 정렬하여 나열합니다.

### 예시:

1. 현재 작업 디렉터리 내의 파일 표시:

```{bash}
ls
```

2. 특정 디렉터리 내의 파일 및 디렉터리 표시:

```{bash}
ls {Directory_Path}
```

3. 숨김 파일을 포함한 모든 파일을 사람이 읽기 쉬운 크기 단위의 상세 형식으로 나열:

```{bash}
ls -lah
```

4. 수정 시간을 기준으로 정렬하여 최신 파일부터 나열:

```{bash}
ls -lt
```

5. 파일 크기를 기준으로 정렬하여 가장 큰 파일부터 나열:

```{bash}
ls -lhS
```

6. 현재 경로의 디렉터리만 표시:

```{bash}
ls -d */
```

7. 모든 텍스트 파일의 상세 정보 나열:

```{bash}
ls -lh *.txt
```

8. 하위 디렉터리의 모든 파일을 재귀적으로 나열:

```{bash}
ls -R
```

### 구문(Syntax):

```{bash}
ls [-OPTION] [DIRECTORY_PATH]
```

### 대화형 실습

이 대화형 튜토리얼에서는 `ls` 명령어를 사용하는 다양한 방법을 배울 수 있습니다.

[Tony의 ls 명령어 튜토리얼](https://devdojo.com/tnylea/ls-command)

### 추가 플래그 및 기능:

|**단축 플래그**|**긴 플래그**|**설명**|
|:---|:---|:---|
|`-l`|-|상세 형식(long format)으로 결과 표시|
|`-S`|-|파일 크기순으로 결과 정렬|
|`-t`|-|수정 시간순으로 결과 정렬|
|`-r`|`--reverse`|파일 및 디렉터리를 역순*(알파벳 내림차순)*으로 표시|
|`-a`|`--all`|점(`.`)으로 시작하는 숨김 파일을 포함한 모든 파일 표시|
|`-la`|-|숨김 파일을 포함한 파일 및 디렉터리를 상세 형식으로 표시|
|`-lh`|-|읽기 쉬운 크기 단위로 파일 및 디렉터리를 상세 형식으로 표시|
|`-A`|`--almost-all`|`-a`처럼 모두 표시하지만 `.`(현재 디렉터리) 및 `..`(상위 디렉터리)는 제외|
|`-d`|`--directory`|디렉터리 내부 파일 목록 대신 디렉터리 자체의 정보 표시(`-l`과 함께 사용하여 상세 정보 확인 가능)|
|`-F`|`--classify`|나열된 이름 끝에 유형 표시 기호를 추가(예: 디렉터리 이름 뒤에 `/` 추가)|
|`-h`|`--human-readable`|`-l`과 비슷하지만 파일 크기를 바이트 대신 사람이 읽기 쉬운 단위로 표시|
|`-i`|`--inode`|각 파일의 아이노드(inode) 번호 표시|
|`-R`|`--recursive`|하위 디렉터리를 재귀적으로 나열|
|`-1`|-|한 줄에 하나의 파일만 나열|
|`-c`|-|변경 시간(파일 메타데이터가 마지막으로 변경된 시간)순 정렬|
|`-u`|-|접근 시간(파일에 마지막으로 접근한 시간)순 정렬|
|`-X`|-|파일 확장자별 알파벳순 정렬|
|-|`--color=auto`|파일 유형을 구별할 수 있도록 출력을 색상화|
|`-g`|-|`-l`과 비슷하지만 소유자를 표시하지 않음|
|`-o`|-|`-l`과 비슷하지만 그룹을 표시하지 않음|
|`-C`|-|열(column) 단위로 항목 나열|
|`-s`|`--size`|각 파일의 할당된 크기 출력|
|-|`--group-directories-first`|파일보다 디렉터리를 먼저 나열|

### 파일 유형 표시 기호:

`-F` 또는 `--classify` 플래그를 사용할 때 `ls`는 파일 이름 뒤에 파일 유형을 나타내는 특수 문자를 추가합니다.

|**표시 기호**|**파일 유형**|**예시**|
|:---|:---|:---|
|`/`|디렉터리|`docs/`|
|`*`|실행 파일|`script.sh*`|
|`@`|심볼릭 링크|`link@`|
|`\|`|FIFO (이름 있는 파이프)|`pipe\|`|
|`=`|소켓|`socket=`|
|No indicator|일반 파일|`file.txt`|

**Example:**

```bash
ls -F
documents/  script.sh*  link@  data.txt  pipe|  socket=
```

### Red Hat 기반 시스템의 SELinux 지원:

SELinux를 사용하는 Red Hat 기반 배포판(RHEL, CentOS, Fedora, Rocky Linux, AlmaLinux)에서 `ls` 명령어는 SELinux 보안 컨텍스트 정보를 표시하는 추가 옵션을 제공합니다.

|**단축 플래그**|**긴 플래그**|**설명**|
|:---|:---|:---|
|`-Z`|`--context`|파일 및 디렉터리의 SELinux 보안 컨텍스트 표시|
|`-lZ`|-|SELinux 보안 컨텍스트를 포함하여 상세 형식으로 표시|

**출력 예시:**

```bash
ls -Z
unconfined_u:object_r:user_home_t:s0 file.txt
unconfined_u:object_r:user_home_t:s0 directory
```

```bash
ls -lZ
-rw-rw-r--. 1 user user unconfined_u:object_r:user_home_t:s0 1234 Jan 15 10:30 file.txt
drwxrwxr-x. 2 user user unconfined_u:object_r:user_home_t:s0 4096 Jan 15 10:25 directory
```

SELinux 컨텍스트 형식: `사용자:역할:유형:수준`

**참고:** `-Z` 옵션은 SELinux가 활성화된 시스템에서만 작동합니다. SELinux가 비활성화된 시스템에서는 이 옵션을 사용할 수 없거나 추가 정보가 표시되지 않을 수 있습니다.

### 상세 출력(Long Format) 이해하기:

`ls -l`을 사용할 때 각 줄은 파일이나 디렉터리에 대한 상세 정보를 표시합니다.

```bash
-rw-r--r-- 1 user group 1234 Jan 15 10:30 file.txt
```

**열별 상세 설명:**

1. **파일 유형 및 권한** (`-rw-r--r--`):
   - 첫 번째 문자: 파일 유형 (`-` = 일반 파일, `d` = 디렉터리, `l` = 심볼릭 링크, `b` = 블록 장치, `c` = 문자 장치, `p` = 파이프, `s` = 소켓)
   - 다음 9개 문자: 3개 그룹(소유자, 그룹, 기타 사용자)에 대한 권한
     - `r` = 읽기, `w` = 쓰기, `x` = 실행, `-` = 권한 없음

2. **링크 수** (`1`): 파일에 대한 하드 링크 수

3. **소유자** (`user`): 파일 소유자의 사용자 이름

4. **그룹** (`group`): 파일을 소유한 그룹 이름

5. **크기** (`1234`): 바이트 단위 파일 크기 (`1.2K`, `3.4M`와 같이 사람이 읽기 쉬운 형식을 위해 `-h` 플래그 사용)

6. **수정 시간** (`Jan 15 10:30`): 파일이 마지막으로 수정된 날짜 및 시간

7. **파일 이름** (`file.txt`): 파일 또는 디렉터리의 이름

**사람이 읽기 쉬운 크기 단위 예시:**

```bash
ls -lh
drwxr-xr-x 2 user group 4.0K Jan 15 10:25 documents
-rwxr-xr-x 1 user group 2.3M Jan 14 09:15 script.sh
-rw-r--r-- 1 user group  15K Jan 16 14:30 data.csv
```

### 와일드카드 및 패턴 사용하기:

`ls` 명령어는 패턴 일치를 위한 다양한 와일드카드를 지원합니다.

|**와일드카드**|**설명**|**예시**|**매칭 대상**|
|:---|:---|:---|:---|
|`*`|임의의 개수의 문자와 매칭|`ls *.txt`|`.txt`로 끝나는 모든 파일|
|`?`|정확히 한 문자와 매칭|`ls file?.txt`|`file1.txt`, `file2.txt` (단, `file10.txt`는 제외)|
|`[]`|대괄호 안의 문자 중 하나와 매칭|`ls file[123].txt`|`file1.txt`, `file2.txt`, `file3.txt` 매칭|
|`[!]`|대괄호 안에 없는 문자와 매칭|`ls file[!3].txt`|`file1.txt`, `file2.txt` (단, `file3.txt`는 제외)|
|`{}`|쉼표로 구분된 패턴 중 하나와 매칭|`ls *.{jpg,png,gif}`|해당 확장자를 가진 모든 이미지 파일|

**예시:**

```bash
# List all PDF and DOCX files
ls *.{pdf,docx}

# List files starting with 'test' followed by a single digit
ls test?.log

# List all files starting with uppercase letters
ls [A-Z]*

# List all hidden configuration files
ls .config*
```

### Red Hat 기반 시스템의 SELinux 지원:

SELinux를 사용하는 Red Hat 기반 배포판(RHEL, CentOS, Fedora, Rocky Linux, AlmaLinux)에서 `ls` 명령어는 SELinux 보안 컨텍스트 정보를 표시하는 추가 옵션을 제공합니다.

|**단축 플래그**|**긴 플래그**|**설명**|
|:---|:---|:---|
|`-Z`|`--context`|파일 및 디렉터리의 SELinux 보안 컨텍스트 표시|
|`-lZ`|-|SELinux 보안 컨텍스트를 포함하여 상세 형식으로 표시|

**출력 예시:**

```bash
ls -Z
unconfined_u:object_r:user_home_t:s0 file.txt
unconfined_u:object_r:user_home_t:s0 directory
```

```bash
ls -lZ
-rw-rw-r--. 1 user user unconfined_u:object_r:user_home_t:s0 1234 Jan 15 10:30 file.txt
drwxrwxr-x. 2 user user unconfined_u:object_r:user_home_t:s0 4096 Jan 15 10:25 directory
```

SELinux 컨텍스트 형식: `사용자:역할:유형:수준`

**참고:** `-Z` 옵션은 SELinux가 활성화된 시스템에서만 작동합니다. SELinux가 비활성화된 시스템에서는 이 옵션을 사용할 수 없거나 추가 정보가 표시되지 않을 수 있습니다.

### 영구 옵션 설정하기:

Linux에서 `alias` 명령어를 사용하면 명령어 동작을 쉽게 커스터마이징할 수 있습니다. 변경 사항을 영구적으로 적용하려면 다음 단계를 따르세요.

1. **별칭(Alias) 생성**: 원하는 옵션으로 별칭을 정의합니다. 예를 들어 `ls` 명령어를 확장하려면 다음과 같이 설정합니다.

    ```bash
    alias ls="ls --color=auto -lh"
    ```

2. **영구 적용**: 이 별칭은 현재 세션에서만 유효합니다. 영구적으로 유지하려면 셸의 설정 파일에 별칭을 추가하세요.

    - **Bash**: `~/.bashrc` 파일 끝에 별칭을 추가합니다.

        ```bash
        echo 'alias ls="ls --color=auto -lh"' >> ~/.bashrc
        source ~/.bashrc
        ```

3. **확인**: 새 터미널 세션을 열면 `ls` 명령어 설정대로 파일이 표시됩니다.

### 성능 팁:

**1. 대용량 디렉터리에서 재귀 목록 나열 피하기:**

   - 하위 디렉터리와 파일이 많은 디렉터리에서 `-R` 플래그는 속도가 느릴 수 있습니다.
   - 더 디테일한 제어를 위해 `find` 명령어를 사용하는 것을 고려하세요: `find /path -type f`

**2. 스크립트에서 색상 출력 비활성화:**

   - 성능을 향상시키기 위해 출력을 파이프하거나 스크립트 내에서는 `--color=never`를 사용하세요.
   - 예시: `ls --color=never | grep pattern`

**3. 대용량 디렉터리의 출력 제한:**

   - `head`와 조합하여 처음 몇 개의 항목만 확인하세요: `ls -1 | head -n 20`
   - 또는 파일 목록을 출력하지 않고 개수만 세려면 `ls -1 | wc -l`을 사용하세요.

**4. 가능한 경우 와일드카드 대신 구체적인 경로 사용:**

   - 정확한 파일 이름을 알고 있는 경우 `ls /var/log/sys*`보다 `ls /var/log/syslog`가 더 빠릅니다.

### 주요 사용 사례:

**1. 디렉터리에서 가장 큰 파일 찾기:**

```bash
ls -lhS | head -n 10
```

**2. 디렉터리만 나열:**

```bash
ls -d */
```

또는 상세 정보 포함:

```bash
ls -ld */
```

**3. 디렉터리 내 파일 개수 세기:**

```bash
ls -1 | wc -l
```

**4. 최근 24시간 동안 수정된 파일 나열:**

```bash
ls -lt | head
```

**5. 확장자순으로 정렬된 파일 표시:**

```bash
ls -lX
```

**6. 아이노드 번호와 함께 파일 나열(디버깅 시 유용):**

```bash
ls -li
```

**7. 디렉터리 내용을 한 줄에 하나씩 표시(스크립팅 시 유용):**

```bash
ls -1
```

**8. 여러 정렬 옵션 조합(크기 + 역순):**

```bash
ls -lhSr
```

# `cd` 명령어

`cd` 명령어는 현재 작업 디렉터리, 즉 현재 사용자가 작업 중인 디렉터리를 변경하는 데 사용됩니다. `cd`는 **change directory(디렉터리 변경)**의 약자이며 Linux 터미널에서 가장 자주 사용되는 명령어 중 하나입니다.

`cd` 명령어는 시스템을 탐색할 때 `ls` 명령어(1장 참조)와 함께 사용하는 경우가 많습니다. 디렉터리 이름을 자동 완성하려면 `Tab` 키를 누르면 되며 현재 위치에서 이용 가능한 디렉터리 목록을 표시하려면 `Tab` 키를 두 번 누르면 됩니다.

### 구문(Syntax):

```
cd [OPTIONS] [directory]
```
### 기본 예시

1. **특정 디렉터리로 이동:**

```bash
cd /path/to/directory
```

2. **홈 디렉터리로 이동:**

```bash
cd ~
```

또는 간단히 다음과 같이 입력할 수 있습니다.

```bash
cd
```

3. **이전 디렉터리로 이동:**

```bash
cd -
```

이 명령어는 이전 디렉터리의 절대 경로도 출력합니다.

4. **시스템의 루트 디렉터리로 이동:**

```bash
cd /
```

5. **한 단계 상위 디렉터리로 이동:**

```bash
cd ..
```

6. **여러 단계 상위 디렉터리로 이동:**

```bash
cd ../../..
```

이 예제는 세 단계 위로 이동합니다.

### 실용적인 예시

**상대 경로 사용:**

```bash
cd Documents/Projects/MyApp
```

**절대 경로 사용:**

```bash
cd /usr/local/bin
```

**홈 디렉터리 단축 표현과 함께 사용:**

```bash
cd ~/Downloads
```

**이름에 공백이 포함된 디렉터리로 이동:**

```bash
cd "My Documents"
```

또는

```bash
cd My\ Documents
```

**두 디렉터리 사이를 전환:**

```bash
cd /var/log
cd /etc
cd -  # /var/log로 돌아감
cd -  # /etc로 돌아감
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-L` | - | 심볼릭 링크를 따라갑니다. (기본 동작) `cd` 명령어는 심볼릭 링크를 따라가고 작업 디렉터리를 대상 위치로 변경합니다. |
| `-P` | - | 심볼릭 링크를 따라가지 않고 실제 디렉터리 구조를 사용합니다. 심볼릭 링크 경로가 아닌 실제 경로를 표시합니다. |

**심볼릭 링크에서 `-L`과 `-P`를 사용하는 예시:**

```bash
# /var/www가 /home/user/web을 가리키는 심볼릭 링크라고 가정
cd -L /var/www      # 작업 디렉터리가 /var/www로 표시됨
pwd                 # 출력: /var/www


cd -P /var/www      # 작업 디렉터리가 실제 경로로 확인됨
pwd                 # 출력: /home/user/web
```

### 일반적인 오류 및 문제 해결

**권한 거부:**

```bash
cd /root
# bash: cd: /root: Permission denied
```

해결 방법: 디렉터리에 접근할 수 있는 적절한 권한이 필요합니다. 필요한 경우 `sudo` 사용을 고려하십시오.

**파일이나 디렉터리가 없음:**

```bash
cd /invalid/path
# bash: cd: /invalid/path: No such file or directory
```

해결 방법: `ls`를 사용하여 경로가 존재하는지 확인하거나 오타가 있는지 확인하십시오. 경로는 대소문자를 구분한다는 점을 기억하십시오.

**디렉터리가 아님:**

```bash
cd /etc/passwd
# bash: cd: /etc/passwd: Not a directory
```

해결 방법: 파일이 아닌 디렉터리로 이동하고 있는지 확인하십시오.

### 중요 참고 사항

- **대소문자 구분:** Linux 파일 시스템은 대소문자를 구분합니다. `cd Documents`와 `cd documents`는 서로 다릅니다.
- **특수 문자:** 공백이나 특수 문자가 포함된 디렉터리 이름은 따옴표로 묶거나 이스케이프해야 합니다.
- **`cd` 명령어는 셸 내장 명령어:** 외부 프로그램이 아니므로 셸의 현재 디렉터리를 변경할 수 있습니다.
- **성공 시 출력 없음:** 기본적으로 `cd`는 성공적으로 실행되어도 아무것도 출력하지 않습니다. 단, `cd -`는 새 경로를 출력합니다.

# `cat` 명령어

***

`cat` 명령어를 사용하면 하나 또는 여러 파일을 생성하거나, 파일의 내용을 확인하거나, 여러 파일을 연결하고 그 출력을 터미널이나 파일로 리디렉션할 수 있습니다.

`cat`은 `concatenate`(연결하다)의 앞부분에서 유래한 이름입니다. Linux 터미널에서 가장 자주 사용되는 명령어 중 하나입니다.

### 사용 예시

1. 터미널에 파일 내용을 표시합니다.

```bash
cat <specified_file_name>
```

2. 터미널에 여러 파일의 내용을 표시합니다.

```bash
cat file1 file2 ...
```

3. `cat` 명령어로 파일을 생성합니다.

```bash
cat > file_name
```

4. 현재 디렉터리에서 같은 파일 형식을 가진 모든 파일의 내용을 표시합니다.

```bash
cat *.<filetype>
```

5. 현재 디렉터리에 있는 모든 파일의 내용을 표시합니다.

```bash
cat *
```

6. 지정한 파일의 출력을 다른 파일에 저장합니다.

```bash
cat old_file_name > new_file_name
```

7. `more` 및 `less` 옵션과 함께 `cat` 명령어를 사용합니다.

```bash
cat filename | more
cat filename | less
```

8. `file1`의 내용을 `file2`에 추가합니다.

```bash
cat file1 >> file2
```

9. 두 파일을 새 파일로 연결합니다.

```bash
cat file1_name file2_name merge_file_name
```

10. 일부 `cat` 구현에서는 `-n` 옵션을 사용하여 행 번호를 표시할 수 있습니다.

```
cat -n file1_name file2_name > new_numbered_file_name
```

### 구문(Syntax):

```
cat [OPTION] [FILE]...
```

### 추가 플래그 및 기능:
| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-A` | `--show-all` | `-vET`와 동일합니다. |
| `-b` | `--number-nonblank` | 비어 있지 않은 출력 줄에 번호를 매깁니다. `-n`보다 우선합니다. |
| `-e` | - | `-vE`와 동일합니다. |
| `-T` | - | `cat` 명령어로 연 파일에서 탭으로 구분된 줄을 표시합니다. |
| `-E` | - | 각 줄 끝에 `$`를 표시합니다. |
| `-n` | `--number` | 모든 출력 줄에 번호를 매깁니다. |
| `-s` | `--squeeze-blank` | 반복되는 빈 출력 줄을 하나로 줄입니다. |
| `-u` | - | 무시됩니다. |
| `-v` | `--show-nonprinting` | 줄바꿈 문자(LFD)와 탭을 제외한 비출력 문자를 `^` 및 `M-` 표기법으로 표시합니다. |
| - | `--help` | 이 도움말을 표시하고 종료합니다. |
| - | `--version` | 버전 정보를 출력하고 종료합니다. |

# `tac` 명령어

`tac`은 파일을 마지막 줄부터 시작하여 한 줄씩 볼 수 있게 해주는 Linux 명령어입니다. (`tac`은 각 줄의 내용을 뒤집는 것이 아니라 줄이 표시되는 순서만 뒤집습니다.) 이름은 `cat`을 거꾸로 한 것에서 유래했습니다.

### 사용 예시:


1. 터미널에 파일 내용 출력:

```
tac <specified_file_name>
```

2. 구분자를 뒤가 아닌 앞에 붙여서 출력:

```
tac -b concat_file_name tac_example_file_name
```

3. 구분자를 정규 표현식으로 해석하여 처리:

```
tac -r concat_file_name tac_example_file_name
```

4. 줄바꿈 대신 지정한 문자열을 구분자로 사용:

```
tac -s concat_file_name tac_example_file_name
```

5. 도움말을 출력하고 종료:

```
tac --help
```

6. 버전 정보를 출력하고 종료:

```
tac --version
```

### 구문(Syntax):

```
tac [OPTION]... [FILE]...
```
### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-b` | `--before` | 구분자를 뒤가 아닌 앞에 붙입니다. |
| `-r` | `--regex` | 구분자를 정규 표현식으로 해석합니다. |
| `-s` | `--separator=STRING` | 줄바꿈 대신 `STRING`을 구분자로 사용합니다. |
| - | `--help` | 이 도움말을 표시한 후 종료합니다. |
| - | `--version` | 버전 정보를 출력한 후 종료합니다. |

# `head` 명령어

`head` 명령어는 파일의 처음 10개 행을 출력합니다.

예시:

```bash
head filename.txt
```

구문:

```bash
head [OPTION] [FILENAME]
```

### 특정 행 수 가져오기

`-n` 옵션과 표시할 행 수를 함께 사용합니다. 행 수는 정수여야 합니다.

예시:

```bash
head -n 10 foo.txt
```

이 명령어는 `foo.txt` 파일의 처음 10개 행을 표시합니다.

구문:

```bash
head -n <number> foo.txt
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-c` | `--bytes=[-]NUM` | 각 파일의 처음 `NUM`바이트를 출력합니다.<br>앞에 `-`를 붙이면 각 파일의 마지막 `NUM`바이트를 제외한 모든 내용을 출력합니다. |
| `-n` | `--lines=[-]NUM` | 처음 10개 행 대신 처음 `NUM`개 행을 출력합니다.<br>앞에 `-`를 붙이면 각 파일의 마지막 `NUM`개 행을 제외한 모든 행을 출력합니다. |
| `-q` | `--quiet`, `--silent` | 파일 이름을 표시하는 헤더를 출력하지 않습니다. |
| `-v` | `--verbose` | 파일 이름을 표시하는 헤더를 항상 출력합니다. |
| `-z` | `--zero-terminated` | 행 구분자로 줄바꿈 대신 NUL 문자를 사용합니다. |
| - | `--help` | 이 도움말을 표시한 후 종료합니다. |
| - | `--version` | 버전 정보를 출력한 후 종료합니다. |

# `tail` 명령어

`tail` 명령어는 파일의 마지막 10개 행을 출력합니다.

예시:

```bash
tail filename.txt
```

구문:

```bash
tail [OPTION] [FILENAME]
```

## 일반적인 사용 사례

### 특정 줄 수 가져오기

표시할 줄 수를 나타내는 정수와 함께 `-n` 옵션을 사용합니다.

Example:

```
tail -n 10 foo.txt
```
이 명령어는 `foo.txt` 파일의 마지막 10줄을 표시합니다.

### 파일에 새로 추가되는 내용을 실시간으로 모니터링하기

`tail`이 확인 중인 파일에 새로 추가되는 줄을 출력하도록 설정할 수 있습니다. 따라서 파일에 새 줄이 기록되면 해당 줄이 출력 결과에 즉시 표시됩니다. 이를 위해 `--follow` 또는 `-f` 옵션을 사용할 수 있습니다. 이 기능은 로그 파일을 모니터링할 때 특히 유용합니다.

예제:

```
tail -f foo.txt
```

실시간 추적을 중단하려면 `Ctrl+C`를 누르세요.

### 로그 모니터링을 위한 옵션 조합

```
tail -n 100 -f app.log      # Show last 100 lines, then follow
tail -f -s 2 slow.log       # Follow with 2-second refresh interval
```

### 여러 파일 동시 실시간 모니터링

```
tail -f /var/log/nginx/access.log /var/log/nginx/error.log
```

### 특정 바이트 범위 출력

```
tail -c 100 file.txt        # Last 100 bytes
tail -c +50 file.txt        # From byte 50 to end
```

### 로그 로테이션 후에도 파일 추적 유지

```
tail -F /var/log/app.log
```

`-F` 옵션은 logrotate 등으로 관리되는 로그 파일을 끊김 없이 추적할 때 유용합니다.

### 헤더 행 건너뛰기

```
tail -n +2 data.csv         # Start from line 2 (skip header)
tail -n +11 file.txt        # Start from line 11 onwards
```

### 여러 파일 출력 시 헤더 숨기기(저소음 모드)

```
tail -q -n 5 *.log          # No filename headers
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-c` | `--bytes=[+]NUM` | 마지막 `NUM`바이트를 출력합니다.<br>또는 `-c +NUM`을 사용하여 각 파일의 `NUM`번째 바이트부터 출력합니다. |
| `-f` | `--follow[={name\|descriptor}]` | 파일이 커질 때 추가된 데이터를 출력합니다.<br>옵션 인수를 생략하면 `descriptor`가 사용됩니다. |
| `-F` | - | `--follow=name --retry`와 동일합니다. |
| `-n` | `--lines=[+]NUM` | 마지막 10줄 대신 마지막 `NUM`줄을 출력합니다.<br>또는 `-n +NUM`을 사용하여 `NUM`번째 줄부터 출력합니다. |
| - | `--max-unchanged-stats=N` | `--follow=name`과 함께 사용할 때, `N`번(기본값 5) 반복한 후에도 크기가 변경되지 않은 파일을 다시 엽니다. 파일이 삭제되었거나 이름이 변경되었는지 확인하기 위한 기능이며 로그 순환 파일에서 일반적으로 사용됩니다. `inotify`를 사용할 때는 거의 유용하지 않습니다. |
| - | `--pid=PID` | `-f`와 함께 사용하며 프로세스 ID `PID`가 종료되면 `tail`도 종료합니다. |
| `-q` | `--quiet`, `--silent` | 파일 이름을 표시하는 헤더를 출력하지 않습니다. |
| - | `--retry` | 파일에 접근할 수 없더라도 파일을 다시 열기 위해 계속 시도합니다. |
| `-s` | `--sleep-interval=N` | `-f`와 함께 사용할 때 반복 사이에 약 `N`초 동안 대기합니다(기본값 1.0초). `inotify` 및 `--pid=P`와 함께 사용하면 최소 `N`초마다 프로세스 `P`를 확인합니다. |
| `-v` | `--verbose` | 파일 이름을 표시하는 헤더를 항상 출력합니다. |
| `-z` | `--zero-terminated` | 줄 구분자로 줄바꿈 문자가 아닌 NUL 문자를 사용합니다. |
| - | `--help` | 이 도움말을 표시하고 종료합니다. |
| - | `--version` | 버전 정보를 출력하고 종료합니다. |

## 팁

- 운영 환경에서 로그를 모니터링할 때는 `-f` 대신 `-F`를 사용하세요.
- 필터링된 모니터링을 수행하려면 `grep`, `awk` 또는 `sed`와 함께 사용하세요.
- 대용량 파일을 확인할 때 `tail`은 편집기에서 파일을 여는 것보다 훨씬 빠릅니다.
- 특정 줄부터 시작하려면 `-n +N`을 사용하세요. 헤더를 건너뛸 때 유용합니다.
# `pwd` 명령어

`pwd`는 **Print Working Directory(현재 작업 디렉터리 출력)**의 약자입니다. 루트 디렉터리부터 시작하는 현재 작업 디렉터리의 경로를 출력합니다.

예시:

```bash
pwd
```

출력에는 현재 디렉터리가 표시됩니다.

```text
/home/your_user/some_directory
```

구문:

```bash
pwd [OPTION]
```

팁:

`$PWD` 변수를 출력하여 현재 작업 디렉터리를 확인할 수도 있습니다.

```bash
echo $PWD
```

출력 결과는 `pwd` 명령어의 결과와 동일합니다.

### 옵션

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-L` | `--logical` | 환경 변수 `$PWD`에 현재 디렉터리의 절대 경로가 저장되어 있고 `.` 또는 `..` 구성 요소가 없다면, 심볼릭 링크가 포함되어 있더라도 해당 값을 출력합니다. 그렇지 않으면 기본 동작인 `-P` 방식으로 처리합니다. |
| `-P` | `--physical` | 현재 디렉터리의 경로를 완전히 확인하여 출력합니다. 경로의 모든 구성 요소는 심볼릭 링크가 아닌 실제 디렉터리 이름이어야 합니다. |
| - | `--help` | 도움말 메시지를 표시한 후 종료합니다. |
| - | `--version` | 버전 정보를 표시한 후 종료합니다. |

기본적으로 `pwd`는 `-L` 옵션이 지정된 것처럼 동작합니다.

# `touch` 명령어

`touch` 명령어는 파일의 타임스탬프를 변경합니다. 지정한 파일이 존재하지 않으면 해당 이름으로 빈 파일을 생성합니다.

### 구문

```bash
touch [OPTION]... FILE...
```

### 옵션

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-a` | - | 접근 시간만 변경합니다. |
| `-c` | `--no-create` | 파일을 생성하지 않습니다. |
| `-d STRING` | `--date=STRING` | `STRING`을 해석하여 현재 시간 대신 사용합니다. |
| `-f` | - | 무시되는 옵션입니다. 아무 작업도 수행하지 않지만 BSD 버전 `touch` 명령어와의 호환성을 위해 허용됩니다. |
| `-h` | `--no-dereference` | 참조 대상 파일이 아닌 각 심볼릭 링크 자체에 적용합니다. 심볼릭 링크의 타임스탬프를 변경할 수 있는 시스템에서만 유용합니다. 이 옵션은 `-c`를 포함하므로 파일이 존재하지 않아도 아무 파일도 생성되지 않습니다. |
| `-m` | - | 수정 시간만 변경합니다. |
| `-r=FILE` | `--reference=FILE` | 현재 시간 대신 지정한 파일의 시간을 사용합니다. |
| `-t STAMP` | - | 현재 시간 대신 숫자로 된 시간 `STAMP`를 사용합니다. `STAMP`의 형식은 `[[CC]YY]MMDDhhmm[.ss]`입니다. |
| - | `--time=WORD` | 설정할 시간의 유형을 지정하는 다른 방법입니다. 예를 들어 `access`, `modification`, `change` 등을 지정할 수 있습니다. `WORD`가 `access` 또는 `atime`이면 `-a`와 같고, `modify` 또는 `mtime`이면 `-m`과 같습니다. |
| - | `--help` | 도움말을 표시하고 종료합니다. |
| - | `--version` | 버전 정보를 표시하고 종료합니다. |

### 예제

1. `file.txt`가 존재하면 모든 타임스탬프를 현재 시스템 시간으로 설정합니다. `file.txt`가 존재하지 않으면 해당 이름으로 빈 파일을 생성합니다.


```
touch file.txt
```
2. **`file.txt`가 존재하면** 해당 파일의 시간을 현재 시스템 시간으로 설정합니다. 파일이 존재하지 않으면 아무 작업도 수행하지 않습니다.

```bash
touch -c file.txt
```

3. **`file.txt`의 접근 시간**을 변경합니다. **수정 시간**은 변경하지 않습니다. **상태 변경 시간**은 현재 시스템 시간으로 설정됩니다. `file.txt`가 존재하지 않으면 파일이 생성됩니다.

```bash
touch -a file.txt
```

4. **`symboliclink` 파일의 시간**을 변경합니다. 심볼릭 링크인 경우 참조된 파일의 시간이 아니라 심볼릭 링크 자체의 시간을 변경합니다.

```bash
touch -h symboliclink
```

5. **`file-b.txt`의 접근 시간과 수정 시간**을 `file-a.txt`의 시간과 동일하게 변경합니다. 상태 변경 시간은 현재 시스템 시간으로 설정됩니다. `file-b.txt`가 존재하지 않으면 생성되지 않습니다. 이 경우 `file-a.txt`는 반드시 이미 존재해야 합니다.

```bash
touch -cr file-a.txt file-b.txt
```

6. **`file.txt`의 접근 시간과 수정 시간**을 현재 연도의 **2월 1일**로 설정합니다. 상태 변경 시간은 현재 시스템 시간으로 설정됩니다.

```bash
touch -d "1 Feb" file.txt
```

# `cal` 명령어

`cal` 명령어는 터미널에 형식이 지정된 달력을 표시합니다. 옵션을 지정하지 않으면 현재 월을 표시하고 오늘 날짜를 강조 표시합니다.

### 구문(Syntax)

```bash
cal [general options] [-jy] [[month] year]
```

### 옵션

| **옵션** | **설명** |
|:--|:--|
| `-h` | 오늘 날짜를 강조 표시하지 않습니다. |
| `-m month` | 표시할 월을 지정합니다. 월 지정자는 전체 월 이름(예: `February`), 최소 세 글자로 된 월 약어(예: `Feb`) 또는 숫자(예: `2`)로 지정할 수 있습니다. 숫자 뒤에 `f` 또는 `p`를 붙이면 각각 다음 연도 또는 이전 연도의 해당 월을 표시합니다. 예를 들어 `-m 2f`는 다음 연도의 2월을 표시합니다. |
| `-y year` | 표시할 연도를 지정합니다. 예를 들어 `-y 1970`은 1970년 전체 달력을 표시합니다. |
| `-3` | 이전 달, 현재 달 및 다음 달을 표시합니다. |
| `-1` | 현재 달만 표시합니다. 기본값입니다. |
| `-A num` | 이미 지정된 월 이후에 해당하는 `num`개월을 표시합니다. 예를 들어 `-3 -A 3`은 이전 달, 현재 달 및 현재 달 이후 4개월을 표시하고, `-y 1970 -A 2`는 1970년 전체와 1971년의 첫 두 달을 표시합니다. |
| `-B num` | 이미 지정된 월 이전에 해당하는 `num`개월을 표시합니다. 예를 들어 `-3 -B 2`는 이전 3개월, 현재 달 및 다음 달을 표시합니다. |
| `-d YYYY-MM` | 현재 월이 `YYYY`년의 `MM`번째 월인 것처럼 동작합니다. |

### 예시

1. 오늘 날짜를 강조 표시하면서 이번 달의 달력을 표시합니다.

```bash
cal
```

2. 이전 명령어와 동일하지만 오늘 날짜를 강조 표시하지 않습니다.

```bash
cal -h
```

3. 이전 달, 현재 달 및 다음 달을 표시합니다.

```bash
cal -3
```

4. 올해 전체 달력을 표시합니다.

```bash
cal -y
```

5. 2000년 전체 달력을 표시합니다.

```bash
cal -y 2000
```

6. 이전 명령어와 동일합니다.

```bash
cal 2000
```

7. 올해 12월의 달력을 표시합니다.

```bash
cal -m [December, Dec, or 12]
```

10. 2000년 12월의 달력을 표시합니다.

```bash
cal 12 2000
```

# `bc` 명령어

`bc` 명령어는 명령줄에서 수학 계산을 수행할 수 있는 기능을 제공합니다.

### 예시

1. 산술 연산:

```text
입력: $ echo "11+5" | bc
출력: 16
```

2. 증가 연산:

- `var++`: 후위 증가 연산자입니다. 먼저 변수의 값이 사용된 다음 변수가 증가합니다.
- `++var`: 전위 증가 연산자입니다. 먼저 변수가 증가한 다음 증가한 변수의 값이 결과로 저장됩니다.

```text
입력: $ echo "var=3;++var" | bc
출력: 4
```

3. 감소 연산:

- `var--`: 후위 감소 연산자입니다. 먼저 변수의 값이 사용된 다음 변수가 감소합니다.
- `--var`: 전위 감소 연산자입니다. 먼저 변수가 감소한 다음 감소한 변수의 값이 결과로 저장됩니다.

```text
입력: $ echo "var=3;--var" | bc
출력: 2
```

4. 대입:

- `var = value`: 변수에 값을 대입합니다.
- `var += value`: `var = var + value`와 유사합니다.
- `var -= value`: `var = var - value`와 유사합니다.
- `var *= value`: `var = var * value`와 유사합니다.
- `var /= value`: `var = var / value`와 유사합니다.
- `var ^= value`: `var = var ^ value`와 유사합니다.
- `var %= value`: `var = var % value`와 유사합니다.

```text
입력: $ echo "var=4;var" | bc
출력: 4
```

5. 비교 또는 관계 연산:

- 비교 결과가 참이면 `1`, 거짓이면 `0`을 반환합니다.
- `expr1<expr2`: `expr1`이 `expr2`보다 엄격하게 작으면 결과는 `1`입니다.
- `expr1<=expr2`: `expr1`이 `expr2`보다 작거나 같으면 결과는 `1`입니다.
- `expr1>expr2`: `expr1`이 `expr2`보다 엄격하게 크면 결과는 `1`입니다.
- `expr1>=expr2`: `expr1`이 `expr2`보다 크거나 같으면 결과는 `1`입니다.
- `expr1==expr2`: `expr1`과 `expr2`가 같으면 결과는 `1`입니다.
- `expr1!=expr2`: `expr1`과 `expr2`가 같지 않으면 결과는 `1`입니다.

```text
입력: $ echo "6<4" | bc
출력: 0
```

```text
입력: $ echo "2==2" | bc
출력: 1
```

6. 논리 또는 부울 연산:

- `expr1 && expr2`: 두 표현식이 모두 0이 아니면 결과는 `1`입니다.
- `expr1 || expr2`: 두 표현식 중 하나라도 0이 아니면 결과는 `1`입니다.
- `! expr`: `expr`이 0이면 결과는 `1`입니다.

```text
입력: $ echo "! 1" | bc
출력: 0


입력: $ echo "10 && 5" | bc
출력: 1
```

### 구문(Syntax)

```bash
bc [ -hlwsqv ] [long-options] [ file ... ]
```

### 추가 플래그 및 기능

*참고: 아래 목록은 모든 옵션을 포함한 전체 목록이 아닙니다.*

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-i` | `--interactive` | 대화형 모드를 강제로 활성화합니다. |
| `-l` | `--mathlib` | 미리 정의된 수학 함수를 사용합니다. |
| `-q` | `--quiet` | 헤더를 출력하지 않고 `bc`의 대화형 모드를 엽니다. |
| `-s` | `--standard` | 비표준 `bc` 구문을 오류로 처리합니다. |
| `-w` | `--warn` | 비표준 `bc` 구문이 사용되면 경고를 표시합니다. |

### 참고 사항

1. 스크립트에서 `bc`를 사용하면 그 기능을 더욱 유용하게 활용할 수 있습니다. 기본적인 산술 연산 외에도 증가/감소, 복잡한 계산, 논리 비교 등을 지원합니다.
2. `bc`의 플래그 중 두 개는 비표준 구문과 관련이 있습니다. 예를 들어 `100>50 | bc`를 평가하면 이상한 경고가 표시될 수 있습니다. `bc`에 대한 POSIX 문서에 따르면 관계 연산자는 `if`, `while` 또는 `for` 문 내부에서 사용할 때만 유효합니다.

# `df` 명령어

Linux/Unix에서 `df` 명령어는 디스크 사용량과 정보를 표시할 때 사용합니다. `df`는 “disk free(디스크 여유 공간)”의 약자입니다.

`df`는 각 파일 이름 인수가 포함된 파일 시스템에서 사용할 수 있는 디스크 공간의 양을 표시합니다. 파일 이름을 지정하지 않으면 현재 마운트된 모든 파일 시스템에서 사용할 수 있는 공간을 표시합니다.

### 구문

```bash
df [OPTION]... [FILE]...
```

### 옵션

| **단축 플래그** | **긴 플래그** | **설명** |
|:--|:--|:--|
| `-a` | `--all` | 의사 파일 시스템, 중복 파일 시스템 및 접근할 수 없는 파일 시스템을 포함합니다. |
| `-B` | `--block-size=SIZE` | 출력하기 전에 크기를 `SIZE` 단위로 조정합니다. 예를 들어 `-BM`은 크기를 1,048,576바이트 단위로 표시합니다. `SIZE` 형식은 아래를 참조하십시오. |
| `-h` | `--human-readable` | 1024의 거듭제곱 단위로 크기를 표시합니다. (예: `1023M`) |
| `-H` | `--si` | 1000의 거듭제곱 단위로 크기를 표시합니다. (예: `1.1G`) |
| `-i` | `--inodes` | 블록 사용량 대신 아이노드 정보를 나열합니다. |
| `-k` | - | `--block-size=1K`와 동일합니다. |
| `-l` | `--local` | 로컬 파일 시스템만 나열하도록 제한합니다. |
| - | `--no-sync` | 사용량 정보를 가져오기 전에 `sync`를 호출하지 않습니다. (기본값) |
| - | `--output[=FIELD_LIST]` | `FIELD_LIST`에 정의된 출력 형식을 사용합니다. `FIELD_LIST`를 생략하면 모든 필드를 출력합니다. |
| `-P` | `--portability` | `POSIX` 출력 형식을 사용합니다. |
| - | `--sync` | 사용량 정보를 가져오기 전에 `sync`를 호출합니다. |
| - | `--total` | 사용 가능한 공간에 중요하지 않은 모든 항목을 생략하고 총합을 표시합니다. |
| `-t` | `--type=TYPE` | 파일 시스템 유형이 `TYPE`인 파일 시스템만 나열합니다. |
| `-T` | `--print-type` | 파일 시스템 유형을 출력합니다. |
| `-x` | `--exclude-type=TYPE` | 파일 시스템 유형이 `TYPE`이 아닌 파일 시스템만 나열합니다. |
| `-v` | - | 무시됩니다. 호환성을 위해 포함되어 있습니다. |
| - | `--help` | 도움말 메시지를 표시한 후 종료합니다. |
| - | `--version` | 버전 정보를 출력한 후 종료합니다. |

### 예시

1. 사용 가능한 디스크 공간을 표시합니다.

**작업:**
--- 사용 가능한 디스크 공간과 디렉터리가 마운트된 위치를 출력합니다.

**세부 정보:**
--- 출력 값은 사람이 읽기 쉬운 형식이 아닙니다. (바이트 단위)

**명령어:**

```bash
df
```

2. 사용 가능한 디스크 공간을 사람이 읽기 쉬운 형식으로 표시합니다.

**작업:**
--- 사용 가능한 디스크 공간과 디렉터리가 마운트된 위치를 출력합니다.

**세부 정보:**
--- 출력 값은 사람이 읽기 쉬운 형식입니다. (GB/MB 단위)

**명령어:**

```bash
df -h
```

3. 특정 파일 시스템의 사용 가능한 디스크 공간을 표시합니다.

**작업:**
--- 사용 가능한 디스크 공간과 디렉터리가 마운트된 위치를 출력합니다.

**세부 정보:**
--- 선택한 파일 시스템에 대한 값만 출력합니다.

**명령어:**

```bash
df -hT file_system_name
```

4. 사용 가능한 아이노드를 표시합니다.

**작업:**
--- 모든 파일 시스템에서 사용 가능한 아이노드를 출력합니다.

**세부 정보:**
--- 출력 값은 사용 가능한 공간이 아니라 아이노드에 대한 정보입니다.

**명령어:**

```bash
df -i
```

5. 파일 시스템 유형을 표시합니다.

**작업:**
--- 파일 시스템 유형을 출력합니다.

**세부 정보:**
--- 모든 파일 시스템에 대한 값을 출력합니다.

**명령어:**

```bash
df -T
```

6. 출력에서 특정 파일 시스템 유형을 제외합니다.

**작업:**
--- 선택한 파일 시스템 유형을 제외한 정보를 출력합니다.

**세부 정보:**
--- 출력 값은 선택한 파일 시스템 유형을 제외한 모든 파일 시스템에 대한 정보입니다.

**명령어:**

```bash
df -x file_system_type
```

# `help` 명령어

`help` 명령어 displays information about builtin commands.
셸 내장 명령어에 대한 정보를 출력합니다.

`PATTERN`이 지정된 경우 `PATTERN`과 일치하는 모든 명령어에 대한 상세 도움말을 제공하며 지정되지 않은 경우 사용 가능한 도움말 항목 목록이 출력됩니다.

> **팁:** `help` 명령어는 Bash 셸 자체에 내장된 명령어(`cd`, `pwd`, `echo`, `read` 등)에 대해서만 작동합니다. `ls`, `grep`, `find`와 같은 독립형 프로그램에는 작동하지 않습니다. 그러한 프로그램의 도움말을 보려면 `man` 명령어(예: `man ls`)를 사용해야 합니다.

## 구문(Syntax)

```bash
$ help [-dms] [PATTERN ...]
```
## 옵션

| **옵션** | **설명** |
|:--|:--|
| `-d` | 각 주제에 대한 간단한 설명을 출력합니다. |
| `-m` | 의사 매뉴얼 페이지 형식으로 사용법을 표시합니다. |
| `-s` | 제공된 `PATTERN`과 일치하는 각 주제에 대해 간단한 사용법 요약만 출력합니다. |

## 사용 예시

1. `cd` 명령어에 대한 전체 정보를 확인합니다.

```bash
$ help cd
cd: cd [-L|[-P [-e]] [-@]] [dir]
    셸의 현재 작업 디렉터리를 변경합니다.


    현재 디렉터리를 DIR로 변경합니다. 기본 DIR은
    HOME 셸 변수의 값입니다.
...
(옵션 및 종료 상태에 대한 추가 세부 정보가 이어집니다.)
```

2. `pwd` 명령어에 대한 간단한 설명을 확인합니다.

```bash
$ help -d pwd
pwd: pwd [-LP]
    현재 작업 디렉터리의 이름을 출력합니다.
```

3. `cd` 명령어의 구문을 확인합니다.

```bash
$ help -s cd
cd [-L|[-P [-e]] [-@]] [dir]
```

# `factor` 명령어

`factor` 명령어는 지정한 각 정수 `NUMBER`의 소인수를 출력합니다. 명령줄에 숫자를 지정하지 않으면 표준 입력에서 숫자를 읽습니다.

## 구문(Syntax)

```bash
$ factor [NUMBER]...
```

또는:

```bash
$ factor OPTION
```

## 옵션

| **옵션** | **설명** |
|:--|:--|
| `--help` | 도움말 메시지를 표시한 후 종료합니다. |
| `--version` | 버전 정보를 출력한 후 종료합니다. |

## 예시

1. 소수의 소인수를 출력합니다.

```bash
$ factor 50
```

2. 합성수의 소인수를 출력합니다.

```bash
$ factor 75
```

# `uname` 명령어

`uname` 명령어는 시스템 정보를 출력할 때 사용하며 기본적으로 커널 이름을 출력합니다.

## 구문

```bash
$ uname [OPTION]
```

## 예시

1. 모든 시스템 정보를 출력합니다.

```bash
$ uname -a
```

2. 커널 버전을 출력합니다.

```bash
$ uname -v
```

## 옵션

| **단축 플래그** | **긴 플래그** | **설명** |
|:-|:-|:--|
| `-a` | `--all` | 알 수 없는 경우 프로세서 및 하드웨어 플랫폼 정보를 생략하고 모든 정보를 출력합니다. |
| `-s` | `--kernel-name` | 커널 이름을 출력합니다. |
| `-n` | `--nodename` | 네트워크 노드 호스트 이름을 출력합니다. |
| `-r` | `--kernel-release` | 커널 릴리스를 출력합니다. |
| `-v` | `--kernel-version` | 커널 버전을 출력합니다. |
| `-m` | `--machine` | 시스템 하드웨어 이름을 출력합니다. |
| `-p` | `--processor` | 프로세서 유형을 출력합니다. (이식성이 없습니다.) |
| `-i` | `--hardware-platform` | 하드웨어 플랫폼을 출력합니다. (이식성이 없습니다.) |
| `-o` | `--operating-system` | 운영 체제를 출력합니다. |

# `mkdir` 명령어

`mkdir` 명령어는 Linux/Unix 시스템에서 디렉터리(폴더)를 생성할 때 사용합니다. 가장 기본적인 파일 시스템 명령어 중 하나이며 단일 디렉터리, 여러 디렉터리 및 중첩된 디렉터리 구조를 생성하기 위한 다양한 옵션을 제공합니다.

## 구문(Syntax)

```bash
mkdir [OPTIONS] DIRECTORY [DIRECTORY ...]
```

## 주요 기능

- **단일 디렉터리 생성**: 개별 디렉터리 생성
- **다중 디렉터리 생성**: 여러 디렉터리를 한 번에 생성
- **중첩 디렉터리 생성**: 상위 디렉터리를 자동으로 함께 생성
- **권한 설정**: 디렉터리 생성 시 권한 지정
- **상세 출력**: 생성 진행 상황 표시
- **SELinux 지원**: 보안 컨텍스트 설정

## 기본 사용법

### 단일 디렉터리 생성

```bash
# Create a directory in current location
mkdir mydir

# Create directory with absolute path
mkdir /home/user/documents

# Create directory in home directory
mkdir ~/projects
```

### 다중 디렉터리 생성

```bash
# Create multiple directories at once
mkdir dir1 dir2 dir3

# Create directories with different paths
mkdir ~/documents ~/downloads ~/pictures

# Create numbered directories
mkdir project{1,2,3,4,5}

# Create directories with ranges
mkdir folder{01..10}
```

## 고급 사용법

### 중첩 디렉터리 생성 (계층 구조)

```bash
# Create nested directory structure (creates parent directories)
mkdir -p projects/web/frontend/src

# Create complex directory structure
mkdir -p company/{departments/{hr,finance,it},projects/{web,mobile,desktop}}

# Create directory structure with absolute path
mkdir -p /opt/myapp/{bin,config,logs,data}
```

### 생성 시 디렉터리 권한 지정

```bash
# Create directory with specific permissions (755)
mkdir -m 755 public_dir

# Create directory with full permissions for owner only (700)
mkdir -m 700 private_dir

# Create directory with read/write for owner, read for group and others (644)
mkdir -m 644 shared_dir

# Create directory with full access for everyone (777)
mkdir -m 777 temp_dir

# Using symbolic notation
mkdir -m u=rwx,g=rx,o=rx public_folder
```

### 상세 출력 모드

```bash
# Show what directories are being created
mkdir -v newdir

# Verbose with multiple directories
mkdir -v dir1 dir2 dir3

# Verbose with nested structure
mkdir -pv projects/{frontend/{src,dist},backend/{api,database}}
```

## 실무 활용 예제

### 프로젝트 디렉터리 구조 생성

```bash
# Create a typical web project structure
mkdir -p mywebsite/{css,js,images,includes,admin}

# Create a software project structure
mkdir -p myproject/{src/{main,test},docs,build,config}

# Create a backup directory structure
mkdir -p backups/{daily,weekly,monthly}/{system,database,files}
```

### 시스템 관리 작업

```bash
# Create log directories for an application
sudo mkdir -p /var/log/myapp/{error,access,debug}

# Create configuration directories
sudo mkdir -p /etc/myapp/{conf.d,ssl,keys}

# Create data directories with proper permissions
sudo mkdir -p /var/lib/myapp/data
sudo mkdir -m 750 /var/lib/myapp/secure
```

### 사용자 환경 구성

```bash
# Set up user development environment
mkdir -p ~/development/{projects,tools,scripts}
mkdir -p ~/development/projects/{personal,work,opensource}

# Create organization directories
mkdir -p ~/documents/{work,personal,finance,education}
mkdir -p ~/documents/work/{reports,presentations,spreadsheets}
```

## 특수 문자 및 공백 처리

### 공백이 포함된 디렉터리

```bash
# Create directory with spaces (use quotes)
mkdir "My Documents"
mkdir 'Project Files'

# Create multiple directories with spaces
mkdir "Dir One" "Dir Two" "Dir Three"

# Using escape characters
mkdir My\ Documents
```

### 특수 문자가 포함된 이름

```bash
# Create directories with special characters
mkdir "data-2024"
mkdir "backup_$(date +%Y%m%d)"
mkdir "temp.$(whoami)"

# Avoid problematic characters
mkdir project_2024  # Better than "project 2024"
mkdir user_data     # Better than "user's data"
```

## 오류 처리 및 검증

### 자주 발생하는 오류 상황

```bash
# Check if directory exists before creating
if [ ! -d "mydir" ]; then
    mkdir mydir
    echo "Directory created"
else
    echo "Directory already exists"
fi

# Create directory only if parent exists
mkdir subdir  # Fails if current directory doesn't exist
mkdir -p parentdir/subdir  # Creates both if needed
```

### Safe 디렉터리 Creation

```bash
# Function to safely create directories
create_safe_dir() {
    if mkdir -p "$1" 2>/dev/null; then
        echo "Created directory: $1"
    else
        echo "Failed to create directory: $1"
        return 1
    fi
}

# Usage
create_safe_dir "/path/to/new/directory"
```

## 다른 명령어와의 조합 활용

### 디렉터리 Creation and Navigation

```bash
# Create directory and immediately change to it
mkdir myproject && cd myproject

# Create and navigate in one command (function)
mkcd() {
    mkdir -p "$1" && cd "$1"
}
mkcd ~/projects/newproject
```

### 디렉터리와 파일 동시 생성

```bash
# Create directory structure and add files
mkdir -p project/{src,docs,tests}
touch project/src/main.py
touch project/docs/README.md
touch project/tests/test_main.py

# Create directory and set up basic files
mkdir website
cd website
mkdir {css,js,images}
touch index.html css/style.css js/script.js
```

### 일괄(배치) 작업

```bash
# Create directories from a list
cat > dirlist.txt << EOF
projects/web
projects/mobile
projects/desktop
documents/reports
documents/presentations
EOF

# Create all directories from file
while read dir; do
    mkdir -p "$dir"
done < dirlist.txt
```

## 권한 및 소유권 관리

### 생성 후 소유권 변경

```bash
# Create directory and set ownership
mkdir myapp
sudo chown user:group myapp

# Create with specific permissions and ownership
sudo mkdir -m 755 /opt/myapp
sudo chown user:group /opt/myapp
```

### 특정 사용자를 위한 디렉터리 생성

```bash
# Create user-specific directories
sudo mkdir -p /home/newuser/{Documents,Downloads,Pictures}
sudo chown -R newuser:newuser /home/newuser
sudo chmod 755 /home/newuser
```

## SELinux 보안 컨텍스트

### SELinux 보안 컨텍스트 설정

```bash
# Create directory with specific SELinux context
mkdir -Z user_home_t user_data

# Create directory and set context afterward
mkdir secure_data
sudo semanage fcontext -a -t httpd_exec_t "/path/to/secure_data(/.*)?"
sudo restorecon -R /path/to/secure_data
```

## 문제 해결 및 트러블슈팅

### 자주 묻는 질문 및 해결책

```bash
# Permission denied
sudo mkdir /restricted/path  # Use sudo for system directories

# Parent directory doesn't exist
mkdir -p path/to/deep/directory  # Use -p flag

# Directory already exists
mkdir -p existing_dir  # -p prevents error if directory exists

# Invalid characters in name
mkdir "valid_name"  # Use quotes or escape special characters
```

### Debugging 디렉터리 Creation

```bash
# Check available space before creating
df -h .

# Verify parent directory permissions
ls -ld parent_directory

# Check if directory was created successfully
if [ -d "newdir" ]; then
    echo "Directory created successfully"
    ls -ld newdir
fi
```
## 옵션 참조

| **옵션** | **긴 형식** | **설명** |
|:---|:---|:---|
| `-m MODE` | `--mode=MODE` | 생성할 디렉터리의 파일 모드(권한)를 설정합니다. |
| `-p` | `--parents` | 필요한 경우 상위 디렉터리를 생성하며 이미 존재해도 오류를 표시하지 않습니다. |
| `-v` | `--verbose` | 생성되는 각 디렉터리에 대한 메시지를 출력합니다. |
| `-Z CTX` | `--context=CTX` | SELinux 보안 컨텍스트를 설정합니다. |
| `--help` | - | 도움말 메시지를 표시한 후 종료합니다. |
| `--version` | - | 버전 정보를 출력한 후 종료합니다. |

## 모범 사례

### 디렉터리 이름 지정 규칙

```bash
# 설명이 포함된 이름 사용
mkdir user_documents    # 권장
mkdir stuff              # 비권장


# 일관된 이름 패턴 사용
mkdir project_2024_01
mkdir project_2024_02


# 공백과 특수 문자 사용 피하기
mkdir my-project       # 권장
mkdir "my project"     # 작동하지만 문제를 일으킬 수 있음
```

### 구성 전략

```bash
# 날짜를 기준으로 구성
mkdir -p archives/$(date +%Y)/{01..12}


# 프로젝트를 기준으로 구성
mkdir -p projects/{active,completed,archived}


# 사용자를 기준으로 구성
mkdir -p users/{admins,developers,testers}
```

### 자동화 및 스크립팅

```bash
#!/bin/bash
# 표준 프로젝트 구조를 생성하는 스크립트


PROJECT_NAME="$1"
if [ -z "$PROJECT_NAME" ]; then
    echo "사용법: $0 <project_name>"
    exit 1
fi


echo "프로젝트 구조를 생성합니다: $PROJECT_NAME"
mkdir -p "$PROJECT_NAME"/{
    src/{main,test},
    docs/{api,user},
    config/{dev,prod,test},
    scripts/{build,deploy},
    data/{input,output,temp}
}


echo "프로젝트 구조가 성공적으로 생성되었습니다!"
tree "$PROJECT_NAME"
```

## 다른 도구와의 통합

### 버전 관리와 함께 사용

```bash
# Git을 초기화하면서 프로젝트 생성
mkdir myproject
cd myproject
git init
mkdir {src,docs,tests}
touch .gitignore README.md
```

### Docker와 함께 사용

```bash
# Docker 프로젝트 구조 생성
mkdir -p docker-project/{app,data,logs,config}
touch docker-project/Dockerfile
touch docker-project/docker-compose.yml
```

## 중요 참고 사항

- 디렉터리가 이미 존재할 때 오류가 발생하지 않도록 `-p` 플래그를 사용하십시오.
- 시스템 디렉터리를 생성할 때는 권한에 주의하십시오.
- 공백이 포함된 디렉터리 이름은 항상 따옴표로 묶으십시오.
- 생성한 디렉터리 구조를 시각화하려면 `tree` 명령어 사용을 고려하십시오.
- `mkdir` 명령어는 `umask`의 영향을 받은 기본 권한으로 디렉터리를 생성합니다.
- 현재 위치 밖에 디렉터리를 생성할 때는 절대 경로를 사용하십시오.

`mkdir` 명령어는 Linux 시스템에서 파일을 구성하고 디렉터리 구조를 생성하는 데 필수적입니다.

자세한 내용은 다음 매뉴얼을 확인하십시오.

```bash
man mkdir
```

# `gzip` 명령어

Linux/Unix에서 `gzip` 명령어는 데이터를 압축하거나 압축 해제할 때 사용합니다.

# 사용법

## 파일 압축
**작업:**
--- 파일 압축

**세부 정보:**
--- 압축을 적용하여 파일 크기를 줄입니다.

**명령어:**

```bash
gzip file_name
```

## 파일 압축 해제

**작업:**
--- 파일 압축 해제

**세부 정보:**
--- 데이터와 크기 측면에서 파일을 원래 형태로 복원합니다.

**명령어:**

```bash
gzip -d archive_01.gz
```

## 여러 파일 압축

**작업:**
--- 여러 파일 압축

**세부 정보:**
--- 여러 파일을 여러 아카이브로 압축합니다.

**명령어:**

```bash
gzip file_name_01 file_name_02 file_name_03
```

## 여러 파일 압축 해제

**작업:**
--- 여러 파일 압축 해제

**세부 정보:**
--- 여러 아카이브에서 여러 파일의 압축을 해제합니다.

**명령어:**

```bash
gzip -d archive_01.gz archive_02.gz archive_03.gz
```

## 디렉터리 압축

**작업:**
--- 디렉터리의 모든 파일 압축

**세부 정보:**
--- 디렉터리 아래의 여러 파일을 하나의 아카이브로 압축합니다.

**명령어:**

```bash
gzip -r directory_name
```

## 디렉터리 압축 해제

**작업:**
--- 디렉터리의 모든 파일 압축 해제

**세부 정보:**
--- 하나의 아카이브에서 디렉터리 아래의 여러 파일을 압축 해제합니다.

**명령어:**

```bash
gzip -dr directory_name
```

## 압축 중 자세한 출력 표시

**작업:**
--- 파일을 자세한 정보 표시 모드로 압축

**세부 정보:**
--- 명령어 작업에 대한 더 많은 정보를 출력합니다.

**명령어:**

```bash
gzip -v file_name
```

# `whatis` 명령어

`whatis` 명령어는 명령어에 대한 매뉴얼 페이지의 한 줄 설명을 표시합니다. 알 수 없는 명령어가 어떤 용도로 사용되는지 기본적으로 파악할 때 사용할 수 있습니다.

### 사용 예시

1. `ls`가 어떤 용도로 사용되는지 표시합니다.

```bash
whatis ls
```

2. `make`로 시작하는 모든 명령어의 용도를 표시하려면 다음 명령어를 실행합니다.

```bash
whatis -w make*
```

### 구문(Syntax)

```bash
whatis [-OPTION] [KEYWORD]
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-d` | `--debug` | 디버깅 메시지를 표시합니다. |
| `-r` | `--regex` | 각 키워드를 정규 표현식으로 해석합니다. |
| `-w` | `--wildcard` | 키워드에 와일드카드가 포함되어 있음을 지정합니다. |

# `who` 명령어

`who` 명령어는 로그인한 사용자 목록, 시스템의 현재 실행 수준(runlevel) 및 마지막 시스템 부팅 시간을 표시합니다.

### 예시

1. 현재 로그인한 사용자에 대한 모든 세부 정보를 표시합니다.

```bash
who -a
```

2. 종료된 모든 프로세스의 목록을 표시합니다.

```bash
who -d -H
```

### 구문(Syntax)

```bash
who [options] [filename]
```

### 추가 플래그 및 기능

| **단축 플래그** | **설명** |
|:---|:---|
| `-r` | 현재 실행 수준을 모두 표시합니다. |
| `-d` | 종료된 모든 프로세스를 표시합니다. |
| `-q` | 모든 로그인 이름과 로그인한 사용자 수를 표시합니다. |
| `-h` | 표시되는 열의 헤더를 출력합니다. |
| `-b` | 마지막 시스템 부팅 시간을 표시합니다. |

# `free` 명령어

Linux/Unix에서 `free` 명령어는 메모리(RAM/SWAP) 정보를 표시할 때 사용합니다.

# 사용법

## 메모리 사용량 표시

**작업:**
--- 사용 가능한 메모리와 사용 중인 메모리 및 스왑 사용량을 출력합니다.

**세부 정보:**
--- 값은 기본적으로 키비바이트(KiB) 단위로 표시됩니다.

**명령어:**

```bash
free
```

## 사람이 읽기 쉬운 형식으로 메모리 사용량 표시

**작업:**
--- 사용 가능한 메모리와 사용 중인 메모리 및 스왑 사용량을 출력합니다.

**세부 정보:**
--- 출력 값이 사람이 읽기 쉬운 형식으로 표시됩니다. (GB/MB 단위)

**명령어:**

```bash
free -h
```

## 전체 행과 함께 메모리 사용량 표시

**작업:**
--- 메모리 사용량을 출력하고 전체 합계를 요약한 행을 추가합니다.

**세부 정보:**
--- `-t` 플래그는 메모리와 스왑의 합산된 전체 용량을 확인할 때 유용합니다.

**명령어:**

```bash
free -t
```

# `top/htop` 명령어

`top`은 Linux 배포판과 Unix 계열 운영 체제에 기본으로 설치되어 제공되는 명령줄 유틸리티입니다. 시스템 정보, CPU를 가장 많이 사용하는 프로세스 및 RAM 사용량을 표시할 때 사용합니다.

`htop`은 `ncurses`를 기반으로 하는 Linux 및 Unix 계열 운영 체제용 대화형 프로세스 뷰어이자 프로세스 관리자입니다. `top`에 강력한 기능을 추가했다고 생각하면 `htop`을 쉽게 이해할 수 있습니다.

## `top`과 `htop` 비교

| **기능** | **`top`** | **`htop`** |
|:---|:---|:---|
| 유형 | 대화형 시스템 모니터, 프로세스 뷰어 및 프로세스 관리자 | 대화형 시스템 모니터, 프로세스 뷰어 및 프로세스 관리자 |
| 운영 체제 | Linux 배포판, macOS | Linux 배포판, macOS |
| 설치 | 기본으로 포함되어 항상 사용할 수 있습니다. 이 때문에 더 널리 사용됩니다. | 대부분의 Linux 배포판에 기본으로 설치되어 있지 않으므로 직접 설치해야 합니다. |
| 사용자 인터페이스 | 기본적인 텍스트 인터페이스만 제공합니다. | 색상을 사용하는 더 보기 좋은 텍스트 그래픽 인터페이스를 제공합니다. |
| 스크롤 지원 | 지원하지 않습니다. | 지원합니다. 가로 및 세로 스크롤이 가능합니다. |
| 마우스 지원 | 지원하지 않습니다. | 지원합니다. |
| 프로세스 표시 | 프로세스를 표시하지만 트리 형식은 아닙니다. | 사용자 스레드와 커널 스레드를 포함하여 트리 형식으로 표시할 수 있습니다. |
| 네트워크 사용량 | 지원하지 않습니다. | 지원하지 않습니다. |
| 디스크 사용량 | 지원하지 않습니다. | 지원하지 않습니다. |
| 설명 | 검색이나 프로세스에 메시지 보내기와 같은 고급 옵션을 사용하려면 학습이 필요합니다. 많은 시스템에서 기본 프로세스 뷰어로 사용되므로 `top`에 대한 기본 지식을 갖추는 것이 좋습니다. | 사용하기 더 쉽고 `/`를 사용한 `vi`와 유사한 검색을 지원합니다. 프로세스에 메시지를 보내거나 프로세스를 종료하고 우선순위를 변경하는 작업이 더 쉽고, `top`처럼 프로세스 번호를 직접 입력할 필요가 없습니다. |

## 예시
### `top`

1. 실행 중인 프로세스에 대한 동적 실시간 정보를 표시합니다.

```bash
top
```

2. 메모리 사용량을 기준으로 프로세스를 정렬합니다. (기본 정렬 기준은 프로세스 ID입니다.)

```bash
top -o mem
```

3. CPU 사용량을 기준으로 먼저 정렬한 다음 실행 시간을 기준으로 정렬합니다.

```bash
top -o cpu -O time
```

4. 지정한 사용자가 소유한 프로세스만 표시합니다.

```bash
top -user {user_name}
```

### `htop`

1. 실행 중인 프로세스에 대한 동적 실시간 정보를 표시합니다. `top`의 향상된 버전입니다.

```bash
htop
```

2. 특정 사용자가 소유한 프로세스를 표시합니다.

```bash
htop --user {user_name}
```

3. 지정한 `sort_item`을 기준으로 프로세스를 정렬합니다. 사용 가능한 옵션은 `htop --sort help`로 확인할 수 있습니다.

```bash
htop --sort {sort_item}
```

## 구문(Syntax)

```bash
top [OPTIONS]
```

```bash
htop [OPTIONS]
```

## 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-a` | - | 메모리 사용량을 기준으로 정렬합니다. |
| `-b` | - | 일괄 처리 모드로 실행합니다. `top`을 일괄 처리 모드로 시작하며 이 모드는 `top`의 출력을 다른 프로그램이나 파일로 전달할 때 유용합니다. 이 모드에서는 `top`이 입력을 받지 않으며 `-n` 명령줄 옵션으로 지정한 반복 횟수에 도달하거나 프로세스가 종료될 때까지 실행됩니다. |
| `-h` | - | `top --user {user_name}`과 같이 사용하며 지정한 사용자가 소유한 프로세스만 표시합니다. |
| `-U` | -user | 도움말을 표시합니다. |
| `-u` | - | `-o cpu -O time`과 동일한 별칭입니다. |

# `sl` 명령어

Linux에서 `sl` 명령어는 터미널을 가로질러 증기 기관차(`sl`)를 실행하는 재미있는 프로그램입니다.



## 설치

실행하기 전에 패키지를 설치합니다.

```bash
sudo apt install sl
```

## 구문(Syntax)

```bash
sl
```

# `echo` 명령어

`echo` 명령어는 터미널에 텍스트 문자열을 표시할 때 사용합니다. Linux/Unix 시스템에서 가장 기본적인 명령어 중 하나이며 텍스트·변수·형식이 지정된 콘텐츠를 출력하기 위해 셸 스크립트, 명령줄 작업 및 시스템 관리 작업에서 자주 사용됩니다.

## 구문(Syntax)

```bash
echo [OPTIONS] [STRING...]
```

## 주요 기능

- **텍스트 출력**: 간단한 텍스트 문자열을 표시합니다.
- **변수 확장**: 환경 변수의 값을 표시합니다.
- **이스케이프 시퀀스**: 특수 문자를 사용하여 출력 형식을 지정합니다.
- **파일 작업**: 파일에 텍스트를 작성하거나 추가합니다.
- **스크립트 통합**: 셸 스크립팅에 필수적으로 사용됩니다.

## 기본 사용법

### 간단한 텍스트 출력

```bash
# 간단한 텍스트 표시
echo "Hello, World!"
echo Hello World


# 여러 인수 표시
echo Hello World Linux
echo "Multiple" "words" "here"


# 빈 줄
echo
```

### 변수 표시

```bash
# 환경 변수 표시
echo $HOME
echo $USER
echo $PATH


# 사용자 지정 변수 표시
name="John"
echo $name
echo "Hello, $name"


# 변수 확장과 함께 표시
echo "현재 사용자: $USER, 홈 디렉터리: $HOME"
```

## 고급 기능

### 이스케이프 시퀀스

```bash
# 이스케이프 시퀀스 해석 활성화
echo -e "Hello\nWorld"          # 줄바꿈
echo -e "Name:\tJohn"           # 탭
echo -e "Hello\bWorld"          # 백스페이스
echo -e "Hello\rWorld"          # 캐리지 리턴
echo -e "Line 1\vLine 2"        # 수직 탭
echo -e "\aAlert sound"         # 경고음/알림음


# 백슬래시를 문자 그대로 표시
echo -e "Path: C:\\\\Users"     # 백슬래시를 문자 그대로 표시
echo -e "Quote: \"Hello\""      # 이스케이프된 따옴표
```

### 출력 제어

```bash
# 끝의 줄바꿈 억제
echo -n "이름을 입력하십시오: "


# 줄바꿈 없이 여러 줄 출력
echo -n "Loading"
echo -n "."
echo -n "."
echo "."


# 사용자 입력을 위해 read와 함께 사용
echo -n "비밀번호를 입력하십시오: "
read -s password
```

## 파일 작업

### 파일에 쓰기

```bash
# 파일 내용을 덮어씀
echo "Hello World" > file.txt


# 여러 줄이 포함된 파일 생성
echo -e "Line 1\nLine 2\nLine 3" > multiline.txt


# 변수를 파일에 기록
echo "현재 날짜: $(date)" > info.txt
echo "현재 사용자: $USER" >> info.txt
```

### 파일에 추가하기

```bash
# 기존 파일에 추가
echo "New line" >> file.txt


# 타임스탬프와 함께 추가
echo "$(date): 로그 항목" >> logfile.txt


# 여러 줄 추가
echo -e "오류 발생\n타임스탬프: $(date)" >> error.log
```

## 문자열 형식 지정 및 조작

### 텍스트 형식 지정

```bash
# Center text (simple approach)
echo "        Centered Text        "

# Create separators
echo "================================"
echo "          IMPORTANT             "
echo "================================"

# Box drawing
echo "┌─────────────────┐"
echo "│   System Info   │"
echo "└─────────────────┘"
```

###  색상 출력

```bash
# ANSI color codes
echo -e "\033[31mRed text\033[0m"
echo -e "\033[32mGreen text\033[0m"
echo -e "\033[33mYellow text\033[0m"
echo -e "\033[34mBlue text\033[0m"

# Background colors
echo -e "\033[41mRed background\033[0m"
echo -e "\033[42mGreen background\033[0m"

# Bold and italic
echo -e "\033[1mBold text\033[0m"
echo -e "\033[3mItalic text\033[0m"
```

##  실무 활용 방안

###  시스템 정보 출력

```bash
# System status script
echo "=== System Information ==="
echo "Hostname: $(hostname)"
echo "Current User: $USER"
echo "Current Directory: $(pwd)"
echo "Date/Time: $(date)"
echo "Uptime: $(uptime -p)"
echo "Memory Usage: $(free -h | grep Mem | awk '{print $3"/"$2}')"
```

###  설정 파일 자동 생성

```bash
# Generate configuration file
echo "# Generated configuration - $(date)" > app.conf
echo "server_name=$HOSTNAME" >> app.conf
echo "port=8080" >> app.conf
echo "debug=false" >> app.conf
echo "log_level=info" >> app.conf
```

###  로그 파일 관리

```bash
# Structured logging
log_info() {
    echo "$(date '+%Y-%m-%d %H:%M:%S') [INFO] $1" >> application.log
}

log_error() {
    echo "$(date '+%Y-%m-%d %H:%M:%S') [ERROR] $1" >> application.log
}

# Usage
log_info "Application started"
log_error "Database connection failed"
```

###  대화형 메뉴 생성

```bash
# Simple menu system
show_menu() {
    echo "================================="
    echo "        Main Menu"
    echo "================================="
    echo "1. View System Info"
    echo "2. List Files"
    echo "3. Check Disk Usage"
    echo "4. Exit"
    echo "================================="
    echo -n "Enter your choice: "
}
```

##  명령어 대체 및 변수 활용

###  명령어 대체

```bash
# Display command output
echo "Current date is: $(date)"
echo "Number of files: $(ls | wc -l)"
echo "Free disk space: $(df -h / | tail -1 | awk '{print $4}')"

# Multiple command substitution
echo "System: $(uname -s), Kernel: $(uname -r), Architecture: $(uname -m)"
```

###  배열 및 변수 조작

```bash
# Array display
fruits=("apple" "banana" "orange")
echo "Fruits: ${fruits[@]}"
echo "First fruit: ${fruits[0]}"

# Variable with default values
echo "Editor: ${EDITOR:-nano}"
echo "Shell: ${SHELL:-/bin/bash}"

# String length and manipulation
text="Hello World"
echo "Text: $text"
echo "Length: ${#text}"
echo "Uppercase: ${text^^}"
echo "Lowercase: ${text,,}"
```

## 오류 처리 및 검증

###  입력값 검증

```bash
# Check if variable is set
if [ -n "$USER" ]; then
    echo "User is set to: $USER"
else
    echo "User variable is not set"
fi

# Conditional output
[ -d "/tmp" ] && echo "Directory /tmp exists" || echo "Directory /tmp not found"
```

###  오류 메시지 처리

```bash
# Error to stderr
echo "Error: Invalid input" >&2

# Success/failure with exit codes
if some_command; then
    echo "✓ Command succeeded"
else
    echo "✗ Command failed" >&2
    exit 1
fi
```

##  대화형 기능

###  사용자 입력 프롬프트

```bash
# Simple prompt
echo -n "Enter your name: "
read name
echo "Hello, $name!"

# Yes/No prompt
echo -n "Do you want to continue? (y/n): "
read -n 1 answer
echo
case $answer in
    y|Y) echo "Continuing..." ;;
    n|N) echo "Aborting..." ;;
    *) echo "Invalid choice" ;;
esac
```

###  진행률 표시기

```bash
# Simple progress bar
echo -n "Processing: "
for i in {1..20}; do
    echo -n "="
    sleep 0.1
done
echo " Complete!"

# Percentage progress
for i in {0..100..10}; do
    echo -ne "Progress: $i%\r"
    sleep 0.2
done
echo -e "\nDone!"
```

## Other Commands와의 통합

###  파이프 및 리디렉션 활용

```bash
# Pipe to other commands
echo "hello world" | tr '[:lower:]' '[:upper:]'
echo "one two three" | wc -w

# Complex pipelines
echo "$PATH" | tr ':' '\n' | sort | uniq

# Tee for simultaneous output and file writing
echo "Important message" | tee -a important.log
```

###  데이터 가공 및 처리

```bash
# Generate data for processing
echo "apple,5,red" | cut -d',' -f2
echo "one:two:three" | awk -F':' '{print $2}'

# Create structured data
echo -e "Name,Age,City\nJohn,25,NYC\nJane,30,LA" > data.csv
```
## 옵션 및 플래그 참조

| **옵션** | **설명** |
|:---|:---|
| `-n` | 끝에 줄바꿈을 출력하지 않습니다. |
| `-e` | 백슬래시 이스케이프 시퀀스의 해석을 활성화합니다. |
| `-E` | 백슬래시 이스케이프 시퀀스의 해석을 비활성화합니다. (기본값) |

## 이스케이프 시퀀스 참조

| **시퀀스** | **설명** |
|:---|:---|
| `\a` | 경고음 또는 알림음 |
| `\b` | 백스페이스 |
| `\c` | 끝의 줄바꿈을 생략합니다. |
| `\e` | 이스케이프 문자 |
| `\f` | 폼 피드 |
| `\n` | 줄바꿈 |
| `\r` | 캐리지 리턴 |
| `\t` | 수평 탭 |
| `\v` | 수직 탭 |
| `\\` | 문자 그대로의 백슬래시 |
| `\"` | 문자 그대로의 큰따옴표 |
| `\nnn` | 8진수 값 `nnn`에 해당하는 문자 |
| `\xhh` | 16진수 값 `hh`에 해당하는 문자 |

## 모범 사례

### 스크립트 작성

```bash
# 단어 분할을 방지하려면 따옴표를 사용합니다.
echo "Value: $variable"  # 올바른 사용
echo Value: $variable    # 문제가 발생할 수 있음


# 필요한 경우 -e를 사용합니다.
echo -e "Multi\nLine"    # 이스케이프 시퀀스를 올바르게 해석
echo "Multi\nLine"       # 백슬래시-n을 문자 그대로 출력


# 일관된 형식 사용
echo "프로세스를 시작합니다..."
echo "프로세스가 성공적으로 완료되었습니다."
echo "결과가 다음 파일에 저장되었습니다: $output_file"
```

### 디버깅 및 로깅

```bash
# 디버그 정보
echo "DEBUG: 변수 값은 '$variable'입니다." >&2


# 타임스탬프가 포함된 로그
echo "[$(date)] 백업 프로세스를 시작합니다." >> backup.log


# 일관된 로깅을 위한 함수
log() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a application.log
}
```

## 중요 참고 사항

- `echo`의 동작은 셸 종류에 따라 달라질 수 있습니다. (예: bash, dash, zsh)
- 이식성과 정밀한 형식 지정이 필요하다면 `printf`를 사용하십시오.
- 작은따옴표는 값을 그대로 유지하고, 큰따옴표는 변수 확장을 허용합니다.
- 단어 분할을 방지하려면 항상 변수를 따옴표로 묶으십시오.
- 이스케이프 시퀀스를 해석해야 하는 경우에만 `echo -e`를 사용하십시오.
- `echo`는 기본적으로 줄바꿈을 추가하며 줄바꿈을 생략하려면 `-n`을 사용하십시오.

`echo` 명령어는 셸 스크립팅과 명령줄 작업의 기본적인 도구입니다. 다양한 사용 사례에서 유연하게 텍스트를 출력할 수 있습니다.

자세한 내용은 다음 매뉴얼을 확인하십시오.

```bash
man echo
```

# `finger` 명령어

`finger` 명령어는 `/etc/passwd`, `/var/run/utmp`, `/var/log/wtmp`와 같은 파일을 조회하여 로컬 시스템 사용자의 정보를 표시합니다. 로컬 명령어이므로 실행하기 위해 특정 서비스나 데몬에 의존하지 않습니다.

이 명령어를 사용하면 로그인 시간, 유휴 상태 및 기타 시스템 정보와 같은 사용자 관련 세부 정보를 빠르게 확인할 수 있습니다.

### 예시:

1. 특정 사용자의 상세 정보 확인:

```
finger abc
```

*Output*

```
Login: abc                          Name: (null)
Directory: /home/abc                Shell: /bin/bash
On since Mon Nov  1 18:45 (IST) on :0 (messages off)
On since Mon Nov  1 18:46 (IST) on pts/0 from :0.0
New mail received Fri May  7 10:33 2013 (IST)
Unread since Sat Jun  7 12:59 2003 (IST)
No Plan.
```

2. 특정 사용자의 로그인 정보 및 유휴 상태 확인:

```
finger -s root
```

*Output*

```
Login     Name       		Tty      Idle  Login Time   Office     Office Phone
root         root           *1    19d Wed 17:45
root         root           *2     3d Fri 16:53
root         root           *3        Mon 20:20
root         root           *ta    2  Tue 15:43
root         root           *tb    2  Tue 15:44
```

### 구문(Syntax):

```
finger [-l] [-m] [-p] [-s] [username]
```
### 추가 플래그 및 기능

| **플래그** | **설명** |
|:---|:---|
| `-l` | 긴 출력 형식을 강제로 사용합니다. |
| `-m` | 사용자 이름으로만 인수를 일치시킵니다. (이름이나 성은 사용하지 않습니다.) |
| `-p` | 긴 형식의 출력에서 `.plan` 파일 내용을 표시하지 않습니다. |
| `-s` | 짧은 출력 형식을 강제로 사용합니다. |

### 추가 정보

**기본 형식:**  
기본 형식에는 로그인 이름, 사용자 전체 이름, 터미널 이름 및 쓰기 상태와 같은 항목이 포함됩니다. 또한 유휴 시간, 로그인 시간 및 시스템별 정보도 제공합니다.

**긴 형식:**  
긴 형식에서는 사용자의 홈 디렉터리, 로그인 셸, `.plan` 파일 및 `.project` 파일의 내용과 같은 정보가 추가로 표시됩니다.

***

## 개인정보 보호 고려 사항

`finger` 명령어는 시스템 사용자의 정보를 확인하는 데 유용하지만 공유 환경이나 다중 사용자 환경에서는 민감한 세부 정보가 노출될 수도 있습니다.

1. **사용자 이름 및 로그인 시간:** 로그인 시간이 표시되므로 사용자의 활동을 추적하는 데 이용될 수 있습니다.
2. **홈 디렉터리:** 사용자의 홈 디렉터리 경로가 노출됩니다.
3. **유휴 상태:** 사용자가 비활성 상태였던 시간이 표시되므로 현재 시스템을 사용 중인지 추측할 수 있습니다.
4. **메일 상태:** 메일 정보가 표시되므로 사용자의 활동 여부가 의도치 않게 드러날 수 있습니다.

### 잠재적인 위험

신뢰할 수 없는 사용자가 있는 환경에서는 `finger`로 노출되는 정보가 다음과 같은 공격에 악용될 수 있습니다.

- **사회공학 공격:** 악의적인 공격자가 이 정보를 이용하여 개인화된 피싱 공격을 설계할 수 있습니다.
- **타이밍 공격:** 사용자가 유휴 상태인지 활성 상태인지 알면 공격 시점을 정하는 데 유리할 수 있습니다.
- **표적 공격:** 사용자의 홈 디렉터리 위치를 파악하여 해당 위치를 집중적으로 공격할 수 있습니다.

### 개인정보 위험 완화

사용자 개인정보 보호가 중요한 환경에서는 이러한 위험을 줄이기 위해 `finger` 명령어에 대한 접근을 제한하는 것을 고려하십시오.

***

## `in.fingerd` 서비스

`finger` 명령어와 **`in.fingerd` 서비스**를 구분하는 것이 중요합니다. `finger` 명령어는 로컬 명령어인 반면, `in.fingerd`는 원격에서 사용자 정보를 조회할 수 있도록 하는 네트워크 데몬입니다. 잠재적인 보안 위험 때문에 최신 시스템에서는 일반적으로 이 서비스가 기본적으로 비활성화되어 있습니다.

`in.fingerd` 서비스가 활성화되어 있으면 네트워크를 통해 사용자 정보가 노출될 수 있으며 공격자가 이를 악용할 수 있습니다. 이러한 위험을 줄이려면 시스템 관리자는 필요하지 않은 경우 해당 서비스가 비활성화되어 있는지 확인해야 합니다.

### `in.fingerd` 서비스 비활성화

원격 조회가 우려된다면 다음 명령어로 `in.fingerd` 서비스를 비활성화할 수 있습니다.

```bash
sudo systemctl disable in.fingerd
sudo systemctl stop in.fingerd
```

`in.fingerd` 서비스를 비활성화하면 사용자 정보를 원격으로 조회할 수 없게 되어 시스템 보안이 향상됩니다.

# `groups` 명령어

Linux에서는 여러 사용자(시스템을 사용하거나 운영하는 사람)와 여러 그룹(사용자들의 모음)이 존재할 수 있습니다.

그룹을 사용하면 동일한 보안 및 접근 권한을 가진 사용자를 쉽게 관리할 수 있습니다. 한 사용자가 여러 그룹에 속할 수도 있습니다.

### 중요 사항

`groups` 명령어는 지정한 각 사용자 이름의 기본 그룹과 보조 그룹 이름을 출력합니다. 사용자 이름을 지정하지 않으면 현재 프로세스가 속한 그룹을 출력합니다.

사용자 이름을 두 개 이상 지정하면 각 사용자의 이름이 해당 사용자의 그룹 목록 앞에 표시됩니다. 사용자 이름과 그룹 목록은 콜론으로 구분됩니다.

### 구문(Syntax)

```bash
groups [username]
```

#### 예제 1

사용자 이름을 지정합니다.

```bash
groups demon
```

이 예제에서는 `demon`이라는 사용자 이름을 `groups` 명령어에 전달합니다. 출력에는 `demon` 사용자가 속한 그룹이 콜론으로 구분되어 표시됩니다.

#### 예제 2

사용자 이름을 지정하지 않으면 현재 사용자의 그룹 구성원 정보를 표시합니다.

```bash
groups
```

여기서 현재 사용자는 `demon`입니다. 따라서 인수 없이 `groups` 명령어를 실행하면 `demon`이 속한 그룹이 표시됩니다.

#### 예제 3

`groups` 명령어에 root를 전달합니다.

```bash
$demon# groups
```

> 참고: 프로세스의 기본 그룹과 보조 그룹은 일반적으로 부모 프로세스에서 상속되며 로그인 이후에는 대체로 변경되지 않습니다. 따라서 로그인한 후 그룹 데이터베이스를 변경하더라도 기존 로그인 세션에서는 `groups` 명령어에 변경 사항이 반영되지 않습니다. 사용할 수 있는 옵션은 `--help`와 `--version`뿐입니다.

# `man` 명령어

`man` 명령어는 터미널에서 실행할 수 있는 명령어의 매뉴얼을 표시할 때 사용합니다.

`man`은 다음과 같은 정보를 제공합니다.

- 설명(Description)
- 옵션(Options)
- 작성자(Authors)
- 기타 정보

### 예시:

1. `printf` 명령어 매뉴얼 페이지 확인:

```
man printf
```

2. `intro`에 대한 매뉴얼 2섹션(시스템 호출) 확인:

```
man 2 intro
```

3. 로컬 파일의 매뉴얼 페이지 보기 (`-l` 플래그 사용):

```
man -l [LOCAL-FILE]
```

### 구문(Syntax):

```
man [SECTION-NUM] [COMMAND NAME]
```
### 추가 플래그 및 기능:

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-f` | - | 명령어의 섹션을 반환합니다. |
| `-a` | - | 해당 명령어의 모든 매뉴얼 페이지를 표시합니다. |
| `-k` | - | 모든 매뉴얼 페이지에서 정규 표현식(RegEx)을 사용하여 지정된 명령어를 검색합니다. |
| `-w` | - | 지정된 명령어의 매뉴얼 페이지 위치를 반환합니다. |
| `-I` | - | 대소문자를 구분하여 명령어 매뉴얼을 검색합니다. |

# `passwd` 명령어

Linux에서 `passwd` 명령어는 사용자 계정의 비밀번호를 변경합니다. 일반 사용자는 자신의 계정 비밀번호만 변경할 수 있지만 슈퍼유저는 모든 계정의 비밀번호를 변경할 수 있습니다. 또한 `passwd` 명령어는 계정 또는 연결된 비밀번호의 유효 기간도 변경합니다.

## 예시

```bash
$ passwd

```

## `passwd`의 명령어 문법:

```bash
$ passwd [options] [LOGIN]

```

## 옵션

```bash
-a, --all
        This option can be used only with -S and causes show status for all users.

-d, --delete
        Delete a user's password.

-e, --expire
        Immediately expire an account's password.

-h, --help
        Display help message and exit.

-i, --inactive
        This option is used to disable an account after the password has been expired for a number of days.

-k, --keep-tokens
        Indicate password change should be performed only for expired authentication tokens (passwords).

-l, --lock
        Lock the password of the named account.

-q, --quiet
        Quiet mode.

-r, --repository
        change password in repository.

-S, --status
        Display account status information.                                                                        

```

# `w` 명령어

`w` 명령어는 현재 시스템에서 활동 중인 사용자와 해당 사용자의 [프로세스](https://www.computerhope.com/jargon/p/process.htm)에 대한 정보를 표시합니다.

### 예시:

1. [인수](https://www.computerhope.com/jargon/a/argument.htm) 없이 `w` 명령어를 실행하면 로그인한 사용자와 해당 사용자의 프로세스 목록이 표시됩니다.

```
w
```

2. *hope* 사용자의 정보 출력:

```
w hope
```

### 구문(Syntax):

```text
finger [-l] [-m] [-p] [-s] [username]
```

### 추가 플래그 및 기능:

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-h` | `--no-header` | 헤더를 출력하지 않습니다. |
| `-u` | `--no-current` | 현재 프로세스와 CPU 시간을 계산할 때 사용자 이름을 무시합니다. *(예를 들어 `su`를 사용하여 root 사용자로 전환한 다음 `w`와 `w -u`를 차례로 실행하면 이 옵션의 동작을 확인할 수 있습니다.)* |
| `-s` | `--short` | 축약된 출력을 표시합니다. *(로그인 시간, JCPU 또는 PCPU 시간을 출력하지 않습니다.)* |
| `-f` | `--from` | `from` 필드 *(원격 호스트 이름)*의 출력 여부를 전환합니다. 기본적으로는 `from` 필드를 출력하지 않지만 시스템 관리자나 배포판 관리자가 `from` 필드를 기본적으로 표시하도록 컴파일한 버전을 사용하고 있을 수도 있습니다. |
| `--help` | - | 도움말을 표시한 후 종료합니다. |
| `-V` | `--version` | 버전 정보를 표시한 후 종료합니다. |
| `-o` | `--old-style` | 이전 스타일의 출력을 사용합니다. *(유휴 시간이 1분 미만인 경우 공백으로 표시합니다.)* |
| *`user`* | - | 지정한 사용자에 대한 정보만 표시합니다. |

### 추가 정보

출력의 [헤더](https://www.computerhope.com/jargon/h/header.htm)는 다음 정보를 순서대로 표시합니다. 현재 시간, 시스템이 실행된 시간, 현재 로그인한 사용자 수, 그리고 지난 1분·5분·15분 동안의 시스템 [로드 평균](https://www.computerhope.com/jargon/l/load.htm)입니다.

각 사용자에 대해 다음 항목이 표시됩니다.

- 로그인 이름
- 사용자의 [TTY](https://www.computerhope.com/jargon/t/tty.htm)
- 사용자가 로그인해 있는 [원격 호스트](https://www.computerhope.com/jargon/r/remote.htm)
- 로그인한 상태로 경과한 시간
- [유휴 시간](https://www.computerhope.com/jargon/i/idle.htm)
- JCPU
- PCPU
- 현재 프로세스의 [명령줄](https://www.computerhope.com/jargon/c/commandi.htm)

JCPU 시간은 해당 TTY에 연결된 모든 프로세스가 사용한 시간입니다. 과거에 실행된 백그라운드 작업은 포함하지 않지만 현재 실행 중인 백그라운드 작업은 포함합니다.

PCPU 시간은 `what` 필드에 표시된 현재 프로세스가 사용한 시간입니다.

# `whoami` 명령어

***

`whoami` 명령어는 현재 유효 사용자의 사용자 이름을 표시합니다. 즉, 실행하면 현재 로그인한 사용자의 사용자 이름을 출력합니다.

유효 사용자 ID를 확인하려면 터미널에서 `whoami`를 입력하면 됩니다.

```bash
manish@godsmack:~$ whoami
# 출력:
manish
```

구문:

```bash
whoami [-OPTION]
```

이 명령어에는 다음과 같은 옵션을 사용할 수 있습니다.

`--help`: 도움말을 표시한 후 종료합니다.

예시:

```bash
whoami --help
```

출력:

```text
사용법: whoami [옵션]...
현재 유효 사용자 ID에 연결된 사용자 이름을 출력합니다.
id -un과 동일합니다.


      --help     이 도움말을 표시한 후 종료합니다.
      --version  버전 정보를 출력한 후 종료합니다.
```

`--version`: 버전 정보를 출력한 후 종료합니다.

예시:

```bash
whoami --version
```

출력:

```text
whoami (GNU coreutils) 8.32
Copyright (C) 2020 Free Software Foundation, Inc.
License GPLv3+: GNU GPL version 3 or later <https://gnu.org/licenses/gpl.html>.
This is free software: you are free to change and redistribute it.
There is NO WARRANTY, to the extent permitted by law.


Written by Richard Mlynarik.
```

# `history` 명령어

`history` 명령어는 현재 셸 세션과 이전 세션에서 실행한 명령어 목록을 표시합니다. 따라서 명령어를 다시 입력하지 않고도 명령어를 검토하거나 검색하고 재실행할 수 있습니다.

### 명령어 구문

```bash
history [옵션] [n]
```

### History의 작동 방식

명령어 기록은 홈 디렉터리의 파일에 저장됩니다.

- **Bash**: `~/.bash_history`
- **Zsh**: `~/.zsh_history`

저장되는 명령어의 수는 다음 셸 변수로 제어됩니다.

- `HISTSIZE`: 세션 중 메모리에 보관할 최대 명령어 수
- `HISTFILESIZE`: 기록 파일에 저장할 최대 명령어 수

다음 명령어로 이 값을 확인할 수 있습니다.

```bash
echo $HISTSIZE
echo $HISTFILESIZE
```

### 주요 옵션

- `history n` 최근 명령어 `n`개를 표시합니다.
- `history -c` 명령어 기록 목록을 삭제합니다. (현재 세션에만 적용됩니다.)
- `history -d offset` `offset` 위치의 기록 항목을 삭제합니다.
- `history -a` 새로운 기록 행을 기록 파일에 추가합니다.
- `history -w` 현재 명령어 기록을 기록 파일에 저장합니다.

### 예시

**1. 전체 명령어 기록 표시:**

```bash
history
```

**2. 최근 명령어 10개만 표시:**

```bash
history 10
```

**3. 기록에서 특정 명령어 검색:**

```bash
history | grep artisan
history | grep git
history | grep docker
```

**4. 기록 번호로 명령어 실행:**

```bash
!123
```

기록에서 123번 위치에 있는 명령어를 다시 실행합니다.

**5. 특정 문자열로 시작하는 가장 최근 명령어 실행:**

```bash
!git
```

`git`으로 시작하는 가장 최근 명령어를 실행합니다.

**6. 이전 명령어 실행:**

```bash
!!
```

**7. 이전 명령어를 `sudo`와 함께 실행:**

```bash
sudo !!
```

**8. 이전 명령어의 인수 재사용:**

```bash
# 다음 명령어를 실행했다고 가정합니다: cat /var/log/syslog
# 다음과 같이 마지막 인수를 사용할 수 있습니다.
vim !$
# 다음 명령어가 실행됩니다: vim /var/log/syslog
```

**9. 명령어 기록 삭제:**

```bash
history -c
```

**10. 기록에서 특정 항목 삭제:**

```bash
history -d 456
```

### 역방향 검색(Ctrl+R)

가장 강력한 기능 중 하나는 **역방향 증분 검색(reverse incremental search)**입니다.

1. `Ctrl+R`을 누릅니다.
2. 명령어의 일부를 입력하기 시작합니다.
3. 가장 최근에 일치하는 명령어가 표시됩니다.
4. `Ctrl+R`을 다시 누르면 이전에 일치한 항목을 차례로 확인할 수 있습니다.
5. `Enter`를 누르면 명령어를 실행하고, `Esc`를 누르면 명령어를 수정할 수 있습니다.

### History 제어 변수

`~/.bashrc` 또는 `~/.zshrc` 파일에서 다음 변수를 사용하여 기록 동작을 사용자 지정할 수 있습니다.

```bash
# 기록 크기 늘리기
export HISTSIZE=10000
export HISTFILESIZE=20000


# 중복 명령어 무시
export HISTCONTROL=ignoredups


# 공백으로 시작하는 명령어 무시
export HISTCONTROL=ignorespace


# 두 옵션 모두 적용
export HISTCONTROL=ignoreboth


# 저장하지 않을 특정 명령어 지정
export HISTIGNORE="ls:cd:pwd:exit:history"


# 기록에 타임스탬프 추가
export HISTTIMEFORMAT="%Y-%m-%d %H:%M:%S "
```

### 보안 고려 사항

**민감한 명령어가 저장되지 않도록 방지하기:**

1. **명령어 앞에 공백을 입력합니다.** (`HISTCONTROL=ignorespace`가 설정된 경우)

```bash
 mysql -u root -p
```

2. **일시적으로 기록 기능을 비활성화합니다.**

```bash
set +o history
# 이곳에서 민감한 명령어를 실행합니다.
set -o history
```

3. **특정 기록 항목을 삭제합니다.**

```bash
history -d <line_number>
```

4. **로그아웃하기 전에 기록을 삭제합니다.**

```bash
history -c && history -w
```

### 실제 활용 사례

- **디버깅**: 오류가 발생하기까지 실행한 명령어 순서를 검토합니다.
- **문서화**: 스크립트나 문서에 사용할 명령어 시퀀스를 복사합니다.
- **효율성 향상**: 복잡한 명령어를 다시 입력하지 않고 빠르게 재실행합니다.
- **학습**: 적절한 상황에서 공유 시스템을 사용하는 다른 사람이 실행한 명령어를 검토합니다.
- **감사 추적**: 타임스탬프를 활성화하여 어떤 명령어가 언제 실행되었는지 추적합니다.

# `login` 명령어

`login` 명령어는 사용자 세션을 시작합니다.

## 구문(Syntax)

```bash
$ login [-p] [-h host] [-H] [-f username|username]
```
## 플래그 및 기능

| **단축 플래그** | **설명** |
|---|---|
| `-f` | 로그인 인증을 건너뛸 때 사용합니다. 이 옵션은 일반적으로 `getty(8)`의 자동 로그인 기능에서 사용됩니다. |
| `-h` | 다른 서버(예: `telnetd(8)`)가 원격 호스트 이름을 `login`에 전달할 때 사용합니다. 전달된 호스트 이름은 `utmp`와 `wtmp`에 기록됩니다. 이 옵션은 슈퍼유저만 사용할 수 있습니다. |
| `-p` | `getty(8)`가 `login`에 환경을 보존하도록 알릴 때 사용합니다. |
| `-H` | 다른 서버(예: `telnetd(8)`)가 로그인 프롬프트의 `login:`에 호스트 이름을 출력하지 않도록 알릴 때 사용합니다. |
| `--help` | 도움말을 표시한 후 종료합니다. |
| `-v` | 버전 정보를 표시한 후 종료합니다. |

## 예시

`abhishek` 사용자로 시스템에 로그인하려면 로그인 프롬프트에 다음을 입력합니다.

```bash
$ login: abhishek
```

비밀번호가 설정되어 있다면 비밀번호 입력 프롬프트가 표시됩니다. 해당 프롬프트에 비밀번호를 입력합니다.

# `lscpu` 명령어

Linux/Unix에서 `lscpu`는 CPU 아키텍처 정보를 표시하는 데 사용됩니다. `lscpu`는 `sysfs`와 `/proc/cpuinfo` 파일에서 CPU 아키텍처 정보를 수집합니다.

예시:

```text
  manish@godsmack:~$ lscpu
    Architecture:        x86_64
    CPU op-mode(s):      32-bit, 64-bit
    Byte Order:          Little Endian
    CPU(s):              4
    On-line CPU(s) list: 0-3
    Thread(s) per core:  2
    Core(s) per socket:  2
    Socket(s):           1
    NUMA node(s):        1
    Vendor ID:           GenuineIntel
    CPU family:          6
    Model:               142
    Model name:          Intel(R) Core(TM) i5-7200U CPU @ 2.50GHz
    Stepping:            9
    CPU MHz:             700.024
    CPU max MHz:         3100.0000
    CPU min MHz:         400.0000
    BogoMIPS:            5399.81
    Virtualization:      VT-x
    L1d cache:           32K
    L1i cache:           32K
    L2 cache:            256K
    L3 cache:            3072K
    NUMA node0 CPU(s):   0-3
```

## 옵션

`-a, --all`  
온라인 CPU와 오프라인 CPU를 모두 출력에 포함합니다(`-e`의 기본 동작). 이 옵션은 `-e` 또는 `-p` 옵션과 함께 사용할 때만 지정할 수 있습니다.  
예시: `lsof -a`

`-b, --online`  
출력을 온라인 CPU로 제한합니다(`-p`의 기본 동작). 이 옵션은 `-e` 또는 `-p` 옵션과 함께 사용할 때만 지정할 수 있습니다.  
예시: `lscpu -b`

`-c, --offline`  
출력을 오프라인 CPU로 제한합니다. 이 옵션은 `-e` 또는 `-p` 옵션과 함께 사용할 때만 지정할 수 있습니다.

`-e, --extended [=list]`  
CPU 정보를 사람이 읽기 쉬운 형식으로 표시합니다.  
예시: `lsof -e`

자세한 내용은 `man lscpu` 또는 `lscpu --help`를 사용하여 확인할 수 있습니다.

# `cp` 명령어

`cp`는 파일과 디렉터리를 복사하는 명령줄 유틸리티입니다. `cp`는 `copy`를 의미합니다. 이 명령어는 파일 하나, 여러 파일 또는 디렉터리를 복사할 때 사용합니다. 디스크에 있는 파일을 다른 파일 이름으로 동일하게 복사합니다. `cp` 명령어의 인수에는 최소 두 개의 파일 이름이 필요합니다.

### 예시

1. 원본 파일의 내용을 대상 파일로 복사합니다.

```bash
cp sourceFile destFile
```

대상 파일이 존재하지 않으면 파일이 생성되고 내용이 복사됩니다. 대상 파일이 이미 존재하면 덮어씁니다.

2. 파일을 다른 디렉터리로 복사하려면 대상 디렉터리의 절대 경로 또는 상대 경로를 지정합니다.

```bash
cp sourceFile /folderName/destFile
```

3. 모든 파일과 하위 디렉터리를 포함하여 디렉터리를 복사합니다.

```bash
cp -R folderName1 folderName2
```

위 명령어는 대상 디렉터리를 생성하고, 원본의 모든 파일과 하위 디렉터리를 대상 디렉터리로 재귀적으로 복사합니다.

대상 디렉터리가 이미 존재하면 원본 디렉터리 자체와 그 내용이 대상 디렉터리 안에 복사됩니다.

4. 원본 디렉터리는 복사하지 않고 파일과 하위 디렉터리만 복사합니다.

```bash
cp -RT folderName1 folderName2
```

### 구문(Syntax)

`cp` 명령어의 일반적인 구문은 다음과 같습니다.

```bash
cp [OPTION] SOURCE DESTINATION
cp [OPTION] SOURCE DIRECTORY
cp [OPTION] SOURCE-1 SOURCE-2 SOURCE-3 SOURCE-n DIRECTORY
```

첫 번째와 두 번째 구문은 원본 파일을 대상 파일 또는 디렉터리로 복사할 때 사용합니다. 세 번째 구문은 여러 원본 파일을 디렉터리로 복사할 때 사용합니다.

#### 유용한 옵션

1. `-i` (대화형)

`i`는 대화형 복사를 의미합니다. 이 옵션을 사용하면 시스템이 대상 파일을 덮어쓰기 전에 사용자에게 먼저 경고합니다. `cp`는 응답을 요청하며 `y`를 입력하면 파일을 덮어쓰고 다른 문자를 입력하면 복사하지 않습니다.

```bash
$ cp -i file1.txt fileName2.txt
cp: overwrite 'file2.txt'? y
```

2. `-b` (백업)

`-b` 옵션을 사용하면 `cp` 명령어가 대상 파일의 백업을 같은 폴더에 다른 이름과 형식으로 생성합니다.

```bash
$ ls
a.txt b.txt


$ cp -b a.txt b.txt


$ ls
a.txt b.txt b.txt~
```

3. `-f` (강제)

사용자에게 대상 파일에 대한 쓰기 권한이 없어 시스템이 대상 파일을 쓰기 모드로 열 수 없는 경우, `cp` 명령어와 함께 `-f` 옵션을 사용하면 대상 파일을 먼저 삭제한 다음 원본에서 대상 파일로 내용을 복사합니다.

```bash
$ ls -l b.txt
-r-xr-xr-x+ 1 User User 3 Nov 24 08:45 b.txt
```

사용자, 그룹 및 기타 사용자는 쓰기 권한을 갖고 있지 않습니다.

`-f` 옵션을 사용하지 않으면 명령어가 실행되지 않습니다.

```bash
$ cp a.txt b.txt
cp: cannot create regular file 'b.txt': Permission denied
```

`-f` 옵션을 사용하면 명령어가 성공적으로 실행됩니다.

```bash
$ cp -f a.txt b.txt
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-i` | --interactive | 덮어쓰기 전에 확인을 요청합니다. |
| `-f` | --force | 기존 대상 파일을 열 수 없으면 해당 파일을 삭제하고 다시 시도합니다. |
| `-b` | - | 대상 파일의 백업을 같은 폴더에 다른 이름과 형식으로 생성합니다. |
| `-r` 또는 `-R` | `--recursive` | 전체 디렉터리 구조를 재귀적으로 복사하여 `cp` 명령어의 재귀적 동작을 수행합니다. |
| `-n` | `--no-clobber` | 기존 파일을 덮어쓰지 않습니다. (이전에 지정한 `-i` 옵션보다 우선합니다.) |
| `-p` | - | 가능한 경우 지정된 속성을 보존합니다. (기본값: 모드, 소유권, 타임스탬프. 추가 속성: 컨텍스트, 링크, xattr, 전체 속성) |

# `mv` 명령어

`mv` 명령어는 UNIX와 같은 파일 시스템에서 **하나 이상의 파일 또는 디렉터리를 한 위치에서 다른 위치로 이동**할 수 있게 합니다.

다음 두 가지 용도로 사용할 수 있습니다.

- 파일 또는 폴더 이름을 변경합니다.
- 여러 파일을 다른 디렉터리로 이동합니다.

_**참고:** 이름을 변경할 때는 디스크 공간이 추가로 사용되지 않으며 `mv` 명령어는 확인 프롬프트를 표시하지 않습니다._

### 구문(Syntax)

```bash
mv [options] source (file or directory) destination
```

### 예시

1. `old_name.txt`라는 파일의 이름을 변경합니다.

```bash
mv old_name.txt new_name.txt
```

2. 현재 디렉터리의 `_essay.txt_` 파일을 `_assignments_`라는 디렉터리로 이동하면서 `_essay1.txt_`로 이름을 변경합니다.

```bash
mv essay.txt assignments/essay1.txt
```

3. 현재 디렉터리의 `_essay.txt_` 파일을 이름 변경 없이 `_assignments_` 디렉터리로 이동합니다.

```bash
mv essay.txt assignments
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-f` | `--force` | 확인 없이 대상 파일을 덮어쓰면서 강제로 이동합니다. |
| `-i` | `--interactive` | 덮어쓰기 전에 대화형 확인을 요청합니다. |
| `-u` | `--update` | 원본 파일이 대상 파일보다 최신이거나 대상 파일이 없을 때만 이동합니다. |
| `-n` | `--no-clobber` | 기존 파일을 덮어쓰지 않습니다. |
| `-v` | `--verbose` | 원본 파일과 대상 파일을 출력합니다. |
| `-b` | `--backup` | 기존 대상 파일의 백업을 생성합니다. |

# `ps` 명령어

`ps` 명령어(`process status`)는 Linux 시스템에서 실행 중인 프로세스의 정보를 표시할 때 사용합니다. 예를 들어 프로세스 ID(PID), 메모리 사용량, CPU 시간, 관련 사용자 등의 정보를 표시합니다.

특정 프로세스를 검색하기 위해 `grep`과 함께 파이프(`|`)로 연결하거나, 많은 양의 출력을 스크롤하기 위해 `less`와 함께 사용하는 경우가 많습니다.

## `ps`를 사용하는 이유

시스템이 느려지거나 앱이 응답하지 않는 상황을 가정해 보겠습니다. 이때 `ps`를 사용하여 다음 작업을 수행할 수 있습니다.

- CPU 또는 메모리를 많이 사용하는 프로세스를 식별합니다.
- 프로그램의 PID(Process ID)를 확인합니다.
- 멈춘 프로세스를 종료하거나 디버깅합니다.
- 공유 시스템에서 누가 어떤 프로그램을 실행 중인지 확인합니다.

## 기본 구문

```bash
ps [options]
```

옵션 없이 실행하면 `ps`는 현재 터미널 세션의 프로세스만 표시합니다.

예시:

```bash
ps
```

출력:

```text
  PID TTY          TIME CMD
 4587 pts/0    00:00:00 bash
 4621 pts/0    00:00:00 ps
```

## 필수 사용법

**기억해 둘 조합:** `ps aux`

- `a` = 모든 프로세스(모든 사용자)
- `u` = 사용자 또는 소유자 정보 표시
- `x` = 터미널이 연결되지 않은 프로세스 포함

```bash
ps aux
```

출력 예시:

```text
USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
root         1  0.0  0.1 168208  1100 ?        Ss   10:15   0:02 /sbin/init
myuser    2471  0.5  1.2 431204 24500 ?        Sl   10:17   1:05 code
myuser    2523  2.3  0.7 230940 14860 pts/0    R+   10:22   0:01 ps aux
```

## 일상적인 실전 예시

### 프로세스 찾기 및 종료

멈춘 **OpenShot** 프로세스를 중지하려는 경우를 살펴보겠습니다.

```bash
ps aux | grep openshot
```

출력:

```text
myuser   3625  99.9  6.1 1243924 252340 ?  Rl  10:30  25:17 openshot
myuser   3649   0.0  0.0   6348   740 pts/0  S+  10:31   0:00 grep --color=auto openshot
```

이제 프로세스를 종료합니다.

```bash
kill -9 3625
```

### 사용자별 프로세스 표시

```bash
ps -u username
```

출력:

```text
  PID TTY          TIME CMD
 2284 ?        00:00:00 sshd
 2455 ?        00:00:02 bash
```

### 출력 필터링 및 정렬

메모리를 가장 많이 사용하는 프로세스 10개를 표시합니다.

```bash
ps aux --sort=-%mem | head -10
```

CPU를 가장 많이 사용하는 프로세스 10개를 표시합니다.

```bash
ps aux --sort=-%cpu | head -10
```

### 부모 및 자식 프로세스 계층 확인

```bash
ps -ef --forest
```

이 명령어는 부모-자식 관계를 트리 형태로 표시합니다. 서비스가 생성한 하위 프로세스를 디버깅할 때 유용합니다.

### 사용자 지정 출력 형식

PID, 사용자, 메모리 및 명령어만 표시하려면 다음과 같이 입력합니다.

```bash
ps -eo pid,user,%mem,cmd
```

## 실제 DevOps 예시

### 1. 특정 포트를 사용하는 프로세스 확인 {.unnumbered}

```bash
sudo ps -fp $(sudo lsof -t -i:8080)
```

### 2. Jenkins, Nginx 또는 Docker 프로세스 모니터링 {.unnumbered}

```bash
ps aux | grep nginx
ps aux | grep jenkins
ps aux | grep docker
```

### 3. 좀비 프로세스 찾기 {.unnumbered}

```bash
ps aux | awk '{ if ($8 == "Z") print $0; }'
```

## 빠른 참조를 위한 주요 옵션

| 옵션 | 설명 |
|:---|:---|
| `aux` | 자세한 정보와 함께 모든 프로세스를 표시합니다. |
| `-ef` | 전체 목록을 표시합니다. (`aux`의 대안입니다.) |
| `-eo format` | 출력 열을 사용자 지정합니다. |
| `--sort` | 열을 기준으로 정렬합니다. (`-%mem`, `-%cpu` 등) |
| `-p PID` | 특정 PID를 표시합니다. |
| `-C name` | 명령어 이름으로 프로세스를 표시합니다. |
| `-u user` | 특정 사용자의 프로세스를 표시합니다. |
| `f` | ASCII 아트 형식의 프로세스 트리를 표시합니다. |

### 추가 옵션

| **옵션** | **설명** |
|:---|:---|
| `a` | 터미널(TTY)이 연결된 모든 프로세스의 목록을 표시합니다. |
| `-A` | 모든 프로세스를 나열합니다. `-e`와 동일합니다. |
| `-a` | 세션 리더와 터미널에 연결되지 않은 프로세스를 제외한 모든 프로세스를 표시합니다. |
| `-d` | 세션 리더를 제외한 모든 프로세스를 선택합니다. |
| `--deselect` | 지정된 조건을 충족하는 프로세스를 제외한 모든 프로세스를 표시합니다. `-N`과 동일합니다. |
| `-e` | 모든 프로세스를 나열합니다. `-A`와 동일합니다. |
| `-N` | 지정된 조건을 충족하는 프로세스를 제외한 모든 프로세스를 표시합니다. `--deselect`와 동일합니다. |
| `T` | 현재 터미널과 연결된 모든 프로세스를 선택합니다. 인수 없이 사용하는 `-t` 옵션과 동일합니다. |
| `r` | 실행 중인 프로세스만 선택하도록 제한합니다. |
| `--help simple` | 모든 기본 옵션을 표시합니다. |
| `--help all` | 사용 가능한 모든 옵션을 표시합니다. |

## 관련 도구

**실시간** 모니터링이 필요하다면 다음 명령어를 사용합니다.

```bash
top
```

또는 더 사용자 친화적인 최신 버전인 다음 명령어를 사용할 수 있습니다.

```bash
htop
```

# `kill` 명령어

Linux의 `kill` 명령어(`/bin/kill`에 위치)는 프로세스를 수동으로 종료할 때 사용하는 명령어입니다. `kill` 명령어는 프로세스에 신호를 보내 프로세스를 종료합니다. `kill` 명령어와 함께 보낼 신호를 지정하지 않으면 프로세스를 종료하는 기본 `_TERM_` 신호가 전송됩니다.

신호는 다음 세 가지 방식으로 지정할 수 있습니다.

- **번호 사용** (예: `-5`)
- **SIG 접두사 사용** (예: `-SIGkill`)
- **SIG 접두사 없이 사용** (예: `-kill`)

### 구문

```bash
kill [OPTIONS] [PID]...
```

### 예시

1. 사용 가능한 모든 신호를 표시하려면 다음 명령어를 사용합니다.

```bash
kill -l
```

2. `kill` 명령어에서 _PID_를 사용하는 방법은 다음과 같습니다.

```bash
$ kill pid
```

3. 프로세스에 신호를 보내려면 다음과 같이 입력합니다.

```bash
kill {-signal | -s signal} pid
```

4. 신호 지정:

- 신호 번호 사용

```bash
kill -9 pid
```

- 신호에 SIG 접두사 사용

```bash
kill -SIGHUP pid
```

- 신호에 SIG 접두사 없이 사용

```bash
kill -HUP pid
```

### 인수

신호를 보낼 프로세스 목록에는 이름과 PID를 함께 사용할 수 있습니다.

```text
pid    각 PID는 다음 방법 중 하나로 표현할 수 있습니다.

       n      여기서 n은 0보다 큰 값입니다. PID가 n인 프로세스에 신호를 보냅니다.

       0      현재 프로세스 그룹의 모든 프로세스에 신호를 보냅니다.

       -1     PID가 1보다 큰 모든 프로세스에 신호를 보냅니다.

       -n     여기서 n은 1보다 큰 값입니다. 프로세스 그룹 n의 모든 프로세스에
              신호를 보냅니다.
              '-n' 형식의 인수가 프로세스 그룹을 의미하는 경우에는 먼저 신호를
              지정하거나 인수 앞에 '--' 옵션을 추가해야 합니다. 그렇지 않으면
              해당 인수는 보낼 신호로 해석됩니다.

name   해당 이름으로 실행된 모든 프로세스에 신호를 보냅니다.
```

### 옵션

```text
-s, --signal signal
       보낼 신호를 지정합니다. 신호 이름 또는 번호로 지정할 수 있습니다.

-l, --list [number]
       신호 이름 목록을 출력하거나, 지정된 신호 번호를 이름으로 변환합니다.
       신호는 /usr/include/linux/signal.h에서 확인할 수 있습니다.

-L, --table
       -l과 비슷하지만 신호 이름과 해당 번호를 함께 출력합니다.

-a, --all
       명령어 이름을 PID로 변환할 때 현재 프로세스와 동일한 UID를 가진
       프로세스로 제한하지 않습니다.

-p, --pid
       지정된 프로세스의 프로세스 ID(PID)만 출력하고 신호는 보내지 않습니다.

--verbose
       kill 명령어로 신호를 보낼 PID와 신호를 출력합니다.
```

# `killall` 명령어

`killall`은 지정된 명령어를 실행 중인 **모든** 프로세스에 신호를 보냅니다. 신호 이름을 지정하지 않으면 `SIGTERM`이 전송됩니다. 일반적으로 `killall` 명령어는 프로세스 이름을 사용하여 해당 이름의 모든 프로세스를 종료합니다.

신호는 이름(예: `-HUP` 또는 `-SIGHUP`), 번호(예: `-1`) 또는 `-s` 옵션을 사용하여 지정할 수 있습니다.

명령어 이름이 정규 표현식이 아니고(`-r` 옵션을 사용하지 않은 경우) 슬래시(`/`)를 포함하면, 프로세스 이름과 관계없이 해당 파일을 실행 중인 프로세스가 종료 대상으로 선택됩니다.

나열된 각 명령어에 대해 하나 이상의 프로세스가 종료되었거나, 명령어가 지정되지 않았고 `-u` 및 `-Z` 검색 조건에 일치하는 프로세스가 하나 이상 있으면 `killall`은 종료 상태 0을 반환합니다. 그렇지 않으면 0이 아닌 값을 반환합니다.

`killall` 프로세스 자체는 스스로 종료하지 않습니다. (다른 `killall` 프로세스를 종료할 수는 있습니다.)

### 예시

1. `SIGTERM`으로 `conky`라는 이름과 일치하는 모든 프로세스를 종료합니다.

```bash
killall conky
# 또는
killall -SIGTERM conky
# 또는
killall -15 conky
```

다음과 같은 방법으로 Wine 프로세스도 종료할 수 있습니다. Wine 프로세스는 Linux에서 실행 중인 Windows 실행 파일입니다.

```bash
killall TQ.exe
```

2. 지원되는 모든 신호를 나열합니다.

```bash
$ killall -l
HUP INT QUIT ILL TRAP ABRT BUS FPE KILL USR1 SEGV USR2 PIPE ALRM TERM STKFLT
CHLD CONT STOP TSTP TTIN TTOU URG XCPU XFSZ VTALRM PROF WINCH POLL PWR SYS
```

신호 번호는 다음과 같이 확인할 수 있습니다.

```bash
$ for s in $(killall -l); do echo -n "$s " && kill -l $s; done
HUP 1
INT 2
QUIT 3
ILL 4
TRAP 5
ABRT 6
BUS 7
FPE 8
KILL 9
USR1 10
SEGV 11
USR2 12
PIPE 13
ALRM 14
TERM 15
STKFLT 16
CHLD 17
CONT 18
STOP 19
TSTP 20
TTIN 21
TTOU 22
URG 23
XCPU 24
XFSZ 25
VTALRM 26
PROF 27
WINCH 28
POLL 29
PWR 30
SYS 31
```

3. 실수로 프로세스를 종료하지 않도록 종료 전에 확인을 요청합니다.

```bash
$ killall -i conky
Kill conky(1685) ? (y/N)
```

4. 모든 프로세스를 종료하고 프로세스가 종료될 때까지 기다립니다.

```bash
killall -w conky
```

5. 실행 시간을 기준으로 프로세스를 종료합니다.

```bash
# 실행된 지 2분 미만인 모든 firefox 프로세스 종료
killall -y 2m firefox


# 실행된 지 2시간이 넘은 모든 firefox 프로세스 종료
killall -o 2h firefox
```

### 구문(Syntax)

```bash
killall [OPTION]... [--] NAME...
killall -l, --list
killall -V, --version
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-e` | `--exact` | 매우 긴 이름에 대해 정확히 일치하는 이름을 요구합니다. |
| `-I` | `--ignore-case` | 프로세스 이름을 대소문자를 구분하지 않고 일치시킵니다. |
| `-g` | `--process-group` | 프로세스 대신 프로세스 그룹을 종료합니다. |
| `-y` | `--younger-than` | 지정한 시간보다 최근에 실행된 프로세스를 종료합니다. |
| `-o` | `--older-than` | 지정한 시간보다 오래전에 실행된 프로세스를 종료합니다. |
| `-i` | `--interactive` | 실수로 종료하는 것을 방지하기 위해 프로세스를 종료하기 전에 확인을 요청합니다. |
| `-l` | `--list` | 알려진 모든 신호 이름을 나열합니다. |
| `-q` | `--quiet` | 오류 또는 경고 메시지를 출력하지 않습니다. |
| `-r` | `--regexp` | `NAME`을 확장 정규 표현식으로 해석합니다. |
| `-s` | `--signal SIGNAL` | `SIGTERM` 대신 지정한 신호를 보냅니다. |
| `-u` | `--user USER` | `USER`로 실행 중인 프로세스만 종료합니다. |
| `-v` | `--verbose` | 신호가 성공적으로 전송되었는지 보고합니다. |
| `-w` | `--wait` | 프로세스가 종료될 때까지 기다립니다. |
| `-n` | `--ns PID` | 지정된 PID와 동일한 네임스페이스에 속한 프로세스와 일치시킵니다. |
| `-Z` | `--context` | 지정된 컨텍스트를 가진 프로세스만 정규 표현식으로 검색하여 종료합니다. (다른 인수보다 먼저 지정해야 합니다.) |

### 관련 명령어

`kill`, `pidof`

# `env` 명령어

Linux/Unix에서 `env` 명령어는 현재 환경 변수 목록을 출력하거나, 현재 환경을 변경하지 않고 사용자 지정 환경에서 프로그램을 실행할 때 사용합니다.

## 구문(Syntax)

```bash
env [OPTION]... [-] [NAME=VALUE]... [COMMAND [ARG]...]
```

## 사용법

1. 현재 환경 변수 목록을 출력합니다.

```bash
env
```

2. 빈 환경에서 명령어를 실행합니다.

```bash
env -i command_name
```

3. 환경에서 변수를 제거합니다.

```bash
env -u variable_name
```

4. 각 출력 항목의 끝에 NULL 문자를 추가합니다.

```bash
env -0
```

## 전체 옵션 목록

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-i` | `--ignore-environment` | 빈 환경에서 시작합니다. |
| `-0` | `--null` | 각 출력 행을 줄바꿈이 아닌 NUL 문자로 끝냅니다. |
| `-u` | `--unset=NAME` | 환경에서 변수를 제거합니다. |
| `-C` | `--chdir=DIR` | 작업 디렉터리를 `DIR`로 변경합니다. |
| `-S` | `--split-string=S` | `S`를 처리하고 여러 인수로 분할합니다. 셰뱅(`shebang`) 행에서 여러 인수를 전달할 때 사용합니다. |
| `-v` | `--debug` | 각 처리 단계에 대한 자세한 정보를 출력합니다. |
| - | `--help` | 도움말 메시지를 출력합니다. |
| - | `--version` | 버전 정보를 출력합니다. |

# `printenv` 명령어

`printenv`는 지정된 [환경 _변수_](https://www.computerhope.com/jargon/e/envivari.htm)의 값을 출력합니다. [_변수_](https://www.computerhope.com/jargon/v/variable.htm)를 지정하지 않으면 모든 환경 변수의 이름과 값을 쌍으로 출력합니다.

### 예시

1. 모든 환경 변수의 값을 표시합니다.

```bash
printenv
```

2. 현재 사용자의 [홈 디렉터리](https://www.computerhope.com/jargon/h/homedir.htm) 위치를 표시합니다.

```bash
printenv HOME
```

3. 출력 항목 사이의 종료 문자로 `--null` 명령줄 옵션을 사용합니다.

```bash
printenv --null SHELL HOME
```

*참고: 기본적으로* `printenv` *명령어는 출력 항목 사이의 종료 문자로 줄바꿈을 사용합니다.*

### 구문(Syntax)

```bash
printenv [OPTION]... PATTERN...
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-0` | `--null` | [줄바꿈](https://www.computerhope.com/jargon/n/newline.htm) 대신 **0** 바이트로 각 출력 행을 끝냅니다. |
| `--help` | - | 도움말 메시지를 표시한 후 종료합니다. |

# `hostname` 명령어

`hostname`은 시스템의 DNS 이름을 표시하고, 호스트 이름 또는 NIS 도메인 이름을 표시하거나 설정할 때 사용합니다.

### 구문(Syntax):

```
hostname [-a|--alias] [-d|--domain] [-f|--fqdn|--long] [-A|--all-fqdns] [-i|--ip-address] [-I|--all-ip-addresses] [-s|--short] [-y|--yp|--nis]
```

### 예시:

1. ``` hostname -a, hostname --alias ```
    호스트의 별칭(alias)을 표시합니다(사용 중단된 옵션이므로 사용을 권장하지 않습니다).

2. ```hostname -s, hostname --short```
    짧은 호스트 이름을 출력합니다(첫 번째 마침표 앞부분만 표시).

3.  ```hostname -V, hostname --version```
    버전 정보를 표준 출력으로 표시하고 정상 종료합니다.

###  도움말 확인

`hostname` 명령어에 대한 자세한 가이드를 보려면 다음 명령을 실행하세요.

```
man hostname
```
# `nano` 명령어

`nano` 명령어를 사용하면 텍스트 파일을 생성하거나 편집할 수 있습니다.

### 설치

Nano 텍스트 편집기는 macOS와 대부분의 Linux 배포판에 기본으로 설치되어 있습니다. `vi`와 `vim`의 대안으로 사용할 수 있습니다. 시스템에 설치되어 있는지 확인하려면 다음 명령어를 입력합니다.

```bash
nano --version
```

`nano`가 설치되어 있지 않다면 패키지 관리자를 사용하여 설치할 수 있습니다.

Ubuntu 또는 Debian:

```bash
sudo apt install nano
```

### 예시

1. 기존 파일을 열려면 `nano` 뒤에 파일 경로를 입력합니다.

```bash
nano /path/to/filename
```

2. 새 파일을 생성하려면 `nano` 뒤에 파일 이름을 입력합니다.

```bash
nano filename
```

3. 특정 행과 문자 위치에 커서를 둔 상태로 파일을 열려면 다음 구문을 사용합니다.

```bash
nano +line_number,character_number filename
```

### 단축키와 기능 개요

| **단축키** | **설명** |
|:---|:---|
| `Ctrl + S` | 현재 파일을 저장합니다. |
| `Ctrl + O` | 파일을 저장할지 묻습니다. (`Save as`, 다른 이름으로 저장) |
| `Ctrl + X` | nano 버퍼를 닫고 종료합니다. |
| `Ctrl + K` | 현재 행을 잘라내기 버퍼로 이동합니다. |
| `Ctrl + U` | 잘라내기 버퍼의 내용을 붙여넣습니다. |
| `Alt + 6` | 현재 행을 잘라내기 버퍼로 복사합니다. |
| `Alt + U` | 마지막 작업을 실행 취소합니다. |
| `Alt + E` | 마지막으로 실행 취소한 작업을 다시 실행합니다. |

# `rm` 명령어

`rm`은 “remove”의 약자로, 지정된 파일을 제거(삭제)할 때 사용하는 명령어입니다. 적절한 옵션을 사용하면 디렉터리도 삭제할 수 있습니다.

### 예시

```bash
rm filename.txt
```

### 문법

```bash
rm [OPTION] [FILE|DIRECTORY]
```

### 옵션과 기능

| **짧은 옵션** | **긴 옵션** | **설명** |
|:---|:---|:---|
| `-f` | `--force` | 파일이나 디렉터리가 존재하지 않아도 무시하며 확인을 요청하지 않습니다. |
| `-i` | - | 삭제할 때마다 확인을 요청합니다. |
| `-I` | - | 파일을 3개 초과하여 삭제하거나 재귀적으로 삭제하기 전에 한 번 확인을 요청합니다. |
| `-d` | `--dir` | 빈 디렉터리를 삭제합니다. |
| `-v` | `--verbose` | 수행 중인 작업을 설명합니다. |
| `-r` 또는 `-R` | `--recursive` | 디렉터리와 그 내용을 재귀적으로 삭제합니다. |
| - | `--help` | 도움말을 표시한 후 종료합니다. |
| - | `--version` | 먼저 버전 정보를 출력한 후 종료합니다. |
| - | `--no-preserve-root` | `/`를 특별하게 처리하지 않습니다. |
| - | `--preserve-root[=all]` | `/`를 삭제하지 않습니다. (기본값) `all`을 지정하면 상위 디렉터리와 다른 장치에 있는 모든 명령줄 인수를 거부합니다. |
| - | `--interactive[=WHEN]` | `WHEN` 값에 따라 확인을 요청합니다. `never`는 요청하지 않고, `once` 또는 `-I`는 한 번, `always` 또는 `-i`는 항상 확인을 요청합니다. `WHEN`을 지정하지 않으면 항상 확인을 요청합니다. |
| - | `--one-file-system` | 계층 구조를 재귀적으로 삭제할 때 해당 명령줄 인수가 속한 파일 시스템과 다른 파일 시스템에 있는 디렉터리는 건너뜁니다. |

***중요 안내:***

1. `rm`은 기본적으로 디렉터리를 삭제하지 않습니다. 나열된 각 디렉터리와 그 안의 모든 내용을 삭제하려면 `-r`, `-R`, `--recursive` 옵션을 사용합니다.
2. `-foo`처럼 이름이 `-`로 시작하는 파일을 삭제하려면 다음 명령어 중 하나를 사용합니다.
   - `rm -- -foo`
   - `rm ./-foo`
3. 삭제한 파일이나 디렉터리를 실제로 복구할 수 없도록 하려면 `shred` 명령어 사용을 고려하십시오.

# `ifconfig` 명령어

`ifconfig`는 커널에 상주하는 네트워크 인터페이스를 설정할 때 사용합니다. 부팅 시 필요한 인터페이스를 설정하는 데 사용되며 이후에는 일반적으로 디버깅이나 시스템 튜닝이 필요한 경우에만 사용합니다.

인수를 지정하지 않으면 `ifconfig`는 현재 활성화된 인터페이스의 상태를 표시합니다. 하나의 인터페이스 인수를 지정하면 해당 인터페이스의 상태만 표시합니다. `-a` 인수 하나를 지정하면 비활성화된 인터페이스를 포함하여 모든 인터페이스의 상태를 표시합니다. 그 외의 경우에는 인터페이스를 설정합니다.

### 문법

```bash
ifconfig [-v] [-a] [-s] [interface]
ifconfig [-v] interface [aftype] options
```

### 예시

1. 현재 활성화된 인터페이스를 표시합니다.

```bash
ifconfig
```

2. 비활성화된 인터페이스를 포함하여 현재 존재하는 모든 인터페이스를 표시합니다.

```bash
ifconfig -a
```

3. 모든 오류 상태를 표시합니다.

```bash
ifconfig -v
```

4. 간단한 목록을 표시합니다.

```bash
ifconfig -s
```

5. 특정 네트워크 인터페이스(예: `eth0`)의 세부 정보를 표시합니다.

```bash
ifconfig eth0
```

6. 인터페이스(예: `eth0`)의 드라이버를 활성화합니다.

```bash
ifconfig eth0 up
```

7. 인터페이스(예: `eth0`)의 드라이버를 비활성화합니다.

```bash
ifconfig eth0 down
```

8. 네트워크 인터페이스(예: `eth0`)에 특정 IP 주소를 할당합니다.

```bash
ifconfig eth0 10.10.1.23
```

9. 네트워크 인터페이스(예: `eth0`)의 MAC(Media Access Control) 주소를 변경합니다.

```bash
ifconfig eth0 hw ether AA:BB:CC:DD:EE:FF
```

10. 네트워크 인터페이스(예: `eth0`)에 넷마스크를 지정합니다.

```bash
ifconfig eth0 netmask 255.255.255.224
```

11. 네트워크 인터페이스(예: `eth0`)에서 무차별 모드를 활성화합니다.

```bash
ifconfig eth0 promisc
```

일반 모드에서는 네트워크 카드가 패킷을 수신하면 해당 패킷이 자신에게 전달된 것인지 확인합니다. 자신에게 전달된 패킷이 아니면 일반적으로 해당 패킷을 폐기합니다. 그러나 무차별 모드에서는 네트워크 카드를 통해 흐르는 모든 패킷을 수락합니다.

12. 네트워크 인터페이스(예: `eth0`)에서 무차별 모드를 비활성화합니다.

```bash
ifconfig eth0 -promisc
```

13. 네트워크 인터페이스(예: `eth0`)의 최대 전송 단위(MTU)를 설정합니다.

```bash
ifconfig eth0 mtu 1000
```

MTU를 사용하면 인터페이스에서 전송되는 패킷의 최대 크기를 설정할 수 있습니다. MTU는 하나의 트랜잭션에서 인터페이스가 처리할 수 있는 최대 옥텟 수를 지정합니다.

14. 네트워크 인터페이스에 추가 IP 주소를 설정하려면 네트워크 인터페이스에 네트워크 별칭을 구성할 수 있습니다.

```bash
ifconfig eth0:0 10.10.1.24
```

별칭 네트워크 주소는 네트워크 인터페이스와 동일한 서브넷 마스크에 속해야 합니다. 예를 들어 `eth0` 네트워크의 IP 주소가 `10.10.1.23`이라면 별칭 IP 주소는 `10.10.1.24`가 될 수 있습니다. 인터페이스의 서브넷 마스크가 `255.255.255.224`이므로 `10.10.2.24`는 유효하지 않은 IP 주소입니다.

15. 네트워크 별칭을 제거합니다.

```bash
ifconfig eth0:0 down
```

각 범위(즉, 동일한 네트워크와 주소/넷마스크 조합)에 대해 첫 번째 별칭을 삭제하면 모든 별칭이 삭제된다는 점에 유의하십시오.

### 도움말 명령어

다음 명령어를 실행하면 `ifconfig` 명령어에 대한 전체 안내를 확인할 수 있습니다.

```bash
man ifconfig
```

# `ip` 명령어

`ip` 명령어는 네트워크 관리 작업에 사용되는 `iproute2` 패키지의 강력한 유틸리티입니다. `ifconfig`, `route`, `arp`와 같은 기존 네트워크 도구를 대체하는 최신 도구입니다. `ip` 명령어를 사용하면 라우팅, 네트워크 장치, 인터페이스 및 터널을 표시하거나 조작할 수 있습니다.

## 구문(Syntax)

```bash
ip [ OPTIONS ] OBJECT { COMMAND | help }
```

## 주요 기능

- **인터페이스 관리**: 네트워크 인터페이스를 설정하고 모니터링합니다.
- **IP 주소 관리**: IP 주소를 추가, 제거 및 표시합니다.
- **라우팅 제어**: 라우팅 테이블과 경로를 관리합니다.
- **이웃 관리**: ARP 또는 이웃 캐시 항목을 처리합니다.
- **네트워크 네임스페이스**: 네트워크 네임스페이스를 사용합니다.
- **터널링**: 네트워크 터널을 생성하고 관리합니다.

## 기본 사용법

### 네트워크 인터페이스 표시

```bash
# 모든 네트워크 인터페이스 표시
ip link show


# 특정 인터페이스 표시
ip link show eth0


# 인터페이스 통계 표시
ip -s link show eth0
```

### IP 주소 관리

```bash
# 모든 IP 주소 표시
ip addr show


# 특정 인터페이스의 주소 표시
ip addr show eth0


# 인터페이스에 IP 주소 추가
sudo ip addr add 192.168.1.100/24 dev eth0


# 인터페이스에서 IP 주소 제거
sudo ip addr del 192.168.1.100/24 dev eth0


# 인터페이스의 모든 주소 삭제
sudo ip addr flush dev eth0
```

## 인터페이스 관리

### 인터페이스 활성화 및 비활성화

```bash
# 인터페이스 활성화
sudo ip link set eth0 up


# 인터페이스 비활성화
sudo ip link set eth0 down


# 인터페이스 MTU 설정
sudo ip link set eth0 mtu 1400


# MAC 주소 변경
sudo ip link set eth0 address 00:11:22:33:44:55
```

### 가상 인터페이스 생성

```bash
# VLAN 인터페이스 생성
sudo ip link add link eth0 name eth0.100 type vlan id 100


# 브리지 인터페이스 생성
sudo ip link add name br0 type bridge


# 가상 이더넷 쌍 생성
sudo ip link add veth0 type veth peer name veth1


# 가상 인터페이스 삭제
sudo ip link delete veth0
```

## 라우팅 관리

### 라우팅 경로 확인

```bash
# 라우팅 테이블 표시
ip route show


# 특정 대상에 대한 경로 표시
ip route get 8.8.8.8


# 특정 인터페이스를 통한 경로 표시
ip route show dev eth0


# IPv6 라우팅 경로 표시
ip -6 route show
```

### 라우팅 경로 관리

```bash
# 기본 경로 추가
sudo ip route add default via 192.168.1.1


# 특정 경로 추가
sudo ip route add 10.0.0.0/8 via 192.168.1.1


# 특정 인터페이스를 통한 경로 추가
sudo ip route add 172.16.0.0/16 dev eth1


# 경로 삭제
sudo ip route del 10.0.0.0/8


# 기존 경로 교체
sudo ip route replace default via 192.168.1.254
```

### 여러 라우팅 테이블 사용

```bash
# 모든 라우팅 테이블 표시
ip route show table all


# 특정 테이블에 경로 추가
sudo ip route add 192.168.2.0/24 via 10.0.0.1 table 100


# 특정 라우팅 테이블 표시
ip route show table 100


# 라우팅 규칙 추가
sudo ip rule add from 192.168.1.0/24 table 100
```

## 이웃(ARP) 관리

### ARP 캐시 작업

```bash
# ARP 캐시 표시
ip neigh show


# 특정 인터페이스의 이웃 표시
ip neigh show dev eth0


# 정적 ARP 항목 추가
sudo ip neigh add 192.168.1.50 lladdr 00:11:22:33:44:55 dev eth0


# ARP 항목 삭제
sudo ip neigh del 192.168.1.50 dev eth0


# ARP 캐시 삭제
sudo ip neigh flush all
```

## 고급 기능

### 네트워크 네임스페이스

```bash
# 네트워크 네임스페이스 목록 표시
ip netns list


# 네트워크 네임스페이스 생성
sudo ip netns add myns


# 네임스페이스에서 명령어 실행
sudo ip netns exec myns ip addr show


# 네트워크 네임스페이스 삭제
sudo ip netns del myns


# 인터페이스를 네임스페이스로 이동
sudo ip link set eth1 netns myns
```

### 터널링

```bash
# GRE 터널 생성
sudo ip tunnel add gre1 mode gre remote 10.0.0.2 local 10.0.0.1 ttl 255


# IPIP 터널 생성
sudo ip tunnel add ipip1 mode ipip remote 192.168.1.2 local 192.168.1.1


# 터널 표시
ip tunnel show


# 터널 삭제
sudo ip tunnel del gre1
```

### 트래픽 제어

```bash
# 큐잉 규율 표시
ip qdisc show


# 트래픽 셰이핑 추가
sudo ip qdisc add dev eth0 root handle 1: htb default 30


# 트래픽 제어 통계 표시
ip -s qdisc show dev eth0
```

## 모니터링 및 통계
### 인터페이스 통계

```bash
# 인터페이스의 상세 통계 표시
ip -s link show


# 확장 통계 표시
ip -s -s link show eth0


# 인터페이스 변경 사항 모니터링
ip monitor link


# 주소 변경 사항 모니터링
ip monitor addr
```

### 실시간 모니터링

```bash
# 모든 네트워크 이벤트 모니터링
ip monitor


# 라우팅 경로 변경만 모니터링
ip monitor route


# 타임스탬프와 함께 모니터링
ip -t monitor
```

## 일반 옵션

### 일반 옵션

```bash
# 특정 프로토콜 패밀리 사용
ip -4 addr show    # IPv4만
ip -6 addr show    # IPv6만


# 더 자세한 정보 표시
ip -d link show


# JSON 형식으로 출력
ip -j addr show


# 출력에 색상 적용
ip -c addr show


# 이름을 확인하여 변환하지 않음
ip -n route show
```

### 일괄 작업

```bash
# 파일에서 명령어 실행
sudo ip -batch commands.txt


# 일괄 작업 파일의 예시 내용:
# link set eth0 up
# addr add 192.168.1.100/24 dev eth0
# route add default via 192.168.1.1
```

## 실제 예시

### 고정 IP 설정

```bash
# 고정 IP 전체 설정
sudo ip addr add 192.168.1.100/24 dev eth0
sudo ip link set eth0 up
sudo ip route add default via 192.168.1.1


# DNS 추가 (/etc/resolv.conf 편집)
echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf
```

### VLAN을 사용하는 브리지 생성

```bash
# 브리지 생성
sudo ip link add name br0 type bridge


# VLAN 인터페이스 생성
sudo ip link add link eth0 name eth0.10 type vlan id 10
sudo ip link add link eth0 name eth0.20 type vlan id 20


# 인터페이스를 브리지에 추가
sudo ip link set eth0.10 master br0
sudo ip link set eth0.20 master br0


# 모든 장치 활성화
sudo ip link set br0 up
sudo ip link set eth0.10 up
sudo ip link set eth0.20 up
```

### 네트워크 문제 해결

```bash
# 연결 경로 확인
ip route get 8.8.8.8


# 인터페이스 상태 확인
ip link show | grep -E "(UP|DOWN)"


# 중복 IP 확인
ip addr show | grep inet


# 네트워크 변경 사항 모니터링
ip monitor all
```

## 옵션 참조

| **옵션** | **설명** |
|:---|:---|
| `-4, -6` | IPv4 또는 IPv6 프로토콜 패밀리를 사용합니다. |
| `-b, -batch` | 파일에서 명령어를 읽습니다. |
| `-c, -color` | 색상이 적용된 출력을 사용합니다. |
| `-d, -details` | 상세 정보를 표시합니다. |
| `-f, -family` | 프로토콜 패밀리를 지정합니다. |
| `-h, -human` | 사람이 읽기 쉬운 형식으로 출력합니다. |
| `-j, -json` | JSON 형식으로 출력합니다. |
| `-n, -numeric` | 이름을 확인하여 변환하지 않습니다. |
| `-o, -oneline` | 한 줄 형식으로 출력합니다. |
| `-r, -resolve` | 호스트 이름을 확인하여 변환합니다. |
| `-s, -stats` | 통계를 표시합니다. |
| `-t, -timestamp` | 타임스탬프를 표시합니다. |

## 객체 참조

| **객체** | **설명** |
|:---|:---|
| `link` | 네트워크 장치(인터페이스) |
| `addr` | IPv4 또는 IPv6 주소 |
| `route` | 라우팅 테이블 항목 |
| `rule` | 라우팅 정책 데이터베이스의 규칙 |
| `neigh` | 이웃(ARP) 테이블 항목 |
| `ntable` | 이웃 테이블 설정 |
| `tunnel` | IP를 통한 터널 |
| `maddr` | 멀티캐스트 주소 |
| `mroute` | 멀티캐스트 라우팅 캐시 항목 |
| `monitor` | netlink 메시지를 감시합니다. |

## 중요 참고 사항

- 대부분의 네트워크 설정 변경 작업에는 root 권한이 필요합니다.
- `ip` 명령어로 변경한 내용은 즉시 적용되지만 시스템을 재부팅하면 유지되지 않습니다.
- 영구적인 설정을 위해서는 네트워크 설정 파일이나 NetworkManager를 사용하십시오.
- 네트워크 설정을 변경하기 전에 항상 설정을 백업하십시오.
- 더 이상 사용이 권장되지 않는 `ifconfig`나 `route` 같은 도구 대신 `ip` 명령어를 사용하십시오.

## NetworkManager와의 통합

```bash
# NetworkManager가 인터페이스를 관리하고 있는지 확인
nmcli device status


# 인터페이스에 대한 NetworkManager 관리를 일시적으로 비활성화
sudo nmcli device set eth0 managed no


# NetworkManager 관리 다시 활성화
sudo nmcli device set eth0 managed yes
```

`ip` 명령어는 최신 Linux 네트워크 관리에 필수적인 도구이며 네트워크 설정을 포괄적으로 제어할 수 있습니다.

자세한 내용은 다음 매뉴얼을 확인하십시오.

```bash
man ip
```
# `clear` 명령어

Linux에서 `clear` 명령어는 터미널 화면을 지울 때 사용합니다.

## 예시

```bash
$ clear
```

## 실행 전

```bash
$ echo Hello World
Hello World


$ clear
```

## `clear` 명령어 실행 후

```bash
$
```

스크린샷:



명령어를 실행하면 터미널 화면이 지워집니다.



# `su` 명령어

`su`(substitute user, 사용자 전환) 명령어를 사용하면 다른 사용자 계정의 권한으로 명령어를 실행할 수 있습니다. 일반적으로 관리 작업을 위해 root 계정으로 전환하거나, 로그아웃한 후 다시 로그인하지 않고 다른 사용자의 권한으로 명령어를 실행할 때 사용합니다.

## 구문(Syntax)

```bash
su [OPTIONS] [-] [USER [ARGUMENT...]]
```

## 주요 기능

- **사용자 전환**: 시스템의 다른 사용자 계정으로 전환합니다.
- **환경 제어**: 환경 변수를 상속할지 초기화할지 선택합니다.
- **셸 선택**: 사용할 셸을 지정합니다.
- **그룹 관리**: 기본 그룹과 보조 그룹을 전환합니다.
- **명령어 실행**: 다른 사용자의 권한으로 특정 명령어를 실행합니다.

## 기본 사용법

### root 사용자로 전환

```bash
# root 사용자로 전환 (root 비밀번호 필요)
su


# 로그인 셸을 사용하여 root로 전환 (권장)
su -


# 로그인 셸을 사용하는 다른 구문
su -l
su --login
```

### 특정 사용자로 전환

```bash
# 특정 사용자로 전환
su username


# 로그인 셸을 사용하여 사용자로 전환
su - username
su -l username


# 사용자로 전환한 후 특정 명령어 실행
su - username -c "whoami"
```

## 환경 처리

### 로그인 셸과 비로그인 셸

```bash
# 비로그인 셸 (현재 환경 유지)
su username
# 현재 디렉터리와 환경 변수가 유지됩니다.


# 로그인 셸 (새 환경으로 시작)
su - username
# 사용자의 홈 디렉터리로 이동하고 사용자 프로필을 불러옵니다.
```

### 환경 변수 제어

```bash
# 현재 환경 유지
su -m username
su --preserve-environment username


# 특정 환경 변수 유지
su -w HOME,TERM username
su --whitelist-environment=HOME,TERM username


# 환경을 초기화하되 특정 변수는 유지
su - username --preserve-environment=PATH
```

## 고급 사용법

### 그룹 관리

```bash
# 사용자와 기본 그룹 전환
su -g developers username


# 보조 그룹과 함께 전환
su -G developers,admins username


# 현재 그룹 확인
su - username -c "groups"
```

### 셸 선택

```bash
# 특정 셸 사용
su -s /bin/bash username
su --shell=/bin/zsh username


# /etc/shells에서 허용된 셸 사용
su -s /usr/bin/fish username


# 사용 가능한 셸 확인
cat /etc/shells
```

### 명령어 실행

```bash
# 다른 사용자의 권한으로 단일 명령어 실행
su - username -c "ls -la /home/username"


# 여러 명령어 실행
su - username -c "cd /tmp && ls -la && pwd"


# 다른 사용자의 권한으로 스크립트 실행
su - username -c "/path/to/script.sh"


# 인수와 함께 명령어 실행
su - username -c "grep 'pattern' /var/log/syslog"
```

## 실제 예시

### 시스템 관리

```bash
# 관리 작업을 위해 root로 전환
su -
# 이제 관리 명령어를 실행할 수 있습니다.


# 단일 관리 명령어 실행
su -c "systemctl restart apache2"


# 시스템 설정 파일 편집
su -c "nano /etc/hosts"


# 시스템 로그 확인
su -c "tail -f /var/log/syslog"
```

### 개발 작업 흐름

```bash
# 배포를 위해 애플리케이션 사용자로 전환
su - appuser -c "cd /opt/myapp && ./deploy.sh"


# 특정 사용자의 권한으로 애플리케이션 실행
su - www-data -c "/usr/bin/php /var/www/html/script.php"


# 다른 사용자의 권한으로 권한 테스트
su - testuser -c "ls -la /shared/directory"
```

### 사용자 관리 작업

```bash
# 특정 사용자의 권한으로 파일 생성
su - username -c "touch /home/username/newfile.txt"


# 사용자의 환경 확인
su - username -c "env | sort"


# 사용자의 셸 설정 실행
su - username -c "source ~/.bashrc && echo \$PATH"
```

## 보안 고려 사항

### 비밀번호 요구 사항

```bash
# su는 대상 사용자의 비밀번호를 요구합니다.
su username  # username 사용자의 비밀번호 필요


# root는 비밀번호 없이 모든 사용자로 전환할 수 있습니다.
sudo su - username  # sudo 인증 사용


# su를 사용할 수 있는 사용자 확인
grep su /etc/group
```

### 감사 및 로깅

```bash
# 로그에서 su 사용 기록 확인
sudo grep su /var/log/auth.log


# 현재 su 세션 모니터링
w
who


# 로그인 기록 확인
last
```

### 안전한 사용 패턴

```bash
# 관리 작업에는 항상 로그인 셸 사용
su -  # 단순히 'su'를 사용하는 것보다 권장


# 가능한 경우 su 대신 sudo 사용
sudo command  # 'su -c command'보다 권장


# root 권한 사용 시간을 제한
su -c "command1 && command2 && exit"
```

## sudo와의 비교

### `su`를 사용하는 경우

```bash
# 여러 관리 명령어 실행
su -
# root 권한으로 여러 명령어 실행
exit


# 대화형 root 세션
su -
# 일정 시간 동안 root 권한으로 작업
```

### `sudo`를 사용하는 경우

```bash
# 단일 명령어 실행
sudo systemctl restart service


# 더 나은 보안 및 로깅
sudo -u username command


# 일시적인 권한 상승
sudo apt update && sudo apt upgrade
```

## 설정 및 사용자 지정

### PAM 설정

```bash
# su에 대한 PAM 설정 확인
cat /etc/pam.d/su


# wheel 그룹으로 su 사용 제한 (일부 배포판)
# /etc/pam.d/su를 편집하고 다음 줄의 주석을 해제합니다.
# auth required pam_wheel.so use_uid
```

### 셸 설정

```bash
# 셸이 허용되는지 확인
grep username /etc/passwd
cat /etc/shells


# 사용자의 셸 설정
sudo chsh -s /bin/bash username
```

### 환경 사용자 지정

```bash
# 로그인 환경 사용자 지정
# ~/.profile, ~/.bashrc 또는 ~/.bash_profile 편집


# su 세션에 사용할 특정 환경 설정
# ~/.surc를 생성하거나 셸 설정을 수정
```

## 문제 해결

### 일반적인 문제

```bash
# 인증 실패
su: Authentication failure
# 비밀번호, 사용자 존재 여부 및 계정 상태 확인


# 권한 거부
su: Permission denied
# PAM 설정 및 wheel 그룹 구성원 여부 확인


# 허용되지 않은 셸
su: Warning: shell not allowed
# 셸을 /etc/shells에 추가하거나 -s 옵션 사용
```

### 디버깅

```bash
# 사용자 계정 상태 확인
sudo passwd -S username


# 사용자 존재 여부 확인
id username
grep username /etc/passwd


# 그룹 구성원 확인
groups username


# 자세한 출력을 사용하여 테스트
su -v username
```
## 스크립트 통합

### 스크립트에서 `su` 사용

```bash
#!/bin/bash
# 다른 사용자 권한으로 명령어를 실행하는 스크립트


if [ "$EUID" -ne 0 ]; then
    echo "root 권한으로 실행하십시오."
    exit 1
fi


# appuser로 전환하여 명령어 실행
su - appuser -c "
    cd /opt/myapp
    ./backup.sh
    ./cleanup.sh
"
```

### 자동화된 작업

```bash
# 특정 사용자로 실행되는 cron 작업
# root의 crontab에 추가:
# 0 2 * * * su - backupuser -c '/usr/local/bin/backup.sh'


# 사용자 권한으로 실행되는 시스템 서비스
su - serviceuser -c '/opt/service/start.sh' &
```

## 옵션 참조

| **옵션** | **긴 형식** | **설명** |
|:---|:---|:---|
| `-` | `--login` | 로그인 셸을 시작하고 사용자의 환경을 불러옵니다. |
| `-c CMD` | `--command=CMD` | 명령어를 실행한 후 종료합니다. |
| `-f` | `--fast` | 셸에 `-f`를 전달합니다. (`csh`/`tcsh`용) |
| `-g GRP` | `--group=GRP` | 기본 그룹을 지정합니다. |
| `-G GRP` | `--supp-group=GRP` | 보조 그룹을 지정합니다. |
| `-l` | `--login` | `-` 옵션과 동일합니다. |
| `-m` | `--preserve-environment` | 환경 변수를 초기화하지 않습니다. |
| `-p` | `--preserve-environment` | `-m` 옵션과 동일합니다. |
| `-s SHL` | `--shell=SHL` | 지정한 셸을 사용합니다. |
| `-w VAR` | `--whitelist-environment=VAR` | 지정한 변수를 초기화하지 않습니다. |
| `--help` | - | 도움말 메시지를 표시합니다. |
| `--version` | - | 버전 정보를 표시합니다. |

## 모범 사례

### 보안 모범 사례

- 더 나은 로깅을 위해 가능한 경우 `su` 대신 `sudo`를 사용합니다.
- 관리 작업에는 항상 로그인 셸(`su -`)을 사용합니다.
- root 사용자로 작업하는 시간을 제한합니다.
- 가능한 경우 대화형 세션보다 특정 명령어를 사용합니다.
- 시스템 로그를 통해 `su` 사용 기록을 정기적으로 감사합니다.

### 운영 모범 사례

- 스크립트에서 사용자를 전환할 때 설명이 포함된 주석을 작성합니다.
- 사용자 전환을 시도하기 전에 해당 사용자가 존재하는지 확인합니다.
- 스크립트에서 인증 실패를 적절히 처리합니다.
- 시스템 문서에 사용자 전환 요구 사항을 기록합니다.

## 중요 참고 사항

- `su`는 대상 사용자의 비밀번호를 요구합니다. 단, root 권한으로 실행하는 경우는 예외입니다.
- `su -`는 깨끗한 환경을 제공하므로 관리 작업에 권장됩니다.
- `su` 세션은 `/var/log/auth.log` 또는 이와 유사한 시스템 로그에 기록됩니다.
- 일부 시스템에서는 `wheel` 그룹 제한이 활성화되어 있을 수 있습니다.
- 작업을 마치면 항상 `su` 세션을 종료하여 원래 사용자로 돌아가십시오.

`su` 명령어는 Linux 시스템에서 사용자 전환과 권한 관리에 필수적이지만 적절한 보안 고려 사항을 적용하여 신중하게 사용해야 합니다.

자세한 내용은 다음 매뉴얼을 확인하십시오.

```bash
man su
```

# `wget` 명령어

`wget` 명령어는 인터넷에서 파일을 다운로드할 때 사용합니다. HTTP, HTTPS 및 FTP 프로토콜을 사용한 파일 다운로드를 지원합니다. 여러 파일을 한 번에 다운로드하거나, 백그라운드에서 다운로드하거나, 다운로드를 재개하거나, 대역폭을 제한하거나, 웹 사이트를 미러링하는 등 다양한 작업을 수행할 수 있습니다.

## 구문(Syntax)

`wget` 구문에서는 다운로드 옵션과 다운로드할 파일의 URL을 지정해야 합니다.

```bash
$ wget [options] [URL]
```

### 예시

이 예시에서는 서로 다른 소스에서 Ubuntu 20.04 데스크톱 ISO 파일을 다운로드합니다. 터미널을 열고 다음 `wget` 명령어를 입력합니다. 그러면 다운로드가 시작됩니다. 다운로드가 완료되기까지 몇 분이 걸릴 수 있습니다.

1. 일반적인 다운로드 시작

```bash
wget https://releases.ubuntu.com/20.04/ubuntu-20.04.3-desktop-amd64.iso
```

2. `-c` 옵션을 사용하여 다운로드를 재개할 수 있습니다.

```bash
wget -c https://mirrors.piconets.webwerks.in/ubuntu-mirror/ubuntu-releases/20.04.3/ubuntu-20.04.3-desktop-amd64.iso
```

3. 백그라운드에서 다운로드하려면 `-b` 옵션을 사용합니다.

```bash
wget -b https://mirrors.piconets.webwerks.in/ubuntu-mirror/ubuntu-releases/20.04.3/ubuntu-20.04.3-desktop-amd64.iso
```

## 추가 옵션

다운로드 외에도 `wget`은 여러 파일 다운로드, 백그라운드 다운로드, 다운로드 대역폭 제한, 중단된 다운로드 재개 등 다양한 기능을 제공합니다. 모든 `wget` 옵션은 매뉴얼 페이지에서 확인할 수 있습니다.

```bash
man wget
```

### 추가 플래그 및 기능

| **짧은 옵션** | **설명** |
|:---|:---|
| `-v` | 시스템에서 사용할 수 있는 `wget`의 버전을 출력합니다. |
| `-h` | 사용 가능한 모든 옵션을 표시하는 도움말 메시지를 출력합니다. |
| `-b` | 프로세스가 시작되는 즉시 백그라운드로 보냅니다. |
| `-t` | 재시도 횟수를 지정한 횟수로 설정합니다. |
| `-c` | 부분적으로 다운로드된 파일의 다운로드를 재개합니다. |

# `curl` 명령어

Linux에서 `curl`은 HTTP, HTTPS 및 FTP를 포함한 다양한 프로토콜을 사용하여 서버와 데이터를 주고받는 강력한 명령줄 도구입니다. API 테스트, 파일 다운로드 및 웹 관련 작업 자동화에 자주 사용됩니다.

## `curl` 명령어의 구문

```bash
$ curl [options...] <url>
```

이 명령어는 `example.com` 홈페이지의 소스 코드를 터미널 창에 출력합니다.

## 일반 옵션

`curl`에는 200개가 넘는 옵션이 있습니다. 다음은 가장 일반적이고 유용한 옵션입니다.

| **옵션** | **긴 형식** | **설명** |
|:---|:---|:---|
| `-O` | `--remote-name` | 파일을 다운로드하고 원격 파일과 동일한 이름으로 저장합니다. |
| `-o <file>` | `--output <file>` | 다운로드한 출력을 지정한 파일 이름으로 저장합니다. |
| `-L` | `--location` | 서버가 요청한 페이지가 이동했다고 응답하면 리디렉션을 따라갑니다. |
| `-X <METHOD>` | `--request <METHOD>` | 사용할 HTTP 요청 메서드를 지정합니다. (예: `POST`, `PUT`, `DELETE`) |
| `-H <header>` | `--header <header>` | 요청에 사용자 지정 HTTP 헤더를 추가할 수 있습니다. |

## 예시

### 1. 웹 페이지의 소스 코드 보기 {.unnumbered}

`curl`의 가장 간단한 사용법입니다. URL에서 콘텐츠를 가져와 HTML 소스 코드를 터미널에 직접 출력합니다.

```bash
$ curl example.com
```

### 2. 파일 다운로드 {.unnumbered}

`-O` 플래그는 파일을 다운로드할 때 사용합니다. `curl`은 원격 파일과 동일한 이름으로 현재 디렉터리에 저장합니다.

```bash
$ curl -O https://github.com/bobbyiliev/101-linux-commands/archive/refs/tags/v1.0.zip
```

### 3. 파일을 다운로드하고 이름 변경 {.unnumbered}

`-o` 플래그를 사용하면 다운로드한 파일의 새 이름을 지정할 수 있습니다.

```bash
$ curl -o linux-commands.zip https://github.com/bobbyiliev/101-linux-commands/archive/refs/tags/v1.0.zip
```

## 설치

`curl` 명령어는 대부분의 Linux 배포판에 포함되어 있습니다. 그러나 시스템에 `curl`이 기본으로 설치되어 있지 않다면 직접 설치해야 합니다. `curl`을 설치하려면 다음 명령어를 실행합니다.

먼저 다음 명령어를 실행하여 시스템을 업데이트합니다.

```bash
$ sudo apt update
$ sudo apt upgrade
```

이제 다음 명령어를 실행하여 `curl` 유틸리티를 설치합니다.

```bash
$ sudo apt install curl
```

다음 명령어를 실행하여 설치를 확인합니다.

```bash
$ curl -version
```

위 명령어는 설치된 `curl` 명령어의 버전을 표시합니다.

# `yes` 명령어

Linux에서 `yes` 명령어는 지정된 _문자열(STRING)_을 계속해서 출력할 때 사용합니다. _문자열(STRING)_을 지정하지 않으면 `y`를 출력합니다. 이 명령어는 종료될 때까지 문자열을 반복해서 출력합니다. (예: `Ctrl + C` 사용)

### 예시

1. 종료할 때까지 터미널에 `hello world`를 무한히 출력합니다.

```bash
yes hello world
```

2. 보다 일반적인 구문:

```bash
yes [STRING]
```

## 옵션

다음 옵션을 사용할 수 있습니다.

1. `--help`

   > 이 도움말을 표시한 후 종료합니다.

2. `--version`

   > 버전 정보를 출력한 후 종료합니다.

# `last` 명령어

이 명령어는 `/var/log/wtmp` 파일이 생성된 이후 로그인하고 로그아웃한 모든 사용자의 목록을 표시합니다. 특정 사용자가 언제 로그인했는지, 얼마나 오래 로그인해 있었는지 등을 보여 주는 여러 매개변수도 사용할 수 있습니다.

최근 5개의 로그를 확인하려면 다음과 같이 명령어에 `-5`를 추가합니다.

```bash
last -5
```

최근 10개의 로그를 확인하려면 `-10`을 추가합니다.

로그인 및 로그아웃 시간과 날짜를 함께 확인하려면 `-F` 옵션을 사용할 수 있습니다.

```bash
last -F
```

이 명령어를 사용하면 다양한 정보를 확인할 수 있습니다. 자세한 내용을 확인하려면 다음 명령어를 실행합니다.

```bash
last --help
```
# `locate` 명령어

`locate` 명령어는 `updatedb` 명령어가 생성한 데이터베이스 파일을 사용하여, 지정된 패턴과 이름이 일치하는 파일과 디렉터리를 파일 시스템에서 검색합니다.

### 예시

1. 이름이 `.bashrc`인 파일을 검색하려면 다음과 같이 실행합니다.

```bash
locate .bashrc
```

*출력*

```text
/etc/bash.bashrc
/etc/skel/.bashrc
/home/linuxize/.bashrc
/usr/share/base-files/dot.bashrc
/usr/share/doc/adduser/examples/adduser.local.conf.examples/bash.bashrc
/usr/share/doc/adduser/examples/adduser.local.conf.examples/skel/dot.bashrc
```

일반 사용자로 명령어를 실행했으며 `/root` 디렉터리에 접근할 권한이 없기 때문에 `/root/.bashrc` 파일은 표시되지 않습니다.

결과 목록이 긴 경우 가독성을 높이기 위해 출력을 [`less`](https://linuxize.com/post/less-command-in-linux/) 명령어로 파이프할 수 있습니다.

```bash
locate .bashrc | less
```

2. 시스템에서 모든 `.md` 파일을 검색합니다.

```bash
locate *.md
```

3. 모든 `.py` 파일을 검색하고 결과를 10개만 표시합니다.

```bash
locate -n 10 *.py
```

4. 대소문자를 구분하지 않고 검색합니다.

```bash
locate -i readme.md
```

*출력*

```text
/home/linuxize/p1/readme.md
/home/linuxize/p2/README.md
/home/linuxize/p3/ReadMe.md
```

5. 이름에 `.bashrc`가 포함된 모든 파일의 개수를 반환합니다.

```bash
locate -c .bashrc
```

*출력*

```text
6
```

6. 다음 명령어는 파일 시스템에 실제로 존재하는 `.json` 파일만 반환합니다.

```bash
locate -e *.json
```

7. 더 복잡한 검색을 수행하려면 `-r`(`--regexp`) 옵션을 사용합니다.

시스템에서 모든 `.mp4` 및 `.avi` 파일을 검색하고 대소문자를 무시합니다.

```bash
locate --regex -i "(\.mp4|\.avi)"
```

### 문법

```bash
locate [OPTION]... PATTERN...
```

### 추가 플래그 및 기능

| **짧은 옵션** | **긴 옵션** | **설명** |
|:---|:---|:---|
| `-A` | `--all` | 지정된 패턴 중 하나만 일치하면 되는 대신, 모든 패턴과 일치하는 항목만 표시합니다. |
| `-b` | `--basename` | 지정된 패턴을 기본 이름에 대해서만 일치시킵니다. |
| `-c` | `--count` | 표준 출력에 파일 이름 대신 일치하는 항목의 개수를 출력합니다. |
| `-d` | `--database DBPATH` | 기본 데이터베이스를 `DBPATH`로 대체합니다. |
| `-e` | `--existing` | 명령어가 실행되는 동안 실제로 존재하는 파일을 가리키는 항목만 표시합니다. |
| `-L` | `--follow` | `--existing` 옵션이 지정된 경우 파일의 존재 여부를 확인하고 끝에 있는 심볼릭 링크를 따라갑니다. 끊어진 심볼릭 링크는 출력에서 제외합니다. 이것이 기본 동작입니다. 반대 동작은 `--nofollow` 옵션으로 지정할 수 있습니다. |
| `-h` | `--help` | 사용 가능한 옵션의 요약이 포함된 도움말 문서를 표시합니다. |
| `-i` | `--ignore-case` | 지정된 패턴을 검색할 때 대소문자를 구분하지 않습니다. |
| `-p` | `--ignore-spaces` | 패턴을 일치시킬 때 구두점과 공백을 무시합니다. |
| `-t` | `--transliterate` | 패턴을 일치시킬 때 `iconv` 음역 기능을 사용하여 악센트를 무시합니다. |
| `-l` | `--limit`, `-n LIMIT` | 이 옵션을 지정하면 `LIMIT`개의 항목을 찾은 후 명령어가 성공적으로 종료됩니다. |
| `-m` | `--mmap` | BSD 및 GNU `locate`와의 호환성을 무시합니다. |
| `-0` | `--null` | 각 항목을 별도의 행에 출력하는 대신 ASCII NUL 문자를 사용하여 출력 항목을 구분합니다. |
| `-S` | `--statistics` | 파일을 검색하는 대신 읽은 각 데이터베이스에 대한 통계를 표준 출력에 출력합니다. |
| `-r` | `--regexp REGEXP` | 기본 정규 표현식 `REGEXP`를 사용하여 검색합니다. |
| `--regex` | - | 모든 `PATTERN`을 확장 정규 표현식으로 처리합니다. |
| `-V` | `--version` | 버전 및 라이선스 정보를 표시합니다. |
| `-w` | `--wholename` | 지정된 패턴을 전체 경로 이름에 대해서만 일치시킵니다. |

# `iostat` 명령어

Linux에서 `iostat` 명령어는 장치와 파티션의 시스템 입출력 통계를 모니터링할 때 사용합니다. 장치의 활성 시간과 평균 전송 속도를 관찰하여 시스템 입출력을 모니터링합니다.

`iostat`가 생성하는 보고서는 물리 디스크 간 입출력 균형을 높이도록 시스템 설정을 변경하는 데 사용할 수 있습니다. `iostat`는 `sysstat` 패키지에 포함되어 있습니다. 설치되어 있지 않다면 먼저 설치해야 합니다.

### 문법:

```[linux]
iostat [ -c ] [ -d ] [ -h ] [ -N ] [ -k | -m ] [ -t ] [ -V ] [ -x ]
       [ -z ] [ [ [ -T ] -g group_name ] { device [...] | ALL } ]
       [ -p [ device [,...] | ALL ] ] [ interval [ count ] ]
```

### 예시:

1. 부팅 이후 전체 CPU 및 장치 통계 1회 출력:

```bash
iostat -d 2
```

2. 2초 간격으로 장치 보고서 연속 출력:

```bash
iostat -d 2 6
```

3. 모든 장치에 대해 2초 간격으로 6회 통계 출력:

```bash
iostat -x sda sdb 2 6
```

4. sda 및 sdb 장치에 대해 2초 간격으로 확장 통계 6회 출력:

```bash
iostat -p sda 2 6
```

### 추가 플래그와 기능:

| **짧은 플래그**                  | **설명**                                            |
| :------------------------------ | :--------------------------------------------------------- |
| `-x`                            | 더 자세한 통계 정보를 표시합니다.                  |
| `-c`                            | CPU 통계만 표시합니다.                               |
| `-d`                            | 장치 보고서만 표시합니다                             |
| `-xd                            | 장치에 대한 확장 I/O 통계만 표시합니다.               |
| `-k`                            | 통계를 킬로바이트 또는 메가바이트 단위로 수집합니다.          |
| `-k23`                          | 지연 간격을 두고 CPU 및 장치 통계를 표시합니다.              |
| `-j ID mmcbkl0 sda6 -x -m 2 2`  | 영속적인 장치 이름 통계를 표시합니다.                 |
| `-p `                           | 블록 장치에 대한 통계를 표시합니다.                      |
| `-N `                           | LVM2 통계 정보를 표시합니다.                       |
# `sudo` 명령어

`sudo`(“substitute user do” 또는 “super user do”) 명령어는 적절한 권한을 가진 사용자가 슈퍼유저와 같은 다른 사용자의 권한으로 명령어를 실행할 수 있도록 합니다.

Windows의 “관리자 권한으로 실행” 옵션과 동일한 기능입니다. `sudo` 명령어를 사용하면 현재 사용자 계정에 root 권한을 일시적으로 부여할 수 있습니다. `sudo`의 root 권한은 일시적으로만 유효합니다. 해당 시간이 만료되면 root 권한을 다시 얻기 위해 비밀번호를 입력해야 합니다.

> **경고:** `sudo` 명령어를 사용할 때는 매우 주의하십시오. root 권한으로 작업하는 동안 되돌릴 수 없고 치명적인 변경을 일으킬 수 있습니다!

### 구문

```bash
sudo [-OPTION] command
```

### 추가 플래그 및 기능

| **플래그** | **설명** |
|:---|:---|
| `-V` | `-V`(버전) 옵션은 `sudo`가 버전 번호를 출력한 후 종료하도록 합니다. 명령어를 실행한 사용자가 이미 root인 경우, `-V` 옵션은 `sudo`가 컴파일될 때 사용된 기본 설정과 시스템의 로컬 네트워크 주소 목록을 출력합니다. |
| `-l` | `-l`(목록) 옵션은 현재 호스트에서 사용자에게 허용되거나 금지된 명령어를 출력합니다. |
| `-L` | `-L`(기본 설정 목록) 옵션은 `Defaults` 행에 설정된 매개변수와 각 매개변수에 대한 간단한 설명을 목록으로 표시합니다. 이 옵션은 `grep`과 함께 사용하면 유용합니다. |
| `-h` | `-h`(도움말) 옵션은 사용법 메시지를 출력한 후 종료합니다. |
| `-v` | `-v`(검증) 옵션을 지정하면 `sudo`가 사용자의 타임스탬프를 갱신하고 필요한 경우 사용자의 비밀번호를 요청합니다. 이를 통해 sudo 제한 시간이 5분(또는 `sudoers`에 설정된 제한 시간) 더 연장되지만 명령어는 실행하지 않습니다. |
| `-K` | `-K`(완전 삭제) 옵션은 사용자의 타임스탬프를 완전히 제거합니다. 이 옵션 역시 비밀번호를 요구하지 않습니다. |
| `-u` | `-u`(사용자) 옵션은 지정된 명령어를 root가 아닌 다른 사용자의 권한으로 실행합니다. 사용자 이름 대신 UID를 지정하려면 `#uid` 형식을 사용합니다. |
| `-s` | `-s`(셸) 옵션은 `SHELL` 환경 변수가 설정되어 있으면 해당 변수에 지정된 셸을 실행하고, 그렇지 않으면 `passwd` 파일에 지정된 셸을 실행합니다. |
| `--` | `--` 플래그는 `sudo`가 명령줄 인수 처리를 중단하도록 합니다. `-s` 플래그와 함께 사용할 때 가장 유용합니다. |

## 예제

다음 명령어는 명령 프롬프트를 root 사용자의 BASH 셸로 전환합니다.

```bash
sudo bash
```

명령줄 프롬프트가 다음과 같이 변경됩니다.

```bash
root@hostname:/home/[username]
```

파일을 편집기로 열지 않고 파일에 문자열을 추가하는 방식은 소프트웨어 저장소 이름을 소스 파일에 추가할 때 자주 사용됩니다. `echo`, `sudo`, `tee` 명령어를 다음 구문과 같이 사용합니다.

```bash
echo ‘string-of-text’ | sudo tee -a [path_to_file]
```

예제:

```bash
echo "deb http://nginx.org/packages/debian `lsb_release -cs` nginx" \ | sudo tee /etc/apt/sources.list.d/nginx.list
```

# `apt` 명령어

`apt`(Advanced Package Tool) 명령어는 Debian에서 사용하는 패키징 시스템인 `dpkg`와 상호 작용할 때 사용합니다. `.deb` 패키지를 관리하는 `dpkg` 명령어가 이미 있지만 `apt`가 더 사용자 친화적이고 효율적인 방법입니다.

간단히 말해 `apt`는 Debian 기반 Linux에서 패키지를 설치, 삭제하고 기타 작업을 수행할 때 사용하는 명령어입니다.

대부분의 경우 `apt` 명령어를 `sudo` 권한과 함께 사용합니다.

### 패키지 설치

`apt`에서 `install` 뒤에 `package_name`을 지정하면 새 패키지를 설치할 수 있습니다.

#### 구문

```bash
sudo apt install package_name
```

#### 예제

```bash
sudo apt install g++
```

이 명령어는 시스템에 `g++`를 설치합니다.

### 패키지 제거

`apt`에서 `remove` 뒤에 `package_name`을 지정하면 특정 패키지를 제거할 수 있습니다.

#### 구문

```bash
sudo apt remove package_name
```

#### 예제

```bash
sudo apt remove g++
```

이 명령어는 시스템에서 `g++`를 제거합니다.

### 패키지 검색

`apt`에서 `search` 뒤에 `package_name`을 지정하면 모든 저장소에서 패키지를 검색할 수 있습니다.

#### 구문

```bash
apt search package_name
```

참고: `sudo`는 필요하지 않습니다.

#### 예제

```bash
apt search g++
```

### 사용하지 않는 패키지 제거

시스템에 다른 패키지에 의존하는 새 패키지를 설치하면 해당 패키지의 의존성도 함께 설치됩니다. 패키지를 제거해도 의존성 패키지는 시스템에 남습니다. 이러한 남은 패키지가 다른 패키지에서 더 이상 사용되지 않는다면 제거할 수 있습니다.

#### 구문

```bash
sudo apt autoremove
```

이 명령어는 시스템에서 사용되지 않는 모든 패키지를 제거합니다.

### 패키지 인덱스 업데이트

`apt` 패키지 인덱스는 시스템에서 활성화된 패키지에 대한 정보를 저장하는 데이터베이스입니다.

#### 구문

```bash
sudo apt update
```

이 명령어는 시스템의 패키지 인덱스를 업데이트합니다.

### 패키지 업그레이드

설치된 패키지에 대한 최신 업데이트를 설치하려면 다음 명령어를 실행할 수 있습니다.

#### 구문

```bash
sudo apt upgrade
```

이 명령어는 설치된 패키지를 제거해야 하는 경우 해당 패키지를 업그레이드하지 않습니다.

특정 패키지 하나만 업그레이드하려면 패키지 이름을 지정합니다.

#### 구문

```bash
sudo apt upgrade package_name
```

이 명령어는 지정한 패키지를 최신 버전으로 업그레이드합니다.

# `yum` 명령어

`yum` 명령어는 Red Hat Enterprise Linux에서 소프트웨어 패키지를 설치, 업데이트, 제거 및 관리할 때 사용하는 기본 패키지 관리 도구입니다. `yum`은 _`Yellow Dog Updater, Modified`_의 약자입니다.

`yum`은 소프트웨어 패키지를 설치, 업데이트 및 제거할 때 의존성을 해결합니다. 시스템에 설치된 저장소의 패키지나 `.rpm` 패키지를 관리할 수 있습니다.

### 구문

```bash
yum -option command
```

### 예제

1. 과거 트랜잭션에서 발생한 작업의 개요를 확인합니다.

```bash
yum history
```

2. 이전 트랜잭션을 되돌립니다.

```bash
yum history undo <id>
```

3. 모든 확인 요청에 `yes`로 응답하면서 `firefox` 패키지를 설치합니다.

```bash
yum -y install firefox
```

4. `mysql` 패키지를 최신 안정 버전으로 업데이트합니다.

```bash
yum update mysql
```

### `yum`과 함께 자주 사용하는 명령어

| **명령어** | **설명** |
|:---|:---|
| `install` | 지정한 패키지를 설치합니다. |
| `remove` | 지정한 패키지를 제거합니다. |
| `search` | 키워드로 패키지 메타데이터를 검색합니다. |
| `info` | 설명을 표시합니다. |
| `update` | 각 패키지를 최신 버전으로 업데이트합니다. |
| `repolist` | 저장소 목록을 표시합니다. |
| `history` | 과거 트랜잭션에서 발생한 작업을 표시합니다. |
| `groupinstall` | 특정 패키지 그룹을 설치합니다. |
| `clean` | 활성화된 저장소에서 캐시된 모든 파일을 삭제합니다. |

### 추가 플래그 및 기능

| **짧은 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-C` | `--cacheonly` | 시스템 캐시만 사용하여 실행합니다. 캐시를 업데이트하지 않으며 캐시가 만료된 경우에도 사용합니다. |
| - | `--security` | 보안 문제를 해결하는 패키지를 포함합니다. `upgrade` 명령어에 적용됩니다. |
| `-y` | `--assumeyes` | 모든 질문에 자동으로 `yes`라고 응답합니다. |
| - | `--skip-broken` | 문제를 일으키는 패키지를 트랜잭션에서 제거하여 의존성 해결 문제를 처리합니다. 값이 `False`인 엄격한 설정 옵션의 별칭입니다. |
| `-v` | `--verbose` | 자세한 작업을 수행하며 디버그 메시지를 표시합니다. |
# `zip` 명령어

`zip` 명령어는 파일을 압축하여 크기를 줄일 때 사용합니다. 하나 이상의 압축된 파일이나 디렉터리가 포함된 아카이브를 생성합니다.

### 예제

`zip` 명령어로 단일 파일을 압축하는 구문은 다음과 같습니다.

```bash
zip myZipFile.zip filename.txt
```

여러 파일에도 동일한 방법을 사용할 수 있습니다.

```bash
zip multipleFiles.zip file1.txt file2.txt
```

전체 디렉터리를 압축하는 경우에는 `-r` 플래그를 추가해야 합니다.

```bash
zip -r zipFolder.zip myFolder/
```

### 구문

```bash
zip [OPTION] zipFileName filesList
```

### 사용 가능한 옵션

| **플래그** | **설명** |
|:---|:---|
| `-d` | ZIP 아카이브에서 파일을 제거합니다. ZIP 파일을 생성한 후에도 `-d` 옵션을 사용하여 아카이브에서 파일을 삭제할 수 있습니다. |
| `-u` | ZIP 아카이브의 파일을 업데이트합니다. 지정한 파일 목록을 업데이트하거나 기존 ZIP 파일에 새 파일을 추가할 때 사용할 수 있습니다. ZIP 아카이브에 있는 기존 항목보다 수정 시간이 더 최근인 경우에만 해당 항목을 업데이트합니다. |
| `-m` | 압축한 후 원본 파일을 삭제합니다. |
| `-r` | 디렉터리를 재귀적으로 압축합니다. 지정한 디렉터리에 있는 모든 파일을 압축할 때 유용합니다. |
| `-x` | ZIP 파일을 생성할 때 지정한 파일을 제외합니다. |
| `-v` | 자세한 정보 표시 모드 또는 진단용 버전 정보 출력 모드입니다. 일반 작업에 적용하면 압축 중 진행률 표시기를 보여 주며 ZIP 파일 구조의 특이점에 대한 자세한 진단 정보를 출력합니다. |

# `unzip` 명령어

`unzip` 명령어는 지정된 ZIP 아카이브의 모든 파일을 현재 디렉터리에 추출합니다.

### 예제

파일을 추출하는 구문은 다음과 같습니다.

```bash
unzip myZipFile.zip
```

현재 디렉터리가 아닌 다른 디렉터리에 ZIP 파일을 압축 해제하려면 `-d` 플래그를 추가해야 합니다.

```bash
unzip myZipFile.zip -d /path/to/directory
```

ZIP 파일을 압축 해제할 때 특정 파일이나 디렉터리를 제외하려면 `-x` 플래그를 추가합니다.

```bash
unzip myZipFile.zip -x file1.txt file2.txt
```

### 구문

```bash
unzip zipFileName [OPTION] [PARAMS]
```

### 사용 가능한 옵션

| **플래그** | **설명** | **매개변수** |
|:---|:---|:---|
| `-d` | 아카이브를 다른 디렉터리에 압축 해제합니다. | `/path/to/directory` |
| `-x` | 아카이브를 추출하되 지정한 파일은 추출하지 않습니다. | `filename(s)` |
| `-j` | ZIP 아카이브에 폴더 구조가 포함되어 있어도 새 폴더를 생성하지 않고 압축 해제합니다. | - |
| `-l` | 아카이브 파일의 내용을 추출하지 않고 목록으로 표시합니다. | - |
| `-n` | 기존 파일을 덮어쓰지 않고 다른 파일 이름을 사용합니다. | - |
| `-o` | 파일을 덮어씁니다. | - |
| `-P` | 암호로 보호된 아카이브 파일의 압축을 해제하기 위한 비밀번호를 제공합니다. | `password` |
| `-q` | 표준 출력에 상태 메시지를 출력하지 않고 압축을 해제합니다. | - |
| `-t` | 아카이브 파일이 유효한지 테스트합니다. | - |
| `-v` | 압축을 해제하지 않고 아카이브에 대한 자세한 정보를 표시합니다. | - |

# `shutdown` 명령어

`shutdown` 명령어를 사용하면 안전한 방식으로 시스템을 종료할 수 있습니다. `shutdown`을 실행하면 시스템이 로그인한 모든 사용자에게 알리고 추가 로그인을 차단합니다.

시스템을 즉시 종료하거나 특정 시간이 지난 후 종료하도록 설정할 수 있습니다.

root 권한 또는 `sudo` 권한이 있는 사용자만 `shutdown` 명령어를 사용할 수 있습니다.

### 예제

1. 시스템을 즉시 종료합니다.

```bash
sudo shutdown now
```

2. 10분 후 시스템을 종료합니다.

```bash
sudo shutdown +10
```

3. 5분 후 메시지를 표시하면서 시스템을 종료합니다.

```bash
sudo shutdown +5 "System will shutdown in 5 minutes"
```

### 구문

```bash
shutdown [OPTIONS] [TIME] [MESSAGE]
```

### 추가 플래그 및 기능

| **짧은 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-r` | - | 시스템을 재부팅합니다. |
| `-c` | - | 예약된 시스템 종료를 취소합니다. |

# `dir` 명령어

`dir` 명령어는 디렉터리의 내용을 나열합니다. 기본적으로 현재 디렉터리의 내용을 표시합니다. **내용을 나열하는 형식에서 `ls` 명령어와 차이가 있습니다.** 기본적으로 `dir` 명령어는 파일과 폴더를 열 단위로 세로 방향으로 정렬하여 표시하며 특수 문자는 백슬래시 이스케이프 시퀀스로 나타냅니다.

### 구문

```bash
dir [OPTIONS] [FILE]
```

### 예제

1. 현재 디렉터리의 파일을 나열합니다.

```bash
dir
```

2. 현재 디렉터리의 숨김 파일도 나열합니다.

```bash
dir -a
```

3. 각 항목에 대한 자세한 정보와 함께 내용을 나열합니다.

```bash
dir -l
```

### 추가 플래그 및 기능

| **짧은 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-a` | `--all` | 숨김 파일(`.`으로 시작하는 파일)을 모두 표시하며 `.`과 `..`으로 표시되는 두 항목도 함께 표시합니다. |
| `-A` | `--almost-all` | **`-a` 옵션과 유사**하지만 현재 디렉터리와 이전 디렉터리를 나타내는 항목은 표시하지 않습니다. |
| `-l` | - | 각 항목에 대한 자세한 정보를 표시합니다. |
| `-s` | `--size` | 각 파일에 할당된 크기를 블록 단위로 출력합니다. |
| `-h` | `--human-readable` | `-l` 및 `-s`와 함께 사용하며 파일 크기를 `1K`, `2M`과 같이 사람이 읽기 쉬운 형식으로 표시합니다. |
| `-F` | - | 뒤에 추가되는 기호(`/`, `*`, `@`, `%`, `=`)를 기준으로 항목의 유형을 표시합니다. |
| `-v` | `--verbose` | 원본 파일과 대상 파일을 출력합니다. |
| - | `--group-directories-first` | 파일보다 디렉터리를 먼저 모아서 표시합니다. |
| `-R` | `--recursive` | 하위 디렉터리를 재귀적으로 나열합니다. |
| `-S` | - | 파일 크기를 기준으로 정렬하며 가장 큰 파일부터 표시합니다. |
| `-d` | `--directory` | 디렉터리의 내용을 표시하는 대신 디렉터리 항목 자체를 나열합니다. |

# `reboot` 명령어

`reboot` 명령어는 Linux 시스템을 재시작할 때 사용합니다. 그러나 [sudo](https://github.com/bobbyiliev/101-linux-commands/blob/main/ebook/en/content/051-the-sudo-command.md) 명령어를 사용하여 상승된 권한을 부여받아야 합니다. 일반적으로 시스템 또는 네트워크에 중요한 업데이트를 적용한 후 이 명령어를 사용해야 하는 경우가 생깁니다.

## 구문

```
reboot [OPTIONS...]
```
### 옵션

- **`--help`**: 간단한 도움말을 출력한 후 종료합니다.
- **`--halt`**: 시스템을 중지합니다.
- **`-w`, `--wtmp-only`**: `wtmp`에 시스템 종료 항목만 기록하며 실제로 시스템을 중지하거나 전원을 끄거나 재부팅하지는 않습니다.

### 예제

1. 기본 사용법. 주로 추가 정보 없이 시스템을 재시작할 때 사용합니다.

```bash
$ sudo reboot
```

또는 `-r` 옵션과 함께 `shutdown` 명령어를 사용할 수도 있습니다.

```bash
$ sudo shutdown -r now
```

**참고:** `reboot`, `halt`, `poweroff` 명령어는 구문과 동작이 거의 유사합니다. 각 명령어를 `--help`와 함께 실행하면 자세한 내용을 확인할 수 있습니다.

2. `reboot` 명령어의 사용 범위는 제한적입니다. 따라서 더욱 고급화된 재부팅 및 종료 요구 사항을 충족하기 위해 `reboot` 대신 `shutdown` 명령어를 사용합니다. 예약된 재시작이 그중 하나입니다. 구문은 다음과 같습니다.

```bash
$ sudo shutdown -r [TIME] [MESSAGE]
```

여기서 `TIME`은 여러 형식으로 지정할 수 있습니다. 가장 간단한 형식은 이전 절에서 설명한 `now`이며 시스템을 즉시 재시작하도록 합니다. 그 밖에 유효한 형식으로는 재시작할 때까지 기다릴 시간(분)을 나타내는 `+m`과 24시간제 시각을 지정하는 `HH:MM`이 있습니다.

**2분 후 시스템을 재부팅하는 예제**

```bash
$ sudo shutdown -r +2
```

**오전 3시에 예약된 재시작을 설정하는 예제**

```bash
$ sudo shutdown -r 03:00
```

3. 재부팅을 취소합니다. 일반적으로 예약된 재시작을 취소해야 할 때 사용합니다.

**구문**

```bash
$ sudo shutdown -c [MESSAGE]
```

**사용법**

```bash
$ sudo shutdown -c "닭이 길을 건너서 예약된 재부팅을 취소했습니다."
```

4. 시스템 재부팅 기록을 표시합니다.

**구문**

```bash
$ last reboot
```

# `sort` 명령어

`sort` 명령어는 파일의 레코드를 특정 순서로 정렬할 때 사용합니다. 기본적으로 `sort` 명령어는 파일의 내용이 ASCII 형식이라고 가정하고 정렬합니다. `sort` 명령어의 옵션을 사용하면 숫자 기준으로 정렬할 수도 있습니다.

### 예제

`file.txt`라는 이름의 데이터 파일을 생성한다고 가정해 보겠습니다.

```bash
명령어:
$ cat > file.txt
abhishek
chitransh
satish
rajan
naveen
divyam
harsh
```

파일 정렬: 이제 `sort` 명령어를 사용합니다.

구문:

```bash
sort filename.txt
```

```bash
명령어:
$ sort file.txt


출력:
abhishek
chitransh
divyam
harsh
naveen
rajan
satish
```

참고: 이 명령어는 입력 파일, 즉 `file.txt` 자체를 실제로 변경하지 않습니다.

### 대문자와 소문자가 섞인 파일에 대한 `sort` 함수

대문자와 소문자가 모두 포함된 파일을 정렬하면 먼저 대문자가 정렬되고 그다음 소문자가 정렬됩니다.

예제:

`mix.txt` 파일을 생성합니다.

```bash
명령어:
$ cat > mix.txt
abc
apple
BALL
Abc
bat
```

이제 `sort` 명령어를 사용합니다.

```bash
명령어:
$ sort mix.txt
출력:
Abc
BALL
abc
apple
bat
```

# `paste` 명령어

`paste` 명령어는 두 개 이상의 파일에서 행을 읽어 순서대로 결합하고, 각 파일의 내용을 TAB 문자로 구분하여 표준 출력에 기록합니다.

### 구문

```bash
paste [OPTIONS]... [FILE]...
```

### 예제

1. 두 파일을 결합합니다.

```bash
paste file1 file2
```

2. 줄바꿈을 구분자로 사용하여 두 파일을 결합합니다.

```bash
paste -d '\n' file1 file2
```

### 추가 플래그 및 기능

| **짧은 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-d` | `--delimiter` | TAB 대신 지정한 문자를 구분자로 사용합니다. |
| `-s` | `--serial` | 파일을 병렬로 결합하는 대신 한 번에 하나씩 결합합니다. |
| `-z` | `--zero-terminated` | 행 구분자를 줄바꿈이 아닌 NUL 문자로 설정합니다. |
| - | `--help` | 명령어 도움말을 출력합니다. |
| - | `--version` | 버전 정보를 출력합니다. |

# `exit` 명령어

`exit` 명령어는 활성화된 셸 세션을 종료(닫기)할 때 사용합니다.

### 구문

```bash
exit
```

***단축키:** `exit`를 입력하는 대신 `Ctrl + D`를 누르면 동일하게 동작합니다.*

# `diff/sdiff` 명령어

이 명령어는 파일을 한 줄씩 비교하여 파일 간의 차이를 표시할 때 사용합니다.

### 구문

```bash
diff [options] File1 File2
```

### 예제

1. `a.txt`와 `b.txt`라는 두 파일이 있고, 각각 다음과 같이 5개의 인도 주 이름을 포함한다고 가정해 보겠습니다.

```text
$ cat a.txt
Gujarat
Uttar Pradesh
Kolkata
Bihar
Jammu and Kashmir


$ cat b.txt
Tamil Nadu
Gujarat
Andhra Pradesh
Bihar
Uttar pradesh
```

`diff` 명령어를 입력하면 다음과 같은 출력이 표시됩니다.

```text
$ diff a.txt b.txt
0a1
> Tamil Nadu
2,3c3
< Uttar Pradesh
 Andhra Pradesh
5c5
 Uttar pradesh
```

### 플래그 및 기능

| **짧은 플래그** | **설명** |
|:---|:---|
| `-c` | 차이점을 컨텍스트 모드로 확인하려면 `-c` 옵션을 사용합니다. |
| `-u` | 차이점을 통합 모드로 확인하려면 `-u` 옵션을 사용합니다. 컨텍스트 모드와 유사합니다. |
| `-i` | 기본적으로 이 명령어는 대소문자를 구분합니다. 대소문자를 구분하지 않으려면 `diff`와 함께 `-i` 옵션을 사용합니다. |
| `-version` | 현재 시스템에서 실행 중인 `diff`의 버전을 표시합니다. |

# `tar` 명령어

`tar`는 `tape archive`의 약자로, 아카이브를 생성하고 아카이브 파일을 추출할 때 사용합니다. 이 명령어는 Linux에서 아카이빙 기능을 제공합니다. `tar` 명령어를 사용하여 압축되거나 압축되지 않은 아카이브 파일을 생성할 수 있으며 생성된 아카이브를 유지하고 수정할 수도 있습니다.

### 예제

1. `abel` 디렉터리를 tar 파일로 생성합니다.

```bash
tar -cvf file-14-09-12.tar /home/abel/
```

2. 현재 디렉터리에 있는 tar 파일의 압축을 해제합니다.

```bash
tar -xvf file-14-09-12.tar
```

### 구문

```bash
tar [options] [archive-file] [file or directory to be archived]
```

### 추가 플래그 및 기능

| **플래그** | **설명** |
|:---|:---|
| `-c` | 아카이브를 생성합니다. |
| `-x` | 아카이브를 추출합니다. |
| `-f` | 지정한 파일 이름으로 아카이브를 생성합니다. |
| `-t` | 아카이브 파일의 파일을 표시하거나 목록으로 출력합니다. |
| `-u` | 기존 아카이브 파일에 파일을 보관하고 추가합니다. |
| `-v` | 자세한 정보를 표시합니다. |
| `-A` | 아카이브 파일을 연결합니다. |
| `-z` | `tar` 명령어가 gzip을 사용하여 tar 파일을 생성하도록 지정합니다. |
| `-j` | `bzip`을 사용하여 tar 아카이브 파일을 필터링합니다. |
| `w` | 아카이브 파일을 확인합니다. |
| `r` | 이미 존재하는 `.tar` 파일에 파일 또는 디렉터리를 업데이트하거나 추가합니다. |
| `-?` | 프로젝트에 대한 간단한 요약을 표시합니다. |
| `-d` | 아카이브와 파일 시스템 간의 차이를 찾습니다. |
| `--usage` | 사용 가능한 tar 옵션을 표시합니다. |
| `--version` | 설치된 tar 버전을 표시합니다. |
| `--show-defaults` | 기본적으로 활성화된 옵션을 표시합니다. |

| **옵션 플래그** | **설명** |
|:---|:---|
| `--check-device` | 증분 아카이브를 생성하는 동안 장치 번호를 확인합니다. |
| `-g` | GNU 형식의 증분 백업과 호환되도록 합니다. |
| `--hole-detection` | 희소 파일의 홀을 감지합니다. |
| `-G` | 이전 GNU 형식의 증분 백업과 호환되도록 합니다. |
| `--ignore-failed-read` | 파일 읽기 오류가 발생해도 프로그램을 종료하지 않습니다. |
| `--level` | 생성할 아카이브의 덤프 수준을 설정합니다. |
| `-n` | 아카이브에서 검색이 가능하다고 가정합니다. |
| `--no-check-device` | 아카이브를 생성할 때 장치 번호를 확인하지 않습니다. |
| `--no-seek` | 아카이브에서 검색할 수 없다고 가정합니다. |
| `--occurrence=N` | 각 파일에서 N번째로 발생한 항목만 처리합니다. |
| `--restrict` | 잠재적으로 유해한 옵션의 사용을 비활성화합니다. |
| `--sparse-version=MAJOR,MINOR` | 사용할 희소 파일 형식의 버전을 설정합니다. |
| `-S` | 희소 파일을 효율적으로 처리합니다. |

| **덮어쓰기 제어 플래그** | **설명** |
|:---|:---|
| `-k` | 기존 파일을 교체하지 않습니다. |
| `--keep-newer-files` | 아카이브 버전보다 최신인 기존 파일을 교체하지 않습니다. |
| `--keep-directory-symlink` | 기존 심볼릭 링크를 교체하지 않습니다. |
| `--no-overwrite-dir` | 기존 디렉터리의 메타데이터를 보존합니다. |
| `--one-top-level=DIR` | 모든 파일을 `DIR` 디렉터리로 추출합니다. |
| `--overwrite` | 기존 파일을 덮어씁니다. |
| `--overwrite-dir` | 디렉터리의 메타데이터를 덮어씁니다. |
| `--recursive-unlink` | 추출하기 전에 디렉터리의 모든 파일을 재귀적으로 제거합니다. |
| `--remove-files` | 디렉터리에 추가한 후 파일을 제거합니다. |
| `--skip-old-files` | 추출할 때 기존 파일을 교체하지 않습니다. |
| `-u` | 각 파일을 추출하여 덮어쓰기 전에 해당 파일을 제거합니다. |
| `-w` | 아카이브를 작성한 후 아카이브를 확인합니다. |

# `gunzip` 명령어

`gunzip` 명령어는 `gzip` 명령어의 반대 기능을 수행합니다. 즉, `gzip` 명령어로 압축한 파일의 압축을 해제합니다.

`gunzip`은 파일 목록을 인수로 받습니다. 이름이 `.gz`, `-gz`, `.z`, `-z` 또는 `_z`로 끝나고 대소문자를 구분하지 않으며 올바른 매직 넘버로 시작하는 각 파일을 압축이 해제된 파일로 교체합니다. 이때 원래 확장자는 제거됩니다.

### 예제

1. 파일의 압축을 해제합니다.

```bash
gunzip filename.gz
```

2. `gunzip`이 지원하는 압축 파일 형식과 일치하는 디렉터리 내부의 모든 파일을 재귀적으로 압축 해제합니다.

```bash
gunzip -r directory_name/
```

3. 현재 작업 디렉터리에서 접미사가 `.tgz`와 일치하는 모든 파일의 압축을 해제합니다.

```bash
gunzip -S .tgz *
```

4. 입력 압축 파일의 압축된 크기, 압축 해제된 크기, 압축률 및 압축 해제 후 이름을 표시합니다.

```bash
gunzip -l file_1 file_2
```

### 구문

```bash
gunzip [ -acfhklLnNrtvV ] [-S suffix] [ name ... ]
```

### gzip, gunzip 및 tar 명령어 사용에 관한 동영상 튜토리얼

[이 동영상](https://www.youtube.com/watch?v=OBtG8zfVwuI)은 Unix 셸에서 파일을 압축하고 압축 해제하는 방법을 보여 줍니다. 압축 해제 명령어로 `gunzip`을 사용합니다.

### 추가 플래그 및 기능

| **짧은 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-c` | `--stdout` | 표준 출력에 기록하고 원본 파일은 변경하지 않습니다. |
| `-h` | `--help` | 도움말 정보를 표시합니다. |
| `-k` | `--keep` | 입력 파일을 유지합니다. (삭제하지 않습니다.) |
| `-l` | `--list` | 압축 파일의 내용을 목록으로 표시합니다. |
| `-q` | `--quiet` | 모든 경고를 표시하지 않습니다. |
| `-r` | `--recursive` | 디렉터리에 대해 재귀적으로 작업합니다. |
| `-S` | `--suffix=SUF` | 압축 파일에 `SUF` 접미사를 사용합니다. |
| - | `--synchronous` | 동기식 출력을 사용합니다. (시스템 충돌이 발생했을 때 더 안전하지만 느립니다.) |
| `-t` | `--test` | 압축 파일의 무결성을 검사합니다. |
| `-v` | `--verbose` | 자세한 정보 표시 모드로 실행합니다. |
| `-V` | `--version` | 버전 번호를 표시합니다. |

:::{.callout-note}

`gunzip filename.gz`는 `gzip -d filename.gz`와 동일합니다.

:::

:::{.callout-warning}

기본적으로 `gunzip`을 `-c` 옵션 없이 실행하면 원본 압축 파일이 삭제됩니다.

:::

# `hostnamectl` 명령어

`hostnamectl` 명령어는 Linux 시스템의 호스트 이름과 관련 설정을 제어하는 데 사용되는 공식 API를 제공합니다. 또한 시스템에서 `/etc/hostname` 파일을 직접 찾아 편집하지 않고도 호스트 이름을 변경할 수 있습니다.

## 구문

```bash
$ hostnamectl [OPTIONS...] COMMAND ...
```

여기서 **COMMAND**에는 다음 중 하나를 지정할 수 있습니다.

**`status`**: 현재 호스트 이름 설정을 확인합니다.

**`set-hostname NAME`**: 시스템 호스트 이름을 설정합니다.

**`set-icon-name NAME`**: 호스트의 아이콘 이름을 설정합니다.

## 예제

1. 현재 호스트 이름을 확인하는 기본 사용법

```bash
$ hostnamectl
```

또는

```bash
$ hostnamectl status
```

2. 정적 호스트 이름을 `_myhostname_`으로 변경합니다. root 권한이 필요할 수도 있고 필요하지 않을 수도 있습니다.

```bash
$ hostnamectl set-hostname myhostname --static
```

3. 임시 호스트 이름을 설정하거나 변경합니다.

```bash
$ hostnamectl set-hostname myotherhostname --transient
```

4. 보기 좋은 호스트 이름(pretty hostname)을 설정합니다. 설정할 이름은 큰따옴표(`" "`)로 묶어야 합니다.

```bash
$ hostname set-hostname "prettyname" --pretty
```

# `iptables` 명령어

`iptables` 명령어는 Linux 시스템을 위한 강력한 방화벽 관리 도구입니다. IP 패킷 필터 규칙 테이블을 설정, 유지 관리 및 검사하여 Linux 커널 방화벽인 `netfilter`를 구성할 수 있습니다.

## 구문

```bash
iptables [options] [chain] [rule-specification] [target]
```

## 기본 개념

### 테이블

- **`filter`**: 패킷 필터링을 위한 기본 테이블입니다. (`INPUT`, `OUTPUT`, `FORWARD`)
- **`nat`**: 네트워크 주소 변환(Network Address Translation)을 담당합니다. (`PREROUTING`, `POSTROUTING`, `OUTPUT`)
- **`mangle`**: 패킷을 변경합니다. (`PREROUTING`, `POSTROUTING`, `INPUT`, `OUTPUT`, `FORWARD`)
- **`raw`**: 연결 추적 예외를 처리합니다. (`PREROUTING`, `OUTPUT`)

### 체인

- **`INPUT`**: 로컬 시스템으로 들어오는 패킷입니다.
- **`OUTPUT`**: 로컬 시스템에서 나가는 패킷입니다.
- **`FORWARD`**: 시스템을 통과하여 라우팅되는 패킷입니다.
- **`PREROUTING`**: 라우팅 결정이 내려지기 전의 패킷입니다.
- **`POSTROUTING`**: 라우팅 결정이 내려진 후의 패킷입니다.

### 대상

- **`ACCEPT`**: 패킷을 허용합니다.
- **`DROP`**: 패킷을 조용히 폐기합니다.
- **`REJECT`**: 패킷을 폐기하고 오류 메시지를 전송합니다.
- **`LOG`**: 패킷을 로그에 기록한 후 계속 처리합니다.
- **`DNAT`**: 목적지 NAT(Network Address Translation)입니다.
- **`SNAT`**: 출발지 NAT입니다.
- **`MASQUERADE`**: 동적 출발지 NAT입니다.

## 기본 명령

### 규칙 나열

```bash
# List all rules
sudo iptables -L

# List rules with line numbers
sudo iptables -L --line-numbers

# List rules in specific table
sudo iptables -t nat -L
sudo iptables -t mangle -L

# Show packet and byte counters
sudo iptables -L -v

# Show rules in iptables-save format
sudo iptables -S
```

### 기본 규칙 작업

```bash
# Add rule to end of chain
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT

# Insert rule at specific position
sudo iptables -I INPUT 1 -p tcp --dport 80 -j ACCEPT

# Delete specific rule
sudo iptables -D INPUT -p tcp --dport 22 -j ACCEPT

# Delete rule by line number
sudo iptables -D INPUT 3

# Replace rule at specific position
sudo iptables -R INPUT 1 -p tcp --dport 443 -j ACCEPT
```

## Common Rule 예제

###  1. 특정 포트 허용 및 차단 {.unnumbered}

```bash
# Allow SSH (port 22)
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT

# Allow HTTP (port 80)
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT

# Allow HTTPS (port 443)
sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT

# Block specific port
sudo iptables -A INPUT -p tcp --dport 8080 -j DROP
```

###  2. IP 주소별 허용 및 차단 {.unnumbered}

```bash
# Allow specific IP
sudo iptables -A INPUT -s 192.168.1.100 -j ACCEPT

# Block specific IP
sudo iptables -A INPUT -s 192.168.1.50 -j DROP

# Allow subnet
sudo iptables -A INPUT -s 192.168.1.0/24 -j ACCEPT

# Block IP range
sudo iptables -A INPUT -m iprange --src-range 192.168.1.100-192.168.1.200 -j DROP
```

###  3. 네트워크 인터페이스별 허용 및 차단 {.unnumbered}

```bash
# Allow traffic on loopback
sudo iptables -A INPUT -i lo -j ACCEPT

# Allow on specific interface
sudo iptables -A INPUT -i eth0 -p tcp --dport 80 -j ACCEPT

# Block on specific interface
sudo iptables -A INPUT -i eth1 -j DROP
```

## 고급 규칙

###  1. 상태 기반 연결(Stateful Connections) {.unnumbered}

```bash
# Allow established and related connections
sudo iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT

# Allow new connections on specific ports
sudo iptables -A INPUT -m state --state NEW -p tcp --dport 22 -j ACCEPT
```

###  2. 연결 속도 및 요청 수 제한 {.unnumbered}

```bash
# Limit SSH connections (6 per minute)
sudo iptables -A INPUT -p tcp --dport 22 -m limit --limit 6/min -j ACCEPT

# Limit ping requests
sudo iptables -A INPUT -p icmp --icmp-type echo-request -m limit --limit 1/sec -j ACCEPT
```

###  3. 시간대별 규칙 적용 {.unnumbered}

```bash
# Allow access during business hours
sudo iptables -A INPUT -p tcp --dport 80 -m time --timestart 09:00 --timestop 17:00 -j ACCEPT

# Allow access on weekdays
sudo iptables -A INPUT -p tcp --dport 22 -m time --weekdays Mon,Tue,Wed,Thu,Fri -j ACCEPT
```

###  4. 다중 포트 일괄 규칙 {.unnumbered}

```bash
# Allow multiple ports
sudo iptables -A INPUT -p tcp -m multiport --dports 22,80,443 -j ACCEPT

# Block multiple ports
sudo iptables -A INPUT -p tcp -m multiport --dports 135,445,1433 -j DROP
```

## NAT 구성

### 1. 소스 NAT(SNAT) {.unnumbered}

```bash
# Static SNAT
sudo iptables -t nat -A POSTROUTING -s 192.168.1.0/24 -o eth0 -j SNAT --to-source 203.0.113.1

# Dynamic SNAT (Masquerading)
sudo iptables -t nat -A POSTROUTING -s 192.168.1.0/24 -o eth0 -j MASQUERADE
```

### 2. 대상 NAT(DNAT) {.unnumbered}

```bash
# Port forwarding
sudo iptables -t nat -A PREROUTING -p tcp --dport 8080 -j DNAT --to-destination 192.168.1.100:80

# Forward to different IP
sudo iptables -t nat -A PREROUTING -d 203.0.113.1 -j DNAT --to-destination 192.168.1.100
```

## 정책 구성

### 기본 정책

```bash
# Set default policies
sudo iptables -P INPUT DROP
sudo iptables -P FORWARD DROP
sudo iptables -P OUTPUT ACCEPT

# View current policies
sudo iptables -L | grep "policy"
```

### 체인 관리

```bash
# Create custom chain
sudo iptables -N CUSTOM_CHAIN

# Delete custom chain (must be empty)
sudo iptables -X CUSTOM_CHAIN

# Flush specific chain
sudo iptables -F INPUT

# Flush all chains
sudo iptables -F
```

## Logging

```bash
# Log dropped packets
sudo iptables -A INPUT -j LOG --log-prefix "DROPPED: " --log-level 4

# Log before dropping
sudo iptables -A INPUT -p tcp --dport 23 -j LOG --log-prefix "TELNET_ATTEMPT: "
sudo iptables -A INPUT -p tcp --dport 23 -j DROP

# View logs
sudo tail -f /var/log/syslog | grep "DROPPED:"
```

## 일반적인 방화벽 구성

###  1. 기본 데스크톱 방화벽 설정 {.unnumbered}

```bash
#!/bin/bash
# Clear existing rules
sudo iptables -F
sudo iptables -X
sudo iptables -t nat -F
sudo iptables -t nat -X

# Default policies
sudo iptables -P INPUT DROP
sudo iptables -P FORWARD DROP
sudo iptables -P OUTPUT ACCEPT

# Allow loopback
sudo iptables -A INPUT -i lo -j ACCEPT

# Allow established connections
sudo iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT

# Allow SSH
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT

# Allow ping
sudo iptables -A INPUT -p icmp --icmp-type echo-request -j ACCEPT
```

###  2. 웹 서버 방화벽 설정 {.unnumbered}

```bash
#!/bin/bash
# Basic web server configuration
sudo iptables -F

# Default policies
sudo iptables -P INPUT DROP
sudo iptables -P FORWARD DROP
sudo iptables -P OUTPUT ACCEPT

# Allow loopback and established connections
sudo iptables -A INPUT -i lo -j ACCEPT
sudo iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT

# Allow SSH (limit attempts)
sudo iptables -A INPUT -p tcp --dport 22 -m limit --limit 6/min -j ACCEPT

# Allow HTTP/HTTPS
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT

# Allow ping
sudo iptables -A INPUT -p icmp --icmp-type echo-request -j ACCEPT
```

###  3. 라우터 및 게이트웨이 구성 {.unnumbered}

```bash
#!/bin/bash
# Enable IP forwarding
echo 1 > /proc/sys/net/ipv4/ip_forward

# NAT for internal network
sudo iptables -t nat -A POSTROUTING -s 192.168.1.0/24 -o eth0 -j MASQUERADE

# Allow forwarding for established connections
sudo iptables -A FORWARD -m state --state ESTABLISHED,RELATED -j ACCEPT

# Allow forwarding from internal network
sudo iptables -A FORWARD -s 192.168.1.0/24 -j ACCEPT
```

## Persistence

### 1. 규칙 저장/복원 {.unnumbered}

```bash
# Save current rules
sudo iptables-save > /etc/iptables/rules.v4

# Restore rules
sudo iptables-restore < /etc/iptables/rules.v4

# Install persistence package (Ubuntu/Debian)
sudo apt install iptables-persistent
```

### 2. 자동 로드 {.unnumbered}

```bash
# Create systemd service
sudo vim /etc/systemd/system/iptables-restore.service

[Unit]
Description=Restore iptables firewall rules
Before=network-pre.target

[Service]
Type=oneshot
ExecStart=/sbin/iptables-restore /etc/iptables/rules.v4

[Install]
WantedBy=multi-user.target

# Enable service
sudo systemctl enable iptables-restore.service
```

## 문제 해결

### 1. 규칙 테스트 {.unnumbered}

```bash
# Test connectivity
telnet target-ip port
nc -zv target-ip port

# Check if rule matches
sudo iptables -L -v -n | grep "rule-description"

# Monitor rule usage
watch "sudo iptables -L -v -n"
```

### 2. 디버깅 {.unnumbered}

```bash
# Enable all logging temporarily
sudo iptables -A INPUT -j LOG --log-prefix "INPUT: "
sudo iptables -A OUTPUT -j LOG --log-prefix "OUTPUT: "
sudo iptables -A FORWARD -j LOG --log-prefix "FORWARD: "

# Monitor logs
sudo tail -f /var/log/syslog | grep "INPUT:\|OUTPUT:\|FORWARD:"
```

### 3. 긴급 액세스 {.unnumbered}

```bash
# Temporary rule to allow all (emergency)
sudo iptables -I INPUT 1 -j ACCEPT

# Flush all rules (removes all protection)
sudo iptables -F
sudo iptables -X
sudo iptables -P INPUT ACCEPT
sudo iptables -P OUTPUT ACCEPT
sudo iptables -P FORWARD ACCEPT
```

## 보안 모범 사례

### 1. 기본 거부 정책

```bash
# Always start with deny-all policy
sudo iptables -P INPUT DROP
sudo iptables -P FORWARD DROP
# Keep OUTPUT as ACCEPT for normal operation
```

### 2. 순서가 중요합니다 {.unnumbered}

```bash
# More specific rules should come first
sudo iptables -I INPUT 1 -s 192.168.1.100 -p tcp --dport 22 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 22 -j DROP
```

### 3. 핵심 서비스의 속도 제한 {.unnumbered}

```bash
# Protect SSH from brute force
sudo iptables -A INPUT -p tcp --dport 22 -m recent --set --name SSH
sudo iptables -A INPUT -p tcp --dport 22 -m recent --update --seconds 60 --hitcount 3 --name SSH -j DROP
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT
```

## 성능 고려 사항

```bash
# Use connection tracking for better performance
sudo iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT

# Place frequently matched rules first
sudo iptables -I INPUT 1 -m state --state ESTABLISHED,RELATED -j ACCEPT

# Use specific matches to reduce processing
sudo iptables -A INPUT -p tcp --dport 80 -s 192.168.1.0/24 -j ACCEPT
```
## 중요 참고 사항

- 규칙을 영구적으로 적용하기 전에 항상 테스트하십시오.
- 규칙으로 인해 시스템에 접속할 수 없게 되는 경우를 대비하여 시스템에 접근할 수 있는 방법을 유지하십시오.
- 포괄적인 규칙보다는 특정 프로토콜과 포트를 사용하십시오.
- 트래픽 패턴을 파악하기 위해 로그를 모니터링하십시오.
- 나중에 참고할 수 있도록 방화벽 규칙을 문서화하십시오.
- 정상적으로 작동하는 설정을 정기적으로 백업하십시오.
- 더 간단한 방화벽 관리를 위해 UFW 사용을 고려하십시오.

`iptables` 명령어는 포괄적인 방화벽 기능을 제공하지만 보안 문제나 시스템 접속 차단을 방지하려면 신중한 계획과 테스트가 필요합니다.

자세한 내용은 다음 매뉴얼을 확인하십시오.

```bash
man iptables
```

# `netstat` 명령어

`netstat`이라는 용어는 Network Statistics(네트워크 통계)를 의미합니다. 쉽게 말해 `netstat` 명령어는 현재 네트워크 연결, 네트워크 프로토콜 통계 및 다양한 네트워크 인터페이스 정보를 표시합니다.

PC에 `netstat`이 설치되어 있는지 확인합니다.

```bash
netstat -v
```

PC에 `netstat`이 설치되어 있지 않다면 다음 명령어로 설치할 수 있습니다.

```bash
sudo apt install net-tools
```

### `netstat` 명령어의 사용 사례

- `-nr` 플래그와 함께 사용하면 터미널에 라우팅 테이블의 세부 정보를 표시합니다.

예시:

```bash
netstat -nr
```

- `-i` 플래그와 함께 사용하면 현재 구성된 네트워크 인터페이스의 통계를 표시합니다.

예시:

```bash
netstat -i
```

- `-tunlp` 플래그와 함께 사용하면 네트워크 목록, 현재 상태 및 연결된 포트 목록을 표시합니다.

예시:

```bash
netstat -tunlp
```

- `-at` 플래그와 함께 사용하면 모든 TCP 포트 연결 목록을 확인할 수 있습니다.

```bash
netstat -at
```

- `-au` 플래그와 함께 사용하면 모든 UDP 포트 연결 목록을 확인할 수 있습니다.

```bash
netstat -au
```

- `-l` 플래그와 함께 사용하면 모든 활성 연결 목록을 확인할 수 있습니다.

```bash
netstat -l
```

# `lsof` 명령어

`lsof` 명령어는 실행 중인 프로세스가 열어 둔 모든 파일의 **파일 정보**를 표시합니다. 이름 역시 `list open files`(열린 파일 목록)에서 유래했습니다.

열린 파일은 일반 파일, 디렉터리, 블록 특수 파일, 문자 특수 파일, 실행 중인 텍스트 참조, 라이브러리, 스트림 또는 네트워크 파일일 수 있습니다. 네트워크 파일에는 인터넷 소켓, NFS 파일 및 UNIX 도메인 소켓이 포함됩니다. 경로를 지정하여 특정 파일이나 파일 시스템의 모든 파일을 선택할 수 있습니다.

### 구문(Syntax)

```bash
lsof [-OPTION] [USER_NAME]
```

### 예시

1. 모든 활성 프로세스가 열어 둔 모든 파일을 표시합니다.

```bash
lsof
```

2. 특정 사용자가 열어 둔 파일을 표시합니다.

```bash
lsof -u [USER_NAME]
```

3. 지정한 디렉터리 아래에서 열린 파일을 가진 프로세스를 나열합니다.

```bash
lsof +d [PATH_TO_DIR]
```

### 옵션 및 기능

| **옵션** | **추가 옵션** | **설명** |
|:---|:---|:---|
| `-i` | `tcp` / `udp` / `:port` | 실행 중인 모든 네트워크 연결을 나열합니다. 추가로 UDP, TCP 또는 지정한 포트의 연결만 나열할 수 있습니다. |
| `-i4` | - | IPv4 연결을 사용하는 모든 프로세스를 나열합니다. |
| `-i6` | - | IPv6 연결을 사용하는 모든 프로세스를 나열합니다. |
| `-c` | `[PROCESS_NAME]` | 지정한 이름의 특정 프로세스가 연 모든 파일을 나열합니다. |
| `-p` | `[PROCESS_ID]` | 지정한 프로세스 ID가 연 모든 파일을 나열합니다. |
| `-p` | `^[PROCESS_ID]` | 지정한 프로세스 ID가 열지 않은 모든 파일을 나열합니다. |
| `+d` | `[PATH]` | 지정한 디렉터리 아래에서 열린 파일을 가진 프로세스를 나열합니다. |
| `+R` | - | 부모 프로세스 ID가 연 파일을 나열합니다. |

### 도움말 명령어

다음 명령어를 실행하면 `lsof` 명령어에 대한 전체 안내를 확인할 수 있습니다.

```bash
man lsof
```

```
man lsof
```
# `bzip2` 명령어

`bzip2` 명령어를 사용하면 파일을 압축하고 압축 해제할 수 있습니다. 즉, 여러 파일을 원본보다 적은 저장 공간을 차지하는 하나의 파일로 묶을 수 있습니다.

### 구문(Syntax)

```bash
bzip2 [OPTIONS] filenames ...
```

#### 참고

각 파일은 압축된 버전으로 대체되며 파일 이름 뒤에 `.bz2` 확장자가 추가됩니다.

### 옵션 및 기능

| **옵션** | **별칭** | **설명** |
|:---|:---|:---|
| `-d` | `--decompress` | 압축된 파일의 압축을 해제합니다. |
| `-f` | `--force` | 기존 출력 파일을 강제로 덮어씁니다. |
| `-h` | `--help` | 도움말 메시지를 표시한 후 종료합니다. |
| `-k` | `--keep` | 파일을 압축하되 원본 입력 파일은 삭제하지 않습니다. |
| `-L` | `--license` | 라이선스 약관을 표시합니다. |
| `-q` | `--quiet` | 중요하지 않은 경고 메시지를 표시하지 않습니다. |
| `-t` | `--test` | 지정한 `.bz2` 파일의 무결성을 검사하지만 압축을 해제하지는 않습니다. |
| `-v` | `--verbose` | 각 압축 작업에 대한 세부 정보를 표시합니다. |
| `-V` | `--version` | 소프트웨어 버전을 표시합니다. |
| `-z` | `--compress` | 파일을 압축하며 원본 입력 파일은 삭제합니다. |

> #### 기본적으로 `bzip2`는 파일을 압축할 때 원본(입력) 파일을 삭제합니다. 원본 파일을 유지하려면 명령줄 옵션 `-k`를 사용하십시오.

### 예시

1. 강제로 압축합니다.

```bash
bzip2 -z input.txt
```

**참고: 이 옵션은 원본 파일도 삭제합니다.**

2. 강제로 압축하면서 원본 입력 파일도 유지합니다.

```bash
bzip2 -k input.txt
```

3. 강제로 압축을 해제합니다.

```bash
bzip2 -d input.txt.bz2
```

4. 압축 파일의 무결성을 검사합니다.

```bash
bzip2 -t input.txt.bz2
```

5. 처리하는 각 파일의 압축률을 표시합니다.

```bash
bzip2 -v input.txt
```

# `service` 명령어

`service`는 가능한 한 예측 가능한 환경에서 System V 초기화 스크립트를 실행합니다. 이때 대부분의 환경 변수를 제거하고 현재 작업 디렉터리를 `/`로 설정합니다.

`SCRIPT` 매개변수는 `/etc/init.d/SCRIPT`에 위치한 System V 초기화 스크립트를 지정합니다. 사용할 수 있는 `COMMAND` 값은 호출된 스크립트에 따라 달라지며 `service`는 `COMMAND`와 `OPTIONS`를 수정하지 않고 초기화 스크립트에 전달합니다. 모든 스크립트는 최소한 `start`와 `stop` 명령어를 지원해야 합니다.

`COMMAND`가 `--full-restart`인 경우에는 예외적으로 스크립트가 두 번 실행됩니다. 먼저 `stop` 명령어로 실행한 다음 `start` 명령어로 실행합니다.

`COMMAND`에는 최소한 `start`, `stop`, `status`, `restart`를 사용할 수 있습니다.

`service --status-all`은 모든 초기화 스크립트를 알파벳순으로 실행하면서 `status` 명령어를 전달합니다.

### 예시

1. 실행 중인 모든 서비스의 상태를 확인합니다.

```bash
service --status-all
```

2. 스크립트를 실행합니다.

```bash
service SCRIPT-Name start
```

3. 보다 일반적인 구문:

```bash
service [SCRIPT] [COMMAND] [OPTIONS]
```

# `vmstat` 명령어

`vmstat` 명령어를 사용하면 시스템 성능을 모니터링할 수 있습니다. 메모리, 디스크, 프로세스, CPU 스케줄링, 페이징 및 블록 I/O에 대한 정보를 표시합니다. 이 명령어는 **가상 메모리 통계 보고서(virtual memory statistic report)**라고도 합니다.

처음 생성되는 보고서는 마지막 재부팅 이후의 평균 정보를 보여 줍니다. 그다음부터 생성되는 보고서는 시간 경과에 따른 정보를 표시합니다.

### `vmstat`



위에서 볼 수 있듯이 `vmstat`는 매우 유용한 명령어입니다. 위 출력에서 가장 중요한 항목은 다음과 같습니다.

- `free`: 사용되지 않고 남아 있는 여유 공간을 표시합니다.
- `si`: 매초 스왑 영역에서 메모리로 스왑 인되는 메모리 양을 kB 단위로 표시합니다.
- `so`: 매초 메모리에서 스왑 영역으로 스왑 아웃되는 메모리 양을 kB 단위로 표시합니다.

### `vmstat -a`

`vmstat -a`를 실행하면 실행 중인 시스템의 활성 메모리와 비활성 메모리를 표시합니다.



### `vmstat -d`

`vmstat -d` 명령어는 모든 디스크 통계를 표시합니다.



위에서 볼 수 있듯이 `vmstat`는 가상 메모리에 대한 다양한 통계를 표시하는 매우 유용한 명령어입니다.

# `mpstat` 명령어

`mpstat` 명령어는 프로세서 관련 통계를 보고할 때 사용합니다. 시스템의 CPU 사용량 통계와 CPU 사용률 및 성능 정보를 정확하게 표시합니다.

### 구문(Syntax)

```bash
mpstat [options] [<interval> [<count>]]
```

#### 참고

첫 번째 프로세서는 CPU 0으로, 두 번째 프로세서는 CPU 1로 초기화되며 이후에도 같은 방식으로 번호가 지정됩니다.

### 옵션 및 기능

| **옵션** | **설명** |
|:---|:---|
| `-A` | 모든 상세 통계를 표시합니다. |
| `-h` | `mpstat` 도움말을 표시합니다. |
| `-I` | 상세 인터럽트 통계를 표시합니다. |
| `-n` | NUMA 노드 배치에 따른 CPU 통계 요약을 보고합니다. |
| `-N` | 통계를 보고할 NUMA 노드를 지정합니다. |
| `-P` | 통계를 보고할 프로세서를 지정합니다. |
| `-o` | 통계를 JSON(JavaScript Object Notation) 형식으로 표시합니다. |
| `-T` | CPU 보고서에 토폴로지 요소를 표시합니다. |
| `-u` | CPU 사용률을 보고합니다. |
| `-v` | 가상 프로세서 수준에서 사용률 통계를 표시합니다. |
| `-V` | `mpstat` 버전을 표시합니다. |
| `-ALL` | 모든 CPU에 대한 상세 통계를 표시합니다. |

### 예시:

1. 프로세서 및 CPU 통계 출력:

```
mpstat
```

2. 모든 CPU의 프로세서 번호 출력:

```
mpstat -P ALL
```

3. 수집 가능한 모든 정보 조회:

```
mpstat -A
```

4. 특정 프로세서별 CPU 사용률 출력:

```
mpstat -P 0
```

5. 지정한 시간 간격으로 CPU 사용률 출력:

``` 
mpstat 1 5
```

**참고: 이 명령어는 1초 간격으로 총 5회의 보고서를 출력합니다.**

# `ncdu` 명령어

`ncdu`(NCurses Disk Usage)는 유명한 `du` 명령어를 ncurses 기반의 텍스트 UI로 구현한 도구입니다. 어떤 디렉터리가 디스크 공간을 많이 차지하고 있는지 빠르고 직관적으로 파악할 수 있습니다.

## 예제

1. 저소음 모드(Quiet Mode)

```
ncdu -q
```

2. 마운트된 디렉터리 제외

```
ncdu -q -x
```

## 구문(Syntax)

```
ncdu [-hqvx] [--exclude PATTERN] [-X FILE] dir
```
## 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-h` | - | 간단한 도움말 메시지를 출력합니다. |
| `-q` | - | 조용한 모드입니다. 디스크 공간을 계산하는 동안 `ncdu`는 기본적으로 초당 10회 화면을 업데이트하지만 조용한 모드에서는 2초에 한 번으로 업데이트 횟수가 줄어듭니다. 원격 연결에서 대역폭을 절약할 때 사용합니다. |
| `-v` | - | 버전을 출력합니다. |
| `-x` | - | 지정한 디렉터리와 동일한 파일 시스템에 있는 파일과 디렉터리만 계산합니다. |
| - | `--exclude PATTERN` | `PATTERN`과 일치하는 파일을 제외합니다. 이 인수는 여러 번 추가하여 패턴을 여러 개 지정할 수 있습니다. |
| `-X FILE` | `--exclude-from FILE` | `FILE`에 있는 패턴 중 하나와 일치하는 파일을 제외합니다. 패턴은 줄바꿈으로 구분해야 합니다. |

# `uniq` 명령어

Linux에서 `uniq` 명령어는 파일에서 반복되는 행을 보고하거나 필터링하는 명령줄 유틸리티입니다.

간단히 말해 `uniq`는 연속해서 중복되는 행을 감지하고 중복 행을 제거하는 도구입니다. `uniq`는 인수로 전달된 입력 파일에서 서로 연속하여 일치하는 행을 필터링하고, 필터링된 데이터를 출력 파일에 기록합니다.

### 예시

파일에서 반복되는 행을 생략하려면 다음과 같은 구문을 사용합니다.

```
uniq kt.txt
```

각 줄이 중복된 횟수를 함께 출력하려면 다음과 같이 실행합니다.

```
uniq -c kt.txt
```

중복된 줄만 출력하려면 다음과 같이 실행합니다.

```
uniq -d kt.txt
```

중복되지 않은 고유한 줄만 출력하려면 다음과 같이 실행합니다.

```
uniq -u kt.txt
```

줄의 고유성을 비교할 때 처음 N개 필드를 건너뛰려면 다음과 같이 실행합니다.

```
uniq -f 2 kt.txt
```

줄의 고유성을 비교할 때 처음 N개 문자를 건너뛰려면 다음과 같이 실행합니다.

```
uniq -s 5 kt.txt
```

대소문자를 구분하지 않고 비교하려면 다음과 같이 실행합니다.

```
uniq -i kt.txt
```

### 구문(Syntax):

```
uniq [OPTION] [INPUT[OUTPUT]]
```
### 사용 가능한 옵션

| **플래그** | **설명** | **매개변수** |
|:---|:---|:---|
| `-c` | 각 행 앞에 숫자를 표시하여 해당 행이 몇 번 반복되었는지 알려 줍니다. | - |
| `-d` | 반복되는 행만 출력하고 반복되지 않는 행은 출력하지 않습니다. | - |
| `-i` | 기본적으로 비교 시 대소문자를 구분하지만 이 옵션을 사용하면 대소문자를 구분하지 않고 비교할 수 있습니다. | - |
| `-f` | 행의 고유성을 판단하기 전에 `N`개의 필드를 건너뛸 수 있습니다. (필드는 공백으로 구분된 문자 그룹입니다.) | `N` |
| `-s` | 행의 고유성을 판단할 때 각 행의 처음 `N`개 문자를 비교하지 않습니다. `-f` 옵션과 유사하지만 필드가 아닌 개별 문자를 건너뜁니다. | `N` |
| `-u` | 고유한 행만 출력합니다. | - |
| `-z` | 행을 줄바꿈 대신 0 바이트(NULL)로 끝냅니다. | - |
| `-w` | 행에서 `N`개의 문자만 비교합니다. | `N` |
| `--help` | 도움말 메시지를 표시한 후 종료합니다. | - |
| `--version` | 버전 정보를 표시한 후 종료합니다. | - |

# `RPM` 명령어

`rpm` RPM Package Manager(RPM 패키지 관리자)

`rpm`은 개별 소프트웨어 패키지를 빌드, 설치, 조회, 검증, 업데이트 및 삭제할 수 있는 강력한 **패키지 관리자**입니다. **패키지**는 아카이브 파일을 설치하고 삭제하는 데 사용되는 파일과 메타데이터의 묶음입니다. 메타데이터에는 도우미 스크립트, 파일 속성 및 패키지에 대한 설명 정보가 포함됩니다.

패키지는 두 가지 종류로 제공됩니다.

- 설치할 소프트웨어를 캡슐화한 바이너리 패키지
- 바이너리 패키지를 생성하는 데 필요한 소스 코드와 빌드 방법이 포함된 소스 패키지

다음 기본 모드 중 하나를 선택해야 합니다.

**조회, 검증, 서명 확인, 설치/업그레이드/갱신, 제거, 데이터베이스 초기화, 데이터베이스 재구축, 재서명, 서명 추가, 소유자/그룹 설정, 조회 태그 표시 및 설정 표시**

## 일반 옵션

이 옵션은 다양한 모드에서 모두 사용할 수 있습니다.

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-?` | `--help` | 일반적인 사용법 메시지보다 더 자세한 사용법 메시지를 출력합니다. |
| - | `--version` | 사용 중인 `rpm`의 버전 번호가 포함된 한 줄을 출력합니다. |
| - | `--quiet` | 가능한 한 적은 정보를 출력합니다. 일반적으로 오류 메시지만 표시됩니다. |
| `-v` | - | 자세한 정보를 출력합니다. 일반적으로 일반적인 진행 상황 메시지가 표시됩니다. |
| `-vv` | - | 많은 양의 상세한 디버깅 정보를 출력합니다. |
| - | `--rcfile FILELIST` | 콜론으로 구분된 `FILELIST`의 각 파일을 `rpm`이 순서대로 읽어 설정 정보를 가져옵니다. 목록의 첫 번째 파일만 반드시 존재해야 하며 물결표(`~`)는 `$HOME`의 값으로 확장됩니다. 기본 `FILELIST`는 `/usr/lib/rpm/rpmrc:/usr/lib/rpm/redhat/rpmrc:/etc/rpmrc:~/.rpmrc`입니다. |
| - | `--pipe CMD` | `rpm`의 출력을 `CMD` 명령어로 파이프합니다. |
| - | `--dbpath DIRECTORY` | 기본 경로 `/var/lib/rpm` 대신 `DIRECTORY`에 있는 데이터베이스를 사용합니다. |
| - | `--root DIRECTORY` | 모든 작업에 대해 `DIRECTORY`를 루트로 하는 파일 시스템 트리를 사용합니다. 이는 의존성 검사에 `DIRECTORY` 내부의 데이터베이스가 사용되고, 모든 스크립틀릿(예: 패키지를 설치할 때의 `%post` 또는 패키지를 빌드할 때의 `%prep`)이 `DIRECTORY`로 `chroot(2)`를 실행한 후 수행된다는 의미입니다. |
| `-D` | `--define='MACRO EXPR'` | `MACRO`를 `EXPR` 값으로 정의합니다. |
| `-E` | `--eval='EXPR'` | `EXPR`의 매크로 확장 결과를 출력합니다. |

# 구문 요약(Synopsis)

## 패키지 조회 및 검증:

```
rpm {-q|--query} [select-options] [query-options]

rpm {-V|--verify} [select-options] [verify-options]

rpm --import PUBKEY ...

rpm {-K|--checksig} [--nosignature] [--nodigest] PACKAGE_FILE ...
```

## 패키지 설치, 업그레이드 및 삭제:

```
rpm {-i|--install} [install-options] PACKAGE_FILE ...

rpm {-U|--upgrade} [install-options] PACKAGE_FILE ...

rpm {-F|--freshen} [install-options] PACKAGE_FILE ...

rpm {-e|--erase} [--allmatches] [--nodeps] [--noscripts] [--notriggers] [--test] PACKAGE_NAME ...
```

## Miscellaneous:

```
rpm {--initdb|--rebuilddb}

rpm {--addsign|--resign} PACKAGE_FILE...

rpm {--querytags|--showrc}

rpm {--setperms|--setugids} PACKAGE_NAME .

```

### 조회 옵션 (query-options)

```
[--changelog] [-c,--configfiles] [-d,--docfiles] [--dump]
[--filesbypkg] [-i,--info] [--last] [-l,--list]
[--provides] [--qf,--queryformat QUERYFMT]
[-R,--requires] [--scripts] [-s,--state]
[--triggers,--triggerscripts]
```

### 검증 옵션 (verify-options)

```
[--nodeps] [--nofiles] [--noscripts]
[--nodigest] [--nosignature]
[--nolinkto] [--nofiledigest] [--nosize] [--nouser]
[--nogroup] [--nomtime] [--nomode] [--nordev]
[--nocaps]
```

### 설치 옵션 (install-options)

```
[--aid] [--allfiles] [--badreloc] [--excludepath OLDPATH]
[--excludedocs] [--force] [-h,--hash]
[--ignoresize] [--ignorearch] [--ignoreos]
[--includedocs] [--justdb] [--nodeps]
[--nodigest] [--nosignature] [--nosuggest]
[--noorder] [--noscripts] [--notriggers]
[--oldpackage] [--percent] [--prefix NEWPATH]
[--relocate OLDPATH=NEWPATH]
[--replacefiles] [--replacepkgs]
[--test]
```
# `scp` 명령어

SCP(Secure Copy)는 두 위치 사이에서 파일과 디렉터리를 안전하게 복사할 수 있는 명령줄 유틸리티입니다.

파일과 비밀번호가 모두 암호화되므로, 네트워크 트래픽을 엿보는 사람이 민감한 정보를 확인할 수 없습니다.

### 파일 또는 디렉터리를 복사하는 방법

- 로컬 시스템에서 원격 시스템으로 복사합니다.
- 원격 시스템에서 로컬 시스템으로 복사합니다.
- 로컬 시스템에서 두 원격 시스템 사이로 복사합니다.

### 예시

1. 로컬 시스템에서 원격 시스템으로 파일을 복사합니다.

```bash
scp /home/documents/local-file root@{remote-ip-address}:/home/
```

2. 원격 시스템에서 로컬 시스템으로 파일을 복사합니다.

```bash
scp root@{remote-ip-address}:/home/remote-file /home/documents/
```

3. 로컬 시스템에서 두 원격 시스템 사이로 파일을 복사합니다.

```bash
scp root@{remote1-ip-address}:/home/remote-file root@{remote2-ip-address}/home/
```

4. 점프 호스트 서버를 통해 파일을 복사합니다.

```bash
scp /home/documents/local-file -oProxyJump=<jump-host-ip> root@{remote-ip-address}/home/
```

일부 시스템의 최신 `scp` 버전에서는 위 명령어에 `-J` 플래그를 사용할 수도 있습니다.

```bash
scp /home/documents/local-file -J <jump-host-ip> root@{remote-ip-address}/home/
```

### 구문(Syntax)

```bash
scp [OPTION] [user@]SRC_HOST:]file1 [user@]DEST_HOST:]file2
```

- `OPTION` 암호화 방식, SSH 설정, SSH 포트, 제한, 재귀적 복사 등을 지정하는 `scp` 옵션입니다.
- `[user@]SRC_HOST:]file1` 원본 파일입니다.
- `[user@]DEST_HOST:]file2` 대상 파일입니다.

로컬 파일은 절대 경로나 상대 경로로 지정해야 하며 원격 파일 이름에는 사용자와 호스트 정보가 포함되어야 합니다.

`scp`는 동작의 모든 측면을 제어할 수 있는 여러 옵션을 제공합니다. 가장 널리 사용되는 옵션은 다음과 같습니다.

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-P` | - | 원격 호스트의 SSH 포트를 지정합니다. |
| `-p` | - | 파일의 수정 시간과 접근 시간을 보존합니다. |
| `-q` | - | 진행률 표시기와 오류가 아닌 메시지를 표시하지 않습니다. |
| `-C` | - | 대상 시스템으로 전송할 때 데이터를 압축합니다. |
| `-r` | - | 디렉터리를 재귀적으로 복사합니다. |

### 시작하기 전에

`scp` 명령어는 데이터 전송에 `ssh`를 사용하므로, 원격 시스템에서 인증하려면 `ssh 키` 또는 비밀번호가 필요합니다.

콜론(`:`)은 `scp`가 로컬 위치와 원격 위치를 구분하는 기준입니다.

파일을 복사하려면 원본 파일에 대한 최소 읽기 권한과 대상 시스템에 대한 쓰기 권한이 있어야 합니다.

두 시스템에서 이름과 위치가 같은 파일을 복사할 때는 주의하십시오. `scp`는 경고 없이 기존 파일을 덮어씁니다.

대용량 파일을 전송할 때는 `screen` 또는 `tmux` 세션 안에서 `scp` 명령어를 실행하는 것이 좋습니다.

# `sleep` 명령어

`sleep` 명령어는 임시 작업(dummy job)을 생성할 때 사용합니다. 임시 작업은 명령어 실행을 지연하는 데 도움이 됩니다. 기본적으로 초 단위의 시간을 사용하지만 끝에 작은 접미사(`s`, `m`, `h`, `d`)를 추가하여 다른 시간 단위로 지정할 수 있습니다. 이 명령어는 `NUMBER`로 지정한 시간 동안 실행을 일시 중지합니다.

참고: `sleep` 명령어에 둘 이상의 `NUMBER`를 지정하면 각 값의 합만큼 실행이 지연됩니다.


### Examples :

1. 10초 동안 대기:

```
sleep 10s
```
 

2. 일반적인 형식의 대기 명령:

```
sleep NUMBER[SUFFIX]...
```

## 옵션

다음과 같은 옵션을 지원합니다.

1.   --help
	   > 도움말을 표시하고 종료합니다
2.  --version
	 > 버전 정보를 출력하고 종료합니다


# `split` 명령어

`split` 명령어는 Linux에서 파일을 더 작은 파일들로 분할하는 데 사용됩니다.

### 예제

1. 파일 이름을 지정하여 작은 파일로 분할:

```
split filename.txt
```

2. `filename` 파일을 접두사 `file`을 붙여 200줄 단위로 분할:

```
split -l 200 filename file
```

이 명령은 각각 200줄씩 담긴 `fileaa`, `fileab`, `fileac`, `filead` 등의 파일을 생성합니다.

3. `filename` 파일을 접두사 `file`을 붙여 40바이트 단위로 분할:

```
split -b 40 filename file
```

이 명령은 각각 40바이트 크기의 `fileaa`, `fileab`, `fileac`, `filead` 등의 파일을 생성합니다.

4. `--verbose` 옵션으로 분할 파일 생성 과정 확인:

```
split filename.txt --verbose
```

### 구문(Syntax):

```
split [options] filename [prefix]
```
### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-a` | `--suffix-length=N` | 길이가 `N`인 접미사를 생성합니다. (기본값: 2) |
| - | `--additional-suffix=SUFFIX` | 파일 이름에 추가 `SUFFIX`를 덧붙입니다. |
| `-b` | `--bytes=SIZE` | 각 출력 파일에 `SIZE`바이트를 저장합니다. |
| `-C` | `--line-bytes=SIZE` | 각 출력 파일에 레코드를 최대 `SIZE`바이트까지 저장합니다. |
| `-d` | - | 알파벳 접미사 대신 0부터 시작하는 숫자 접미사를 사용합니다. |
| - | `--numeric-suffixes[=FROM]` | `-d`와 동일하지만 시작 값을 설정할 수 있습니다. |
| `-x` | - | 알파벳 접미사 대신 0부터 시작하는 16진수 접미사를 사용합니다. |
| - | `--hex-suffixes[=FROM]` | `-x`와 동일하지만 시작 값을 설정할 수 있습니다. |
| `-e` | `--elide-empty-files` | `-n`을 사용할 때 빈 출력 파일을 생성하지 않습니다. |
| - | `--filter=COMMAND` | 셸 `COMMAND`에 기록합니다. 파일 이름은 `$FILE`입니다. |
| `-l` | `--lines=NUMBER` | 각 출력 파일에 `NUMBER`개의 행 또는 레코드를 저장합니다. |
| `-n` | `--number=CHUNKS` | `CHUNKS`개의 출력 파일을 생성합니다. 자세한 내용은 아래 설명을 참조하십시오. |
| `-t` | `--separator=SEP` | 레코드 구분자로 줄바꿈 대신 `SEP`를 사용합니다. `\0`은 NUL 문자를 지정합니다. |
| `-u` | `--unbuffered` | `-n r/...`과 함께 사용하여 입력을 출력으로 즉시 복사합니다. |
| - | `--verbose` | 각 출력 파일을 열기 직전에 진단 메시지를 출력합니다. |
| - | `--help` | 이 도움말을 표시한 후 종료합니다. |
| - | `--version` | 버전 정보를 출력한 후 종료합니다. |

`SIZE` 인수는 정수와 선택적 단위로 구성됩니다. (예: `10K`는 `10*1024`를 의미합니다.)

단위는 1024의 거듭제곱을 나타내는 `K`, `M`, `G`, `T`, `P`, `E`, `Z`, `Y` 또는 1000의 거듭제곱을 나타내는 `KB`, `MB` 등의 형식을 사용할 수 있습니다.

`CHUNKS`는 다음과 같은 형식으로 지정할 수 있습니다.

| **CHUNKS** | **설명** |
|:---|:---|
| `N` | 입력 크기를 기준으로 `N`개의 파일로 분할합니다. |
| `K/N` | `N`개 중 `K`번째 파일을 표준 출력으로 보냅니다. |
| `l/N` | 행 또는 레코드를 분할하지 않고 `N`개의 파일로 나눕니다. |
| `l/K/N` | 행 또는 레코드를 분할하지 않고 `N`개 중 `K`번째 파일을 표준 출력으로 보냅니다. |
| `r/N` | `l`과 유사하지만 라운드 로빈 방식으로 분배합니다. |
| `r/K/N` | 위와 동일하지만 `N`개 중 `K`번째 파일만 표준 출력으로 보냅니다. |

# `stat` 명령어

`stat` 명령어를 사용하면 파일 또는 파일 시스템의 상태를 표시할 수 있습니다. 이 명령어를 실행한 파일 또는 디렉터리에 대한 유용한 정보를 제공합니다.

### 예시

1. 기본 명령어 사용법

```bash
stat file.txt
```

2. `-c`(또는 `--format`) 인수를 사용하여 확인하려는 정보만 표시합니다. 다음 예제에서는 전체 크기를 바이트 단위로 표시합니다.

```bash
stat file.txt -c %s
```

### 구문(Syntax)

```bash
stat [OPTION] [FILE]
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-L` | `--dereference` | 링크를 따라갑니다. |
| `-f` | `--file-system` | 파일 상태 대신 파일 시스템 상태를 표시합니다. |
| `-c` | `--format=FORMAT` | 출력 형식을 지정합니다. (아래 참조) |
| `-t` | `--terse` | 정보를 간결한 형식으로 출력합니다. |
| - | `--cached=MODE` | 캐시된 속성을 사용하는 방법을 지정합니다. `always`, `never` 또는 `default`를 사용할 수 있습니다. |
| - | `--printf=FORMAT` | `--format`과 유사하지만 백슬래시 이스케이프(`\n`, `\t` 등)를 해석합니다. |
| - | `--help` | 도움말을 표시한 후 종료합니다. |
| - | `--version` | 버전 정보를 출력한 후 종료합니다. |

### 파일에 사용할 수 있는 형식 시퀀스 예시

| **형식** | **설명** |
|:---|:---|
| `%a` | 8진수 형식의 권한 비트 |
| `%A` | 사람이 읽기 쉬운 형식의 권한 비트 및 파일 유형 |
| `%d` | 10진수 형식의 장치 번호 |
| `%D` | 16진수 형식의 장치 번호 |
| `%F` | 파일 유형 |
| `%g` | 소유자의 그룹 ID |
| `%G` | 소유자의 그룹 이름 |
| `%h` | 하드 링크 수 |
| `%i` | 아이노드 번호 |
| `%m` | 마운트 지점 |
| `%n` | 파일 이름 |
| `%N` | 따옴표로 묶은 파일 이름. 심볼릭 링크인 경우 역참조 결과를 표시합니다. |
| `%s` | 전체 크기(바이트) |
| `%u` | 소유자의 사용자 ID |
| `%U` | 소유자의 사용자 이름 |
| `%w` | 파일 생성 시간(사람이 읽기 쉬운 형식). 알 수 없으면 `-`를 표시합니다. |
| `%x` | 마지막 접근 시간(사람이 읽기 쉬운 형식) |
| `%y` | 마지막 데이터 수정 시간(사람이 읽기 쉬운 형식) |
| `%z` | 마지막 상태 변경 시간(사람이 읽기 쉬운 형식) |

# `useradd` 명령어

`useradd` 명령어는 시스템에 사용자 계정을 추가하거나 업데이트할 때 사용합니다.

### 예시

`useradd` 명령어로 새 사용자를 추가하는 구문은 다음과 같습니다.

```
useradd NewUser
```
`useradd` 명령어로 새 사용자를 추가하고 해당 사용자의 홈 디렉터리 경로를 지정하는 구문은 다음과 같습니다.

```bash
useradd -d /home/NewUser NewUser
```

`useradd` 명령어로 새 사용자를 추가하고 특정 ID를 지정하는 구문은 다음과 같습니다.

```bash
useradd -u 1234 NewUser
```

### 구문(Syntax)

```bash
useradd [OPTIONS] NameOfUser
```

### 사용 가능한 옵션

| **플래그** | **설명** | **매개변수** |
|:---|:---|:---|
| `-d` | `/path/to/directory`를 사용자의 로그인 디렉터리 값으로 지정하여 새 사용자를 생성합니다. | `/path/to/directory` |
| `-u` | 사용자의 숫자형 ID를 지정합니다. | `ID` |
| `-g` | 특정 그룹 ID를 사용하는 사용자를 생성합니다. | `GroupID` |
| `-M` | 홈 디렉터리 없이 사용자를 생성합니다. | - |
| `-e` | 만료 날짜가 설정된 사용자를 생성합니다. | `DATE` (형식: `YYYY-MM-DD`) |
| `-c` | 설명이 포함된 사용자를 생성합니다. | `COMMENT` |
| `-s` | 변경된 로그인 셸을 사용하는 사용자를 생성합니다. | `/path/to/shell` |
| `-p` | 사용자의 암호화되지 않은 비밀번호를 설정합니다. | `PASSWORD` |

# `userdel` 명령어

`userdel` 명령어는 사용자 계정과 관련 파일을 삭제할 때 사용합니다.

### 예시

`userdel` 명령어로 사용자를 삭제하는 구문은 다음과 같습니다.

```bash
userdel userName
```

사용자가 아직 로그인한 상태라도 `userdel` 명령어를 사용하여 사용자 계정을 강제로 제거하는 구문은 다음과 같습니다.

```bash
userdel -f userName
```

`userdel` 명령어를 사용하여 사용자의 홈 디렉터리에 있는 파일과 함께 사용자를 삭제하는 구문은 다음과 같습니다.

```bash
userdel -r userName
```

### 구문(Syntax)

```bash
userdel [OPTIONS] userName
```

### 사용 가능한 옵션

| **플래그** | **설명** |
|:---|:---|
| `-f` | 사용자가 로그인한 상태라도 지정한 사용자 계정을 강제로 제거합니다. |
| `-r` | 사용자의 홈 디렉터리와 그 안의 파일, 그리고 사용자의 메일 스풀을 함께 삭제합니다. |
| `-Z` | 사용자의 로그인에 대한 SELinux(Security-Enhanced Linux) 사용자 매핑을 제거합니다. |

# `usermod` 명령어

`usermod` 명령어를 사용하면 명령줄에서 Linux 사용자 계정의 속성을 변경할 수 있습니다. 사용자를 생성한 후 비밀번호나 로그인 디렉터리와 같은 속성을 변경해야 하는 경우가 있습니다. 이러한 작업을 수행할 때 `usermod` 명령어를 사용합니다.

### 구문(Syntax)

```bash
usermod [options] USER
```

#### 참고

슈퍼유저(root)만 `usermod` 명령어를 실행할 수 있습니다.

### 옵션 및 기능

| **옵션** | **설명** |
|:---|:---|
| `-a` | 사용자를 보조 그룹에 추가합니다. |
| `-c` | 사용자 계정에 설명 필드를 추가합니다. |
| `-d` | 기존 사용자 계정의 디렉터리를 변경합니다. |
| `-g` | 사용자의 기본 그룹을 변경합니다. |
| `-G` | 보조 그룹을 추가합니다. |
| `-l` | 기존 사용자의 로그인 이름을 변경합니다. |
| `-L` | 시스템 사용자 계정을 잠급니다. |
| `-m` | 기존 홈 디렉터리의 내용을 새 디렉터리로 이동합니다. |
| `-p` | 암호화되지 않은 비밀번호를 생성합니다. |
| `-s` | 새 계정에 지정한 셸을 설정합니다. |
| `-u` | 사용자 계정에 UID를 할당합니다. |
| `-U` | 잠긴 사용자의 잠금을 해제합니다. |

### 예시:

1. 사용자에 대한 설명(코멘트) 추가:

```
sudo usermod -c "This is test user" test_user
```

2. 사용자의 홈 디렉터리 변경:

```
sudo usermod -d /home/sam test_user
```

3. 사용자 계정 만료일 변경:

```
sudo usermod -e 2021-10-05 test_user
```

4. 사용자의 기본 그룹 변경:

```
sudo usermod -g sam test_user
```

5. 사용자 로그인 이름(계정명) 변경:

``` 
sudo usermod -l test_account test_user
```

6. 사용자 계정 잠금:

```
sudo usermod -L test_user
```

7. 사용자 계정 잠금 해제:

```
sudo usermod -U test_user
```

8. 사용자의 미암호화 비밀번호 설정:

```
sudo usermod -p test_password test_user
```

9. 사용자의 기본 셸 지정:

```
sudo usermod -s /bin/sh test_user
```

10. 사용자의 사용자 ID(UID) 변경:

``` 
sudo usermod -u 1234 test_user
```

# `ionice` 명령어

`ionice` 명령어는 프로세스의 I/O 스케줄링 클래스와 우선순위를 설정하거나 확인할 때 사용합니다.

인수를 지정하지 않으면 `ionice`는 해당 프로세스의 현재 I/O 스케줄링 클래스와 우선순위를 조회합니다.

## Usage

 ```
ionice [options] -p <pid>
```

```
ionice [options] -P <pgid>
```

```
ionice [options] -u <uid>
```

```
ionice [options] <command>
```
## 프로세스의 스케줄링 클래스

프로세스는 다음 세 가지 스케줄링 클래스 중 하나에 속할 수 있습니다.

### 유휴 상태(Idle)

- 유휴 I/O 우선순위를 가진 프로그램은 **정해진 유예 시간 동안 다른 프로그램이 디스크 I/O를 요청하지 않을 때만** 디스크 사용 시간을 할당받습니다.
- 유휴 프로세스가 정상적인 시스템 활동에 미치는 영향은 **0이어야 합니다**.
- 이 스케줄링 클래스는 **우선순위 인수를 사용하지 않습니다**.
- 현재 이 스케줄링 클래스는 일반 사용자도 사용할 수 있습니다(커널 2.6.25부터).

### 최선 노력(Best Effort)

- 특정 I/O 우선순위를 요청하지 않은 모든 프로세스에 적용되는 **기본 스케줄링 클래스**입니다.
- 우선순위 값은 `0`부터 `7`까지이며 **숫자가 낮을수록 우선순위가 높습니다**.
- 동일한 최선 노력 우선순위로 실행되는 프로그램은 **라운드 로빈 방식**으로 처리됩니다.
- 커널 2.6.26 이전에는 I/O 우선순위를 요청하지 않은 프로세스가 형식상 `None` 스케줄링 클래스를 사용했습니다. 그러나 I/O 스케줄러는 이러한 프로세스를 최선 노력 클래스에 속한 것처럼 처리합니다.
- 최선 노력 클래스 내의 우선순위는 프로세스의 CPU nice 수준에서 동적으로 계산됩니다.

```text
io_priority = (cpu_nice + 20) / 5
```

- 커널 2.6.26 이후 CFQ I/O 스케줄러에서는 I/O 우선순위를 요청하지 않은 프로세스가 CPU 스케줄링 클래스를 상속합니다.
- 즉, **프로세스의 I/O 우선순위는 CPU nice 수준에서 파생됩니다**(커널 2.6.26 이전의 경우).

### 실시간(Real Time)

- 실시간 스케줄링 클래스는 **시스템에서 다른 작업이 수행 중이더라도 디스크에 대한 접근 권한을 가장 먼저 부여받습니다**.
- 따라서 다른 프로세스의 실행을 굶길 수 있으므로 실시간 클래스를 사용할 때는 주의해야 합니다.
- 최선 노력 클래스와 마찬가지로 8개의 우선순위 레벨이 정의되어 있으며 각 스케줄링 구간에서 프로세스가 할당받는 시간 조각의 크기를 결정합니다.
- 이 스케줄링 클래스는 일반 사용자(루트가 아닌 사용자)가 사용할 수 없습니다.

## 옵션

| 옵션 | 설명 |
|---|---|
| `-c`, `--class <class>` | 스케줄링 클래스의 이름 또는 번호입니다. `0`: none, `1`: realtime, `2`: best-effort, `3`: idle |
| `-n`, `--classdata <num>` | 지정한 스케줄링 클래스의 우선순위(`0`~`7`)입니다. realtime 및 best-effort 클래스에서만 사용할 수 있습니다. |
| `-p`, `--pid <pid>...` | 이미 실행 중인 지정된 프로세스에 작업을 적용합니다. |
| `-P`, `--pgid <pgrp>...` | 지정된 프로세스 그룹에 속한 실행 중인 프로세스에 작업을 적용합니다. |
| `-t`, `--ignore` | 오류를 무시합니다. |
| `-u`, `--uid <uid>...` | 지정된 사용자가 소유한 실행 중인 프로세스에 작업을 적용합니다. |
| `-h`, `--help` | 도움말을 표시합니다. |
| `-V`, `--version` | 버전 정보를 표시합니다. |

자세한 내용은 `ionice(1)` 매뉴얼을 참고하세요.

## 예제

| 명령어 | 출력 | 설명 |
|---|---|---|
| `$ ionice` | `none: prio 4` | 인수 없이 `ionice`를 실행하면 현재 프로세스의 클래스와 우선순위를 표시합니다. |
| `$ ionice -p 101` | `none: prio 4` | 지정한 PID를 가진 프로세스의 세부 정보(클래스와 우선순위)를 표시합니다. |
| `$ ionice -p 2` | `none: prio 4` | PID가 2인 프로세스의 클래스와 우선순위를 확인합니다. 이 프로세스의 클래스는 `none`, 우선순위는 `4`입니다. |
| `$ ionice -c2 -n0 -p2` | `2 (best-effort), priority 0, process 2` | PID가 2인 프로세스를 최우선순위의 best-effort 프로세스로 설정합니다. |
| `$ ionice -p 2` | `best-effort: prio 0` | PID가 2인 프로세스의 업데이트된 세부 정보를 확인합니다. |
| `$ ionice /bin/ls` |  | `/bin/ls`의 우선순위와 클래스를 확인합니다. |
| `$ ionice -n4 -p2` |  | PID가 2인 프로세스의 우선순위를 `4`로 설정합니다. |
| `$ ionice -p 2` | `best-effort: prio 4` | 앞의 명령어 실행 결과와 비교합니다. 우선순위가 `0`에서 `4`로 변경되었습니다. |
| `$ ionice -c0 -n4 -p2` | `ionice: ignoring given class data for none class` | 커널 2.6.26 이전에는 I/O 우선순위를 요청하지 않은 프로세스가 형식상 `None` 클래스를 사용했습니다. 그러나 I/O 스케줄러는 해당 프로세스를 best-effort 클래스로 처리합니다. |
| `-t` 옵션 |  | 오류를 무시하는 옵션입니다. |
| `$ ionice -c0 -n4 -p2 -t` |  | 앞의 경고를 무시하려면 `-t` 옵션을 사용할 수 있습니다. 이 옵션은 오류를 무시합니다. |


## 결론

 이제 `ionice` 명령어를 살펴보았습니다.

# `du` 명령어

`disk usage`의 약자인 `du` 명령어를 사용하면 지정한 디렉터리의 디스크 공간 사용 정보를 확인할 수 있습니다. 필요한 정보에 맞게 출력 형식을 사용자 지정하려면 적절한 옵션이나 플래그를 함께 사용하면 됩니다.

### 예시:

1. 현재 디렉터리 내 하위 디렉터리의 예상 크기 확인:

```
du
```

2. 지정한 디렉터리 내 하위 디렉터리의 예상 크기 확인:

```
du {PATH_TO_DIRECTORY}
```

### 구문:

```
du [OPTION]... [FILE]...
du [OPTION]... --files0-from=F
```

### 추가 플래그와 기능:

*참고: 모든 옵션을 망라한 목록은 아닙니다.*

|**단축 플래그**|**긴 플래그**|**설명**|
|:---|:---|:---|
|`-a`|`--all`|파일과 디렉터리 모두에 대한 정보를 포함합니다|
|`-c`|`--total`|파일/디렉터리 목록 끝에 총합을 표시합니다|
|`-d`|`--max-depth=N`|명령을 실행한 디렉터리에서 `N` 단계까지의 정보를 제공합니다|
|`-h`|`--human-readable`|파일 크기를 바이트가 아닌 사람이 읽기 쉬운 단위로 표시합니다|
|`-s`|`--summarize`|파일/디렉터리 목록 대신 총 파일 크기만 표시합니다|

# `ping` 명령어

`ping`(Packet Internet Groper) 명령어는 호스트와 서버 또는 다른 호스트 사이의 네트워크 연결을 확인하는 네트워크 유틸리티입니다. 지정한 IP 주소나 URL로 ICMP(Internet Control Message Protocol) 에코 요청을 보내고 응답을 받는 데 걸리는 시간을 측정합니다. 이 지연 시간을 "지연 시간(latency)"이라고 합니다. Ping은 네트워크 문제 해결과 모니터링에 기본적으로 사용되는 도구입니다.

## 지연 시간 이해하기

네트워크에서 지연 시간은 패킷을 보내고 응답을 받기까지 걸리는 시간입니다.

`ping` 명령어는 대상 호스트에 일련의 패킷을 보내고 각 패킷의 왕복에 걸리는 시간을 계산해 지연 시간을 측정합니다. 지연 시간은 일반적으로 밀리초(ms) 단위로 측정합니다. 지연 시간을 이해해야 하는 이유는 다음과 같습니다.

- **네트워크 성능**: 지연 시간이 짧을수록 데이터 전송이 빨라지고 네트워크 응답성이 향상되며 이는 실시간 애플리케이션에 매우 중요합니다.

- **문제 해결**: 높은 지연 시간은 네트워크 혼잡, 패킷 손실 또는 점검이 필요한 연결 문제를 나타낼 수 있습니다.

- **서비스 품질(QoS)**: 서비스 제공업체와 네트워크 관리자는 네트워크 서비스가 품질 표준을 충족하도록 지연 시간 지표를 사용합니다.

기본 ping 구문은 ping 뒤에 호스트 이름, 웹사이트 이름 또는 정확한 IP 주소를 지정합니다.

```
ping [option] [hostname] or [IP address]
```

### 예시:

1. 시스템에 설치된 ping 버전 확인:

```
sudo ping -v
```

2. 원격 호스트(이 경우 google.com)가 작동 중인지 확인하려면 터미널에 입력:

```
ping google.com
```

3. 보낼 패킷 수 제어:
앞서 서버/호스트로 전송할 패킷 수를 지정하지 않았으나, -c 옵션을 사용하면 전송할 패킷 수를 제어할 수 있습니다.

 ```
ping -c 5 google.com
```

4. 패킷 크기 제어:
기본적으로 기본 크기의 패킷이 전송되지만
-s 옵션을 사용하여 더 작거나 큰 패킷을 전송할 수 있습니다.

```
ping -s 40 -c 5 google.com
```
5. **ping 패킷 사이의 시간 간격 변경:**

기본적으로 `ping`은 다음 패킷을 전송하기 전에 1초 동안 기다립니다. `-i` 옵션을 사용하면 이 시간을 변경할 수 있습니다.

```bash
ping -i 2 google.com
```

# `rsync` 명령어

`rsync` 명령어는 가장 많이 사용되는 명령어 중 하나입니다. SSH를 통해 한 서버에서 다른 서버로 파일을 안전하게 복사할 때 사용합니다.

비슷한 작업을 수행하는 `scp` 명령어와 비교하면 `rsync`는 파일을 훨씬 빠르게 전송하며 전송이 중단된 경우에도 전송 과정을 복원하거나 재개할 수 있습니다.

이 튜토리얼에서는 `rsync` 명령어를 사용하여 한 서버에서 다른 서버로 파일을 복사하는 방법과 몇 가지 유용한 팁을 설명합니다.

시작하기 전에 Linux 서버 2대가 필요합니다. 여기서는 데모를 위해 DigitalOcean에서 Ubuntu 서버 2대를 배포하여 사용합니다.

DigitalOcean 서버에 가상 머신을 배포하고 이 안내서를 직접 테스트하는 데 사용할 수 있는 100달러 무료 크레딧은 다음 추천 링크를 통해 받을 수 있습니다.

**[DigitalOcean 100달러 무료 크레딧](https://m.do.co/c/2a9bba940f39)**

## 로컬 서버에서 원격 서버로 파일 전송

가장 일반적인 사용 사례 중 하나입니다. 현재 접속해 있는 서버(원본 서버)의 파일을 원격 서버 또는 대상 서버로 복사하는 방법입니다.

먼저 파일이 있는 서버에 SSH로 접속한 다음, 전송하려는 디렉터리로 이동합니다.

```bash
cd /var/www/html
```

그런 다음 다음 명령어를 실행합니다.

```bash
rsync -avz user@your-remote-server.com:/home/user/dir/
```

위 명령어는 현재 서버의 폴더에 있는 모든 파일과 디렉터리를 원격 서버로 복사합니다.

명령어의 구성은 다음과 같습니다.

- `-a`: 재귀적 복사를 수행하고 파일 권한 등을 보존하도록 지정합니다.
- `-v`: 자세한 정보 표시 모드입니다. 전송 중 표시되는 정보의 양을 늘립니다.
- `-z`: 대상 시스템으로 전송할 때 파일 데이터를 압축합니다. 전송되는 데이터의 양을 줄이므로 느린 연결에서 유용합니다.

명령어와 인수를 매우 알기 쉽게 설명하는 다음 웹 사이트를 참고하는 것을 권장합니다.

[https://explainshell.com/explain?cmd=rsync+-avz](https://explainshell.com/explain?cmd=rsync+-avz)

원격 서버의 SSH 서비스가 표준 포트인 `22`에서 실행되지 않는 경우에는 다음과 같이 특수 SSH 포트와 함께 `rsync`를 사용할 수 있습니다.

```bash
rsync -avz -e 'ssh -p 1234' user@your-remote-server.com:/home/user/dir/
```

## 원격 서버에서 로컬 서버로 파일 전송

원격 서버에서 로컬 서버로 파일을 전송해야 하는 경우에는 다음 구문을 사용합니다.

```bash
rsync -avz your-user@your-remote-server.com:/home/user/dir/ /home/user/local-dir/
```

SSH 포트가 표준 포트가 아닌 경우에는 다음 명령어를 사용할 수 있습니다.

```bash
rsync -avz -e 'ssh -p 2510' your-user@your-remote-server.com:/home/user/dir/ /home/user/local-dir/
```

## 누락된 파일만 전송

누락된 파일만 전송하려면 `--ignore-existing` 플래그를 사용할 수 있습니다.

이 옵션은 웹 사이트나 서버를 마이그레이션한 후 누락된 파일이 없는지 확인하기 위한 최종 동기화 작업에 매우 유용합니다.

기본 명령어에 `--ignore-existing` 플래그만 추가하면 됩니다.

```bash
rsync -avz --ignore-existing user@your-remote-server.com:/home/user/dir/
```

## 결론

`rsync`는 SSH를 통해 한 시스템에서 다른 시스템으로 파일을 빠르고 안전하게 전송할 수 있는 훌륭한 방법입니다.

다른 유용한 Linux 네트워크 도구는 다음 튜토리얼을 참고하십시오.

[알아 두어야 할 Linux 네트워크 도구 15가지](https://devdojo.com/serverenthusiast/top-15-linux-networking-tools-that-you-should-know)

도움이 되었기를 바랍니다.

최초 게시글: [rsync를 사용하여 한 Linux 서버에서 다른 Linux 서버로 파일을 전송하는 방법](https://devdojo.com/bobbyiliev/how-to-transfer-files-from-one-linux-server-to-another-using-rsync)

# `dig` 명령어

`dig` DNS 조회 유틸리티

`dig`는 DNS 네임 서버를 조회하는 유연한 도구입니다. DNS 조회를 수행하고 조회한 네임 서버에서 반환된 응답을 표시합니다.

### 예시

1. `dig`는 도메인 이름 시스템(DNS)을 조회하는 네트워크 관리용 명령줄 도구입니다.

```bash
dig google.com
```

2. 시스템은 검색된 모든 `google.com` DNS 레코드와 IP 주소를 표시합니다.

```bash
dig google.com ANY
```

### 구문

```text
dig [server] [name] [type] [q-type] [q-class] {q-opt}
            {global-d-opt} host [@local-server] {local-d-opt}
            [ host [@local-server] {local-d-opt} [...]]
```

### 추가 플래그 및 기능

```text
domain    도메인 이름 시스템의 도메인입니다.
        q-class  다음 중 하나입니다. (in, hs, ch, ...) [기본값: in]
        q-type   다음 중 하나입니다. (a, any, mx, ns, soa, hinfo, axfr, txt, ...) [기본값: a]
                 (ixfr 유형에는 ixfr=version을 사용합니다.)
        q-opt    다음 중 하나입니다.
                 -4                  (IPv4 쿼리 전송만 사용)
                 -6                  (IPv6 쿼리 전송만 사용)
                 -b address[#port]   (소스 주소/포트에 바인딩)
                 -c class            (쿼리 클래스 지정)
                 -f filename         (일괄 처리 모드)
                 -k keyfile          (TSIG 키 파일 지정)
                 -m                  (메모리 사용량 디버깅 활성화)
                 -p port             (포트 번호 지정)
                 -q name             (쿼리 이름 지정)
                 -r                  (~/.digrc를 읽지 않음)
                 -t type             (쿼리 유형 지정)
                 -u                  (시간을 밀리초가 아닌 마이크로초로 표시)
                 -x dot-notation     (역방향 조회를 위한 단축 형식)
                 -y [hmac:]name:key  (이름이 지정된 Base64 TSIG 키 지정)

        d-opt는 +keyword[=value] 형식이며 keyword에는 다음이 있습니다.
                 +[no]aaflag         (쿼리에서 AA 플래그 설정)
                 +[no]aaonly         (쿼리에서 AA 플래그 설정)
                 +[no]additional     (추가 섹션 표시 제어)
                 +[no]adflag         (쿼리에서 AD 플래그 설정, 기본값은 활성화)
                 +[no]all            (모든 표시 플래그 설정 또는 해제)
                 +[no]answer         (응답 섹션 표시 제어)
                 +[no]authority      (권한 섹션 표시 제어)
                 +[no]badcookie      (BADCOOKIE 응답을 다시 시도)
                 +[no]besteffort     (잘못된 메시지도 파싱 시도)
                 +bufsize[=###]      (EDNS0 최대 UDP 패킷 크기 설정)
                 +[no]cdflag         (쿼리에서 검사 비활성화 플래그 설정)
                 +[no]class          (레코드의 클래스 표시 제어)
                 +[no]cmd            (명령줄 표시 제어 - 전역 옵션)
                 +[no]comments       (패킷 헤더 및 섹션 이름 주석 표시 제어)
                 +[no]cookie         (요청에 COOKIE 옵션 추가)
                 +[no]crypto         (레코드의 암호화 필드 표시 제어)
                 +[no]defname        (검색 목록 사용)
                 +[no]dnssec         (DNSSEC 레코드 요청)
                 +domain=###         (기본 도메인 이름 설정)
                 +[no]dscp[=###]     (DSCP 값을 ###으로 설정 [0..63])
                 +[no]edns[=###]     (EDNS 버전 설정) [0]
                 +ednsflags=###      (EDNS 플래그 비트 설정)
                 +[no]ednsnegotiation (EDNS 버전 협상 설정)
                 +ednsopt=###[:value] (지정한 EDNS 옵션 전송)
                 +noednsopt          (+ednsopt 옵션 목록 초기화)
                 +[no]expandaaaa     (AAAA 레코드 확장)
                 +[no]expire         (만료 시간 요청)
                 +[no]fail           (SERVFAIL 발생 시 다음 서버로 시도하지 않음)
                 +[no]header-only    (질문 섹션 없이 쿼리 전송)
                 +[no]identify       (짧은 응답에서 응답자 식별)
                 +[no]idnin          (IDN 이름 파싱 [TTY에서 기본값은 활성화])
                 +[no]idnout         (IDN 응답 변환 [TTY에서 기본값은 활성화])
                 +[no]ignore         (TC 응답에서 TCP로 전환하지 않음)
                 +[no]keepalive      (EDNS TCP keepalive 요청)
                 +[no]keepopen       (쿼리 사이에 TCP 소켓을 열린 상태로 유지)
                 +[no]mapped         (IPv6를 통한 매핑된 IPv4 허용)
                 +[no]multiline      (레코드를 확장된 형식으로 출력)
                 +ndots=###          (검색 NDOTS 값 설정)
                 +[no]nsid           (네임 서버 ID 요청)
                 +[no]nssearch       (모든 권한 있는 네임 서버 검색)
                 +[no]onesoa         (AXFR에서 SOA 레코드 하나만 출력)
                 +[no]opcode=###     (요청의 opcode 설정)
                 +padding=###        (패딩 블록 크기 설정 [0])
                 +[no]qr             (전송 전에 질문 출력)
                 +[no]question       (질문 섹션 표시 제어)
                 +[no]raflag         (쿼리에서 RA 플래그 설정)
                 +[no]rdflag         (재귀 모드)
                 +[no]recurse        (재귀 모드)
                 +retry=###          (UDP 재시도 횟수 설정) [2]
                 +[no]rrcomments     (레코드별 주석 표시 제어)
                 +[no]search         (검색 목록 사용 여부 설정)
                 +[no]short          (짧은 형식의 응답만 표시 - 전역 옵션)
                 +[no]showsearch     (중간 결과와 함께 검색)
                 +[no]split=##       (16진수/Base64 필드를 여러 부분으로 분할)
                 +[no]stats          (통계 표시 제어)
                 +subnet=addr        (EDNS 클라이언트 서브넷 옵션 설정)
                 +[no]tcflag         (쿼리에서 TC 플래그 설정)
                 +[no]tcp            (TCP 모드)
                 +timeout=###        (쿼리 시간 제한 설정) [5]
                 +[no]trace          (루트부터 위임 경로 추적 [+dnssec])
                 +tries=###          (UDP 시도 횟수 설정) [3]
                 +[no]ttlid          (레코드에서 TTL 표시 제어)
                 +[no]ttlunits       (TTL을 사람이 읽기 쉬운 단위로 표시)
                 +[no]unexpected     (예상치 못한 출처의 응답 출력, 기본값은 비활성화)
                 +[no]unknownformat  (RDATA를 RFC 3597 "unknown" 형식으로 출력)
                 +[no]vc             (TCP 모드)
                 +[no]yaml           (결과를 YAML 형식으로 표시)
                 +[no]zflag          (쿼리에서 Z 플래그 설정)

        호스트 이름 앞에 지정한 전역 d-옵션과 서버는 모든 쿼리에 영향을 줍니다.
        호스트 이름 뒤에 지정한 로컬 d-옵션과 서버는 해당 조회에만 영향을 줍니다.
        -h                           (도움말을 출력한 후 종료)
        -v                           (버전을 출력한 후 종료)
```

# `whois` 명령어

Linux에서 `whois` 명령어는 도메인 소유자, 소유자의 연락처 정보 및 도메인이 사용하는 네임 서버 등 도메인에 대한 정보를 확인할 때 사용합니다.

### 예시

1. 도메인 이름에 대해 WHOIS 조회를 수행합니다.

```bash
whois {Domain_name}
```

2. `-H` 옵션은 많은 도메인 등록 기관이 도메인 정보와 함께 제공하는 긴 법적 고지 사항을 생략합니다.

```
whois -H {Domain_name}
```

### 구문:

```
whois [ -h HOST ] [ -p PORT ] [ -aCFHlLMmrRSVx ] [ -g SOURCE:FIRST-LAST ] 
      [ -i ATTR ] [ -S SOURCE ] [ -T TYPE ] object
```

```
whois -t TYPE
```

```
whois -v TYPE
```

```
whois -q keyword
```

### 추가 플래그와 기능:
| **플래그** | **설명** |
|:---|:---|
| `-h HOST`, `--host HOST` | `HOST`에 연결합니다. |
| `-H` | 일부 레지스트리에서 표시하는 법적 고지 사항을 표시하지 않습니다. |
| `-p`, `--port PORT` | `PORT`에 연결합니다. |
| `--verbose` | 자세한 정보를 표시합니다. |
| `--help` | 온라인 도움말을 표시합니다. |
| `--version` | 클라이언트 버전 정보를 표시합니다. 그 밖의 옵션은 `whois.ripe.net` 및 일부 다른 RIPE 계열 서버에서 인식하는 플래그입니다. |
| `-a` | 미러링된 모든 데이터베이스도 검색합니다. |
| `-b` | 악용 신고 연락처가 포함된 간략한 IP 주소 범위를 반환합니다. |
| `-B` | 객체 필터링을 비활성화합니다. *(이메일 주소를 표시합니다.)* |
| `-c` | `irt` 객체에 대한 참조가 포함된 가장 작은 IP 주소 범위를 반환합니다. |
| `-d` | 역방향 DNS 위임 객체도 반환합니다. |
| `-g SOURCE:FIRST-LAST` | `SOURCE` 데이터베이스에서 첫 번째 업데이트 일련 번호와 마지막 업데이트 일련 번호 사이의 업데이트를 검색합니다. 준실시간 미러링 스트림을 가져올 때 유용합니다. |
| `-G` | 연결된 객체의 그룹화를 비활성화합니다. |
| `-i ATTR[,ATTR]...` | 연결된 속성을 가진 객체를 검색합니다. `ATTR`은 속성 이름이며 속성 값은 위치 기반 `OBJECT` 인수입니다. |
| `-K` | 기본 키 속성만 반환합니다. 단, 세트 객체의 `members` 속성은 항상 반환됩니다. 또한 `organisation`, `person`, `role` 객체의 모든 속성은 반환되지 않는 예외가 있습니다. |
| `-l` | 한 단계 덜 구체적인 객체를 반환합니다. |
| `-L` | 덜 구체적인 객체를 모든 단계에 걸쳐 반환합니다. |
| `-m` | 한 단계 더 구체적인 모든 객체를 반환합니다. |
| `-M` | 더 구체적인 객체를 모든 단계에 걸쳐 반환합니다. |
| `-q KEYWORD` | 서버에서 지원하는 키워드 목록을 반환합니다. `KEYWORD`에는 서버 버전을 확인하는 `version`, 원본 데이터베이스 목록을 확인하는 `sources`, 객체 유형을 확인하는 `types`를 지정할 수 있습니다. |
| `-r` | 연락처 정보에 대한 재귀적 조회를 비활성화합니다. |
| `-R` | 참조를 따라가는 기능을 비활성화하고 서버의 로컬 복사본에 있는 객체를 강제로 표시합니다. |
| `-s SOURCE[,SOURCE]...` | 서버에 `SOURCES`에서 미러링된 객체를 검색하도록 요청합니다. 소스는 쉼표로 구분하며 순서가 중요합니다. 유효한 소스 목록은 `-q sources` 옵션으로 확인할 수 있습니다. |
| `-t TYPE` | `TYPE` 유형 객체의 템플릿을 반환합니다. |
| `-T TYPE[,TYPE]...` | 검색 대상을 `TYPE` 유형의 객체로 제한합니다. 여러 유형은 쉼표로 구분합니다. |
| `-v TYPE` | `TYPE` 유형 객체의 자세한 템플릿을 반환합니다. |
| `-x` | 네트워크 주소 접두사가 정확히 일치하는 항목만 검색합니다. |

# `ssh` 명령어

Linux에서 `ssh` 명령어는 “Secure Shell(보안 셸)”을 의미합니다. 원격 서버나 시스템에 안전하게 연결할 때 사용하는 프로토콜입니다. `ssh`는 호스트와 클라이언트 사이에서 데이터를 암호화된 형태로 전송하므로 더 안전합니다. `ssh`는 TCP/IP 포트 22에서 실행됩니다.

### 예시:

1. 특정 포트 번호로 SSH 연결: 	

```
ssh test.server.com -p 3322
```

2. 개인 키 파일(`-i`)을 사용하여 원격 서버에 SSH 접속:

```
ssh -i private.key user_name@host
```

3. 다른 사용자 이름을 지정하여 SSH 접속 (`-l`):

```
ssh -l alternative-username sample.ssh.com
```

### 구문:

```
ssh user_name@host(IP/Domain_Name)
```

```
ssh -i private.key user_name@host
```

```
ssh sample.ssh.com  ls /tmp/doc
```

### 추가 플래그와 기능

| **플래그** | **설명** |
|:---|:---|
| `-1` | SSH가 SSH-1 프로토콜만 사용하도록 강제합니다. |
| `-2` | SSH가 SSH-2 프로토콜만 사용하도록 강제합니다. |
| `-4` | IPv4 주소만 사용합니다. |
| `-A` | 인증 에이전트 연결 전달을 활성화합니다. |
| `-a` | 인증 에이전트 연결 전달을 비활성화합니다. |
| `-B bind_interface` | 대상 호스트에 연결하기 전에 `bind_interface`의 주소에 바인딩합니다. 둘 이상의 주소를 가진 시스템에서만 유용합니다. |
| `-b bind_address` | 연결의 소스 주소로 로컬 컴퓨터의 `bind_address`를 사용합니다. 둘 이상의 주소를 가진 시스템에서만 유용합니다. |
| `-C` | 더 빠른 데이터 전송을 위해 모든 데이터를 압축합니다(stdin, stdout, stderr 및 전달되는 X11·TCP 연결 데이터 포함). |
| `-c cipher_spec` | 세션 암호화에 사용할 암호화 사양을 선택합니다. |
| `-D [bind_address:]port` | 동적 애플리케이션 수준 포트 전달을 설정합니다. 로컬 측의 지정된 포트에서 수신 대기할 소켓을 할당합니다. 해당 포트로 연결이 들어오면 보안 채널을 통해 전달되며 애플리케이션 프로토콜이 원격 컴퓨터에서 연결할 대상을 결정합니다. |
| `-E log_file` | 디버그 로그를 표준 오류 대신 `log_file`에 추가합니다. |
| `-e escape_char` | 의사 터미널(pty) 세션의 이스케이프 문자를 설정합니다(기본값: `~`). 이스케이프 문자는 줄의 시작 부분에서만 인식됩니다. 이스케이프 문자 뒤에 마침표(`.`)를 입력하면 연결을 닫고, `Ctrl+Z`를 입력하면 연결을 일시 중지하며 이스케이프 문자를 한 번 더 입력하면 해당 문자를 한 번 전송합니다. 문자를 `none`으로 설정하면 모든 이스케이프 기능이 비활성화되어 세션이 완전히 투명해집니다. |
| `-F configfile` | 사용자별 구성 파일을 지정합니다. 기본 사용자별 구성 파일은 `~/.ssh/config`입니다. |
| `-f` | 명령어를 실행하기 직전에 SSH를 백그라운드로 보내도록 요청합니다. |
| `-G` | `Host` 및 `Match` 블록을 평가한 후 SSH 구성을 출력하고 종료합니다. |
| `-g` | 원격 호스트가 로컬에서 전달된 포트에 연결할 수 있도록 허용합니다. |
| `-I pkcs11` | 키를 제공하는 PKCS#11 토큰과 통신할 때 SSH가 사용할 PKCS#11 공유 라이브러리를 지정합니다. |
| `-i identity_file` | 공개 키 인증에 사용할 ID 키(개인 키)를 읽어 올 파일을 지정합니다. |
| `-J [user@]host[:port]` | 먼저 점프 호스트에 SSH로 연결한 다음, 해당 호스트에서 최종 대상까지 TCP 전달을 설정하여 대상 호스트에 연결합니다. |
| `-K` | GSSAPI 기반 인증을 활성화하고 GSSAPI 자격 증명을 서버로 전달(위임)합니다. |
| `-k` | GSSAPI 자격 증명을 서버로 전달(위임)하지 않습니다. |
| `-L [bind_address:]port:host:hostport` 등 | 로컬 클라이언트 호스트의 지정된 TCP 포트 또는 Unix 소켓으로 들어오는 연결을 원격 측의 지정된 호스트와 포트 또는 Unix 소켓으로 전달합니다. |
| `-l login_name` | 원격 컴퓨터에 로그인할 사용자 이름을 지정합니다. |
| `-M` | 연결 공유를 위해 SSH 클라이언트를 “마스터” 모드로 실행합니다. `-M`을 여러 번 지정하면 새 세션 열기 등 다중화 상태를 변경하는 작업마다 `ssh-askpass`를 통한 확인이 필요합니다. |
| `-m mac_spec` | 우선순위 순서로 지정한 MAC(메시지 인증 코드) 알고리즘의 쉼표로 구분된 목록입니다. |
| `-N` | 원격 명령어를 실행하지 않습니다. 포트 전달만 수행할 때 유용합니다. |
| `-n` | 표준 입력을 읽지 않도록 합니다. |
| `-O ctl_cmd` | 활성 연결 다중화 마스터 프로세스를 제어합니다. `ctl_cmd`에는 `check`(마스터 실행 여부 확인), `forward`(명령어 실행 없이 전달 요청), `cancel`(전달 취소), `exit`(마스터 종료 요청), `stop`(추가 다중화 요청 수락 중지 요청) 등을 지정할 수 있습니다. |
| `-o` | 구성 파일에서 사용하는 형식으로 옵션을 지정합니다. 별도의 명령줄 플래그가 없는 옵션을 지정할 때 유용합니다. |
| `-p`, `--port PORT` | 원격 호스트에서 연결할 포트를 지정합니다. |
| `-Q query_option` | 지정한 SSH 프로토콜 버전 2에서 지원하는 알고리즘을 조회합니다. |
| `-q` | 조용한 모드입니다. 대부분의 경고 및 진단 메시지를 표시하지 않습니다. |
| `-R [bind_address:]port:host:hostport` 등 | 원격 서버 호스트의 지정된 TCP 포트 또는 Unix 소켓으로 들어오는 연결을 로컬 측으로 전달합니다. |
| `-S ctl_path` | 연결 공유에 사용할 제어 소켓의 위치를 지정하거나, 연결 공유를 비활성화하려면 `none`을 지정합니다. |
| `-s` | 원격 시스템에서 서브시스템을 실행하도록 요청합니다. 서브시스템은 SSH를 다른 애플리케이션(예: `sftp`)의 보안 전송 수단으로 사용할 수 있게 합니다. |
| `-T` | 의사 터미널 할당을 비활성화합니다. |
| `-t` | 의사 터미널 할당을 강제합니다. 원격 컴퓨터에서 화면 기반 프로그램을 실행할 때 사용할 수 있습니다. `-t`를 여러 번 지정하면 로컬 TTY가 없어도 TTY 할당을 강제합니다. |
| `-V` | 버전 번호를 표시합니다. |
| `-v` | 상세 모드입니다. 연결을 설정하는 동안 수행하는 모든 작업을 표시하며 연결 실패를 디버깅할 때 유용합니다. |
| `-W host:port` | 클라이언트의 표준 입력과 출력을 보안 채널을 통해 지정한 `host:port`로 전달합니다. `-N`, `-T`, `ExitOnForwardFailure`, `ClearAllForwardings`가 자동으로 적용됩니다. |
| `-w local_tun[remote_tun]` | 지정한 터널 장치를 사용하여 클라이언트와 서버 사이에 터널 장치 전달을 설정합니다. 장치는 숫자 ID 또는 다음 사용 가능한 터널 장치를 사용하는 `any`로 지정할 수 있습니다. `remote_tun`을 생략하면 기본값은 `any`입니다. |
| `-X` | X11 전달(GUI 전달)을 활성화합니다. |
| `-x` | X11 전달(GUI 전달)을 비활성화합니다. |
| `-Y` | 신뢰된 X11 전달을 활성화합니다. |
| `-y` | syslog 시스템 모듈을 사용하여 로그 정보를 전송합니다. 기본적으로 이 정보는 표준 오류로 전송됩니다. |

# `awk` 명령어

Awk는 고급 텍스트 처리를 위해 설계된 범용 스크립팅 언어입니다. 주로 보고서 생성 및 데이터 분석 도구로 사용됩니다.

#### AWK로 할 수 있는 작업

1. **AWK 작업**
   - (a) 파일을 한 줄씩 검색합니다.
   - (b) 각 입력 줄을 필드로 나눕니다.
   - (c) 입력 줄 또는 필드를 패턴과 비교합니다.
   - (d) 일치하는 줄에 대해 작업을 수행합니다.

2. **유용한 용도**
   - (a) 데이터 파일을 변환합니다.
   - (b) 형식이 지정된 보고서를 생성합니다.

3. **프로그래밍 구성 요소**
   - (a) 출력 줄의 형식을 지정합니다.
   - (b) 산술 연산과 문자열 연산을 수행합니다.
   - (c) 조건문과 반복문을 사용합니다.

#### 구문

```
awk options 'selection _criteria {action }' input-file > output-file
```

#### Example

다음 텍스트 파일을 실습 예제의 입력 파일로 사용합니다.

    ```
    $cat > employee.txt
    ```
    ```
    ajay manager account 45000
    sunil clerk account 25000
    varun manager sales 50000
    amit manager account 47000
    tarun peon sales 15000
    ```

1. `awk`의 기본 동작: 기본적으로 지정한 파일의 모든 데이터 행을 출력합니다.

```
$ awk '{print}' employee.txt
```

```
ajay manager account 45000
sunil clerk account 25000
varun manager sales 50000
amit manager account 47000
tarun peon sales 15000
```

위 예제에서는 패턴을 지정하지 않았으므로 모든 줄에 동작이 적용됩니다. 인자 없는 `print` 동작은 기본적으로 줄 전체를 출력하므로 파일의 모든 내용이 출력됩니다.

2. 지정한 패턴과 일치하는 행만 출력: 

```
awk '/manager/ {print}' employee.txt 
```

```
ajay manager account 45000
varun manager sales 50000
amit manager account 47000
```

위 예제에서 `awk` 명령어는 'manager'와 일치하는 모든 줄을 출력합니다.

3. 줄을 필드로 분할: `awk`는 각 줄(레코드)을 공백을 기준으로 분할하여 `$1`, `$2`, `$3`, `$4` 등의 위치 변수에 순서대로 저장합니다. `$0`은 줄 전체를 나타냅니다.

```
$ awk '{print $1,$4}' employee.txt 
```

```
ajay 45000
sunil 25000
varun 50000
amit 47000
tarun 15000
```

#### Awk의 내장 변수

Awk의 내장 변수에는 필드 변수인 `$1`, `$2`, `$3` 등이 포함됩니다(`$0`은 전체 줄). 이 변수들은 텍스트 한 줄을 필드라고 하는 개별 단어나 조각으로 나눕니다.

- **NR**: 입력 레코드 수의 현재 누적 개수를 저장합니다. 레코드는 일반적으로 한 줄을 의미합니다. Awk 명령어는 파일의 각 레코드에 대해 패턴/액션 문을 한 번씩 실행합니다.
- **NF**: 현재 입력 레코드에 포함된 필드 수를 저장합니다.
- **FS**: 입력 줄에서 필드를 나누는 데 사용되는 필드 구분 문자를 저장합니다. 기본값은 공백 문자이며 스페이스와 탭 문자를 의미합니다. 필드 구분자를 변경하려면 `FS`를 다른 문자로 재할당할 수 있습니다(일반적으로 `BEGIN` 블록에서 설정).
- **RS**: 현재 레코드 구분 문자를 저장합니다. 기본적으로 입력 줄이 입력 레코드이므로, 기본 레코드 구분 문자는 줄바꿈 문자입니다.
- **OFS**: Awk가 필드를 출력할 때 필드 사이를 구분하는 출력 필드 구분 문자를 저장합니다. 기본값은 공백 하나입니다. `print`에 여러 매개변수를 쉼표로 구분하여 지정하면 각 매개변수 사이에 `OFS` 값이 출력됩니다.
- **ORS**: Awk가 출력할 때 출력 줄을 구분하는 출력 레코드 구분 문자를 저장합니다. 기본값은 줄바꿈 문자입니다. `print`는 출력할 내용의 끝에 `ORS` 값을 자동으로 출력합니다.

# `crontab` 명령어

`crontab`은 개별 사용자의 crontab 파일을 관리하는 데 사용됩니다(Vixie Cron).

`crontab`은 Vixie Cron의 `cron(8)` 데몬을 구동하는 데 사용되는 작업 테이블을 설치, 삭제 또는 나열하는 프로그램입니다. 각 사용자는 자신만의 crontab을 가질 수 있습니다. 이러한 파일은 `/var/spool/cron/crontabs`에 저장되지만 직접 편집하도록 설계된 파일은 아닙니다.

### 구문:

```
crontab [ -u user ] file
crontab [ -u user ] [ -i ] { -e | -l | -r }
```

### 예시:

1. `-l` 옵션: 현재 등록된 crontab 일정을 표준 출력으로 표시합니다.

```
crontab -l
```

2. `-r` 옵션: 현재 등록된 crontab 일정을 삭제합니다.

```
crontab -r
```

3. `-e` 옵션: `VISUAL` 또는 `EDITOR` 환경 변수에 지정된 편집기를 열어 crontab 일정을 편집합니다. 편집기를 종료하고 저장하면 수정된 일정이 자동으로 적용됩니다. 환경 변수가 없으면 시스템 기본 편집기(`/usr/bin/editor`)가 실행됩니다.

```
crontab -e
```

4. 특정 사용자의 crontab을 지정하여 편집할 수도 있습니다. 모든 사용자는 각자의 crontab을 가집니다. 예를 들어 Apache 웹 서버의 실행 계정인 `www-data` 사용자의 일정을 수정하려면 다음과 같이 실행합니다.

```
crontab -u www-data -e 
```

###  도움말 확인

`crontab` 명령어에 대한 전체 가이드를 확인하려면 다음을 참조하세요.

```
man crontab
```
# `xargs` 명령어

`xargs`는 표준 입력으로부터 명령줄을 구성하고 실행할 때 사용합니다.

`grep`과 같은 일부 명령어는 입력을 매개변수로 받을 수 있지만 어떤 명령어는 인수를 받습니다. 이때 `xargs`가 사용됩니다.

### 구문

```bash
xargs [options] [command [initial-arguments]]
```

### 옵션

```bash
-0, --null
```

입력 항목을 공백이 아닌 NUL 문자로 종료합니다. 따옴표와 백슬래시는 특별한 의미를 갖지 않으며 모든 문자를 있는 그대로 처리합니다. 다른 인수와 동일하게 처리되는 파일 끝 문자열 기능을 비활성화합니다. 입력 항목에 공백, 따옴표 또는 백슬래시가 포함될 수 있을 때 유용합니다.

```bash
-a file, --arg-file=file
```

표준 입력 대신 `file`에서 항목을 읽습니다. 이 옵션을 사용하면 명령어가 실행될 때 표준 입력이 변경되지 않습니다. 이 옵션을 사용하지 않으면 표준 입력이 `/dev/null`에서 리디렉션됩니다.

```bash
-o, --open-tty
```

명령어를 실행하기 전에 자식 프로세스의 표준 입력을 `/dev/tty`로 다시 엽니다. `xargs`로 대화형 애플리케이션을 실행하려는 경우 유용합니다.

```bash
--delimiter=delim, -d delim
```

지정한 문자를 기준으로 입력 항목을 종료합니다. 지정하는 구분자는 단일 문자, `\n`과 같은 C 스타일 문자 이스케이프 또는 8진수나 16진수 이스케이프 코드일 수 있습니다. 8진수 및 16진수 이스케이프 코드는 `printf` 명령어와 동일한 방식으로 해석됩니다. 멀티바이트 문자는 지원되지 않습니다.

입력을 처리할 때 따옴표와 백슬래시는 특별한 의미를 갖지 않으며 입력의 모든 문자를 있는 그대로 처리합니다. `-d` 옵션은 파일 끝 문자열 기능을 비활성화합니다. 해당 문자열도 다른 인수와 동일하게 처리됩니다. 입력이 단순히 줄바꿈으로 구분된 항목으로 구성된 경우 이 옵션을 사용할 수 있지만 가능한 경우 프로그램이 `--null`을 사용하도록 설계하는 것이 거의 항상 더 좋습니다.

```bash
-p, --interactive
```

각 명령줄을 실행할지 사용자에게 묻고 터미널에서 한 줄을 읽습니다. 응답이 `y` 또는 `Y`로 시작하는 경우에만 명령줄을 실행합니다. `-t` 옵션을 자동으로 포함합니다.

### 예시

```bash
find /tmp -name core -type f -print | xargs /bin/rm -f
```

`/tmp` 디렉터리와 그 하위 디렉터리에서 이름이 `core`인 파일을 찾아 삭제합니다. 파일 이름에 줄바꿈이나 공백이 포함되어 있으면 이 명령어가 올바르게 작동하지 않는다는 점에 유의하십시오.

```bash
find /tmp -name core -type f -print0 | xargs -0 /bin/rm -f
```

`/tmp` 디렉터리와 그 하위 디렉터리에서 이름이 `core`인 파일을 찾아 삭제합니다. 파일 또는 디렉터리 이름에 공백이나 줄바꿈이 포함된 경우에도 올바르게 처리되도록 파일 이름을 처리합니다.

```bash
find /tmp -depth -name core -type f -delete
```

`/tmp` 디렉터리와 그 하위 디렉터리에서 이름이 `core`인 파일을 찾아 삭제합니다. 이전 예제보다 효율적인 방법입니다. `rm`을 실행하기 위해 `fork(2)`와 `exec(2)`를 호출할 필요가 없고 추가적인 `xargs` 프로세스도 필요하지 않기 때문입니다.

```bash
cut -d: -f1 < /etc/passwd | sort | xargs echo
```

시스템의 모든 사용자에 대한 간결한 목록을 생성합니다.

### 도움말 명령어

다음 명령어를 실행하면 `xargs` 명령어에 대한 전체 안내를 확인할 수 있습니다.

```bash
man xargs
```

# `nohup` 명령어

셸이 종료되면(예를 들어 SSH 세션에서 로그아웃할 때) `HUP`(hang up, 연결 끊김) 시그널이 모든 자식 프로세스에 전송되어 해당 프로세스가 종료될 수 있습니다. 셸을 종료한 후에도 장시간 실행되는 프로세스를 계속 실행해야 한다면 `nohup` 명령어를 사용해야 합니다.

명령어 앞에 `nohup`을 붙이면 해당 명령어가 `HUP` 시그널의 영향을 받지 않게 됩니다. 또한 표준 입력(`STDIN`)은 무시되고 모든 출력은 로컬 파일인 `./nohup.out`으로 리디렉션됩니다.

### 예시:

1. 시간이 오래 걸리는 데비안 패키지 업그레이드에 `nohup` 적용:

```
nohup apt-get -y upgrade
```

### 구문:

```
nohup COMMAND [ARG]...
nohup OPTION
```
# `pstree` 명령어

`pstree` 명령어는 `ps`와 유사하지만 실행 중인 프로세스를 목록으로 나열하는 대신 트리 형태로 표시합니다. 트리 형식은 프로세스 계층 구조를 나타내는 데 더 적합한 경우가 있으며 실행 중인 프로세스를 훨씬 쉽게 시각화할 수 있습니다. 트리의 루트는 `init`이거나 지정한 PID를 가진 프로세스입니다.

### 예시

1. 실행 중인 모든 프로세스를 계층적 트리 구조로 표시합니다.

```bash
pstree
```

2. 지정한 프로세스를 트리의 루트로 하여 트리를 표시합니다.

```bash
pstree [pid]
```

3. 특정 사용자가 시작한 프로세스만 표시합니다.

```bash
pstree [USER]
```

4. 지정한 프로세스의 부모 프로세스를 표시합니다.

```bash
pstree -s [PID]
```

5. 출력을 한 페이지씩 확인하려면 `less` 명령어로 파이프합니다.

```bash
pstree | less
```

### 구문

```bash
ps [OPTIONS] [USER 또는 PID]
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-a` | `--arguments` | 명령줄 인수를 표시합니다. |
| `-A` | `--ascii` | ASCII 선 그리기 문자를 사용합니다. |
| `-c` | `--compact` | 동일한 하위 트리를 압축하지 않습니다. |
| `-h` | `--highlight-all` | 현재 프로세스와 해당 조상 프로세스를 강조 표시합니다. |
| `-H PID` | `--highlight-pid=PID` | 지정한 프로세스와 해당 조상 프로세스를 강조 표시합니다. |
| `-g` | `--show-pgids` | 프로세스 그룹 ID를 표시합니다. `-c` 옵션이 함께 적용됩니다. |
| `-G` | `--vt100` | VT100 선 그리기 문자를 사용합니다. |
| `-l` | `--long` | 긴 줄을 잘라내지 않습니다. |
| `-n` | `--numeric-sort` | PID를 기준으로 출력을 정렬합니다. |
| `-N type` | `--ns-sort=type` | 네임스페이스 유형(cgroup, ipc, mnt, net, pid, user, uts)을 기준으로 정렬합니다. |
| `-p` | `--show-pids` | PID를 표시합니다. `-c` 옵션이 함께 적용됩니다. |
| `-s` | `--show-parents` | 선택한 프로세스의 상위 프로세스를 표시합니다. |
| `-S` | `--ns-changes` | 네임스페이스 전환을 표시합니다. |
| `-t` | `--thread-names` | 전체 스레드 이름을 표시합니다. |
| `-T` | `--hide-threads` | 스레드를 숨기고 프로세스만 표시합니다. |
| `-u` | `--uid-changes` | UID 전환을 표시합니다. |
| `-U` | `--unicode` | UTF-8(유니코드) 선 그리기 문자를 사용합니다. |
| `-V` | `--version` | 버전 정보를 표시합니다. |
| `-Z` | `--security-context` | SELinux 보안 컨텍스트를 표시합니다. |

# `tree` 명령어

Linux의 `tree` 명령어는 디렉터리를 트리 구조로 재귀적으로 나열합니다. 각 항목은 트리의 루트에서 해당 항목까지의 깊이에 따라 들여쓰기되어 표시됩니다.

### 예시

1. 현재 디렉터리를 트리 형태로 표시합니다.
```
tree
```

2. `-L <숫자>` 옵션을 사용하여 너무 깊은 디렉터리 트리가 출력되지 않도록 탐색 깊이를 제한:

```
tree -L 2 /
```

### 구문:

```
tree  [-acdfghilnpqrstuvxACDFQNSUX]  [-L  level [-R]] [-H baseHREF] [-T title]
      [-o filename] [--nolinks] [-P pattern] [-I  pattern]  [--inodes]
      [--device] [--noreport] [--dirsfirst] [--version] [--help] [--filelimit #]
      [--si] [--prune] [--du] [--timefmt  format]  [--matchdirs]  [--from-file]
      [--] [directory ...]
```

### 추가 플래그 및 기능:

| **플래그** | **설명** |
|:---|:---|
| `-a` | 숨김 파일을 포함한 모든 파일을 출력합니다. |
| `-d` | 디렉터리만 나열합니다. |
| `-l` | 디렉터리 내부의 심볼릭 링크를 따라갑니다. |
| `-f` | 각 항목의 기본 이름만 표시하지 않고 전체 경로를 출력합니다. |
| `-x` | 다른 파일 시스템으로 이동하지 않습니다. |
| `-L #` | 재귀 탐색 깊이를 `#`로 제한합니다. |
| `-P REGEX` | 재귀적으로 탐색하되, `REGEX`와 일치하는 파일만 나열합니다. |
| `-I REGEX` | 재귀적으로 탐색하되, `REGEX`와 일치하는 파일은 나열하지 않습니다. |
| `--ignore-case` | 패턴을 일치시킬 때 대소문자를 구분하지 않습니다. |
| `--prune` | 비어 있는 디렉터리를 출력에서 제외합니다. |
| `--filelimit #` | 파일이 `#`개를 초과하는 디렉터리를 생략합니다. |
| `-o FILE` | 표준 출력(`STDOUT`)을 `FILE`로 리디렉션합니다. |
| `-i` | 들여쓰기를 출력하지 않습니다. |

# `whereis` 명령어

`whereis` 명령어는 Linux 시스템에서 지정한 명령어의 소스 파일과 바이너리 파일 위치, 그리고 매뉴얼 섹션의 위치를 찾는 데 사용됩니다. `whereis` 명령어를 `find` 명령어와 비교하면 두 명령어 모두 비슷한 용도로 사용할 수 있지만 `whereis`는 상대적으로 더 적은 시간을 사용하면서 더 정확한 결과를 제공합니다.

#### `whereis` 명령어 사용 시 주의할 점

`whereis` 명령어는 결과를 빠르게 제공하기 위해 `chdir`(디렉터리 변경)을 사용합니다. 따라서 `-M`, `-S` 또는 `-B` 옵션으로 지정하는 경로명은 완전하고 명확한 경로여야 합니다. 즉, `/`로 시작해야 하며 시스템 디렉터리에 실제로 존재하는 유효한 경로여야 합니다. 그렇지 않으면 유효한 결과를 출력하지 않고 종료됩니다.

`whereis` 명령어는 하드코딩된 경로(동적으로 변경되지 않고 사양에 따라 고정된 코드)를 사용하므로, 항상 찾고자 하는 항목을 찾을 수 있는 것은 아닙니다.

### 구문

```
whereis [options] [filename]
```
## 옵션

- `-b`: 바이너리만 검색할 때 사용합니다.
- `-m`: 매뉴얼 섹션만 검색할 때 사용합니다.
- `-s`: 소스 파일만 검색할 때 사용합니다.
- `-u`: 일반적이지 않은 항목을 검색합니다. `-bmsu`로 지정한 유형에 해당하는 파일이 시스템에 존재하지 않으면 해당 소스 파일 또는 바이너리 파일을 일반적이지 않은 항목으로 간주합니다. 따라서 `whereis -m -u *`는 현재 디렉터리에서 매뉴얼 항목이 없는 파일을 검색합니다.
- `-B`: `whereis`가 바이너리를 검색하는 위치를 변경하거나 제한합니다.
- `-M`: `whereis`가 매뉴얼 섹션을 검색하는 위치를 변경하거나 제한합니다.
- `-S`: `whereis`가 소스 파일을 검색하는 위치를 변경하거나 제한합니다.
- `-f`: 마지막 디렉터리 목록을 종료하고 파일 이름의 시작을 나타냅니다. `-B`, `-M` 또는 `-S` 옵션을 사용할 때 반드시 사용해야 합니다.
- `-V`: 버전 정보를 표시하고 종료합니다.
- `-h`: 도움말을 표시하고 종료합니다.

# `printf` 명령어

이 명령어는 형식 지정 규칙을 사용하여 변수의 값을 출력합니다. C 언어의 `printf` 함수와 매우 유사합니다.

### 구문

```bash
printf [-v variable_name] format [arguments]
```

### 옵션

| 옵션 | 설명 |
|---|---|
| `FORMAT` | 출력 형식을 제어하고, `ARGUMENT`가 출력 결과에 어떤 방식으로 표현될지 정의합니다. |
| `ARGUMENT` | `FORMAT`의 정의에 따라 형식이 지정된 출력에 삽입됩니다. |
| `--help` | 도움말을 표시하고 종료합니다. |
| `--version` | 버전 정보를 출력하고 종료합니다. |

### 형식

`FORMAT` 문자열의 구성은 다음 세 부분으로 나눌 수 있습니다.

- **일반 문자**: 원래 사용된 문자 그대로 출력됩니다.
- **해석되는 문자 시퀀스**: 백슬래시(`\`)로 이스케이프된 문자 시퀀스입니다.
- **변환 지정**: `ARGUMENT`가 출력의 일부로 어떤 형식으로 표현될지 정의합니다.

다음 예에서 이러한 부분을 확인할 수 있습니다.

```bash
printf " %s is where over %d million developers shape \"the future of sofware.\" " Github 65
```

출력 결과:

```
Github is where over 65 million developers shape "the future of sofware."
```

변환 지정자는 `%s`와 `%d`, 두 가지가 있으며 _the future of software_라는 단어를 감싸는 여는 큰따옴표와 닫는 큰따옴표라는 두 개의 이스케이프 문자가 있습니다. 그 외의 문자는 일반 문자입니다.

### 변환 지정

각 변환 지정은 `%`로 시작하고 `변환 문자`로 끝납니다. `%`와 `변환 문자` 사이에는 다음 항목이 순서대로 올 수 있습니다.

| 항목 | 설명 |
|---|---|
| `-` | 빼기 기호입니다. 인수의 변환 결과를 왼쪽으로 정렬하도록 `printf`에 지시합니다. |
| _숫자_ | 필드 너비를 지정하는 정수입니다. `printf`는 최소 _숫자_만큼의 문자 너비를 갖는 필드에 `ARGUMENT`의 변환 결과를 출력합니다. 필요한 경우 필드 너비를 맞추기 위해 왼쪽에 공백을 채웁니다(왼쪽 정렬을 지정한 경우에는 오른쪽에 채움). |
| `.` | 필드 너비와 정밀도를 구분하는 마침표입니다. |
| _숫자_ | 정밀도를 지정하는 정수입니다. 문자열에서 출력할 최대 문자 수, 부동 소수점 값에서 소수점 이하 자릿수 또는 정수의 최소 자릿수를 지정합니다. |
| `h` 또는 `l` | 각각 short 정수와 long 정수를 구분합니다. 일반적으로 컴퓨터 프로그래밍에서만 필요합니다. |

변환 문자는 `printf`에 출력할 인수의 유형을 알려주며 다음과 같습니다.

| 변환 문자 | 인수 유형 |
|---|---|
| `s` | 문자열 |
| `c` | ASCII 코드에 해당하는 문자로 표현되는 정수 |
| `d`, `i` | 10진수로 표현되는 정수 |
| `o` | 부호 없는 8진수로 표현되는 정수 |
| `x`, `X` | 부호 없는 16진수로 표현되는 정수 |
| `u` | 부호 없는 10진수로 표현되는 정수 |
| `f` | 기본 정밀도가 6인 부동 소수점 숫자 |
| `e`, `E` | 과학적 표기법으로 표현되는 부동 소수점 숫자 |
| `p` | 메모리 주소 포인터 |
| `%` | 변환하지 않음 |

다음은 `printf`에서 `ARGUMENT`를 출력하는 몇 가지 예입니다. 여기서는 어떤 단어든 사용할 수 있지만 공백과 관련된 위치를 쉽게 확인할 수 있도록 `linuxcommand`라는 단어를 사용하고 큰따옴표로 감쌌습니다.

| 형식 문자열 | 인수 문자열 | 출력 문자열 |
|---|---|---|
| `"%s"` | `"linuxcommand"` | `"linuxcommand"` |
| `"%5s"` | `"linuxcommand"` | `"linuxcommand"` |
| `"%.5s"` | `"linuxcommand"` | `"linux"` |
| `"%-8s"` | `"linuxcommand"` | `"linuxcommand"` |
| `"%-15s"` | `"linuxcommand"` | `"linuxcommand "` |
| `"%12.5s"` | `"linuxcommand"` | `" linux"` |
| `"%-12.5"` | `"linuxcommand"` | `"linux "` |
| `"%-12.4"` | `"linuxcommand"` | `"linu "` |

참고:

- `printf`에서는 변환 문자열의 개수와 `ARGUMENT`의 개수가 일치해야 합니다.
- `printf`는 변환 문자열을 일대일로 대응시키며 각 변환 문자열에 대해 정확히 하나의 `ARGUMENT`가 있기를 기대합니다.
- 변환 문자열은 항상 왼쪽에서 오른쪽 순서로 해석됩니다.

다음은 예제입니다.

입력:

```
printf "We know %f is %s %d" 12.07 "larger than" 12
```
출력:

```text
12.070000이 12보다 크다는 것을 알 수 있습니다.
```

위 예제에는 `_12.07_`, `_larger than_`, `_12_`라는 세 개의 인수가 있습니다. 각 인수는 지정된 세 개의 변환 문자열(`%f`, `%d`, `%s`)에 왼쪽부터 일대일로 대응하여 해석됩니다.

`printf`가 특수 문자로 해석하는 문자 시퀀스:

| **이스케이프 문자** | **설명** |
|:---|:---|
| `\a` | 경고를 발생시킵니다. (벨 소리를 재생합니다.) 일반적으로 ASCII BEL 문자입니다. |
| `\b` | 백스페이스를 출력합니다. |
| `\c` | `printf`가 더 이상 출력하지 않도록 합니다. |
| `\e` | 이스케이프 문자를 출력합니다. (ASCII 코드 27) |
| `\f` | 폼 피드를 출력합니다. |
| `\n` | 줄바꿈을 출력합니다. |
| `\r` | 캐리지 리턴을 출력합니다. |
| `\t` | 수평 탭을 출력합니다. |
| `\v` | 수직 탭을 출력합니다. |
| `\"` | 큰따옴표(`"`)를 출력합니다. |
| `\\` | 백슬래시(`\`)를 출력합니다. |
| `\NNN` | 8진수 값 `NNN`에 해당하는 바이트를 출력합니다. (1~3자리) |
| `\xHH` | 16진수 값 `HH`에 해당하는 바이트를 출력합니다. (1~2자리) |
| `\uHHHH` | 16진수 값 `HHHH`에 해당하는 유니코드 문자를 출력합니다. (4자리) |
| `\UHHHHHHHH` | 16진수 값 `HHHHHHHH`에 해당하는 유니코드 문자를 출력합니다. (8자리) |
| `%b` | 위에 나열된 `\` 이스케이프를 해석한 문자열로 `ARGUMENT`를 출력합니다. 단, 8진수 이스케이프는 `\0` 또는 `\0NN` 형식을 사용합니다. |

### 예시

`printf`에서 일반적으로 사용하는 형식 지정자는 다음 예제와 같습니다.

- `%s`

```bash
$ printf "%s\n" "Printf command documentation!"
```

이 명령어는 셸에 `Printf command documentation!`을 출력합니다.


### `printf` 명령어의 기타 주요 속성:

- `%b`: 백슬래시 이스케이프 시퀀스를 확장하여 인수를 출력합니다.
- `%q`: 입력으로 재사용할 수 있도록 셸 인용 형식으로 인수를 출력합니다.
- `%d`, `%i`: 부호 있는 10진 정수 형식으로 인수를 출력합니다.
- `%u`: 부호 없는 10진 정수 형식으로 인수를 출력합니다.
- `%o`: 부호 없는 8진수(8진법) 정수 형식으로 인수를 출력합니다.
- `%x`, `%X`: 부호 없는 16진수(16진법) 정수 형식으로 인수를 출력합니다. `%x`는 소문자를 출력하고 `%X`는 대문자를 출력합니다.
- `%e`, `%E`: 지수 표기법의 부동 소수점 형식으로 인수를 출력합니다. `%e`는 소문자를 출력하고 `%E`는 대문자를 출력합니다.
- `%a`, `%A`: 16진수(16진법) 소수 표기의 부동 소수점 형식으로 인수를 출력합니다. `%a`는 소문자를 출력하고 `%A`는 대문자를 출력합니다.
- `%g`, `%G`: 주어진 값과 정밀도에 더 적합한 일반 표기법 또는 지수 표기의 부동 소수점 형식으로 인수를 출력합니다. `%g`는 소문자를 출력하고 `%G`는 대문자를 출력합니다.
- `%c`: 인수를 단일 문자로 출력합니다.
- `%f`: 인수를 부동 소수점 숫자로 출력합니다.
- `%s`: 인수를 문자열로 출력합니다.
- `%%`: `%` 기호를 출력합니다.

#### 추가 예제

입력:

```
printf 'Hello\nyoung\nman!'
```

출력 결과:

```
hello 
young
man!
```

두 개의 `\n` 개행 문자가 문장을 3개의 단어/줄로 나눕니다.

입력 데이터:

```
printf "%f\n" 2.5 5.75
```

출력 결과

```
2.500000
5.750000
```
`%f` 지정자는 `\n`과 함께 사용되어 두 인수를 부동 소수점 형식으로 해석하고, 각 인수를 서로 다른 줄에 출력합니다.

# `cut` 명령어

`cut` 명령어는 파일의 각 행에서 특정 부분을 제거하거나 추출할 때 사용합니다. 각 `FILE`에서 선택한 행의 일부를 표준 출력으로 출력합니다. `FILE`을 지정하지 않거나 `FILE`에 `-`를 지정하면 표준 입력을 읽습니다.

### 사용법 및 예시

1. 파일에서 특정 필드를 선택합니다.

```bash
cut -d "delimiter" -f (field number) file.txt
```

2. 특정 문자를 선택합니다.

```bash
cut -c [(k)-(n)/(k),(n)/(n)] filename
```

여기서 **`k`**는 각 행에서 문자가 시작되는 위치를 나타내고, **`n`**은 문자가 끝나는 위치를 나타냅니다. `k`와 `n`이 `-`로 연결되어 있으면 지정한 범위의 문자를 선택하고, `-`로 연결되어 있지 않으면 입력 파일의 각 행에서 지정한 위치의 문자만 선택합니다.

3. 특정 바이트를 선택합니다.

```bash
cut -b 1,2,3 filename             // 1, 2, 3번 바이트 선택
cut -b 1-4 filename               // 1~4번 바이트 선택
cut -b 1- filename                // 1번 바이트부터 파일 끝까지 선택
cut -b -4 filename                // 파일 시작부터 4번 바이트까지 선택
```

**탭과 백스페이스**는 1바이트 크기의 문자로 처리됩니다.

### 구문

```bash
cut OPTION... [FILE]...
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-b` | `--bytes=LIST` | 지정한 바이트만 선택합니다. |
| `-c` | `--characters=LIST` | 지정한 문자만 선택합니다. |
| `-d` | `--delimiter=DELIM` | 필드 구분자로 TAB 대신 `DELIM`을 사용합니다. |
| `-f` | `--fields` | 지정한 필드만 선택합니다. `-s` 옵션을 지정하지 않은 경우 구분 문자가 없는 행도 출력합니다. |
| `-s` | `--only-delimited` | 구분 문자가 포함되지 않은 행은 출력하지 않습니다. |
| `-z` | `--zero-terminated` | 행 구분자로 줄바꿈 대신 NUL 문자를 사용합니다. |

# `sed` 명령어

`sed` 명령어는 **stream editor(스트림 편집기)**의 약자입니다. 스트림 편집기는 입력 스트림(파일 또는 파이프라인을 통해 전달된 입력)에 대해 기본적인 텍스트 변환을 수행하는 데 사용됩니다. 예를 들어 파일에서 검색, 찾기 및 바꾸기, 삽입 또는 삭제와 같은 다양한 작업을 수행할 수 있습니다.

`sed`는 스크립트를 사용해 편집할 수 있는 `ed`와 같은 편집기와 어느 정도 유사하지만 입력을 한 번만 순회하면서 처리하므로 더 효율적입니다. 그러나 다른 유형의 편집기와 `sed`를 특히 구별하는 기능은 파이프라인에서 텍스트를 필터링할 수 있다는 점입니다.

`sed` 명령어는 주로 텍스트를 치환하거나 찾기 및 바꾸기 작업을 수행할 때 사용됩니다. 파일을 직접 열지 않고도 `sed`를 사용하여 파일을 편집할 수 있으므로, 파일에서 특정 내용을 찾아 바꾸는 더 빠른 방법이 될 수 있습니다. 또한 복잡한 패턴을 검색할 수 있도록 기본 정규 표현식과 확장 정규 표현식을 지원합니다. 대부분의 Linux 배포판에는 GNU `sed`가 기본적으로 미리 설치되어 있습니다.

### 예시:

1. `sed`로 문자열을 찾아 다른 문자열로 교체:

```
sed -i 's/{search_regex}/{replace_value}/g' input-file
```

2. `find`와 조합하여 하위 디렉터리 내 문자열을 재귀적으로 일괄 교체:

> 디렉터리 내에서 특정 문자열을 포함하는 파일을 재귀적으로 검색하여 일괄 교체하고 싶을 때가 있습니다. `find` 명령어로 파일을 찾은 뒤 파이프를 통해 `sed`로 전달하면 손쉽게 처리할 수 있습니다.
다음 명령어는 현재 작업 디렉터리에서 파일을 재귀적으로 검색한 후 파일 이름을 `sed`로 전달하여 일괄 교체합니다.

```
find . -type f -exec sed -i 's/{search_regex}/{replace_value}/g' {} +
```

### 구문:

```
sed [OPTION]... {script-only-if-no-other-script} [INPUT-FILE]... 
```
- `OPTION` `sed` 옵션입니다. 파일을 직접 수정하거나, 조용히 실행하거나, 심볼릭 링크를 따라가거나, 행 길이를 지정하거나, NUL 데이터를 사용하는 등의 기능이 있습니다.
- `{script-only-if-no-other-script}` 사용 가능한 경우 해당 스크립트를 명령어에 추가합니다.
- `INPUT-FILE` 입력 스트림입니다. 파일 또는 파이프라인에서 전달된 입력을 의미합니다.

옵션을 지정하지 않으면 첫 번째 옵션이 아닌 인수를 해석할 `sed` 스크립트로 처리합니다. 나머지 인수는 입력 파일의 이름으로 처리됩니다. 입력 파일을 지정하지 않으면 표준 입력을 읽습니다.

GNU `sed` 홈페이지: [http://www.gnu.org/software/sed/](http://www.gnu.org/software/sed/)

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-i[SUFFIX]` | --in-place[=SUFFIX] | 파일을 직접 수정합니다. `SUFFIX`를 지정하면 백업 파일을 생성합니다. |
| `-n` | --quiet, --silent | 패턴 공간을 자동으로 출력하지 않습니다. |
| `-e script` | --expression=script | 실행할 명령어에 스크립트를 추가합니다. |
| `-f script-file` | --file=script-file | `script-file`의 내용을 실행할 명령어에 추가합니다. |
| `-l N` | --line-length=N | `l` 명령어에 사용할 원하는 줄바꿈 길이를 지정합니다. |
| `-r` | --regexp-extended | 스크립트에서 확장 정규 표현식을 사용합니다. |
| `-s` | --separate | 파일을 하나의 연속된 긴 스트림이 아니라 서로 পৃথ개의 파일로 처리합니다. |
| `-u` | --unbuffered | 입력 파일에서 최소한의 데이터만 읽고 출력 버퍼를 더 자주 비웁니다. |
| `-z` | --null-data | 행을 NUL 문자로 구분합니다. |

### 시작하기 전에

처음에는 복잡하고 어렵게 보일 수 있지만 `sed`를 사용하여 파일에서 텍스트를 검색하고 바꾸는 작업은 매우 간단합니다.

더 자세한 내용은 [GNU `sed` 매뉴얼](https://www.gnu.org/software/sed/manual/sed.html)을 참고하세요.

# `vim` 명령어

[vim](https://www.vim.org/)은 Linux, BSD 및 macOS에 포함되어 제공되는 Unix용 텍스트 편집기입니다. Vim은 빠르고 강력한 편집기로 알려져 있는데, 이는 부분적으로 터미널에서 실행할 수 있는 작은 프로그램이기 때문입니다(그래픽 인터페이스도 제공됩니다).

Vim 텍스트 편집기는 [Bram Moolenaar](https://en.wikipedia.org/wiki/Bram_Moolenaar)가 개발했습니다. 대부분의 파일 형식을 지원하며 Vim 편집기는 프로그래머를 위한 편집기로도 알려져 있습니다. 주로 메뉴나 마우스를 사용하지 않고 키보드만으로 모든 작업을 수행할 수 있기 때문입니다.

::: {.callout-note}
`vim`과 `vi`를 혼동하지 마세요. “Visual”을 의미하는 `vi`는 [Bill Joy](https://en.wikipedia.org/wiki/Bill_Joy)가 1976년에 개발한 텍스트 편집기입니다. `vim`은 “Vi Improved”의 약자로, `vi` 편집기를 개선한 클론입니다. 
:::

### `vim`에 관해 가장 많이 검색되는 질문

```text
Vim 편집기를 어떻게 종료하나요?
```

Vim 편집기에 관해 가장 많이 검색되는 질문은 매우 재미있어 보이지만 실제로 Vim을 처음 사용하는 사람은 시작하자마자 편집기에서 빠져나오지 못하는 경우가 많습니다.

파일을 저장하고 Vim 편집기를 종료하는 명령어는 `:wq` 또는 `:x`입니다.

파일을 저장하지 않고 Vim 편집기를 종료하는 명령어는 `:q!`입니다.

#### 재미로 읽어 보기

같은 질문에 대한 [설문 조사](https://stackoverflow.blog/2017/05/23/stack-overflow-helping-one-million-developers-exit-vim/)가 있습니다. 이 글을 읽고 Vim 편집기를 종료하려고 하지 마세요.

### 설치

먼저 Vim이 이미 설치되어 있는지 확인하려면 터미널에 다음 명령어를 입력하세요.

```
vim --version
```

이미 설치되어 있다면 버전이 출력되며 설치되지 않았다면 아래 명령어로 설치할 수 있습니다.

Ubuntu/Debian 계열:

```
sudo apt-get install vim
```

Arch Linux 계열:

```
sudo pacman -S vim
```

CentOS/Fedora 계열:

```
sudo yum install vim
```

CentOS/Fedora에서 고급 기능을 사용하려면 향상된 vim 에디터를 설치해야 합니다. 다음 명령어를 실행하세요.

```
sudo yum install -y vim-enhanced
```

macOS 계열:

```
brew install vim
```

### 구문:

```
vim [FILE_PATH/FILE_NAME]
```

### 예시:

1. 현재 디렉터리에서 `demo.txt` 파일 열기:

```
vim demo.txt
```

2. 특정 디렉터리의 파일 열기:

```
vim {File_Path/filename}
```

3. 특정 행 번호부터 파일 열기:

```
vim {File_Path/filename} +LINE_NUMBER
```

### Vim 편집기의 모드: 
Vim에는 몇 가지 모드가 있는지에 대해 의견이 나뉘지만 가장 많이 사용하게 될 모드는 `명령 모드`와 `입력 모드`입니다. 이러한 [모드](https://www.freecodecamp.org/news/vim-editor-modes-explained/)를 사용하면 문서 작성과 저장, 검색 및 치환 기능을 활용한 고급 편집 등 거의 모든 작업을 수행할 수 있습니다. Vim에는 일반 모드, 입력 모드, 명령줄 모드, 비주얼 모드가 있다는 설명도 사용됩니다. [inpa.tistory](https://inpa.tistory.com/entry/LINUX-%F0%9F%93%9A-Vi-Vim-%EC%97%90%EB%94%94%ED%84%B0-%EB%8B%A4%EB%A3%A8%EA%B8%B0-%EB%AA%85%EB%A0%B9%EC%96%B4-%F0%9F%92%AF-%EC%A0%95%EB%A6%AC)

### Vim 편집기 사용 흐름

1. `vim filename`을 사용하여 새 파일이나 기존 파일을 엽니다.
2. `i`를 입력하여 입력 모드로 전환하고 파일 편집을 시작합니다.
3. 파일의 내용을 입력하거나 수정합니다.
4. 작업이 끝나면 `Esc` 키를 눌러 입력 모드를 종료하고 명령 모드로 돌아갑니다.
5. `:w` 또는 `:wq`를 입력하여 파일을 저장하거나 저장 후 종료합니다.

### Vim에서 이동하기

자주 사용하는 명령어는 다음과 같습니다.

- `j`: 한 행 아래로 이동합니다.
- `k`: 한 행 위로 이동합니다.
- `h`: 한 문자 왼쪽으로 이동합니다.
- `l`: 한 문자 오른쪽으로 이동합니다.
- `w`: 한 단어 앞으로 이동합니다.
- `b`: 한 단어 뒤로 이동합니다.
- `e`: 단어의 끝으로 이동합니다.
- `0`: 행의 시작으로 이동합니다.
- `$`: 행의 끝으로 이동합니다.
- `gg`: 파일의 시작으로 이동합니다.
- `G`: 파일의 끝으로 이동합니다.
- `:linenumber`: 특정 행 번호로 이동합니다.

### 복사, 붙여넣기 및 삭제

1. 복사(Yank):

Vim에서 복사하기는 “yanking”이라고 합니다.

- `yy`: 현재 행을 복사합니다.
- `2yy`: 2개 행을 복사합니다.
- `y$`: 커서 위치부터 행의 끝까지 복사합니다.
- `y^`: 커서 위치부터 행의 시작까지 복사합니다.
- `yw`: 단어 하나를 복사합니다.
- `y}`: 문단의 끝까지 복사합니다.

2. 붙여넣기:

- `p`: 커서 뒤에 붙여넣습니다.
- `P`: 커서 앞에 붙여넣습니다.

3. 삭제:

- `x`: 문자 하나를 삭제합니다.
- `dd`: 현재 행 전체를 삭제합니다.
- `2dd`: 2개 행을 삭제합니다. (또는 원하는 숫자 `n`을 사용하여 `ndd` 형식으로 입력합니다.)
- `d$`: 커서 위치부터 행의 끝까지 삭제합니다.
- `d^`: 커서 위치부터 행의 시작까지 삭제합니다.
- `dG`: 커서 위치부터 파일의 끝까지 삭제합니다.
- `dgg`: 커서 위치부터 파일의 시작까지 삭제합니다.
- `dw`: 커서 위치부터 단어의 끝까지 삭제합니다.
- `di"`: 큰따옴표 내부의 내용을 삭제합니다.
- `diw`: 단어 내부를 삭제합니다. (공백 제외)
- `dip`: 문단 내부를 삭제합니다. (줄바꿈 제외)

### 선택(비주얼 모드)

- `v`: 문자 단위 선택을 시작합니다.
- `V`: 행 단위 선택을 시작합니다.
- `Ctrl + V`: 블록 단위 선택을 시작합니다.

### 대화형 학습

이 대화형 튜토리얼에서는 `vim` 명령어를 사용하는 다양한 방법을 배울 수 있습니다.

[Open Vim 튜토리얼](https://www.openvim.com/)

### 추가 플래그 및 기능

| **플래그/옵션** | **설명** |
|:---|:---|
| `-e` | Ex 모드로 시작합니다. ([Ex 모드](http://vimdoc.sourceforge.net/htmldoc/intro.html#Ex-mode) 참조) |
| `-R` | 읽기 전용 모드로 시작합니다. |
| `-R` | 읽기 전용 모드로 시작합니다. |
| `-g` | [GUI](http://vimdoc.sourceforge.net/htmldoc/gui.html#GUI)를 시작합니다. |
| `-eg` | GUI를 Ex 모드로 시작합니다. |
| `-Z` | `vim`과 같지만 제한 모드로 실행합니다. |
| `-d` | diff 모드로 시작합니다. [diff 모드](http://vimdoc.sourceforge.net/htmldoc/diff.html#diff-mode) |
| `-h` | 사용법(도움말) 메시지를 표시한 후 종료합니다. |
| `+NUMBER` | 파일을 열고 커서를 `NUMBER`로 지정한 행 번호에 둡니다. |

### Vim에 대해 더 알아보기

Vim은 하루 만에 배울 수 없습니다. 일상적인 작업에서 사용하며 Vim 편집기를 직접 익혀 보십시오.

```vim
vim
```

Vim에 대해 더 알아보려면 다음 문서를 참고하십시오.

[Daniel Miessler의 문서](https://danielmiessler.com/study/vim/)

# `chown` 명령어

`chown` 명령어를 사용하면 파일이나 디렉터리의 소유권을 변경할 수 있습니다. 사용자와 그룹은 Linux의 기본 구성 요소이며 `chown`을 사용하여 파일이나 디렉터리의 소유자를 변경할 수 있습니다. 디렉터리의 소유권을 재귀적으로 변경하는 것도 가능합니다. `chown`은 지정한 파일 또는 디렉터리의 소유자를 변경하는 명령어입니다. [ibm](https://www.ibm.com/docs/ko/ssw_aix_72/c_commands/chown.html)

### 예시:
1. 파일의 소유자를 변경합니다.

```bash
chown user file.txt
```

2. 파일의 그룹을 변경합니다.

```bash
chown :group file.txt
```

3. 한 줄에서 사용자와 그룹을 변경합니다.

```bash
chown user:group file.txt
```

4. 폴더와 그 하위 항목의 소유권을 재귀적으로 변경합니다.

```bash
chown -R user:group folder
```

### 구문

```bash
chown [-OPTION] [DIRECTORY_PATH]
```

# `find` 명령어

`find` 명령어는 이름, 크기, 수정 시간, 권한 등 다양한 조건을 기준으로 파일과 디렉터리를 검색할 수 있는 Linux의 강력한 유틸리티 중 하나입니다. `chown`은 파일이나 디렉터리의 소유자와 그룹을 변경할 때 사용하며 `-R` 옵션을 사용하면 하위 항목에도 재귀적으로 적용됩니다. [rhrhth23.tistory](https://rhrhth23.tistory.com/47)

## 기본 구문

```bash
find [path] [options] [expression]
```

**간단히 말하면:**

> `[path]`에서 검색을 시작하고, `[options 또는 filters]`를 적용한 다음, 결과에 대해 `[action]`을 수행합니다.

예시:

```bash
find /home/user -name "*.log"
```

`/home/user` 아래에서 모든 `.log` 파일을 검색합니다.

## 일반적인 사용 사례

### 1. 정확한 이름으로 파일 검색 {.unnumbered}

```bash
find ./directory1 -name sample.txt
```

`directory1` 및 모든 하위 디렉터리에서 `sample.txt`를 검색합니다.

대소문자를 구분하지 않고 검색합니다.

```bash
find ./directory1 -iname "sample.txt"
```

### 2. 패턴(와일드카드)으로 파일 검색 {.unnumbered}

`directory1` 아래에서 모든 `.txt` 파일을 검색합니다.

```bash
find ./directory1 -name "*.txt"
```

추가 예시:

```bash
find /var/log -name "*.log"
find /etc -name "conf*"
```

### 3. 이름으로 디렉터리 검색 {.unnumbered}

```bash
find / -type d -name test
```

루트 디렉터리(`/`)부터 이름이 `test`인 모든 디렉터리를 나열합니다.

### 4. 빈 파일 및 디렉터리 검색 {.unnumbered}

```bash
find . -empty
```

현재 폴더에서 빈 파일과 빈 디렉터리를 모두 검색합니다.

빈 파일만 검색합니다.

```bash
find . -type f -empty
```

### 5. 수정 또는 접근 시간으로 파일 검색 {.unnumbered}

| **옵션** | **설명** |
|:---|:---|
| `-mtime n` | `n`일 전에 수정된 파일 |
| `-atime n` | `n`일 전에 접근한 파일 |
| `-ctime n` | `n`일 전에 상태가 변경된 파일 |
| `-mmin n` | `n`분 전에 수정된 파일 |

예시:

최근 2일 이내에 수정된 파일을 검색합니다.

```bash
find /var/log -mtime -2
```

최근 1시간 이내에 수정된 파일을 검색합니다.

```bash
find . -mmin -60
```

### 6. 크기로 파일 검색 {.unnumbered}

| **표현식** | **의미** |
|:---|:---|
| `+n` | `n`보다 큼 |
| `-n` | `n`보다 작음 |
| `n` | 정확히 `n` |

예시:

100MB보다 큰 파일을 검색합니다.

```bash
find / -size +100M
```

10KB보다 작은 파일을 검색합니다.

```bash
find . -size -10k
```

### 7. 다른 파일보다 최근에 수정된 파일 검색 {.unnumbered}

```bash
find . -newer reference.txt
```

`reference.txt` 이후에 수정된 파일을 나열합니다.

### 8. 유형으로 파일 검색 {.unnumbered}

| **유형** | **설명** |
|:---|:---|
| `f` | 일반 파일 |
| `d` | 디렉터리 |
| `l` | 심볼릭 링크 |
| `b` | 블록 장치 |
| `c` | 문자 장치 |

예시:

모든 블록 장치를 검색합니다.

```bash
find /dev -type b
```

### 9. 권한으로 파일 검색 {.unnumbered}

정확히 644 권한을 가진 파일을 검색합니다.

```bash
find /var/www -type f -perm 644
```

모든 사용자가 실행할 수 있는 파일을 검색합니다.

```bash
find /usr -type f -perm /111
```

### 10. 검색된 파일에 명령어 실행 {.unnumbered}

`-exec` 옵션을 사용하면 검색된 각 파일에 대해 명령어를 실행할 수 있습니다.

```bash
find . -type f -name "*.log" -exec rm -f {} \;
```

현재 디렉터리 아래의 모든 `.log` 파일을 삭제합니다.

**대안(더 빠른 방법):**

```bash
find . -type f -name "*.log" | xargs rm -f
```

### 11. 여러 조건 결합 {.unnumbered}

필터를 함께 사용할 수 있습니다.

```bash
find /var/log -name "*.log" -size +50M -mtime -2
```

크기가 50MB보다 크고 최근 2일 이내에 수정된 `.log` 파일을 검색합니다.

### 12. 파일 이름만 출력(간결한 출력) {.unnumbered}

```bash
find /etc -type f -name "*.conf" -print
```

`-print` 플래그는 출력이 표시되도록 합니다. 대부분의 시스템에서는 기본 동작입니다.

## 빠른 참조 표

| **작업** | **명령어** |
|:---|:---|
| 특정 이름을 가진 파일 검색 | `find . -name filename.txt` |
| 대소문자를 구분하지 않고 검색 | `find . -iname filename.txt` |
| 디렉터리만 검색 | `find . -type d` |
| 빈 파일 검색 | `find . -type f -empty` |
| 1GB보다 큰 파일 검색 | `find . -size +1G` |
| 최근 하루 이내에 수정된 파일 검색 | `find . -mtime -1` |
| `.tmp` 파일 삭제 | `find . -name "*.tmp" -delete` |
| 검색 후 명령어 실행 | `find . -name "*.log" -exec gzip {} \;` |

## 도움말 보기

`find` 명령어에 대한 전체 안내를 보려면 다음 명령어를 실행합니다.

```bash
man find
```

# `rmdir` 명령어

**`rmdir`** 명령어는 Linux에서 파일 시스템의 빈 디렉터리를 제거할 때 사용합니다. 명령줄에 지정한 각 디렉터리가 비어 있는 경우에만 해당 디렉터리를 제거합니다.

### 사용법 및 예시

1. 디렉터리와 상위 디렉터리를 함께 제거합니다.

```
rmdir -p a/b/c			// is similar to 'rmdir a/b/c a/b a'
```

2. 여러 디렉터리 일괄 삭제:

```
rmdir a b c				// removes empty directories a,b and c
```

### 구문:

```
rmdir [OPTION]... DIRECTORY...
```
### 추가 플래그와 기능:
| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-` | `--ignore-fail-on-non-empty` | 디렉터리가 비어 있지 않아서 발생한 실패는 무시합니다. |
| `-p` | `--parents` | `DIRECTORY`와 그 상위 디렉터리를 제거합니다. |
| `-d` | `--delimiter=DELIM` | 필드 구분자로 TAB 대신 `DELIM`을 사용합니다. |
| `-v` | `--verbose` | 처리하는 각 디렉터리에 대한 진단 메시지를 출력합니다. |

# `lsblk` 명령어

## 요약

`lsblk` 명령어는 시스템의 블록 장치와 루프 장치를 표시합니다. 디스크를 포맷하거나 파일 시스템을 작성하고, 파일 시스템을 확인하거나 장치의 마운트 지점을 확인할 때 특히 유용합니다. `lsblk`는 사용 가능한 블록 장치를 트리 형태로 보여 주므로 디스크 및 파티션 구조를 확인하는 데도 사용할 수 있습니다. [docs.redhat](https://docs.redhat.com/ko/documentation/red_hat_enterprise_linux/7/html/system_administrators_guide/s1-sysinfo-filesystems)

## 예제

1. 기본 사용법은 매우 간단합니다. 옵션 없이 `lsblk`를 실행합니다.

```bash
lsblk
```

2. 빈 장치도 표시합니다.

```bash
lsblk -a
```

3. 크기 정보를 바이트 단위로 출력합니다.

```bash
lsblk -b
```

4. 각 장치의 영역 모델을 출력합니다.

```bash
lsblk -z
```

5. 슬레이브 항목을 건너뜁니다.

```bash
lsblk -d
```

6. 트리 형식에 ASCII 문자를 사용합니다.

```bash
lsblk -i
```

7. 장치의 소유자, 그룹 및 모드 정보를 표시합니다.

```bash
lsblk -m
```

8. 선택한 열만 출력합니다.

```bash
lsblk -o NAME,SIZE
```

## 구문

```bash
lsblk [options] [<device> ...]
```

## `lsblk`가 표시하는 정보 읽기

플래그나 명령줄 인수 없이 `lsblk`를 실행하면 일반적인 디스크 정보를 표준 출력으로 출력합니다. 다음은 해당 정보에 대한 설명입니다.

| **열 이름** | **의미** | **해석** |
|:---:|:---|:---|
| `NAME` | 장치 이름 | 장치의 이름을 표시합니다. |
| `RM` | 이동식 여부 | 장치가 이동식이면 `1`, 그렇지 않으면 `0`을 표시합니다. |
| `SIZE` | 장치 크기 | 장치의 크기를 표시합니다. |
| `RO` | 읽기 전용 여부 | 읽기 전용이면 `1`, 그렇지 않으면 `0`을 표시합니다. |
| `TYPE` | 블록 장치 또는 루프 장치의 유형 | 전체 디스크는 `disk`, 파티션은 `part`로 표시합니다. |
| `MOUNTPOINTS` | 장치가 마운트된 위치 | 장치가 마운트된 위치를 표시합니다. 마운트되지 않았으면 비어 있습니다. |

## 특정 장치의 정보 확인

장치의 절대 경로를 전달하면 `lsblk`로 특정 장치의 정보를 표시할 수 있습니다.

예를 들어 `sda` 디스크의 정보를 표시하려면 다음과 같이 입력합니다.

```bash
lsblk /dev/sda
```

## `lsblk`의 유용한 플래그

다음은 `lsblk`에서 사용할 수 있는 유용한 플래그입니다.

| **단축 플래그** | **긴 플래그** | **설명** |
|:---:|:---|:---|
| `-a` | `--all` | 기본적으로 표시하지 않는 빈 장치도 표시합니다. |
| `-b` | `--bytes` | `SIZE` 열을 사람이 읽기 쉬운 형식이 아닌 바이트 단위로 출력합니다. |
| `-d` | `--nodeps` | 장치 홀더나 슬레이브를 출력하지 않습니다. |
| `-D` | `--discard` | 각 장치의 discard(TRIM, UNMAP) 기능에 대한 정보를 출력합니다. |
| `-E` | `--dedup column` | 지정한 열을 중복 제거 키로 사용하여 출력 트리의 중복을 제거합니다. 해당 장치에 키가 없거나, 장치가 파티션이고 상위 전체 디스크 장치가 동일한 키를 제공하는 경우에는 장치를 항상 출력합니다. |
| `-e` | `--exclude list` | 쉼표로 구분한 주 장치 번호 목록에 지정된 장치를 제외합니다. RAM 디스크(`major=1`)는 기본적으로 제외됩니다. 필터는 최상위 장치에만 적용됩니다. |
| `-f` | `--fs` | 파일 시스템 정보를 표시합니다. |
| `-h` | `--help` | 도움말 텍스트를 표시한 후 종료합니다. |
| `-l` | `--include list` | 목록 형식으로 모든 정보를 표시합니다. |
| `-J` | `--json` | 모든 정보를 JSON 형식으로 표시합니다. |
| `-l` | `--list` | 모든 정보를 목록 형식으로 표시합니다. |
| `-m` | `--perms` | 장치의 소유자, 그룹 및 모드 정보를 표시합니다. |
| `-M` | `--merge` | 하위 트리의 부모를 그룹화하여 RAID 및 다중 경로 장치의 출력을 더 읽기 쉽게 표시합니다. 트리 형식 출력이 필요합니다. |
| `-n` | `--noheadings` | 헤더 행을 출력하지 않습니다. |
| `-o` | `--output list` | 출력할 열을 지정합니다. 지원되는 모든 열 목록은 `--help`로 확인할 수 있습니다. |
| `-O` | `--output-all` | 사용 가능한 모든 열을 표시합니다. |
| `-p` | `--paths` | 장치의 절대 경로를 표시합니다. |
| `-P` | `--pairs` | `key="value"` 출력 형식을 사용합니다. 안전하지 않을 가능성이 있는 모든 문자는 16진수 형식(`\x<code>`)으로 이스케이프됩니다. |
| `-r` | `--raw` | 원시 출력 형식을 사용합니다. `NAME`, `KNAME`, `LABEL`, `PARTLABEL`, `MOUNTPOINT` 열의 안전하지 않을 가능성이 있는 모든 문자는 16진수 형식(`\x<code>`)으로 이스케이프됩니다. |
| `-S` | `--scsi` | SCSI 장치에 대한 정보만 출력합니다. 모든 파티션, 슬레이브 및 홀더 장치는 무시됩니다. |
| `-s` | `--inverse` | 의존성을 역순으로 출력합니다. |
| `-t` | `--topology` | 블록 장치 토폴로지 정보를 출력합니다. `-o NAME,ALIGNMENT,MIN-IO,OPT-IO,PHY-SEC,LOG-SEC,ROTA,SCHED,RQ-SIZE`와 동일합니다. |
| `-T` | `--tree[=column]` | 모든 정보를 트리 형식으로 표시합니다. |
| `-V` | `--version` | 버전 정보를 출력한 후 종료합니다. |
| `-w` | `--width` | 출력 너비를 문자 수로 지정합니다. 기본값은 터미널 열의 수이며 터미널에서 실행하지 않으면 기본적으로 출력 너비에 제한이 없습니다. |
| `-x` | `--sort [column]` | 열을 기준으로 출력 행을 정렬합니다. 이 옵션은 기본적으로 `--list` 출력 형식을 활성화합니다. `--tree` 옵션을 사용하면 트리 형식 출력을 강제할 수 있으며 이 경우 트리 분기가 해당 열을 기준으로 정렬됩니다. |
| `-z` | `--zoned` | 각 장치의 영역 모델을 출력합니다. |
| `-` | `--sysroot directory` | `lsblk` 명령어를 실행한 시스템이 아닌 다른 Linux 인스턴스의 데이터를 수집합니다. 지정한 디렉터리는 검사할 Linux 인스턴스의 시스템 루트입니다. |

## 종료 코드

다른 모든 Unix/Linux 프로그램과 마찬가지로 `lsblk`도 환경에 종료 코드를 반환합니다. 다음은 모든 종료 코드에 대한 표입니다.
| **종료 코드** | **의미** |
|:---:|:---|
| `0` | 성공적으로 종료되었습니다. |
| `1` | 실패와 함께 종료되었습니다. |
| `32` | 지정한 장치를 찾을 수 없습니다. |
| `64` | 지정한 장치 중 일부는 찾았지만 일부는 찾지 못했습니다. |

# `cmatrix` 명령어

이 명령어는 Linux에 기본으로 설치되어 있지 않습니다. 설치해야 하며 052장의 `apt` 명령어에서 설명한 것처럼 다음 명령어를 실행합니다.

```bash
sudo apt-get install cmatrix
```

모든 설치가 완료되면 이제 ‘진짜 해커’가 된 것처럼 느낄 수 있습니다. 이 명령어를 사용하려면 `cmatrix`를 입력하고 Enter 키를 누릅니다.

```bash
cmatrix
```

그러면 다음과 같은 화면이 표시됩니다.



이제 매트릭스에 접근할 수 있는 것처럼 보입니다. 물론 실제로 그런 것은 아닙니다.

실제로는 재미로 가지고 놀 수 있는 간단한 명령어입니다. 사용할 수 있는 옵션도 몇 가지 있습니다. 예를 들어 텍스트 색상을 변경할 수 있습니다. **초록색, 빨간색, 파란색, 흰색, 노란색, 청록색, 자홍색 및 검은색** 중에서 선택할 수 있습니다.

```bash
cmatrix -C red
```



그러면 화면에서 떨어지는 문자가 빨간색으로 표시됩니다. 이 명령어가 업무에 도움을 주는 것은 아니지만 Linux에서 이런 재미있는 작업도 할 수 있다는 점을 알아 두면 좋습니다. `cmatrix`는 Debian/Ubuntu 계열에서 패키지로 설치하여 실행할 수 있습니다. [summerorange.tistory](https://summerorange.tistory.com/entry/Ubuntu-%EC%9E%88%EC%96%B4%EB%B9%8C%EB%A6%AC%ED%8B%B0%ED%95%9C-cmatrix)

# `chmod` 명령어

`chmod` 명령어를 사용하면 기호 모드, 숫자 모드 또는 참조 파일을 이용하여 파일의 권한을 변경할 수 있습니다.

### 예시

1. 기호 모드를 사용하여 파일 권한을 변경합니다.

```bash
chmod u=rwx,g=rx,o=r myfile
```

위 명령어의 의미는 다음과 같습니다.

- 사용자는 `myfile`을 읽고, 쓰고, 실행할 수 있습니다.
- 그룹은 `myfile`을 읽고 실행할 수 있습니다.
- 기타 사용자는 `myfile`을 읽을 수 있습니다.

2. 숫자 모드를 사용하여 파일 권한을 변경합니다.

```bash
chmod 754 myfile user:group file.txt
```

위 명령어의 의미는 다음과 같습니다.

- 사용자는 `myfile`을 읽고, 쓰고, 실행할 수 있습니다.
- 그룹은 `myfile`을 읽고 실행할 수 있습니다.
- 기타 사용자는 `myfile`을 읽을 수 있습니다.

3. 폴더의 권한을 재귀적으로 변경합니다.

```bash
chmod -R 754 folder
```

### 구문(Syntax)

```bash
chmod [OPTIONS] MODE FILE(s)
```

- `[OPTIONS]`:
  - `-R`: 재귀적 옵션입니다. 디렉터리 안의 모든 파일에 적용한다는 의미입니다.

- `MODE`: 권한을 설정하는 다양한 방식입니다.

- **기호 모드 설명**
  - `u`: 사용자
  - `g`: 그룹
  - `o`: 기타 사용자
  - `=`: 권한 설정
  - `r`: 읽기
  - `w`: 쓰기
  - `x`: 실행
  - 예를 들어 `u=rwx`는 사용자가 읽고, 쓰고, 실행할 수 있다는 의미입니다.

- **숫자 모드 설명**

**숫자 모드**는 사용자, 그룹 및 기타 사용자의 권한을 이진 표현으로 나타낸 것입니다. 자세한 내용은 DigitalOcean 커뮤니티의 [권한 설명](https://www.digitalocean.com/community/tutorials/linux-permissions-basics-and-how-to-use-umask-on-a-vps#types-of-permissions)을 참고하십시오.

- `4`는 “읽기”를 의미합니다.
- `2`는 “쓰기”를 의미합니다.
- `1`은 “실행”을 의미합니다.
- `0`은 “권한 없음”을 의미합니다.
- 예를 들어 `7`은 읽기 + 쓰기 + 실행을 의미합니다.

# `grep` 명령어

`grep` 필터는 파일에서 특정 문자 패턴을 검색하고, 해당 패턴이 포함된 모든 행을 표시합니다. `grep`은 “globally search for regular expression and print out”의 약자입니다. 파일에서 검색하는 패턴을 정규 표현식이라고 합니다.

### 예시

1. `destination.txt` 파일에서 문자열 `"KeY"`를 대소문자 구분 없이 검색합니다.

```bash
grep -i "KeY" destination.txt
```

2. 일치하는 항목의 개수를 표시합니다.

```bash
grep -c "key" destination.txt
```

3. 여러 파일을 검색하고 지정한 문자열 또는 패턴이 포함된 파일만 표시합니다.

```bash
grep -l "key" destination1.txt destination2.txt destination3.xt destination4.txt
```

4. 일치하는 행과 행 번호를 표시합니다.

```bash
grep -n "key" destination.txt
```

5. 모니터링 중인 로그 파일을 `grep`으로 검색하려면 `--line-buffered` 옵션을 추가하여 실시간으로 검색할 수 있습니다.

```bash
tail -f destination.txt | grep --line-buffered "key"
```

### 구문(Syntax)

`grep` 명령어의 일반적인 구문은 다음과 같습니다.

```bash
grep [options] pattern [files]
```

### 추가 플래그 및 기능

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-c` | `--count` | 각 입력 파일에서 일치하는 행의 개수를 출력합니다. |
| `-h` | `--no-filename` | 일치하는 행을 표시하지만 파일 이름은 표시하지 않습니다. |
| `-i` | `--ignore-case` | 일치 여부를 판단할 때 대소문자를 무시합니다. |
| `-l` | `--files-with-matches` | 파일 이름 목록만 표시합니다. |
| `-n` | `--line-number` | 일치하는 행과 행 번호를 표시합니다. |
| `-v` | `--invert-match` | 패턴과 일치하지 않는 모든 행을 출력합니다. |
| `-e` | `--regexp=` | 이 옵션으로 표현식을 지정합니다. 여러 번 사용할 수 있습니다. |
| `-f` | `--file=` | 파일에서 한 행에 하나씩 패턴을 가져옵니다. |
| `-F` | `--fixed-strings=` | 패턴을 정규 표현식이 아닌 고정 문자열로 해석합니다. |
| `-E` | `--extended-regexp` | 패턴을 확장 정규 표현식(ERE)으로 처리합니다. |
| `-w` | `--word-regexp` | 전체 단어와 일치시킵니다. |
| `-o` | `--only-matching` | 일치하는 행에서 일치한 부분만 출력하며 각 부분을 별도의 출력 행에 표시합니다. |
| - | `--line-buffered` | 출력을 행 버퍼 방식으로 강제합니다. |

### 추가 실전 예시

6. 디렉터리 안의 모든 파일을 재귀적으로 검색합니다.

```bash
grep -r "pattern" /path/to/directory
```

7. 부분적으로 일치하는 항목이 아닌 전체 단어만 검색합니다.

```bash
grep -w "key" destination.txt
```

8. 패턴이 포함되지 않은 행을 표시합니다. (반전 검색)

```bash
grep -v "unwanted" destination.txt
```

9. 확장 정규 표현식을 사용하여 여러 패턴을 검색합니다.

```bash
grep -E "pattern1|pattern2" destination.txt
```

10. 일치하는 행의 앞뒤 문맥을 함께 검색하고 표시합니다.

```bash
# 각 일치 항목 앞 2개 행과 뒤 2개 행 표시
grep -C 2 "error" logfile.txt


# 각 일치 항목 앞 3개 행만 표시
grep -B 3 "error" logfile.txt


# 각 일치 항목 뒤 3개 행만 표시
grep -A 3 "error" logfile.txt
```

11. 행의 시작이나 끝에 있는 패턴을 검색합니다.

```bash
# "Error"로 시작하는 행
grep "^Error" logfile.txt


# ".txt"로 끝나는 행
grep "\.txt$" filelist.txt
```

12. 여러 파일에서 일치하는 항목의 전체 개수를 계산합니다.

```bash
grep -c "pattern" file1.txt file2.txt file3.txt
```

13. 패턴이 포함된 파일을 찾고 다른 명령어로 파이프합니다.

```bash
# "import numpy"가 포함된 모든 Python 파일 검색
grep -r "import numpy" --include="*.py" .


# 압축된 로그 파일 검색
zgrep "error" /var/log/syslog.*.gz
```

# `screen` 명령어

`screen`을 사용하면 screen 세션을 시작한 후 해당 세션 안에서 여러 개의 창(가상 터미널)을 열 수 있습니다.

Screen에서 실행 중인 프로세스는 창이 보이지 않거나 연결이 끊긴 경우에도 계속 실행됩니다. 따라서 매우 오래 실행되는 Bash 스크립트와 같은 장시간 작업을 실행할 때 매우 유용합니다.

Screen 세션을 시작하려면 `screen`을 입력합니다. 그러면 가상 터미널이 열린 새 screen 세션이 시작됩니다.

다음은 Linux Screen 창을 관리할 때 사용하는 주요 명령어입니다. `screen -r`로 분리된 세션을 다시 연결할 수 있습니다. [jjeongil.tistory](https://jjeongil.tistory.com/1286)

| **명령어** | **설명** |
|:---|:---|
| `Ctrl+a` + `c` | 셸이 포함된 새 창을 생성합니다. |
| `Ctrl+a` + `"` | 모든 창을 나열합니다. |
| `Ctrl+a` + `0` | 번호가 0인 창으로 전환합니다. |
| `Ctrl+a` + `A` | 현재 창의 이름을 변경합니다. |
| `Ctrl+a` + `S` | 현재 영역을 가로 방향으로 두 영역으로 나눕니다. |
| `Ctrl+a` + `'` | 현재 영역을 세로 방향으로 두 영역으로 나눕니다. |
| `Ctrl+a` + `Tab` | 입력 포커스를 다음 영역으로 전환합니다. |
| `Ctrl+a` + `Ctrl+a` | 현재 창과 이전 창 사이를 전환합니다. |
| `Ctrl+a` + `Q` | 현재 영역을 제외한 모든 영역을 닫습니다. |
| `Ctrl+a` + `X` | 현재 영역을 닫습니다. |

## Linux Screen 복원

Screen 세션으로 돌아가려면 `screen -r`을 입력합니다. 열려 있는 Screen 세션이 두 개 이상인 경우 올바른 세션에 연결하려면 세션 ID를 명령어에 추가해야 합니다.

```bash
screen -r
```

## 열려 있는 Screen 세션 모두 나열

세션 ID를 확인하려면 현재 실행 중인 Screen 세션을 다음과 같이 나열합니다.

```bash
screen -ls
```

```text
There are screens on:
18787.pts-0.your-server   (Detached)
15454.pts-0.your-server   (Detached)
2 Sockets in /run/screens/S-yourserver.
```

`18787.pts-0` Screen 세션을 복원하려면 다음 명령어를 입력합니다.

```bash
screen -r 18787
```

# `nc` 명령어

`nc` 또는 `netcat` 명령어는 TCP(Transmission Control Protocol, 연결 지향형), UDP(User Datagram Protocol, 비연결형이며 데이터 전달을 보장하지 않음) 또는 UNIX 도메인 소켓과 관련된 작업을 수행할 때 사용합니다.

통신 프로토콜 유틸리티를 위한 만능 도구라고 생각할 수 있습니다. `nc`는 TCP/IP 네트워크 작업, 포트 확인, 파일 전송 및 소켓 연결 테스트 등에 사용할 수 있습니다.

### 구문(Syntax):

```

nc [options] [ip] [port]

```

### 예시:
#### 1. 포트 80에 TCP 연결을 열고, 소스 포트로 1337을 사용하며 제한 시간을 5초로 설정합니다. {.unnumbered}

```bash
$ nc -p 1337 -w 5 host.ip 80
```

#### 2. 호스트의 포트 80에 UDP 연결을 엽니다. {.unnumbered}

```bash
$ nc -u host.ip 80
```

#### 3. UNIX 도메인 스트림 소켓을 생성하고 연결을 기다립니다. {.unnumbered}

```bash
$ nc -lU /var/tmp/dsocket
```

#### 4. 기본 서버/클라이언트 모델 생성 {.unnumbered}

연결이 설정되면 `nc`의 관점에서는 특정한 서버 측이나 클라이언트 측이 정해지지 않은 상태로 연결이 생성됩니다.

```bash
$ nc -l 1234 # 한 터미널에서 실행


$ nc 127.0.0.1 1234 # 다른 터미널에서 실행
```

#### 5. 기본 데이터 전송 모델 구성 {.unnumbered}

파일 전송이 순차적으로 완료되면 연결이 자동으로 종료됩니다.

```bash
$ nc -l 1234 > filename.out # 한 터미널에서 수신 대기하고 데이터를 저장


$ nc host.ip 1234 < filename.in
```

#### 6. 서버와 통신 {.unnumbered}

헤더와 함께 호스트의 홈페이지를 가져오는 기본 예시입니다.

```bash
$ printf "GET / HTTP/1.0\r\n\r\n" | nc host.ip 80
```

#### 7. 포트 스캔 {.unnumbered}

대상 장치에서 어떤 포트가 열려 있고 어떤 서비스가 실행 중인지 확인합니다. `-z` 플래그를 사용하면 연결을 실제로 시작하지 않고 해당 정보를 확인합니다.

```bash
$ nc -zv host.ip 20-2000 # 확인할 포트 범위
```

### 플래그 및 기능

| **단축 플래그** | **설명** |
|:---|:---|
| `-4` | `nc`가 IPv4 주소를 사용하도록 강제합니다. |
| `-6` | `nc`가 IPv6 주소를 사용하도록 강제합니다. |
| `-b` | 브로드캐스트를 허용합니다. |
| `-D` | 소켓 디버깅을 활성화합니다. |
| `-i` | 송수신하는 행 사이의 시간 간격을 지정합니다. |
| `-k` | 현재 연결이 종료된 후에도 다른 연결을 기다립니다. |
| `-l` | 원격 연결을 시작하는 대신 수신 연결을 기다립니다. |
| `-T` | TCP 길이를 지정합니다. |
| `-p` | 사용할 소스 포트를 지정합니다. |
| `-r` | 소스 포트 및 대상 포트를 무작위로 지정합니다. |
| `-s` | 패킷을 전송하는 데 사용할 인터페이스의 IP를 지정합니다. |
| `-U` | UNIX 도메인 소켓을 사용합니다. |
| `-u` | TCP 대신 UDP를 프로토콜로 사용합니다. |
| `-w` | 유휴 연결 또는 아직 설정되지 않은 연결에 대한 제한 시간을 지정합니다. |
| `-x` | 프록시 서버와 통신할 때 지정한 프로토콜을 사용합니다. |
| `-z` | 데이터를 전송하지 않고 수신 대기 중인 데몬을 검색합니다. |

`nc`는 TCP/UDP 연결, 파일 전송, 포트 확인 등에 사용할 수 있습니다. [betwe.tistory](https://betwe.tistory.com/entry/Linux-%EB%A6%AC%EB%88%85%EC%8A%A4-nc-%EB%AA%85%EB%A0%B9%EC%96%B4%EC%97%90-%EB%8C%80%ED%95%9C-%EC%9E%90%EC%84%B8%ED%95%9C-%EC%84%A4%EB%AA%85-%EB%B0%8F-%EC%98%88%EC%8B%9C)

# `make` 명령어

`make` 명령어는 특정 디렉터리 구조에서 여러 명령어를 재사용하는 작업을 자동화할 때 사용합니다.

예를 들어 Azure에서 서로 다른 구독을 변경하면서 `terraform init`, `terraform plan`, `terraform validate`를 실행하는 작업을 생각해 볼 수 있습니다. 일반적으로 다음과 같은 단계로 수행합니다.

```bash
az account set --subscription "Subscription - Name"
terraform init
```

`make` 명령어를 사용하면 이 모든 작업을 한 번에 자동화할 수 있습니다.

```make
tf-init
```

### 구문

```bash
make [ -f makefile ] [ options ] ... [ targets ] ...
```

### 사용 예(가이드)

#### 1. 가이드 디렉터리에 `Makefile` 생성 {.unnumbered}

#### 2. `Makefile`에 다음 내용 추가 {.unnumbered}

```make
hello-world:
        echo "Hello, World!"


hello-bobby:
        echo "Hello, Bobby!"


touch-letter:
        echo "This is a text that is being inputted into our letter!" > letter.txt


clean-letter:
        rm letter.txt
```

#### 3. `make hello-world` 실행 {.unnumbered}

터미널에 `Hello, World!`를 출력합니다.

#### 4. `make hello-bobby` 실행 {.unnumbered}

터미널에 `Hello, Bobby!`를 출력합니다.

#### 5. `make touch-letter` 실행 {.unnumbered}

`letter.txt`라는 텍스트 파일을 생성하고 한 줄을 기록합니다.

#### 6. `make clean-letter` 실행 {.unnumbered}

`letter.txt` 파일을 삭제합니다.

> 참고: `Makefile`의 명령어(recipe) 행은 앞에 반드시 TAB 문자가 있어야 합니다.

### 더 길고 자세한 튜토리얼 참고 자료

- [linoxide Linux make 명령어 예제](https://linoxide.com/linux-make-command-examples/)
- [makefiletutorial.com 이름만 보아도 내용을 알 수 있는 Makefile 튜토리얼](https://makefiletutorial.com/)

# `basename` 명령

`basename`은 지정한 파일 이름에서 디렉터리 부분을 제거하는 명령줄 유틸리티입니다. 선택적으로 뒤에 붙은 접미사도 제거할 수 있습니다. 몇 가지 옵션만 지원하는 간단한 명령입니다.

### 예제

가장 기본적인 예제는 앞쪽 디렉터리를 제거한 파일 이름을 출력하는 것입니다.

```bash
basename /etc/bar/foo.txt
```

출력에는 파일 이름이 표시됩니다.

```bash
foo.txt
```

디렉터리를 가리키는 경로 문자열에 basename을 실행하면 경로의 마지막 구성 요소를 얻습니다. 이 예제에서 /etc/bar는 디렉터리입니다.

```bash
basename /etc/bar
```

출력

```bash
bar
```

basename 명령은 뒤에 붙은 `/` 문자도 제거합니다.

```bash
basename /etc/bar/foo.txt/
```

출력

```bash
foo.txt
```

### 옵션

1. 기본적으로 각 출력 행은 줄 바꿈 문자로 끝납니다. 행을 NUL로 끝내려면 `-z` (`--zero`) 옵션을 사용합니다.

```bash
$ basename -z /etc/bar/foo.txt
foo.txt$
```

2. `basename` 명령은 여러 이름을 인수로 받을 수 있습니다. 파일 목록을 공백으로 구분하고 그 앞에 `-a` (`--multiple`) 옵션을 지정해 명령을 실행합니다. `/etc/bar/foo.txt`와 `/etc/spam/eggs.docx`의 파일 이름을 얻으려면 다음을 실행합니다.

```bash
basename -a /etc/bar/foo.txt /etc/spam/eggs.docx
```

```bash
foo.txt
eggs.docx
```

### 구문

basename 명령은 다음 두 가지 구문 형식을 지원합니다.

```bash
basename NAME [SUFFIX]
basename OPTION... NAME...
```

### 추가 기능

**뒤에 붙은 접미사 제거**: 파일 이름에서 뒤에 붙은 접미사를 제거하려면 두 번째 인수로 접미사를 전달합니다.

```bash
basename /etc/hostname name
host
```

일반적으로 이 기능은 파일 확장자를 제거하는 데 사용됩니다.

### 도움말 명령

`basename` 명령에 대한 전체 안내를 보려면 다음 명령을 실행합니다.

```bash
man basename
```

# `banner` 명령

`banner` 명령은 ASCII 문자 문자열을 큰 글자로 표준 출력에 기록합니다. 출력의 각 행에는 최대 10개의 대문자 또는 소문자가 들어갈 수 있습니다. 출력 시 모든 문자는 대문자로 표시되며 입력이 소문자인 문자는 대문자 입력보다 작게 표시됩니다.

참고: sleep 명령에 NUMBER를 둘 이상 지정하면 이 명령은 각 값의 합만큼 지연됩니다.

### 예제:

1. 워크스테이션에 배너 표시:

```
banner LINUX!
```

2. 한 행에 둘 이상의 단어 표시: 다음과 같이 텍스트를 따옴표로 묶습니다.

```
banner "Intro to" Linux
```

> Intro to는 한 행에, Linux는 다음 행에 표시됩니다.

3. “101LinuxCommands”를 큰 글자로 출력합니다.

```
banner 101LinuxCommands
```

> banner의 기본 용량이 10이므로 101LinuxCo만 배너로 출력됩니다.

***

# `alias` 명령

`alias` 명령을 사용하면 명령의 단축키를 만들거나 사용자 지정 명령을 정의할 수 있습니다.  
주로 긴 명령을 입력하지 않기 위해 사용합니다.

### 예제:

1. 정의된 모든 alias를 재사용 가능한 `alias NAME=VALUE` 형식으로 표시:

```
alias -p
```

2. `ls -A` 단축키 만들기:

```
alias la='ls -A'
```

### 영구 옵션 설정:

대부분의 Linux 터미널 사용자 지정 설정과 마찬가지로 정의한 alias는 현재 열려 있는 터미널 세션에만 적용됩니다.

모든 새 세션에서 alias를 활성화하려면 새 터미널이 시작될 때마다 실행되도록 해당 명령을 rc 파일에 추가해야 합니다.
이 파일은 다음과 같을 수 있습니다.

- **Bash**: ~/.bashrc
- **ZSH**: ~/.zshrc
- **Fish**  ~/.config/fish/config.fish

다음과 같이 선호하는 편집기로 파일을 열 수 있습니다.

```
vim ~/.bashrc
```

명령을 한 줄에 하나씩 입력한 다음 파일을 저장하고 종료합니다.
명령은 다음 세션부터 자동으로 적용됩니다.

현재 세션에 적용하려면 다음 명령을 실행합니다.

```
source ~/.bashrc
```

### 반대 명령:

미리 정의된 alias를 제거하려면 다음과 같이 `unalias` 명령을 사용할 수 있습니다.

```
unalias alias_name
```

모든 alias를 제거하려면:

```
unalias -a
```

# `which` 명령

`which` 명령은 셸에서 명령을 입력했을 때 실행되는 실행 파일을 식별합니다. 컴퓨터에 같은 프로그램의 다른 버전이 여러 개 있다면 which를 사용해 셸이 사용할 버전을 확인할 수 있습니다.

반환 상태는 다음 세 가지입니다.

    0 : 지정한 모든 명령을 찾았고 실행할 수 있음.
    1 : 지정한 명령 중 하나 이상이 존재하지 않거나 실행할 수 없음.
    2 : 잘못된 옵션이 지정됨.

### 예제

1. ls 명령의 전체 경로를 확인하려면 다음을 입력합니다.

```
which ls
```

2. which 명령에 둘 이상의 인수를 제공할 수 있습니다.

```
which netcat uptime ping
```

which 명령은 왼쪽에서 오른쪽으로 검색합니다. PATH 경로 변수에 나열된 디렉터리에서 일치 항목을 둘 이상 찾으면 첫 번째 항목만 출력합니다.

3. 지정한 명령의 모든 경로 표시:

```
which [filename] -a
```

4. node 실행 파일의 경로 표시:

```
which node
```

5. Java 실행 파일의 경로 표시:

```
which java  
```

### 구문

```
which [filename1] [filename2] ...
```

which에 여러 프로그램과 명령을 전달하면 순서대로 확인합니다.

예:

```bash
which ping cat uptime date head
```

### 옵션

-a: 찾은 실행 파일의 모든 인스턴스를 나열합니다(각 실행 파일의 첫 번째 항목만 나열하는 대신).

-s: 출력을 표시하지 않습니다. 모든 실행 파일을 찾으면 0을, 일부를 찾지 못하면 1을 반환합니다.

# `date` 명령

`date` 명령은 시스템의 현재 날짜와 시간을 출력하는 데 사용됩니다.

`date` 명령은 시스템의 날짜와 시간을 설정하는 데도 사용되지만 이를 수행하려면 슈퍼유저 *(root)* 권한이 필요합니다.

### 예제:

1. 현재 날짜와 시간 표시:

```
date
```

2. -u 옵션을 사용해 UTC *(협정 세계시)* 시간대의 날짜와 시간 표시

```
date -u
```

1. 지정한 날짜 문자열을 형식화된 날짜로 표시:

```
date --date="2/02/2010"
date --date="2 years ago"
```

### 구문:

```
date [OPTION]... [+FORMAT]
date [-u|--utc|--universal] [MMDDhhmm[[CC]YY][.ss]]
```

### 추가 플래그와 기능:

|**짧은 플래그**   |**긴 플래그**   |**설명**   |
|:---|:---|:---|
|`-d`|`--date=STRING`|지정한 문자열을 형식화된 날짜로 변환|
|`-f`|`--file=DATEFILE`|`--date`와 같지만 파일에 사용|
|`-I[FMT]`|`--iso-8601[=FMT]`|날짜와 시간을 ISO 8601 형식으로 표시|
|`-r`|`--reference=FILE`|FILE의 마지막 수정 시간 표시|
|`-s`|`--set=STRING`|STRING으로 설명한 시간으로 설정|
|`-u`|`--universal`|UTC *(협정 세계시)* 시간대의 날짜와 시간 표시|
|`-R`|`--rfc-email`|날짜와 시간을 ISO 8601 형식으로 표시. 예: (Fri, 22 Oct 2021 05:18:42 +0200)|
|-|`rfc-3339=FMT`|날짜와 시간을 RFC 3339 형식으로 표시|
|-|`--debug`|일반적으로 `--date`와 함께 사용하여 파싱한 날짜에 주석을 달고 의심스러운 사용법을 stderr에 경고|

### 출력 제어:

형식 지정자를 사용해 출력 날짜와 시간을 제어할 수 있습니다.

### 예제:

|**명령**   |**출력**   |
|:---|:---|
|`$ date "+%D"`|`10/22/21`|
|`$ date "+%D %T"`|`10/22/21 05:33:51`|
|`$ date "+%A %B %d %T %y"`|`Friday October 22 05:34:47 21`|

### 구문:

```
date "+%[format-options ...]"
```

### 출력 날짜와 시간을 제어하는 형식 지정자 목록:

|**지정자**   |**설명**   |
|:---|:---|
|`%a`|축약된 요일 이름 *(예: Sun)*|
|`%A`|전체 요일 이름 *(예: Sunday)*|
|`%b`|축약된 월 이름 *(예: Jan)*|
|`%B`|전체 월 이름 *(예: January)*|
|`%c`|날짜와 시간 *(예: Thu Mar  3 23:05:25 2005)*|
|`%C`|세기. %Y와 같지만 마지막 두 자리를 생략 *(예: 20)*|
|`%d`|월의 일 *(예: 01)*|
|`%D`|날짜. %m/%d/%y와 같음|
|`%e`|공백으로 채운 월의 일. %_d와 같음|
|`%F`|전체 날짜. %Y-%m-%d와 같음|
|`%g`|ISO 주 번호의 연도 마지막 두 자리(%G 참조)|
|`%G`|ISO 주 번호의 연도(%V 참조). 일반적으로 %V와 함께 사용할 때만 유용|
|`%h`|%b와 같음|
|`%H`|시(00..23)|
|`%I`|시(01..12)|
|`%j`|연중 일수(001..366)|
|`%k`|공백으로 채운 시( 0..23). %_H와 같음|
|`%l`|공백으로 채운 시( 1..12). %_I와 같음|
|`%m`|월(01..12)|
|`%M`|분(00..59)|
|`%n`|줄 바꿈|
|`%N`|나노초(000000000..999999999)|
|`%p`|로캘에서 AM 또는 PM에 해당하는 표현. 알 수 없으면 공백|
|`%P`|%p와 같지만 소문자|
|`%q`|분기(1..4)|
|`%r`|로캘의 12시간제 시간 *(예: 11:11:04 PM)*|
|`%R`|24시간제 시와 분. %H:%M과 같음|
|`%s`|UTC 기준 1970-01-01 00:00:00 이후의 초|
|`%S`|초(00..60)|
|`%t`|탭|
|`%T`|시간. %H:%M:%S와 같음|
|`%u`|요일(1..7). 1은 월요일|
|`%U`|일요일을 주의 첫날로 하는 연중 주 번호(00..53)|
|`%V`|월요일을 주의 첫날로 하는 ISO 주 번호(01..53)|
|`%w`|요일(0..6). 0은 일요일|
|`%W`|월요일을 주의 첫날로 하는 연중 주 번호(00..53)|
|`%x`|로캘의 날짜 표현 *(예: 12/31/99)*|
|`%X`|로캘의 시간 표현 *(예: 23:13:48)*|
|`%y`|연도의 마지막 두 자리(00..99)|
|`%Y`|연도|
|`%z`|+hhmm 숫자 시간대 *(예: -0400)*|
|`%:z`|+hh:mm 숫자 시간대 *(예: -04:00)*|
|`%::z`|+hh:mm:ss 숫자 시간대 *(예: -04:00:00)*|
|`%:::z`|필요한 정밀도에 맞춰 `:`를 사용한 숫자 시간대 *(예: -04, +05:30)*|
|`%Z`|알파벳 시간대 약어 *(예: EDT)*|

# `mount` 명령

`mount` 명령은 파일시스템을 마운트(‘연결’)하여 기존 디렉터리 구조 트리에서 접근할 수 있게 하는 데 사용됩니다.

### 예제:

1. 버전 정보 표시:

```
mount -V
```

2. 장치에서 찾은 지정 유형의 파일시스템을 디렉터리 dir에 연결:

```
mount -t type device dir
```

### 구문 형식:

```
mount [-lhV]
```

```
mount -a [-fFnrsvw] [-t vfstype] [-O optlist]
```

```
mount [-fnrsvw] [-t fstype] [-o options] device dir
```

### 추가 플래그와 기능:

|**짧은 플래그**   |**긴 플래그**   |**설명**   |
|:---|:---|:---|
|`-h`|`--help`|도움말을 표시하고 종료|
|`-n`|`--no-mtab`|/etc/mtab에 기록하지 않고 마운트|
|`-a`|`--all`|fstab에 지정된 모든 유형의 파일시스템을 마운트|
|`-r`|`--read-only`|파일시스템을 읽기 전용으로 마운트|
|`-w`|`--rw`|파일시스템을 읽기/쓰기로 마운트.|
|`-M`|`--move`|하위 트리를 다른 위치로 이동.|
|`-B`|`--bind`|하위 트리를 다른 위치에 다시 마운트 *(두 위치에서 해당 내용에 접근할 수 있도록 함)*.|

# `nice/renice` 명령

`nice/renice` 명령은 실행할 프로그램의 우선순위를 변경하는 데 사용됩니다. 우선순위 범위는 -20에서 19까지이며 19가 가장 낮은 우선순위입니다.

### 예제:

1. 기본값보다 낮은 우선순위(느리게)로 cc 명령을 백그라운드에서 실행:

```
nice -n 15 cc -c *.c &
```

2. "test" 그룹에 속한 모든 프로세스의 우선순위 높이기:

```
renice --20 -g test
```

### 구문:

```
nice [  -Increment|  -n Increment ] Command [ Argument ... ]
```

### 플래그:

|**짧은 플래그**   |**긴 플래그**   |**설명**   |
|:---|:---|:---|
|`-Increment`|-|할당할 우선순위 값인 Increment입니다.|
|`-n Increment`|-|`-Increment`와 같습니다.|

# `wc` 명령

`wc` 명령은 word count의 약자입니다. 파일 또는 표준 입력에서 행, 단어 및 바이트 *(문자)* 수를 세고 그 결과를 표준 출력에 표시합니다.

### 예제:

1. 파일의 행, 단어 및 문자 수를 순서대로 계산:

```
wc file.txt
```

2. 디렉터리 안의 디렉터리 수 계산:

```
ls -F | grep / | wc -l
```

### 구문:

```bash
wc [OPTION]... [FILE]...
```

### 추가 플래그와 기능:

|**짧은 플래그**   |**긴 플래그**   |**설명**   |
|:---|:---|:---|
|`-c` | `--bytes` | 바이트 수 출력|
|`-m` | `--chars` | 문자 수 출력|
|`-l` | `--lines` | 줄 바꿈 수 출력|
|- | `--files0-from=F` | 파일 F에서 NUL로 끝나는 이름으로 지정한 파일의 입력을 읽음. F가 `-`이면 표준 입력에서 이름을 읽음|
|`-L` | `--max-line-length` | 최대 표시 너비 출력|
|`-w` | `--words` | 단어 수 출력|

### 추가 참고:

* `wc` 명령에 둘 이상의 파일을 전달하면 각 파일의 개수와 전체 개수를 출력합니다.
* 원하는 결과를 출력하기 위해 둘 이상의 플래그를 조합할 수 있습니다.

# `tr` 명령

UNIX에서 tr 명령은 문자를 변환하거나 삭제하는 명령줄 유틸리티입니다.
대문자를 소문자로 변환하거나, 반복 문자를 하나로 줄이거나, 특정 문자를 삭제하고, 기본적인 찾기 및 바꾸기를 수행하는 등 다양한 변환을 지원합니다.
UNIX 파이프와 함께 사용하면 더 복잡한 변환을 수행할 수 있습니다. tr은 translate의 약자입니다.

### 예제:

1. file1의 모든 소문자를 대문자로 변환.

```
$ cat file1
foo
bar
baz
tr a-z A-Z < file1
FOO
BAR
BAZ
```

2. 연속된 줄 바꿈을 하나로 만들기.

```
$ cat file1
foo


bar


baz
$ tr -s "\n" < file1
foo
bar
baz
```

3. 줄 바꿈 코드 제거.

```
$ cat file1
foo
bar
baz
$ tr -d "\n" < file1
foobarbaz%
```

### 구문:

tr 명령의 일반적인 구문은 다음과 같습니다.

```
tr [options] string1 [string2]
```

### 추가 플래그와 기능:

| **짧은 플래그** | **긴 플래그** | **설명**                                                                                               |
| :------------- | :------------ | :------------------------------------------------------------------------------------------------------------ |
| `-C`           |               | string1의 문자 집합을 보수 집합으로 만듭니다. 즉 `-C ab`는 `a`와 `b`를 제외한 모든 문자를 포함합니다. |
| `-c`           |               | -C와 같습니다.                                                                                                   |
| `-d`           |               | 입력에서 string1의 문자를 삭제합니다.                                                                  |
| `-s`           |               | string1에 문자 시퀀스가 있으면 하나로 결합합니다.                                       |

# `fdisk` 명령

`fdisk` 명령은 디스크 파티션 테이블을 제어하고 변경하는 데 사용됩니다. 다음은 이 명령이 제공하는 옵션 중 일부입니다: </b>

- 새 드라이브를 위한 공간 구성.
- 기존 드라이브 수정.
- 새 파티션을 위한 공간 생성.
- 데이터를 새 파티션으로 이동.
  

### 예제:

1. 시스템에서 사용 가능한 모든 파티션의 기본 세부 정보 보기:

```
fdisk -l
```

2. 파티션 크기 표시:

```
fdisk -s /dev/sda
```

3. 도움말 메시지와 명령의 모든 옵션 보기:

```
fdisk -h
```

### 구문(Syntax):

```bash
fdisk [options] device
```
### 일부 명령어 옵션:

다음 명령어를 입력하면 다음 창이 나타납니다:

```
fdisk /dev/sdb
```

![Options](https://media.geeksforgeeks.org/wp-content/uploads/20190219152451/Screenshot-711.png)
그다음 m을 입력하면 새 파티션 생성, 파티션 삭제 등 필요한 모든 옵션이 다음 그림처럼 표시됩니다:
![Options](https://media.geeksforgeeks.org/wp-content/uploads/20190219153114/Screenshot-741.png)

# `Wait` 명령어

wait 명령어는 특정 백그라운드 프로세스 또는 실행 중인 모든 자식 프로세스가 완료될 때까지 스크립트 실행을 일시 중지하는 셸 내장 명령어입니다.

주된 목적은 작업을 동기화하여, 선행 백그라운드 작업이 완료되기 전에는 스크립트가 다음 단계로 진행하지 않도록 하는 것입니다. 백그라운드 프로세스는 끝에 앰퍼샌드(&)를 붙여 실행하는 명령어로, 셸이 완료를 기다리지 않고 실행하도록 합니다.

## 구문

```bash
$ wait [PID]
```

[PID] - 기다릴 프로세스 ID(선택 사항)입니다. PID를 지정하지 않으면 wait는 활성 자식 프로세스가 모두 완료될 때까지 기다립니다.

## 예제

### 1. 특정 프로세스 기다리기 {.unnumbered}

이 예제에서는 단일 백그라운드 프로세스를 시작한 뒤 해당 프로세스만 기다리는 방법을 보여 줍니다.

### 스크립트:

```bash
#!/bin/bash
echo "This process will run in the background..." &
process_id=$!

echo "Script is now waiting for process ID: $process_id"
wait $process_id
echo "Process $process_id has finished."
echo "The script exited with status: $?"
```

### 설명:

* &: 앰퍼샌드는 echo 명령어를 백그라운드에서 실행하므로 스크립트가 즉시 다음 줄로 진행할 수 있습니다.

* $!: 가장 최근에 실행된 백그라운드 명령어의 프로세스 ID(PID)를 저장하는 특수 셸 변수입니다. 이 값을 process_id 변수에 저장합니다.

* wait $process_id: 핵심 명령어입니다. 스크립트는 해당 ID의 프로세스가 완료될 때까지 여기서 일시 중지됩니다.

* $?: 마지막으로 완료된 명령어의 종료 상태를 저장하는 변수입니다. 종료 상태가 0이면 성공을 의미합니다.

### 출력:

```bash
$ bash wait_example.sh
Script is now waiting for process ID: 12345
This process will run in the background...
Process 12345 has finished.
The script exited with status: 0
```

### 2. 모든 백그라운드 프로세스 기다리기 {.unnumbered}

가장 일반적인 사용 사례입니다. 여러 백그라운드 작업을 시작한 다음 wait 명령어 하나로 모든 작업이 끝날 때까지 일시 중지합니다.

### 스크립트:

```bash
#!/bin/bash

echo "Starting multiple background jobs..."
sleep 3 &
sleep 1 &
sleep 2 &

echo "Waiting for all sleep commands to finish."
wait
echo "All jobs are done. Continuing with the rest of the script."
```

### 출력:

```bash
$ bash wait_all_example.sh
Starting multiple background jobs...
Waiting for all sleep commands to finish.
(after about 3 seconds)
All jobs are done. Continuing with the rest of the script.
```

# `zcat` 명령어

`zcat`은 압축 파일의 내용을 확인할 수 있게 해 줍니다.

### 예제:

1. 압축 파일의 내용을 확인합니다:

```
~$ zcat test.txt.gz
Hello World
```

2. 여러 파일에도 사용할 수 있습니다:

```
~$ zcat test2.txt.gz test.txt.gz
hello
Hello world
```

### 구문:

`zcat` 명령어의 일반적인 구문은 다음과 같습니다:

```
zcat [  -n ] [  -V ] [  File ... ]
```

# `fold` 명령어

Linux의 `fold` 명령어는 입력 파일의 각 줄을 지정한 너비에 맞게 줄바꿈하여 표준 출력으로 표시합니다. 

기본적으로 최대 80열 너비에서 줄을 바꾸지만 이 값은 변경할 수 있습니다. 

fold 명령어로 입력을 줄바꿈하려면 파일 또는 표준 입력을 명령어에 전달합니다.

### 구문:

```
fold [OPTION]... [FILE]...
```

### 옵션

**-w** : 이 옵션을 사용하면 열 수로 너비를 제한할 수 있습니다.

이 명령어를 사용하면 기본 너비인 80열을 변경할 수 있습니다.
구문:

```
fold -w[n] [FILE]
```

 예: file1.txt의 줄을 60열 너비로 줄바꿈합니다

```
fold -w60 file1.txt
```

**-b** : 이 옵션은 열 수가 아니라 바이트 수로 출력 너비를 제한합니다. 

이 옵션을 사용하면 출력 너비를 지정한 바이트 수로 강제할 수 있습니다.

```
fold -b[n] [FILE]
```

예: 파일의 출력 너비를 40바이트로 제한하면 명령어가 40바이트마다 출력 내용을 줄바꿈합니다.

```
fold -b40 file1.txt
```

**-s** : 단어가 끊어지지 않도록 공백을 기준으로 줄을 바꾸는 옵션입니다. 

줄의 처음 width열 안에 공백 문자가 있으면, 너비 제한을 만족하는 마지막 공백 문자 뒤에서 줄을 바꿉니다.

```
fold -w[n] -s [FILE]
```

# `quota` 명령어

`quota`는 디스크 사용량과 제한을 표시합니다.

### 설치:

Ubuntu 시스템에서는 다음 명령어를 실행해 quota를 설치할 수 있습니다:

```
sudo apt-get install quota
```

Debian에서는 sudo 없이 다음 설치 명령어를 사용합니다:

```
apt-get install quota
```

### 구문:

`quota` 명령어의 기본 구문은 다음과 같습니다.

```
quota [ -u [ User ] ] [ -g [ Group ] ] [ -v | -q ]
```

# `aplay` 명령어

`aplay`는 ALSA(Advanced Linux Sound Architecture) 사운드 카드 드라이버를 위한 명령줄 오디오 플레이어입니다. 여러 파일 형식과 여러 장치가 연결된 여러 사운드 카드를 지원합니다. 기본적으로 명령줄 인터페이스에서 오디오를 재생하는 데 사용됩니다.

`aplay`는 녹음 대신 재생을 수행한다는 점을 제외하면 `arecord`와 매우 유사합니다. 지원되는 사운드 파일 형식의 경우 샘플링 레이트, 비트 깊이 등의 정보가 사운드 파일 헤더에서 자동으로 확인될 수 있습니다.

## 구문

```
$ aplay [flags] [filename [filename]] ...
```

## 옵션:

```
-h, help : Show the help information.
-d, duration=# : Interrupt after # seconds.
-r, rate=# : Sampling rate in Hertz. The default rate is 8000 Hertz.
version : Print current version.
-l, list-devices : List all soundcards and digital audio devices.
-L, list-pcms : List all PCMs(Pulse Code Modulation) defined.
-D, device=NAME : Select PCM by name.
```

::: {.callout-note}
이 명령어에는 일반적으로 필요하지 않은 다양한 옵션도 포함되어 있습니다. 더 자세한 내용을 알고 싶다면 터미널에서 다음 명령어를 실행하면 됩니다.
:::

```
aplay --help
```

## 예제:

1. 2500Hz 주파수로 10초간 오디오 재생:

   ```
   $ aplay -d 10 -r 2500hz sample.mp3
   ```

   > `sample.mp3` 파일을 2500Hz 주파수로 10초 동안 재생합니다.

2. 2500Hz 주파수로 전체 오디오 클립 재생:

   ```
   $ aplay -r 2500hz sample.mp3
   ```

   > `sample.mp3` 파일을 2500Hz 주파수로 재생합니다.

3. 버전 정보 출력:

   ```
   $ aplay --version
   ```

   > 버전 정보를 출력합니다 (예: `aplay: version 1.1.0`).

***

# `spd-say` 명령어

`spd-say`는 텍스트 음성 변환 출력을 요청하는 메시지를 `speech-dispatcher` 프로세스로 전송합니다. `speech-dispatcher` 프로세스가 이 요청을 처리하며 정상적으로 작동하는 경우 결과를 오디오 시스템으로 출력합니다.

## Syntax:

```
$ spd-say [options] "some text"
```

## 옵션:

```
-r, --rate
       Set the rate of the speech (between -100 and +100, default: 0)

-p, --pitch
       Set the pitch of the speech (between -100 and +100, default: 0)

-i, --volume
       Set the volume (intensity) of the speech (between -100 and +100, default: 0)

-o, --output-module
       Set the output module

-l, --language
       Set the language (iso code)

-t, --voice-type
       Set the preferred voice type  (male1,  male2,  male3,  female1,  female2,  female3,
       child_male, child_female)

-m, --punctuation-mode
       Set the punctuation mode (none, some, all)

-s, --spelling
       Spell the message

-x, --ssml
       Set SSML mode on (default: off)

-e, --pipe-mode
       Pipe from stdin to stdout plus Speech Dispatcher

-P, --priority
       Set  priority  of  the  message  (important, message, text, notification, progress;
       default: text)

-N, --application-name
       Set the application name used to establish the connection to specified string value
       (default: spd-say)

-n, --connection-name
       Set  the connection name used to establish the connection to specified string value
       (default: main)

-w, --wait
       Wait till the message is spoken or discarded

-S, --stop
       Stop speaking the message being spoken in Speech Dispatcher

-C, --cancel
       Cancel all messages in Speech Dispatcher

-v, --version
       Print version and copyright info

-h, --help
       Print this info
```

## 예제:

1. 지정한 텍스트를 음성으로 읽어주기:

```
$ spd-say "Hello"
```

> "Hello"라는 음성을 재생합니다.

# `xeyes` 명령어

`xeyes`는 마우스 커서의 움직임을 따라가는 눈 모양을 데스크톱에 표시하는 그래픽 사용자 인터페이스 프로그램입니다. 실용적인 명령어라기보다는 재미를 위한 명령어처럼 보이지만 재미를 제공하는 것 자체도 유용한 기능의 한 측면이라고 할 수 있습니다.


### 구문:

```
xeyes
```

### `xeyes`의 용도는 무엇인가요?

`xeyes`는 단순한 재미만을 위한 프로그램은 아닙니다. 이 프로그램은 때때로 찾기 어려운 마우스 포인터를 따라갈 수 있도록 해줍니다. 특히 여러 모니터가 서로 떨어져 있는 멀티헤드 컴퓨터에서 유용하며 누군가(예를 들어 학교의 교사)가 한 화면에서 내용을 발표할 때 다른 모니터를 보는 사람들도 `xeyes`를 통해 마우스 위치를 쉽게 따라갈 수 있습니다. [unix.stackexchange](https://unix.stackexchange.com/questions/162769/what-is-the-purpose-of-xeyes)

# `parted` 명령어

`parted` 명령어는 Linux에서 하드 디스크 파티션을 관리하는 데 사용됩니다. 파티션에 저장된 파일 시스템과 함께 디스크 파티션을 추가, 삭제, 축소 및 확장할 수 있습니다.

`parted` 명령어를 실행하려면 시스템에 대한 루트 권한이 필요합니다.

**참고:** `parted`는 변경 사항을 디스크에 즉시 기록하므로 디스크 파티션을 수정할 때 주의해야 합니다.
### 예제:

1. 모든 블록 장치의 파티션 구조 출력:

```
sudo parted -l
```

2. 특정 디스크의 파티션 테이블 확인:

```
sudo parted disk print
```

디스크 장치명의 예: `/dev/sda`, `/dev/sdb`

3. 특정 디스크에 `label-type` 유형의 새 디스크 레이블 생성

```
sudo parted mklabel disk label-type
```

`label-type`은 "gpt", "msdos", "bsd", "mac", "sun" 등의 파티션 테이블 형식을 지정할 수 있습니다.<br />

4. 특정 디스크에 파티션 유형, 파일 시스템 유형 및 크기(MB)를 지정하여 새 파티션 생성:

```
sudo parted disk mkpart part-time fs-type 1 size
```

`part-time`에는 `"primary"`, `"logical"`, `"extended"` 값을 사용할 수 있습니다.

`fs-type`은 선택 사항입니다. 다음 값을 사용할 수 있습니다: `"btrfs"`, `"ext2"`, `"ext3"`, `"ext4"`, `"fat16"`, `"fat32"`, `"hfs"`, `"hfs+"`, `"linux-swap"`, `"ntfs"`, `"reiserfs"`, `"udf"` 또는 `"xfs"`.

`size`는 지정한 디스크의 전체 크기보다 작아야 합니다. 크기가 50MB인 파티션을 생성하려면 `<size>`에 `50`을 지정합니다.

5. `parted`는 대화형 형식으로도 실행할 수 있습니다. 대화형 세션에서 적절한 명령어를 입력하여 디스크 파티션을 관리할 수 있습니다.

대화형 세션에서 `help` 명령어를 입력하면 수행할 수 있는 모든 디스크 관리 작업의 목록이 표시됩니다.

```
  $ sudo parted
  GNU Parted 3.3
  Using /dev/sda
  Welcome to GNU Parted! Type 'help' to view a list of commands.
  (parted) print  # prints the partition table of the default selected disk - /dev/sda                                                  
  Model: ATA VBOX HARDDISK (scsi)
  Disk /dev/sda: 53.7GB
  Sector size (logical/physical): 512B/512B
  Partition Table: msdos
  Disk Flags: 

  Number  Start   End     Size    Type     File system  Flags
   1      1049kB  53.7GB  53.7GB  primary  ext4         boot

  (parted) select /dev/sdb  # change the current disk on which operations have to be performed                                                
  Using /dev/sdb
  (parted) quit  # exit the interactive session
```

### Syntax Forms:

```
parted [options] [device [command [options...]...]]
```

### 옵션:

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-h` | `--help` | 사용 가능한 모든 `commands [options]`를 나열하는 도움말 메시지를 표시합니다. |
| `-l` | `--list` | 모든 블록 장치의 파티션 레이아웃을 나열합니다. |
| `-m` | `--machine` | 시스템이 분석할 수 있는 형식으로 출력합니다. |
| `-v` | `--version` | 버전 정보를 표시합니다. |
| `-a` | `--align` | 새로 생성되는 파티션의 정렬 유형을 설정합니다. 다음 값을 사용할 수 있습니다:<br />`none`: 디스크 유형에서 허용하는 최소 정렬을 사용합니다.<br />`cylinder`: 파티션을 실린더 단위로 정렬합니다.<br />`minimal`: 디스크 토폴로지 정보에 따라 최소 정렬을 사용합니다.<br />`optimal`: 디스크 토폴로지 정보에 따라 최적 정렬을 사용합니다. |

# `nl` 명령어

`nl` 명령어는 파일의 각 줄에 번호를 매깁니다. 파일 내용을 확인하는 또 다른 방법으로, 다양한 작업에 매우 유용하게 사용할 수 있습니다.

## 구문(Syntax)

```
nl [ -b Type ] [ -f Type ] [ -h Type ] [ -l Number ] [ -d Delimiter ] [ -i Number ] [ -n Format ] [ -v Number ] [ -w Number ] [ -p ] [ -s Separator ] [ File ]
```

## Examples:

1. 모든 줄에 번호 붙이기:

```
nl  -ba  chap1
```

2. 모든 텍스트 줄을 표시합니다:

```
[server@ssh ~]$ nl states
1 Alabama
2 Alaska
3 Arizona
4 Arkansas
5 California
6 Colorado
7 Connecticut.
8 Delaware
```

3. 다른 줄 번호 형식 지정

```
nl  -i10  -nrz  -s::  -v10  -w4  chap1
```

명령줄에서는 파일을 하나만 지정할 수 있습니다. 플래그와 파일 이름은 어떤 순서로든 나열할 수 있습니다.

# `pidof` 명령어

`pidof`는 실행 중인 프로그램의 프로세스 ID(PID)를 확인할 수 있는 명령줄 유틸리티입니다.

## 구문(Syntax)

```
pidof [OPTIONS] PROGRAM_NAME
```

명령어의 도움말과 전체 옵션을 확인하려면:

```
[user@home ~]$ pidof -h

 -c           Return PIDs with the same root directory
 -d <sep>     Use the provided character as output separator
 -h           Display this help text
 -n           Avoid using stat system function on network shares
 -o <pid>     Omit results with a given PID
 -q           Quiet mode. Do not display output
 -s           Only return one PID
 -x           Return PIDs of shells running scripts with a matching name
 -z           List zombie and I/O waiting processes. May cause pidof to hang.
```

## 예제

SSH 서버의 PID를 확인하려면 다음 명령어를 실행합니다.

```bash
pidof sshd
```

`sshd`와 일치하는 이름의 실행 중인 프로세스가 있으면 해당 프로세스의 PID가 화면에 표시됩니다. 일치하는 프로세스를 찾지 못하면 출력 결과는 비어 있습니다.

```
# Output
4382 4368 811
```

`pidof`는 요청한 이름과 일치하는 실행 중인 프로그램이 하나 이상 있으면 `0`을 반환합니다. 그렇지 않으면 종료 코드로 `1`을 반환합니다. 이는 셸 스크립트를 작성할 때 유용하게 사용할 수 있습니다.

검색하려는 프로그램의 PID만 확실하게 표시하려면 프로그램의 전체 경로명을 인수로 사용하세요. 예를 들어 서로 다른 디렉터리에 같은 이름의 실행 중인 프로그램이 두 개 있다면, `pidof`는 두 프로그램의 PID를 모두 표시합니다.

기본적으로 일치하는 실행 중인 프로그램의 모든 PID가 표시됩니다. `-s` 옵션을 사용하면 `pidof`가 하나의 PID만 표시하도록 할 수 있습니다.

```
pidof -s program_name
```

`-o` 옵션을 사용하면 지정한 PID를 가진 프로세스를 결과에서 제외할 수 있습니다.

```
pidof -o pid program_name
```

`pidof`에 `-o` 옵션을 사용할 때 호출한 셸이나 스크립트를 나타내는 `%PPID` 특수 값을 지정할 수 있습니다.

동일한 루트 디렉터리에서 실행 중인 프로세스의 PID만 반환하려면 `-c` 옵션을 사용합니다.
이 옵션은 `root` 권한이나 `sudo`로 실행할 때만 동작합니다.

```
pidof -c pid program_name
```

## 결론

`pidof` 명령어는 현재 실행 중인 특정 프로그램의 PID를 확인하는 데 사용됩니다.

`pidof`는 옵션이 많지 않은 간단한 명령어입니다. 일반적으로 검색하려는 프로그램의 이름만 지정하여 실행합니다.

# `shuf` 명령어

Linux의 `shuf` 명령어는 입력된 줄을 무작위로 섞은 순열 형태로 표준 출력에 기록합니다. 카드 패를 섞는 것과 같은 방식으로 입력을 의사 무작위화합니다. 이 명령어는 GNU Coreutils에 포함되어 있으며 POSIX 표준에는 포함되지 않습니다. Bash에서 파일 또는 표준 입력으로부터 데이터를 읽고, 입력된 줄을 무작위로 섞어 출력합니다.

## 구문(Syntax)

```
# file shuf
shuf [OPTION] [FILE]

# list shuf
shuf -e [OPTION]... [ARG]

# range shuf
shuf -i LO-HI [OPTION]
```

다른 Linux 명령어와 마찬가지로 `shuf` 명령어도 `--help` 옵션을 제공합니다.

```
[user@home ~]$ shuf --help
Usage: shuf [OPTION]... [FILE]
  or:  shuf -e [OPTION]... [ARG]...
  or:  shuf -i LO-HI [OPTION]...
Write a random permutation of the input lines to standard output.

With no FILE, or when FILE is -, read standard input.

Mandatory arguments to long options are mandatory for short options too.
  -e, --echo                treat each ARG as an input line
  -i, --input-range=LO-HI   treat each number LO through HI as an input line
  -n, --head-count=COUNT    output at most COUNT lines
  -o, --output=FILE         write result to FILE instead of standard output
      --random-source=FILE  get random bytes from FILE
  -r, --repeat              output lines can be repeated
  -z, --zero-terminated     line delimiter is NUL, not newline
```

## 예제

### 옵션이나 인수 없이 `shuf` 명령어 사용

```bash
shuf
```

명령줄에서 `shuf` 명령어를 아무 인수 없이 사용하면, 사용자가 `Ctrl+D`를 입력하여 입력을 종료할 때까지 사용자로부터 입력을 받습니다. 그런 다음 입력된 줄을 무작위 순서로 표시합니다.

입력 줄로 `1, 2, 3, 4, 5`를 입력하면 다음과 같이 `1, 2, 3, 4, 5`가 무작위 순서로 출력됩니다.

```
[user@home ~]$ shuf
1
2
3
4
5

4
5
1
2
3
```

파이프를 통해 표준 입력을 전달받는 예제입니다.

```
{
seq 5 | shuf
}
```

`seq 5`는 1부터 5까지의 정수를 순서대로 반환하며 `shuf`는 이를 표준 입력으로 받아 무작위로 섞어 출력합니다.

```
[user@home ~]$ {
> seq 5 | shuf
> }
5
4
2
3
1
```

### 파일 셔플(`File shuf`)

`shuf` 명령어를 `-e` 또는 `-i` 옵션 없이 사용하면 **파일 셔플(file shuf)**로 동작합니다. 즉, 파일의 내용을 무작위 순서로 섞습니다.

`<file_name>`은 `shuf` 명령어의 마지막 매개변수입니다. 파일 이름을 지정하지 않으면 셸이나 파이프를 통해 입력을 제공해야 합니다.

파일에서 입력을 받는 예는 다음과 같습니다.

```
shuf file.txt
```

`file.txt`에 6개 행이 포함되어 있다고 가정할 때, `shuf` 명령어는 입력된 행들을 무작위 순서로 섞어 출력합니다.

```
[user@home ~]$ cat file.txt
line-1
line-2
line-3
line-4
line-5

[user@home ~]$ shuf file.txt
line-5
line-4
line-1
line-3
line-2
```

`-n` 옵션을 사용하면 원하는 개수의 줄만 무작위로 추출할 수 있습니다.

```
shuf -n 2 file.txt
```

파일에서 무작위로 선택된 2개의 줄을 출력합니다.

```
line-5
line-2
```

### 목록 셔플(`List shuf`)

`shuf` 명령어와 함께 `-e` 옵션을 사용하면 **목록 셔플(list shuf)**로 동작합니다. 이때 명령어의 인자들이 `shuf`의 입력 줄로 처리됩니다.

예를 들어 다음과 같이 사용할 수 있습니다.

```
shuf -e A B C D E
```

`A, B, C, D, E`를 입력 줄로 받아 섞은 후 그 결과를 출력합니다.

```
A
C
B
D
E
```

`-e` 옵션과 함께 `-n` 옵션을 사용하면 무작위로 선택할 줄 수를 지정할 수 있습니다.

```
shuf -e -n 2 A B C D E
```

입력값 중 무작위로 2개를 선택하여 출력합니다.

```
E
A
```

### 범위 셔플(`Range shuf`)

`shuf` 명령어와 함께 `-i` 옵션을 사용하면 **범위 셔플(range shuf)**로 동작합니다. 이 경우 입력으로 숫자 범위를 지정해야 하며 `LO`는 하한, `HI`는 상한을 의미합니다. `LO`부터 `HI`까지의 정수를 무작위로 섞어 출력합니다.

```
[user@home ~]$ shuf -i 1-5
4
1
3
2
5
```
## 결론

`shuf` 명령어는 입력된 줄을 무작위로 섞는 데 사용됩니다. 또한 출력할 줄 수를 제한하거나, 줄을 반복해서 출력하거나, 양의 난수를 생성하는 기능도 제공합니다. 여기서 다룬 내용을 충분히 연습한 후에는 [`shuf` 매뉴얼 페이지](https://linux.die.net/man/1/shuf)를 확인하여 더 자세한 내용을 알아보세요.

# `less` 명령어

`less` 명령어는 파일의 내용을 한 번에 한 화면씩 보여주는 Linux 터미널용 페이지 표시 도구입니다.

대용량 텍스트 파일을 다룰 때 유용합니다. 파일 전체를 한꺼번에 로드하지 않고 페이지 단위로 접근하므로 빠르게 불러올 수 있습니다.

## 구문(Syntax)

```
less [options] file_path
```

## 옵션

자주 쓰이는 주요 옵션 플래그는 다음과 같습니다.

```
-E	less automatically exits upon reaching the end of file.
-f	Forces less to open non-regular files (a directory or a device-special file).
-F	Exit less if the entire file can be displayed on the first screen.
-g	Highlights the string last found using search. By default, less highlights all strings matching the last search command.
-G	Removes all highlights from strings found using search.
```

전체 옵션 목록을 확인하려면 `less` 실행 중 `h` 키를 누르거나 도움말을 참조하세요.

```
    less --help
```

## Few Examples:

1. 텍스트 파일 열기

```
less /etc/updatedb.conf
```

2. 줄 번호 표시

```
less -N /etc/init/mysql.conf
```

3. 패턴 검색으로 파일 열기

```
less -pERROR /etc/init/mysql.conf
```

4. 연속된 빈 줄 제거:

```
less welcome.txt
```
여기서는 Linux에서 `less` 명령어를 사용하는 방법을 설명했습니다. `most`나 `more`와 같은 다른 터미널 페이지 표시기도 있지만 `less`는 거의 모든 시스템에 존재하는 강력한 도구이므로 더 나은 선택이 될 수 있습니다. `less`는 대용량 입력 전체를 읽기 전에 표시를 시작할 수 있어 빠르게 내용을 확인할 수 있습니다.

# `nslookup` 명령어

`nslookup` 명령어는 도메인 이름과 IP 주소의 매핑 또는 특정 DNS 레코드를 확인하기 위해 DNS(Domain Name System)를 조회하는 네트워크 관리용 명령줄 도구입니다. [geeksforgeeks](https://www.geeksforgeeks.org/linux-unix/nslookup-command-in-linux-with-examples/)

## 구문(Syntax)

```bash
nslookup [options] [host]
```

## 옵션

일반적으로 사용하는 옵션 플래그는 다음과 같습니다.

```text
-domain=[domain-name]   기본 DNS 이름을 변경합니다.
-debug                  디버깅 정보를 표시합니다.
-port=[port-number]     조회에 사용할 포트를 지정합니다. 기본 포트 번호는 53입니다.
-timeout=[seconds]      서버가 응답할 때까지 허용할 시간을 지정합니다.
-type=a                 DNS A 주소 레코드 정보를 표시합니다.
-type=any               사용 가능한 모든 레코드를 표시합니다.
-type=hinfo             호스트와 관련된 하드웨어 정보를 표시합니다.
-type=mx                메일 교환(MX) 서버 정보를 표시합니다.
-type=ns                네임 서버 레코드를 표시합니다.
-type=ptr               포인터 레코드를 표시합니다. 역방향 DNS 조회에 사용합니다.
-type=soa               권한 시작(SOA) 레코드를 표시합니다.
```

## 예시

1. DNS 서버를 조회합니다.

```bash
nslookup www.google.com
```

2. 조회에 사용할 포트를 지정합니다.

```bash
nslookup -port=53 www.google.com
```

3. MX 레코드를 가져옵니다.

```bash
nslookup -type=mx google.com
```

여기서는 Linux에서 `nslookup` 명령어를 사용하는 방법을 설명했습니다. `dig`와 같은 다른 DNS 조회 도구도 있지만 `nslookup`은 거의 모든 시스템에 존재하는 강력한 도구이므로 더 나은 선택이 될 수 있습니다.

자세한 내용은 [Wikipedia의 nslookup 문서](https://en.wikipedia.org/wiki/Nslookup)를 참고하십시오.

# `cmp` 명령어

`cmp` 명령어는 두 파일을 바이트 단위로 비교하는 간단한 유틸리티입니다.

파일이 동일하면 `cmp`는 아무것도 출력하지 않고 성공 상태로 종료합니다. 파일이 다르면 처음 차이가 발생한 바이트와 행 번호를 표시합니다.

## 구문(Syntax)

```bash
$ cmp [OPTION]... FILE1 [FILE2]
```

## 예제

다음 예제에서는 세 개의 파일이 있다고 가정합니다.

`file1.txt` 내용:

```text
hello world
```

`file2.txt` 내용:

```text
hello world
```

`file3.txt` 내용:

```text
hello World
```

### 1. 동일한 두 파일 비교 {.unnumbered}

파일이 동일하면 `cmp`는 아무것도 출력하지 않습니다. 두 파일이 동일한지 확인하는 표준적인 방법입니다.

```bash
$ cmp file1.txt file2.txt
```

(출력이 표시되지 않습니다.)

### 2. 서로 다른 두 파일 비교 {.unnumbered}

차이가 발견되면 `cmp`는 처음으로 차이가 발생한 위치를 표시합니다.

```text
$ cmp file1.txt file3.txt
file1.txt file3.txt differ: byte 7, line 1
```

### 3. 차이가 있는 모든 바이트 표시(`--verbose` 또는 `-l`) {.unnumbered}

`-l`(소문자 L) 플래그는 매우 유용합니다. 파일에서 발견되는 모든 차이에 대해 바이트 번호를 10진수로 표시하고, 서로 다른 바이트의 값을 8진수로 표시합니다.

```text
$ cmp -l file1.txt file3.txt
 7 167 127
```

#### 설명

이 출력은 7번째 바이트에서 `file1.txt`가 8진수 값 `167`(문자 `w`)을 가지고 있는 반면, `file3.txt`는 8진수 값 `127`(문자 `W`)을 가진다는 의미입니다.

### 4. 처음 `n`바이트만 비교(`--bytes` 또는 `-n`) {.unnumbered}

비교할 바이트 수를 제한할 수 있습니다. 여기서는 처음 5바이트만 비교합니다. 두 파일에서 `hello`가 동일하므로 `cmp`는 차이를 찾지 못합니다.

```bash
$ cmp -n 5 file1.txt file3.txt
```

(출력이 표시되지 않습니다.)

### 5. 처음 `n`바이트 무시(`--ignore-initial` 또는 `-i`) {.unnumbered}

비교를 시작하기 전에 파일의 시작 부분에서 지정한 바이트 수를 건너뛰도록 `cmp`에 지시할 수 있습니다. 여기서는 처음 6바이트를 건너뛰므로 문자 `w`부터 비교가 시작됩니다.

```text
$ cmp -i 6 file1.txt file3.txt
file1.txt file3.txt differ: byte 1, line 1
```

#### 설명

처음 6바이트를 무시한 후 비교가 시작되었기 때문에 출력에는 차이가 “바이트 1”에서 발생했다고 표시됩니다.

### 일반적인 옵션

| **단축 플래그** | **긴 플래그** | **설명** |
|:---|:---|:---|
| `-b` | `--print-bytes` | 서로 다른 바이트를 출력합니다. |
| `-i SKIP` | `--ignore-initial=SKIP` | 두 파일의 시작 부분에서 `SKIP`바이트를 건너뜁니다. |
| `-l` | `--verbose` | 서로 다른 모든 바이트의 번호와 값을 출력합니다. |
| `-n LIMIT` | `--bytes=LIMIT` | 최대 `LIMIT`바이트까지 비교합니다. |
| `-s` | `--quiet`, `--silent` | 모든 출력을 억제하고 종료 상태만 반환합니다. |
# `expr` 명령어

`expr` 명령어는 지정한 표현식을 평가하고 그 결과를 출력합니다. 정수의 덧셈, 뺄셈, 곱셈, 나눗셈, 나머지 계산과 같은 기본 연산뿐 아니라 정규 표현식 평가, 부분 문자열 추출, 문자열 길이 계산 등의 문자열 작업에도 사용됩니다.

## 구문(Syntax)

```bash
expr expression
```

## 예시

1. `expr` 명령어로 기본 산술 연산을 수행합니다.

```bash
expr 7 + 14
expr 7 \* 8
```

2. 두 표현식을 비교합니다.

```bash
x=10
y=20
res=`expr $x = $y`
echo $res
```

3. 두 문자열에서 일치하는 문자의 개수를 확인합니다.

```bash
expr alphabet : alpha
```

4. 나머지 값을 확인합니다.

```bash
expr 20 % 30
```

5. 부분 문자열을 추출합니다.

```bash
a=HelloWorld
b=`expr substr $a 6 10`
echo $b
```

### 추가 플래그 및 기능

| **플래그** | **설명** |
|:---|:---|
| `--version` | 버전 정보를 출력한 후 종료합니다. |
| `--help` | 이 도움말을 표시한 후 종료합니다. |

자세한 내용은 [Wikipedia의 Expr 문서](https://en.wikipedia.org/wiki/Expr)를 참고하십시오.

# `wall` 명령어

`wall` 명령어는 *write all*의 약자로, Linux 시스템에 로그인한 모든 사용자에게 메시지를 전송할 때 사용합니다. 시스템 관리자가 예정된 유지 보수나 긴급 공지와 같은 중요한 정보를 여러 사용자에게 알릴 때 주로 사용합니다.

## 구문(Syntax)

```bash
$ wall [options] [message]
```

명령줄에 `[message]`를 지정하지 않으면 `wall`은 파일 끝 문자(`Ctrl+D`)를 받을 때까지 표준 입력을 읽습니다.

## 옵션

| **옵션** | **설명** |
|:---|:---|
| `-n` | 배너(메시지를 보낸 사용자를 표시하는 부분)를 숨기고 메시지 본문만 표시합니다. |
| `-t [seconds]` | 제한 시간을 초 단위로 설정합니다. `wall`은 포기하기 전에 지정한 시간 동안 사용자의 터미널에 메시지를 쓰려고 시도합니다. |

## 예시

### 1. 모든 사용자에게 메시지 방송 {.unnumbered}

이 명령어는 로그인한 모든 사용자에게 메시지를 직접 전송합니다.

```bash
$ wall "시스템이 10분 후 종료됩니다. 작업 내용을 저장하십시오."
```

### 다른 사용자의 터미널에 표시되는 출력

```text
Broadcast message from your_username@hostname (pts/0) (Sat Oct  4 19:50:00 2025):


시스템이 10분 후 종료됩니다. 작업 내용을 저장하십시오.
```

### 2. 텍스트 파일의 내용을 메시지로 방송 {.unnumbered}

파일의 내용을 리디렉션하여 메시지로 사용할 수 있습니다.

### `message.txt`의 내용

```text
시스템 유지 보수가 곧 시작됩니다.
연결이 일시적으로 불안정할 수 있습니다.
```

### 명령어

```bash
$ wall < message.txt
```

### 3. 표준 입력으로 여러 줄 메시지 전송 {.unnumbered}

메시지 없이 `wall`을 실행하면 터미널에서 여러 줄로 된 메시지를 직접 입력할 수 있습니다. 입력을 마친 후 `Ctrl+D`를 누르면 메시지가 전송됩니다.

```bash
$ wall
서버가 다시 온라인 상태가 되었습니다.
모든 서비스가 정상적으로 실행 중입니다.
<Ctrl+D>
```

### 다른 사용자의 터미널에 표시되는 출력

```text
Broadcast message from your_username@hostname (pts/0) (Sat Oct  4 19:52:00 2025):


서버가 다시 온라인 상태가 되었습니다.
모든 서비스가 정상적으로 실행 중입니다.
```

# `ln` 명령어

`ln` 명령어는 Linux에서 파일 간 링크를 생성할 때 사용합니다. 파일 시스템을 관리하고 구성하는 데 필수적인 하드 링크와 심볼릭 링크(소프트 링크)를 모두 생성할 수 있습니다.

## 구문(Syntax)

```bash
ln [options] target linkname
ln [options] target... directory
```

## 링크의 유형

### 하드 링크

- 파일의 아이노드를 직접 가리킵니다.
- 서로 다른 파일 시스템에 걸쳐 생성할 수 없습니다.
- 디렉터리를 대상으로 생성할 수 없습니다.
- 원본 파일을 삭제해도 하드 링크에는 데이터가 계속 남아 있습니다.

### 심볼릭 링크(소프트 링크)

- 바로 가기처럼 파일 경로를 가리킵니다.
- 서로 다른 파일 시스템에 걸쳐 생성할 수 있습니다.
- 디렉터리를 대상으로 생성할 수 있습니다.
- 원본 파일을 삭제하면 심볼릭 링크가 끊어집니다.

## 옵션

일반적으로 사용하는 옵션 플래그는 다음과 같습니다.

```text
-s  하드 링크 대신 심볼릭 링크(소프트 링크)를 생성합니다.
-f  기존 대상 파일을 삭제하여 링크 생성을 강제로 수행합니다.
-v  자세한 출력을 표시하고 어떤 링크가 생성되는지 보여 줍니다.
-n  대상이 디렉터리를 가리키는 심볼릭 링크인 경우 대상을 일반 파일로 처리합니다.
-r  상대 심볼릭 링크를 생성합니다.
-t  링크를 생성할 대상 디렉터리를 지정합니다.
```

## 예시

1. 하드 링크를 생성합니다.

```bash
ln file.txt hardlink.txt
```

2. 심볼릭 링크를 생성합니다.

```bash
ln -s /path/to/original/file.txt symlink.txt
```

3. 디렉터리를 가리키는 심볼릭 링크를 생성합니다.

```bash
ln -s /var/log logs
```

4. 디렉터리에 여러 심볼릭 링크를 생성합니다.

```bash
ln -s /usr/bin/python3 /usr/bin/gcc /usr/local/bin/
```

5. 상대 심볼릭 링크를 생성합니다.

```bash
ln -sr ../config/app.conf current_config
```

6. 링크를 강제로 생성하여 기존 링크를 덮어씁니다.

```bash
ln -sf /new/target existing_link
```

7. 자세한 출력을 표시하면서 링크를 생성합니다.

```bash
ln -sv /source/file /destination/link
```

## 사용 사례

- 자주 사용하는 파일이나 디렉터리에 대한 바로 가기 생성
- 여러 버전의 설정 파일 관리
- 저장 공간을 중복으로 사용하지 않고 파일 구성
- 중요한 파일에 대한 백업 참조 생성
- 공유 라이브러리를 사용하는 개발 환경 설정

## 중요 참고 사항

- 파일이 심볼릭 링크인지 확인하려면 `ls -l`을 사용합니다. 심볼릭 링크는 `->`로 표시됩니다.
- 하드 링크의 아이노드 번호를 확인하려면 `ls -i`를 사용합니다.
- 순환 참조가 생성되지 않도록 심볼릭 링크를 사용할 때 주의하십시오.
- 하드 링크는 동일한 아이노드와 디스크 공간을 공유합니다.
- 심볼릭 링크는 경로 정보만 저장하므로 디스크 공간을 거의 사용하지 않습니다.

`ln` 명령어는 효율적인 파일 시스템 구성에 필수적이며 시스템 관리 및 개발 작업 흐름에서 널리 사용됩니다.

자세한 내용은 다음 매뉴얼을 확인하십시오.

```bash
man ln
```

# `systemctl` 명령어

`systemctl` 명령어는 Linux에서 systemd 서비스와 systemd 시스템 및 서비스 관리자를 제어하고 관리하는 데 사용됩니다. 최신 Linux 배포판에서 서비스를 관리하는 기본 도구입니다.

## 구문(Syntax)

```
systemctl [options] command [service-name]
```

## Common Commands

### Service Management

```
start [service]         Start a service
stop [service]          Stop a service
restart [service]       Restart a service
reload [service]        Reload service configuration
status [service]        Show service status
enable [service]        Enable service to start at boot
disable [service]       Disable service from starting at boot
```

### System Commands

```
reboot                  Restart the system
poweroff               Shutdown the system
suspend                Suspend the system
hibernate              Hibernate the system
```
## 옵션

자주 사용하는 옵션 플래그는 다음과 같습니다.

```
-l          Show full output (don't truncate)
--no-pager  Don't pipe output into a pager
--failed    Show only failed units
--all       Show all units, including inactive ones
-q          Quiet mode, suppress output
-t          Specify unit type (service, socket, etc.)
```

## 예제

1. 서비스 시작

```bash
systemctl start nginx
```

2. 서비스 중지

```bash
systemctl stop apache2
```

3. 서비스 상태 확인

```bash
systemctl status ssh
```

4. 부팅 시 서비스가 시작되도록 활성화

```bash
systemctl enable mysql
```

5. 부팅 시 서비스가 시작되지 않도록 비활성화

```bash
systemctl disable bluetooth
```

6. 서비스 재시작

```bash
systemctl restart networking
```

7. 서비스를 중지하지 않고 서비스 구성 다시 로드

```bash
systemctl reload nginx
```

8. 활성 상태인 모든 서비스 나열

```bash
systemctl list-units --type=service
```

9. 모든 서비스 나열(활성 및 비활성 포함)

```bash
systemctl list-units --type=service --all
```

10. 실패한 서비스 나열

```bash
systemctl --failed
```

11. 서비스 종속성 표시

```bash
systemctl list-dependencies nginx
```

12. 서비스가 활성화되어 있는지 확인

```bash
systemctl is-enabled ssh
```

13. 서비스가 실행 중인지 확인

```bash
systemctl is-active mysql
```

14. 시스템 재시작

```bash
systemctl reboot
```

15. 시스템 종료

```bash
systemctl poweroff
```

## 서비스 상태 정보

상태를 확인하면 다음과 같은 정보를 볼 수 있습니다.

- **Active (running)**: 서비스가 현재 실행 중임
- **Active (exited)**: 서비스가 성공적으로 완료됨
- **Inactive (dead)**: 서비스가 실행 중이 아님
- **Failed**: 서비스 시작에 실패함

## 사용 사례

- 웹 서버 관리(nginx, apache)
- 데이터베이스 서비스 제어(mysql, postgresql)
- 시스템 서비스 관리(ssh, networking)
- 서비스 문제 해결
- 스크립트에서 서비스 관리 자동화
- 시스템 관리 및 유지보수

## 중요 참고 사항

- 대부분의 작업에는 root 권한이 필요합니다(`sudo` 사용).
- systemd 용어에서 서비스는 "unit"이라고 합니다.
- 구성 파일은 `/etc/systemd/system/`에 있습니다.
- 변경한 후에는 항상 서비스 상태를 확인하세요.
- 자세한 서비스 로그를 보려면 `journalctl`을 사용하세요.

`systemctl` 명령은 최신 Linux 시스템 관리에 필수적입니다.

자세한 내용은 매뉴얼을 확인하세요: `man systemctl`

# `journalctl` 명령

`journalctl` 명령은 구조화되고 색인된 형식으로 시스템 로그를 수집하고 저장하는 systemd 저널을 조회하고 검색하는 데 사용됩니다. 최신 Linux 배포판에서 시스템 로그를 확인할 때 사용하는 기본 도구입니다.

## 구문

```
journalctl [options] [matches]
```

## 옵션

자주 사용하는 옵션 플래그는 다음과 같습니다.

```
-f          Follow journal (like tail -f)
-u [unit]   Show logs for specific unit/service
-p [level]  Filter by priority level (0-7)
-S [time]   Show entries since specified time
-U [time]   Show entries until specified time
-b          Show logs from current boot
-k          Show kernel messages only
-r          Reverse output (newest first)
-n [lines]  Show last N lines
--no-pager  Don't pipe output to pager
-x          Add explanatory help texts
-o [format] Output format (json, short, verbose, etc.)
--disk-usage Show current disk usage
--vacuum-size=[size] Remove logs to reduce size
--vacuum-time=[time] Remove logs older than time
```

## 우선순위 수준

```
0  Emergency (emerg)
1  Alert (alert)
2  Critical (crit)
3  Error (err)
4  Warning (warning)
5  Notice (notice)
6  Informational (info)
7  Debug (debug)
```

## 예제

1. 모든 저널 항목 보기

```bash
journalctl
```

2. 실시간 저널 항목 추적

```bash
journalctl -f
```

3. 특정 서비스의 로그 보기

```bash
journalctl -u nginx
```

4. 마지막 부팅 이후의 로그 보기

```bash
journalctl -b
```

5. 이전 부팅의 로그 보기

```bash
journalctl -b -1
```

6. 커널 메시지 보기

```bash
journalctl -k
```

7. 특정 시점 이후의 로그 보기

```bash
journalctl --since "2024-01-01 00:00:00"
```

8. 지난 1시간의 로그 보기

```bash
journalctl --since "1 hour ago"
```

9. 특정 시간 구간의 로그 보기

```bash
journalctl --since "2024-01-01" --until "2024-01-02"
```

10. 오류 및 치명적 메시지만 보기

```bash
journalctl -p err
```

11. 마지막 50줄 보기

```bash
journalctl -n 50
```

12. 특정 서비스의 로그 추적

```bash
journalctl -u ssh -f
```

13. JSON 형식으로 로그 보기

```bash
journalctl -o json
```

14. 디스크 사용량 보기

```bash
journalctl --disk-usage
```

15. 공간 확보를 위해 오래된 로그 삭제

```bash
journalctl --vacuum-size=100M
```

16. 2주보다 오래된 로그 삭제

```bash
journalctl --vacuum-time=2weeks
```

17. 설명과 함께 로그 보기

```bash
journalctl -x
```

18. 특정 프로세스 ID의 로그 보기

```bash
journalctl _PID=1234
```

19. 특정 사용자의 로그 보기

```bash
journalctl _UID=1000
```

20. 로그를 역순으로 보기

```bash
journalctl -r
```

## 시간 지정

다양한 시간 형식을 사용할 수 있습니다.

- `"2024-01-01 12:00:00"`
- `"yesterday"`
- `"today"`
- `"1 hour ago"`
- `"30 minutes ago"`
- `"2 days ago"`

## 출력 형식

```
short      Default format
verbose    All available fields
json       JSON format
json-pretty Pretty-printed JSON
export     Binary export format
cat        Very short format
```

## 사용 사례

- 시스템 문제 해결
- 서비스 동작 모니터링
- 보안 감사
- 성능 분석
- 시스템 문제 디버깅
- 사용자 활동 추적

## 중요 참고 사항

- 저널 파일은 `/var/log/journal/` 또는 `/run/log/journal/`에 저장됩니다.
- 시스템 로그를 보려면 적절한 권한이 필요합니다.
- 시간이 지나면 디스크 공간을 상당히 차지할 수 있습니다.
- 로그 크기를 관리하려면 vacuum 옵션을 사용하세요.
- 영구 로깅을 사용하려면 적절한 구성이 필요합니다.

`journalctl` 명령은 systemd 기반 Linux 배포판에서 시스템 관리와 문제 해결에 필수적입니다.

자세한 내용은 매뉴얼을 확인하세요: `man journalctl`

# `watch` 명령

`watch` 명령은 일정한 간격으로 명령을 반복 실행하고 그 출력을 표시하는 데 사용됩니다. 시스템 상태, 파일 내용 또는 시간에 따른 명령 출력의 변화를 모니터링할 때 특히 유용합니다.

## 구문

```
watch [options] command
```

## 옵션

자주 사용하는 옵션 플래그는 다음과 같습니다.

```
-n [seconds]    Set update interval (default is 2 seconds)
-d              Highlight differences between updates
-t              Turn off header showing interval and command
-b              Beep if command has non-zero exit status
-e              Exit on error (non-zero exit status)
-g              Exit when output changes
-c              Interpret ANSI color sequences
-x              Pass command to shell with exec
-p              Precise timing mode
```

## 예제

1. 2초마다 시스템 가동 시간 확인(기본값)

```bash
watch uptime
```

2. 사용자 지정 간격으로 디스크 공간 확인

```bash
watch -n 5 df -h
```

3. 차이를 강조하여 메모리 사용량 모니터링

```bash
watch -d free -h
```

4. 네트워크 연결 확인

```bash
watch -n 1 'netstat -tuln'
```

5. 특정 디렉터리 내용 모니터링

```bash
watch 'ls -la /var/log'
```

6. CPU 정보 확인

```bash
watch -n 2 'cat /proc/cpuinfo | grep "cpu MHz"'
```

7. 활성 프로세스 모니터링

```bash
watch -d 'ps aux | head -20'
```

8. 파일 크기 변화 확인

```bash
watch -n 1 'ls -lh /var/log/syslog'
```

9. 시스템 부하 모니터링

```bash
watch -n 3 'cat /proc/loadavg'
```

10. 정확한 타이밍으로 확인

```bash
watch -p -n 0.5 date
```

11. 서비스 상태 모니터링

```bash
watch 'systemctl status nginx'
```

12. 색상 지원을 사용하여 확인

```bash
watch -c 'ls --color=always'
```

13. 출력이 변경되면 종료

```bash
watch -g 'cat /tmp/status.txt'
```

14. 오류 발생 시 경고음과 함께 확인

```bash
watch -b 'ping -c 1 google.com'
```

15. 로그 파일 크기 모니터링

```bash
watch 'wc -l /var/log/messages'
```

16. Docker 컨테이너 확인

```bash
watch 'docker ps'
```

17. 온도 센서 모니터링

```bash
watch -n 2 sensors
```

18. Git 상태 확인

```bash
watch -d 'git status --porcelain'
```

19. 대역폭 사용량 모니터링

```bash
watch -n 1 'cat /proc/net/dev'
```

20. 헤더 없이 확인

```bash
watch -t 'date'
```

## 사용 사례

- 시스템 모니터링 및 성능 분석
- 로그 파일 변화 확인
- 네트워크 연결 상태 모니터링
- 파일 시스템 변화 추적
- 프로세스 동작 관찰
- 시스템 문제 디버깅
- 자동화 및 스크립팅
- 실시간 상태 모니터링

## 주요 기능

- **실시간 업데이트**: 출력을 지속적으로 새로 고침
- **차이 강조**: 업데이트 간 변경된 내용을 표시
- **유연한 간격**: 업데이트 빈도 사용자 지정
- **종료 조건**: 변경 또는 오류 발생 시 종료 가능
- **헤더 정보**: 명령과 업데이트 간격 표시

## 중요 참고 사항

- `Ctrl+C`를 눌러 watch를 종료하세요.
- 파이프나 리디렉션이 포함된 복잡한 명령은 따옴표로 감싸세요.
- 명령은 실행할 때마다 서브셸에서 실행됩니다.
- 리소스를 많이 사용하는 명령을 짧은 간격으로 실행할 때는 주의하세요.
- 화면은 업데이트할 때마다 지워지고 새로 고쳐집니다.
- 헤더에는 마지막 업데이트 시간과 간격이 표시됩니다.

## 팁

- 변경 사항을 쉽게 확인하려면 `-d` 옵션을 사용하세요.
- 출력 결과를 필터링하려면 `grep`과 함께 사용하세요.
- 중요도가 낮은 모니터링에는 더 긴 간격을 사용하세요.
- 간격을 너무 짧게 설정할 때는 시스템 부하를 고려하세요.

`watch` 명령어는 실시간으로 변화를 모니터링해야 하는 시스템 관리자와 개발자에게 필수적인 도구입니다.

자세한 내용은 매뉴얼을 확인하세요: `man watch`

# `jobs` 명령어

`jobs` 명령어는 현재 셸 세션에서 활성화된 작업에 대한 정보를 표시합니다. 작업이란 셸에서 시작되었으며 작업 제어 명령어를 사용해 관리할 수 있는 프로세스를 의미합니다.

## 구문

```bash
jobs [options] [job_spec]
```

## 옵션

자주 사용하는 옵션은 다음과 같습니다.

```
-l      List process IDs along with job information
-p      List only process IDs
-n      List only jobs that have changed status since last notification
-r      List only running jobs
-s      List only stopped jobs
-x      Replace job specifications with process IDs in command
```

## 작업 상태

작업은 다음과 같은 다양한 상태가 될 수 있습니다.

- **실행 중(Running)**: 작업이 현재 실행되고 있습니다.
- **중지됨(Stopped)**: 작업이 일시 중지된 상태입니다.
- **완료됨(Done)**: 작업이 성공적으로 완료되었습니다.
- **종료됨(Terminated)**: 작업이 강제 종료되었거나 비정상적으로 끝났습니다.

## 예시

1. 현재 모든 작업 나열

```bash
jobs
```

2. 프로세스 ID와 함께 작업 나열

```bash
jobs -l
```

3. 프로세스 ID만 나열

```bash
jobs -p
```

4. 실행 중인 작업만 나열:

```bash
jobs -r
```

5. 중지된 작업만 나열:

```bash
jobs -s
```

6. 특정 작업의 상태 확인:

```bash
jobs %1
```

## Job Control Examples

1. 백그라운드 작업 시작

```bash
sleep 100 &
```

2. 여러 백그라운드 작업 시작

```bash
find / -name "*.log" > /tmp/logs.txt 2>/dev/null &
ping google.com > /tmp/ping.txt &
```

3. 전체 백그라운드 작업 목록 확인:

```bash
jobs
```

출력 예시:

```
[1]-  Running    find / -name "*.log" > /tmp/logs.txt 2>/dev/null &
[2]+  Running    ping google.com > /tmp/ping.txt &
```

4. 실행 중인 작업 일시 중지 (`Ctrl+Z`):

```bash
# Start a command
vim myfile.txt
# Press Ctrl+Z to stop it
# Then check jobs
jobs
```

5. 작업을 포그라운드로 가져오기:

```bash
fg %1
```

6. 작업을 백그라운드로 전송:

```bash
bg %1
```

7. 특정 작업 강제 종료:

```bash
kill %2
```

## 작업 지정

다음과 같은 형식으로 작업을 지정할 수 있습니다.

- `%1`: 작업 번호가 1인 작업
- `%+` 또는 `%%`: 현재 작업, 즉 가장 최근 작업
- `%-`: 이전 작업
- `%string`: 명령줄이 `string`으로 시작하는 작업
- `%?string`: 명령줄에 `string`이 포함된 작업

## 작업 제어 예제

1. 여러 작업을 시작하고 관리합니다.

```bash
# Start some background jobs
sleep 300 &
ping localhost > /dev/null &
find /usr -name "*.conf" > /tmp/configs.txt 2>/dev/null &

# List all jobs
jobs -l

# Bring first job to foreground
fg %1

# Put it back to background (after stopping with Ctrl+Z)
bg %1

# Kill second job
kill %2

# Check remaining jobs
jobs
```

2. 중지된 작업 다루기

```bash
# Start a text editor
nano myfile.txt

# Stop it with Ctrl+Z
# Check jobs
jobs

# Resume in background
bg

# Resume in foreground
fg
```

## 사용 사례

- **멀티태스킹**: 여러 명령어를 동시에 실행합니다.
- **장시간 실행 프로세스**: 완료하는 데 시간이 오래 걸리는 작업을 관리합니다.
- **백그라운드 처리**: 다른 작업을 수행하는 동안 작업을 실행합니다.
- **작업 모니터링**: 실행 중인 프로세스를 추적합니다.
- **프로세스 관리**: 셸 프로세스를 제어하고 정리합니다.

## 관련 명령어

- `fg`: 작업을 포그라운드로 가져옵니다.
- `bg`: 작업을 백그라운드로 보냅니다.
- `nohup`: 터미널 연결 종료의 영향을 받지 않고 명령어를 실행합니다.
- `disown`: 작업 테이블에서 작업을 제거합니다.
- `kill`: 작업 또는 프로세스를 종료합니다.

## 중요 참고 사항

- 작업은 현재 셸 세션에 종속됩니다.
- 작업 번호는 순차적으로 할당됩니다.
- 작업이 완료되거나 셸을 종료하면 작업 목록에서 사라집니다.
- 명령어 끝에 `&`를 사용하면 백그라운드에서 실행됩니다.
- 실행 중인 작업을 중지하려면 `Ctrl+Z`를 누르세요.
- 실행 중인 작업을 종료하려면 `Ctrl+C`를 누르세요.

## 고급 예제

1. 명령어를 백그라운드에서 실행한 후 작업 테이블에서 제거하기

```bash
long_running_script.sh &
disown %1
```

2. 완료된 작업 확인

```bash
jobs -n
```

3. 모든 작업 종료

```bash
kill $(jobs -p)
```

`jobs` 명령어는 셸에서 여러 프로세스를 관리하고 효과적인 작업 흐름 관리를 구현하는 데 필수적입니다.

더 자세한 내용은 `man jobs` 또는 `help jobs`를 참조하세요.

# `bg` 명령어

`bg` 명령어는 일시 중지된 작업을 백그라운드로 보내 다시 실행하는 데 사용됩니다. 이를 통해 해당 작업이 계속 실행되는 동안 터미널을 다른 작업에 사용할 수 있습니다. `bg`는 Unix 계열 셸의 작업 제어 기능에 포함되어 있습니다.

## 구문(Syntax)

```
bg [job_spec]
```

작업 지정(job specification)을 지정하지 않으면 `bg`는 현재 작업, 즉 가장 최근 작업에 적용됩니다.

## 작업 지정

다음과 같은 형식으로 작업을 지정할 수 있습니다.

- `%1`: 작업 번호가 1인 작업
- `%+` 또는 `%%`: 현재 작업, 즉 가장 최근 작업
- `%-`: 이전 작업
- `%string`: 명령줄이 `string`으로 시작하는 작업
- `%?string`: 명령줄에 `string`이 포함된 작업

## 예시

1. 현재 중지된 작업을 백그라운드로 보내기

```bash
bg
```

2. 특정 작업을 백그라운드로 보내기

```bash
bg %1
```

3. 여러 작업을 백그라운드로 보내기

```bash
bg %1 %2 %3
```

## 전체 작업 제어 과정

다음은 `bg` 사용 방법을 보여주는 일반적인 작업 흐름입니다.

1. 오래 실행되는 명령어를 시작합니다.


```bash
find / -name "*.log" > /tmp/findlogs.txt 2>/dev/null
```

2. Ctrl+Z로 작업 중지

```
^Z
[1]+  Stopped    find / -name "*.log" > /tmp/findlogs.txt 2>/dev/null
```

3. Check jobs

```bash
jobs
```

Output:

```
[1]+  Stopped    find / -name "*.log" > /tmp/findlogs.txt 2>/dev/null
```

4. 일시 중지된 작업을 백그라운드에서 재개:

```bash
bg %1
```

Output:

```
[1]+ find / -name "*.log" > /tmp/findlogs.txt 2>/dev/null &
```

5. 작업이 백그라운드에서 실행 중인지 확인:

```bash
jobs
```

Output:

```
[1]+  Running    find / -name "*.log" > /tmp/findlogs.txt 2>/dev/null &
```

## 실무 활용 예제

1. 텍스트 편집기 사용하기

```bash
# Start editing a file
vim myfile.txt

# Stop with Ctrl+Z
# Put it in background
bg

# Now you can run other commands while vim runs in background
ls -la

# Bring vim back to foreground when needed
fg %1
```

2. 여러 백그라운드 작업 관리하기

```bash
# Start several tasks and stop them
ping google.com > /tmp/ping1.txt
# Ctrl+Z
sleep 300
# Ctrl+Z
tar czf backup.tar.gz /home/user/documents
# Ctrl+Z

# Check all stopped jobs
jobs

# Put all in background
bg %1
bg %2
bg %3

# Or put specific ones
bg %ping    # Job starting with "ping"
```

3. 명령어를 백그라운드에서 바로 시작 vs bg 사용

```bash
# Method 1: Start directly in background
find /usr -name "*.conf" > /tmp/configs.txt &

# Method 2: Start normally, stop, then background
find /usr -name "*.conf" > /tmp/configs.txt
# Ctrl+Z
bg
```

## Related Commands

- `fg` - 작업을 포그라운드로 가져옵니다
- `jobs` - 활성 작업 목록을 표시합니다
- `kill` - 작업을 종료합니다
- `nohup` - 접속 끊김(hangup)에 영향을 받지 않고 명령을 실행합니다
- `disown` - 작업 테이블에서 작업을 제거합니다

## Use Cases

- **멀티태스킹**: 여러 작업을 동시에 실행
- **장시간 실행 프로세스**: 시간이 오래 걸리는 작업을 실행해 두고 다른 작업을 진행
- **대화형 프로그램**: 편집기나 대화형 도구를 일시적으로 백그라운드로 전환
- **개발**: 코딩하는 동안 컴파일을 백그라운드에서 실행
- **시스템 관리**: 다른 작업을 수행하는 동안 모니터링을 백그라운드에서 실행

## 중요 참고 사항

- `bg`로 백그라운드에 넣은 작업은 여전히 터미널에 연결되어 있습니다
- 터미널을 닫으면 백그라운드 작업이 종료될 수 있습니다
- 지속적인 백그라운드 프로세스에는 `nohup`이나 `disown`을 사용하세요
- 백그라운드 작업은 stdin(키보드 입력)을 읽을 수 없습니다
- `fg`를 사용하면 백그라운드 작업을 다시 포그라운드로 가져올 수 있습니다
- 리다이렉트하지 않는 한 백그라운드 작업은 계속 stdout/stderr에 출력합니다

## Error Handling

`bg` 실행 실패 시 주요 원인:

- 작업이 존재하지 않음
- 작업이 이미 실행 중임
- 작업을 백그라운드로 보낼 수 없음(일부 대화형 프로그램)

## Tips

- `bg`를 사용하기 전후로 항상 `jobs`로 작업 상태를 확인하세요
- 터미널이 어수선해지지 않도록 백그라운드 작업의 출력을 리다이렉트하세요
- 시스템 자원 문제를 피하기 위해 작업 제어를 신중하게 사용하세요
- 지속적인 세션에는 `screen`이나 `tmux` 같은 터미널 멀티플렉서 사용을 고려하세요

`bg` 명령어는 셸 환경에서 효과적인 멀티태스킹과 작업 관리에 필수적입니다.

더 자세한 내용은 `help bg` 매뉴얼을 참조하세요.

# `fg` 명령어

`fg` 명령어는 백그라운드 또는 중단된 작업을 포그라운드로 가져와 터미널의 활성 프로세스로 만드는 데 사용됩니다. Unix 계열 셸에서 작업 제어의 필수적인 부분입니다.

## 구문(Syntax)

```
fg [job_spec]
```

작업 번호를 지정하지 않으면 `fg`는 가장 최근에 중지된 현재 작업에 동작합니다.

## Job Specifications

작업(Job)을 지정할 때 다음과 같은 형식을 사용할 수 있습니다.

- `%1` - 1번 작업
- `%+` 또는 `%%` - 현재 작업(가장 최근)
- `%-` - 이전 작업
- `%string` - 명령줄이 string으로 시작하는 작업
- `%?string` - 명령줄에 string이 포함된 작업

## 예시

1. 현재 작업을 포그라운드로 가져오기

```bash
fg
```

2. 특정 작업을 포그라운드로 가져오기

```bash
fg %1
```

3. 부분 명령어 이름으로 작업 가져오기

```bash
fg %vim
```

4. 특정 문자열을 포함하는 작업을 포그라운드로 전환:

```bash
fg %?backup
```

## Complete Job Control Workflow

`fg` 명령어를 활용하는 전형적인 작업 흐름:

1. 백그라운드 작업 시작

```bash
ping google.com > /tmp/ping.txt &
```

2. 다른 작업 시작 후 중지하기

```bash
vim myfile.txt
# Press Ctrl+Z to stop
```

3. 현재 작업 확인

```bash
jobs
```

Output:

```
[1]-  Running    ping google.com > /tmp/ping.txt &
[2]+  Stopped    vim myfile.txt
```

4. vim 작업을 포그라운드로 전환:

```bash
fg %2
```

5. vim에서 작업하다가 다시 중지(Ctrl+Z)하고 ping을 포그라운드로 가져오기

```bash
fg %1
```

## 실무 활용 예제

1. 편집기 사용하기

```bash
# Start editing
nano config.txt
# Stop with Ctrl+Z
# Do other work
ls -la
# Return to editor
fg
```

2. 여러 개발 작업 관리하기

```bash
# Start compilation in background
make all > build.log 2>&1 &

# Start editing source code
vim main.c
# Stop editor (Ctrl+Z)

# Check build progress
fg %make
# Stop build monitoring (Ctrl+Z)

# Return to editing
fg %vim
```

3. 대화형 디버깅 세션

```bash
# Start debugger
gdb ./myprogram
# Stop debugger (Ctrl+Z)

# Check core dumps or logs
ls -la core.*

# Return to debugger
fg %gdb
```

4. 여러 터미널 및 세션 간 작업 관리:

```bash
# Start SSH session
ssh user@remote-server
# Stop SSH (Ctrl+Z)

# Do local work
ps aux | grep myprocess

# Return to SSH session
fg %ssh
```

## 고급 사용법

1. 여러 중지된 작업 간 전환

```bash
# Start several editors
vim file1.txt
# Ctrl+Z
vim file2.txt
# Ctrl+Z
nano file3.txt
# Ctrl+Z

# Check all jobs
jobs

# Switch between them
fg %1    # vim file1.txt
# Ctrl+Z
fg %2    # vim file2.txt
# Ctrl+Z
fg %3    # nano file3.txt
```

2. 스크립트에서 작업 제어와 함께 사용하기

```bash
#!/bin/bash
# Start background monitoring
tail -f /var/log/syslog &
MONITOR_PID=$!

# Do main work
./main_script.sh

# Bring monitor to foreground for review
fg %tail

# Or kill it
kill $MONITOR_PID
```

## Related Commands

- `bg` - 작업을 백그라운드로 보냅니다
- `jobs` - 활성 작업 목록을 표시합니다
- `kill` - 작업을 종료합니다
- `Ctrl+Z` - 현재 작업을 중지(일시 중단)합니다
- `Ctrl+C` - 현재 작업을 종료합니다

## Use Cases

- **코드 편집**: 열어 둔 여러 편집기 사이를 전환
- **개발**: 컴파일과 편집을 번갈아 수행
- **시스템 모니터링**: 모니터링 도구 사이를 전환
- **원격 세션**: SSH 등 원격 연결 재개
- **대화형 프로그램**: 일시 중지한 대화형 애플리케이션으로 돌아가기
- **디버깅**: 디버거 세션 재개

## 중요 참고 사항

- 작업을 포그라운드로 가져오면 활성 프로세스가 됩니다
- 한 번에 하나의 포그라운드 작업만 가질 수 있습니다
- 포그라운드 작업은 키보드 입력을 받을 수 있습니다
- Ctrl+Z로 포그라운드 작업을 중지(일시 중단)할 수 있습니다
- Ctrl+C로 포그라운드 작업을 종료할 수 있습니다
- 백그라운드 작업은 포그라운드에 없어도 계속 실행됩니다

## Error Handling

`fg` 사용 시 자주 발생하는 문제:

- 작업이 존재하지 않음: `fg: %3: no such job`
- 사용 가능한 작업 없음: `fg: no current job`
- 작업이 이미 포그라운드에 있음

## Tips for Effective 사용법

1. **작업 번호 사용**: 부분 이름보다 더 안정적입니다
2. **먼저 jobs 확인**: 현재 상태를 보려면 항상 `jobs`를 실행하세요
3. **일관된 워크플로**: 작업 전환 루틴을 만들어 두세요
4. **출력 리다이렉트**: 간섭을 피하도록 백그라운드 작업의 출력을 리다이렉트하세요

```bash
# Good practice
tail -f /var/log/messages > monitor.out 2>&1 &
vim script.sh
# Ctrl+Z
fg %tail  # Review logs
# Ctrl+Z
fg %vim   # Continue editing
```

## Other Tools와의 통합

`fg` 명령어는 다음과 함께 유용하게 쓰입니다.

- **터미널 멀티플렉서**: `screen`, `tmux`
- **개발 환경**: IDE, 편집기
- **시스템 모니터링**: `top`, `htop`, `tail`
- **네트워크 도구**: `ssh`, `ping`, `netstat`

`fg` 명령어는 터미널에서 효율적인 멀티태스킹에 매우 중요하며 여러 작업 간 원활한 전환을 가능하게 합니다.

더 자세한 내용은 `help fg` 매뉴얼을 참조하세요.

# `time` 명령어

`time` 명령어는 프로그램과 명령의 실행 시간을 측정하는 데 사용됩니다. 사용자 시간, 시스템 시간, 실제(real, wall-clock) 시간 등 명령 실행에 걸리는 시간에 대한 상세 정보를 제공합니다.

## 구문(Syntax)

```
time [options] command [arguments]
```

## Types of Time Measurement

### Real Time (Wall-clock time)

- 시작부터 끝까지의 총 경과 시간
- I/O나 다른 프로세스 대기 등에 소요된 시간 포함

### User Time

- 사용자 수준 코드를 실행하는 데 소요된 시간
- 프로세스 자체가 사용한 CPU 시간

### System Time

- 커널 모드에서 소요된 시간
- 시스템 콜에 사용된 CPU 시간

## Output Format

표준 출력으로 다음 세 가지 측정값이 표시됩니다.

```
real    0m2.345s
user    0m1.234s
sys     0m0.567s
```

## 옵션

자주 쓰이는 주요 옵션 플래그는 다음과 같습니다.

```
-p          Use POSIX format output
-f format   Use custom format string
-o file     Write output to file instead of stderr
-a          Append to output file instead of overwriting
-v          Verbose output with detailed statistics
```

## 예시

1. 간단한 명령어 실행 시간 측정

```bash
time ls -la
```

2. 스크립트 실행 시간 측정

```bash
time ./my_script.sh
```

3. 컴파일 과정 소요 시간 측정:

```bash
time make all
```

4. POSIX 표준 형식으로 시간 측정:

```bash
time -p find /usr -name "*.txt"
```

5. 시간 측정 결과를 파일로 저장:

```bash
time -o timing.log -a make clean && make
```

6. 상세 시간 측정 정보 출력:

```bash
time -v python large_calculation.py
```

## 고급 사용법

1. 여러 명령어 실행 시간 측정

```bash
time (command1 && command2 && command3)
```

2. 사용자 지정 형식으로 시간 측정

```bash
/usr/bin/time -f "Time: %E, Memory: %M KB" ./memory_intensive_program
```

3. 실행 시간 측정 및 출력 리디렉션:

```bash
time (find /usr -name "*.log" > found_logs.txt 2>&1)
```

4. 실행 시간 비교 측정:

```bash
echo "Method 1:"
time method1_script.sh

echo "Method 2:"
time method2_script.sh
```

## Using GNU time (Advanced)

GNU 버전의 `time`(`/usr/bin/time`)은 더욱 상세한 실행 통계를 제공합니다.

```bash
/usr/bin/time -v command
```

다음과 같은 추가 통계가 함께 표시됩니다.

- 최대 상주 집합 크기(메모리 사용량)
- 페이지 폴트(page fault)
- 컨텍스트 스위치(context switch)
- 파일 시스템 입출력

## Format Specifiers for GNU time

```
%E    Elapsed real time (wall clock time)
%U    User CPU time
%S    System CPU time
%M    Maximum resident set size (KB)
%P    Percentage of CPU used
%X    Average size of shared text (KB)
%D    Average size of unshared data (KB)
%c    Number of voluntary context switches
%w    Number of involuntary context switches
%I    Number of file system inputs
%O    Number of file system outputs
```

## 실무 활용 예제

1. Python 스크립트 프로파일링

```bash
time python -c "
import time
for i in range(1000000):
    str(i)
"
```

2. 서로 다른 알고리즘 비교

```bash
echo "Bubble sort:"
time ./bubble_sort < large_dataset.txt

echo "Quick sort:"
time ./quick_sort < large_dataset.txt
```

3. 데이터베이스 작업 소요 시간 측정:

```bash
time mysql -u user -p database < complex_query.sql
```

4. 네트워크 작업 소요 시간 측정:

```bash
time wget https://large-file.example.com/bigfile.zip
```

5. 압축 작업 소요 시간 측정:

```bash
echo "gzip compression:"
time gzip -c large_file.txt > large_file.gz

echo "bzip2 compression:"
time bzip2 -c large_file.txt > large_file.bz2
```

6. 빌드 프로세스 성능 프로파일링:

```bash
echo "Clean build timing:"
time (make clean && make -j4)
```
## 출력 이해하기

출력 예시:

```text
real    0m5.234s    # 총 경과 시간(5.234초)
user    0m3.456s    # 사용자 모드에서 사용한 CPU 시간(3.456초)
sys     0m0.789s    # 시스템 모드에서 사용한 CPU 시간(0.789초)
```

**분석:**

- `real` > `user` + `sys`: 프로세스가 I/O 작업에 묶여 있거나 대기 중이었습니다.
- `real` ≈ `user` + `sys`: 프로세스가 CPU 작업에 묶여 있었습니다.
- `user` >> `sys`: 프로세스가 대부분의 시간을 사용자 코드에서 사용했습니다.
- `sys` >> `user`: 프로세스가 많은 시스템 호출을 수행했습니다.

## 벤치마킹 모범 사례

1. **여러 번 실행:** 여러 번 실행한 후 결과의 평균을 계산합니다.

```bash
for i in {1..5}; do
    echo "실행 $i:"
    time ./program
done
```

2. **워밍업 실행:** 캐시를 워밍업하기 위해 몇 번 실행합니다.

```bash
# 워밍업
./program > /dev/null 2>&1


# 실제 시간 측정
time ./program
```

3. **일관된 환경:** 측정 환경을 통제합니다.

```bash
# 캐시 삭제
sync && echo 3 > /proc/sys/vm/drop_caches


# 일관된 우선순위로 실행
nice -n 0 time ./program
```

## 사용 사례

- **성능 최적화:** 느린 작업을 식별합니다.
- **벤치마킹:** 서로 다른 구현을 비교합니다.
- **시스템 분석:** 리소스 사용 패턴을 파악합니다.
- **빌드 최적화:** 컴파일에 걸리는 시간을 측정합니다.
- **스크립트 프로파일링:** 셸 스크립트의 병목 지점을 찾습니다.
- **개발:** 알고리즘의 효율성을 측정합니다.

## 중요 참고 사항

- 셸 내장 `time`과 `/usr/bin/time`은 기능이 다를 수 있습니다.
- 시스템 부하 때문에 실행할 때마다 결과가 달라질 수 있습니다.
- I/O 작업은 측정 시간에 큰 영향을 줄 수 있습니다.
- 정확한 벤치마킹을 위해 여러 번 측정하십시오.
- 파일 작업 시간을 측정할 때는 시스템 캐시를 고려하십시오.

## 다른 도구와 함께 사용하기

1. `nice`와 함께 우선순위를 제어합니다.

```bash
time nice -n 10 ./cpu_intensive_task
```

2. `timeout`과 함께 최대 실행 시간을 제한합니다.

```bash
time timeout 30s ./potentially_slow_command
```

3. `strace`와 함께 시스템 호출을 분석합니다.

```bash
time strace -c ./program 2> syscalls.log
```

`time` 명령어는 Linux 시스템에서 성능을 분석하고 최적화하며 프로그램의 동작을 파악하는 데 필수적입니다.

자세한 내용은 다음 매뉴얼을 확인하십시오.

```bash
man time
```

# `export` 명령어

`export` 명령어는 자식 프로세스에서 사용할 수 있는 환경 변수를 설정할 때 사용합니다. 현재 셸 세션에서 시작되는 모든 프로세스가 해당 변수를 사용할 수 있도록 합니다.

## 구문(Syntax)

```bash
export [options] [variable[=value]]
export [options] [name[=value] ...]
```

## 환경 변수의 작동 방식

- **지역 변수:** 현재 셸에서만 사용할 수 있습니다.
- **환경 변수:** 현재 셸과 모든 자식 프로세스에서 사용할 수 있습니다.
- `export`: 지역 변수를 환경 변수로 변환합니다.

## 옵션

일반적으로 사용하는 옵션 플래그는 다음과 같습니다.

```text
-f    변수 대신 함수를 내보냅니다.
-n    변수를 환경에서 제거합니다. (export 해제)
-p    내보낸 모든 변수를 표시합니다.
```

## 예시

1. 간단한 변수를 내보냅니다.

```bash
export MY_VAR="Hello World"
```

2. 여러 변수를 한 번에 내보냅니다.

```bash
export VAR1="value1" VAR2="value2" VAR3="value3"
```

3. 기존 지역 변수를 내보냅니다.

```bash
LOCAL_VAR="test"
export LOCAL_VAR
```

4. 내보낸 모든 변수를 표시합니다.

```bash
export -p
```

5. `PATH`를 수정하여 내보냅니다.

```bash
export PATH="$PATH:/usr/local/bin"
```

6. 명령어 치환과 함께 내보냅니다.

```bash
export CURRENT_DATE=$(date)
export HOSTNAME=$(hostname)
```

7. 변수의 export를 해제하여 환경에서 제거합니다.

```bash
export -n MY_VAR
```

8. 함수를 내보냅니다.

```bash
my_function() {
    echo "함수에서 보내는 인사말"
}
export -f my_function
```

## 일반적인 환경 변수

1. **`PATH`** 실행 파일 검색 경로

```bash
export PATH="/usr/local/bin:$PATH"
```

2. **`HOME`** 사용자의 홈 디렉터리

```bash
export HOME="/home/username"
```

3. **`EDITOR`** 기본 텍스트 편집기

```bash
export EDITOR="vim"
export VISUAL="code"
```

4. **`LANG`** 시스템 언어 및 로케일

```bash
export LANG="en_US.UTF-8"
```

5. **`PS1`** 기본 프롬프트 문자열

```bash
export PS1="\u@\h:\w\$ "
```

6. **`JAVA_HOME`** Java 설치 디렉터리

```bash
export JAVA_HOME="/usr/lib/jvm/java-11-openjdk"
```

7. **`NODE_ENV`** Node.js 환경

```bash
export NODE_ENV="production"
```

## 개발 환경 예시

1. **Python 개발**

```bash
export PYTHONPATH="$PYTHONPATH:/path/to/modules"
export VIRTUAL_ENV="/path/to/venv"
```

2. **Node.js 개발**

```bash
export NODE_PATH="/usr/local/lib/node_modules"
export NPM_CONFIG_PREFIX="$HOME/.npm-global"
```

3. **Go 개발**

```bash
export GOPATH="$HOME/go"
export GOROOT="/usr/local/go"
export PATH="$PATH:$GOROOT/bin:$GOPATH/bin"
```

4. **데이터베이스 설정**

```bash
export DB_HOST="localhost"
export DB_PORT="5432"
export DB_NAME="myapp"
export DB_USER="dbuser"
```

## 셸 설정 파일

설정 파일에 export 문을 추가하면 내보내기를 영구적으로 유지할 수 있습니다.

1. **Bash** `~/.bashrc` 또는 `~/.bash_profile`

```bash
echo 'export MY_VAR="permanent_value"' >> ~/.bashrc
```

2. **Zsh** `~/.zshrc`

```bash
echo 'export MY_VAR="permanent_value"' >> ~/.zshrc
```

3. **시스템 전체** `/etc/environment` 또는 `/etc/profile`

```bash
# /etc/environment
MY_GLOBAL_VAR="system_wide_value"
```

## 변수 확인

1. 변수가 export되었는지 확인합니다.

```bash
env | grep MY_VAR
printenv MY_VAR
echo $MY_VAR
```

2. 변수의 범위를 확인합니다.

```bash
# 지역 변수
MY_LOCAL="test"
bash -c 'echo $MY_LOCAL'  # 빈 출력


# 내보낸 변수
export MY_EXPORTED="test"
bash -c 'echo $MY_EXPORTED'  # "test" 출력
```

## 고급 사용법

1. **조건부 export**

```bash
if [ -d "/opt/myapp" ]; then
    export MYAPP_HOME="/opt/myapp"
fi
```

2. **기본값과 함께 export**

```bash
export EDITOR="${EDITOR:-vim}"
export PORT="${PORT:-3000}"
```

3. **배열 export(Bash 4 이상)**

```bash
declare -a my_array=("item1" "item2" "item3")
export my_array
```

4. **검증과 함께 export**

```bash
validate_and_export() {
    if [ -n "$1" ] && [ -n "$2" ]; then
        export "$1"="$2"
        echo "$1=$2를 export했습니다."
    else
        echo "오류: 잘못된 인수입니다."
    fi
}


validate_and_export "API_KEY" "your-secret-key"
```

## 사용 사례

- **개발 환경:** 프로그래밍 언어별 경로를 설정합니다.
- **애플리케이션 설정:** 데이터베이스 URL, API 키 및 기능 플래그를 설정합니다.
- **시스템 관리:** 사용자 지정 `PATH` 수정 및 프록시 설정에 사용합니다.
- **CI/CD 파이프라인:** 빌드 설정 및 배포 대상을 지정합니다.
- **보안:** 스크립트에 직접 포함해서는 안 되는 민감한 데이터를 전달합니다.

## 중요 참고 사항

- export된 변수는 자식 프로세스에 상속됩니다.
- 자식 프로세스에서 export된 변수를 변경해도 부모 프로세스에는 영향을 주지 않습니다.
- 공백이나 특수 문자가 포함된 값에는 따옴표를 사용하십시오.
- 환경 변수는 일반적으로 대문자로 작성합니다.
- 환경 변수에 민감한 데이터를 저장할 때 주의하십시오.
- `PATH`와 같은 일부 변수는 기존 값을 대체하지 말고 뒤에 추가하는 것이 좋습니다.

## 보안 고려 사항

1. **export에 민감한 데이터를 직접 포함하지 않기**

```bash
# 잘못된 방법
export PASSWORD="secret123"


# 더 나은 방법 - 보안 파일에서 읽거나 입력받기
read -s -p "비밀번호를 입력하십시오: " PASSWORD
export PASSWORD
```

2. **민감한 작업에는 임시 export 사용**

```bash
# 임시로 export
export TEMP_TOKEN="secret"
my_command_that_needs_token
unset TEMP_TOKEN  # 정리
```

`export` 명령어는 셸 스크립팅과 시스템 관리의 기본 요소로, 애플리케이션과 프로세스가 올바른 환경 설정을 사용할 수 있도록 합니다.

자세한 내용은 다음을 확인하십시오.

```bash
help export
```

또는

```bash
man bash
```
# `ufw` 명령어

UFW(Uncomplicated Firewall)는 Ubuntu 및 기타 Debian 기반 시스템에서 `iptables` 방화벽 규칙을 관리하기 위한 사용자 친화적인 명령줄 프런트엔드입니다. 복잡한 `iptables` 구문을 직접 다루지 않고도 방화벽 규칙을 간단하게 설정할 수 있습니다. [webdir.tistory](https://webdir.tistory.com/206)

## 구문(Syntax)

```bash
ufw [options] command [parameters]
```

## 설치

```bash
# Ubuntu/Debian
sudo apt update && sudo apt install ufw


# UFW 설치 여부 확인
which ufw
```

## 기본 명령어

### UFW 활성화 및 비활성화

```bash
# UFW 활성화
sudo ufw enable


# UFW 비활성화
sudo ufw disable


# UFW 상태 확인
sudo ufw status
sudo ufw status verbose
sudo ufw status numbered
```

## 기본 규칙

### 트래픽 허용 및 거부

1. **특정 포트 허용**

```bash
# SSH 허용 (포트 22)
sudo ufw allow 22
sudo ufw allow ssh


# HTTP 허용 (포트 80)
sudo ufw allow 80
sudo ufw allow http


# HTTPS 허용 (포트 443)
sudo ufw allow 443
sudo ufw allow https


# 사용자 지정 포트 허용
sudo ufw allow 8080
```

2. **특정 포트 거부**

```bash
# 포트 80 거부
sudo ufw deny 80


# 특정 IP에서의 SSH 접근 거부
sudo ufw deny from 192.168.1.100 to any port 22
```

3. **서비스 이름으로 허용 또는 거부**

```bash
# 일반적인 서비스 허용
sudo ufw allow ssh
sudo ufw allow http
sudo ufw allow https
sudo ufw allow ftp
sudo ufw allow smtp
```

## 고급 규칙

### 포트 범위

```bash
# Allow port range
sudo ufw allow 1000:2000/tcp
sudo ufw allow 1000:2000/udp

# Allow specific protocol
sudo ufw allow 53/udp  # DNS
sudo ufw allow 53/tcp  # DNS over TCP
```

### IP 주소 규칙

1. **특정 IP 주소 허용/거부**

```bash
# 특정 IP 허용
sudo ufw allow from 192.168.1.100

# 특정 IP 거부
sudo ufw deny from 192.168.1.50

# 서브넷 허용
sudo ufw allow from 192.168.1.0/24
```

2. **특정 포트에 대한 IP 허용**

```bash
# 특정 IP에 SSH 허용
sudo ufw allow from 192.168.1.100 to any port 22

# 웹 서버에 서브넷 허용
sudo ufw allow from 10.0.0.0/8 to any port 80
```

### 인터페이스별 규칙

```bash
# 특정 인터페이스에서 허용
sudo ufw allow in on eth0 to any port 80

# 특정 인터페이스에서 아웃바운드 허용
sudo ufw allow out on eth1 to any port 443
```

## 규칙 관리

### 규칙 목록

```bash
# 상태 및 규칙 표시
sudo ufw status

# 번호가 매겨진 규칙 표시
sudo ufw status numbered

# 상세 상태 표시
sudo ufw status verbose
```

### 규칙 삭제

```bash
# 규칙 번호로 삭제
sudo ufw delete 3

# 규칙을 지정하여 삭제
sudo ufw delete allow 80
sudo ufw delete allow from 192.168.1.100
```

### 규칙 삽입

```bash
# 특정 위치에 규칙 삽입
sudo ufw insert 1 allow from 192.168.1.0/24
```

## 기본 정책

```bash
# 기본 정책 설정
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw default deny forward

# 현재 기본 설정 확인
sudo ufw status verbose
```

## 애플리케이션 프로필

### 사용 가능한 프로필 목록

```bash
# 애플리케이션 프로필 목록
sudo ufw app list

# 프로필 정보 표시
sudo ufw app info OpenSSH
sudo ufw app info "Apache Full"
```

### 애플리케이션 프로필 사용

```bash
# 애플리케이션 허용
sudo ufw allow OpenSSH
sudo ufw allow "Apache Full"
sudo ufw allow "Nginx Full"

# 주요 애플리케이션 프로필
sudo ufw allow "OpenSSH"
sudo ufw allow "Apache"
sudo ufw allow "Apache Secure"
sudo ufw allow "Nginx HTTP"
sudo ufw allow "Nginx HTTPS"
sudo ufw allow "Nginx Full"
```

## 로깅

```bash
# 로깅 활성화
sudo ufw logging on

# 로그 레벨 설정
sudo ufw logging low
sudo ufw logging medium
sudo ufw logging high

# 로깅 비활성화
sudo ufw logging off

# 로그 확인
sudo tail -f /var/log/ufw.log
```

## 초기화 및 다시 로드

```bash
# 모든 규칙을 기본값으로 리셋
sudo ufw --force reset

# UFW 새로고침
sudo ufw reload
```

## 일반적인 사용 사례

### 1. 기본 웹 서버 설정 {.unnumbered}

```bash
# SSH, HTTP 및 HTTPS 허용
sudo ufw allow ssh
sudo ufw allow http
sudo ufw allow https
sudo ufw enable
```

### 2. 데이터베이스 서버(MySQL) {.unnumbered}

```bash
# 애플리케이션 서버에서만 MySQL 허용
sudo ufw allow from 192.168.1.10 to any port 3306
sudo ufw allow from 192.168.1.11 to any port 3306
```

### 3. 개발 서버 {.unnumbered}

```bash
# 주요 개발 포트 허용
sudo ufw allow 3000  # Node.js
sudo ufw allow 8000  # Django
sudo ufw allow 5000  # Flask
sudo ufw allow 4200  # Angular
```

### 4. 메일 서버 {.unnumbered}

```bash
# 메일 서버 포트 허용
sudo ufw allow smtp      # Port 25
sudo ufw allow 587/tcp   # SMTP submission
sudo ufw allow 993/tcp   # IMAPS
sudo ufw allow 995/tcp   # POP3S
```

### 5. DNS 서버 {.unnumbered}

```bash
# DNS 트래픽 허용
sudo ufw allow 53/tcp
sudo ufw allow 53/udp
```

## 보안 모범 사례

### 1. 최소 권한 원칙 {.unnumbered}

```bash
# 모든 연결 거부로 시작
sudo ufw default deny incoming
sudo ufw default allow outgoing

# 필요한 연결만 허용
sudo ufw allow ssh
sudo ufw allow from 192.168.1.0/24 to any port 80
```

### 2. SSH 액세스 제한 {.unnumbered}

```bash
# SSH 시도 제한 (30초 동안 6회 시도)
sudo ufw limit ssh

# 특정 네트워크에서만 SSH 허용
sudo ufw allow from 192.168.1.0/24 to any port 22
sudo ufw deny ssh
```

### 3. 모니터링 및 로깅 {.unnumbered}

```bash
# 로깅 활성화
sudo ufw logging medium

# 로그 모니터링
sudo tail -f /var/log/ufw.log | grep DPT
```

## 문제 해결

### 1. 현재 규칙 확인 {.unnumbered}

```bash
sudo ufw status numbered
sudo iptables -L -n
```

### 2. 연결 테스트 {.unnumbered}

```bash
# 포트 접근 가능 여부 테스트
telnet your-server-ip 80
nc -zv your-server-ip 22
```

### 3. UFW 디버깅 {.unnumbered}

```bash
# 드라이 런(시뮬레이션)
sudo ufw --dry-run allow 80

# UFW 버전 확인
ufw --version
```

## 고급 설정

### 1. 사용자 지정 규칙 파일 {.unnumbered}

```bash
# UFW 규칙 직접 편집
sudo vim /etc/ufw/user.rules
sudo vim /etc/ufw/user6.rules
```

### 2. 속도 제한 {.unnumbered}

```bash
# IP당 연결 제한
sudo ufw limit ssh
sudo ufw limit 80/tcp
```

### 3. 포트 전달 {.unnumbered}

```bash
# IP 포워딩 활성화
echo 'net.ipv4.ip_forward=1' | sudo tee -a /etc/ufw/sysctl.conf

# /etc/ufw/before.rules에 NAT 규칙 추가
```

## 서비스 통합

### 1. Docker 통합 {.unnumbered}

```bash
# Docker 컨테이너 허용
sudo ufw allow from 172.17.0.0/16

# Docker 우회 차단(/etc/ufw/after.rules)
```

### 2. Fail2ban 통합 {.unnumbered}

```bash
# UFW와 fail2ban 연동
sudo apt install fail2ban
# UFW 액션을 사용하도록 fail2ban 설정
```

## 중요 참고 사항

- UFW는 iptables의 프런트엔드이며 iptables를 대체하지 않습니다.
- 규칙은 순서대로 처리됩니다(먼저 일치하는 규칙이 적용됨).
- 특정 규칙과 일치하지 않을 때 기본 정책이 적용됩니다.
- 기본적으로 UFW는 기존 iptables 규칙에 간섭하지 않습니다.
- 운영 환경에서 활성화하기 전에 항상 규칙을 테스트하세요.
- 원격에서 UFW를 활성화하기 전에 SSH 액세스 규칙을 먼저 추가하세요.

## 빠른 참조

```bash
# 주요 필수 명령어
sudo ufw enable                    # Enable firewall
sudo ufw status                    # Check status
sudo ufw allow 22                  # Allow SSH
sudo ufw allow from 192.168.1.0/24 # Allow subnet
sudo ufw delete 3                  # Delete rule #3
sudo ufw reset                     # Reset all rules
sudo ufw reload                    # Reload configuration
```

UFW는 단순성과 기능성의 균형이 뛰어나 iptables의 복잡성 없이 효과적으로 방화벽을 관리해야 하는 시스템 관리자에게 적합합니다.

자세한 내용은 다음 매뉴얼을 참조하세요: `man ufw`

# `traceroute` 명령

`traceroute` 명령은 네트워크를 통해 컴퓨터에서 대상 호스트까지 패킷이 이동하는 경로를 추적합니다. 경로상의 각 홉(라우터)을 표시하고 각 홉에 도달하는 데 걸리는 시간을 측정합니다.

## 구문

```
traceroute [options] destination
```

## 설치

```bash
# Ubuntu/Debian
sudo apt update && sudo apt install traceroute

# CentOS/RHEL/Fedora
sudo yum install traceroute
# or
sudo dnf install traceroute

# macOS (usually pre-installed)
traceroute

# 설치 여부 확인
which traceroute
```

## 기본 사용법

1. **웹사이트까지의 경로 추적**

```bash
traceroute google.com
traceroute github.com
traceroute 8.8.8.8
```

2. **IP 주소까지의 경로 추적**

```bash
traceroute 192.168.1.1
traceroute 208.67.222.222
```

## 옵션

자주 사용하는 옵션 플래그는 다음과 같습니다:

```
-n          Don't resolve hostnames (show IP addresses only)
-w [sec]    Set timeout for responses (default 5 seconds)
-q [num]    Set number of probe packets per hop (default 3)
-m [hops]   Set maximum number of hops (default 30)
-p [port]   Set destination port (default 33434)
-f [ttl]    Set first TTL value (starting hop)
-g [addr]   Use loose source route gateway
-I          Use ICMP ECHO instead of UDP
-T          Use TCP SYN instead of UDP
-U          Use UDP (default)
-4          Force IPv4
-6          Force IPv6
-s [addr]   Set source address
-i [iface]  Set network interface
```

## 예제

1. **기본 traceroute**

```bash
traceroute google.com
```

2. **IP 주소만 표시(DNS 확인 안 함)**

```bash
traceroute -n google.com
```

3. **사용자 지정 시간 초과 설정**

```bash
traceroute -w 10 google.com
```

4. **UDP 대신 ICMP 사용**

```bash
traceroute -I google.com
```

5. **TCP traceroute 사용**

```bash
traceroute -T google.com
```

6. **최대 홉 수 설정**

```bash
traceroute -m 15 google.com
```

7. **홉당 프로브 수 설정**

```bash
traceroute -q 1 google.com
```

8. **IPv6 강제 사용**

```bash
traceroute -6 ipv6.google.com
```

9. **사용자 지정 포트 설정**

```bash
traceroute -p 80 google.com
```

10. **특정 TTL부터 시작**

```bash
traceroute -f 5 google.com
```

## 출력 이해하기

traceroute 출력 예:

```
traceroute to google.com (172.217.164.174), 30 hops max, 60 byte packets
 1  router.local (192.168.1.1)  1.234 ms  1.123 ms  1.045 ms
 2  10.0.0.1 (10.0.0.1)  12.345 ms  11.234 ms  10.123 ms
 3  isp-gateway.net (203.0.113.1)  25.678 ms  24.567 ms  23.456 ms
 4  * * *
 5  google-router.net (172.217.164.174)  45.123 ms  44.234 ms  43.345 ms
```

### 출력 설명:

- **Hop number**: 각 라우터의 순서 번호
- **Hostname/IP**: 라우터의 이름과 IP 주소
- **Three times**: 세 프로브 패킷의 왕복 시간
- ***** : 시간 초과 또는 필터링된 응답을 나타냄

## 일반적인 사용 사례

### 1. 네트워크 문제 해결 {.unnumbered}

```bash
# 패킷이 손실되는 위치 확인
traceroute -n problematic-server.com

# 서로 다른 목적지 경로 비교
traceroute server1.com
traceroute server2.com
```

### 2. 성능 분석 {.unnumbered}

```bash
# 속도가 느린 홉 식별
traceroute -w 10 slow-website.com

# 여러 지역에 대한 지연 시간 확인
traceroute eu-server.com
traceroute us-server.com
traceroute asia-server.com
```

### 3. 네트워크 보안 분석 {.unnumbered}

```bash
# 트래픽이 예기치 않은 국가를 거치는지 확인
traceroute -n suspicious-site.com

# VPN 라우팅 검증
traceroute -n whatismyip.com
```

### 4. ISP 경로 분석 {.unnumbered}

```bash
# ISP 라우팅 결정 확인
traceroute -n 8.8.8.8
traceroute -n 1.1.1.1
traceroute -n 208.67.222.222
```

## 고급 기법

### 1. TCP Traceroute (tcptraceroute) {.unnumbered}

```bash
# tcptraceroute 설치
sudo apt install tcptraceroute

# 웹 서버 경로로 TCP 추적
sudo tcptraceroute google.com 80
sudo tcptraceroute -n github.com 443
```

### 2. MTR (My TraceRoute) {.unnumbered}

```bash
# mtr 설치
sudo apt install mtr

# 통계 정보가 포함된 지속적인 Traceroute
mtr google.com
mtr -n google.com    # No DNS resolution
mtr -r google.com    # Report mode
```

### 3. Paris Traceroute {.unnumbered}

```bash
# 로드 밸런싱 네트워크에서 더 정확함
sudo apt install paris-traceroute
paris-traceroute google.com
```

## traceroute 변형

### 1. IPv6 Traceroute {.unnumbered}

```bash
# IPv6 경로 추적
traceroute6 ipv6.google.com
traceroute -6 ipv6.google.com
```

### 2. Visual Traceroute Tools {.unnumbered}

```bash
# 웹 기반 시각적 Traceroute
# 방문: traceroute-online.com
# 또는 GUI 버전 mtr 사용

# GUI용 mtr-gtk 설치
sudo apt install mtr-gtk
mtr-gtk
```

## 결과 분석

### 1. 문제 식별 {.unnumbered}

```bash
# 특정 홉에서의 높은 지연 시간
# 응답 시간의 급격한 증가 확인

# 패킷 손실
# * * * 응답 확인

# 비대칭 라우팅
# 패킷별로 서로 다른 경로 사용
```

### 2. 지리적 분석 {.unnumbered}

```bash
# 홉 위치를 식별하기 위해 whois 사용
whois 203.0.113.1

# 온라인 IP 위치 정보 서비스 사용
# 경로를 지리적으로 매핑
```

## 일반적인 문제 해결

### 1. 시간 초과 및 별표 {.unnumbered}

```bash
# 다양한 프로토콜 시도
traceroute -I google.com    # ICMP
traceroute -T google.com    # TCP
traceroute -U google.com    # UDP (default)

# 타임아웃 증가
traceroute -w 10 google.com
```

### 2. 권한 문제 {.unnumbered}

```bash
# UDP traceroute는 권한이 필요할 수 있음
sudo traceroute google.com

# ICMP는 반드시 권한이 필요함
sudo traceroute -I google.com
```

### 3. 방화벽 간섭 {.unnumbered}

```bash
# 일부 방화벽은 traceroute를 차단함
# 다른 포트 시도
traceroute -p 53 google.com   # DNS port
traceroute -p 80 google.com   # HTTP port
```

## 보안 고려 사항

### 1. 정보 노출 {.unnumbered}

```bash
# Traceroute는 네트워크 토폴로지를 드러냄
# 결과를 공개적으로 공유할 때 주의

# 내부 호스트 이름을 노출하지 않으려면 -n 사용
traceroute -n destination
```

### 2. 방화벽 우회 {.unnumbered}

```bash
# 차단된 경우 다른 프로토콜 시도
traceroute -T -p 443 target.com
traceroute -I target.com
```

## 자동화 및 스크립팅

### 1. traceroute 일괄 실행 {.unnumbered}

```bash
#!/bin/bash
# 여러 목적지로의 경로 추적
destinations=("google.com" "github.com" "stackoverflow.com")

for dest in "${destinations[@]}"; do
    echo "Tracing route to $dest"
    traceroute -n "$dest" > "traceroute_$dest.txt"
done
```

### 2. 모니터링 스크립트 {.unnumbered}

```bash
#!/bin/bash
# 경로 변경 모니터링
while true; do
    traceroute -n google.com > "/tmp/trace_$(date +%s).txt"
    sleep 3600  # Check every hour
done
```

### 3. 경로 비교 {.unnumbered}

```bash
#!/bin/bash
# 서로 다른 위치에서의 경로 비교
echo "Route from current location:"
traceroute -n $1

echo "Route from VPN:"
# VPN 연결 후 다시 실행
```

## 대체 명령

### 1. pathping (Windows 대응 명령) {.unnumbered}

```bash
# Windows 시스템에서
pathping google.com
```

### 2. mtr (더 나은 대안) {.unnumbered}

```bash
# 지속적 모니터링
mtr --report google.com
mtr --report-cycles 10 google.com
```

### 3. hping3 (고급 프로빙) {.unnumbered}

```bash
sudo apt install hping3
sudo hping3 -T -p 80 -c 3 google.com
```

## 성능 최적화

### 1. traceroute 속도 향상 {.unnumbered}

```bash
# 홉당 프로브 수 감소
traceroute -q 1 google.com

# 최대 홉 수 감소
traceroute -m 15 google.com

# DNS 이름 확인 건너뛰기
traceroute -n google.com
```

### 2. 상세 분석 {.unnumbered}

```bash
# 정확성을 위한 더 많은 프로브 전송
traceroute -q 5 google.com

# 느린 링크를 위한 더 긴 타임아웃
traceroute -w 15 google.com
```

## 중요 참고 사항

- 부하 분산 네트워크에서는 traceroute가 실제 경로를 표시하지 않을 수 있습니다.
- 일부 라우터는 traceroute 프로브에 응답하지 않습니다.
- 경로 변경으로 인해 실행할 때마다 결과가 달라질 수 있습니다.
- ICMP traceroute가 UDP보다 잘 작동하는 경우가 많습니다.
- Modern networks may use ECMP (Equal Cost Multi-Path) routing
- VPN과 프록시는 겉으로 보이는 경로를 변경합니다.

`traceroute` 명령은 네트워크 진단에 필수적이며 라우팅 문제와 네트워크 성능 문제를 파악하고 네트워크 토폴로지를 이해하는 데 도움이 됩니다.

자세한 내용은 `man traceroute` 매뉴얼을 참조하세요.

# `nmcli` 명령

`nmcli` 명령은 네트워크를 처리하는 데몬인 NetworkManager를 명령줄에서 제어하여 네트워크 연결을 관리하는 데 사용됩니다. 
이 명령은 Network Manager Command Line Interface의 약자입니다. 

### 설치:

대부분의 Linux 배포판에는 `nmcli` 명령이 기본으로 설치되어 있습니다. 시스템에 설치되어 있는지 확인하려면 다음을 입력합니다: 

```
nmcli --version
```

nmcli가 설치되어 있지 않다면 패키지 관리자를 사용해 설치할 수 있습니다:

Ubuntu 또는 Debian:

```
sudo apt install network manager
```

이 명령은 시스템에 NetworkManager를 설치합니다. 
이제 네트워크 관리 서비스를 시작해야 합니다.

```
sudo systemctl start NetworkManager
```

Red Hat 기반 시스템(Fedora, CentOS, REHL 등):

```
sudo dnf install NetworkManager
```

이 명령은 시스템에 NetworkManager를 설치합니다.
이제 네트워크 관리 서비스를 시작해야 합니다.

```
sudo systemctl start NetworkManager
```

Arch Linux:

```
sudo pacman -S networkmanager
```

이 명령은 시스템에 NetworkManager를 설치합니다.
이제 네트워크 관리 서비스를 시작해야 합니다.

```
sudo systemctl start NetworkManager
```

### 예제:

1. 사용 가능한 모든 Wi-Fi 네트워크 나열

```
nmcli device wifi list
```

2. 네트워크 상태 확인

```
nmcli device status
```

3. Wi-Fi 네트워크에 연결

```
nmcli device wifi connect "SSID_NAME" password "YOUR_PASSWORD"
```

4. Wi-Fi 네트워크 연결 해제

```
nmcli connection down "CONNECTION_NAME"
```

5. Wi-Fi 켜기/끄기

```
nmcli radio wifi on
nmcli radio wifi off
```

각각.

6. Bluetooth 켜기/끄기

```
nmcli radio bluetooth on
nmcli radio bluetooth off
```

7. 모든 연결 표시

```
nmcli connection show
```

8. 특정 연결의 상세 정보 표시

```
nmcli connection show "CONNECTION_NAME"
```

### 구문: 

nmcli 명령의 일반적인 구문은 다음과 같습니다:

```
nmcli [OPTIONS...] { help | general | networking | radio | connection | device | agent | monitor } [COMMAND] [ARGUMENTS...]
```

### 추가 플래그 및 기능:

#### Options

| **Short Flag** | **Long Flag**          | **설명**                                                                                                |
| :------------- | :--------------------- | :------------------------------------------------------------------------------------------------------------- |
| `-a`           | `--ask`                | 필수 인수가 누락되면 nmcli가 중지하고 입력을 요청합니다(비대화형 옵션에는 사용하지 않음)              |
| `-c`           | `--color`{yes/no}      | 색상 출력을 제어합니다. yes는 색상을 활성화하고 no는 비활성화합니다                                         |
| `-h`           | `--help`               | 도움말 정보를 출력합니다                                                                                        |
| `-p`           | `--pretty`             | 헤더를 포함해 더 이해하기 쉬운 출력을 생성하고 값을 정렬합니다                |
| `-v`           | `--version`            | nmcli 버전을 표시합니다                                                                                        |
| `-f`           | `--fields`{field1,...} | 출력할 필드를 지정합니다. 유효한 필드 이름은 명령에 따라 다릅니다. |
| `-g`           | `--get-value`{field1,.}| 특정 필드의 값을 출력합니다. --mode tabular --terse --fields의 단축형입니다 |

#### General Commands

| **Command**                    |**설명**                                                       |
| :----------------------------- | :------------------------------------------------------------------- |
| `nmcli general status`         | NetworkManager의 전체 상태 표시                                   |
| `nmcli general hostname`       | 현재 호스트 이름 표시                                             |

#### Networking Commands

| **Command**                    | **설명**                                                      |
| :----------------------------- | :------------------------------------------------------------------- |
| `nmcli networking on`          | 모든 네트워킹 활성화                                                |
| `nmcli networking off`         | 모든 네트워킹 비활성화                                               |
| `nmcli networking connectivity`| 네트워크 연결 상태 확인                                    |

#### Radio Commands

| **Command**                    | **설명**                                                      |
| :----------------------------- | :------------------------------------------------------------------- |
| `nmcli radio wifi on`          | Wi-Fi 무선 기능 활성화                                                   |
| `nmcli radio wifi off`         | Wi-Fi 무선 기능 비활성화                                                  |
| `nmcli radio all`              | 모든 무선 스위치의 상태 표시                                    |
| `nmcli radio wifi`             | Wi-Fi 무선 기능 상태 표시                                              |

#### Connection Management Commands

| **Command**                                      | **설명**                                    |
| :----------------------------------------------- | :------------------------------------------------- |
| `nmcli connection show`                          | 저장된 모든 연결 프로필 나열                 |
| `nmcli connection show --active`                 | 활성 연결만 나열                       |
| `nmcli connection show "NAME"`                   | 특정 연결의 상세 정보 표시       |
| `nmcli connection up "NAME"`                     | 연결 활성화                              |
| `nmcli connection down "NAME"`                   | 연결 비활성화                            |
| `nmcli connection modify "NAME" [OPTIONS]`       | 연결 설정 수정                         |
| `nmcli connection delete "NAME"`                 | 연결 프로필 삭제                        |
| `nmcli connection reload`                        | 디스크에서 모든 연결 파일 다시 로드              |

#### Device Management Commands

| **Command**                                      | **설명**                                    |
| :----------------------------------------------- | :------------------------------------------------- |
| `nmcli device status`                            | 모든 장치의 상태 표시                         |
| `nmcli device show "DEVICE"`                     | 특정 장치의 상세 정보 표시             |
| `nmcli device disconnect "DEVICE"`               | 장치 연결 해제                           |
| `nmcli device wifi list`                         | 사용 가능한 모든 Wi-Fi 네트워크 나열                  |
| `nmcli device wifi connect "SSID" password "PWD"`| 암호로 보호된 Wi-Fi에 연결                |

# `badblocks` 명령

`badblocks` 명령은 하드 드라이브나 SSD 등의 저장 장치에서 불량 섹터(배드 블록)를 검사하고 찾아내는 유용한 도구입니다.

## 기본 사용법

### 1. 비파괴 읽기 테스트 (기본) {.unnumbered}

```bash
# 기본 비파괴 읽기 검사
sudo badblocks -sv /dev/sdb
```

### 2. 파괴적 쓰기 테스트 (데이터 손실 주의) {.unnumbered}

```bash
# 쓰기 테스트 (모든 데이터가 삭제되므로 사전 백업 필수!)
# Make sure to backup first!
sudo badblocks -wsv /dev/sdb

# 다회차 파괴적 테스트
sudo badblocks -wsv -p 3 /dev/sdb
```

### 3. 특정 범위 테스트 {.unnumbered}

```bash
# Test blocks 1000 to 2000
sudo badblocks -sv /dev/sdb 2000 1000

# Test first 1GB (assuming 4K blocks)
sudo badblocks -sv -b 4096 /dev/sdb 262144 0
```

### 4. fsck 통합 {.unnumbered}

```bash
# Create bad blocks file
sudo badblocks -sv -o /tmp/badblocks /dev/sdb1

# Use with fsck to mark bad blocks
sudo fsck.ext4 -l /tmp/badblocks /dev/sdb1

# For new filesystem
sudo mke2fs -l /tmp/badblocks /dev/sdb1
```

## 다양한 블록 크기

### 블록 크기 선택

```bash
# 1KB blocks (default)
sudo badblocks -b 1024 /dev/sdb

# 4KB blocks (common for modern drives)
sudo badblocks -b 4096 /dev/sdb

# 512 byte blocks (traditional sector size)
sudo badblocks -b 512 /dev/sdb

# Match filesystem block size
sudo tune2fs -l /dev/sdb1 | grep "Block size"
sudo badblocks -b 4096 /dev/sdb1
```

## 테스트 패턴

### 쓰기 테스트 패턴

```bash
# Alternating pattern (0xaa = 10101010)
sudo badblocks -w -t 0xaa /dev/sdb

# All ones pattern
sudo badblocks -w -t 0xff /dev/sdb

# All zeros pattern
sudo badblocks -w -t 0x00 /dev/sdb

# Random pattern
sudo badblocks -w -t random /dev/sdb
```

### 여러 패턴

```bash
# Test with multiple patterns
sudo badblocks -w -t 0xaa -t 0x55 -t 0xff -t 0x00 /dev/sdb

# Four-pass test with different patterns
sudo badblocks -wsv -p 4 -t 0xaa -t 0x55 -t 0xff -t 0x00 /dev/sdb
```

## 출력 및 보고

### 표준 출력

```bash
# Basic output (just bad block numbers)
sudo badblocks /dev/sdb

# Verbose output with details
sudo badblocks -v /dev/sdb

# Progress indicator
sudo badblocks -s /dev/sdb
```

### 결과를 파일에 저장

```bash
# Save bad blocks list
sudo badblocks -o badblocks.txt /dev/sdb

# Append to existing file
sudo badblocks -o badblocks.txt /dev/sdb >> all_badblocks.txt

# Save with verbose output to different files
sudo badblocks -v -o badblocks.txt /dev/sdb 2> scan_log.txt
```

## 다양한 저장 장치 유형 사용

### 1. 기존 하드 드라이브 {.unnumbered}

```bash
# Standard test for HDDs
sudo badblocks -sv /dev/sda

# Thorough test with multiple passes
sudo badblocks -wsv -p 4 /dev/sda
```

### 2. 솔리드 스테이트 드라이브 {.unnumbered}

```bash
# Read-only test (preferred for SSDs)
sudo badblocks -sv /dev/sdb

# Non-destructive test
sudo badblocks -nsv /dev/sdb

# Avoid excessive write tests on SSDs
```

### 3. USB 드라이브 {.unnumbered}

```bash
# Test USB drive
sudo badblocks -sv /dev/sdc

# Fast test for quick verification
sudo badblocks -sv -c 65536 /dev/sdc
```

### 4. SD 카드 {.unnumbered}

```bash
# Test SD card
sudo badblocks -sv /dev/mmcblk0

# Write test for fake capacity detection
sudo badblocks -wsv /dev/mmcblk0
```

## 파일 시스템 통합

### 1. ext2/ext3/ext4 {.unnumbered}

```bash
# Create filesystem with bad block check
sudo mke2fs -c /dev/sdb1

# Check existing filesystem
sudo fsck.ext4 -c /dev/sdb1

# Thorough check
sudo fsck.ext4 -cc /dev/sdb1
```

### 2. e2fsck와 함께 사용 {.unnumbered}

```bash
# Create bad blocks list
sudo badblocks -sv -o /tmp/badblocks /dev/sdb1

# Apply to filesystem
sudo e2fsck -l /tmp/badblocks /dev/sdb1
```

## 모니터링 및 자동화

### 1. 예약 점검 {.unnumbered}

```bash
# Create script for regular checking
#!/bin/bash
DEVICE="/dev/sdb"
LOGFILE="/var/log/badblocks_$(date +%Y%m%d).log"

sudo badblocks -sv -o /tmp/badblocks "$DEVICE" > "$LOGFILE" 2>&1

if [ -s /tmp/badblocks ]; then
    echo "Bad blocks found on $DEVICE!" | mail -s "Bad Blocks Alert" admin@domain.com
fi
```

### 2. SMART 통합 {.unnumbered}

```bash
# Check SMART status first
sudo smartctl -a /dev/sdb

# Run badblocks if SMART shows issues
sudo smartctl -t short /dev/sdb
sleep 300
sudo smartctl -a /dev/sdb
sudo badblocks -sv /dev/sdb
```

## 성능 고려 사항

### 1. 속도 최적화 {.unnumbered}

```bash
# Increase block count for faster scanning
sudo badblocks -c 65536 /dev/sdb

# Use larger block size
sudo badblocks -b 4096 -c 16384 /dev/sdb

# Combine options for maximum speed
sudo badblocks -sv -b 4096 -c 65536 /dev/sdb
```

### 2. 시스템 영향 {.unnumbered}

```bash
# Run with lower priority
sudo nice -n 19 badblocks -sv /dev/sdb

# Limit I/O impact
sudo ionice -c 3 badblocks -sv /dev/sdb

# Combine nice and ionice
sudo nice -n 19 ionice -c 3 badblocks -sv /dev/sdb
```

## 결과 해석

### 1. 불량 블록 없음 {.unnumbered}

```bash
# Output: "Pass completed, 0 bad blocks found."
# This indicates a healthy drive
```

### 2. 불량 블록 발견 {.unnumbered}

```bash
# Output shows block numbers of bad sectors
# Example:
# Pass completed, 5 bad blocks found. (0/0/5 errors)
# 1024
# 2048
# 4096
# 8192
# 16384
```

### 3. 블록 번호 이해하기 {.unnumbered}

```bash
# Convert block numbers to byte offsets
# Block 1024 with 4KB block size = 1024 * 4096 = 4,194,304 bytes
# This helps locate physical position on drive
```

## 문제 해결

### 1. 권한 거부 {.unnumbered}

```bash
# Must run as root
sudo badblocks /dev/sdb

# Check device permissions
ls -l /dev/sdb
```

### 2. 장치 사용 중 {.unnumbered}

```bash
# Unmount all partitions first
sudo umount /dev/sdb1
sudo umount /dev/sdb2

# Check for active processes
lsof /dev/sdb*

# Use force option if necessary
sudo badblocks -f /dev/sdb
```

### 3. 느린 성능 {.unnumbered}

```bash
# Increase block count
sudo badblocks -c 65536 /dev/sdb

# Use appropriate block size
sudo badblocks -b 4096 /dev/sdb

# Check system load
iostat 1
```

## 안전 고려 사항

### 1. 데이터 백업 {.unnumbered}

```bash
# Always backup before destructive tests
sudo dd if=/dev/sdb of=/backup/sdb_backup.img bs=1M

# Or use filesystem-level backup
sudo rsync -av /mount/point/ /backup/location/
```

### 2. 드라이브 상태 평가 {.unnumbered}

```bash
# Check SMART data first
sudo smartctl -a /dev/sdb

# Look for reallocated sectors
sudo smartctl -A /dev/sdb | grep Reallocated
```

### 3. 드라이브 교체 시점 {.unnumbered}

```bash
# Replace if:
# - Many bad blocks found (>50-100)
# - Bad blocks increasing over time
# - SMART indicates drive failure
# - Critical system drive affected
```

## 대체 도구

### 1. SMART 도구 {.unnumbered}

```bash
# Use smartctl for health monitoring
sudo smartctl -t long /dev/sdb
sudo smartctl -a /dev/sdb
```

### 2. 제조업체 도구 {.unnumbered}

```bash
# Many manufacturers provide specific tools
# - Western Digital: WD Data Lifeguard
# - Seagate: SeaTools
# - Samsung: Samsung Magician
```

## 중요 참고 사항

- 읽기 테스트는 안전하며 데이터를 손상시키지 않습니다
- 쓰기 테스트는 장치의 모든 데이터를 파괴합니다
- 테스트 전에 반드시 장치를 언마운트하세요
- 테스트 결과는 다른 상태 지표와 함께 해석해야 합니다
- 정기적인 테스트는 데이터 손실 예방에 도움이 됩니다
- 불량 블록이 많이 발견되면 드라이브 교체를 고려하세요
- 최신 드라이브는 불량 블록 관리를 위한 예비 섹터를 갖추고 있습니다

`badblocks` 명령은 저장 장치의 건강 상태를 유지하고 데이터 손실을 방지하는 데 필수적인 도구입니다.

자세한 내용은 `man badblocks` 매뉴얼을 참조하세요.

# `fsck` 명령

`fsck`(파일 시스템 검사) 명령은 Linux 파일 시스템을 검사하고 복구하는 데 사용됩니다. 다양한 파일 시스템 불일치, 손상 및 구조적 문제를 감지하고 수정할 수 있습니다. 파일 시스템 무결성을 유지하고 시스템 충돌 후 복구하는 데 필수적인 도구입니다.

## 구문

```
fsck [options] [filesystem...]
```

## 주요 기능

- **여러 파일 시스템 지원**: ext2, ext3, ext4, XFS 등과 함께 작동
- **자동 감지**: 파일 시스템 유형을 자동으로 감지할 수 있음
- **대화형 복구**: 수정 전에 확인을 요청
- **배치 모드**: 사용자 상호작용 없이 자동 실행 가능
- **읽기 전용 모드**: 변경 없이 검사

## 기본 사용법

### Simple File System Check

```bash
# Check specific partition
sudo fsck /dev/sdb1

# Check by mount point
sudo fsck /home

# Check with verbose output
sudo fsck -v /dev/sdb1
```

### Check All File Systems

```bash
# Check all file systems in /etc/fstab
sudo fsck -A

# Check all except root
sudo fsck -AR

# Check all with progress
sudo fsck -AV
```

## 일반 옵션

### 기본 옵션

```bash
# -y: Answer "yes" to all questions
sudo fsck -y /dev/sdb1

# -n: Answer "no" to all questions (read-only)
sudo fsck -n /dev/sdb1

# -f: Force check even if file system seems clean
sudo fsck -f /dev/sdb1

# -v: Verbose output
sudo fsck -v /dev/sdb1
```

### 고급 옵션

```bash
# -p: Automatically repair (preen mode)
sudo fsck -p /dev/sdb1

# -r: Interactive repair mode
sudo fsck -r /dev/sdb1

# -t: Specify file system type
sudo fsck -t ext4 /dev/sdb1

# -C: Show progress bar
sudo fsck -C /dev/sdb1
```

## 파일 시스템별 명령

### 1. ext2/ext3/ext4 (e2fsck) {.unnumbered}

```bash
# Check ext4 file system
sudo fsck.ext4 /dev/sdb1

# Force check
sudo e2fsck -f /dev/sdb1

# Fix bad blocks
sudo e2fsck -c /dev/sdb1

# Thorough check with bad block scan
sudo e2fsck -cc /dev/sdb1
```

### 2. XFS (xfs_repair) {.unnumbered}

```bash
# Check XFS file system (read-only)
sudo xfs_repair -n /dev/sdb1

# Repair XFS file system
sudo xfs_repair /dev/sdb1

# Verbose repair
sudo xfs_repair -v /dev/sdb1
```

### 3. FAT32 (fsck.fat) {.unnumbered}

```bash
# Check FAT32 file system
sudo fsck.fat /dev/sdb1

# Repair FAT32
sudo fsck.fat -r /dev/sdb1

# Verbose check
sudo fsck.fat -v /dev/sdb1
```

## 실용적인 예제

### 1. 마운트 전 확인 {.unnumbered}

```bash
# Always check before mounting suspicious drives
sudo fsck -n /dev/sdb1

# If clean, mount normally
sudo mount /dev/sdb1 /mnt/usb

# If errors found, repair first
sudo fsck -y /dev/sdb1
sudo mount /dev/sdb1 /mnt/usb
```

### 2. 충돌 후 시스템 복구 {.unnumbered}

```bash
# Boot from live CD/USB
# Check root file system
sudo fsck -f /dev/sda1

# Check other partitions
sudo fsck -f /dev/sda2
sudo fsck -f /dev/sda3

# Reboot if repairs were made
sudo reboot
```

### 3. 예약 유지 관리 {.unnumbered}

```bash
# Force check on all file systems
sudo fsck -Af

# Check with automatic repair
sudo fsck -Ap

# Check with progress indicators
sudo fsck -AVC
```

## 다양한 시나리오에 적용

### 1. 읽기 전용 확인 {.unnumbered}

```bash
# Check without making changes
sudo fsck -n /dev/sdb1

# Verbose read-only check
sudo fsck -nv /dev/sdb1

# Generate report only
sudo fsck -n /dev/sdb1 > fsck_report.txt 2>&1
```

### 2. 자동 복구 {.unnumbered}

```bash
# Repair automatically (dangerous!)
sudo fsck -y /dev/sdb1

# Safer automatic repair
sudo fsck -p /dev/sdb1

# Batch mode for multiple file systems
sudo fsck -Ap
```

### 3. 대화형 복구 {.unnumbered}

```bash
# Interactive mode (default)
sudo fsck /dev/sdb1

# Ask before each fix
sudo fsck -r /dev/sdb1

# Show detailed information
sudo fsck -rv /dev/sdb1
```

## 부팅 시 파일 시스템 검사

### 1. 자동 검사 {.unnumbered}

```bash
# Configure automatic checks in /etc/fstab
# Sixth field controls fsck behavior:
# 0 = no check
# 1 = check first (root filesystem)
# 2 = check after root filesystem

# Example /etc/fstab entry:
# /dev/sda1 / ext4 defaults 1 1
# /dev/sda2 /home ext4 defaults 1 2
```

### 2. 다음 부팅 시 강제 검사 {.unnumbered}

```bash
# Create forcefsck file (traditional method)
sudo touch /forcefsck

# Or use tune2fs for ext filesystems
sudo tune2fs -C 1 -c 1 /dev/sda1

# Set maximum mount count
sudo tune2fs -c 30 /dev/sda1
```

### 3. 검사 간격 {.unnumbered}

```bash
# Set check interval (ext filesystems)
sudo tune2fs -i 30d /dev/sda1  # Check every 30 days
sudo tune2fs -i 0 /dev/sda1    # Disable time-based checks

# Set mount count interval
sudo tune2fs -c 25 /dev/sda1   # Check every 25 mounts
sudo tune2fs -c 0 /dev/sda1    # Disable mount-based checks
```

## 일반적인 문제 해결

### 1. 마운트할 수 없는 파일 시스템 {.unnumbered}

```bash
# Try read-only check first
sudo fsck -n /dev/sdb1

# If errors found, try repair
sudo fsck -y /dev/sdb1

# For severe corruption
sudo fsck -f /dev/sdb1
```

### 2. 슈퍼블록 손상 {.unnumbered}

```bash
# List backup superblocks
sudo mke2fs -n /dev/sdb1

# Use backup superblock
sudo e2fsck -b 32768 /dev/sdb1

# Try different backup
sudo e2fsck -b 98304 /dev/sdb1
```

### 3. Lost+Found 디렉터리 {.unnumbered}

```bash
# Recovered files appear in lost+found
ls -la /mnt/partition/lost+found/

# Files are numbered by inode
# Use file command to identify type
file /mnt/partition/lost+found/*

# Restore files based on content
```

## 고급 복구 옵션

### 1. 불량 블록 처리 {.unnumbered}

```bash
# Scan for bad blocks during check
sudo e2fsck -c /dev/sdb1

# Thorough bad block scan
sudo e2fsck -cc /dev/sdb1

# Use existing bad block list
sudo badblocks -sv /dev/sdb1 > badblocks.list
sudo e2fsck -l badblocks.list /dev/sdb1
```

### 2. 저널 복구 {.unnumbered}

```bash
# ext3/ext4 journal recovery
sudo e2fsck -y /dev/sdb1

# Force journal recovery
sudo tune2fs -O ^has_journal /dev/sdb1
sudo e2fsck -f /dev/sdb1
sudo tune2fs -j /dev/sdb1
```

### 3. inode 문제 {.unnumbered}

```bash
# Check inode usage
sudo e2fsck -D /dev/sdb1

# Rebuild directory index
sudo e2fsck -D -f /dev/sdb1

# Fix inode count problems
sudo e2fsck -f /dev/sdb1
```

## 모니터링 및 로깅
### 1. 결과 확인 {.unnumbered}

```bash
# View fsck results
dmesg | grep -i fsck

# Check system logs
journalctl | grep fsck
tail -f /var/log/messages | grep fsck
```

### 2. 파일 시스템 상태 {.unnumbered}

```bash
# Check filesystem status
sudo tune2fs -l /dev/sda1 | grep -i "filesystem state"

# Check last fsck time
sudo tune2fs -l /dev/sda1 | grep -i "last checked"

# Check mount count
sudo tune2fs -l /dev/sda1 | grep -i "mount count"
```

### 3. 자동 모니터링 {.unnumbered}

```bash
# Script to check filesystem health
#!/bin/bash
for fs in $(awk '$3 ~ /^ext[234]$/ && $2 != "/" {print $1}' /etc/fstab); do
    echo "Checking $fs..."
    sudo fsck -n "$fs" || echo "Errors found on $fs"
done
```

## 예방 및 모범 사례

### 1. 정기 유지 관리 {.unnumbered}

```bash
# Schedule regular checks
# Add to crontab for non-critical systems
# 0 3 * * 0 /sbin/fsck -Ap > /var/log/fsck.log 2>&1
```

### 2. 올바른 종료 {.unnumbered}

```bash
# Always shutdown properly
sudo shutdown -h now

# Use sync before emergency shutdown
sync
sudo shutdown -h now
```

### 3. UPS 보호 {.unnumbered}

```bash
# Install UPS monitoring
sudo apt install apcupsd  # For APC UPS
sudo apt install nut      # Network UPS Tools

# Configure automatic shutdown on power loss
```

## 복구 시나리오

### 1. 부팅 실패 {.unnumbered}

```bash
# Boot from live USB/CD
# Mount root filesystem read-only
sudo mount -o ro /dev/sda1 /mnt

# Copy important data
sudo cp -r /mnt/home/user/important /backup/

# Unmount and check
sudo umount /mnt
sudo fsck -f /dev/sda1
```

### 2. 데이터 복구 {.unnumbered}

```bash
# Use ddrescue for severely damaged drives
sudo ddrescue /dev/sdb /backup/disk.img /backup/disk.log

# Then fsck the image
sudo fsck -f /backup/disk.img

# Mount and recover data
sudo mount -o loop /backup/disk.img /mnt/recovery
```

### 3. 여러 오류 {.unnumbered}

```bash
# Progressive repair approach
sudo fsck -n /dev/sdb1           # Check first
sudo fsck -p /dev/sdb1           # Auto-repair safe issues
sudo fsck -y /dev/sdb1           # Force repair remaining issues
sudo fsck -f /dev/sdb1           # Final thorough check
```

## 성능 고려 사항

### 1. 속도 최적화 {.unnumbered}

```bash
# Use progress indicator
sudo fsck -C /dev/sdb1

# Parallel checking (careful with dependencies)
sudo fsck -A -P

# Skip time-consuming checks when appropriate
sudo fsck -p /dev/sdb1
```

### 2. 시스템 영향 {.unnumbered}

```bash
# Run during low-activity periods
# Schedule during maintenance windows
# Use ionice for lower priority
sudo ionice -c 3 fsck /dev/sdb1
```

## 중요한 안전 참고 사항

- 검사하기 전에 항상 파일 시스템을 마운트 해제하세요.
- 복구 작업 전에 중요한 데이터를 백업하세요.
- 작업 중인 fsck를 절대 중단하지 마세요.
- 손상 상태를 평가하려면 먼저 읽기 전용 모드를 사용하세요.
- 자동 복구 모드의 위험성을 이해하세요.
- 루트 파일 시스템을 검사할 때는 라이브 미디어로 부팅하세요.
- 복구를 시작하기 전에 복구 계획을 마련해 두세요.

## 종료 코드

```bash
# fsck exit codes:
# 0: No errors
# 1: Filesystem errors corrected
# 2: System should be rebooted
# 4: Filesystem errors left uncorrected
# 8: Operational error
# 16: Usage or syntax error
# 32: Checking canceled by user request
# 128: Shared-library error
```

`fsck` 명령은 파일 시스템 무결성을 유지하고 손상 문제를 복구하는 데 핵심적입니다.

자세한 내용은 매뉴얼을 확인하십시오: `man fsck`

# `mkfs` 명령

`mkfs`(파일 시스템 생성) 명령은 저장 장치에 파일 시스템을 생성하는 데 사용됩니다. 이 명령은 파티션이나 전체 디스크를 ext4, XFS, FAT32 등의 특정 파일 시스템 유형으로 포맷합니다. 이는 저장 장치를 Linux 시스템에서 사용할 수 있도록 준비하는 데 필수적입니다.

## 구문

```
mkfs [options] [-t type] device
mkfs.type [options] device
```

## 주요 기능

- **다양한 파일 시스템 지원**: ext2/3/4, XFS, FAT32, NTFS 등
- **사용자 지정 매개변수**: 블록 크기, inode 비율, 레이블 및 기능
- **빠른 포맷과 전체 포맷**: 신속한 포맷 또는 철저한 초기화
- **고급 옵션**: 암호화, 압축 및 성능 조정

## 기본 사용법

### Create File Systems

```bash
# Auto-detect and create default filesystem
sudo mkfs /dev/sdb1

# Specify filesystem type
sudo mkfs -t ext4 /dev/sdb1

# Direct filesystem creation
sudo mkfs.ext4 /dev/sdb1
sudo mkfs.xfs /dev/sdb2
sudo mkfs.fat /dev/sdb3
```

### Common File System Types

```bash
# ext4 (recommended for Linux)
sudo mkfs.ext4 /dev/sdb1

# XFS (good for large files)
sudo mkfs.xfs /dev/sdb1

# FAT32 (cross-platform compatibility)
sudo mkfs.fat -F32 /dev/sdb1

# NTFS (Windows compatibility)
sudo mkfs.ntfs /dev/sdb1
```

## 파일 시스템별 옵션

### 1. ext4 File System {.unnumbered}

```bash
# Basic ext4 creation
sudo mkfs.ext4 /dev/sdb1

# With label
sudo mkfs.ext4 -L MyData /dev/sdb1

# Custom block size
sudo mkfs.ext4 -b 4096 /dev/sdb1

# Custom inode ratio
sudo mkfs.ext4 -i 4096 /dev/sdb1

# With journal
sudo mkfs.ext4 -J size=128 /dev/sdb1
```

### Advanced ext4 Options

```bash
# Disable journaling (ext2-like)
sudo mkfs.ext4 -O ^has_journal /dev/sdb1

# Enable encryption support
sudo mkfs.ext4 -O encrypt /dev/sdb1

# Set reserved blocks percentage
sudo mkfs.ext4 -m 1 /dev/sdb1

# Custom UUID
sudo mkfs.ext4 -U 12345678-1234-1234-1234-123456789012 /dev/sdb1
```

### 2. XFS File System {.unnumbered}

```bash
# Basic XFS creation
sudo mkfs.xfs /dev/sdb1

# Force creation (overwrite existing)
sudo mkfs.xfs -f /dev/sdb1

# With label
sudo mkfs.xfs -L MyXFS /dev/sdb1

# Custom block size
sudo mkfs.xfs -b size=4096 /dev/sdb1

# Custom sector size
sudo mkfs.xfs -s size=4096 /dev/sdb1
```

### Advanced XFS Options

```bash
# Separate log device
sudo mkfs.xfs -l logdev=/dev/sdc1 /dev/sdb1

# Real-time device
sudo mkfs.xfs -r rtdev=/dev/sdd1 /dev/sdb1

# Custom inode size
sudo mkfs.xfs -i size=512 /dev/sdb1

# Allocation group size
sudo mkfs.xfs -d agcount=8 /dev/sdb1
```

### 3. FAT32 File System {.unnumbered}

```bash
# Basic FAT32 creation
sudo mkfs.fat -F32 /dev/sdb1

# With label
sudo mkfs.fat -F32 -n USBDRIVE /dev/sdb1

# Custom cluster size
sudo mkfs.fat -F32 -s 8 /dev/sdb1

# Custom volume ID
sudo mkfs.fat -F32 -i 12345678 /dev/sdb1
```

### 4. NTFS File System {.unnumbered}

```bash
# Basic NTFS creation
sudo mkfs.ntfs /dev/sdb1

# Quick format
sudo mkfs.ntfs -Q /dev/sdb1

# With label
sudo mkfs.ntfs -L WindowsData /dev/sdb1

# Custom cluster size
sudo mkfs.ntfs -c 4096 /dev/sdb1
```

## 실용적인 예제

### 1. Preparing a USB Drive {.unnumbered}

```bash
# Check device name
lsblk

# Create partition table (if needed)
sudo fdisk /dev/sdc
# or
sudo cfdisk /dev/sdc

# Format with FAT32 for compatibility
sudo mkfs.fat -F32 -n MYUSB /dev/sdc1

# Mount and test
sudo mkdir /mnt/usb
sudo mount /dev/sdc1 /mnt/usb
```

### 2. Setting Up a Data Drive {.unnumbered}

```bash
# Create ext4 with optimal settings
sudo mkfs.ext4 -L DataDrive -b 4096 -m 1 /dev/sdb1

# Create mount point
sudo mkdir /mnt/data

# Add to fstab for automatic mounting
echo "LABEL=DataDrive /mnt/data ext4 defaults 0 2" | sudo tee -a /etc/fstab

# Mount
sudo mount /mnt/data
```

### 3. High-Performance Storage {.unnumbered}

```bash
# XFS for large files and high performance
sudo mkfs.xfs -f -L HighPerf -b size=4096 -d agcount=8 /dev/sdb1

# Or ext4 with performance optimizations
sudo mkfs.ext4 -L HighPerf -b 4096 -E stride=32,stripe-width=64 /dev/sdb1
```

### 4. SSD Optimization {.unnumbered}

```bash
# ext4 for SSD with TRIM support
sudo mkfs.ext4 -L SSD -b 4096 -E discard /dev/sdb1

# XFS for SSD
sudo mkfs.xfs -f -L SSD -K /dev/sdb1

# Add discard option in fstab
# /dev/sdb1 /mnt/ssd ext4 defaults,discard 0 2
```

## 고급 구성

### 1. Large File Systems {.unnumbered}

```bash
# ext4 for very large filesystems
sudo mkfs.ext4 -T largefile4 /dev/sdb1

# XFS with optimizations for large files
sudo mkfs.xfs -f -i size=512 -d agcount=32 /dev/sdb1

# Increase inode ratio for many small files
sudo mkfs.ext4 -T small /dev/sdb1
```

### 2. RAID Configurations {.unnumbered}

```bash
# ext4 for RAID arrays
sudo mkfs.ext4 -b 4096 -E stride=16,stripe-width=32 /dev/md0

# XFS for RAID
sudo mkfs.xfs -f -d su=64k,sw=2 /dev/md0

# Where:
# stride = (chunk-size / block-size)
# stripe-width = (number-of-data-disks * stride)
```

### 3. Encryption Support {.unnumbered}

```bash
# ext4 with encryption
sudo mkfs.ext4 -O encrypt /dev/sdb1

# Setup LUKS encryption first
sudo cryptsetup luksFormat /dev/sdb1
sudo cryptsetup open /dev/sdb1 encrypted_disk
sudo mkfs.ext4 /dev/mapper/encrypted_disk
```

## 백업 및 안전

### 1. 포맷하기 전 확인 {.unnumbered}

```bash
# Verify device
lsblk /dev/sdb
fdisk -l /dev/sdb

# Check for mounted filesystems
mount | grep /dev/sdb
lsof /dev/sdb*

# Backup important data
sudo dd if=/dev/sdb of=/backup/sdb_backup.img bs=1M
```

### 2. 파티션 테이블 백업 {.unnumbered}

```bash
# Backup partition table
sudo sfdisk -d /dev/sdb > sdb_partition_table.txt

# Restore if needed
sudo sfdisk /dev/sdb < sdb_partition_table.txt
```

### 3. 사용 전 테스트 {.unnumbered}

```bash
# Create filesystem
sudo mkfs.ext4 /dev/sdb1

# Mount and test
sudo mkdir /mnt/test
sudo mount /dev/sdb1 /mnt/test

# Basic functionality test
echo "test" | sudo tee /mnt/test/testfile
cat /mnt/test/testfile
sudo rm /mnt/test/testfile

# Unmount
sudo umount /mnt/test
```

## 문제 해결

### 1. 장치 사용 중 오류 {.unnumbered}

```bash
# Check what's using the device
lsof /dev/sdb1
fuser -v /dev/sdb1

# Unmount if mounted
sudo umount /dev/sdb1

# Kill processes if necessary
sudo fuser -k /dev/sdb1
```

### 2. 공간 부족 {.unnumbered}

```bash
# Check available space
fdisk -l /dev/sdb

# Verify partition size
cat /proc/partitions

# Check for existing filesystems
file -s /dev/sdb1
```

### 3. 권한 문제 {.unnumbered}

```bash
# Must run as root
sudo mkfs.ext4 /dev/sdb1

# Check device permissions
ls -l /dev/sdb1

# Add user to disk group if needed
sudo usermod -a -G disk username
```

## 성능 최적화

### 1. 블록 크기 선택 {.unnumbered}

```bash
# For small files (default: 4096)
sudo mkfs.ext4 -b 1024 /dev/sdb1

# For large files
sudo mkfs.ext4 -b 4096 /dev/sdb1

# For very large files (ext4 only)
sudo mkfs.ext4 -b 65536 /dev/sdb1
```

### 2. inode 구성 {.unnumbered}

```bash
# More inodes for many small files
sudo mkfs.ext4 -i 1024 /dev/sdb1

# Fewer inodes for large files
sudo mkfs.ext4 -i 16384 /dev/sdb1

# Fixed number of inodes
sudo mkfs.ext4 -N 1000000 /dev/sdb1
```

### 3. 저널 최적화 {.unnumbered}

```bash
# Large journal for write-heavy workloads
sudo mkfs.ext4 -J size=128 /dev/sdb1

# External journal device
sudo mkfs.ext4 -J device=/dev/sdc1 /dev/sdb1

# Disable journal for read-only media
sudo mkfs.ext4 -O ^has_journal /dev/sdb1
```

## 특수 사용 사례

### 1. 부팅 가능한 미디어 {.unnumbered}

```bash
# FAT32 for UEFI boot
sudo mkfs.fat -F32 -n BOOT /dev/sdb1

# ext4 for Linux boot
sudo mkfs.ext4 -L BOOT /dev/sdb2

# Install bootloader after filesystem creation
```

### 2. 네트워크 저장소 {.unnumbered}

```bash
# XFS for NFS exports
sudo mkfs.xfs -f -L NFSSHARE /dev/sdb1

# ext4 for Samba shares
sudo mkfs.ext4 -L SAMBA /dev/sdb1
```

### 3. 컨테이너 저장소 {.unnumbered}

```bash
# ext4 with specific features for containers
sudo mkfs.ext4 -O project -L CONTAINERS /dev/sdb1

# XFS with project quotas
sudo mkfs.xfs -f -i size=512 /dev/sdb1
```

## 모니터링 및 검증

### 1. 파일 시스템 정보 {.unnumbered}

```bash
# ext4 information
sudo tune2fs -l /dev/sdb1

# XFS information
sudo xfs_info /dev/sdb1

# General filesystem info
df -T /mnt/mountpoint
```

### 2. 상태 확인 {.unnumbered}

```bash
# Check filesystem after creation
sudo fsck -n /dev/sdb1

# Mount and verify
sudo mount /dev/sdb1 /mnt/test
sudo df -h /mnt/test
sudo ls -la /mnt/test
```

## 모범 사례

### 1. 계획 {.unnumbered}

```bash
# Determine optimal filesystem type based on use case:
# - ext4: General purpose, good for most Linux use cases
# - XFS: Large files, high performance, NAS/database storage
# - FAT32: Cross-platform compatibility, small devices
# - NTFS: Windows compatibility
```

### 2. 레이블 지정 {.unnumbered}

```bash
# Always use descriptive labels
sudo mkfs.ext4 -L "SystemData" /dev/sdb1
sudo mkfs.xfs -L "MediaStorage" /dev/sdb2
sudo mkfs.fat -F32 -n "BACKUP" /dev/sdb3
```

### 3. 문서화 {.unnumbered}

```bash
# Document filesystem configuration
echo "Created: $(date)" > /root/filesystem_log.txt
echo "Device: /dev/sdb1" >> /root/filesystem_log.txt
echo "Type: ext4" >> /root/filesystem_log.txt
echo "Label: DataDrive" >> /root/filesystem_log.txt
```

## 중요 참고 사항

- **포맷하기 전에 항상 데이터를** 백업하세요.
- 데이터 손실을 방지하려면 **장치 이름을** 주의 깊게 확인하세요.
- 포맷하기 전에 **파일 시스템을** 마운트 해제하세요.
- 사용 목적에 맞는 **적절한 파일 시스템을** 선택하세요.
- 옵션을 선택할 때 **성능 요구 사항을** 고려하세요.
- 생성한 후 **파일 시스템을** 테스트하세요.
- 나중에 참고할 수 있도록 **구성을** 문서화하세요.

`mkfs` 명령은 저장 장치를 준비하는 기본 도구이며 요구 사항과 안전 절차를 충분히 고려해 사용해야 합니다.

자세한 내용은 매뉴얼을 확인하십시오: `man mkfs`

# `cpio` 명령

`cpio`(copy in, copy out) 명령은 아카이브를 생성하고 추출하며 파일을 복사하고 특수 파일 형식을 처리할 수 있는 다목적 아카이빙 유틸리티입니다. 특히 시스템 백업, initramfs 이미지 생성, 테이프 아카이브 작업에 유용합니다.

## 구문

```
cpio [options] < name-list
cpio [options] -i [patterns] < archive
cpio [options] -o > archive
cpio [options] -p destination-directory
```

## 작동 모드

### 1. Copy-out mode (-o) {.unnumbered}

```bash
# Create archive from file list
find . -name "*.txt" | cpio -o > archive.cpio

# Create compressed archive
find . -type f | cpio -o | gzip > archive.cpio.gz

# Verbose output
find . -name "*.conf" | cpio -ov > config_backup.cpio
```

### 2. Copy-in mode (-i) {.unnumbered}

```bash
# Extract archive
cpio -i < archive.cpio

# Extract to specific directory
cd /restore && cpio -i < /backup/archive.cpio

# Extract specific files
cpio -i "*.txt" < archive.cpio
```

### 3. Pass-through mode (-p) {.unnumbered}

```bash
# Copy files to another directory
find /source -type f | cpio -p /destination

# Preserve attributes while copying
find /etc -name "*.conf" | cpio -pdm /backup/configs
```

## 일반 옵션

### 기본 옵션

```bash
# -o: Copy-out (create archive)
find . | cpio -o > archive.cpio

# -i: Copy-in (extract archive)
cpio -i < archive.cpio

# -p: Pass-through (copy files)
find . | cpio -p /destination

# -v: Verbose output
find . | cpio -ov > archive.cpio

# -t: List archive contents
cpio -tv < archive.cpio
```

### 고급 옵션

```bash
# -d: Create directories as needed
cpio -id < archive.cpio

# -m: Preserve modification times
cpio -im < archive.cpio

# -u: Unconditional extraction (overwrite)
cpio -iu < archive.cpio

# -H: Specify archive format
cpio -oH newc > archive.cpio

# -B: Use 5120-byte blocks
cpio -oB > archive.cpio
```

## 아카이브 형식

### 사용 가능한 형식

```bash
# Binary format (default, obsolete)
cpio -o > archive.cpio

# New ASCII format (recommended)
cpio -oH newc > archive.cpio

# Old ASCII format
cpio -oH odc > archive.cpio

# CRC format
cpio -oH crc > archive.cpio

# TAR format
cpio -oH tar > archive.tar

# USTAR format
cpio -oH ustar > archive.tar
```

## 실용적인 예제

### 1. 시스템 백업 {.unnumbered}

```bash
# Backup entire system (excluding certain directories)
find / -path /proc -prune -o -path /sys -prune -o -path /dev -prune -o -print | \
sudo cpio -oH newc | gzip > system_backup.cpio.gz

# Backup specific directories
find /etc /home /var -type f | cpio -oH newc > important_files.cpio

# Backup with verbose output
find /home/user -type f | cpio -ovH newc > user_backup.cpio
```

### 2. initramfs 생성 {.unnumbered}

```bash
# Create initramfs image (common in Linux boot process)
cd /tmp/initramfs
find . | cpio -oH newc | gzip > /boot/initramfs.img

# Extract existing initramfs
cd /tmp/extract
zcat /boot/initramfs.img | cpio -id
```

### 3. 선택적 파일 작업 {.unnumbered}

```bash
# Archive only configuration files
find /etc -name "*.conf" -o -name "*.cfg" | cpio -oH newc > configs.cpio

# Archive files modified in last 7 days
find /home -type f -mtime -7 | cpio -oH newc > recent_files.cpio

# Archive files larger than 1MB
find /var/log -type f -size +1M | cpio -oH newc > large_logs.cpio
```

### 4. 압축 아카이브 작업 {.unnumbered}

```bash
# Create compressed archive
find . -type f | cpio -oH newc | gzip > archive.cpio.gz
find . -type f | cpio -oH newc | bzip2 > archive.cpio.bz2
find . -type f | cpio -oH newc | xz > archive.cpio.xz

# Extract compressed archives
zcat archive.cpio.gz | cpio -id
bzcat archive.cpio.bz2 | cpio -id
xzcat archive.cpio.xz | cpio -id
```

## 특수 파일 형식

### 1. Device Files and Special Files {.unnumbered}

```bash
# Archive including device files
find /dev -type c -o -type b | sudo cpio -oH newc > device_files.cpio

# Archive symbolic links
find . -type l | cpio -oH newc > symlinks.cpio

# Archive with all file types preserved
find . | sudo cpio -oH newc > complete_backup.cpio
```

### 2. 속성 보존 {.unnumbered}

```bash
# Preserve ownership and permissions
find . | sudo cpio -oH newc > backup.cpio
sudo cpio -idm < backup.cpio

# Preserve modification times
cpio -im < archive.cpio

# Preserve all attributes in pass-through mode
find /source | sudo cpio -pdm /destination
```

## 파일 필터링 및 선택

### 1. 패턴 일치 {.unnumbered}

```bash
# Extract specific file patterns
cpio -i "*.txt" "*.conf" < archive.cpio

# Extract files in specific directory
cpio -i "etc/*" < archive.cpio

# Extract with shell globbing
cpio -i "*backup*" < archive.cpio
```

### 2. 제외 패턴 {.unnumbered}

```bash
# Create archive excluding certain files
find . -type f ! -name "*.tmp" ! -name "*.log" | cpio -oH newc > clean_archive.cpio

# Use grep to filter
find . -type f | grep -v -E '\.(tmp|log|cache)$' | cpio -oH newc > filtered.cpio
```

## 네트워크 작업

### 1. 원격 백업 {.unnumbered}

```bash
# Send archive over network
find /home | cpio -oH newc | ssh user@remote "cat > backup.cpio"

# Compressed network backup
find /data | cpio -oH newc | gzip | ssh user@remote "cat > data_backup.cpio.gz"

# Receive archive from network
ssh user@remote "cat backup.cpio" | cpio -id
```

### 2. 테이프 작업 {.unnumbered}

```bash
# Write to tape device
find /home | cpio -oH newc > /dev/st0

# Read from tape
cpio -itv < /dev/st0  # List contents
cpio -id < /dev/st0   # Extract

# Multi-volume archives
find /large_dataset | cpio -oH newc --split-at=700M > /dev/st0
```

## 아카이브 관리

### 1. 아카이브 내용 나열 {.unnumbered}

```bash
# List all files in archive
cpio -tv < archive.cpio

# List with detailed information
cpio -itv < archive.cpio

# Count files in archive
cpio -it < archive.cpio | wc -l

# Search for specific files
cpio -it < archive.cpio | grep "filename"
```

### 2. 아카이브 검증 {.unnumbered}

```bash
# Test archive integrity
cpio -it < archive.cpio > /dev/null

# Compare with filesystem
find . | cpio -oH newc | cpio -it | sort > archive_list.txt
find . | sort > filesystem_list.txt
diff archive_list.txt filesystem_list.txt
```

### 3. 아카이브 업데이트 {.unnumbered}

```bash
# Append to existing archive (not directly supported)
# Workaround: extract, add files, recreate
mkdir /tmp/archive_work
cd /tmp/archive_work
cpio -id < /path/to/archive.cpio
# Add new files
find . | cpio -oH newc > /path/to/new_archive.cpio
```

## 성능 최적화

### 1. 블록 사이즈 최적화 {.unnumbered}

```bash
# Use larger block size for better performance
cpio -oB > archive.cpio          # 5120 bytes
cpio -o --block-size=32768 > archive.cpio  # 32KB blocks

# For tape drives
cpio -o --block-size=65536 > /dev/st0  # 64KB blocks
```

### 2. 압축 전략 {.unnumbered}

```bash
# Different compression methods
find . | cpio -oH newc | gzip -1 > fast_compress.cpio.gz    # Fast
find . | cpio -oH newc | gzip -9 > best_compress.cpio.gz    # Best ratio
find . | cpio -oH newc | lz4 > lz4_compress.cpio.lz4        # Very fast
find . | cpio -oH newc | xz -9 > xz_compress.cpio.xz        # Best ratio
```

### 3. 병렬 처리 {.unnumbered}

```bash
# Use parallel compression
find . | cpio -oH newc | pigz > parallel_compressed.cpio.gz

# Parallel decompression
pigz -dc archive.cpio.gz | cpio -id
```

## 다른 도구와 통합

### 1. `find`와 같이 쓰기 {.unnumbered}

```bash
# Complex find expressions
find /var/log -name "*.log" -size +10M -mtime +30 | cpio -oH newc > old_large_logs.cpio

# Execute commands during find
find . -name "*.txt" -exec grep -l "important" {} \; | cpio -oH newc > important_texts.cpio
```

### 2. `rsync`와 같이 쓰기 {.unnumbered}

```bash
# Create incremental backups
rsync -av --link-dest=/backup/previous /source/ /backup/current/
find /backup/current -type f | cpio -oH newc > incremental.cpio
```

### 3. `tar`과 같이 쓰기 {.unnumbered}

```bash
# Convert tar to cpio
tar -cf - files | cpio -oH tar > archive.tar

# Convert cpio to tar
cpio -it < archive.cpio | tar -cf archive.tar -T -
```

## 문제 해결

### 1. 흔한 오류 {.unnumbered}

```bash
# "Premature end of file" error
# Check if archive is complete or corrupted
file archive.cpio

# Permission denied errors
# Use sudo for system files
sudo cpio -id < archive.cpio

# "Cannot create directory" errors
# Use -d option
cpio -id < archive.cpio
```

### 2. 디버깅 {.unnumbered}

```bash
# Verbose mode for debugging
cpio -idv < archive.cpio

# Check archive format
file archive.cpio
hexdump -C archive.cpio | head

# Test archive before extraction
cpio -it < archive.cpio > /dev/null
echo $?  # Should return 0 for success
```

### 3. 복구 {.unnumbered}

```bash
# Partial archive recovery
dd if=damaged_archive.cpio of=partial.cpio bs=512 count=1000
cpio -id < partial.cpio

# Skip damaged portions
cpio -id --only-verify-crc < archive.cpio
```

## 스크립팅 및 자동화

### 1. 백업 스크립트 {.unnumbered}

```bash
#!/bin/bash
# Automated backup script
BACKUP_DIR="/backup/$(date +%Y%m%d)"
SOURCE="/home /etc /var/log"

mkdir -p "$BACKUP_DIR"
for dir in $SOURCE; do
    find "$dir" -type f | cpio -oH newc | gzip > "$BACKUP_DIR/$(basename $dir).cpio.gz"
done
```

### 2. 복원 스크립트 {.unnumbered}

```bash
#!/bin/bash
# Automated restoration script
ARCHIVE="$1"
DEST="${2:-/restore}"

if [ ! -f "$ARCHIVE" ]; then
    echo "Archive not found: $ARCHIVE"
    exit 1
fi

mkdir -p "$DEST"
cd "$DEST"

if [[ "$ARCHIVE" =~ \.gz$ ]]; then
    zcat "$ARCHIVE" | cpio -idm
else
    cpio -idm < "$ARCHIVE"
fi
```

## 모범 사례

### 1. 아카이브 이름 지정 {.unnumbered}

```bash
# Use descriptive names with dates
backup_$(hostname)_$(date +%Y%m%d_%H%M%S).cpio.gz

# Include source information
home_backup_$(date +%Y%m%d).cpio
etc_configs_$(date +%Y%m%d).cpio
```

### 2. 검증 {.unnumbered}

```bash
# Always verify critical archives
find /important | cpio -oH newc > critical.cpio
cpio -it < critical.cpio | wc -l
find /important | wc -l
```

### 3. 문서화 {.unnumbered}

```bash
# Document archive contents
cpio -itv < archive.cpio > archive_contents.txt
echo "Created: $(date)" >> archive_info.txt
echo "Source: /path/to/source" >> archive_info.txt
```

## 중요 참고 사항

- **적절한 형식 선택**: 최신 시스템에는 `newc` 형식을 사용하세요.
- **권한 보존**: `-m` 옵션을 사용하고 적절한 사용자 권한으로 실행하세요.
- **아카이브 테스트**: 항상 아카이브의 무결성을 확인하세요.
- **압축 사용**: 공간을 효율적으로 사용하려면 gzip, bzip2 또는 xz와 함께 사용하세요.
- **특수 파일 처리**: 장치 파일과 심볼릭 링크를 주의해서 다루세요.
- **네트워크 보안**: 원격 작업에는 안전한 통신 채널을 사용하세요.
- **백업 전략**: 정기적으로 백업하고 백업 상태를 확인하세요.
`cpio` 명령은 유연한 아카이브와 시스템 백업을 만드는 강력한 도구이며 특히 find 및 압축 도구와 함께 사용할 때 유용합니다.

자세한 내용은 매뉴얼을 확인하십시오: `man cpio`

# `lsb_release` 명령

`lsb_release` 명령어는 Linux Standard Base(LSB) 정보와 배포판별 정보를 표시합니다. Linux 배포판의 버전, 코드네임 및 기타 식별 정보를 제공합니다. [en.wikipedia](https://en.wikipedia.org/wiki/Linux_Standard_Base)

## 구문

```
lsb_release [options]
```

## 주요 기능

- **배포판 정보**: 이름, 버전, 코드명
- **LSB 호환성**: LSB 버전 지원 여부 표시
- **표준 형식**: 배포판 전반에서 일관된 출력
- **스크립팅에 적합**: 출력을 쉽게 파싱할 수 있음

## 기본 사용법

### Show All Information

```bash
# Display all available information
lsb_release -a

# Example output:
# Distributor ID: Ubuntu
# Description:    Ubuntu 22.04.1 LTS
# Release:        22.04
# Codename:       jammy
```

### Individual Information

```bash
# Distribution ID only
lsb_release -i

# Release version only
lsb_release -r

# Codename only
lsb_release -c

# Description only
lsb_release -d
```

## 일반 옵션

### 기본 옵션

```bash
# -a: Show all information
lsb_release -a

# -i: Distributor ID
lsb_release -i

# -d: Description
lsb_release -d

# -r: Release number
lsb_release -r

# -c: Codename
lsb_release -c
```

### Output Format Options

```bash
# -s: Short format (no field names)
lsb_release -a -s

# Example output:
# Ubuntu
# Ubuntu 22.04.1 LTS
# 22.04
# jammy

# Individual fields in short format
lsb_release -i -s  # Output: Ubuntu
lsb_release -r -s  # Output: 22.04
lsb_release -c -s  # Output: jammy
```

## 실용적인 예제

### 1. 시스템 식별 {.unnumbered}

```bash
# Get distribution name
DISTRO=$(lsb_release -i -s)
echo "Running on: $DISTRO"

# Get version
VERSION=$(lsb_release -r -s)
echo "Version: $VERSION"

# Get codename
CODENAME=$(lsb_release -c -s)
echo "Codename: $CODENAME"
```

### 2. 조건부 스크립팅 {.unnumbered}

```bash
#!/bin/bash
# Script that behaves differently based on distribution

DISTRO=$(lsb_release -i -s)
VERSION=$(lsb_release -r -s)

case $DISTRO in
    "Ubuntu")
        echo "Ubuntu detected, version $VERSION"
        if [[ "$VERSION" == "22.04" ]]; then
            echo "Running on Ubuntu 22.04 LTS"
        fi
        ;;
    "Debian")
        echo "Debian detected, version $VERSION"
        ;;
    "CentOS"|"RedHatEnterprise")
        echo "Red Hat based system detected"
        ;;
    *)
        echo "Unknown distribution: $DISTRO"
        ;;
esac
```

### 3. 패키지 관리 스크립트 {.unnumbered}

```bash
#!/bin/bash
# Install packages based on distribution

install_package() {
    local package=$1
    local distro=$(lsb_release -i -s)

    case $distro in
        "Ubuntu"|"Debian")
            sudo apt-get install -y "$package"
            ;;
        "CentOS"|"RedHatEnterprise")
            sudo yum install -y "$package"
            ;;
        "Fedora")
            sudo dnf install -y "$package"
            ;;
        *)
            echo "Unsupported distribution: $distro"
            return 1
            ;;
    esac
}

install_package "curl"
```

## 배포판 특이적인 예제

### 1. Ubuntu/Debian 시스템 {.unnumbered}

```bash
# Check Ubuntu version
lsb_release -a
# Distributor ID: Ubuntu
# Description:    Ubuntu 22.04.1 LTS
# Release:        22.04
# Codename:       jammy

# Check if it's LTS version
if lsb_release -d -s | grep -q "LTS"; then
    echo "This is an LTS release"
fi
```

### 2. CentOS/RHEL 시스템 {.unnumbered}

```bash
# CentOS example
lsb_release -a
# Distributor ID: CentOS
# Description:    CentOS Linux release 8.4.2105
# Release:        8.4.2105
# Codename:       n/a

# Check major version
MAJOR_VERSION=$(lsb_release -r -s | cut -d. -f1)
echo "Major version: $MAJOR_VERSION"
```

### 3. Fedora 시스템 {.unnumbered}

```bash
# Fedora example
lsb_release -a
# Distributor ID: Fedora
# Description:    Fedora release 36 (Thirty Six)
# Release:        36
# Codename:       ThirtySix
```

## 대안 정보 소스

### 1. lsb_release를 사용할 수 없을 때 {.unnumbered}

```bash
# Check if lsb_release exists
if command -v lsb_release >/dev/null 2>&1; then
    lsb_release -a
else
    echo "lsb_release not available, using alternatives:"

    # Try /etc/os-release (systemd standard)
    if [ -f /etc/os-release ]; then
        cat /etc/os-release
    fi

    # Try distribution-specific files
    if [ -f /etc/redhat-release ]; then
        cat /etc/redhat-release
    elif [ -f /etc/debian_version ]; then
        echo "Debian $(cat /etc/debian_version)"
    elif [ -f /etc/issue ]; then
        cat /etc/issue
    fi
fi
```

### 2. /etc/os-release 사용 {.unnumbered}

```bash
# Modern alternative to lsb_release
cat /etc/os-release

# Example output:
# PRETTY_NAME="Ubuntu 22.04.1 LTS"
# NAME="Ubuntu"
# VERSION_ID="22.04"
# VERSION="22.04.1 LTS (Jammy Jellyfish)"
# ID=ubuntu
# ID_LIKE=debian
# HOME_URL="https://www.ubuntu.com/"
# SUPPORT_URL="https://help.ubuntu.com/"

# Parse specific values
source /etc/os-release
echo "Distribution: $NAME"
echo "Version: $VERSION_ID"
echo "Pretty Name: $PRETTY_NAME"
```

## 스크립팅 및 자동화

### 1. System Information Script {.unnumbered}

```bash
#!/bin/bash
# Comprehensive system information

echo "=== System Information ==="
echo "Date: $(date)"
echo "Hostname: $(hostname)"
echo "Uptime: $(uptime)"
echo

echo "=== Distribution Information ==="
if command -v lsb_release >/dev/null 2>&1; then
    lsb_release -a
else
    echo "lsb_release not available"
    if [ -f /etc/os-release ]; then
        echo "Using /etc/os-release:"
        cat /etc/os-release
    fi
fi
echo

echo "=== Kernel Information ==="
uname -a
echo

echo "=== Hardware Information ==="
lscpu | head -10
echo
free -h
echo
df -h
```

### 2. Environment Setup Script {.unnumbered}

```bash
#!/bin/bash
# Setup development environment based on distribution

DISTRO=$(lsb_release -i -s 2>/dev/null || echo "Unknown")
VERSION=$(lsb_release -r -s 2>/dev/null || echo "Unknown")

echo "Setting up environment for $DISTRO $VERSION"

case $DISTRO in
    "Ubuntu")
        echo "Configuring for Ubuntu..."
        sudo apt update
        sudo apt install -y build-essential git curl

        if [[ "$VERSION" == "22.04" ]]; then
            echo "Ubuntu 22.04 specific setup..."
            # Add 22.04 specific configurations
        fi
        ;;
    "Debian")
        echo "Configuring for Debian..."
        sudo apt update
        sudo apt install -y build-essential git curl
        ;;
    "CentOS"|"RedHatEnterprise")
        echo "Configuring for Red Hat based system..."
        sudo yum groupinstall -y "Development Tools"
        sudo yum install -y git curl
        ;;
    *)
        echo "Unknown or unsupported distribution: $DISTRO"
        echo "Manual configuration required"
        ;;
esac
```

### 3. Version Comparison {.unnumbered}

```bash
#!/bin/bash
# Compare distribution versions

check_minimum_version() {
    local current_version=$(lsb_release -r -s)
    local minimum_version=$1
    local distro=$(lsb_release -i -s)

    echo "Current $distro version: $current_version"
    echo "Minimum required version: $minimum_version"

    if [ "$(printf '%s\n' "$minimum_version" "$current_version" | sort -V | head -n1)" = "$minimum_version" ]; then
        echo "Version requirement satisfied"
        return 0
    else
        echo "Version requirement NOT satisfied"
        return 1
    fi
}

# Check if Ubuntu 20.04 or later
if [[ "$(lsb_release -i -s)" == "Ubuntu" ]]; then
    check_minimum_version "20.04"
fi
```

## 설정 관리 도구와의 통합

### 1. Ansible Facts {.unnumbered}

```bash
# lsb_release information is often used in Ansible
# Example Ansible task:

# - name: Install package on Ubuntu
#   apt:
#     name: nginx
#     state: present
#   when: ansible_lsb.id == "Ubuntu"

# - name: Install package on CentOS
#   yum:
#     name: nginx
#     state: present
#   when: ansible_lsb.id == "CentOS"
```

### 2. Docker Integration {.unnumbered}

```bash
#!/bin/bash
# Create Dockerfile based on host distribution

HOST_DISTRO=$(lsb_release -i -s)
HOST_VERSION=$(lsb_release -r -s)

cat > Dockerfile << EOF
# Generated Dockerfile based on host: $HOST_DISTRO $HOST_VERSION
FROM ${HOST_DISTRO,,}:$HOST_VERSION

RUN apt-get update && apt-get install -y \\
    curl \\
    wget \\
    git

# Add application-specific configurations
EOF

echo "Dockerfile generated for $HOST_DISTRO $HOST_VERSION"
```

## 문제 해결

### 1. Command Not Found {.unnumbered}

```bash
# Install lsb_release if missing

# Ubuntu/Debian
sudo apt-get install lsb-release

# CentOS/RHEL 7
sudo yum install redhat-lsb-core

# CentOS/RHEL 8/Fedora
sudo dnf install redhat-lsb-core

# Alternative: use built-in files
cat /etc/os-release
```

### 2. Inconsistent Output {.unnumbered}

```bash
# Some distributions may not fully populate LSB information
# Use fallback methods

get_distro_info() {
    if command -v lsb_release >/dev/null 2>&1; then
        echo "Distribution: $(lsb_release -i -s)"
        echo "Version: $(lsb_release -r -s)"
        echo "Codename: $(lsb_release -c -s)"
    else
        echo "Using alternative detection methods..."
        if [ -f /etc/os-release ]; then
            source /etc/os-release
            echo "Distribution: $NAME"
            echo "Version: $VERSION_ID"
        fi
    fi
}
```

### 3. Parsing Issues {.unnumbered}

```bash
# Handle cases where information might be incomplete
parse_distro_safe() {
    local distro=$(lsb_release -i -s 2>/dev/null)
    local version=$(lsb_release -r -s 2>/dev/null)

    if [ -z "$distro" ]; then
        distro="Unknown"
    fi

    if [ -z "$version" ]; then
        version="Unknown"
    fi

    echo "Distribution: $distro"
    echo "Version: $version"
}
```

## 최신 대안 명령어

### 1. hostnamectl (systemd) {.unnumbered}

```bash
# Modern systemd-based alternative
hostnamectl

# Example output:
#    Static hostname: ubuntu-server
#           Icon name: computer-vm
#             Chassis: vm
#          Machine ID: 12345...
#             Boot ID: 67890...
#      Virtualization: vmware
#    Operating System: Ubuntu 22.04.1 LTS
#              Kernel: Linux 5.15.0-58-generic
#        Architecture: x86-64
```

### 2. /etc/os-release {.unnumbered}

```bash
# Standard file across modern distributions
cat /etc/os-release

# Parse specific fields
grep '^NAME=' /etc/os-release | cut -d= -f2 | tr -d '"'
grep '^VERSION_ID=' /etc/os-release | cut -d= -f2 | tr -d '"'
```

## 모범 사례

### 1. 오류 처리 {.unnumbered}

```bash
# Always check if command exists before using
if ! command -v lsb_release >/dev/null 2>&1; then
    echo "lsb_release not available, using fallback method"
    # Use alternative method
fi
```

### 2. 결과 캐싱 {.unnumbered}

```bash
# Cache results in scripts that call lsb_release multiple times
DISTRO_INFO=$(lsb_release -a 2>/dev/null)
DISTRO_ID=$(echo "$DISTRO_INFO" | awk '/Distributor ID:/ {print $3}')
DISTRO_VERSION=$(echo "$DISTRO_INFO" | awk '/Release:/ {print $2}')
```

### 3. 플랫폼 간 호환성 {.unnumbered}

```bash
# Write scripts that work across different distributions
detect_os() {
    if [ -f /etc/os-release ]; then
        source /etc/os-release
        echo "$NAME $VERSION_ID"
    elif command -v lsb_release >/dev/null 2>&1; then
        lsb_release -d -s
    elif [ -f /etc/redhat-release ]; then
        cat /etc/redhat-release
    else
        echo "Unknown"
    fi
}
```

## 중요 참고 사항

- **설치 필요**: `lsb_release`는 기본적으로 설치되어 있지 않을 수 있습니다.
- **LSB 표준**: Linux Standard Base 사양을 따릅니다.
- **배포판별 차이**: 출력 형식은 Linux 배포판에 따라 다를 수 있습니다.
- **스크립트 활용**: 배포판을 구분해야 하는 스크립트에 매우 유용합니다.
- **최신 대안**: 최신 시스템에서는 `/etc/os-release` 사용을 고려하세요.
- **대체 방법**: 항상 다른 확인 방법도 마련해 두세요.

`lsb_release` 명령은 배포판을 인식하는 스크립트와 시스템 식별에 필수적입니다.

자세한 내용은 매뉴얼을 확인하십시오: `man lsb_release`# `dmidecode` 명령

`dmidecode` 명령은 DMI(Desktop Management Interface) 테이블(시스템 관리 BIOS, SMBIOS(System Management BIOS)라고도 함)에서 하드웨어 정보를 가져오는 데 사용됩니다. 메인보드, CPU, RAM, BIOS 및 기타 시스템 구성 요소를 비롯한 시스템 하드웨어에 대한 상세 정보를 제공합니다.

## 구문

```
dmidecode [options] [type]
```

## 주요 기능

- **하드웨어 정보**: CPU, 메모리, 메인보드, BIOS 세부 정보
- **SMBIOS 액세스**: 시스템 관리 정보에 직접 액세스
- **구조화된 출력**: 형식이 잘 갖춰진 하드웨어 인벤토리
- **시스템 식별**: 일련 번호, 모델 정보
- **루트 권한 불필요**: 모든 사용자가 기본 정보에 액세스 가능

## 기본 사용법

### 모든 정보 표시

```bash
# Display all available hardware information
sudo dmidecode

# Show specific information types
sudo dmidecode -t system
sudo dmidecode -t processor
sudo dmidecode -t memory
```

### 일반적인 정보 유형

```bash
# BIOS information
sudo dmidecode -t bios

# System information
sudo dmidecode -t system

# Base board (motherboard) information
sudo dmidecode -t baseboard

# Chassis information
sudo dmidecode -t chassis

# Processor information
sudo dmidecode -t processor

# Memory information
sudo dmidecode -t memory
```

## 정보 유형

### 숫자 유형 코드

```bash
# Type 0: BIOS Information
sudo dmidecode -t 0

# Type 1: System Information
sudo dmidecode -t 1

# Type 2: Base Board Information
sudo dmidecode -t 2

# Type 3: Chassis Information
sudo dmidecode -t 3

# Type 4: Processor Information
sudo dmidecode -t 4

# Type 16: Physical Memory Array
sudo dmidecode -t 16

# Type 17: Memory Device
sudo dmidecode -t 17
```

### 문자열 유형 이름

```bash
# BIOS information
sudo dmidecode -t bios

# System information (manufacturer, model, serial)
sudo dmidecode -t system

# Motherboard information
sudo dmidecode -t baseboard

# Case/chassis information
sudo dmidecode -t chassis

# CPU information
sudo dmidecode -t processor

# Memory information
sudo dmidecode -t memory

# Slot information
sudo dmidecode -t slot

# Cache information
sudo dmidecode -t cache
```

## 실전 예제

### 1. 시스템 식별 {.unnumbered}

```bash
# Get system manufacturer and model
sudo dmidecode -s system-manufacturer
sudo dmidecode -s system-product-name
sudo dmidecode -s system-serial-number

# Example output:
# Dell Inc.
# OptiPlex 7090
# 1234567

# Complete system information
sudo dmidecode -t system
```

### 2. 하드웨어 인벤토리 {.unnumbered}

```bash
# CPU information
echo "=== CPU Information ==="
sudo dmidecode -t processor | grep -E "(Family|Model|Speed|Cores|Threads)"

# Memory information
echo "=== Memory Information ==="
sudo dmidecode -t memory | grep -E "(Size|Speed|Manufacturer|Type)"

# Motherboard information
echo "=== Motherboard Information ==="
sudo dmidecode -t baseboard | grep -E "(Manufacturer|Product|Version)"
```

### 3. 메모리 세부 정보 {.unnumbered}

```bash
# Total memory slots and usage
echo "Memory Slots:"
sudo dmidecode -t memory | grep -E "(Locator|Size|Speed)" | grep -v "Bank Locator"

# Maximum memory capacity
echo "Maximum Memory Capacity:"
sudo dmidecode -t 16 | grep "Maximum Capacity"

# Memory module details
echo "Installed Memory Modules:"
sudo dmidecode -t 17 | grep -E "(Size|Speed|Manufacturer|Part Number)" | grep -v "No Module"
```

## 특정 하드웨어 정보

### 1. BIOS 정보 {.unnumbered}

```bash
# BIOS details
sudo dmidecode -t bios

# Specific BIOS information
sudo dmidecode -s bios-vendor
sudo dmidecode -s bios-version
sudo dmidecode -s bios-release-date

# Example output:
# American Megatrends Inc.
# 2.15.1237
# 03/15/2023
```

### 2. CPU 정보 {.unnumbered}

```bash
# Processor details
sudo dmidecode -t processor

# Specific CPU information
echo "CPU Family: $(sudo dmidecode -s processor-family)"
echo "CPU Manufacturer: $(sudo dmidecode -s processor-manufacturer)"
echo "CPU Version: $(sudo dmidecode -s processor-version)"

# CPU specifications
sudo dmidecode -t processor | grep -E "(Version|Family|Max Speed|Current Speed|Core Count|Thread Count)"
```

### 3. 메모리 정보 {.unnumbered}

```bash
# Memory array information
sudo dmidecode -t 16

# Individual memory modules
sudo dmidecode -t 17

# Memory summary
echo "Total Memory Slots:"
sudo dmidecode -t 17 | grep "Locator:" | wc -l

echo "Occupied Memory Slots:"
sudo dmidecode -t 17 | grep "Size:" | grep -v "No Module" | wc -l

echo "Total Installed Memory:"
sudo dmidecode -t 17 | grep "Size:" | grep -v "No Module" | awk '{sum+=$2} END {print sum " MB"}'
```

### 4. 메인보드 정보 {.unnumbered}

```bash
# Motherboard details
sudo dmidecode -t baseboard

# Specific motherboard information
echo "Motherboard Manufacturer: $(sudo dmidecode -s baseboard-manufacturer)"
echo "Motherboard Model: $(sudo dmidecode -s baseboard-product-name)"
echo "Motherboard Version: $(sudo dmidecode -s baseboard-version)"
echo "Motherboard Serial: $(sudo dmidecode -s baseboard-serial-number)"
```

## 고급 사용법

### 1. 파싱 및 필터링 {.unnumbered}

```bash
# Extract specific information using grep and awk
get_memory_info() {
    sudo dmidecode -t 17 | awk '
    /Memory Device/,/^$/ {
        if (/Size:/) size=$2" "$3
        if (/Speed:/) speed=$2" "$3
        if (/Manufacturer:/) manufacturer=$2
        if (/Part Number:/) part=$3
        if (/Locator:/ && !/Bank/) {
            locator=$2
            if (size != "No Module") {
                print locator": "size" @ "speed" ("manufacturer" "part")"
            }
        }
    }'
}

get_memory_info
```

### 2. 시스템 자산 정보 {.unnumbered}

```bash
# Create system asset report
create_asset_report() {
    echo "=== System Asset Report ==="
    echo "Generated: $(date)"
    echo
 
     echo "System Information:"
     echo "  Manufacturer: $(sudo dmidecode -s system-manufacturer)"
     echo "  Model: $(sudo dmidecode -s system-product-name)"
     echo "  Serial Number: $(sudo dmidecode -s system-serial-number)"
     echo "  UUID: $(sudo dmidecode -s system-uuid)"
     echo
 
     echo "BIOS Information:"
     echo "  Vendor: $(sudo dmidecode -s bios-vendor)"
     echo "  Version: $(sudo dmidecode -s bios-version)"
     echo "  Date: $(sudo dmidecode -s bios-release-date)"
     echo
 
     echo "Motherboard Information:"
     echo "  Manufacturer: $(sudo dmidecode -s baseboard-manufacturer)"
     echo "  Model: $(sudo dmidecode -s baseboard-product-name)"
     echo "  Serial: $(sudo dmidecode -s baseboard-serial-number)"
     echo
 
     echo "Chassis Information:"
     echo "  Type: $(sudo dmidecode -s chassis-type)"
     echo "  Manufacturer: $(sudo dmidecode -s chassis-manufacturer)"
     echo "  Serial: $(sudo dmidecode -s chassis-serial-number)"
 }
 
 create_asset_report > system_asset_report.txt
 ```
 
### 3. 하드웨어 검증 {.unnumbered}
 
 ```bash
 # Validate hardware configuration
 validate_hardware() {
     echo "=== Hardware Validation ==="
 
     # Check if virtualization is supported
     if sudo dmidecode -t processor | grep -q "VMX\|SVM"; then
         echo "✓ Virtualization supported"
     else
         echo "✗ Virtualization not supported"
     fi
 
     # Check memory configuration
     local total_slots=$(sudo dmidecode -t 17 | grep "Locator:" | wc -l)
     local used_slots=$(sudo dmidecode -t 17 | grep "Size:" | grep -v "No Module" | wc -l)
     echo "Memory: $used_slots/$total_slots slots used"
 
     # Check for ECC memory
     if sudo dmidecode -t 17 | grep -q "Error Correction Type.*ECC"; then
         echo "✓ ECC memory detected"
     else
         echo "ℹ No ECC memory detected"
     fi
 
     # Check system age (approximate)
     local bios_date=$(sudo dmidecode -s bios-release-date)
     local year=$(echo $bios_date | awk -F'/' '{print $3}')
     local current_year=$(date +%Y)
     local age=$((current_year - year))
     echo "Approximate system age: $age years"
 }
 
 validate_hardware
 ```
 
## 문자열 옵션
 
### 사용 가능한 문자열 옵션
 
 ```bash
 # System strings
 sudo dmidecode -s system-manufacturer
 sudo dmidecode -s system-product-name
 sudo dmidecode -s system-version
 sudo dmidecode -s system-serial-number
 sudo dmidecode -s system-uuid
 
 # BIOS strings
 sudo dmidecode -s bios-vendor
 sudo dmidecode -s bios-version
 sudo dmidecode -s bios-release-date
 
 # Baseboard strings
 sudo dmidecode -s baseboard-manufacturer
 sudo dmidecode -s baseboard-product-name
 sudo dmidecode -s baseboard-version
 sudo dmidecode -s baseboard-serial-number
 
 # Chassis strings
 sudo dmidecode -s chassis-manufacturer
 sudo dmidecode -s chassis-type
 sudo dmidecode -s chassis-version
 sudo dmidecode -s chassis-serial-number
 
 # Processor strings
 sudo dmidecode -s processor-family
 sudo dmidecode -s processor-manufacturer
 sudo dmidecode -s processor-version
 sudo dmidecode -s processor-frequency
 ```
 
## 출력 옵션
 
### 형식 지정 옵션
 
 ```bash
 # Quiet output (suppress headers)
 sudo dmidecode -q -t system
 
 # No piping warning
 sudo dmidecode --no-sysfs -t memory
 
 # Dump raw DMI data
 sudo dmidecode --dump-bin dmi.bin
 
 # Read from binary dump
 dmidecode --from-dump dmi.bin
 ```
 
## 스크립팅 및 자동화
 
### 1. 하드웨어 모니터링 스크립트 {.unnumbered}
 
 ```bash
 #!/bin/bash
 # Monitor hardware changes
 
 HARDWARE_LOG="/var/log/hardware_changes.log"
 CURRENT_STATE="/tmp/current_hardware.txt"
 PREVIOUS_STATE="/var/lib/hardware_baseline.txt"
 
 # Generate current hardware state
 {
     echo "=== Hardware State $(date) ==="
     sudo dmidecode -s system-serial-number
     sudo dmidecode -t memory | grep -E "(Size|Speed)" | grep -v "No Module"
     sudo dmidecode -t processor | grep -E "(Version|Speed|Cores)"
 } > "$CURRENT_STATE"
 
 # Compare with previous state
 if [ -f "$PREVIOUS_STATE" ]; then
     if ! diff -q "$PREVIOUS_STATE" "$CURRENT_STATE" >/dev/null; then
         echo "$(date): Hardware configuration changed" >> "$HARDWARE_LOG"
         diff "$PREVIOUS_STATE" "$CURRENT_STATE" >> "$HARDWARE_LOG"
     fi
 fi
 
 # Update baseline
 cp "$CURRENT_STATE" "$PREVIOUS_STATE"
 ```
 
### 2. 인벤토리 수집 {.unnumbered}
 
 ```bash
 #!/bin/bash
 # Collect hardware inventory for multiple systems
 
 collect_inventory() {
     local hostname=$(hostname)
     local output_file="inventory_${hostname}_$(date +%Y%m%d).txt"
 
     {
         echo "Hardware Inventory for $hostname"
         echo "Generated: $(date)"
         echo "================================="
         echo
 
         echo "System Information:"
         sudo dmidecode -t system | grep -E "(Manufacturer|Product|Serial|UUID)"
         echo
 
         echo "BIOS Information:"
         sudo dmidecode -t bios | grep -E "(Vendor|Version|Release Date)"
         echo
 
         echo "Memory Configuration:"
         sudo dmidecode -t memory | grep -E "(Maximum Capacity|Number Of Devices)"
         sudo dmidecode -t 17 | grep -E "(Locator|Size|Speed|Manufacturer)" | grep -v "Bank Locator"
         echo
 
         echo "Processor Information:"
         sudo dmidecode -t processor | grep -E "(Family|Version|Speed|Core Count|Thread Count)"
         echo
 
         echo "Motherboard Information:"
         sudo dmidecode -t baseboard | grep -E "(Manufacturer|Product|Version|Serial)"
 
     } > "$output_file"
 
     echo "Inventory saved to: $output_file"
 }
 
 collect_inventory
 ```
 
### 3. 라이선스 관리 {.unnumbered}
 
 ```bash
 #!/bin/bash
 # Extract information for software licensing
 
 get_license_info() {
     echo "System Identification for Licensing:"
     echo "UUID: $(sudo dmidecode -s system-uuid)"
     echo "Serial: $(sudo dmidecode -s system-serial-number)"
     echo "Manufacturer: $(sudo dmidecode -s system-manufacturer)"
     echo "Model: $(sudo dmidecode -s system-product-name)"
 
     # Generate unique system fingerprint
     local fingerprint=$(sudo dmidecode -s system-uuid)$(sudo dmidecode -s baseboard-serial-number)
     echo "System Fingerprint: $(echo -n "$fingerprint" | sha256sum | cut -d' ' -f1)"
 }
 
 get_license_info
 ```
 
## 문제 해결
 
### 1. 권한 문제 {.unnumbered}
 
 ```bash
 # dmidecode requires root privileges for full access
 # Some information may be available without root
 
 # Check what's available without root
 dmidecode 2>/dev/null || echo "Root access required for complete information"
 
 # Use sudo for full access
 sudo dmidecode -t system
 ```
 
### 2. 가상 머신 고려 사항 {.unnumbered}
 
 ```bash
 # In VMs, some information may be virtualized or missing
 check_virtualization() {
     local system_product=$(sudo dmidecode -s system-product-name)
 
     case "$system_product" in
         *"VMware"*|*"Virtual Machine"*|*"VirtualBox"*)
             echo "Running in virtual machine: $system_product"
             echo "Some hardware information may be virtualized"
             ;;
         *)
             echo "Physical machine detected"
             ;;
     esac
 }
 
 check_virtualization
 ```
 
### 3. 정보 누락 {.unnumbered}
 
 ```bash
 # Some fields may show "Not Specified" or be empty
 # Handle missing data gracefully
 
 get_safe_value() {
     local value=$(sudo dmidecode -s "$1" 2>/dev/null)
     if [ -z "$value" ] || [ "$value" = "Not Specified" ]; then
         echo "Unknown"
     else
         echo "$value"
     fi
 }
 
 echo "Manufacturer: $(get_safe_value system-manufacturer)"
 echo "Model: $(get_safe_value system-product-name)"
 ```
 
## 다른 도구와의 통합
 
### 1. lscpu와 함께 사용 {.unnumbered}
 
 ```bash
 # Comprehensive CPU information
 echo "=== CPU Information ==="
 echo "DMI Information:"
 sudo dmidecode -t processor | grep -E "(Family|Version|Speed|Cores)"
 echo
 echo "Kernel Information:"
 lscpu
 ```
 
### 2. 메모리 정보 교차 확인 {.unnumbered}
 
 ```bash
 # Compare dmidecode with /proc/meminfo
 echo "DMI Memory Information:"
 sudo dmidecode -t 16 | grep "Maximum Capacity"
 sudo dmidecode -t 17 | grep "Size:" | grep -v "No Module"
 echo
 echo "Kernel Memory Information:"
 cat /proc/meminfo | grep -E "(MemTotal|MemAvailable)"
 ```
 
### 3. 시스템 모니터링 통합 {.unnumbered}
 
 ```bash
 # Add to monitoring systems
 create_monitoring_metrics() {
     local uuid=$(sudo dmidecode -s system-uuid)
     local serial=$(sudo dmidecode -s system-serial-number)
     local manufacturer=$(sudo dmidecode -s system-manufacturer)
 
     # Output in monitoring format (e.g., Prometheus)
     echo "system_info{uuid=\"$uuid\",serial=\"$serial\",manufacturer=\"$manufacturer\"} 1"
 }
 ```
 
## 모범 사례
 
### 1. 결과 캐싱 {.unnumbered}
 
 ```bash
 # Cache dmidecode output for scripts that use it multiple times
 DMI_CACHE="/tmp/dmidecode_cache.txt"
 
 get_cached_dmidecode() {
     if [ ! -f "$DMI_CACHE" ] || [ "$(find "$DMI_CACHE" -mmin +60)" ]; then
         sudo dmidecode > "$DMI_CACHE"
     fi
     cat "$DMI_CACHE"
 }
 ```
 
### 2. 오류 처리 {.unnumbered}
 
 ```bash
 # Always check if dmidecode is available and handle errors
 run_dmidecode() {
     if ! command -v dmidecode >/dev/null 2>&1; then
         echo "dmidecode not available"
         return 1
     fi
 
     if ! sudo dmidecode "$@" 2>/dev/null; then
         echo "Failed to read DMI information"
         return 1
     fi
 }
 ```
 
### 3. 보안 고려 사항 {.unnumbered}
 
 ```bash
 # Be careful with sensitive information in logs
 sanitize_output() {
     sudo dmidecode "$@" | sed 's/Serial Number:.*/Serial Number: [REDACTED]/'
 }
 ```
 
## 중요 참고 사항
 
- **루트 권한**: 모든 기능을 사용하려면 루트 권한이 필요합니다.
- **하드웨어 종속성**: 출력은 BIOS/UEFI 구현에 따라 달라집니다.
- **가상 머신**: 정보가 가상화되거나 제한될 수 있습니다.
- **제조업체별 차이**: 일부 필드는 제조업체에 따라 다를 수 있습니다.
- **버전 차이**: `dmidecode` 버전에 따라 출력 형식이 달라질 수 있습니다.
- **보안**: 로그에 기록되는 민감한 하드웨어 정보를 주의해서 다루세요.
 
 `dmidecode` 명령어는 하드웨어 인벤토리 작성, 시스템 식별 및 하드웨어 관련 문제 해결에 필수적입니다.

자세한 내용은 매뉴얼을 확인하세요: `man dmidecode`

# `apropos` 명령어

`apropos` 명령어는 키워드를 사용하여 매뉴얼 페이지의 이름과 설명을 검색합니다. 이 명령어는 기본적으로 `man -k`와 동일하며 원하는 작업은 알고 있지만 정확한 명령어 이름을 모를 때 관련 명령어와 문서를 찾는 데 유용합니다.
 
 ## 구문(Syntax)
 
 ```
 apropos [options] keyword...
 ```
 
 ## 주요 기능
 
- **키워드 검색**: 매뉴얼 페이지의 이름과 설명을 검색합니다.
- **정규 표현식 지원**: 패턴 매칭을 사용할 수 있습니다.
- **여러 키워드**: 여러 검색어를 검색할 수 있습니다.
- **섹션 필터링**: 특정 매뉴얼 섹션으로 검색 범위를 제한할 수 있습니다.
- **정확히 일치하는 항목 검색**: 검색어와 정확히 일치하는 단어를 찾을 수 있습니다.
 
 ## 기본 사용법
 
 ### Simple Keyword Search
 
 ```bash
 # Search for commands related to "copy"
 apropos copy
 
 # Search for commands related to "network"
 apropos network
 
 # Search for file-related commands
 apropos file
 
 # Search for text processing commands
 apropos text
 ```
 
 ### Multiple Keywords
 
 ```bash
 # Search for commands related to both "file" and "system"
 apropos file system
 
 # Search for networking and configuration
 apropos network config
 
 # Search for user and management
 apropos user management
 ```
 
 ## Common Options
 
 ### Basic Options
 
 ```bash
 # -a: AND search (all keywords must match)
 apropos -a file system
 
 # -e: Exact match
 apropos -e copy
 
 # -r: Use regular expressions
 apropos -r "^net"
 
 # -w: Match whole words only
 apropos -w net
 ```
 
 ### Advanced Options
 
 ```bash
 # -s: Search specific manual sections
 apropos -s 1 copy        # User commands only
 apropos -s 8 network     # System administration commands
 
 # -l: Long output format
 apropos -l file
 
 # -M: Specify manual path
 apropos -M /usr/share/man file
 ```
 
 ## 실무 활용 예제
 
 ### 1. Finding Commands by Function
 
 ```bash
 # Find compression commands
 apropos compress
 
 # Find archive commands
 apropos archive
 
 # Find text editors
 apropos editor
 
 # Find file system commands
 apropos filesystem
 
 # Find process management commands
 apropos process
 ```
 
 ### 2. Network-Related Commands
 
 ```bash
 # General network commands
 apropos network
 
 # Network configuration
 apropos -a network config
 
 # Network troubleshooting
 apropos -a network trouble
 
 # Firewall commands
 apropos firewall
 
 # DNS-related commands
 apropos dns
 ```
 
 ### 3. System Administration
 
 ```bash
 # User management
 apropos user
 
 # Service management
 apropos service
 
 # System monitoring
 apropos -a system monitor
 
 # Log management
 apropos log
 
 # Package management
 apropos package
 ```
 
 ### 4. Development and Programming
 
 ```bash
 # Compiler commands
 apropos compiler
 
 # Debugging tools
 apropos debug
 
 # Version control
 apropos version
 
 # Build tools
 apropos build
 
 # Library commands
 apropos library
 ```
 
 ## Using Regular Expressions
 
 ### 1. Pattern Matching
 
 ```bash
 # Find commands starting with "net"
 apropos -r "^net"
 
 # Find commands ending with "fs"
 apropos -r "fs$"
 
 # Find commands containing "config"
 apropos -r ".*config.*"
 
 # Find commands with specific patterns
 apropos -r "[0-9]+"
 ```
 
 ### 2. Complex Patterns
 
 ```bash
 # Multiple patterns
 apropos -r "(copy|move|transfer)"
 
 # Case-insensitive search
 apropos -r -i "FILE"
 
 # Word boundaries
 apropos -r "\bnet\b"
 ```
 
 ## Section-Specific Searches
 
 ### Manual Sections
 
 ```bash
 # Section 1: User commands
 apropos -s 1 copy
 
 # Section 2: System calls
 apropos -s 2 file
 
 # Section 3: Library functions
 apropos -s 3 string
 
 # Section 5: File formats
 apropos -s 5 config
 
 # Section 8: System administration
 apropos -s 8 mount
 ```
 
 ### Understanding Sections
 
 ```bash
 # List all sections
 man man | grep -A 10 "MANUAL SECTIONS"
 
 # Common sections:
 # 1 - User commands
 # 2 - System calls
 # 3 - Library functions
 # 4 - Device files
 # 5 - File formats and conventions
 # 6 - Games
 # 7 - Miscellaneous
 # 8 - System administration commands
 ```
 
 ## 고급 사용법
 
 ### 1. Combining with Other Commands
 
 ```bash
 # Search and count results
 apropos network | wc -l
 
 # Search and sort
 apropos file | sort
 
 # Search and filter
 apropos copy | grep -v "manual"
 
 # Search and format
 apropos -l network | column -t
 ```
 
 ### 2. Scripting Applications
 
 ```bash
 #!/bin/bash
 # Find commands for specific tasks
 
 find_commands() {
     local task="$1"
     echo "Commands related to '$task':"
     apropos "$task" | head -10
     echo
 }
 
 # Usage examples
 find_commands "backup"
 find_commands "monitor"
 find_commands "security"
 ```
 
 ### 3. Learning Tool
 
 ```bash
 # Daily command discovery
 daily_discovery() {
     local keywords=("network" "file" "text" "system" "process")
     local keyword=${keywords[$RANDOM % ${#keywords[@]}]}
 
     echo "Today's command discovery - Topic: $keyword"
     apropos "$keyword" | shuf | head -5
 }
 
 daily_discovery
 ```
 
 ## 문제 해결 및 트러블슈팅 Common Issues
 
 ### 1. No Results Found
 
 ```bash
 # Update manual database if no results
 sudo mandb
 
 # Check if manual pages are installed
 ls /usr/share/man/man1/ | head
 
 # Try different keywords
 apropos copy
 apropos duplicate
 apropos clone
 ```
 
 ### 2. Database Issues
 
 ```bash
 # Rebuild manual database
 sudo mandb -c
 
 # Force database rebuild
 sudo mandb -f
 
 # Check database status
 mandb --version
 ```
 
 ### 3. Permission Issues
 
 ```bash
 # Check manual path permissions
 ls -la /usr/share/man/
 
 # Check database location
 find /var -name "*man*" -type d 2>/dev/null
 
 # Run with specific path
 apropos -M /usr/local/man keyword
 ```
 
 ## Useful Search Patterns
 
 ### 1. Common Tasks
 
 ```bash
 # File operations
 apropos -a file copy
 apropos -a file move
 apropos -a file remove
 apropos -a file search
 
 # System information
 apropos -a system info
 apropos -a hardware info
 apropos -a disk usage
 
 # Network operations
 apropos -a network interface
 apropos -a network test
 apropos -a network config
 ```
 
 ### 2. By Software Category
 
 ```bash
 # Text processing
 apropos -r "(awk|sed|grep|cut|sort)"
 
 # Compression tools
 apropos -r "(gzip|tar|zip|compress)"
 
 # System monitoring
 apropos -r "(top|ps|iostat|netstat)"
 
 # File systems
 apropos -r "(mount|umount|fsck|mkfs)"
 ```
 
 ### 3. Administration Tasks
 
 ```bash
 # User management
 apropos -a user add
 apropos -a user delete
 apropos -a user modify
 
 # Service management
 apropos -a service start
 apropos -a service stop
 apropos -a service status
 
 # Package management
 apropos -a package install
 apropos -a package remove
 apropos -a package update
 ```
 
## Learning와의 통합
 
 ### 1. Command Discovery Script
 
 ```bash
 #!/bin/bash
 # Interactive command discovery
 
 discover_commands() {
     echo "What do you want to do? (e.g., 'copy files', 'monitor system')"
     read -r task
 
     echo "Searching for commands related to: $task"
     echo "======================================"
 
     apropos "$task" | while read -r line; do
         cmd=$(echo "$line" | awk '{print $1}')
         desc=$(echo "$line" | cut -d' ' -f2-)
 
         echo "Command: $cmd"
         echo "Description: $desc"
         echo "Try: man $cmd"
         echo "---"
     done
 }
 
 discover_commands
 ```
 
 ### 2. Command Recommendation
 
 ```bash
 #!/bin/bash
 # Recommend commands based on keywords
 
 recommend_command() {
     local keyword="$1"
     echo "For '$keyword', you might want to try:"
 
     apropos "$keyword" | head -5 | while read -r line; do
         cmd=$(echo "$line" | awk '{print $1}')
         echo "  • $cmd - $(man -f $cmd 2>/dev/null | cut -d' ' -f2- || echo 'No description')"
     done
 }
 
 # Examples
 recommend_command "backup"
 recommend_command "monitor"
 recommend_command "compress"
 ```
 
 ## Comparison with Similar Commands
 
 ### 1. apropos vs man -k
 
 ```bash
 # These are equivalent
 apropos network
 man -k network
 
 # Both search manual page descriptions
 ```
 
 ### 2. apropos vs whatis
 
 ```bash
 # apropos: searches descriptions (broader)
 apropos copy
 
 # whatis: exact command name match (narrower)
 whatis cp
 ```
 
 ### 3. apropos vs which/whereis
 
 ```bash
 # apropos: finds commands by description
 apropos file
 
 # which: finds command location
 which cp
 
 # whereis: finds command, source, manual locations
 whereis cp
 ```
 
 ## Configuration and Customization
 
 ### 1. Manual Path Configuration
 
 ```bash
 # Check current manual paths
 manpath
 
 # Add custom manual path
 export MANPATH="/usr/local/man:$MANPATH"
 
 # Make permanent in shell profile
 echo 'export MANPATH="/usr/local/man:$MANPATH"' >> ~/.bashrc
 ```
 
 ### 2. Database Configuration
 
 ```bash
 # Manual database location
 echo $MANDB
 
 # Update configuration
 sudo nano /etc/manpath.config
 
 # Force database update
 sudo mandb -f
 ```
 
 ## Advanced Scripting Examples
 
 ### 1. Command Explorer
 
 ```bash
 #!/bin/bash
 # Interactive command explorer
 
 while true; do
     echo -n "Enter search term (or 'quit' to exit): "
     read -r term
 
     if [ "$term" = "quit" ]; then
         break
     fi
 
     results=$(apropos "$term" 2>/dev/null)
 
     if [ -z "$results" ]; then
         echo "No commands found for '$term'"
         echo "Try: sudo mandb  # to update manual database"
     else
         echo "Commands related to '$term':"
         echo "$results" | nl
         echo
         echo -n "Enter number to view manual (or press Enter to continue): "
         read -r choice
 
         if [[ "$choice" =~ ^[0-9]+$ ]]; then
             cmd=$(echo "$results" | sed -n "${choice}p" | awk '{print $1}')
             if [ -n "$cmd" ]; then
                 man "$cmd"
             fi
         fi
     fi
     echo
 done
 ```
 
 ### 2. Command Category Browser
 
 ```bash
 #!/bin/bash
 # Browse commands by category
 
 categories=(
     "file:File operations"
     "network:Network commands"
     "system:System administration"
     "text:Text processing"
     "process:Process management"
     "security:Security tools"
     "backup:Backup and archive"
     "monitor:System monitoring"
 )
 
 echo "Command Categories:"
 for i in "${!categories[@]}"; do
     desc=$(echo "${categories[i]}" | cut -d: -f2)
     echo "$((i+1)). $desc"
 done
 
 echo -n "Select category (1-${#categories[@]}): "
 read -r choice
 
 if [[ "$choice" =~ ^[0-9]+$ ]] && [ "$choice" -ge 1 ] && [ "$choice" -le "${#categories[@]}" ]; then
     keyword=$(echo "${categories[$((choice-1))]}" | cut -d: -f1)
     desc=$(echo "${categories[$((choice-1))]}" | cut -d: -f2)
 
     echo "Commands for $desc:"
     apropos "$keyword" | head -10
 fi
 ```
 
 ## 모범 사례 (Best Practices)
 
 ### 1. Effective Searching
 
 ```bash
 # Start with broad terms, then narrow down
 apropos file
 apropos -a file copy
 apropos -a file copy system
 
 # Use synonyms if no results
 apropos copy || apropos duplicate || apropos clone
 ```
 
 ### 2. Regular Database Updates
 
 ```bash
 # Add to crontab for regular updates
 # 0 3 * * 0 /usr/bin/mandb -q
 
 # Or create update script
 #!/bin/bash
 echo "Updating manual database..."
 sudo mandb -q
 echo "Manual database updated."
 ```
 
 ### 3. Learning Integration
 
 ```bash
 # Create learning aliases
 alias learn='apropos'
 alias find-cmd='apropos'
 alias what-cmd='apropos'
 
 # Create help function
 help-me() {
     echo "What do you want to do?"
     echo "Example: help-me copy files"
     apropos "$*"
 }
 ```
## 중요 참고 사항

- **데이터베이스 의존성**: 최신 매뉴얼 데이터베이스(`mandb`)가 필요합니다.
- **키워드 품질**: 검색 결과는 검색어의 품질에 따라 달라집니다.
- **매뉴얼의 완전성**: 문서화된 명령어만 검색할 수 있습니다.
- **정규 표현식**: 패턴 매칭을 사용하려면 `-r` 옵션을 사용하세요.
- **섹션 인식**: 특정 섹션만 검색하려면 `-s` 옵션을 사용하세요.
- **대소문자 구분**: 일반적으로 기본 설정에서는 대소문자를 구분하지 않습니다.

수행하려는 작업은 알고 있지만 사용할 명령어를 모를 때, `apropos` 명령어는 명령어를 찾고 시스템 기능을 학습하는 데 매우 유용합니다.

자세한 내용은 매뉴얼을 확인하세요: `man apropos`

# `dmesg` 명령어

`dmesg` 명령어는 커널 링 버퍼의 메시지를 표시합니다. 부팅 메시지, 하드웨어 감지 정보, 드라이버 로딩 정보 및 시스템 이벤트를 보여줍니다. 하드웨어 문제와 드라이버 문제를 해결하고 시스템 시작 과정을 파악하는 데 필수적인 명령어입니

## 구문(Syntax)
 
 ```
 dmesg [options]
 ```
 
 ## 주요 기능
 
 - **커널 메시지**: 커널 링 버퍼 내용 표시
 - **부팅 정보**: 하드웨어 감지 및 드라이버 로딩
 - **실시간 모니터링**: 새 메시지를 실시간으로 추적할 수 있음
 - **필터링 옵션**: facility, level, 시간별로 필터링
 - **다양한 형식**: 사람이 읽기 쉬운 형식과 raw 형식
 
 ## 기본 사용법
 
 ### Show All Messages
 
 ```bash
 # Display all kernel messages
 dmesg
 
 # Display with human-readable timestamps
 dmesg -T
 
 # Display last 20 lines
 dmesg | tail -20
 
 # Display first 20 lines (boot messages)
 dmesg | head -20
 ```
 
 ### Follow New Messages
 
 ```bash
 # Follow new kernel messages (like tail -f)
 dmesg -w
 
 # Follow with timestamps
 dmesg -T -w
 
 # Follow last 10 lines and continue
 dmesg | tail -10 && dmesg -w
 ```
 
 ## Message Levels and Facilities
 
 ### Message Levels
 
 ```bash
 # Emergency messages (level 0)
 dmesg -l emerg
 
 # Alert messages (level 1)
 dmesg -l alert
 
 # Critical messages (level 2)
 dmesg -l crit
 
 # Error messages (level 3)
 dmesg -l err
 
 # Warning messages (level 4)
 dmesg -l warn
 
 # Notice messages (level 5)
 dmesg -l notice
 
 # Info messages (level 6)
 dmesg -l info
 
 # Debug messages (level 7)
 dmesg -l debug
 ```
 
 ### Multiple Levels
 
 ```bash
 # Show errors and warnings
 dmesg -l err,warn
 
 # Show critical and above (crit, alert, emerg)
 dmesg -l crit+
 
 # Show warnings and below (warn, notice, info, debug)
 dmesg -l warn-
 ```
 
 ### Facilities
 
 ```bash
 # Kernel messages
 dmesg -f kern
 
 # User-space messages
 dmesg -f user
 
 # Mail system messages
 dmesg -f mail
 
 # System daemons
 dmesg -f daemon
 
 # Authorization messages
 dmesg -f auth
 
 # Syslog messages
 dmesg -f syslog
 ```
 
 ## Common Options
 
 ### Display Options
 
 ```bash
 # -T: Human-readable timestamps
 dmesg -T
 
 # -t: Don't show timestamps
 dmesg -t
 
 # -x: Decode facility and level to human-readable
 dmesg -x
 
 # -H: Enable human-readable output
 dmesg -H
 
 # -k: Show kernel messages only
 dmesg -k
 ```
 
 ### Buffer Options
 
 ```bash
 # -c: Clear ring buffer after reading
 sudo dmesg -c
 
 # -C: Clear ring buffer
 sudo dmesg -C
 
 # -s: Buffer size
 dmesg -s 65536
 
 # -n: Set console log level
 sudo dmesg -n 1
 ```
 
 ## 실무 활용 예제
 
 ### 1. Hardware Detection
 
 ```bash
 # Check USB device detection
 dmesg | grep -i usb
 
 # Check network interface detection
 dmesg | grep -i eth
 
 # Check disk detection
 dmesg | grep -i -E "(sda|sdb|sdc|nvme)"
 
 # Check memory detection
 dmesg | grep -i memory
 
 # Check CPU detection
 dmesg | grep -i cpu
 ```
 
 ### 2. Driver Loading
 
 ```bash
 # Check loaded drivers
 dmesg | grep -i driver
 
 # Check specific driver (e.g., nvidia)
 dmesg | grep -i nvidia
 
 # Check wireless driver
 dmesg | grep -i wifi
 
 # Check bluetooth driver
 dmesg | grep -i bluetooth
 
 # Check sound driver
 dmesg | grep -i audio
 ```
 
 ### 3. Error Debugging
 
 ```bash
 # Show only error messages
 dmesg -l err
 
 # Show errors and warnings
 dmesg -l err,warn
 
 # Search for specific errors
 dmesg | grep -i error
 
 # Search for failed operations
 dmesg | grep -i fail
 
 # Search for timeout issues
 dmesg | grep -i timeout
 ```
 
 ### 4. Boot Process Analysis
 
 ```bash
 # Show boot messages with timestamps
 dmesg -T | head -50
 
 # Find boot completion time
 dmesg | grep -i "boot"
 
 # Check service startup
 dmesg | grep -i systemd
 
 # Check filesystem mounting
 dmesg | grep -i mount
 
 # Check network initialization
 dmesg | grep -i network
 ```
 
 ## Time-Based Filtering
 
 ### Recent Messages
 
 ```bash
 # Messages from last 10 minutes
 dmesg --since="10 minutes ago"
 
 # Messages from last hour
 dmesg --since="1 hour ago"
 
 # Messages from today
 dmesg --since="today"
 
 # Messages from specific time
 dmesg --since="2023-01-01 00:00:00"
 ```
 
 ### Time Ranges
 
 ```bash
 # Messages between specific times
 dmesg --since="2023-01-01" --until="2023-01-02"
 
 # Messages from last boot
 dmesg --since="last boot"
 
 # Messages from specific duration
 dmesg --since="30 minutes ago" --until="10 minutes ago"
 ```
 
 ## Advanced Filtering
 
 ### 1. Combining Filters
 
 ```bash
 # Errors from last hour
 dmesg -l err --since="1 hour ago"
 
 # Kernel warnings with timestamps
 dmesg -T -l warn -f kern
 
 # USB-related errors
 dmesg -l err | grep -i usb
 
 # Network errors from today
 dmesg --since="today" | grep -i -E "(network|eth|wifi)"
 ```
 
 ### 2. Output Formatting
 
 ```bash
 # JSON format
 dmesg --json
 
 # Raw format (no timestamp processing)
 dmesg -r
 
 # Colored output (if supported)
 dmesg --color=always
 
 # No pager
 dmesg --nopager
 ```
 
 ### 3. Custom Formatting
 
 ```bash
 # Extract specific information
 extract_usb_devices() {
     dmesg | grep -E "usb.*: New USB device" | \
     sed -n 's/.*New USB device found, idVendor=\([^,]*\), idProduct=\([^ ]*\).*/Vendor: \1, Product: \2/p'
 }
 
 extract_usb_devices
 ```
 
 ## Monitoring and Alerting
 
 ### 1. Real-time Monitoring
 
 ```bash
 # Monitor for specific events
 monitor_errors() {
     dmesg -w -l err | while read line; do
         echo "$(date): $line"
         # Send alert
         echo "$line" | mail -s "Kernel Error Alert" admin@domain.com
     done
 }
 
 # Monitor USB connections
 monitor_usb() {
     dmesg -w | grep --line-buffered -i usb | while read line; do
         echo "USB Event: $line"
     done
 }
 ```
 
 ### 2. Log Analysis Scripts
 
 ```bash
 #!/bin/bash
 # Analyze kernel messages for issues
 
 analyze_dmesg() {
     echo "=== Kernel Message Analysis ==="
     echo "Generated: $(date)"
     echo
 
     echo "Error Messages:"
     dmesg -l err | tail -10
     echo
 
     echo "Warning Messages:"
     dmesg -l warn | tail -10
     echo
 
     echo "Recent Hardware Events:"
     dmesg --since="1 hour ago" | grep -i -E "(usb|disk|network|memory)" | tail -10
     echo
 
     echo "Driver Loading Issues:"
     dmesg | grep -i -E "(failed|error|timeout)" | grep -i driver | tail -5
 }
 
 analyze_dmesg > kernel_analysis.txt
 ```
 
 ### 3. System Health Check
 
 ```bash
 #!/bin/bash
 # Check system health using dmesg
 
 check_system_health() {
     local issues=0
 
     echo "=== System Health Check ==="
 
     # Check for critical errors
     critical_errors=$(dmesg -l crit,alert,emerg --since="24 hours ago" | wc -l)
     if [ $critical_errors -gt 0 ]; then
         echo "⚠️  $critical_errors critical errors found in last 24 hours"
         ((issues++))
     else
         echo "✅ No critical errors in last 24 hours"
     fi
 
     # Check for hardware errors
     hw_errors=$(dmesg --since="24 hours ago" | grep -i -c -E "(hardware error|machine check|MCE)")
     if [ $hw_errors -gt 0 ]; then
         echo "⚠️  $hw_errors hardware errors detected"
         ((issues++))
     else
         echo "✅ No hardware errors detected"
     fi
 
     # Check for out of memory
     oom_events=$(dmesg --since="24 hours ago" | grep -i -c "out of memory")
     if [ $oom_events -gt 0 ]; then
         echo "⚠️  $oom_events out of memory events"
         ((issues++))
     else
         echo "✅ No out of memory events"
     fi
 
     # Check for filesystem errors
     fs_errors=$(dmesg --since="24 hours ago" | grep -i -c -E "(filesystem error|ext[234] error|xfs error)")
     if [ $fs_errors -gt 0 ]; then
         echo "⚠️  $fs_errors filesystem errors"
         ((issues++))
     else
         echo "✅ No filesystem errors"
     fi
 
     echo
     if [ $issues -eq 0 ]; then
         echo "🎉 System appears healthy!"
     else
         echo "⚠️  Found $issues types of issues - check details above"
     fi
 }
 
 check_system_health
 ```
 
## 일반적인 사용 사례
 
 ### 1. USB Device Troubleshooting
 
 ```bash
 # Monitor USB device connections
 monitor_usb_debug() {
     echo "Monitoring USB events (Ctrl+C to stop)..."
     dmesg -w | grep --line-buffered -i usb | while read line; do
         timestamp=$(date '+%Y-%m-%d %H:%M:%S')
         echo "[$timestamp] $line"
     done
 }
 
 # Check USB device history
 usb_device_history() {
     echo "USB Device Connection History:"
     dmesg | grep -i "usb.*: New USB device" | tail -10
     echo
     echo "USB Device Disconnections:"
     dmesg | grep -i "usb.*disconnect" | tail -10
 }
 ```
 
 ### 2. Network Interface Issues
 
 ```bash
 # Check network interface events
 check_network_issues() {
     echo "Network Interface Events:"
     dmesg | grep -i -E "(eth[0-9]|wlan[0-9]|enp|wlp)" | tail -10
     echo
 
     echo "Network Driver Issues:"
     dmesg | grep -i -E "network.*error|ethernet.*error" | tail -5
     echo
 
     echo "Link Status Changes:"
     dmesg | grep -i "link" | tail -10
 }
 ```
 
 ### 3. Storage Device Monitoring
 
 ```bash
 # Check disk health and errors
 check_storage_health() {
     echo "Storage Device Detection:"
     dmesg | grep -i -E "(sda|sdb|nvme)" | grep -i "sectors" | tail -5
     echo
 
     echo "Storage Errors:"
     dmesg | grep -i -E "(I/O error|Medium Error|critical medium error)" | tail -5
     echo
 
     echo "Filesystem Events:"
     dmesg | grep -i -E "(mounted|unmounted|remounted)" | tail -10
 }
 ```
 
## Other Tools와의 통합
 
 ### 1. Combining with journalctl
 
 ```bash
 # Compare kernel messages
 echo "=== dmesg output ==="
 dmesg --since="1 hour ago" | head -5
 
 echo "=== journalctl kernel messages ==="
 journalctl -k --since="1 hour ago" | head -5
 ```
 
 ### 2. Log File Integration
 
 ```bash
 # Save dmesg to file with rotation
 save_dmesg_log() {
     local logfile="/var/log/dmesg.log"
     local timestamp=$(date '+%Y-%m-%d %H:%M:%S')
 
     echo "[$timestamp] === dmesg snapshot ===" >> "$logfile"
     dmesg -T >> "$logfile"
 
     # Rotate if file gets too large
     if [ $(stat -f%z "$logfile" 2>/dev/null || stat -c%s "$logfile") -gt 10485760 ]; then
         mv "$logfile" "$logfile.old"
         echo "Log rotated at $timestamp" > "$logfile"
     fi
 }
 ```
 
 ### 3. Monitoring System Integration
 
 ```bash
 # Create metrics for monitoring systems
 create_dmesg_metrics() {
     local errors=$(dmesg -l err --since="1 hour ago" | wc -l)
     local warnings=$(dmesg -l warn --since="1 hour ago" | wc -l)
     local timestamp=$(date +%s)
 
     # Output in Prometheus format
     echo "kernel_errors_total $errors $timestamp"
     echo "kernel_warnings_total $warnings $timestamp"
 }
 ```
 
 ## 문제 해결 및 트러블슈팅
 
 ### 1. Buffer Size Issues
 
 ```bash
 # Check current buffer size
 dmesg | wc -l
 
 # Increase buffer size (temporary)
 sudo dmesg -s 1048576
 
 # Make permanent (add to kernel parameters)
 # Add to GRUB: log_buf_len=1048576
 ```
 
 ### 2. Permission Issues
 
 ```bash
 # Check if non-root can read dmesg
 dmesg >/dev/null 2>&1 && echo "Can read dmesg" || echo "Cannot read dmesg"
 
 # Check kernel parameter
 cat /proc/sys/kernel/dmesg_restrict
 
 # Allow non-root users (temporary)
 sudo sysctl kernel.dmesg_restrict=0
 
 # Make permanent
 echo "kernel.dmesg_restrict = 0" | sudo tee -a /etc/sysctl.conf
 ```
 
 ### 3. Message Filtering Issues
 
 ```bash
 # Check available facilities and levels
 dmesg --help | grep -A 20 "supported facilities"
 
 # Test filtering
 dmesg -l err | head -5
 dmesg -f kern | head -5
 
 # Check if systemd is interfering
 systemctl status systemd-journald
 ```
 
 ## 성능 고려 사항
### 1. 대용량 버퍼 처리 {.unnumbered}

```bash
# Process large buffers efficiently
process_large_dmesg() {
    # Use streaming instead of loading all into memory
    dmesg | while IFS= read -r line; do
        # Process line by line
        echo "$line" | grep -q "error" && echo "Error: $line"
    done
}
```

### 2. 효율적인 검색 {.unnumbered}

```bash
# Use specific filters instead of post-processing
dmesg -l err               # Better than dmesg | grep -i error
dmesg --since="1 hour ago" # Better than dmesg | filtering by time
```

## 모범 사례

### 1. 정기 모니터링 {.unnumbered}

```bash
# Create cron job for regular checks
# 0 */4 * * * /usr/local/bin/check_dmesg_errors.sh

# Create check script
#!/bin/bash
errors=$(dmesg -l err --since="4 hours ago" | wc -l)
if [ $errors -gt 0 ]; then
    dmesg -l err --since="4 hours ago" | mail -s "Kernel Errors Detected" admin@domain.com
fi
```

### 2. 로그 보존 {.unnumbered}

```bash
# Save important messages
save_important_messages() {
    local date_str=$(date '+%Y%m%d')
    dmesg -l err,crit,alert,emerg > "/var/log/kernel_errors_$date_str.log"
}
```

### 3. 문서화 {.unnumbered}

```bash
# Document system events
document_system_event() {
    local event="$1"
    local logfile="/var/log/system_events.log"

    echo "$(date): $event" >> "$logfile"
    echo "=== dmesg at time of event ===" >> "$logfile"
    dmesg -T | tail -20 >> "$logfile"
    echo "=================================" >> "$logfile"
}
```

## 중요 참고 사항

- **루트 권한**: 일부 배포판에서는 `dmesg` 사용을 루트 사용자로 제한합니다.
- **버퍼 크기**: 링 버퍼의 크기는 제한되어 있으므로 오래된 메시지는 새 메시지로 덮어써집니다.
- **타임스탬프**: 사람이 읽기 쉬운 타임스탬프를 표시하려면 `-T` 옵션을 사용하세요.
- **로그 레벨**: 효과적으로 필터링하려면 메시지 레벨을 이해해야 합니다.
- **실시간 모니터링**: 새 메시지를 모니터링하려면 `-w` 옵션을 사용하세요.
- **성능**: 버퍼가 지나치게 크면 성능에 영향을 줄 수 있습니다.
- **보안**: 커널 메시지가 외부에 노출되지 않도록 주의하세요.

`dmesg` 명령은 시스템 문제 해결, 하드웨어 디버깅, 커널 동작 이해에 필수적입니다.

자세한 내용은 매뉴얼을 확인하세요: `man dmesg`

# `!!` 명령(히스토리 확장)

`!!` 명령은 마지막 명령을 반복 실행하는 Bash 히스토리 확장 기능입니다. 이전 명령을 빠르게 다시 실행하거나 수정하거나 참조할 수 있는 다양한 히스토리 확장 기능 중 하나입니다. 실수를 수정하거나 `sudo`를 추가하거나 복잡한 명령을 반복할 때 특히 유용합니다.

## 구문

```
!!
!<event>
!<string>
!?<string>?
```

## 주요 기능

- **마지막 명령 반복**: 이전 명령을 빠르게 다시 실행합니다
- **명령 수정**: 이전 명령을 수정한 뒤 다시 실행합니다
- **인수 추출**: 이전 명령에서 특정 인수를 추출합니다
- **패턴 일치**: 패턴으로 명령을 찾습니다
- **시간 절약**: 복잡한 명령을 다시 입력하지 않아도 됩니다

## 기본 사용법

### 간단한 히스토리 확장

```bash
# Run a command
ls -la /home/user

# Repeat the last command
!!

# Add sudo to the last command
sudo !!

# This is equivalent to:
sudo ls -la /home/user
```

### 일반적인 상황

```bash
# Forgot sudo
apt update
# Permission denied

# Fix with sudo
sudo !!
# Executes: sudo apt update

# Wrong directory
cd /var/logs
# No such file or directory

# Fix the path
cd /var/log
# Then repeat with correct path
!!
```

## 히스토리 확장 패턴

### 1. 이벤트 지정자 {.unnumbered}

```bash
# !! - Last command
!!

# !n - Command number n
!123

# !-n - nth command back
!-2    # Two commands ago
!-5    # Five commands ago

# !string - Most recent command starting with 'string'
!ls    # Last command starting with 'ls'
!git   # Last command starting with 'git'

# !?string? - Most recent command containing 'string'
!?config?   # Last command containing 'config'
!?file?     # Last command containing 'file'
```

### 2. 단어 지정자 {.unnumbered}

```bash
# Previous command: git commit -m "Fix bug" --amend

# !^ - First argument
echo !^        # echo commit

# !$ - Last argument
echo !$        # echo --amend

# !* - All arguments
echo !*        # echo commit -m "Fix bug" --amend

# !:n - nth argument (0-based)
echo !:0       # echo git
echo !:1       # echo commit
echo !:2       # echo -m

# !:n-m - Range of arguments
echo !:1-3     # echo commit -m "Fix bug"
```

### 3. 수정자 {.unnumbered}

```bash
# Previous command: ls /home/user/documents/file.txt

# :h - Remove trailing pathname component (head)
echo !!:h      # echo /home/user/documents

# :t - Remove leading pathname components (tail)
echo !!:t      # echo file.txt

# :r - Remove trailing suffix
echo !!:r      # echo /home/user/documents/file

# :e - Remove all but trailing suffix
echo !!:e      # echo txt

# :s/old/new/ - Substitute first occurrence
!!:s/user/admin/   # ls /home/admin/documents/file.txt

# :gs/old/new/ - Global substitution
!!:gs/o/0/         # ls /h0me/user/d0cuments/file.txt
```

## 실용적인 예

### 1. 오류 수정 {.unnumbered}

```bash
# Typo in command
ct /etc/hosts
# Command not found

# Correct and re-run
cat /etc/hosts

# Then if you need to edit it
sudo vi !!    # Becomes: sudo vi /etc/hosts
```

### 2. 빠진 옵션 추가 {.unnumbered}

```bash
# Run command without verbose
tar -czf backup.tar.gz /home/user

# Add verbose flag
!!:s/-czf/-czvf/
# Becomes: tar -czvf backup.tar.gz /home/user

# Or simpler approach
tar -czvf backup.tar.gz /home/user
```

### 3. 파일 작업 {.unnumbered}

```bash
# Create file
touch /tmp/test_file.txt

# Edit the same file
vi !!:$     # vi /tmp/test_file.txt

# Copy to different location
cp !!:$ /home/user/
# Becomes: cp /tmp/test_file.txt /home/user/

# Previous command: find /var/log -name "*.log" -size +10M
# Copy found files
cp !!:3 !!:4 !!:5 /backup/
# Using specific arguments from find command
```

### 4. 디렉터리 탐색 {.unnumbered}

```bash
# Change to a directory
cd /usr/local/bin

# List contents
ls !!:$     # ls /usr/local/bin

# Go back and then to related directory
cd /usr/local/src
# ... later ...
cd !!:h/bin    # cd /usr/local/bin
```

## 고급 히스토리 확장

### 1. 복잡한 치환 {.unnumbered}

```bash
# Previous: rsync -av /home/user/docs/ backup@server:/backup/user/docs/

# Change source directory
!!:s/docs/pictures/
# Becomes: rsync -av /home/user/pictures/ backup@server:/backup/user/docs/

# Change both source and destination
!!:s/docs/pictures/:s/user\/docs/user\/pictures/
# Global changes
!!:gs/docs/pictures/
```

### 2. 다른 기능과 결합 {.unnumbered}

```bash
# Previous: find /var/log -name "*.log" -type f -exec ls -l {} \;

# Modify to use different action
!!:s/-exec ls -l/-exec wc -l/
# Count lines instead of listing

# Extract just the find portion
!:0-4    # find /var/log -name "*.log" -type f

# Use arguments with different command
grep "error" !!:3  # grep "error" "*.log"
```

### 3. 여러 명령 사용 {.unnumbered}

```bash
# Pipeline command
ps aux | grep apache | grep -v grep

# Modify the grep pattern
!!:s/apache/nginx/
# Becomes: ps aux | grep nginx | grep -v grep

# Extract just part of pipeline
!:0-2     # ps aux | grep apache

# Add to existing pipeline
!! | wc -l    # Count the results
```

## 대화형 기능

### 1. 히스토리 확인 {.unnumbered}

```bash
# Enable history verification (before execution)
set +H    # Disable history expansion
set -H    # Enable history expansion

# Show command before execution
shopt -s histverify
# Now !! will show the command and wait for Enter
```

### 2. 히스토리 검색 {.unnumbered}

```bash
# Ctrl+R - Reverse search
# Type to search through history
# Ctrl+R again to find previous matches

# Search for specific command
!?git commit?    # Find last command containing "git commit"
!?ssh?           # Find last command containing "ssh"
```

## 구성 및 설정

### 1. 히스토리 설정 {.unnumbered}

```bash
# History size
export HISTSIZE=1000        # Commands in memory
export HISTFILESIZE=2000    # Commands in file

# History control
export HISTCONTROL=ignoreboth    # Ignore duplicates and spaces
export HISTCONTROL=ignoredups    # Ignore duplicates only

# History ignore patterns
export HISTIGNORE="ls:cd:cd -:pwd:exit:date:* --help"

# Timestamp in history
export HISTTIMEFORMAT="%F %T "
```

### 2. 히스토리 확장 설정 {.unnumbered}

```bash
# Check if history expansion is enabled
set +o | grep histexpand

# Enable history expansion
set -H
# or
set -o histexpand

# Disable history expansion
set +H
# or
set +o histexpand
```

## 안전 및 모범 사례

### 1. 실행 전 확인 {.unnumbered}

```bash
# Enable command verification
shopt -s histverify

# This makes !! show the command first, requiring Enter to execute

# Check what command will be executed
history | tail -1    # See last command
echo !!             # See what !! would execute (without running it)
```

### 2. 안전한 사용법 {.unnumbered}

```bash
# Be careful with destructive commands
rm -rf /tmp/*
# Don't blindly run !! after such commands

# Use history to verify
history | tail -5    # Check recent commands

# For critical operations, type commands fully
# Don't rely on history expansion for:
# - rm commands
# - chmod commands
# - System configuration changes
```

### 3. 디버깅 {.unnumbered}

```bash
# See history expansion in action
set -x    # Enable command tracing
!!        # You'll see the expanded command
set +x    # Disable tracing

# Check history before using
history 10    # Show last 10 commands
!-2           # Run 2nd to last command
```

## 일반적인 패턴과 단축키

### 1. 자주 사용하는 조합 {.unnumbered}

```bash
# Add sudo to last command
sudo !!

# Redirect last command output
!! > output.txt
!! 2>&1 | tee log.txt

# Background last command
!! &

# Time last command
time !!

# Run last command in different directory
(cd /tmp && !!)
```

### 2. 파일 및 디렉터리 작업 {.unnumbered}

```bash
# Previous: vi /etc/apache2/sites-available/default

# Test the configuration
apache2ctl -t

# Copy the file
cp !!:$ !!:$:r.backup    # cp /etc/apache2/sites-available/default /etc/apache2/sites-available/default.backup

# Edit related file
vi !!:h/sites-enabled/default    # vi /etc/apache2/sites-enabled/default
```

### 3. 네트워크 및 시스템 명령 {.unnumbered}

```bash
# Check service status
systemctl status apache2

# Restart if needed
sudo !!:s/status/restart/    # sudo systemctl restart apache2

# Check multiple services
systemctl status nginx
!!:s/nginx/mysql/           # systemctl status mysql
!!:s/mysql/postgresql/      # systemctl status postgresql
```

## 스크립트와 통합

### 1. 스크립트에서의 히스토리 {.unnumbered}

```bash
#!/bin/bash
# Note: History expansion doesn't work in scripts by default
# Enable it explicitly if needed

set -H    # Enable history expansion in script

# Use variables instead of history expansion in scripts
last_command="$1"
echo "Re-running: $last_command"
eval "$last_command"
```

### 2. 대화형 스크립트 {.unnumbered}

```bash
#!/bin/bash
# Interactive script using history concepts

while true; do
    read -p "Command: " cmd

    if [ "$cmd" = "!!" ]; then
        echo "Re-running: $last_cmd"
        eval "$last_cmd"
    elif [ "$cmd" = "exit" ]; then
        break
    else
        eval "$cmd"
        last_cmd="$cmd"
    fi
done
```

## 대안 및 관련 명령

### 1. fc(명령 수정) {.unnumbered}

```bash
# Edit last command in editor
fc

# Edit specific command number
fc 123

# List recent commands
fc -l

# Re-run range of commands
fc -s 100 105
```

### 2. history 명령 {.unnumbered}

```bash
# Show command history
history

# Show last 10 commands
history 10

# Execute specific command number
!123

# Search and execute
history | grep git
!456
```

## 문제 해결

### 1. 히스토리 확장이 작동하지 않음 {.unnumbered}

```bash
# Check if enabled
set +o | grep histexpand

# Enable it
set -H

# Check in current shell
echo $-    # Should contain 'H'
```

### 2. 예상치 못한 확장 {.unnumbered}

```bash
# Escape ! to prevent expansion
echo "The price is \$5\!"

# Use single quotes
echo 'The price is $5!'

# Disable temporarily
set +H
echo "Commands with ! work normally"
set -H
```

### 3. 복잡한 명령 문제 {.unnumbered}

```bash
# For very complex commands, use variables
complex_cmd="find /var/log -name '*.log' -exec grep 'error' {} \;"
eval "$complex_cmd"

# Then modify variable instead of using history expansion
complex_cmd="${complex_cmd/error/warning}"
eval "$complex_cmd"
```

## 중요 참고 사항

- **대화형 전용**: 히스토리 확장은 주로 대화형 셸에서 작동합니다.
- **스크립트에서는 사용하지 않음**: 안전을 위해 스크립트에서는 일반적으로 비활성화되어 있습니다.
- **셸별 기능**: 모든 셸에서 사용할 수 있는 기능이 아니라 Bash와 Zsh의 기능입니다.
- **확인**: 파괴적인 명령어를 실행할 때는 안전을 위해 `histverify` 옵션을 사용하세요.
- **대소문자 구분**: 히스토리 확장은 대소문자를 구분합니다.
- **즉시 실행**: `!!`는 명령어를 즉시 실행하므로, 파괴적인 명령어에 사용할 때 주의하세요.

`!!` 명령과 히스토리 확장 기능은 명령줄 작업을 효율적으로 해 주는 강력한 도구지만 실수를 피하려면 그 동작을 이해하고 신중하게 사용해야 합니다.

자세한 내용은 Bash 매뉴얼을 확인하세요: `man bash`(“History Expansion” 검색)

# `tty` 명령

`tty` 명령은 표준 입력에 연결된 터미널의 파일 이름을 출력합니다. 현재 사용하는 터미널 장치를 보여 주며 입력이 터미널에서 오는지 파일이나 파이프로 리디렉션되었는지 확인할 수 있습니다.

## 구문

```
tty [options]
```

## 주요 기능

- **터미널 식별**: 현재 터미널 장치를 표시합니다
- **리디렉션 감지**: 입력이 터미널이나 파일에서 오는지 확인합니다
- **세션 정보**: 터미널 세션을 식별하는 데 도움을 줍니다
- **스크립팅 지원**: 종료 코드로 터미널 여부를 확인할 수 있습니다

## 기본 사용법

### 현재 터미널 표시

```bash
# Display current terminal device
tty

# Example outputs:
# /dev/pts/0    (pseudo-terminal)
# /dev/tty1     (console terminal)
# /dev/ttys000  (macOS terminal)
```

### 터미널 여부 확인

```bash
# Silent mode - just check if it's a terminal
tty -s

# Check exit code
tty -s && echo "Running in terminal" || echo "Not in terminal"

# Example usage in scripts
if tty -s; then
    echo "Interactive mode"
else
    echo "Non-interactive mode"
fi
```

## 일반 옵션

### 기본 옵션

```bash
# -s: Silent mode (no output, just exit code)
tty -s

# --help: Show help information
tty --help

# --version: Show version information
tty --version
```

## 터미널 유형 이해

### 1. 물리 터미널 {.unnumbered}

```bash
# Console terminals (virtual consoles)
# /dev/tty1, /dev/tty2, /dev/tty3, etc.

# Switch to virtual console and check
# Ctrl+Alt+F1 (then login)
tty
# Output: /dev/tty1
```

### 2. 의사 터미널 {.unnumbered}

```bash
# Most common in desktop environments
# /dev/pts/0, /dev/pts/1, /dev/pts/2, etc.

# In terminal emulator
tty
# Output: /dev/pts/0

# Each new terminal window gets new pts number
```

### 3. 직렬 터미널 {.unnumbered}

```bash
# Serial console connections
# /dev/ttyS0, /dev/ttyS1, etc.

# USB serial devices
# /dev/ttyUSB0, /dev/ttyUSB1, etc.
```

## 실용적인 예

### 1. Session Identification {.unnumbered}

```bash
# Identify current session
echo "Current session: $(tty)"

# Show user and terminal
echo "User: $(whoami) on $(tty)"

# Show all users and their terminals
who

# Show current user's terminals
who | grep $(whoami)
```

### 2. Multi-Terminal Scripts {.unnumbered}

```bash
#!/bin/bash
# Script that behaves differently based on terminal

current_tty=$(tty)
echo "Running on: $current_tty"

case "$current_tty" in
    /dev/tty[1-6])
        echo "Running on virtual console"
        # Console-specific behavior
        ;;
    /dev/pts/*)
        echo "Running in terminal emulator"
        # Terminal emulator behavior
        ;;
    *)
        echo "Unknown terminal type"
        ;;
esac
```

### 3. Interactive vs Non-Interactive Detection {.unnumbered}

```bash
#!/bin/bash
# Detect if script is running interactively

if tty -s; then
    echo "Interactive mode - can prompt user"
    read -p "Enter your name: " name
    echo "Hello, $name!"
else
    echo "Non-interactive mode - using defaults"
    name="User"
    echo "Hello, $name!"
fi
```

### 4. Terminal-Specific Configuration {.unnumbered}

```bash
#!/bin/bash
# Configure based on terminal capabilities

current_tty=$(tty)

if [[ "$current_tty" =~ ^/dev/pts/ ]]; then
    # Modern terminal emulator
    echo -e "\e[32mGreen text\e[0m"
    echo -e "\e[1mBold text\e[0m"
elif [[ "$current_tty" =~ ^/dev/tty[1-6]$ ]]; then
    # Virtual console - limited capabilities
    echo "Plain text output"
else
    echo "Unknown terminal - safe output"
fi
```

## 스크립팅 활용

### 1. Conditional User Interaction {.unnumbered}

```bash
#!/bin/bash
# Only prompt if running in terminal

ask_confirmation() {
    local message="$1"

    if tty -s; then
        read -p "$message (y/n): " response
        case "$response" in
            [Yy]|[Yy][Ee][Ss]) return 0 ;;
            *) return 1 ;;
        esac
    else
        # Non-interactive - assume yes
        echo "$message: Assumed yes (non-interactive)"
        return 0
    fi
}

# Usage
if ask_confirmation "Delete old files?"; then
    echo "Deleting files..."
else
    echo "Keeping files..."
fi
```

### 2. Progress Indicators {.unnumbered}

```bash
#!/bin/bash
# Show progress only in terminal

show_progress() {
    if tty -s; then
        echo -n "Processing: "
        for i in {1..10}; do
            echo -n "."
            sleep 0.5
        done
        echo " Done!"
    else
        echo "Processing... Done!"
    fi
}

show_progress
```

### 3. Logging Behavior {.unnumbered}

```bash
#!/bin/bash
# Different logging based on terminal

log_message() {
    local level="$1"
    local message="$2"
    local timestamp=$(date '+%Y-%m-%d %H:%M:%S')

    if tty -s; then
        # Terminal output - colored
        case "$level" in
            INFO)  echo -e "\e[32m[$timestamp] INFO: $message\e[0m" ;;
            WARN)  echo -e "\e[33m[$timestamp] WARN: $message\e[0m" ;;
            ERROR) echo -e "\e[31m[$timestamp] ERROR: $message\e[0m" ;;
        esac
    else
        # Non-terminal output - plain
        echo "[$timestamp] $level: $message"
    fi
}

# Usage
log_message "INFO" "Script started"
log_message "WARN" "Low disk space"
log_message "ERROR" "Connection failed"
```

## 터미널 세션 관리

### 1. Session Information {.unnumbered}

```bash
# Get detailed session info
get_session_info() {
    echo "=== Session Information ==="
    echo "Terminal: $(tty)"
    echo "User: $(whoami)"
    echo "Shell: $SHELL"
    echo "PID: $$"
    echo "PPID: $PPID"
    echo "Session ID: $(ps -o sid= -p $$)"
    echo "Process Group: $(ps -o pgid= -p $$)"
}

get_session_info
```

### 2. Multiple Terminal Detection {.unnumbered}

```bash
# Count user's active terminals
count_user_terminals() {
    local user=$(whoami)
    local count=$(who | grep "^$user " | wc -l)
    echo "User $user has $count active terminals"

    echo "Active sessions:"
    who | grep "^$user " | while read line; do
        echo "  $line"
    done
}

count_user_terminals
```

### 3. Terminal Communication {.unnumbered}

```bash
# Send message to specific terminal
send_to_terminal() {
    local target_tty="$1"
    local message="$2"

    if [ -w "$target_tty" ]; then
        echo "Message from $(tty): $message" > "$target_tty"
        echo "Message sent to $target_tty"
    else
        echo "Cannot write to $target_tty"
    fi
}

# Usage (if permissions allow)
# send_to_terminal "/dev/pts/1" "Hello from another terminal!"
```

## 다른 명령과 통합

### 1. Combining with ps {.unnumbered}

```bash
# Find processes in current terminal
current_tty=$(tty | sed 's|/dev/||')
ps -t "$current_tty"

# Show process tree for current terminal
pstree -p $(ps -t "$current_tty" -o pid --no-headers | head -1)
```

### 2. Combining with who/w {.unnumbered}

```bash
# Show who is on the same terminal type
current_tty_type=$(tty | cut -d'/' -f3 | sed 's/[0-9]*$//')
who | grep "$current_tty_type"

# Detailed information about current session
w | grep "$(tty | sed 's|/dev/||')"
```

### 3. System Monitoring {.unnumbered}

```bash
# Monitor terminal activity
monitor_terminals() {
    echo "Active terminals:"
    ls -la /dev/pts/
    echo

    echo "Users and terminals:"
    who
    echo

    echo "Current session:"
    echo "  TTY: $(tty)"
    echo "  Uptime: $(uptime)"
}

monitor_terminals
```

## 보안 및 권한

### 1. Terminal Permissions {.unnumbered}

```bash
# Check terminal permissions
check_tty_permissions() {
    local current_tty=$(tty)
    echo "Terminal: $current_tty"
    ls -la "$current_tty"

    # Check if others can write to terminal
    if [ -w "$current_tty" ]; then
        echo "Terminal is writable"
    else
        echo "Terminal is not writable"
    fi
}

check_tty_permissions
```

### 2. Secure Terminal Check {.unnumbered}

```bash
# Verify secure terminal environment
check_secure_terminal() {
    if ! tty -s; then
        echo "WARNING: Not running in a terminal"
        return 1
    fi

    local current_tty=$(tty)
    local tty_perms=$(ls -la "$current_tty" | cut -d' ' -f1)

    if [[ "$tty_perms" =~ .*w.*w.* ]]; then
        echo "WARNING: Terminal is world-writable"
        return 1
    fi

    echo "Terminal security check passed"
    return 0
}

check_secure_terminal
```

## 디버깅 및 문제 해결

### 1. Terminal Issues {.unnumbered}

```bash
# Debug terminal problems
debug_terminal() {
    echo "=== Terminal Debug Information ==="
    echo "TTY: $(tty 2>/dev/null || echo "No TTY")"
    echo "TERM: $TERM"
    echo "Interactive: $(tty -s && echo "Yes" || echo "No")"
    echo "Standard input: $(file /proc/self/fd/0 | cut -d: -f2-)"
    echo "Standard output: $(file /proc/self/fd/1 | cut -d: -f2-)"
    echo "Standard error: $(file /proc/self/fd/2 | cut -d: -f2-)"
}

debug_terminal
```

### 2. Redirection Detection {.unnumbered}

```bash
# Detect various input/output scenarios
detect_io_redirection() {
    echo "Input/Output Analysis:"

    if tty -s; then
        echo "  Standard input: Terminal ($(tty))"
    else
        echo "  Standard input: Redirected/Pipe"
    fi

    if [ -t 1 ]; then
        echo "  Standard output: Terminal"
    else
        echo "  Standard output: Redirected/Pipe"
    fi

    if [ -t 2 ]; then
        echo "  Standard error: Terminal"
    else
        echo "  Standard error: Redirected/Pipe"
    fi
}

detect_io_redirection
```

### 3. Session Recovery {.unnumbered}

```bash
# Help recover lost terminal sessions
find_my_sessions() {
    local user=$(whoami)
    echo "Finding sessions for user: $user"

    echo "Current TTY: $(tty)"
    echo

    echo "All active sessions:"
    who | grep "^$user "
    echo

    echo "Screen sessions:"
    screen -ls 2>/dev/null || echo "No screen sessions"
    echo

    echo "Tmux sessions:"
    tmux list-sessions 2>/dev/null || echo "No tmux sessions"
}

find_my_sessions
```

## 고급 사용법

### 1. Terminal Multiplexing Integration {.unnumbered}

```bash
# Detect if running inside multiplexer
detect_multiplexer() {
    if [ -n "$TMUX" ]; then
        echo "Running inside tmux"
        echo "  Session: $(tmux display-message -p '#S')"
        echo "  Window: $(tmux display-message -p '#W')"
        echo "  Pane: $(tmux display-message -p '#P')"
    elif [ -n "$STY" ]; then
        echo "Running inside screen"
        echo "  Session: $STY"
    else
        echo "Not in a multiplexer"
    fi

    echo "TTY: $(tty)"
}

detect_multiplexer
```

### 2. Remote Session Detection {.unnumbered}

```bash
# Detect remote vs local sessions
detect_session_type() {
    local current_tty=$(tty)

    if [ -n "$SSH_CONNECTION" ]; then
        echo "Remote SSH session"
        echo "  From: $(echo $SSH_CONNECTION | cut -d' ' -f1,2)"
        echo "  To: $(echo $SSH_CONNECTION | cut -d' ' -f3,4)"
    elif [[ "$current_tty" =~ ^/dev/tty[1-6]$ ]]; then
        echo "Local console session"
    elif [[ "$current_tty" =~ ^/dev/pts/ ]]; then
        echo "Local terminal emulator"
    else
        echo "Unknown session type"
    fi

    echo "TTY: $current_tty"
}

detect_session_type
```

## 모범 사례

### 1. 안전한 스크립팅 {.unnumbered}

```bash
# Always check for terminal before interactive operations
safe_interactive() {
    if ! tty -s; then
        echo "Error: This script requires a terminal" >&2
        exit 1
    fi

    # Proceed with interactive operations
    read -p "Continue? (y/n): " response
}
```

### 2. 크로스 플랫폼 호환성 {.unnumbered}

```bash
# Handle different systems
get_terminal_info() {
    if command -v tty >/dev/null 2>&1; then
        local terminal=$(tty 2>/dev/null)
        if [ $? -eq 0 ]; then
            echo "$terminal"
        else
            echo "not a tty"
        fi
    else
        echo "tty command not available"
    fi
}
```

### 3. 오류 처리 {.unnumbered}

```bash
# Robust terminal checking
check_terminal_safe() {
    local tty_output
    tty_output=$(tty 2>/dev/null)
    local exit_code=$?

    if [ $exit_code -eq 0 ]; then
        echo "Terminal: $tty_output"
        return 0
    else
        echo "Not a terminal (exit code: $exit_code)"
        return 1
    fi
}
```

## 중요 참고 사항

- **종료 코드**: 표준 입력이 터미널이면 tty는 0을, 그렇지 않으면 0이 아닌 값을 반환합니다
- **무음 모드**: 터미널 상태만 확인하는 스크립트에는 `-s`를 사용합니다
- **리디렉션**: 표준 입력이 파일이나 파이프에서 리디렉션되면 출력이 달라집니다
- **보안**: 터미널 권한과 쓰기 가능 여부에 유의합니다
- **이식성**: 대부분의 Unix 계열 시스템에서 사용할 수 있습니다
- **세션 관리**: 멀티플렉싱과 세션 추적에 유용합니다

`tty` 명령은 터미널 환경을 감지하고 대화형·비대화형 상황에 맞게 동작해야 하는 스크립트에 필수적입니다.

자세한 내용은 매뉴얼을 확인하세요: `man tty`

# `lspci` 명령

`lspci` 명령은 시스템에 연결된 모든 PCI(Peripheral Component Interconnect) 장치를 나열합니다. 그래픽 카드, 네트워크 어댑터, 사운드 카드, 스토리지 컨트롤러 등 PCI 기반 하드웨어 구성 요소의 상세 정보를 제공합니다.

## 구문

```
lspci [options]
```

## 주요 기능

- **하드웨어 감지**: 모든 PCI 장치를 나열합니다
- **상세 정보**: 공급업체, 장치, 클래스 및 기능을 표시합니다
- **트리 보기**: 장치 계층 구조와 관계를 표시합니다
- **필터링 옵션**: 공급업체, 장치 또는 클래스로 검색합니다
- **상세 출력**: 여러 단계의 상세 정보를 제공합니다

## 기본 사용법

### 간단한 장치 목록

```bash
# List all PCI devices
lspci

# Example output:
# 00:00.0 Host bridge: Intel Corporation Device 9b61
# 00:01.0 PCI bridge: Intel Corporation Device 1901
# 00:02.0 VGA compatible controller: Intel Corporation Device 9bc4
# 00:14.0 USB controller: Intel Corporation Device a36d
```

### 사람이 읽기 쉬운 출력

```bash
# Show device names instead of just IDs
lspci -nn

# More verbose output
lspci -v

# Very verbose output (includes everything)
lspci -vv

# Extremely verbose output
lspci -vvv
```

## 일반 옵션

### 기본 옵션

```bash
# -v: Verbose (show detailed info)
lspci -v

# -vv: Very verbose
lspci -vv

# -n: Show numeric IDs instead of names
lspci -n

# -nn: Show both names and numeric IDs
lspci -nn

# -k: Show kernel drivers
lspci -k
```

### Display Options

```bash
# -t: Tree format
lspci -t

# -tv: Tree format with verbose info
lspci -tv

# -x: Show hex dump of config space
lspci -x

# -xxx: Show full hex dump
lspci -xxx
```

## 필터링 및 선택

### 1. By Device Type {.unnumbered}

```bash
# Graphics cards
lspci | grep -i vga
lspci | grep -i display

# Network adapters
lspci | grep -i network
lspci | grep -i ethernet

# USB controllers
lspci | grep -i usb

# Sound cards
lspci | grep -i audio
lspci | grep -i sound

# Storage controllers
lspci | grep -i storage
lspci | grep -i sata
```

### 2. By Vendor {.unnumbered}

```bash
# Intel devices
lspci | grep -i intel

# AMD devices
lspci | grep -i amd

# NVIDIA devices
lspci | grep -i nvidia

# Broadcom devices
lspci | grep -i broadcom
```

### 3. Specific Device Selection {.unnumbered}

```bash
# Select specific device by bus ID
lspci -s 00:02.0

# Select multiple devices
lspci -s 00:02.0,00:14.0

# Select by vendor ID
lspci -d 8086:

# Select by vendor and device ID
lspci -d 8086:9bc4
```

## 상세 정보

### 1. Verbose Device Information {.unnumbered}

```bash
# Show capabilities and features
lspci -v

# Example output includes:
# - Memory addresses
# - IRQ assignments
# - Capabilities (MSI, Power Management, etc.)
# - Kernel modules/drivers

# Very detailed information
lspci -vv
# Includes:
# - Extended capabilities
# - Configuration registers
# - Link status and speeds
```

### 2. Driver Information {.unnumbered}

```bash
# Show which kernel drivers are in use
lspci -k

# Example output:
# 00:02.0 VGA compatible controller: Intel Corporation Device 9bc4
#         Subsystem: Dell Device 097d
#         Kernel driver in use: i915
#         Kernel modules: i915

# Combine with verbose for more detail
lspci -vk
```

### 3. Configuration Space {.unnumbered}

```bash
# Show configuration space (hex dump)
lspci -x

# Full configuration space
lspci -xxx

# Specific device configuration
lspci -s 00:02.0 -xxx
```

## 트리 보기 및 토폴로지

### 1. Device Hierarchy {.unnumbered}

```bash
# Show device tree structure
lspci -t

# Example output:
# -[0000:00]-+-00.0  Intel Corporation Device 9b61
#            +-01.0-[01]--
#            +-02.0  Intel Corporation Device 9bc4
#            +-14.0  Intel Corporation Device a36d

# Tree with device names
lspci -tv
```

### 2. Bus Information {.unnumbered}

```bash
# Show bridges and connections
lspci -tv | grep -E "(bridge|Bridge)"

# PCI Express information
lspci -vv | grep -A 5 -B 5 "Express"

# Link capabilities and status
lspci -vv | grep -E "(Link|Speed|Width)"
```

## 하드웨어 분석

### 1. Graphics Card Information {.unnumbered}

```bash
# List graphics devices
get_gpu_info() {
    echo "=== Graphics Cards ==="
    lspci | grep -i -E "(vga|display|3d)"
    echo

    echo "=== Detailed GPU Information ==="
    lspci -v | grep -A 10 -B 2 -i -E "(vga|display)"
    echo

    echo "=== GPU Drivers ==="
    lspci -k | grep -A 3 -B 1 -i -E "(vga|display)"
}

get_gpu_info
```

### 2. Network Interface Analysis {.unnumbered}

```bash
# Network adapter details
get_network_info() {
    echo "=== Network Adapters ==="
    lspci | grep -i -E "(network|ethernet|wireless)"
    echo

    echo "=== Network Driver Information ==="
    lspci -k | grep -A 3 -B 1 -i -E "(network|ethernet)"
    echo

    echo "=== Wireless Capabilities ==="
    lspci -vv | grep -A 20 -B 5 -i wireless
}

get_network_info
```

### 3. Storage Controller Information {.unnumbered}

```bash
# Storage device details
get_storage_info() {
    echo "=== Storage Controllers ==="
    lspci | grep -i -E "(storage|sata|ide|scsi|nvme)"
    echo

    echo "=== Storage Driver Information ==="
    lspci -k | grep -A 3 -B 1 -i -E "(storage|sata|ahci)"
    echo

    echo "=== SATA Capabilities ==="
    lspci -vv | grep -A 10 -B 2 -i sata
}

get_storage_info
```

## 시스템 분석 스크립트

### 1. 하드웨어 인벤토리 확인 {.unnumbered}

```bash
#!/bin/bash
# Complete hardware inventory using lspci

hardware_inventory() {
    echo "=== PCI Hardware Inventory ==="
    echo "Generated: $(date)"
    echo

    echo "--- System Overview ---"
    lspci | wc -l | xargs echo "Total PCI devices:"
    echo

    echo "--- Graphics ---"
    lspci | grep -i -E "(vga|display|3d)" || echo "No graphics devices found"
    echo

    echo "--- Network ---"
    lspci | grep -i -E "(network|ethernet|wireless)" || echo "No network devices found"
    echo

    echo "--- Storage ---"
    lspci | grep -i -E "(storage|sata|ide|nvme)" || echo "No storage controllers found"
    echo

    echo "--- Audio ---"
    lspci | grep -i -E "(audio|sound|multimedia)" || echo "No audio devices found"
    echo

    echo "--- USB Controllers ---"
    lspci | grep -i usb || echo "No USB controllers found"
    echo

    echo "--- Other Devices ---"
    lspci | grep -v -i -E "(vga|display|3d|network|ethernet|wireless|storage|sata|ide|nvme|audio|sound|multimedia|usb|bridge|host)" || echo "No other devices"
}

hardware_inventory > hardware_inventory.txt
```

### 2. 드라이버 상태 확인 {.unnumbered}

```bash
#!/bin/bash
# Check driver status for all PCI devices

check_drivers() {
    echo "=== PCI Driver Status ==="

    lspci | while read line; do
        device_id=$(echo "$line" | cut -d' ' -f1)
        device_name=$(echo "$line" | cut -d' ' -f2-)

        driver_info=$(lspci -k -s "$device_id" | grep "Kernel driver in use")

        if [ -n "$driver_info" ]; then
            driver=$(echo "$driver_info" | cut -d':' -f2 | xargs)
            echo "✓ $device_id: $device_name -> $driver"
        else
            echo "✗ $device_id: $device_name -> NO DRIVER"
        fi
    done
}

check_drivers
```

### 3. 성능 분석 {.unnumbered}

```bash
#!/bin/bash
# Analyze PCI device performance capabilities

analyze_performance() {
    echo "=== PCI Performance Analysis ==="

    echo "--- PCIe Link Speeds ---"
    lspci -vv | grep -A 1 -B 1 "Link.*Speed" | grep -E "(:|Speed|Width)"
    echo

    echo "--- Memory Mappings ---"
    lspci -v | grep -E "(Memory at|I/O ports at)" | sort | uniq -c | sort -nr
    echo

    echo "--- Power Management ---"
    lspci -vv | grep -c "Power Management" | xargs echo "Devices with power management:"
    echo

    echo "--- MSI Capabilities ---"
    lspci -vv | grep -c "MSI:" | xargs echo "Devices with MSI support:"
    echo

    echo "--- 64-bit Devices ---"
    lspci -vv | grep -c "64-bit" | xargs echo "64-bit capable devices:"
}

analyze_performance
```

## 문제 해결

### 1. 장치 인식 문제 해결 {.unnumbered}

```bash
# Check if device is detected
check_device_detection() {
    local search_term="$1"

    echo "Searching for: $search_term"

    devices=$(lspci | grep -i "$search_term")
    if [ -n "$devices" ]; then
        echo "✓ Device(s) found:"
        echo "$devices"
        echo

        echo "Driver information:"
        echo "$devices" | while read line; do
            device_id=$(echo "$line" | cut -d' ' -f1)
            lspci -k -s "$device_id" | grep -E "(driver|module)"
        done
    else
        echo "✗ No devices found matching '$search_term'"
        echo "Try checking:"
        echo "  - Physical connections"
        echo "  - BIOS/UEFI settings"
        echo "  - Power supply"
    fi
}

# Usage examples
check_device_detection "graphics"
check_device_detection "network"
```

### 2. 드라이버 문제 해결 {.unnumbered}

```bash
# Find devices without drivers
find_missing_drivers() {
    echo "=== Devices Without Drivers ==="

    lspci | while read line; do
        device_id=$(echo "$line" | cut -d' ' -f1)
        device_name=$(echo "$line" | cut -d' ' -f2-)

        if ! lspci -k -s "$device_id" | grep -q "Kernel driver in use"; then
            echo "Missing driver: $device_id - $device_name"

            # Try to find available modules
            modules=$(lspci -k -s "$device_id" | grep "Kernel modules:" | cut -d':' -f2)
            if [ -n "$modules" ]; then
                echo "  Available modules:$modules"
            fi
        fi
    done
}

find_missing_drivers
```

### 3. 하드웨어 호환성 확인 {.unnumbered}

```bash
# Check hardware compatibility
check_compatibility() {
    echo "=== Hardware Compatibility Check ==="

    echo "--- Unsupported Devices ---"
    lspci -nn | while read line; do
        if echo "$line" | grep -q "\[ffff:"; then
            echo "Possible unsupported device: $line"
        fi
    done
    echo

    echo "--- Legacy Devices ---"
    lspci | grep -i -E "(legacy|isa|parallel|serial|floppy)" || echo "No legacy devices found"
    echo

    echo "--- Vendor Support ---"
    echo "Intel devices: $(lspci | grep -i intel | wc -l)"
    echo "AMD devices: $(lspci | grep -i amd | wc -l)"
    echo "NVIDIA devices: $(lspci | grep -i nvidia | wc -l)"
    echo "Other vendors: $(lspci | grep -v -i -E "(intel|amd|nvidia)" | wc -l)"
}

check_compatibility
```

## 고급 사용법

### 1. PCI 구성 공간(Configuration Space) 분석 {.unnumbered}

```bash
# Analyze specific device configuration
analyze_device_config() {
    local device_id="$1"

    echo "=== Configuration Analysis for $device_id ==="

    echo "--- Basic Information ---"
    lspci -s "$device_id" -v
    echo

    echo "--- Configuration Space ---"
    lspci -s "$device_id" -x
    echo

    echo "--- Capabilities ---"
    lspci -s "$device_id" -vv | grep -A 20 "Capabilities:"
}

# Usage: analyze_device_config "00:02.0"
```

### 2. 링크 대역폭 및 속도 분석 {.unnumbered}

```bash
# Analyze PCIe bandwidth
analyze_bandwidth() {
    echo "=== PCIe Bandwidth Analysis ==="

    lspci -vv | grep -A 2 -B 2 "Express.*Root Port\|Express.*Endpoint" | \
    while read line; do
        if echo "$line" | grep -q "Express"; then
            echo "Device: $line"
        elif echo "$line" | grep -q "Link.*Speed"; then
            echo "  $line"
        fi
    done
}

analyze_bandwidth
```

### 3. 전원 관리 상태 확인 {.unnumbered}

```bash
# Check power management capabilities
check_power_management() {
    echo "=== Power Management Status ==="

    lspci -vv | grep -B 5 -A 10 "Power Management" | \
    grep -E "(^[0-9a-f]{2}:[0-9a-f]{2}\.[0-9]|Power Management|PME)"
}

check_power_management
```

## 다른 도구와 통합

### 1. `lsmod`와 조합하여 드라이버 확인 {.unnumbered}

```bash
# Match PCI devices with loaded modules
match_devices_modules() {
    echo "=== PCI Devices and Kernel Modules ==="

    lspci -k | grep -E "(^[0-9a-f]{2}:|Kernel driver|Kernel modules)" | \
    while read line; do
        if [[ "$line" =~ ^[0-9a-f]{2}: ]]; then
            echo "$line"
        elif [[ "$line" =~ "Kernel driver in use:" ]]; then
            driver=$(echo "$line" | cut -d':' -f2 | xargs)
            echo "  Active driver: $driver"
            lsmod | grep "^$driver" && echo "    ✓ Module loaded" || echo "    ✗ Module not found"
        fi
    done
}
```### 2. udev와 함께 사용하기

```bash
# Check udev rules for PCI devices
check_udev_rules() {
    local device_id="$1"

    echo "Checking udev rules for device: $device_id"

    # Get vendor and device IDs
    vendor_device=$(lspci -n -s "$device_id" | awk '{print $3}')
    vendor_id=$(echo "$vendor_device" | cut -d':' -f1)
    device_id_hex=$(echo "$vendor_device" | cut -d':' -f2)

    echo "Vendor ID: $vendor_id, Device ID: $device_id_hex"

    # Search udev rules
    find /etc/udev/rules.d /lib/udev/rules.d -name "*.rules" -exec grep -l "$vendor_id\|$device_id_hex" {} \; 2>/dev/null
}
```

## 보안 고려 사항

### 1. 하드웨어 보안 {.unnumbered}

```bash
# Check for security-relevant hardware
check_security_hardware() {
    echo "=== Security Hardware Check ==="

    echo "--- TPM Devices ---"
    lspci | grep -i tpm || echo "No TPM devices found"
    echo

    echo "--- Virtualization Support ---"
    lspci -vv | grep -i -E "(vt-x|amd-v|virtualization)" || echo "Check CPU flags for virtualization"
    echo

    echo "--- IOMMU Support ---"
    lspci -vv | grep -i iommu || echo "No explicit IOMMU references found"
    echo

    echo "--- Hardware Security Modules ---"
    lspci | grep -i -E "(security|crypto|hsm)" || echo "No HSM devices found"
}

check_security_hardware
```

## 모범 사례

### 1. 정기적인 하드웨어 모니터링 {.unnumbered}

```bash
# Create hardware monitoring script
#!/bin/bash
# Monitor hardware changes

BASELINE_FILE="/var/lib/hardware_baseline.txt"
CURRENT_FILE="/tmp/current_hardware.txt"

# Generate current state
lspci -nn > "$CURRENT_FILE"

# Compare with baseline
if [ -f "$BASELINE_FILE" ]; then
    if ! diff -q "$BASELINE_FILE" "$CURRENT_FILE" >/dev/null; then
        echo "Hardware configuration changed!"
        echo "Changes:"
        diff "$BASELINE_FILE" "$CURRENT_FILE"
    fi
else
    echo "Creating hardware baseline"
fi

# Update baseline
cp "$CURRENT_FILE" "$BASELINE_FILE"
```

### 2. 문서화 {.unnumbered}

```bash
# Generate hardware documentation
document_hardware() {
    local output_file="hardware_documentation_$(date +%Y%m%d).txt"

    {
        echo "Hardware Documentation"
        echo "Generated: $(date)"
        echo "Hostname: $(hostname)"
        echo "========================="
        echo

        echo "PCI Device Summary:"
        lspci | wc -l | xargs echo "Total devices:"
        echo

        echo "Detailed Device List:"
        lspci -nn
        echo

        echo "Driver Status:"
        lspci -k
        echo

        echo "Device Tree:"
        lspci -tv

    } > "$output_file"

    echo "Documentation saved to: $output_file"
}
```

## 중요 참고 사항

- **루트 권한**: 일부 상세 정보에는 루트 권한이 필요합니다.
- **하드웨어 감지**: PCI 버스에 연결된 장치만 표시합니다.
- **드라이버 상태**: 사용 가능한 모든 드라이버가 아니라 현재 로드된 드라이버를 표시합니다.
- **업데이트**: 장치 정보는 커널에서 읽으므로 하드웨어를 다시 검색해야 할 수 있습니다.
- **공급업체 ID**: 숫자 ID는 표준화되어 있으며 이름은 PCI ID 데이터베이스에서 가져옵니다.
- **트리 보기**: 물리적 버스 토폴로지와 장치 간 관계를 보여 줍니다.

`lspci` 명령은 하드웨어 문제 해결, 드라이버 관리, 시스템 분석에 필수적입니다.

자세한 내용은 매뉴얼을 확인하세요: `man lspci`

# `cfdisk` 명령

`cfdisk` 명령은 Linux용 curses 기반 디스크 파티션 도구입니다. 디스크 파티션을 생성·삭제·관리할 수 있는 사용자 친화적인 텍스트 인터페이스를 제공합니다. `fdisk`와 달리 `cfdisk`는 메뉴 기반으로 더욱 직관적인 작업 방식을 제공합니다.

## 문법

```
cfdisk [options] [device]
```

## 주요 기능

- **대화형 인터페이스**: 메뉴 기반 파티션 관리
- **실시간 표시**: 현재 파티션 테이블 표시
- **안전한 작업**: 변경 사항을 저장하려면 명시적으로 쓰기 명령을 실행해야 함
- **다중 파일 시스템**: 다양한 파티션 유형 지원
- **크기 조정 지원**: 기존 파티션 크기 조정 가능

## 기본 사용법

### cfdisk 시작

```bash
# Open cfdisk on primary disk
sudo cfdisk /dev/sda

# Open cfdisk on secondary disk
sudo cfdisk /dev/sdb

# Auto-detect and use first available disk
sudo cfdisk
```

### 탐색

- **위/아래 화살표**: 파티션 사이를 이동
- **왼쪽/오른쪽 화살표**: 메뉴 옵션 사이를 이동
- **Enter**: 메뉴 옵션 선택
- **Tab**: 인터페이스 요소 사이를 이동

## 메뉴 옵션

### 주요 작업

- **New**: 새 파티션 생성
- **Delete**: 선택한 파티션 삭제
- **Resize**: 선택한 파티션 크기 조정
- **Type**: 파티션 유형 변경
- **Write**: 변경 사항을 디스크에 기록
- **Quit**: 저장하지 않고 종료(변경 사항을 기록하지 않은 경우)

### 추가 옵션

- **Bootable**: 부팅 가능 플래그 전환
- **Verify**: 파티션 테이블 일관성 검사
- **Print**: 파티션 정보 표시

## 일반적인 작업

### 1. 새 파티션 생성 {.unnumbered}

```bash
# Start cfdisk
sudo cfdisk /dev/sdb

# Steps in cfdisk:
# 1. Select "New" from menu
# 2. Choose partition size
# 3. Select partition type (primary/extended)
# 4. Choose partition type (Linux, swap, etc.)
# 5. Select "Write" to save changes
# 6. Type "yes" to confirm
# 7. Select "Quit" to exit
```

### 2. 파티션 삭제 {.unnumbered}

```bash
# In cfdisk interface:
# 1. Navigate to partition to delete
# 2. Select "Delete" from menu
# 3. Confirm deletion
# 4. Select "Write" to save changes
# 5. Type "yes" to confirm
```

### 3. 파티션 유형 변경 {.unnumbered}

```bash
# In cfdisk interface:
# 1. Navigate to target partition
# 2. Select "Type" from menu
# 3. Choose new partition type from list
# 4. Select "Write" to save changes
```

## 일반적인 파티션 유형

### Linux 파티션 유형

- **83**: Linux 파일 시스템
- **82**: Linux 스왑
- **8e**: Linux LVM
- **fd**: Linux RAID 자동 감지

### 기타 일반적인 유형

- **07**: NTFS/HPFS
- **0c**: FAT32 LBA
- **ef**: EFI 시스템 파티션
- **01**: FAT12

## 명령줄 옵션

```bash
# Show help
cfdisk --help

# Show version
cfdisk --version

# Use alternative device
cfdisk /dev/sdc

# Start with specific partition table type
cfdisk -t gpt /dev/sdb
cfdisk -t dos /dev/sdb
```

## 실용적인 예

### 1. 새 USB 드라이브 파티션 나누기 {.unnumbered}

```bash
# Insert USB drive (assume it's /dev/sdc)
# Check device name
lsblk

# Start cfdisk
sudo cfdisk /dev/sdc

# Create new partition table if needed
# Create partitions as needed
# Write changes and quit
```

### 2. 스왑 파티션 추가 {.unnumbered}

```bash
# Start cfdisk on target disk
sudo cfdisk /dev/sda

# Create new partition
# Set type to "82" (Linux swap)
# Write changes

# Format as swap
sudo mkswap /dev/sda3

# Enable swap
sudo swapon /dev/sda3
```

### 3. LVM용 디스크 준비 {.unnumbered}

```bash
# Start cfdisk
sudo cfdisk /dev/sdb

# Create partition
# Set type to "8e" (Linux LVM)
# Write changes

# Create physical volume
sudo pvcreate /dev/sdb1
```

## 안전 기능

### 1. 변경 사항 추적 {.unnumbered}

```bash
# cfdisk tracks all changes
# Shows asterisk (*) next to modified partitions
# Changes only applied when "Write" is selected
```

### 2. 확인 필요 {.unnumbered}

```bash
# Writing changes requires typing "yes"
# Provides final warning before applying changes
# Can quit without saving if no "Write" performed
```

### 3. 검증 {.unnumbered}

```bash
# Built-in partition table verification
# Warns about potential issues
# Suggests corrections for problems
```

## 다양한 파티션 테이블 사용

### 1. GPT(GUID 파티션 테이블) {.unnumbered}

```bash
# Create GPT partition table
sudo cfdisk -t gpt /dev/sdb

# Features:
# - Supports >2TB disks
# - Up to 128 partitions
# - Backup partition table
# - 64-bit LBA addressing
```

### 2. MBR/DOS 파티션 테이블 {.unnumbered}

```bash
# Create MBR partition table
sudo cfdisk -t dos /dev/sdb

# Limitations:
# - 4 primary partitions maximum
# - 2TB disk size limit
# - Extended partitions for >4 partitions
```

## 다른 도구와 연동

### 1. 파티션 작업 후 {.unnumbered}

```bash
# Verify partition creation
lsblk /dev/sdb
fdisk -l /dev/sdb

# Format partitions
sudo mkfs.ext4 /dev/sdb1
sudo mkfs.xfs /dev/sdb2

# Mount partitions
sudo mkdir /mnt/partition1
sudo mount /dev/sdb1 /mnt/partition1
```

### 2. 변경 전 백업 {.unnumbered}

```bash
# Backup partition table before changes
sudo sfdisk -d /dev/sdb > sdb_partition_backup.txt

# Restore if needed
sudo sfdisk /dev/sdb < sdb_partition_backup.txt
```

## 문제 해결

### 1. 권한 문제 {.unnumbered}

```bash
# Must run as root or with sudo
sudo cfdisk /dev/sdb

# Check device permissions
ls -l /dev/sdb
```

### 2. 장치 사용 중 {.unnumbered}

```bash
# Unmount all partitions on device first
sudo umount /dev/sdb1
sudo umount /dev/sdb2

# Check for active processes
lsof /dev/sdb*
```

### 3. 파티션 테이블 손상 {.unnumbered}

```bash
# Verify partition table
sudo cfdisk /dev/sdb

# If corrupted, recreate partition table
# (This will destroy all data!)
sudo cfdisk -t gpt /dev/sdb  # For GPT
sudo cfdisk -t dos /dev/sdb  # For MBR
```

## 모범 사례

### 1. 항상 백업하기 {.unnumbered}

```bash
# Backup important data before partitioning
# Create partition table backup
sudo sfdisk -d /dev/sdb > partition_backup.txt
```

### 2. 장치 확인 {.unnumbered}

```bash
# Double-check device name before starting
lsblk
fdisk -l

# Ensure you're working on correct disk
```

### 3. 파티션 레이아웃 계획 {.unnumbered}

```bash
# Plan your partition scheme:
# - Root partition (/)
# - Swap partition
# - Home partition (/home)
# - Boot partition (/boot) if needed
```

### 4. 정렬 고려 {.unnumbered}

```bash
# Modern cfdisk handles alignment automatically
# Uses 1MB alignment by default
# Optimal for SSDs and advanced format drives
```

## 다른 도구와 비교

### fdisk와 비교

- **cfdisk**: 메뉴 기반, 사용자 친화적
- **fdisk**: 명령 기반, 스크립트 작성에 더 적합

### parted와 비교

- **cfdisk**: 더 단순한 인터페이스, 기본 작업 중심
- **parted**: 더 고급 기능, 명령줄 스크립트 작성 가능

### gparted와 비교

- **cfdisk**: 텍스트 기반, 가벼움
- **gparted**: 그래픽 인터페이스, X11 필요

## 중요 참고 사항

- 수정하기 전에 항상 파티션을 마운트 해제하세요.
- 명시적으로 **Write**를 선택하기 전까지 변경 사항은 기록되지 않습니다.
- 파티션을 변경하기 전에 중요한 데이터를 백업하세요.
- 일부 작업은 적용을 위해 시스템 재부팅이 필요할 수 있습니다.
- 시스템 디스크를 다룰 때는 각별히 주의하세요.
- 더 유연한 파티션 관리를 위해 LVM 사용을 고려하세요.

`cfdisk` 명령은 디스크 파티션 작업에서 편의성과 기능 사이의 균형이 뛰어납니다.

자세한 내용은 매뉴얼을 확인하세요: `man cfdisk`

# ifplugstatus 명령

`ifplugstatus` 명령은 Linux 시스템에서 네트워크 인터페이스의 **물리적 링크 상태**를 확인하는 진단 유틸리티입니다. 인터페이스(예: eth0, enp0s3, wlan0)에 네트워크 케이블이 연결되어 있는지 보고합니다. 유선 네트워크 연결 문제를 해결하고 케이블이 빠졌는지 감지할 때 특히 유용합니다.

## 문법

```bash
ifplugstatus [options] [interface]
```

### 매개변수

- interface 확인할 네트워크 인터페이스(예: eth0, enp0s3, wlan0)

- options 출력을 사용자 지정하는 선택적 플래그

## 설치

`ifplugstatus`는 `ifplugd` 패키지에 포함되어 있습니다.
배포판에 따라 다음 명령으로 설치할 수 있습니다.

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install ifplugd

# CentOS/RHEL/Fedora
sudo yum install ifplugd
# or
sudo dnf install ifplugd
```

### 설치 확인

```bash
which ifplugstatus
```

## 기본 사용법

### 1. 단일 인터페이스 상태 확인 {.unnumbered}

```bash
ifplugstatus eth0
```

### 예시 출력

```bash
eth0: link beat detected
```

### 또는

```bash
eth0: link beat not detected
```

### 설명

- link beat detected → 케이블이 연결되어 있고 활성 상태입니다.

- link beat not detected → 케이블이 연결되지 않았거나 비활성 상태입니다.
*참고: 'link beat'는 과거 이더넷(10/100Mbps)에서 사용하던 용어입니다. 최신 기가비트 이상 인터페이스는 다른 링크 감지 신호를 사용할 수 있지만 이 명령은 일반적으로 물리적 링크의 활성 상태를 보고합니다.*

### 2. 여러 인터페이스 확인 {.unnumbered}

```bash
ifplugstatus eth0 wlan0
```

### 예시 출력

```bash
eth0: link beat detected
wlan0: link beat not detected
```

이를 통해 사용 가능한 모든 인터페이스를 빠르게 확인할 수 있습니다.

## 옵션

자주 사용하는 옵션 플래그는 다음과 같습니다.

| 옵션 | 설명 |
|--------|-------------|
| `-a`   | 모든 인터페이스 표시(기본 동작) |
| `-i`   | 특정 인터페이스 지정 |
| `-s`   | 짧은 요약만 표시 |
| `-v`   | 더 자세한 정보를 포함한 상세 출력 |
| `-q`   | 조용한 모드 최소한의 출력 |
| `-h`   | 도움말 정보 표시 |

## 실용적인 예

### 1. 주 이더넷 인터페이스 연결 여부 확인 {.unnumbered}

```bash
ifplugstatus eth0
```

eth0의 현재 케이블 연결 상태를 표시합니다.

### 2. 사용 가능한 모든 네트워크 인터페이스 확인 {.unnumbered}

```bash
ifplugstatus -a
```

감지된 모든 인터페이스의 링크 상태를 나열합니다.

### 3. 상세 결과를 위한 verbose 모드 사용 {.unnumbered}

```bash
ifplugstatus -v eth0
```

링크 상태에 관한 더 자세한 정보를 제공합니다.

### 4. 스크립트 또는 자동화를 위한 quiet 모드 {.unnumbered}

```bash
ifplugstatus -q eth0
```

셸 스크립트에 적합하도록 핵심 정보만 출력합니다.

## 출력 이해하기

일반적인 출력은 다음과 같습니다.

```bash
eth0: link beat detected
enp0s3: link beat not detected
```

- 인터페이스 이름: eth0, enp0s3 등

- 링크 상태: 물리적 링크(케이블 또는 신호)의 존재 여부를 나타냅니다.

스크립트에 통합하면 연결이 끊긴 인터페이스나 케이블 결함을 자동으로 감지할 수 있습니다.

## 일반적인 사용 사례

### 1. 케이블 연결 테스트 {.unnumbered}

```bash
ifplugstatus eth0
```

네트워크 포트에 케이블이 물리적으로 연결되어 있는지 빠르게 확인합니다.

### 2. 여러 인터페이스 모니터링 {.unnumbered}

```bash
ifplugstatus eth0 wlan0
```

여러 인터페이스의 링크 활동을 동시에 확인합니다.

### 3. 지속적인 모니터링 {.unnumbered}

```bash
watch -n 5 ifplugstatus eth0
```

실시간 링크 모니터링을 위해 5초마다 인터페이스 상태를 갱신합니다.

## 일반적인 문제 해결

### 1. 인터페이스를 찾을 수 없음 {.unnumbered}

잘못된 인터페이스 이름을 지정하면 다음과 같이 표시됩니다.

```bash
eth5: No such device
```

**해결:** `ip a` 또는 `ifconfig` 명령으로 올바른 인터페이스 목록을 확인하세요.

### 2. 권한 거부 {.unnumbered}

일부 시스템에서는 높은 권한이 필요할 수 있습니다.

```bash
sudo ifplugstatus eth0
```

### 3. 명령을 찾을 수 없음 {.unnumbered}

명령이 없으면 다음과 같이 표시됩니다.

```bash
 ifplugstatus: command not found
```

**해결:** 패키지 관리자를 사용해 `ifplugd` 패키지를 설치하세요.

**팁:** 항상 `ifplugd` 패키지가 설치되어 있고 사용자에게 네트워크 인터페이스에 접근할 권한이 있는지 확인하세요.

## 관련 명령

| 명령 | 설명 |
|-----------------------|-------------------------------------------------------|
| `ip a` (권장) / `ifconfig` | 모든 네트워크 인터페이스와 세부 정보를 표시합니다. `ip a`는 최신 도구이고 `ifconfig`는 레거시 도구입니다. |
| `ethtool eth0`        | 네트워크 인터페이스의 고급 정보를 가져옵니다. |
| `nmcli device status` | NetworkManager를 통해 연결 상태를 확인합니다. |
| `ping`                | 케이블 연결을 확인한 후 네트워크 도달 가능성을 테스트합니다. |

## 중요 참고 사항

- 유선 이더넷 인터페이스에서 가장 잘 작동합니다.
- 무선 인터페이스는 링크 상태를 항상 정확하게 보고하지 않을 수 있습니다.
- 읽기 전용 도구이므로 시스템 설정을 변경하지 않습니다.
- 데스크톱과 서버 환경 모두에서 안전하게 사용할 수 있는 가벼운 도구입니다.

## 매뉴얼 참조

자세한 정보와 고급 사용법은 다음을 확인하세요.

```bash
man ifplugstatus
```

# `column` 명령

`column` 명령은 입력을 여러 열로 정렬하는 데 사용합니다. 텍스트 기반 표를 만들고 명령 출력의 가독성을 높일 때 특히 유용합니다.

## 명령 문법

```bash
column [options] [file]...
```

## 명령 옵션

- `-t`: 열 수를 자동으로 결정하고 표 생성
- `-s`: 열 구분 기호 지정(기본값은 공백)
- `-n`: 연속된 여러 구분 기호를 병합하지 않음
- `-c`: 지정한 너비로 열 형식 출력
- `-x`: 행보다 열을 먼저 채움
- `-L`: 모든 항목을 왼쪽 정렬
- `-R`: 모든 항목을 오른쪽 정렬
- `-o`: 표 출력에 사용할 열 구분 기호 지정

## 예제

1. 기본 열 형식 지정

```bash
# Create a simple list and format it into columns
ls | column
```

2. 구분된 데이터로 표 만들기

```bash
# Format /etc/passwd entries into a neat table
cut -d: -f1,6,7 /etc/passwd | column -t -s:
```

3. 명령 출력 형식 지정

```bash
# Display mount information in a clean tabular format
mount | column -t
```

4. 사용자 지정 열 구분 기호

```bash
# Format CSV data with custom separator
echo -e "Name,Age,City\nJohn,25,NYC\nJane,30,LA" | column -t -s,

Name  Age  City
John  25   NYC
Jane  30   LA

```

5. 왼쪽 정렬 표

```bash
# Create a left-aligned table from space-separated data
ps aux | head -n 5 | column -t -L
```

> **참고:** `-L` 및 `-R`과 같은 옵션은 모든 Linux 배포판에서 제공되지 않을 수 있습니다(주로 GNU `column`에서 지원).

# 파일을 열 형식으로 정렬하기(예: data.txt)

```
column -t -s, data.txt
```

## 추가 정보

- `column` 명령은 `util-linux` 패키지에 포함되어 있습니다.
- 셸 스크립트에서 출력을 형식 지정할 때 특히 유용합니다.
- 파일 입력과 표준 입력(stdin)을 모두 처리할 수 있습니다.
- `cut`, `sort`, `grep`과 같은 다른 텍스트 처리 명령과 잘 연동됩니다.

## 함께 보기

- `cut` - 파일에서 구간 제거
- `sort` - 텍스트 파일의 줄 정렬
- `paste` - 파일의 줄 병합
- `tr` - 문자 변환 또는 삭제

## 추가 읽을거리

- `man column` - column 명령 매뉴얼 페이지
- [GNU Coreutils](https://www.gnu.org/software/coreutils/)

# `nmtui` 명령

**`nmtui`**(Network Manager Text User Interface) 명령은 Linux에서 네트워크 연결을 구성하는 **메뉴 기반 도구**입니다. 복잡한 명령줄 옵션을 사용하지 않고 Wi-Fi, 이더넷, 호스트 이름 등의 네트워크 설정을 관리할 수 있는 간단한 텍스트 인터페이스를 제공합니다.

### 🔹 기능

`nmtui`로 다음 작업을 수행할 수 있습니다.

- **네트워크 연결** 보기 및 편집
- 인터페이스 **활성화 또는 비활성화**
- 시스템 **호스트 이름** 설정 또는 변경
- **Wi-Fi 네트워크** 연결
- **IPv4 / IPv6 설정** 관리

특히 그래픽 데스크톱 환경이 없는 서버나 시스템에서 유용합니다.

### 🧠 문법

`nmtui [OPTION]`

### 🖥️ 예제

1. **기본 메뉴 인터페이스 열기:**

   `nmtui`  
   *연결 편집*, *연결 활성화*, *시스템 호스트 이름 설정* 옵션이 있는 기본 TUI(텍스트 사용자 인터페이스) 창을 엽니다.

2. **네트워크 연결 직접 편집:**

   `nmtui edit`  
   이더넷과 Wi-Fi 네트워크 연결을 생성·수정·삭제할 수 있습니다.

3. **연결 직접 활성화 또는 비활성화:**

   `nmtui connect`  
   네트워크 인터페이스를 활성화하거나 비활성화할 수 있는 *연결 활성화* 메뉴를 엽니다.

4. **시스템 호스트 이름 설정 또는 변경:**

   `nmtui hostname`  
   네트워크에서 시스템을 식별할 때 사용하는 호스트 이름을 설정하는 대화 상자를 엽니다.

5. **특정 Wi-Fi 네트워크에 직접 연결:**

   `nmtui connect "MyWiFiNetwork"`  
   지정한 Wi-Fi 네트워크가 통신 범위 안에 있으면 연결합니다.

6. **인터페이스 종료:**  
   **Tab → Quit**을 사용하거나 **Esc**를 누릅니다.


### 🧩 일반 옵션 및 하위 명령

| **명령** | **설명** |
|:-------------|:----------------|
| `nmtui` | 대화형 기본 메뉴 실행 |
| `nmtui edit` | “연결 편집” 화면 열기 |
| `nmtui connect` | “연결 활성화” 화면 열기 |
| `nmtui hostname` | “시스템 호스트 이름 설정” 대화 상자 열기 |
| `nmtui connect <SSID>` | 특정 Wi-Fi 네트워크에 직접 연결 |
| `nmtui help` | 기본 사용법 도움말 표시 |


### ⚙️ 인터페이스 탐색

- **화살표 키** 또는 **Tab**으로 필드 사이를 이동합니다.
- **Enter**를 눌러 선택합니다.
- **스페이스바**로 확인란이나 옵션을 전환합니다.
- **Esc**를 누르거나 **Quit**을 선택해 안전하게 종료합니다.


### 📡 일반적인 사용 사례

**1. 고정 IP 주소 구성:**

`nmtui edit ` 
→ 이더넷 연결 선택  
→ “IPv4 CONFIGURATION”을 *Manual*로 설정  
→ IP, 게이트웨이, DNS 입력  
→ 저장 → 연결 활성화

**2. 터미널에서 Wi-Fi에 연결:**

`nmtui connect`  
→ 무선 인터페이스 선택  
→ SSID 선택  
→ 비밀번호 입력  
→ 연결 활성화

**3. 호스트 이름 변경:**

`nmtui hostname`  
→ 새 호스트 이름 입력  
→ 확인 후 종료  
→ 새 호스트 이름이 즉시 적용됩니다.


### 🛠 문제 해결

| **문제** | **가능한 해결 방법** |
|:-----------|:----------------------|
| `nmtui` command not found | NetworkManager TUI 설치: `sudo apt install network-manager`(Debian/Ubuntu) 또는 `sudo dnf install NetworkManager-tui`(Fedora/RHEL/CentOS) |
| 변경 사항이 적용되지 않음 | NetworkManager 재시작: `sudo systemctl restart NetworkManager` |
| 인터페이스가 표시되지 않음 | 인터페이스가 NetworkManager에서 관리되는지 확인: `/etc/NetworkManager/NetworkManager.conf` 검사 |
| Wi-Fi가 없음 | 무선 드라이버가 설치되어 있고 `nmcli device status`에 Wi-Fi 어댑터가 표시되는지 확인 |


### 🧰 관련 명령

| **명령** | **용도** |
|:-------------|:------------|
| `nmcli` | 명령줄(비대화형) NetworkManager 제어 |
| `ip addr` | 모든 인터페이스의 IP 주소 표시 |
| `ifconfig` | (사용 중단됨) 인터페이스 구성 명령 |
| `systemctl restart NetworkManager` | NetworkManager 서비스 재시작 |
| `hostnamectl` | 시스템 호스트 이름 직접 관리 |


### 🧾 예제: `nmtui`로 이더넷 구성

`sudo nmtui` 
→ **연결 편집** 선택  
→ **Wired connection 1** 선택  
→ IPv4를 **Manual**로 설정  
→ 다음 값 입력:
Address: 192.168.1.50/24  
Gateway: 192.168.1.1  
DNS: 8.8.8.8  
→ 저장 → 뒤로 → **연결 활성화** → **Wired connection 1** 선택

✅ 이제 네트워크 연결에서 고정 IP를 사용합니다.

### 📘 참고

- `nmtui`로 변경한 설정은 재부팅 후에도 유지됩니다.
- **NetworkManager**가 활성 상태여야 합니다.
- `nmtui`는 `nmcli`와 동일한 기능을 더 사용자 친화적인 인터페이스로 제공합니다.
- GUI 도구를 사용할 수 없는 **서버 환경**이나 **SSH 세션**에 적합합니다.

# 맺음말

축하합니다! **101 Linux Commands eBook**의 마지막에 도달했습니다. 이 여정을 통해 시스템 관리, 개발, 일상적인 Linux 사용의 기반이 되는 135개 이상의 필수 Linux 명령을 살펴보았습니다.

## 배운 내용

이 eBook에서는 다음을 비롯한 폭넓은 주제를 다뤘습니다.

### 🗂️ **파일 및 디렉터리 관리**

- 탐색 명령(`cd`, `ls`, `pwd`)
- 파일 조작(`cp`, `mv`, `rm`, `mkdir`)
- 콘텐츠 보기(`cat`, `head`, `tail`, `less`)
- 검색 및 찾기 작업(`find`, `grep`, `locate`)

### 🔧 **시스템 관리**

- 프로세스 관리(`ps`, `kill`, `top`, `htop`)
- 사용자 및 그룹 관리(`useradd`, `usermod`, `chmod`, `chown`)
- 시스템 모니터링(`df`, `du`, `free`, `vmstat`)
- 서비스 관리(`systemctl`, `service`)

### 🌐 **네트워킹 및 통신**

- 네트워크 구성(`ip`, `ifconfig`, `netstat`)
- 원격 액세스(`ssh`, `scp`, `rsync`)
- 네트워크 진단(`ping`, `dig`, `whois`)

### 📦 **패키지 관리 및 아카이브**

- 패키지 관리자(`apt`, `yum`, `rpm`)
- 압축 도구(`tar`, `gzip`, `zip`)
- 아카이브 조작

### ⚙️ **텍스트 처리 및 자동화**

- 텍스트 편집기(`vim`, `nano`)
- 텍스트 처리(`awk`, `sed`, `cut`, `sort`)
- 작업 예약(`crontab`)
- 셸 스크립팅 도우미(`xargs`, `nohup`)

## Linux 여정은 계속됩니다

이 명령들을 익히는 것은 Linux 여정의 시작일 뿐입니다. 학습을 이어가기 위한 몇 가지 권장 사항은 다음과 같습니다.

### 📚 **다음 단계**

1. **정기적으로 연습하기**: Linux 명령을 익히는 가장 좋은 방법은 꾸준히 연습하는 것입니다.
2. **시스템 관리 탐구하기**: 서버 관리, 보안, 배포를 학습하세요.
3. **스크립팅에 깊이 들어가기**: 반복 작업을 자동화하는 bash 스크립트를 작성해 보세요.
4. **버전 관리 배우기**: 코드 관리와 협업을 위해 Git을 익히세요.
5. **컨테이너화 탐구하기**: 현대적인 애플리케이션 배포를 위해 Docker와 Kubernetes를 학습하세요.

### 🛠️ **탐구할 고급 주제**

- **셸 스크립팅**: 복잡한 자동화 스크립트 작성
- **시스템 보안**: 방화벽, SSL 인증서, 보안 강화 학습
- **성능 튜닝**: 시스템 성능 최적화 및 문제 해결
- **DevOps 도구**: CI/CD 파이프라인, 구성 관리, 코드형 인프라 탐구
- **클라우드 컴퓨팅**: AWS, Azure, Google Cloud 및 클라우드 네이티브 기술 학습

### 📖 **추가 자료**

- **Man 페이지**: `man command`로 모든 명령에 대한 상세 정보 확인
- **Linux Documentation Project**: 종합 가이드 및 HOWTO
- **온라인 커뮤니티**: 포럼, Discord 서버, Reddit 커뮤니티 참여
- **실습 환경**: 가상 머신을 설정하거나 클라우드 플랫폼을 사용해 직접 실습

## 마지막으로

Linux는 단순한 운영 체제 그 이상입니다. 개방적인 협업, 지속적인 학습, 기술을 통한 역량 강화를 지향하는 철학이기도 합니다. 이 eBook에서 배운 명령은 기술 분야의 경력 전반에 걸쳐 도움이 될 도구입니다.

서버를 관리하는 시스템 관리자든, 애플리케이션을 만드는 개발자든, 배포를 자동화하는 DevOps 엔지니어든, 기술을 탐구하는 호기심 많은 학습자든, 이 Linux 명령은 여정에서 귀중한 동반자가 될 것입니다.

기억하세요. **터미널은 여러분의 친구입니다.** 실험하고, 실수하고, 그 과정에서 배우는 것을 두려워하지 마세요. 모든 Linux 전문가는 지금 여러분이 있는 바로 그 지점에서 시작했습니다.

### 🚀 **계속 배우고, 계속 성장하세요**

Linux의 세계는 광대하며 끊임없이 발전합니다. 호기심을 유지하고, 계속 연습하며 무엇보다 Linux가 제공하는 놀라운 힘과 유연성을 탐구하는 과정을 즐기세요.

## 라이선스

```
MIT License

Copyright (c) 2020 Bobby Iliev

이 소프트웨어 사본을 취득한 모든 사람에게 다음 조건에 따라 소프트웨어를 제한 없이 사용, 복사, 수정, 병합, 게시, 배포, 재허가 및 판매할 수 있는 권한을 무료로 부여합니다.
이 소프트웨어 및 관련 문서 파일(이하 "소프트웨어")을 제한 없이 취급할 수 있으며 여기에는 소프트웨어 사본을 사용, 복사, 수정, 병합, 게시, 배포, 재허가 및 판매할 권리가 포함되지만 이에 한정되지 않습니다. 또한 소프트웨어를 제공받은 사람에게도 다음 조건에 따라 이를 허용할 수 있습니다.

위 저작권 고지와 이 허가 고지는 소프트웨어의 모든 사본 또는 상당 부분에 포함되어야 합니다.

소프트웨어는 상품성, 특정 목적에의 적합성 및 비침해성에 대한 보증을 포함하되 이에 한정되지 않는 어떠한 종류의 명시적 또는 묵시적 보증도 없이 "있는 그대로" 제공됩니다. 어떠한 경우에도 저자 또는 저작권자는 소프트웨어 또는 소프트웨어의 사용이나 기타 거래와 관련하여 발생하는 모든 청구, 손해 또는 기타 책임에 대해 계약, 불법행위 또는 기타 사유를 불문하고 책임을 지지 않습니다.
```
